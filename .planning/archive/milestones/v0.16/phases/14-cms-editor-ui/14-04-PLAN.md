---
phase: 14-cms-editor-ui
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - app/(dashboard)/web/_sections/theme-controls.tsx
  - test/landing-theme.test.ts
autonomous: true
requirements: [EDIT-04]
must_haves:
  truths:
    - "El dueño elige un preset de tema (Forjo/Modern/Spa/Cyber) y la paleta se resetea al default del preset"
    - "El dueño elige una paleta del set del preset y ajusta el color primario por un input de color validado"
    - "Un primary invalido muestra error inline y NO se escribe (isSafeColor lo rechaza)"
    - "El dueño elige el nivel de motion (Sin movimiento / Sutil / Premium)"
    - "Todo se escribe en landing_config.theme (preset + overrides.palette/primary) y config.motion — NO en businesses.theme/palette/font — y se ve aplicado en el preview"
    - "Con un config null-sembrado (preset 'default'), el swatch activo resalta Forjo (default degrada a forjo via normalizeTheme)"
  artifacts:
    - path: "app/(dashboard)/web/_sections/theme-controls.tsx"
      provides: "Grids de preset y paleta (espejo estructural de settings-client), input de primary validado, segmented de motion — todo escribiendo landing_config.theme/motion"
      min_lines: 90
    - path: "test/landing-theme.test.ts"
      provides: "Extension: active-preset normalization (default→forjo) para el swatch del editor"
      contains: "normalizeTheme"
  key_links:
    - from: "app/(dashboard)/web/_sections/theme-controls.tsx"
      to: "lib/theme-config.ts"
      via: "THEMES/THEME_PALETTES/THEME_DEFAULT_PAL alimentan los grids; normalizeTheme para el active-match"
      pattern: "THEME_PALETTES|THEME_DEFAULT_PAL"
    - from: "app/(dashboard)/web/_sections/theme-controls.tsx"
      to: "lib/landing/theme.ts"
      via: "isSafeColor valida el primary antes de escribirlo a overrides.primary"
      pattern: "isSafeColor"
    - from: "app/(dashboard)/web/_sections/theme-controls.tsx"
      to: "lib/landing/editor-draft.ts"
      via: "onChange/onMotionChange invocan setTheme/setMotion (mutadores puros) sobre el borrador"
      pattern: "setTheme|setMotion"
---

<objective>
Implementar los controles de tema/paleta/primary/motion del editor (EDIT-04). Espejar ESTRUCTURALMENTE el patron de swatches de `settings-client.tsx` pero escribiendo a `landing_config.theme` (`preset` + `overrides.palette` + `overrides.primary`) y `config.motion` — NO a las columnas de chrome del dashboard (`businesses.theme/palette/font`). El resultado se ve aplicado en vivo en el preview del shell via `resolveLandingTheme` (que el shell ya aplica al wrapper del preview). Reemplaza el stub `theme-controls.tsx` de 14-01.

Purpose: EDIT-04 es un Success Criterio. El D-06 es claro: reusar el PATRON de UI, cambiar el TARGET. El primary es un color crudo → barrera `isSafeColor` (anti CSS-injection, T-08-01). El default 'default' degrada a forjo en el active-match (landmine L8).
Output: `theme-controls.tsx` (grids preset/paleta + input primary validado + segmented motion), y una extension del test de theme para la normalizacion del active-preset.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-builder/phases/14-cms-editor-ui/14-CONTEXT.md
@.planning/workstreams/web-builder/phases/14-cms-editor-ui/14-RESEARCH.md
@.planning/workstreams/web-builder/phases/14-cms-editor-ui/14-UI-SPEC.md
@.planning/workstreams/web-builder/phases/14-cms-editor-ui/14-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `app/(dashboard)/web/_sections/theme-controls.tsx` — named export `ThemeControls` (`'use client'`; implementacion completa; reemplaza el stub de 14-01).
- `test/landing-theme.test.ts` — extendido con casos de active-preset normalization para el editor (no crea archivo nuevo).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extender el test de theme con la normalizacion del active-preset (L8)</name>
  <files>test/landing-theme.test.ts</files>
  <read_first>
    - test/landing-theme.test.ts (el archivo actual: describe/it/expect, import de isSafeColor/resolveLandingTheme/normalizeMotion; agregar casos, no reescribir)
    - lib/theme-config.ts (normalizeTheme: 'default'/desconocido → 'forjo'; THEME_DEFAULT_PAL)
    - lib/landing/schema.ts (DEFAULT_LANDING_CONFIG.theme.preset === 'default' — la razon del caso)
    - .planning/workstreams/web-builder/phases/14-cms-editor-ui/14-RESEARCH.md (L8: 'default' degrada a forjo; el swatch activo debe pasar por normalizeTheme)
  </read_first>
  <action>Agregar (sin reescribir el archivo) un bloque `describe` a `test/landing-theme.test.ts` que cubra la normalizacion del preset activo tal como la usara el grid del editor: `normalizeTheme('default')` === 'forjo' (config null-sembrado resalta Forjo, no nada); `normalizeTheme(undefined)` === 'forjo'; `normalizeTheme('modern')` === 'modern' (preset conocido se preserva); y que `THEME_DEFAULT_PAL[normalizeTheme('default')]` da la paleta default de forjo ('red'). Importar `normalizeTheme` y `THEME_DEFAULT_PAL` de `@/lib/theme-config`. Mantener el estilo del archivo (environment node, sin creds). No poner literales de assertions negativas como comentario en el fuente.</action>
  <verify>
    <automated>npx vitest run test/landing-theme.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/landing-theme.test.ts` pasa (casos previos + nuevos).
    - Cubre `normalizeTheme('default') === 'forjo'` y el mapeo a la paleta default.
    - No se rompen los tests existentes del archivo.
  </acceptance_criteria>
  <done>El active-match del preset del editor (default→forjo) queda cubierto por unit test, cerrando el landmine L8 antes de construir el grid.</done>
</task>

<task type="auto">
  <name>Task 2: Controles de tema/paleta/primary/motion apuntados a landing_config.theme</name>
  <files>app/(dashboard)/web/_sections/theme-controls.tsx</files>
  <read_first>
    - app/(dashboard)/web/web-client.tsx (props-contract del stub: `{ theme, onChange, motion, onMotionChange }`; como el shell aplica resolveLandingTheme al preview)
    - app/(dashboard)/settings/settings-client.tsx (lineas 852-967: los grids THEMES.map / THEME_PALETTES[theme].map — anatomia de swatch, active state `border-primary ring-2 ring-primary/20` + Check badge, boton aria-pressed focus-visible; lineas 977-999: segmented para el motion)
    - lib/theme-config.ts (THEMES, THEME_PALETTES, THEME_DEFAULT_PAL, normalizeTheme — la fuente de presets/paletas; seleccionar preset resetea paleta a THEME_DEFAULT_PAL)
    - lib/landing/theme.ts (isSafeColor — allowlist de hex para el primary; resolveLandingTheme para entender que shape produce)
    - lib/landing/editor-draft.ts (setTheme/setMotion — como se aplica al borrador)
    - lib/landing/schema.ts (LandingTheme = { preset: string; overrides?: Record<string,string> }; config.motion enum)
    - .planning/workstreams/web-builder/phases/14-cms-editor-ui/14-UI-SPEC.md (§5 Theme/palette/primary/motion controls; Copywriting Theme/motion: "Estilo visual"/"Paleta de color"/"Color principal"/"Nivel de movimiento"/"Sin movimiento · Sutil · Premium"/primary invalid hint)
    - .planning/workstreams/web-builder/phases/14-cms-editor-ui/14-RESEARCH.md (Focus 4: escribir a landing_config.theme NO businesses.*; primary por input color + hex validado; motion segmented; L8 active-match via normalizeTheme; L6 el shell aplica el tema al wrapper)
  </read_first>
  <action>Reemplazar el stub `theme-controls.tsx` (`'use client'`) con el named export `ThemeControls` (props `{ theme: LandingTheme, onChange(next: LandingTheme|partial), motion: 'none'|'subtle'|'premium'|undefined, onMotionChange(level) }`). Reusar ESTRUCTURALMENTE los grids de settings (852-967): (1) PRESET grid — `THEMES.map` con el mini-preview bg/fg/chips; el active-match compara `normalizeTheme(theme.preset)` contra `t.id` (L8: 'default'→forjo resalta Forjo); seleccionar un preset escribe `theme.preset` Y resetea `overrides.palette` al `THEME_DEFAULT_PAL[preset]` (mismo comportamiento que `selectTheme` de settings). (2) PALETA grid — `THEME_PALETTES[normalizeTheme(theme.preset)].map` de swatch cards; seleccionar escribe `theme.overrides.palette`. (3) PRIMARY — `<input type="color">` nativo con `Label` visible ("Color principal") + un campo de texto hex; validar con `isSafeColor` de `@/lib/landing/theme`; un hex invalido muestra error inline ("Ingresá un color válido (ej. #d94a2b)") y NO escribe `overrides.primary` (el renderer igual lo dropea, pero no lo persistimos). Un valor valido escribe `theme.overrides.primary`. (4) MOTION — segmented control de 3 botones (`role="group"` + group label "Nivel de movimiento") "Sin movimiento"/"Sutil"/"Premium", `aria-pressed` en el activo, active=accent fill; escribe via `onMotionChange`. Todas las escrituras de tema pasan por `onChange` (el shell aplica `setTheme` del reducer, preservando otros overrides); el preview del shell recomputa `resolveLandingTheme` y lo aplica al wrapper → cambio en vivo. TARGET critico: escribir SIEMPRE a `landing_config.theme`/`config.motion`, NUNCA a `businesses.theme/palette/font` (D-06). Active state visual verbatim de settings (`border-primary ring-2 ring-primary/20` + Check). Reusar solo `@/components/ui` + `lucide-react`. Copy en español. Comentarios en español del POR QUE (por que el target es landing_config y no businesses; por que isSafeColor; por que normalizeTheme en el active-match).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sin errores.
    - Las escrituras van a `landing_config.theme`/`config.motion` via los callbacks; NO hay ningun `.from('businesses').update` ni referencia a `businesses.theme/palette/font` en el archivo (grep == 0).
    - El active-match del preset usa `normalizeTheme` (config 'default' resalta Forjo).
    - Seleccionar un preset resetea la paleta a `THEME_DEFAULT_PAL[preset]`.
    - El primary se valida con `isSafeColor`; un hex invalido muestra error inline y no se escribe.
    - Motion es un segmented de 3 con `role="group"`, group label y `aria-pressed`.
    - Swatches/segmented son `<button>` con focus-visible ring y touch targets adecuados.
  </acceptance_criteria>
  <done>Los controles de tema/paleta/primary/motion escriben a landing_config.theme/motion (nunca a businesses.*), validan el primary por allowlist, resaltan el preset correcto para config null-sembrado, y el preview refleja los cambios en vivo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (color input) → borrador → CSS var del preview | El primary es un string crudo que puede cruzar a una CSS var; punto de inyeccion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Injection | overrides.primary → CSS var inline en el preview wrapper | mitigate | `isSafeColor` (allowlist de hex estricto, `theme.ts`) valida el primary antes de escribirlo al draft; un valor con declaraciones CSS extra se rechaza y no se persiste. `resolveLandingTheme` lo re-valida al aplicar (defensa en profundidad). Heredado T-08-01. |
| T-14-14 | Tampering | escribir al target equivocado (businesses.* en vez de landing_config) | mitigate | El componente escribe SOLO via los callbacks a `landing_config.theme`/`config.motion`; grep-verificado que no toca `businesses.*` — evita repintar el chrome del panel y mantiene la separacion de dominios (D-06). |
| T-14-15 | Information Disclosure | preset/paleta desconocidos rompiendo el preview | mitigate | `normalizeTheme`/`normalizePalette` acotan cualquier valor al set del motor; un preset desconocido degrada a forjo (fail-safe, no expone ni rompe). |
| T-14-SC | Tampering | npm installs | accept | Cero paquetes nuevos (RESEARCH). Sin superficie supply-chain. |
</threat_model>

<verification>
- `npx vitest run test/landing-theme.test.ts` verde (active-preset normalization).
- `npx tsc --noEmit` verde.
- `npx vitest run` (suite completa) verde al cerrar la wave.
- Grep: sin `businesses` ni `isSafeColor`-bypass en `theme-controls.tsx`; el primary siempre pasa por `isSafeColor`.
- UAT (manual): cambiar preset/paleta/primary/motion en el editor actualiza el preview al instante; un hex invalido muestra error y no se aplica; un config null-sembrado resalta Forjo.
</verification>

<success_criteria>
- SC4/EDIT-04: el dueño elige preset de tema y ajusta paleta/color primario y motion dentro del set permitido, viendo el resultado aplicado — todo escrito a landing_config.theme/motion, con el primary por allowlist.
</success_criteria>

<output>
Create `.planning/workstreams/web-builder/phases/14-cms-editor-ui/14-04-SUMMARY.md` when done
</output>
