---
phase: 14-cms-editor-ui
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - app/(dashboard)/web/_sections/section-list.tsx
  - app/(dashboard)/web/_sections/section-forms.tsx
autonomous: true
requirements: [EDIT-01, EDIT-03]
must_haves:
  truths:
    - "El dueño ve SIEMPRE las 8 secciones fijas (hero, about, services, gallery, location, hours, cta, booking) en su orden actual"
    - "Los botones subir/bajar reescriben el order de dos secciones adyacentes; la primera no puede subir y la ultima no puede bajar"
    - "El toggle prende/apaga enabled por seccion; booking queda siempre visible (locked on)"
    - "El dueño edita el copy de cada seccion segun su schema data (headline/kicker/subhead/cta_label, title/body, subtitle, map_url/show_address, cta headline, rsvData header/intro) y el preview lo refleja al instante"
    - "services/locations muestran su lista read-only con un hint a donde se editan; hours muestra un panel informativo (se editan en Negocio → Horarios)"
  artifacts:
    - path: "app/(dashboard)/web/_sections/section-list.tsx"
      provides: "Panel con las 8 filas fijas: label + subir/bajar + toggle enabled + expand-to-edit; reorder por arrows (sin DnD)"
      min_lines: 80
    - path: "app/(dashboard)/web/_sections/section-forms.tsx"
      provides: "Un form de copy por seccion segun el schema data; paneles read-only para services/locations/hours"
      min_lines: 100
  key_links:
    - from: "app/(dashboard)/web/_sections/section-list.tsx"
      to: "lib/landing/editor-draft.ts"
      via: "los callbacks onMove/onToggle invocan moveSection/toggleSection (mutadores puros)"
      pattern: "onMove|onToggle"
    - from: "app/(dashboard)/web/_sections/section-forms.tsx"
      to: "lib/landing/schema.ts"
      via: "cada form edita exactamente los campos del schema data de su seccion (heroData/aboutData/servicesData/galleryData/rsvData/locationData/ctaData)"
      pattern: "onSectionDataChange"
---

<objective>
Implementar el panel de secciones (EDIT-03: reorden + on/off del set FIJO de 8) y los forms de copy por seccion (EDIT-01: edicion de textos por sección segun los schemas `data`). Reemplaza el stub `section-list.tsx` de 14-01 con la implementacion completa y crea `section-forms.tsx`. Todas las mutaciones pasan por el reducer puro `editor-draft.ts`; cada keystroke actualiza el borrador → el preview del shell reacciona.

Purpose: EDIT-01 y EDIT-03 son dos de los cinco Success Criteria de la fase. El set es FIJO (anti-feature DnD) — solo reorden por arrows + toggle. El copy se edita por los schemas `data` ya existentes; listas de services/locations y hours son read-only (se editan en otras pantallas).
Output: `section-list.tsx` (panel de 8 filas con arrows/toggle/expand), `section-forms.tsx` (form por seccion + paneles read-only derivados).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-builder/phases/14-cms-editor-ui/14-CONTEXT.md
@.planning/workstreams/web-builder/phases/14-cms-editor-ui/14-UI-SPEC.md
@.planning/workstreams/web-builder/phases/14-cms-editor-ui/14-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `app/(dashboard)/web/_sections/section-list.tsx` — named export `SectionListPanel` (implementacion completa; reemplaza el stub de 14-01).
- `app/(dashboard)/web/_sections/section-forms.tsx` — named export `SectionForm` (dispatcher por `section.type`) + los sub-forms/paneles read-only.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Panel de secciones — 8 filas fijas, reorder por arrows, toggle enabled, expand-to-edit</name>
  <files>app/(dashboard)/web/_sections/section-list.tsx</files>
  <read_first>
    - app/(dashboard)/web/web-client.tsx (el props-contract COMPLETO del stub, ya declarado en 14-01: `{ draft, onMove, onToggle, onSectionDataChange, services, locations, timeBlocks, business }`; como el shell compone el panel y ya le pasa los 4 datos read-only)
    - lib/landing/editor-draft.ts (moveSection/toggleSection: firma y semantica de bordes)
    - lib/landing/schema.ts (SECTION_TYPES = las 8 fijas; sectionSchema con enabled/order)
    - lib/landing/derive.ts (orderedSections para entender el criterio de orden a mostrar)
    - app/(dashboard)/settings/settings-client.tsx (lineas 977-999: patron segmented para el toggle; NO hay componente Switch en @/components/ui — usar boton segmentado con Eye/EyeOff)
    - .planning/workstreams/web-builder/phases/14-cms-editor-ui/14-UI-SPEC.md (§2 Section-list panel: anatomia de fila, botones min-h-11 min-w-11 ghost, disabled en bordes, aria-live, booking locked-on; Copywriting section labels + reorder/toggle)
  </read_first>
  <action>Reemplazar el stub `section-list.tsx` con la implementacion completa del named export `SectionListPanel` (props `{ draft: LandingConfig, onMove(type, dir), onToggle(type), onSectionDataChange(type, partial), services, locations, timeBlocks, business }` — IMPLEMENTAR el contrato que el stub de 14-01 ya declara COMPLETO (incluye los 4 datos read-only services/locations/timeBlocks/business que necesitan los forms); NO se cambia la firma ni se toca web-client.tsx). Renderizar SIEMPRE las 8 secciones fijas en orden por `order` (usar el mismo criterio que orderedSections pero SIN filtrar enabled — se listan todas). Cada fila (`<li>`, lista semantica `<ul>`): grip decorativo `GripVertical` (opacity reducida, NO interactivo), label en español (tabla de Copywriting: Portada/Nosotros/Servicios/Galería/Ubicación/Horarios/Llamado a la acción/Reservas), spacer, boton subir (`ChevronUp`) + boton bajar (`ChevronDown`) icon-only `variant="ghost"` `min-h-11 min-w-11`, y el toggle enabled (boton segmentado con `Eye`/`EyeOff`, `aria-pressed`, "on"=accent fill). La primera fila tiene subir `disabled` (opacity-50, aria-disabled); la ultima tiene bajar `disabled`. onClick de arrows → `onMove(type, 'up'|'down')`; toggle → `onToggle(type)`. `booking` renderiza el toggle en estado locked-on (disabled, pinned "on") con hint "Esta sección siempre se muestra" (D-04b: lockear SOLO booking; hero queda togglable). Anunciar el reorder a AT con un region `aria-live="polite"` ("Sección movida a la posición N"). Una seccion `enabled:false` lleva `text-muted-foreground` en el label pero sigue expandible. Click en el label/cuerpo expande el form de esa seccion (acordeon inline en desktop; en mobile se permite `vaul` Drawer — discrecion, pero el default es acordeon) montando `<SectionForm section=... ... />` de section-forms.tsx; una seccion abierta a la vez. NADA de drag-and-drop, cero dependencia nueva (D-04). Reusar solo componentes de `@/components/ui` y `lucide-react`. Touch targets ≥44px en todos los controles icon-only. Comentarios en español del POR QUE (booking locked, reorder por arrows no DnD).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sin errores.
    - Se renderizan exactamente 8 filas (las de SECTION_TYPES), siempre, sin add/remove.
    - subir en la primera fila y bajar en la ultima estan `disabled` con `aria-disabled`.
    - El toggle de `booking` esta locked-on (disabled) con su hint; el de `hero` es togglable.
    - Botones de reorder/toggle son `<button>` (no `<div>`), icon-only con `min-h-11 min-w-11` y focus-visible ring.
    - Existe un region `aria-live="polite"` para anunciar el reorder.
    - No hay ninguna libreria de drag-and-drop importada (grep == 0).
  </acceptance_criteria>
  <done>El panel lista las 8 secciones fijas con reorden por arrows (bordes deshabilitados), toggle enabled (booking locked-on), expand-to-edit y anuncio a AT, todo por los mutadores puros y sin dependencia nueva.</done>
</task>

<task type="auto">
  <name>Task 2: Forms de copy por seccion + paneles read-only derivados</name>
  <files>app/(dashboard)/web/_sections/section-forms.tsx</files>
  <read_first>
    - lib/landing/schema.ts (los schemas data por seccion — campos EXACTOS: heroData {headline,kicker,subhead,cta_label,image}; aboutData {title,body,image}; servicesData {title,subtitle}; galleryData {title,images}; rsvData {header,intro,images}; locationData {title,map_url,show_address}; ctaData {headline})
    - lib/landing/editor-draft.ts (setSectionData: como se aplica el cambio parcial)
    - app/(dashboard)/settings/settings-client.tsx (patron de Label+Input/Textarea, ProFields con "(opcional)", validacion inline; toasts sonner)
    - components/landing/{hero,about,services,gallery,location,cta,rsv-strip}.tsx (que campos de data consume cada seccion — para saber que se edita y que es read-only)
    - .planning/workstreams/web-builder/phases/14-cms-editor-ui/14-UI-SPEC.md (§3 Per-section copy forms: tabla de campos editables/read-only/images por seccion; labels siempre visibles, (opcional), URL fields validan onBlur, read-only panels con hint; Copywriting section labels)
  </read_first>
  <action>Crear `section-forms.tsx` con named export `SectionForm` (dispatcher por `section.type`) mas los sub-forms (D-05: un form por seccion manejado por los schemas `data` por-seccion; services/locations read-only desde sus tablas; hours deriva de time_blocks; booking solo `rsvData`). Props: `{ section, onDataChange(partial), services, locations, timeBlocks, business, businessId, imageSlot? }` — el `imageSlot` (o render-prop) lo provee el shell/section-list para inyectar los controles de imagen de 14-03 sin acoplar este archivo al upload. Un form por seccion, cada campo = `Label` (siempre visible, nunca placeholder-only) + `Input`/`Textarea`, editando EXACTAMENTE los campos del schema `data` de esa seccion (tabla §3): hero → headline/kicker/subhead/cta_label (Inputs) + slot de imagen `hero.image`; about → title (Input)/body (Textarea) + slot imagen `about.image`; services → title/subtitle (Inputs) + panel read-only de la lista de `services` (de la tabla) con hint "La lista se edita en Servicios"; gallery → title (Input) + slot multi-grid `gallery.images`; location → title (Input), map_url (Input URL, valida https onBlur con error inline inmediato), show_address (toggle) + panel read-only de `locations` con hint; hours → SIN campos: panel informativo read-only "Los horarios se editan en Negocio → Horarios"; cta → headline (Input); booking → header (Input)/intro (Textarea) de `rsvData` + slot multi-grid `rsvData.images` (galeria RSV) — el widget de booking NO se edita (caja negra). Campos opcionales marcados "(opcional)" en `text-xs text-muted-foreground`. Cada onChange llama `onDataChange({ campo: valor })` (el shell aplica `setSectionData` del reducer). Los paneles read-only (services/locations/hours) usan `bg-muted/40` + `text-muted-foreground` + hint explicito. Validacion de `map_url`: onBlur, si no es https URL valida mostrar error inline bajo el campo (no esperar a Guardar). Copy en español rioplatense. NO instanciar el upload aca — se recibe por `imageSlot` (14-03 lo implementa). Reusar solo `@/components/ui`. Comentarios en español del POR QUE (por que services/locations son read-only aca; por que rsvData vive bajo booking).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sin errores.
    - Cada form edita solo los campos declarados en el schema `data` de su seccion (sin campos inventados).
    - services/locations/hours se renderizan como paneles read-only con hint a la pantalla donde se editan.
    - `map_url` valida https onBlur con error inline inmediato.
    - Los campos de imagen se rinden via `imageSlot` (no hay logica de upload en este archivo — grep de `supabase.storage` == 0).
    - Labels siempre visibles; opcionales marcados "(opcional)".
  </acceptance_criteria>
  <done>Existe un form de copy por seccion alineado al schema `data`, con paneles read-only para services/locations/hours, validacion de URL inline, y slots para las imagenes — cada keystroke fluye al borrador via onDataChange.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (forms) → borrador en memoria | El copy editado por el dueño entra al draft; se persiste luego por saveLandingConfig (validado server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Injection | map_url / campos URL del copy | mitigate | Los campos imagen/URL son `z.string().url()` en el schema; la validacion no-bypasseable corre server-side en `parseLandingConfigForWrite` al Guardar (heredado Phase 13). El form valida onBlur solo como UX; una URL invalida se rechaza al escribir. |
| T-14-07 | Tampering | reorder/toggle manipulando el set de secciones | mitigate | Los mutadores puros `moveSection`/`toggleSection` nunca agregan ni borran secciones; el set queda fijo en 8. `orderedSections` server-side garantiza booking presente aunque se togglee off. |
| T-14-08 | Information Disclosure | listas read-only de services/locations en el editor | accept | Son datos del PROPIO negocio (fetchados por la page con `.eq('business_id', business.id)`); mostrarlos al dueño es esperado (mismo dato que ya ve en Servicios/Negocio). |
| T-14-SC | Tampering | npm installs | accept | Cero paquetes nuevos (RESEARCH). Sin superficie supply-chain. |
</threat_model>

<verification>
- `npx tsc --noEmit` verde.
- `npx vitest run` (suite completa) verde al cerrar la wave.
- UAT (manual): reordenar una seccion mueve su posicion en el preview; apagar una seccion la oculta del preview (booking sigue apareciendo); editar el headline del hero se ve al instante; services/locations/hours aparecen como read-only con su hint.
</verification>

<success_criteria>
- SC3/EDIT-03: el dueño reordena y prende/apaga las secciones del set FIJO (sin layout libre/DnD).
- SC1/EDIT-01: el dueño edita el copy de cada seccion desde el editor visual y lo ve aplicado en el preview.
</success_criteria>

<output>
Create `.planning/workstreams/web-builder/phases/14-cms-editor-ui/14-02-SUMMARY.md` when done
</output>
