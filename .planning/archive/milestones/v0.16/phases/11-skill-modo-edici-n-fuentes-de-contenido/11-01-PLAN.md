---
phase: 11-skill-modo-edici-n-fuentes-de-contenido
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/setup-landing.ts
  - scripts/instagram-cosechar.py
autonomous: true
requirements: [SKILL-05, SKILL-06]
tags: [web-builder, skill, tooling, instaloader]

must_haves:
  truths:
    - "El operador puede reconstruir el estado de una landing sin payload guardado corriendo `npm run setup:landing -- --inspect <slug>` y viendo el `landing_config` real completo (no `'poblado'`)."
    - "El operador puede consolidar una descarga de instaloader a `captions.md` + `resumen.json` corriendo `python scripts/instagram-cosechar.py <dir>` desde forjo-app, sin depender del repo hermano."
    - "El modo `--inspect` sigue siendo read-only: cero UPDATE, cero upload, re-correrlo da el mismo output."
  artifacts:
    - path: "scripts/setup-landing.ts"
      provides: "Modo --inspect extendido que vuelca el landing_config completo (D-04)"
      contains: "landing_config: biz.landing_config"
    - path: "scripts/instagram-cosechar.py"
      provides: "Consolidador de descarga instaloader → captions.md + resumen.json (copia del sibling)"
      contains: "def load_node"
  key_links:
    - from: "scripts/setup-landing.ts (runInspect)"
      to: "resolveBusiness select (landing_config ya traído)"
      via: "el resumen vuelca biz.landing_config sin query nueva"
      pattern: "landing_config"
    - from: "scripts/instagram-cosechar.py"
      to: "descarga de instaloader en disco (.json.xz + .jpg)"
      via: "lee lo que instaloader dejó, no baja nada de internet"
      pattern: "json.xz"
---

<objective>
Preparar los dos artefactos de código que habilitan el modo edición y la fuente Instagram de la skill `forjo-web-builder`, SIN tocar el renderer, el schema, ni el modo escritura del script:

1. Extender el modo `--inspect` de `scripts/setup-landing.ts` (D-04) para que vuelque el `landing_config` real completo en vez de colapsarlo a `'poblado'`. Es la fuente de reconstrucción del payload para landings previas a la persistencia de payloads (habilita SC2 de SKILL-05). Cambio ADITIVO y READ-ONLY: cero UPDATE, cero superficie de seguridad nueva.
2. Crear `scripts/instagram-cosechar.py` como copia verbatim del consolidador probado del repo hermano `webs-cms-forjostudio` (D-03), self-contained (sin dependencia cross-repo en runtime). Es el paso 2 del pipeline instaloader que documenta el Plan 02 (habilita la fuente c de SKILL-06).

Purpose: dejar listos los dos "prerrequisitos de código" que la skill (Plan 02) referencia. Son independientes entre sí (tocan archivos distintos) y del SKILL.md (que los documenta pero no los importa).
Output: `scripts/setup-landing.ts` con `--inspect` extendido + `scripts/instagram-cosechar.py` nuevo (primer `.py` del repo).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-builder/STATE.md
@.planning/workstreams/web-builder/ROADMAP.md
@.planning/workstreams/web-builder/phases/11-skill-modo-edici-n-fuentes-de-contenido/11-CONTEXT.md
@.planning/workstreams/web-builder/phases/11-skill-modo-edici-n-fuentes-de-contenido/11-PATTERNS.md

# Archivo a extender (read-only mode) — leer entero antes de tocar
@scripts/setup-landing.ts
</context>

<artifacts_this_phase_produces>
Este plan (11-01) produce:
- **`scripts/setup-landing.ts`** — comportamiento extendido del modo `--inspect`: la clave `landing_config` del objeto `resumen` deja de colapsarse a `'poblado'` y vuelca el config completo (o `null`). El resto del archivo (write path, idempotencia, `resolveBusiness`, flags) queda intacto.
- **`scripts/instagram-cosechar.py`** — script nuevo (primer `.py` del repo). Consolida la descarga de instaloader a `captions.md` + `resumen.json`. Copia byte-por-byte del sibling salvo header de origen + docstring de uso adaptado.

(El Plan 11-02 produce las secciones nuevas de `SKILL.md`: "MODO EDICIÓN" y "FUENTES DE CONTENIDO".)
</artifacts_this_phase_produces>

<tasks>

<!-- planner-discipline-allow: 'poblado' -->
<!-- planner-discipline-allow: _clientes -->

<task type="auto">
  <name>Task 1: Extender el modo --inspect para volcar el landing_config completo (D-04)</name>
  <files>scripts/setup-landing.ts</files>
  <read_first>
    - `scripts/setup-landing.ts` (el archivo a modificar) — leer el header (líneas 1-29, contrato read-only del modo inspect), `resolveBusiness` (76-104, ya trae `landing_config` en el select y lo tipa `unknown`), y `runInspect` (109-155, el objeto `resumen` a extender). NO tocar `runWrite` (226-318) ni `main` (320-359).
    - `.planning/workstreams/web-builder/phases/11-skill-modo-edici-n-fuentes-de-contenido/11-PATTERNS.md` — sección "`scripts/setup-landing.ts` — extender `--inspect`" (el cambio mínimo exacto en la línea 151 y las restricciones heredadas del header).
  </read_first>
  <action>
    Cambio ADITIVO mínimo, READ-ONLY, en `runInspect`. Reemplazar SOLO la línea que hoy colapsa el config (`landing_config: biz.landing_config == null ? null : 'poblado'`) por un volcado completo del config real: `landing_config: biz.landing_config ?? null`. Así el operador reconstruye el `BuilderInput`/`brand` desde el volcado sin re-tipear el copy (D-04, habilita SC2 de SKILL-05).

    Ajustar el comentario español adyacente a esa clave para que refleje el nuevo propósito (volcado completo → insumo de reconstrucción del payload para landings sin payload guardado), conservando el estilo de comentarios densos en español del archivo. Mantener la referencia a que si ya hay config, la escritura lo SOBRE-ESCRIBE.

    NO tocar nada más: `resolveBusiness` (compartido con el write path) ya trae `landing_config` en el `select` (línea 82-83) y ya lo tipa `landing_config: unknown` (línea 102) — no hace falta query nueva ni cambiar el `select`. NO agregar flags de escritura, NO tocar `runWrite`, NO tocar la idempotencia, NO cambiar el dispatch de `main` (el `--inspect` sigue teniendo prioridad y sigue siendo el único camino read-only).

    Restricción de seguridad (heredada del header del archivo, líneas 10-13): inspect es READ-ONLY — cero UPDATE, cero upload; re-correrlo da el mismo output. El `landing_config` NO contiene secretos de tenant (solo copy/imágenes/tema); volcarlo entero no abre superficie de fuga (ver `<threat_model>`).
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v '^#' | grep -c "setup-landing" ; test $? -eq 1 || echo "TSC_ERRORS_IN_SETUP_LANDING"</automated>
  </verify>
  <acceptance_criteria>
    - En `scripts/setup-landing.ts`, el objeto `resumen` de `runInspect` YA NO contiene el literal ternario que produce `'poblado'`; la clave `landing_config` vuelca `biz.landing_config` (config completo) o `null`.
    - `grep -c "'poblado'" scripts/setup-landing.ts` devuelve `0` (el string `'poblado'` ya no aparece en el archivo).
    - `grep -c "runWrite" scripts/setup-landing.ts` es igual que antes del cambio (write path intacto): la función `runWrite` sigue definida y llamada desde `main`.
    - `grep -c "\.update(" scripts/setup-landing.ts` devuelve exactamente `1` (el único UPDATE, dentro de `runWrite`; `runInspect` no introdujo ninguno) — inspect sigue read-only.
    - `npx tsc --noEmit` no reporta errores nuevos en `scripts/setup-landing.ts`.
    - `npm run setup:landing -- --inspect estudioooodiego` imprime un JSON cuyo campo `landing_config` es un objeto (o `null`), NO el string `"poblado"`. (Requiere `.env.local` local del operador; si no hay credenciales el script lo reporta — eso es esperado en CI.)
  </acceptance_criteria>
  <done>El modo `--inspect` vuelca el `landing_config` completo, sigue read-only (sin UPDATE nuevo), tsc verde, y el write path queda intacto.</done>
</task>

<task type="auto">
  <name>Task 2: Copiar instagram-cosechar.py del repo hermano a scripts/ (D-03)</name>
  <files>scripts/instagram-cosechar.py</files>
  <read_first>
    - `C:/Users/franc/Desktop/Forjo Studio/webs-cms-forjostudio/scripts/instagram-cosechar.py` (131 líneas) — el original a copiar byte-por-byte. Stdlib pura (`sys, os, glob, json, lzma, datetime`), cero deps pip en el script; produce `resumen.json` + `captions.md` en la carpeta que le pasás.
    - `.planning/workstreams/web-builder/phases/11-skill-modo-edici-n-fuentes-de-contenido/11-PATTERNS.md` — sección "`scripts/instagram-cosechar.py` (CREATE — copia literal del sibling)": el contrato (qué produce), las convenciones a respetar y el ÚNICO ajuste permitido (header de origen + docstring de uso).
  </read_first>
  <action>
    Crear `scripts/instagram-cosechar.py` como copia del original del sibling `webs-cms-forjostudio/scripts/instagram-cosechar.py`. El cuerpo (funciones `load_node`, `main`, el orden por fecha desc, el bloque `top_fotos` de 15, la escritura de `resumen.json` + `captions.md`) es copia byte-por-byte — NO cambiar el contrato ni la lógica.

    ÚNICOS ajustes permitidos (D-03 / Claude's Discretion del CONTEXT):
    1. Agregar en el header, arriba del shebang o justo debajo, un comentario de origen indicando de dónde salió y que la copia es intencional y self-contained: que este archivo es una copia de `webs-cms-forjostudio/scripts/instagram-cosechar.py`, portada para que forjo-app no dependa del repo hermano en runtime; y que mantener sincronía con el original es manual (queda fuera de scope este milestone). Comentario en español, estilo del proyecto.
    2. Actualizar el docstring de uso (las líneas del original que hoy referencian `_clientes/{slug}/instagram/{handle}`, estructura de webs-cms) para que muestren el uso desde forjo-app: se corre con `python scripts/instagram-cosechar.py "<dir>/<handle>"` sobre el directorio donde instaloader dejó la descarga. Dejar claro (como el original) que "no baja nada de internet: solo procesa lo que instaloader ya dejó en disco".

    Conservar shebang `#!/usr/bin/env python3`, stdlib pura, y todas las convenciones observadas (carruseles como `base_1.jpg`, orden por fecha desc, `top_fotos` una por post). NO agregar `npm script` (D-04: se corre con `python` directo). NO agregar deps pip al script (instaloader/browser_cookie3 son tool local del operador, no del script).
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && python -c "import ast,sys; ast.parse(open('scripts/instagram-cosechar.py',encoding='utf-8').read()); print('PY_PARSE_OK')" 2>&1 | grep -c PY_PARSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/instagram-cosechar.py` existe.
    - `grep -c "def load_node" scripts/instagram-cosechar.py` devuelve `1` y `grep -c "top_fotos" scripts/instagram-cosechar.py` devuelve al menos `2` (bloque preservado del original).
    - El primer renglón es `#!/usr/bin/env python3` (shebang preservado).
    - El archivo contiene un comentario de origen que nombra `webs-cms-forjostudio` (verificable: `grep -c "webs-cms-forjostudio" scripts/instagram-cosechar.py` >= 1).
    - El docstring de uso NO referencia `_clientes/{slug}/instagram/{handle}` (la estructura vieja de webs-cms): `grep -c "_clientes" scripts/instagram-cosechar.py` devuelve `0`.
    - `python -c "import ast; ast.parse(open('scripts/instagram-cosechar.py',encoding='utf-8').read())"` no lanza `SyntaxError` (el script parsea como Python válido). No requiere instaloader instalado — solo valida sintaxis.
    - El script no importa `instaloader` ni `browser_cookie3` (son tool del operador, no del script): `grep -Ec "import (instaloader|browser_cookie3)" scripts/instagram-cosechar.py` devuelve `0`.
  </acceptance_criteria>
  <done>`scripts/instagram-cosechar.py` existe, parsea como Python válido, es copia del sibling con header de origen + docstring de uso adaptado a forjo-app, sin deps pip en el script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| máquina del operador → DB producción | El write path service-role de v0.10 corre localmente; `--inspect` toca la misma DB pero SOLO lee. |
| descarga instaloader (disco) → script `.py` | `instagram-cosechar.py` procesa archivos que instaloader ya dejó en disco; no baja nada de internet. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | `--inspect` volcado de `landing_config` | accept | El `landing_config` contiene SOLO copy/imágenes/tema — cero secretos de tenant (los secretos viven en columnas que este `select` no trae, y el config viaja por `public_businesses` por columnas explícitas desde v0.10). Volcarlo entero en un dump local read-only no amplía superficie. |
| T-11-02 | Tampering | `--inspect` (modo read-only) | mitigate | El cambio es puramente aditivo sobre el objeto `resumen`; no introduce ningún `UPDATE`/`upload`. Acceptance verifica `grep -c '\.update(' == 1` (el único UPDATE sigue siendo el de `runWrite`). Inspect sigue idempotente y read-only. |
| T-11-03 | Tampering | `instagram-cosechar.py` (stdlib pura) | mitigate | El script no baja nada de internet ni ejecuta binarios remotos: solo lee `.json.xz`/`.jpg` locales y escribe `resumen.json`/`captions.md` en la misma carpeta. Copia verbatim de un script probado en producción del sibling; sin deps pip nuevas. |
| T-11-SC | Tampering | pip installs (instaloader, browser_cookie3) | accept | NO son deps de la app: son tool LOCAL del operador, fuera del `package.json` y del runtime web. El script `.py` usa solo stdlib. Documentar en el SKILL.md (Plan 02) como prerrequisito del operador, no como install del repo. Sin `[ASSUMED]`/`[SUS]` npm/pip que entre al build. |

Nota: este plan NO abre superficie web nueva. Toda escritura sigue por el script local service-role de v0.10 con el mismo checkpoint humano pre-escritura (SKILL-04). El `--inspect` extendido es estrictamente read-only.
</threat_model>

<verification>
- `npx tsc --noEmit` verde sin errores nuevos en `scripts/setup-landing.ts`.
- `grep -c "'poblado'" scripts/setup-landing.ts` == 0 (colapso viejo eliminado).
- `grep -c "\.update(" scripts/setup-landing.ts` == 1 (write path intacto, inspect read-only).
- `python -c "import ast; ast.parse(...)"` sobre `scripts/instagram-cosechar.py` sin `SyntaxError`.
- `grep -c "_clientes" scripts/instagram-cosechar.py` == 0 (docstring adaptado a forjo-app).
- `grep -c "webs-cms-forjostudio" scripts/instagram-cosechar.py` >= 1 (comentario de origen presente).
</verification>

<success_criteria>
- El modo `--inspect` vuelca el `landing_config` completo (o `null`), sigue read-only, y el write path + idempotencia quedan intactos.
- `scripts/instagram-cosechar.py` existe, parsea como Python válido, es copia self-contained del sibling con header de origen y docstring de uso de forjo-app.
- Cero deps npm/pip nuevas en el repo (instaloader/browser_cookie3 son tool del operador).
</success_criteria>

<output>
Create `.planning/workstreams/web-builder/phases/11-skill-modo-edici-n-fuentes-de-contenido/11-01-SUMMARY.md` when done.
</output>
