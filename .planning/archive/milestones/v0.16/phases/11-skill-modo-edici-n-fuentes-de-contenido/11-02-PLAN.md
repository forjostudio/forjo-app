---
phase: 11-skill-modo-edici-n-fuentes-de-contenido
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - .claude/skills/forjo-web-builder/SKILL.md
autonomous: true
requirements: [SKILL-05, SKILL-06]
tags: [web-builder, skill, editing, content-sources]

must_haves:
  truths:
    - "El operador puede pedir un cambio puntual ('cambiá el headline', 'sumá 3 fotos') y la skill parte de `landing-payloads/<slug>.json`, aplica SOLO ese campo, muestra un diff acotado (antes→después de lo tocado) y re-escribe idempotente (SC1 / SKILL-05)."
    - "Si no existe el payload guardado, la skill reconstruye el estado con `npm run setup:landing -- --inspect <slug>`, avisa que re-escribe el config completo y pide al operador lo que el inspect no devuelve (el copy) (SC2 / SKILL-05)."
    - "La skill arma el contenido probando fuentes en orden — operador estructurado → web existente (WebFetch) → Instagram (instaloader) — y si una falla cae a la siguiente sin bloquearse (SC3 / SKILL-06)."
    - "Todo el copy nuevo pasa por el `humanizador` y las imágenes se referencian como rutas locales re-hosteadas por el script (nunca hot-link al CDN de origen) (SC4 / SKILL-06)."
  artifacts:
    - path: ".claude/skills/forjo-web-builder/SKILL.md"
      provides: "Sección 'MODO EDICIÓN' (entre paso 2 y 3) + paso 3 renombrado a 'FUENTES DE CONTENIDO' con pipeline instaloader"
      contains: "MODO EDICIÓN"
  key_links:
    - from: ".claude/skills/forjo-web-builder/SKILL.md (MODO EDICIÓN)"
      to: "landing-payloads/<slug>.json + npm run setup:landing -- --inspect <slug>"
      via: "reconstrucción del payload desde el --inspect extendido (Plan 01) para landings sin payload"
      pattern: "--inspect"
    - from: ".claude/skills/forjo-web-builder/SKILL.md (FUENTES DE CONTENIDO, fuente c)"
      to: "python scripts/instagram-cosechar.py"
      via: "pipeline instaloader documentado inline; corre el script copiado en Plan 01"
      pattern: "instagram-cosechar.py"
---

<objective>
Ampliar la skill `.claude/skills/forjo-web-builder/SKILL.md` con las dos piezas de la fase, preservando TODO el flujo que ya funciona (no re-escribir pasos que andan):

- **(A) MODO EDICIÓN** — sección nueva entre el paso 2 (RESOLVER) y el paso 3, para retocar una landing ya publicada sin rehacerla: parte de `landing-payloads/<slug>.json`, aplica SOLO el campo pedido, muestra un checkpoint acotado (diff antes→después de los campos tocados, D-05) y re-escribe idempotente (D-06). Para landings sin payload, reconstruye desde el `--inspect` extendido (D-04, del Plan 01). Reqs: SKILL-05 (SC1 + SC2).
- **(B) FUENTES DE CONTENIDO** — renombrar el paso 3 (hoy "SCRAPE de Instagram" con el motor Playwright de `instagram-a-web`) a "FUENTES DE CONTENIDO", documentando el orden de confiabilidad: (a) operador estructurado (D-01) → (b) web existente vía WebFetch (D-02) → (c) Instagram best-effort vía el pipeline instaloader (D-03, reemplaza Playwright). Fallthrough sin bloqueo. Reqs: SKILL-06 (SC3 + SC4).

Purpose: darle al operador el "retocar sin rehacer" y una cosecha de contenido más robusta (instaloader reusa la sesión de Firefox, sin el Playwright frágil), manteniendo intactos el humanizador obligatorio, el re-host de imágenes y la escritura service-role local.
Output: `SKILL.md` con la sección "MODO EDICIÓN" y el paso "FUENTES DE CONTENIDO", sin regresión en los pasos 1, 2, 4-8.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-builder/STATE.md
@.planning/workstreams/web-builder/ROADMAP.md
@.planning/workstreams/web-builder/phases/11-skill-modo-edici-n-fuentes-de-contenido/11-CONTEXT.md
@.planning/workstreams/web-builder/phases/11-skill-modo-edici-n-fuentes-de-contenido/11-PATTERNS.md

# El archivo a modificar — leer entero antes de tocar
@.claude/skills/forjo-web-builder/SKILL.md

# Depende del Plan 01 (los artefactos que este SKILL.md referencia)
@.planning/workstreams/web-builder/phases/11-skill-modo-edici-n-fuentes-de-contenido/11-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Este plan (11-02) produce, dentro de `.claude/skills/forjo-web-builder/SKILL.md`:
- **Sección "MODO EDICIÓN (retocar una landing existente)"** — insertada entre el paso 2 (RESOLVER) y el paso 3. Documenta: partir del payload guardado / reconstruir desde `--inspect` si no existe, aplicar SOLO el campo pedido, checkpoint con diff acotado a campo (D-05), re-escritura idempotente (D-06).
- **Paso 3 renombrado a "FUENTES DE CONTENIDO"** — reemplaza el bloque Playwright/`instagram-a-web` por el orden (a) operador estructurado → (b) web existente (WebFetch) → (c) Instagram (pipeline instaloader inline). Documenta prerrequisitos de instaloader (Firefox + cuenta dedicada) y el fallthrough sin bloqueo.
- Actualización de referencias cruzadas del archivo (sección "Reusos declarados" + guardrails) para que ya NO citen el motor Playwright de `instagram-a-web` como el mecanismo de IG.

(El Plan 11-01 ya produjo el `--inspect` extendido y `scripts/instagram-cosechar.py` que estas secciones referencian.)
</artifacts_this_phase_produces>

<tasks>

<!-- planner-discipline-allow: FUENTES DE CONTENIDO -->
<!-- planner-discipline-allow: SCRAPE de Instagram -->
<!-- planner-discipline-allow: Playwright -->

<task type="auto">
  <name>Task 1: Insertar la sección MODO EDICIÓN entre el paso 2 y el paso 3 (D-04/D-05/D-06 · SKILL-05)</name>
  <files>.claude/skills/forjo-web-builder/SKILL.md</files>
  <read_first>
    - `.claude/skills/forjo-web-builder/SKILL.md` (el archivo a modificar) — leer entero. Foco: el paso 2 RESOLVER (líneas 47-62), el paso 6 CHECKPOINT bloqueante (127-140, el patrón que el checkpoint acotado espeja), y el paso 7 ESCRIBIR / persistencia de `landing-payloads/<slug>.json` (143-162, la forma `{ "input": <BuilderInput>, "brand": <BrandHints> }` que el modo edición edita).
    - `C:/Users/franc/Desktop/Forjo Studio/forjo-webbuilder-skill-ampliacion.md` §A "Modo edición" — el markdown literal a insertar (5 sub-pasos). ESTE es el texto base autoritativo.
    - `.planning/.../11-PATTERNS.md` sección "(A) MODO EDICIÓN" — dónde insertar y qué patrón espejar (diff acotado a campo, D-05).
    - `.planning/.../11-CONTEXT.md` decisiones D-04/D-05/D-06 (autoritativas, no re-litigar).
  </read_first>
  <action>
    Insertar una sección nueva "### MODO EDICIÓN (retocar una landing existente)" ENTRE el final del paso 2 (RESOLVER) y el inicio del paso 3, siguiendo el markdown literal del brief §A. Preservar el formato del archivo: encabezados `###`, bloques ```powershell para comandos, callouts `>` para avisos, `---` entre secciones grandes.

    La sección debe cubrir, en prosa directiva (español, sin inventar):
    1. Cuándo aplica: el operador quiere RETOCAR una web ya hecha, no rehacerla → no arrancar de cero.
    2. Fuente de verdad: buscar `landing-payloads/<slug>.json` (se persiste en cada escritura, paso 7). Si existe → partir de ahí. Si NO existe (landing previa a la persistencia de payloads) → reconstruir el estado con `npm run setup:landing -- --inspect <slug>` (que ahora vuelca el `landing_config` completo, gracias al Plan 01), avisar al operador que se va a re-escribir el config completo, y pedirle lo que el inspect no devuelve. Nota exacta: el `--inspect` extendido SÍ devuelve el `landing_config` (copy incluido) — el operador solo confirma/retoca, no re-tipea todo. Si por algún motivo el config volcado no alcanza para reconstruir un campo, pedirlo.
    3. Aplicar SOLO el cambio puntual pedido sobre `input`/`brand` del payload (ej. `hero.headline`, sumar `gallery.images`, cambiar `palette`). No tocar lo que no se pidió.
    4. Copy nuevo → pasa por el `humanizador` (paso obligatorio, igual que en el flujo completo).
    5. CHECKPOINT ACOTADO (D-05): mostrar el diff a nivel CAMPO — antes→después SOLO de los campos que cambiaron (ej. `hero.headline: <valor viejo> → <valor nuevo>`), computado comparando el payload viejo vs el nuevo. NO mostrar el config entero. Esperar aprobación explícita (mismo ethos bloqueante del paso 6).
    6. Re-escribir con el mismo comando del paso 7: `npm run setup:landing -- --slug <slug> --config landing-payloads/<slug>.json`. Es IDEMPOTENTE (D-06): re-correr con el mismo payload da el mismo resultado; "aplicar solo el campo" = editar solo ese campo del payload, NO un patch parcial de la DB (el script re-arma el config completo determinísticamente).

    NO re-escribir los pasos 1, 2, 4-8. La inserción es aditiva. Mantener la numeración legible: si el paso 3 sigue llamándose "3." tras la inserción, no hace falta re-numerar (la sección MODO EDICIÓN es un sub-modo con su propio encabezado, no un paso numerado que corra la secuencia); seguir el criterio del brief salvo que re-numerar quede más claro.

    IMPORTANTE (comment-text discipline): no dejar en el archivo strings que un gate pudiera negar-grepear como literal; escribir prosa directiva, no snippets de implementación. El diff acotado se describe por concepto (campo: viejo → nuevo), no como código.
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && grep -c "MODO EDICIÓN" .claude/skills/forjo-web-builder/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `SKILL.md` contiene un encabezado "MODO EDICIÓN" (verificable: `grep -c "MODO EDICIÓN" .claude/skills/forjo-web-builder/SKILL.md` >= 1).
    - La sección "MODO EDICIÓN" aparece DESPUÉS del encabezado del paso 2 ("RESOLVER") y ANTES del encabezado del paso que documenta las fuentes de contenido (verificable por orden de líneas: la línea de "MODO EDICIÓN" está entre la de "2. RESOLVER" y la de "FUENTES DE CONTENIDO").
    - La sección menciona `landing-payloads/<slug>.json` como fuente de verdad y `npm run setup:landing -- --inspect <slug>` como reconstrucción para landings sin payload (`grep -c "landing-payloads" SKILL.md` >= 1 y la sección referencia `--inspect`).
    - La sección describe un checkpoint ACOTADO a nivel campo (antes→después SOLO de lo tocado), no el config entero, y menciona explícitamente el `humanizador` para copy nuevo.
    - La sección referencia el comando idempotente de re-escritura `npm run setup:landing -- --slug <slug> --config landing-payloads/<slug>.json`.
    - Los pasos 1, 2, 4, 5, 6, 7, 8 originales siguen presentes (no se borró ninguno): `grep -c "### 7. ESCRIBIR" SKILL.md` >= 1 y `grep -c "### 6. CHECKPOINT" SKILL.md` >= 1.
  </acceptance_criteria>
  <done>La sección "MODO EDICIÓN" existe entre el paso 2 y el 3, cubre payload→inspect→campo-único→diff-acotado→humanizador→re-escritura idempotente, y no rompió los pasos existentes.</done>
</task>

<task type="auto">
  <name>Task 2: Renombrar paso 3 a FUENTES DE CONTENIDO y reemplazar Playwright por el pipeline instaloader (D-01/D-02/D-03 · SKILL-06)</name>
  <files>.claude/skills/forjo-web-builder/SKILL.md</files>
  <read_first>
    - `.claude/skills/forjo-web-builder/SKILL.md` — el paso 3 actual "SCRAPE de Instagram" (líneas 64-84, con el motor Playwright de `instagram-a-web`) que se reemplaza; el paso 4 COPY→humanizador (86-93) y el paso 5 ARMAR config (95-125) que quedan intactos; la sección "Reusos declarados" (191-198) y los guardrails (170-188) que citan `instagram-a-web` como motor y hay que actualizar.
    - `C:/Users/franc/Desktop/Forjo Studio/forjo-webbuilder-skill-ampliacion.md` §B "Más fuentes de contenido" — el markdown literal (fuentes a/b/c en orden de confiabilidad). Base autoritativa.
    - `C:/Users/franc/Desktop/Forjo Studio/webs-cms-forjostudio/.claude/skills/armar-web-forjo/SKILL.md` §"Cosecha desde Instagram" (líneas 345-397) y §"Modo cosecha" — el patrón instaloader + Firefox + cuenta dedicada a portar inline, y la línea 45 (WebFetch para web existente) que funda D-02.
    - `scripts/instagram-cosechar.py` (creado en Plan 01) — el paso 2 del pipeline que la skill invoca.
    - `lib/landing/builder.ts` `BuilderInput`/`BrandHints` (el shape que las fuentes alimentan; ya documentado en el paso 5 del SKILL.md — no reinventarlo).
  </read_first>
  <action>
    Renombrar el encabezado del paso 3 de "SCRAPE de Instagram" a "FUENTES DE CONTENIDO" y reemplazar su cuerpo (el bloque Playwright/`instagram-a-web`) por el orden de fuentes por confiabilidad, siguiendo el brief §B y portando inline el patrón instaloader del sibling. Preservar el formato (encabezado numerado, bloques ```powershell, callouts `>`).

    Contenido nuevo del paso, en prosa directiva (español):

    **(a) Entrada estructurada del operador (la más confiable — D-01):** pedir en formato fijo — Hero: propuesta de valor SIN ciudad + ciudad/zona (va al `kicker`) + frase secundaria opcional; About: 2-4 líneas; Fotos: ruta a carpeta local con 6-12 imágenes marcando cuál es hero / about / galería. Servicios NO — se derivan solos de la tabla (D10-04).

    **(b) Web existente del negocio (D-02):** si el negocio ya tiene web (Instagram link-in-bio, Linktree, sitio viejo), leerla con **WebFetch nativo** para sacar copy + descargar imágenes localmente. Mismo trato que IG: nunca hot-link (las URLs caducan), el texto pasa por el humanizador, NO se copia estructura/HTML — solo contenido.

    **(c) Instagram best-effort vía instaloader (D-03) — REEMPLAZA Playwright:** documentar inline el pipeline:
      - Prerrequisitos (tool LOCAL del operador, NO deps de la app): `pip install instaloader browser_cookie3`; sesión de Firefox logueada con **cuenta IG dedicada** (no la personal; cookies en disco, incógnito NO sirve; perfil público). Aclarar por qué Firefox (importa cookies confiable) y por qué cuenta dedicada (scrapear puede marcar la cuenta).
      - Paso 1 (descarga): correr `instaloader` con `--load-cookies firefox --no-videos --no-video-thumbnails` reusando la sesión de Firefox (sin password), en un dir por handle.
      - Paso 2 (consolidación): correr `python scripts/instagram-cosechar.py <dir>/<handle>` (el script copiado en el Plan 01) → produce `captions.md` (voz real del negocio → materia prima de copy) + `resumen.json` (posts + `top_fotos`) + imágenes en alta.
      - Falla esperada (login-wall / anti-bot / rate-limit) → **caer a la fuente (a) sin bloquearse** (fallthrough); el `403 Forbidden [retrying]` intermitente es normal, instaloader reintenta solo.

    Cierre del paso (reglas que se mantienen, NO regresión): cualquiera sea la fuente, el copy pasa por el `humanizador` (paso 4) y NADA se inventa; las imágenes van como RUTAS LOCALES y el script las re-hostea a `landing-assets/{businessId}/` (SKILL-03) — nunca hot-link al CDN de IG/web.

    Además, actualizar las referencias cruzadas que quedaron obsoletas por el cambio de motor:
    - En "Reusos declarados" (191-198): la línea de `instagram-a-web` ya no describe el motor de IG de esta skill — reflejar que la cosecha de IG ahora es el pipeline instaloader (documentado en el paso FUENTES DE CONTENIDO) + `scripts/instagram-cosechar.py`; `instagram-a-web` deja de ser el mecanismo. Mantener el resto de los reusos (humanizador, web-scrolling como inspiración, builder.ts).
    - En los guardrails (170-188): NO debe quedar el motor Playwright de `instagram-a-web` descrito como el camino de IG. La regla "solo componentes Forjo-native / no inyectar HTML de instagram-a-web/web-scrolling" (D5) se conserva tal cual — esa sigue vigente.

    NO tocar los pasos 4, 5, 6, 7, 8 (el flujo post-fuentes es idéntico). NO cambiar el shape `BuilderInput` ni el gate del script.

    Verificación de la regla del proyecto: no debe quedar en el archivo ninguna instrucción de instalar Playwright en el root (Pitfall 4 de v0.10) — como el motor Playwright se retira, esa advertencia deja de ser necesaria; si se conserva alguna mención, que sea histórica y no una instrucción activa.
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && grep -c "FUENTES DE CONTENIDO" .claude/skills/forjo-web-builder/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - El paso 3 se llama "FUENTES DE CONTENIDO" (`grep -c "FUENTES DE CONTENIDO" SKILL.md` >= 1) y ya NO se llama "SCRAPE de Instagram" (`grep -c "SCRAPE de Instagram" SKILL.md` == 0).
    - El paso documenta las tres fuentes en orden: operador estructurado → web existente (WebFetch) → Instagram (instaloader). Verificable: el archivo menciona `WebFetch` y `instaloader` en la sección de fuentes (`grep -c "WebFetch" SKILL.md` >= 1 y `grep -c "instaloader" SKILL.md` >= 1).
    - El pipeline referencia el script del Plan 01: `grep -c "instagram-cosechar.py" SKILL.md` >= 1.
    - El paso documenta los prerrequisitos instaloader: menciona `browser_cookie3`, `firefox`/Firefox y "cuenta dedicada" (`grep -Ec "browser_cookie3|firefox|Firefox" SKILL.md` >= 1 y menciona cuenta dedicada).
    - El paso documenta el fallthrough: si IG falla, cae a la fuente (a) sin bloquearse (mención explícita de fallback/caer sin bloquear).
    - El archivo YA NO instruye instalar/usar Playwright como el motor activo de IG: `grep -Ec "Playwright" SKILL.md` == 0 O toda mención restante es histórica, no una instrucción de correr Playwright (verificar manualmente que no diga "corré el scrape desde instagram-a-web").
    - Se conservan las reglas no-regresión: el humanizador sigue obligatorio (`grep -c "humanizador" SKILL.md` >= 2 — paso 4 + reusos), el re-host sigue obligatorio (`grep -c "re-host" SKILL.md` >= 1 O menciona `landing-assets`), y la regla D5 "no inyectar HTML suelto" sigue en guardrails.
    - Los pasos 4-8 originales siguen presentes (`grep -c "### 5. ARMAR" SKILL.md` >= 1, `grep -c "### 8. OUTPUT" SKILL.md` >= 1).
  </acceptance_criteria>
  <done>El paso 3 es "FUENTES DE CONTENIDO" con las 3 fuentes en orden de confiabilidad, el pipeline instaloader inline reemplaza Playwright, las referencias cruzadas quedaron consistentes, y las reglas de humanizador/re-host/D5 no regresionaron.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| máquina del operador → DB producción | Toda escritura sigue por el script local service-role de v0.10; la skill NO abre endpoint web nuevo. |
| fuente externa (WebFetch / instaloader) → config | El copy/imágenes cosechados son materia prima local; nunca se persiste una URL remota no confiable en el config. |
| payload local (`landing-payloads/<slug>.json`) → checkpoint humano | El diff acotado (D-05) se muestra al operador antes de escribir; contiene solo copy/imágenes/tema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Information Disclosure | diff acotado del checkpoint de edición (D-05) | mitigate | El diff se computa sobre `landing-payloads/<slug>.json`, que contiene SOLO copy/imágenes/tema — cero secretos de tenant. El checkpoint muestra `campo: viejo → nuevo` de lo tocado, nunca credenciales ni columnas sensibles. Documentado como regla en la sección MODO EDICIÓN. |
| T-11-05 | Tampering | imágenes cosechadas (WebFetch/instaloader) | mitigate | Las imágenes van como RUTAS LOCALES; el script las re-hostea a `landing-assets/{businessId}/` (SKILL-03). NUNCA se persiste en el config una URL remota del CDN de IG/web (caducan + serían URL no confiable en runtime). Regla conservada en el cierre del paso FUENTES y en guardrails. |
| T-11-06 | Elevation of Privilege | superficie de escritura | accept | La fase NO abre superficie web nueva: toda escritura sigue por el script local service-role de v0.10 (SKILL-04) con el mismo checkpoint humano bloqueante pre-escritura. El modo edición reusa ese mismo write path e idempotencia; no agrega ruta autenticada ni anónima. |
| T-11-SC | Tampering | pip installs (instaloader, browser_cookie3) | accept | Documentados como prerrequisito LOCAL del operador, fuera del `package.json` y del runtime web. Ningún install entra al build del repo. `scripts/instagram-cosechar.py` (Plan 01) usa solo stdlib. |

Nota: riesgo de la fase = BAJO (per ROADMAP Threat note). La skill sigue escribiendo por el script local service-role con el mismo checkpoint humano; el `--inspect` extendido (Plan 01) es read-only; el contenido cosechado se re-hostea local (nunca hot-link).
</threat_model>

<verification>
- `grep -c "MODO EDICIÓN" SKILL.md` >= 1 y aparece entre "2. RESOLVER" y "FUENTES DE CONTENIDO".
- `grep -c "FUENTES DE CONTENIDO" SKILL.md` >= 1 y `grep -c "SCRAPE de Instagram" SKILL.md` == 0.
- `grep -c "instaloader" SKILL.md` >= 1, `grep -c "WebFetch" SKILL.md` >= 1, `grep -c "instagram-cosechar.py" SKILL.md` >= 1.
- `grep -Ec "Playwright" SKILL.md` == 0 (o solo histórico, no instrucción activa).
- `grep -c "humanizador" SKILL.md` >= 2 (no-regresión del gate obligatorio).
- Pasos 4-8 originales intactos (`### 5. ARMAR`, `### 6. CHECKPOINT`, `### 7. ESCRIBIR`, `### 8. OUTPUT` presentes).
</verification>

<success_criteria>
- La sección "MODO EDICIÓN" habilita retocar sin rehacer: payload→inspect→campo-único→diff-acotado→humanizador→re-escritura idempotente (SC1 + SC2 de SKILL-05).
- El paso "FUENTES DE CONTENIDO" documenta las 3 fuentes en orden de confiabilidad con fallthrough, e instaloader reemplaza Playwright como motor de IG (SC3 de SKILL-06).
- Humanizador obligatorio + re-host de imágenes local conservados sin regresión (SC4 de SKILL-06).
- Cero regresión en los pasos 1, 2, 4-8; la escritura sigue por el script local service-role.
</success_criteria>

<output>
Create `.planning/workstreams/web-builder/phases/11-skill-modo-edici-n-fuentes-de-contenido/11-02-SUMMARY.md` when done.
</output>
