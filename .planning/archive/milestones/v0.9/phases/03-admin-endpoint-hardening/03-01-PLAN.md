---
phase: 03-admin-endpoint-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/admin/set-plan/route.ts
  - app/api/admin/setup-plans/route.ts
  - scripts/setup-mp-plans.ts
  - package.json
autonomous: true
requirements: [SEC-03]
tags: [security, admin, mercadopago, timing-safe]

must_haves:
  truths:
    - "Un header x-admin-secret correcto en POST /api/admin/set-plan procesa el cambio de plan (200); uno incorrecto, vacío o de longitud distinta devuelve 401, nunca 500"
    - "El secreto admin NO se acepta por query string en NINGUNA ruta web (la ruta setup-plans que lo aceptaba ya no existe)"
    - "set-plan NO usa comparación no-constante (== / ===/ !==) contra ADMIN_SECRET; usa crypto.timingSafeEqual sobre hashes SHA-256 de ambos lados"
    - "El endpoint web app/api/admin/setup-plans ya no existe; su lógica de creación de preapproval_plan vive en el script local scripts/setup-mp-plans.ts"
    - "El script scripts/setup-mp-plans.ts se ejecuta localmente (sin admin-secret ni auth web), reusa SUBSCRIPTION_PLANS + mpFetch + MP_MODE, e imprime los IDs para copiar a env vars"
  artifacts:
    - path: "app/api/admin/set-plan/route.ts"
      provides: "Admin set-plan endpoint con auth header-only timing-safe (hash-both-sides)"
      contains: "timingSafeEqual"
    - path: "scripts/setup-mp-plans.ts"
      provides: "Script local que crea los 3 preapproval_plan en MP e imprime los IDs"
      contains: "preapproval_plan"
    - path: "package.json"
      provides: "devDependency tsx + npm script setup:mp-plans"
      contains: "setup:mp-plans"
  key_links:
    - from: "scripts/setup-mp-plans.ts"
      to: "lib/mercadopago.ts"
      via: "import { mpFetch, MP_MODE } — el script golpea /preapproval_plan con el token de plataforma"
      pattern: "mpFetch"
    - from: "scripts/setup-mp-plans.ts"
      to: "lib/subscription-plans.ts"
      via: "import { SUBSCRIPTION_PLANS } — reason + price_ars de cada plan"
      pattern: "SUBSCRIPTION_PLANS"
---

<objective>
Endurecer la autenticación de los endpoints admin (SEC-03, D-01/D-02/D-03). Dos superficies hoy débiles:
- `app/api/admin/set-plan/route.ts` — header-only pero con comparación `!==` (vulnerable a side-channel de timing) y un comentario vestigial sobre "Stripe webhook" (el proyecto usa MercadoPago).
- `app/api/admin/setup-plans/route.ts` — acepta el secreto por **query string** (`?secret=`), que queda en logs de acceso de Vercel, historial del browser, proxies y headers Referer.

Purpose: cerrar el agujero del secreto en query string eliminando por completo la superficie web de `setup-plans`, y eliminar el side-channel de timing en `set-plan` con una comparación en tiempo constante hash-both-sides. Es la última fase de endurecimiento de endpoints admin del milestone v0.9; no agrega features.

Output:
- `set-plan` con comparación timing-safe inline (sin helper compartido — es el único endpoint admin web que queda) + comentario en español limpio.
- `app/api/admin/setup-plans/route.ts` BORRADO.
- `scripts/setup-mp-plans.ts` nuevo (corre localmente, sin auth web).
- `tsx` como devDependency + npm script `setup:mp-plans`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-endpoint-hardening/03-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md

# Archivos objetivo y de reuso
@app/api/admin/set-plan/route.ts
@app/api/admin/setup-plans/route.ts
@lib/mercadopago.ts
@lib/subscription-plans.ts
@package.json

# Skills del proyecto (leer antes de tocar MP / multi-tenant)
@.claude/skills/mercadopago-suscripciones/SKILL.md
</context>

<artifacts_this_phase_produces>
**Nuevo:**
- `scripts/setup-mp-plans.ts` — script local de re-setup de los preapproval_plan en MercadoPago. NO está expuesto a la web, no tiene admin-secret. Lo corre el usuario a mano cuando hace falta (nuevo entorno, cambio de `MP_MODE`). Los IDs de plan ya viven en las env vars actuales de producción, así que crear este script NO cambia nada en producción — es capacidad de re-setup, no funcionalidad activa.

**Borrado:**
- `app/api/admin/setup-plans/route.ts` — elimina toda la superficie de ataque web, incluido el agujero del `?secret=`.

**Modificado:**
- `app/api/admin/set-plan/route.ts` — comparación timing-safe inline + comentario limpio.
- `package.json` — `tsx` en devDependencies + script `"setup:mp-plans"`.

**Nota operacional:** esta fase es 100% código, totalmente autónoma. No requiere checkpoint humano. El script `setup-mp-plans.ts` lo ejecuta el usuario más tarde a su criterio (no como parte de esta fase).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: set-plan — comparación admin-secret en tiempo constante (D-02) + limpieza de comentario (D-03)</name>
  <files>app/api/admin/set-plan/route.ts</files>
  <read_first>
    - app/api/admin/set-plan/route.ts — estructura actual completa: import de crypto NO existe todavía; el guard vive en líneas 9-13; el comentario "will be called by Stripe webhook" en línea 9; el shape de error es Response.json({ error: 'Unauthorized' }, { status: 401 }).
    - .planning/research/STACK.md sección "Constant-time comparison (SEC-03)" — el snippet safeEqual hash-both-sides exacto a replicar.
    - .planning/research/PITFALLS.md Pitfall 6 — por qué hash-both-sides en vez de length-guard para secretos de longitud arbitraria.
    - lib/mercadopago.ts líneas 68-72 — patrón existente de timingSafeEqual en el repo (OJO: ese compara buffers HMAC de longitud fija con length-guard; para el admin secret de longitud arbitraria NO sirve el length-guard — hay que hashear a 32 bytes fijos primero).
  </read_first>
  <action>
    Agregar `import crypto from 'crypto'` al tope del archivo (junto a los imports existentes).

    Definir un helper inline a nivel de módulo (NO en lib compartida — con setup-plans borrado, set-plan es el único endpoint admin web; D-02 pide inline) que compare dos strings en tiempo constante usando hash-both-sides: recibe `provided` (string | null | undefined) y `expected` (string | undefined); si `provided` es nulo/vacío O `expected` es nulo/vacío, devuelve false sin llamar a timingSafeEqual; en otro caso hashea ambos lados a SHA-256 con `crypto.createHash('sha256').update(...).digest()` (produce buffers de 32 bytes fijos) y los compara con `crypto.timingSafeEqual`. Hashear a longitud fija evita el RangeError ERR_CRYPTO_TIMING_SAFE_EQUAL_LENGTH (timingSafeEqual tira sobre longitudes distintas) y además no filtra la longitud del secreto real. NO hacer un length-guard previo (eso filtraría longitud y devolvería 500/inconsistencia — Pitfall 6).

    Reemplazar el guard de las líneas 11-13 (`if (!secret || secret !== process.env.ADMIN_SECRET)`) por una llamada al helper: si el helper devuelve false, retornar `Response.json({ error: 'Unauthorized' }, { status: 401 })` (mantener el shape y el status exactos). Seguir leyendo el secreto SOLO del header `x-admin-secret` (header-only ya lo es; no agregar lectura por query string).

    Limpieza D-03: borrar el comentario vestigial de la línea 9 "Authenticate with admin secret — will be called by Stripe webhook" (el proyecto usa MercadoPago, no Stripe). Reemplazarlo por un comentario en español, denso, que explique el porqué del timing-safe: que set-plan lo invoca un actor externo con `x-admin-secret`, que la comparación constante-en-tiempo evita un side-channel de adivinación del secreto byte a byte, y que se hashea a 32 bytes a ambos lados para que un header de longitud distinta devuelva 401 (no 500) sin filtrar la longitud del secreto.

    No tocar nada más del handler (validación de body, lookup de negocio, update, logging) — cambio mínimo (regla del repo: cambiar solo lo necesario).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "set-plan" ; test $? -eq 1 && echo "TYPECHECK_OK_set-plan"</automated>
  </verify>
  <acceptance_criteria>
    - `import crypto from 'crypto'` presente en set-plan/route.ts.
    - El handler usa `crypto.timingSafeEqual` sobre hashes SHA-256 de `provided` y `expected`; NO queda ningún `secret !== process.env.ADMIN_SECRET` ni comparación `==`/`===`/`!==` contra `ADMIN_SECRET`.
    - Guard de nulo/vacío antes de hashear: un `x-admin-secret` ausente o vacío devuelve 401 sin llamar a timingSafeEqual.
    - El shape de respuesta de fallo sigue siendo `Response.json({ error: 'Unauthorized' }, { status: 401 })`.
    - El secreto se lee SOLO del header `x-admin-secret` (no hay `searchParams.get('secret')` ni lectura por query string en este archivo).
    - El comentario "will be called by Stripe webhook" ya no existe; en su lugar hay un comentario en español explicando el timing-safe.
    - `npx tsc --noEmit` no reporta errores nuevos en este archivo.
  </acceptance_criteria>
  <done>
    POST /api/admin/set-plan con `x-admin-secret` correcto procesa el cambio de plan (200); con header incorrecto, vacío o de longitud distinta devuelve 401 (nunca 500). La comparación es en tiempo constante hash-both-sides. El comentario sobre Stripe está eliminado.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Borrar setup-plans web (D-01) + crear scripts/setup-mp-plans.ts local</name>
  <files>app/api/admin/setup-plans/route.ts, scripts/setup-mp-plans.ts</files>
  <read_first>
    - app/api/admin/setup-plans/route.ts — la lógica completa a portar: BACK_URL (NEXT_PUBLIC_APP_URL + '/dashboard'), SUFFIX por MP_MODE ('_TEST' si test), ENV_KEYS por plan, el loop sobre SUBSCRIPTION_PLANS que hace POST /preapproval_plan con reason/auto_recurring(frequency 1, months, transaction_amount=price_ars, currency_id ARS)/back_url, y la impresión de los IDs + env_var + status. El agujero a eliminar: línea 16 lee el secreto de searchParams.
    - lib/mercadopago.ts — `mpFetch` (líneas 166-177, usa el token de plataforma vía getMPAccessToken, ya respeta MP_MODE) y `MP_MODE` (línea 8). El script los importa, no los duplica.
    - lib/subscription-plans.ts — `SUBSCRIPTION_PLANS` (name, price_ars por plan). El script lo importa.
    - .planning/research/PITFALLS.md Pitfall 7 — por qué borrar el endpoint entero (no solo el compare) cierra el agujero del query string.
    - .claude/skills/mercadopago-suscripciones/SKILL.md — patrón validado de MP (preapproval, MP_MODE); el script crea preapproval_plan, no escribe plan_status.
  </read_first>
  <action>
    BORRAR el archivo `app/api/admin/setup-plans/route.ts` por completo (usar `git rm` o eliminar el archivo). Esto elimina toda la superficie de ataque web, incluido el `?secret=` que filtraba el secreto a los logs de acceso (Pitfall 7 / D-01). No queda endpoint web para crear los planes.

    Crear `scripts/setup-mp-plans.ts` portando la lógica del endpoint borrado, SIN ninguna auth (el script corre localmente en Node, fuera del runtime web — Pitfall 7: la transferencia segura es ejecutarlo a mano con los secretos en env, no exponerlo a la web). El script debe:
    - Importar `mpFetch` y `MP_MODE` de `@/lib/mercadopago` y `SUBSCRIPTION_PLANS` de `@/lib/subscription-plans` (reusa la lógica de lib vía el alias `@/*`; el runner tsx lo resuelve — ver Task 3). NO duplicar la lógica de MP.
    - Calcular `BACK_URL` (`process.env.NEXT_PUBLIC_APP_URL || 'https://gestion.forjo.studio'` + `/dashboard`), `SUFFIX` (`_TEST` si `MP_MODE === 'test'`, vacío si no) y el mapa de env-var por plan (`MP_PLAN_BASIC_ID${SUFFIX}`, `MP_PLAN_STUDIO_ID${SUFFIX}`, `MP_PLAN_PRO_ID${SUFFIX}`), igual que el endpoint borrado.
    - Recorrer `SUBSCRIPTION_PLANS` y por cada plan hacer `mpFetch('/preapproval_plan', { method: 'POST', body: JSON.stringify({ reason, auto_recurring: { frequency: 1, frequency_type: 'months', transaction_amount: plan.price_ars, currency_id: 'ARS' }, back_url: BACK_URL }) })` — mismos campos que el endpoint borrado.
    - Imprimir por consola (con `console.log`) el modo `MP_MODE`, y por cada plan el `id` resultante junto al nombre de la env-var donde copiarlo (o el error si MP no devolvió `id`). Cerrar con un mensaje claro en español: "Copiá estos IDs en Vercel como variables de entorno y volvé a desplegar".
    - Envolver el flujo en una función `async function main()` invocada al final (`main().catch(...)` con `process.exitCode = 1` en error), porque un script top-level necesita un punto de entrada ejecutable.
    - Encabezado de comentario en español denso explicando: que este script reemplaza al endpoint web borrado (D-01), que crea/recrea los 3 preapproval_plan en MP, que respeta `MP_MODE`/sufijo `_TEST`, que se corre a mano (`npm run setup:mp-plans`) con los secretos de MP en el entorno, que NO tiene auth porque no está expuesto a la web, y que los IDs que imprime se copian a las env vars de Vercel.

    Verificar que el script respete `MP_MODE` y el sufijo `_TEST` (criterio de la discreción del CONTEXT). NO escribir `plan_status` ni tocar la DB — el script solo crea planes en MP e imprime IDs (regla de oro del skill de MP: el estado lo escribe solo el webhook).
  </action>
  <verify>
    <automated>test ! -f app/api/admin/setup-plans/route.ts && test -f scripts/setup-mp-plans.ts && grep -q "preapproval_plan" scripts/setup-mp-plans.ts && grep -vq "searchParams" scripts/setup-mp-plans.ts && echo "SETUP_SCRIPT_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/admin/setup-plans/route.ts` NO existe.
    - `scripts/setup-mp-plans.ts` existe e importa `mpFetch`/`MP_MODE` de `@/lib/mercadopago` y `SUBSCRIPTION_PLANS` de `@/lib/subscription-plans` (no duplica la lógica de MP).
    - El script hace POST a `/preapproval_plan` por cada plan con los mismos campos del endpoint borrado (reason, auto_recurring mensual, transaction_amount=price_ars, currency_id ARS, back_url).
    - El script respeta `MP_MODE` y el sufijo `_TEST` al calcular los nombres de env-var.
    - El script NO lee ningún secreto por query string ni tiene chequeo de admin-secret (corre local, sin auth web).
    - El script imprime los IDs resultantes + el nombre de la env-var donde copiarlos.
    - El script NO escribe `plan_status` ni toca la base de datos.
  </acceptance_criteria>
  <done>
    El endpoint web `setup-plans` (y su agujero `?secret=`) ya no existe. La capacidad de crear/recrear los preapproval_plan vive en `scripts/setup-mp-plans.ts`, ejecutable localmente, que reusa la lógica de lib y respeta `MP_MODE`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Runner del script — tsx devDep + npm script setup:mp-plans (discreción)</name>
  <files>package.json</files>
  <read_first>
    - package.json — scripts actuales (dev/build/start/lint) en líneas 5-10; devDependencies (líneas 36-45) — NO hay tsx ni ts-node, solo typescript. Hay que agregar tsx para que el script pueda importar `@/lib/*` (el alias del tsconfig) y correr TypeScript directo sin compilar.
    - .planning/phases/03-admin-endpoint-hardening/03-CONTEXT.md "Claude's Discretion" — recomendación explícita: tsx devDep + npm script (reusa la lógica de lib sin duplicar), preferido sobre script autocontenido.
    - .planning/research/STACK.md — confirma que NO se debe agregar dependencias de runtime nuevas para SEC-03; tsx es una devDependency de tooling local, no de runtime de la app (no afecta el bundle de producción).
  </read_first>
  <action>
    Editar `package.json` (Edit parcial, NO reescribir el archivo):
    - Agregar `tsx` a `devDependencies` con un rango de versión estable (`^4`). tsx ejecuta TypeScript directo y resuelve el alias `@/*` del tsconfig vía su integración con tsconfig paths, lo que permite que `scripts/setup-mp-plans.ts` importe `@/lib/mercadopago` y `@/lib/subscription-plans` sin compilar ni duplicar lógica.
    - Agregar a `scripts` la entrada `"setup:mp-plans": "tsx scripts/setup-mp-plans.ts"`.

    Mantener el orden alfabético/de inserción coherente con el archivo (no reordenar el resto de las claves). NO correr `npm install` como parte de esta tarea (el usuario lo correrá cuando vaya a usar el script; el lockfile se actualiza al instalar). Documentar en el SUMMARY que para usar el script hay que `npm install` primero (instala tsx) y luego `npm run setup:mp-plans` con los secretos de MP en el entorno (`.env.local` o exportados).

    Nota sobre tsx + `@/*`: tsx respeta `paths` del `tsconfig.json` (alias `@/*` → raíz). Si el ejecutor verifica que el alias no resuelve bajo tsx, la alternativa documentada en CONTEXT es hacer el script autocontenido sin imports de alias; preferir tsx + alias (recomendación del CONTEXT) y dejar la alternativa solo como fallback documentado, sin implementarla salvo que el alias falle.
  </action>
  <verify>
    <automated>grep -q '"setup:mp-plans"' package.json && grep -q '"tsx"' package.json && node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))" && echo "PKG_JSON_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` tiene `"tsx"` en `devDependencies` con un rango de versión válido.
    - `package.json` tiene `"setup:mp-plans": "tsx scripts/setup-mp-plans.ts"` en `scripts`.
    - `package.json` sigue siendo JSON válido (parsea sin error).
    - No se agregó ninguna dependencia de runtime (tsx es devDependency).
    - No se reordenaron ni eliminaron claves existentes; solo se agregaron las dos entradas.
  </acceptance_criteria>
  <done>
    El script es ejecutable con `npm run setup:mp-plans` (tras `npm install` que instala tsx). El runner reusa la lógica de `@/lib/*` sin duplicarla; tsx es devDependency, no toca el bundle de producción.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| actor externo → `/api/admin/set-plan` | Input no confiable (header `x-admin-secret`) cruza al runtime Node; el secreto es la única credencial. El endpoint puede cambiar el plan/estado de cualquier negocio. |
| (eliminado) actor externo → `/api/admin/setup-plans` | El boundary web de setup-plans SE ELIMINA en esta fase (D-01). El secreto ya no transita por la web para esta operación. |
| operador local → `scripts/setup-mp-plans.ts` | El script corre en la máquina del operador (Node local), lee secretos de MP del entorno. No es un boundary de red; la confianza es la del operador local. |
| `scripts/setup-mp-plans.ts` → API de MercadoPago | Outbound al token de plataforma de Forjo; crea preapproval_plan. Sin entrada de cliente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | comparación admin-secret en `set-plan` (`!==`) | mitigate | Reemplazar `!==` por `crypto.timingSafeEqual` sobre hashes SHA-256 de ambos lados (hash-both-sides), eliminando el side-channel de timing que permitía adivinar el secreto byte a byte (Task 1, D-02, Pitfall 6). |
| T-03-02 | Information Disclosure | secreto admin vía `?secret=` en `setup-plans` (logs de acceso / historial / Referer) | mitigate | BORRAR el endpoint web `setup-plans` por completo (Task 2, D-01); el secreto deja de transitar por la web para esta operación. La capacidad de re-setup pasa a un script local sin auth web (Pitfall 7). |
| T-03-03 | Denial of Service | `timingSafeEqual` con buffers de longitud distinta → RangeError → 500 | mitigate | Hashear ambos lados a 32 bytes fijos antes de comparar: un header de longitud arbitraria nunca dispara el RangeError; un secreto basura devuelve 401, no 500 (Task 1, Pitfall 6). |
| T-03-04 | Spoofing | request sin/ con `x-admin-secret` vacío a `set-plan` | mitigate | Guard de nulo/vacío antes de hashear: ausente/vacío → 401 sin llamar a timingSafeEqual (Task 1). |
| T-03-05 | Elevation of Privilege | mover handlers admin a Edge runtime perdería `crypto` síncrono | accept | No se cambia el runtime; los handlers admin siguen en Node (donde `crypto.timingSafeEqual`/`createHash` existen). RESEARCH (STACK.md) advierte explícitamente NO mover a Edge. Sin cambio de runtime en esta fase. |
| T-03-SC | Tampering | install de `tsx` (npm devDependency) | mitigate | `tsx` es un paquete tooling ampliamente usado (mantenido por el equipo de esbuild/privatenumber). Es devDependency (no entra al bundle de producción). NO se corre `npm install` en CI durante esta fase; el operador instala localmente al usar el script. Sin paquetes `[ASSUMED]`/`[SUS]` nuevos de runtime. |
</threat_model>

<verification>
## Verificación de fase (SEC-03)

1. **Timing-safe en set-plan:** `grep -n "timingSafeEqual" app/api/admin/set-plan/route.ts` devuelve match; `grep -nE "secret\s*(===|!==|==)\s*" app/api/admin/set-plan/route.ts` NO matchea una comparación directa contra `ADMIN_SECRET`.
2. **Sin query string en ninguna ruta admin web:** `app/api/admin/setup-plans/route.ts` no existe; `grep -rn "searchParams.get('secret')" app/api/admin/` no devuelve nada.
3. **Script local existe y reusa lib:** `scripts/setup-mp-plans.ts` existe, importa `mpFetch`/`SUBSCRIPTION_PLANS`, hace POST a `/preapproval_plan`, respeta `MP_MODE`/`_TEST`.
4. **Runner:** `package.json` tiene `tsx` en devDependencies y `setup:mp-plans` en scripts; parsea como JSON válido.
5. **Typecheck:** `npx tsc --noEmit -p tsconfig.json` no reporta errores nuevos en `set-plan/route.ts` ni en `scripts/setup-mp-plans.ts`.
6. **Lint:** `npm run lint` pasa sin errores nuevos.
</verification>

<success_criteria>
Medible (mapea a los Success Criteria de la Fase 3 en ROADMAP):
1. `setup-plans` ya no acepta el secreto por query string — porque el endpoint web ya no existe (D-01).
2. `set-plan` compara el secreto con `crypto.timingSafeEqual` usando hash-both-sides (SHA-256 a ambos lados) (D-02).
3. Un header de secreto basura o de longitud distinta devuelve 401, nunca un 500 por `ERR_CRYPTO_TIMING_SAFE_EQUAL_LENGTH`.
4. El secreto admin no puede aparecer en los logs de acceso de Vercel para `setup-plans` (ruta eliminada) ni para `set-plan` (header-only, no query string).
5. La capacidad de crear los preapproval_plan se preserva en `scripts/setup-mp-plans.ts`, ejecutable localmente.
</success_criteria>

<output>
Create `.planning/phases/03-admin-endpoint-hardening/03-01-SUMMARY.md` when done.
</output>
