---
phase: 02-mp-webhook-signature-amount-check
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mercadopago.ts
  - app/api/subscription/webhook/route.ts
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - "verifyMPSignature vive en lib/mercadopago.ts como export reusable"
    - "El webhook de suscripción sigue rechazando firmas inválidas con 401 (comportamiento sin cambios) importando el verificador compartido"
    - "No queda una copia inline de verifyMPSignature duplicada en el repo"
  prohibitions:
    - "El webhook de suscripción NO cambia su comportamiento observable (sigue 401 en firma inválida, 200 en bad JSON)"
    - "No quedan imports sin usar (crypto / getMPWebhookSecret) en el webhook de suscripción tras el move"
  artifacts:
    - path: "lib/mercadopago.ts"
      provides: "verifyMPSignature exportado, junto a getMPWebhookSecret"
      contains: "export function verifyMPSignature"
    - path: "app/api/subscription/webhook/route.ts"
      provides: "Webhook de suscripción importando el verificador compartido"
      contains: "import { mpFetch, verifyMPSignature } from '@/lib/mercadopago'"
  key_links:
    - from: "app/api/subscription/webhook/route.ts"
      to: "lib/mercadopago.ts"
      via: "import { verifyMPSignature } from '@/lib/mercadopago'"
      pattern: "import.*verifyMPSignature.*from '@/lib/mercadopago'"
---

<objective>
Extraer `verifyMPSignature` (hoy inline en el webhook de suscripción §18-54) a `lib/mercadopago.ts` como `export`, y repuntar el webhook de suscripción para importarlo — SIN cambiar su comportamiento (D-03). Esta es la base compartida que el webhook de seña importará en el Plan 02.

Purpose: Una sola fuente de verdad para la verificación HMAC de firma de MercadoPago, eliminando la duplicación inline antes de que el webhook de seña la consuma. Cierra la mitad "verificador compartido" de SEC-02.
Output: `verifyMPSignature` exportado desde `lib/mercadopago.ts`; el webhook de suscripción importándolo sin copia inline y sin imports muertos.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mp-webhook-signature-amount-check/02-CONTEXT.md
@.planning/phases/02-mp-webhook-signature-amount-check/02-RESEARCH.md
@.planning/phases/02-mp-webhook-signature-amount-check/02-PATTERNS.md

# Skill validado de pagos MP (errores a no repetir)
@.claude/skills/mercadopago-suscripciones/SKILL.md

# Fuente verbatim de la función a mover y del call site que no cambia
@app/api/subscription/webhook/route.ts
@lib/mercadopago.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mover verifyMPSignature a lib/mercadopago.ts (verbatim + export)</name>
  <files>lib/mercadopago.ts</files>
  <read_first>
    - app/api/subscription/webhook/route.ts §1 (import crypto), §5 (import type NextRequest), §12-54 (doc-comment + verifyMPSignature a mover)
    - lib/mercadopago.ts §1-26 (top del archivo; getMPWebhookSecret ya vive en §21-26 — destino de la inserción)
    - .planning/phases/02-mp-webhook-signature-amount-check/02-PATTERNS.md §18-31 (pattern de extracción)
  </read_first>
  <action>
    Agregar al tope de `lib/mercadopago.ts` (que hoy NO los tiene) los imports: `import crypto from 'crypto'` y `import type { NextRequest } from 'next/server'`. El `import type` no agrega runtime; `crypto` es built-in de Node y el módulo ya es server-only (hace fetch + lee process.env), así que es válido (per RESEARCH §177).
    Insertar `verifyMPSignature` INMEDIATAMENTE después de `getMPWebhookSecret()` (su única dependencia, §21-26) — verbatim desde el webhook de suscripción §18-54, cambiando SOLO `function verifyMPSignature` por `export function verifyMPSignature`. La firma queda `(request: NextRequest, dataId: string | null | undefined): boolean`. Mover también el bloque de comentario doc que la precede (§12-17). NO tocar la lógica: manifest `id:<id>;request-id:<reqid>;ts:<ts>;` (omitiendo partes ausentes), `crypto.createHmac('sha256', secret)`, guard de longitud + `crypto.timingSafeEqual`, fail-closed (secreto ausente → `console.error` + `return false`; `x-signature`/`ts`/`v1` ausente → `return false`). Per D-03.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`lib/mercadopago.ts` exporta `verifyMPSignature` con la firma `(request: NextRequest, dataId) => boolean`, ubicada tras `getMPWebhookSecret`, con `crypto` y `NextRequest` importados al tope. `npx tsc --noEmit` pasa sin errores.</done>
</task>

<task type="auto">
  <name>Task 2: Repuntar el webhook de suscripción al verificador compartido (sin cambiar comportamiento)</name>
  <files>app/api/subscription/webhook/route.ts</files>
  <read_first>
    - app/api/subscription/webhook/route.ts (completo — para borrar la copia inline §12-54 y ajustar imports §1,§4)
    - .planning/phases/02-mp-webhook-signature-amount-check/02-PATTERNS.md §89-95 (import refactor only)
  </read_first>
  <action>
    Borrar la implementación inline de `verifyMPSignature` (§18-54) y su bloque de comentario doc (§12-17). Ajustar la línea de import existente §4 a `import { mpFetch, verifyMPSignature } from '@/lib/mercadopago'` — agregando `verifyMPSignature` y QUITANDO `getMPWebhookSecret` (ya no se usa en este archivo tras el move; dejarlo dispara error de import sin usar en ESLint core-web-vitals). Borrar también `import crypto from 'crypto'` (§1) SOLO si `crypto` no se usa en ningún otro lugar del archivo (verificar: grep `crypto` en el archivo tras borrar la función — debe dar 0 usos restantes). NO tocar el call site §68 (`verifyMPSignature(request, dataIdQuery ?? body.data?.id)`) ni ninguna otra línea: el comportamiento del webhook de suscripción NO cambia (sigue fuera de scope, CONTEXT §13). `import type { NextRequest }` §5 se mantiene (el handler lo sigue tipando).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>El webhook de suscripción ya no contiene `verifyMPSignature` inline; importa `verifyMPSignature` (y `mpFetch`) de `@/lib/mercadopago` sin importar `getMPWebhookSecret` ni `crypto` (a menos que `crypto` siga en uso). El call site §68 queda intacto. `npx tsc --noEmit` y `npm run lint` pasan sin errores ni warnings de imports sin usar.</done>
  <acceptance_criteria>
    - `grep -c 'function verifyMPSignature' app/api/subscription/webhook/route.ts` da 0 (no queda copia inline).
    - `grep -c 'getMPWebhookSecret' app/api/subscription/webhook/route.ts` da 0 (import muerto removido).
    - El archivo importa `verifyMPSignature` de `@/lib/mercadopago`.
    - `npm run lint` no reporta imports sin usar.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MercadoPago → webhook route handler | MP (incluidas cuentas conectadas por Connect/OAuth) POSTea notificaciones; el origen es no confiable hasta validar la firma HMAC contra el secreto de la APLICACIÓN de Forjo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | `verifyMPSignature` (extracción) | mitigate | Mover verbatim preserva el algoritmo ya validado en prod (manifest, lowercasing, guard de longitud, `timingSafeEqual`, fail-closed). Un cambio de lógica reintroduciría bugs que el verificador de suscripción ya evitó — por eso el move es verbatim (RESEARCH §223). |
| T-02-02 | Tampering | webhook de suscripción (refactor de import) | accept | El comportamiento del webhook de suscripción NO cambia (solo la fuente del símbolo). Riesgo de regresión acotado por `tsc` + `lint` + el call site sin tocar. Sin superficie de ataque nueva. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (solo `crypto` built-in y módulos ya presentes). Sin superficie de slopsquatting (RESEARCH §83). |
</threat_model>

<verification>
- `npx tsc --noEmit` pasa (la extracción no rompió tipos ni imports).
- `npm run lint` pasa sin warnings de imports sin usar.
- `npm run build` compila ambos webhooks (verificación de cierre de fase — se corre tras Plan 02, ya que el webhook de seña aún no importa el símbolo en este wave).
</verification>

<success_criteria>
- `verifyMPSignature` exportado desde `lib/mercadopago.ts`, ubicado junto a `getMPWebhookSecret`.
- El webhook de suscripción importa el verificador compartido y no tiene copia inline ni imports muertos.
- Comportamiento del webhook de suscripción inalterado (call site §68 intacto).
</success_criteria>

<output>
Create `.planning/phases/02-mp-webhook-signature-amount-check/02-01-SUMMARY.md` when done
</output>
