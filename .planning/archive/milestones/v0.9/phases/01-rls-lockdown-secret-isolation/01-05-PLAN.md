---
phase: 01-rls-lockdown-secret-isolation
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - supabase/migrations/028_lock_base_tables_and_drop_secret_columns.sql
  - lib/business-secrets.ts
  - .planning/phases/01-rls-lockdown-secret-isolation/ROTATION-RUNBOOK.md
autonomous: false
requirements: [SEC-01]
tags: [rls, supabase, secrets, migration, destructive, rotation]
must_haves:
  truths:
    - "Tras 028, anon no puede SELECT de columnas sensibles sobre businesses, services ni business_hours (las policies USING(true) ya no existen)"
    - "Las 7 columnas-secreto ya no existen en la tabla businesses (DROP COLUMN)"
    - "getBusinessSecrets ya no consulta businesses como fallback (el shim de transición se removió tras confirmar 028 live)"
    - "Existe un runbook de rotación de claves (D-06=ROTAR) documentando la acción operativa del usuario fuera del repo"
  prohibitions:
    - "028 NO se aplica antes de que el código de las olas 2 (planes 02/03/04) esté deployado y verificado live (checkpoint blocking)"
    - "El DROP POLICY usa los nombres EXACTOS 'public read services' y 'public read hours' (NO 'public read business_hours')"
    - "La remoción del fallback NO ocurre antes de que 028 esté aplicada y confirmada live"
  artifacts:
    - path: "supabase/migrations/028_lock_base_tables_and_drop_secret_columns.sql"
      provides: "Migración destructiva: DROP POLICY abiertas + DROP de las 7 columnas-secreto"
      contains: "DROP POLICY IF EXISTS \"public read services\""
    - path: ".planning/phases/01-rls-lockdown-secret-isolation/ROTATION-RUNBOOK.md"
      provides: "Runbook de rotación de claves expuestas (D-06)"
      contains: "rotar"
  key_links:
    - from: "supabase/migrations/028_lock_base_tables_and_drop_secret_columns.sql"
      to: "services / business_hours"
      via: "DROP POLICY de las policies abiertas USING(true)"
      pattern: "DROP POLICY IF EXISTS \"public read (services|hours)\""
    - from: "lib/business-secrets.ts"
      to: "business_secrets"
      via: "getBusinessSecrets lee solo business_secrets tras remover el fallback a businesses"
      pattern: "from\\('business_secrets'\\)"
---

<objective>
Cerrar el agujero de forma definitiva (SEC-01, D-02 paso d): la migración DESTRUCTIVA `028` que dropea las policies abiertas `USING(true)` sobre `services`/`business_hours` y elimina las 7 columnas-secreto de `businesses`; más la remoción del shim de fallback en `getBusinessSecrets` (tras confirmar 028 live) y el runbook de rotación de claves (D-06 = ROTAR).

028 NO se aplica hasta que el código de las olas 2 (planes 02/03/04) esté deployado y verificado live — por eso este plan empieza con un `checkpoint:human-verify` (smoke test) que GATEA el `[BLOCKING]` runbook de 028. Esto materializa el build-order seguro (additive-first, code live, recién destructivo) que es el riesgo más alto de la fase.

Purpose: Sin 028, las policies abiertas siguen exponiendo las tablas base a anon y los secretos siguen físicamente en businesses (Pitfall G: copiar no es mover). Sin el runbook de rotación, las claves que ya se filtraron en producción siguen siendo válidas.
Output: migración 028, getBusinessSecrets sin fallback, ROTATION-RUNBOOK.md.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-rls-lockdown-secret-isolation/01-CONTEXT.md
@.planning/phases/01-rls-lockdown-secret-isolation/01-RESEARCH.md
@.planning/phases/01-rls-lockdown-secret-isolation/01-PATTERNS.md

# Patrón de DROP POLICY (mitad destructiva) y nombres exactos de policies
@supabase/migrations/026_public_businesses_view.sql
@supabase/schema.sql
@supabase/migrations/README.md

# El shim a remover (creado en 01-01)
@lib/business-secrets.ts
@AGENTS.md
</context>

<artifacts_this_phase_produces>
Crea: `supabase/migrations/028_lock_base_tables_and_drop_secret_columns.sql`, `.planning/phases/01-rls-lockdown-secret-isolation/ROTATION-RUNBOOK.md`. Modifica: `lib/business-secrets.ts` (remueve el fallback). Cierra los artefactos de la fase: business_secrets queda como única fuente de secretos; public_services/public_business_hours como única lectura pública; las policies USING(true) eliminadas de las tres tablas (businesses ya fue en 026).
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>El código de las olas 2 (planes 01-02, 01-03, 01-04) que lee/escribe secretos desde business_secrets (con fallback) y lee servicios desde public_services. La migración aditiva 027 ya está aplicada (checkpoint del plan 01-01). FALTA verificar que TODO funciona live ANTES de correr la migración destructiva 028.</what-built>
  <how-to-verify>
    Smoke test en producción (o staging con datos reales) — el código de las olas 2 ya debe estar deployado en Vercel y 027 aplicada:

    1. Página pública: abrí `/[slug]` de un negocio real. Debe renderizar nombre, servicios y disponibilidad. (Confirma public_services + public_businesses + public_professionals.)
    2. Pago de seña end-to-end: hacé una reserva con seña de un negocio que tenga MercadoPago configurado y completá el pago de prueba. El turno debe pasar a `confirmed` y llegar el email de confirmación. (Confirma webhook → getBusinessSecrets → MP token + Resend desde business_secrets.)
    3. Dashboard del dueño: entrá a Settings de un negocio. Los campos de MP token / Resend / reCAPTCHA deben MOSTRAR los valores guardados (leídos de business_secrets). Cambiá uno y guardá; recargá → el valor persiste. (Confirma read server-side + upsert owner-RLS a business_secrets.)
    4. Google Calendar: en Agenda, el estado "conectado/desconectado" debe reflejarse correcto (booleano de presencia). Si hay un negocio conectado, una reserva confirmada debería crear el evento. (Confirma getBusinessSecrets owner-side + service-role.)
    5. (Opcional) Probe REST con anon key: `select=mp_access_token` sobre `businesses` TODAVÍA devuelve el valor (las columnas siguen existiendo pre-028) — esto es esperado en este paso; 028 lo cierra.

    Si algo de 1-4 falla, NO correr 028: hay un repunte mal hecho en las olas 2. Reportá qué falló.
  </how-to-verify>
  <resume-signal>Escribí "live OK, correr 028" cuando 1-4 pasen, o describí qué flujo falló.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Migración 028 destructiva — DROP policies abiertas + DROP columnas-secreto</name>
  <files>supabase/migrations/028_lock_base_tables_and_drop_secret_columns.sql</files>
  <read_first>
    - supabase/schema.sql:144-149 (nombres EXACTOS: "public read services" y "public read hours" — NO "public read business_hours")
    - supabase/migrations/026_public_businesses_view.sql (patrón del DROP POLICY + header de seguridad en español)
    - supabase/migrations/README.md (convención de excepción de no-destructivo marcada con ⚠ — 028 la rompe deliberadamente y con review)
    - 01-RESEARCH.md §3 paso 4 (DROP COLUMN recomendado vs UPDATE NULL) y Pitfall A (nombre exacto de la policy)
  </read_first>
  <action>
    Crear la migración destructiva (D-02 paso d). Header en español al estilo de 026 explicando que es la mitad DESTRUCTIVA del fix, que solo debe correrse DESPUÉS de que 027 esté aplicada y el código que lee de business_secrets esté live y verificado (referenciar el checkpoint anterior), y marcando con ⚠ que rompe deliberadamente la convención "nada destructivo" del README (con review). Contenido:

    1. DROP POLICY IF EXISTS "public read services" ON services; — nombre exacto verificado en schema.sql:144.
    2. DROP POLICY IF EXISTS "public read hours" ON business_hours; — nombre exacto schema.sql:147 (⚠ NO "public read business_hours" — un nombre equivocado deja el DROP como no-op silencioso y el hueco abierto, Pitfall A).
    3. DROP de las 7 columnas-secreto en businesses (recomendado por RESEARCH §3: DROP COLUMN, los datos ya están en business_secrets desde 027 y 026 ya quitó la lectura anon de businesses): ALTER TABLE businesses DROP COLUMN IF EXISTS mp_access_token; (y lo mismo para mp_refresh_token, mp_token_expires_at, resend_api_key, resend_from, recaptcha_secret_key, google_refresh_token). NO dropear mp_user_id, recaptcha_site_key ni notification_email (no son secretos). Comentar cada bloque en español.

    Idempotente (DROP POLICY IF EXISTS, DROP COLUMN IF EXISTS). Incluir al final, comentadas, las queries de verificación de RESEARCH §3 (pg_class.relrowsecurity, pg_policy por relname, \d businesses) para que el humano las corra tras aplicar.
  </action>
  <verify>
    <automated>grep -q 'DROP POLICY IF EXISTS "public read services" ON services' supabase/migrations/028_lock_base_tables_and_drop_secret_columns.sql && grep -q 'DROP POLICY IF EXISTS "public read hours" ON business_hours' supabase/migrations/028_lock_base_tables_and_drop_secret_columns.sql && ! grep -q '"public read business_hours"' supabase/migrations/028_lock_base_tables_and_drop_secret_columns.sql && [ "$(grep -c 'DROP COLUMN IF EXISTS' supabase/migrations/028_lock_base_tables_and_drop_secret_columns.sql)" -ge 7 ] && echo "OK: 028 dropea policies con nombre exacto + 7 columnas" || (echo "FALLO: revisar 028" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - 028 dropea exactamente "public read services" y "public read hours" (nombres verificados), nunca "public read business_hours".
    - 028 dropea las 7 columnas-secreto de businesses con DROP COLUMN IF EXISTS; no toca mp_user_id/recaptcha_site_key/notification_email.
    - Header en español marca la excepción destructiva con ⚠ y referencia el orden seguro (post-027, post-deploy, post-verify).
  </acceptance_criteria>
  <done>La migración destructiva existe, con los nombres de policy exactos y el drop de las 7 columnas, lista para aplicarse a mano tras el smoke test.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>Migración destructiva `028_lock_base_tables_and_drop_secret_columns.sql`. Igual que 027, se aplica A MANO en el SQL editor de Supabase (no hay Supabase CLI — backlog v2). Este es el paso que efectivamente CIERRA el agujero.</what-built>
  <how-to-verify>
    RUNBOOK — aplicar 028 a mano (Windows / PowerShell-friendly; SQL pegado en el editor). SOLO correr esto si el checkpoint anterior dio "live OK, correr 028":

    1. Confirmá una vez más que el smoke test (checkpoint previo) pasó: /[slug] carga, un pago de seña confirma, settings guarda/lee secretos. Si algo falla, NO sigas.
    2. (Recomendado) Tomá un backup/snapshot de la base en Supabase (Dashboard → Database → Backups) antes de un DROP COLUMN irreversible.
    3. Abrí el SQL editor de Supabase de PRODUCCIÓN.
    4. Pegá y ejecutá el contenido COMPLETO de `supabase/migrations/028_lock_base_tables_and_drop_secret_columns.sql`.
    5. Corré las queries de verificación (están comentadas al final de 028):
       - `SELECT polname FROM pg_policy WHERE polrelid = 'services'::regclass;` → NO debe aparecer `public read services`.
       - `SELECT polname FROM pg_policy WHERE polrelid = 'business_hours'::regclass;` → NO debe aparecer `public read hours`.
       - `SELECT relrowsecurity FROM pg_class WHERE relname IN ('services','business_hours','business_secrets');` → todas `true`.
       - `\d businesses` (o un SELECT de information_schema.columns) → las 7 columnas-secreto YA NO existen; mp_user_id, recaptcha_site_key, notification_email SIGUEN.
    6. Probe de aislamiento con ANON key (la prueba real del fix): intentá `select=mp_access_token` sobre `businesses` y un `select=*` sobre `services` con la anon key vía REST. Ambos deben fallar/devolver vacío (columna inexistente / sin policy de lectura). /[slug] debe seguir cargando (lee las vistas).
    7. Si todo pasa: el agujero está cerrado. Continuar con la remoción del fallback (Task 2) y leer el ROTATION-RUNBOOK (Task 3).
  </how-to-verify>
  <resume-signal>Escribí "028 aplicada, anon denegado" cuando las verificaciones pasen, o describí el error.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remover el shim de fallback de getBusinessSecrets</name>
  <files>lib/business-secrets.ts</files>
  <read_first>
    - lib/business-secrets.ts (el fallback a businesses agregado en 01-01)
    - 01-RESEARCH.md §3 paso 5 + Open Question 2 (remover el fallback tras confirmar 028 live)
  </read_first>
  <action>
    Tras confirmar 028 aplicada y anon denegado (checkpoint anterior), remover el bloque de fallback de getBusinessSecrets: la segunda query que lee las 7 columnas de businesses. getBusinessSecrets pasa a leer SOLO business_secrets (.maybeSingle()) y devolver { ...EMPTY, ...secrets } o EMPTY si no hay fila. Actualizar el comentario en español: el fallback se removió porque las columnas de businesses ya no existen tras 028 (el fallback era el shim de transición). Mantener la firma y el resto del módulo igual.
  </action>
  <verify>
    <automated>! grep -nE "\.from\('businesses'\)" lib/business-secrets.ts && grep -q "from('business_secrets')" lib/business-secrets.ts && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -q "business-secrets" && echo "revisar tsc" || echo "OK: fallback removido, getBusinessSecrets solo lee business_secrets"</automated>
  </verify>
  <acceptance_criteria>
    - getBusinessSecrets ya no consulta businesses; lee solo business_secrets.
    - El módulo compila (tsc --noEmit).
    - El comentario explica que el shim de transición se removió post-028.
  </acceptance_criteria>
  <done>El helper quedó limpio: una sola fuente de secretos (business_secrets), sin el shim de transición.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Runbook de rotación de claves (D-06 = ROTAR)</name>
  <files>.planning/phases/01-rls-lockdown-secret-isolation/ROTATION-RUNBOOK.md</files>
  <read_first>
    - 01-CONTEXT.md D-06 (RESUELTA → ROTAR: la app estuvo live con secretos reales expuestos a anon; rotar todas las claves afectadas)
    - 01-RESEARCH.md Runtime State Inventory fila Secrets/env vars (qué se rota; es acción operativa fuera del repo)
  </read_first>
  <action>
    Crear ROTATION-RUNBOOK.md (documento operativo, NO código). Encabezar con el contexto de D-06: la app estuvo desplegada en la URL pública de Vercel con secretos reales mientras el agujero USING(true) exponía businesses a anon → exposición real de credenciales → hay que ROTAR. Listar, como checklist accionable para el usuario (fuera del repo), las claves a rotar:
      - Secretos per-negocio que filtró la RLS (estaban en businesses, ahora en business_secrets): mp_access_token / mp_refresh_token (re-OAuth de MercadoPago Connect por negocio o regenerar credenciales en la cuenta MP), google_refresh_token (re-OAuth de Google Calendar por negocio / revocar en la cuenta Google), resend_api_key (regenerar API key en Resend), recaptcha_secret_key (regenerar par de claves en Google reCAPTCHA admin).
      - Por precaución del brief (no filtradas por esta RLS pero el brief las incluye; setup-plans las logueaba por query string — SEC-03): ADMIN_SECRET y CRON_SECRET (regenerar y actualizar en Vercel env vars).
    Para cada una: dónde se regenera (consola), dónde se actualiza (Vercel env vars y/o valor en DB vía dashboard de Forjo o SQL editor), y nota de que tras rotar hay que verificar que el flujo correspondiente sigue funcionando. Dejar claro que esto NO bloquea el código de la fase pero debe ejecutarse pronto porque las claves están comprometidas. Texto en español, accionable, sin paja.
  </action>
  <verify>
    <automated>grep -qi "rotar" .planning/phases/01-rls-lockdown-secret-isolation/ROTATION-RUNBOOK.md && grep -qi "mp_access_token\|MercadoPago" .planning/phases/01-rls-lockdown-secret-isolation/ROTATION-RUNBOOK.md && grep -qi "resend\|recaptcha\|google" .planning/phases/01-rls-lockdown-secret-isolation/ROTATION-RUNBOOK.md && echo "OK: runbook de rotacion cubre las claves" || (echo "FALLO: runbook incompleto" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - El runbook documenta D-06=ROTAR con el contexto de exposición real.
    - Lista accionable de cada clave a rotar (MP, Google, Resend, reCAPTCHA per-negocio + ADMIN_SECRET/CRON_SECRET), dónde se regenera y dónde se actualiza.
    - Marca que es acción operativa del usuario fuera del repo y que no bloquea el código pero es urgente.
  </acceptance_criteria>
  <done>Queda un runbook claro para que el usuario rote las claves expuestas; SEC-01 criterio 5 (decisión de rotación resuelta y documentada) cumplido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (browser) → PostgREST | Tras 028, anon no tiene policy de lectura sobre services/business_hours/businesses ni columnas-secreto que leer. |
| operador humano → SQL editor | Aplica 028 a mano; el checkpoint blocking garantiza que el código live ya verificó antes del drop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Information Disclosure | DROP con nombre de policy equivocado ("public read business_hours") → no-op silencioso, hueco abierto | mitigate | Nombres exactos verificados en schema.sql; verify niega la presencia del nombre equivocado; query pg_policy en el runbook confirma la ausencia. (severity: high) |
| T-01-16 | Information Disclosure | Aplicar 028 antes de que el código live lea business_secrets → pagos/emails rotos en silencio (Pitfall B) | mitigate | checkpoint:human-verify BLOCKING smoke test gatea el runbook de 028; orden additive-first→deploy→verify→destructivo (D-02). (severity: high) |
| T-01-17 | Information Disclosure | Secretos quedan físicamente en businesses (copiar no es mover, Pitfall G) | mitigate | 028 DROP COLUMN de las 7 columnas; verify exige ≥7 DROP COLUMN. (severity: high) |
| T-01-18 | (post-exposición) | Claves filtradas mientras la app estuvo live siguen válidas | transfer | ROTATION-RUNBOOK (D-06=ROTAR): acción operativa del usuario fuera del repo (re-OAuth + regen + update Vercel/DB). (severity: high) |
| T-01-SC | Tampering | npm installs en este plan | accept | Este plan NO instala paquetes. No aplica gate de legitimidad. |
</threat_model>

<verification>
- El smoke-test checkpoint (blocking) confirma el código live antes de 028.
- 028 dropea las policies con nombres exactos y ≥7 columnas (grep).
- El runbook de 028 verifica anon denegado sobre businesses/services y /[slug] funcionando.
- getBusinessSecrets sin fallback compila.
- ROTATION-RUNBOOK cubre todas las claves expuestas (D-06).
</verification>

<success_criteria>
- anon no puede leer columnas sensibles de businesses/services/business_hours (policies USING(true) eliminadas en las tres; businesses fue en 026).
- Las 7 columnas-secreto no existen en businesses; viven solo en business_secrets (owner-only).
- /[slug] sigue cargando desde las vistas acotadas.
- getBusinessSecrets lee solo business_secrets.
- Decisión de rotación (D-06) resuelta y documentada en un runbook accionable.
</success_criteria>

<output>
Create `.planning/phases/01-rls-lockdown-secret-isolation/01-05-SUMMARY.md` when done
</output>
