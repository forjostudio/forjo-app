---
quick_id: 260902-h6m
phase: quick-260902-h6m
plan: 01
type: execute
wave: 1
depends_on: []
workstream: motor-reservas
files_modified:
  - supabase/migrations/076_book_slot_atomic_revoke_anon.sql
  - test/book-slot-atomic-anon-revoke.test.ts
  - .planning/workstreams/motor-reservas/todos/pending/2026-08-18-book-slot-atomic-es-ejecutable-por-anon.md
  - .planning/workstreams/motor-reservas/todos/completed/2026-08-18-book-slot-atomic-es-ejecutable-por-anon.md
autonomous: true
requirements: [X-16-A, RA-05]
user_setup:
  - service: supabase-produccion
    why: "La migración 076 se aplica A MANO en el SQL editor de producción. NO es parte de esta tarea: el plan deja el runbook completo escrito en la cabecera del archivo. La 076 NO está acoplada a ningún deploy (los tres callers usan service_role/authenticated, no anon), así que se puede aplicar sola."
    dashboard_config:
      - task: "Correr el pre-flight, después el archivo 076 COMPLETO de una sola vez, después la verificación posterior"
        location: "Supabase Dashboard → SQL Editor (proyecto de producción)"

must_haves:
  truths:
    - "El bypass queda REPRODUCIDO y registrado antes de escribir una línea de fix: como `anon`, `book_slot_atomic` crea un turno con un payload que el route handler público rechazaría"
    - "Después del fix, el rol `anon` NO puede ejecutar `book_slot_atomic`: la llamada falla y NO queda ninguna fila en `appointments`"
    - "El rol `authenticated` SIGUE pudiendo ejecutar `book_slot_atomic` (es el rol del alta manual del dueño)"
    - "El rol `service_role` SIGUE pudiendo ejecutar `book_slot_atomic` (es el rol del booking público y de la generación de abonos)"
    - "Un test permanente se pone ROJO si alguien vuelve a conceder la ejecución al rol anónimo"
    - "Los tres caminos de alta de turnos (booking público, alta manual, abonos) siguen creando turnos: la suite completa queda verde"
    - "Ninguna migración ya aplicada (041 … 075) ni `supabase/schema.sql` tienen una sola línea de diff"
  artifacts:
    - path: "supabase/migrations/076_book_slot_atomic_revoke_anon.sql"
      provides: "Revocación de EXECUTE al rol anónimo sobre book_slot_atomic + re-grant explícito a authenticated/service_role + guard de post-estado + runbook de producción"
      contains: "book_slot_atomic"
      min_lines: 60
    - path: "test/book-slot-atomic-anon-revoke.test.ts"
      provides: "Reproducción permanente del bypass por la superficie real (PostgREST + anon key sin sesión) + controles positivos de los otros dos roles"
      contains: "hasSupabaseCreds"
      min_lines: 90
  key_links:
    - from: "test/book-slot-atomic-anon-revoke.test.ts"
      to: "test/env.ts"
      via: "import de hasSupabaseCreds — marcador OBLIGATORIO que clasifica la suite en el proyecto vitest `db` (serializado). Sin él, `test/suite-split.test.ts` caso 4 falla nombrando el archivo"
      pattern: "from './env'"
    - from: "test/book-slot-atomic-anon-revoke.test.ts"
      to: "test/helpers/booking-fixtures.ts"
      via: "seedOneTenant / seedTimeBlock / teardownOneTenant — mismo molde que test/concurrency.test.ts"
      pattern: "seedOneTenant"
    - from: "supabase/migrations/076_book_slot_atomic_revoke_anon.sql"
      to: "supabase/migrations/069_shared_capacity_agenda_and_space_gates.sql"
      via: "la firma de 14 argumentos tiene que ser BYTE-IDÉNTICA a la del último GRANT (069:489) o el REVOKE apunta a otra función y no revoca nada"
      pattern: "timestamp with time zone"
    - from: "supabase/migrations/076_book_slot_atomic_revoke_anon.sql"
      to: "lib/booking-core.ts"
      via: "único call site del RPC en producción (línea ~499); recibe el cliente por parámetro, nunca lo crea → el rol lo deciden los tres callers"
      pattern: "book_slot_atomic"
---

<objective>
Cerrar el bypass de `book_slot_atomic`: la función es `SECURITY DEFINER` y tiene `GRANT EXECUTE` al
rol anónimo desde la migración 041 (re-otorgado hasta la 069). Adentro de una función definer la RLS
no corre, así que cualquiera con la anon key del bundle puede llamarla directo por PostgREST y
saltearse **los tres** controles del booking público —ventana de reserva, gate de plan y reCAPTCHA—
que viven SOLO en `app/api/booking/create/route.ts`.

**El camino elegido y por qué.** Los dos candidatos del todo NO cubren lo mismo:

| Camino | Ventana | Gate de plan | reCAPTCHA |
|---|---|---|---|
| Llevar `isDateOutOfWindow` adentro de la función | cierra | **sigue abierto** | **sigue abierto** |
| Revocar la ejecución al rol anónimo | cierra | cierra | cierra |

Se elige **revocar**. La condición que el todo marcaba como previa —"verificar primero que ninguna
superficie anónima llame al RPC directo, o se rompe el booking público"— se cumple: hay UN solo call
site (`lib/booking-core.ts`), que recibe el cliente por parámetro, y sus tres callers son
service_role (booking público, abonos) y authenticated (alta manual). Ninguno es anon.

Esa elección **no se da por buena: se mide**. La Tarea 1 la somete a dos gates falsables (G1 y G2) y
tiene autoridad para VETARLA: si el bypass no se reproduce, o si aparece un caller anónimo, el
ejecutor PARA y escala en vez de escribir la migración. Este workstream ya registró cinco veces un
fix propuesto que se cayó al medirlo; el orden reproducir → elegir → arreglar no es ceremonia.

Purpose: cerrar un hallazgo de severidad alta que hoy permite crear turnos salteando la ventana de
reserva, el cobro del plan y el anti-bot, con la anon key que está en el bundle del navegador.
Output: migración 076 validada en local (sin aplicar a prod), suite de regresión permanente, y el
todo movido a `completed/`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/todos/pending/2026-08-18-book-slot-atomic-es-ejecutable-por-anon.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
@.claude/skills/convenciones-forjo/SKILL.md

# Molde de migración de este repo (cabecera densa, BEGIN/COMMIT, DO $$ idempotente, NOTIFY pgrst)
@supabase/migrations/075_time_block_location_same_tenant.sql
# ÚNICO precedente de revocar la ejecución de una función en el repo — copiar ESTA forma
@supabase/migrations/074_save_agenda_blocks.sql
# Único call site del RPC en producción
@lib/booking-core.ts
# Dónde viven los tres controles que hoy se saltean
@app/api/booking/create/route.ts
@lib/booking-window.ts
# Moldes de test DB-backed con clientes de rol explícito
@test/helpers/booking-fixtures.ts
@test/agenda-save-blocks-rpc.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproducir el bypass y medir los privilegios (RED — sin tocar producción)</name>
  <files>test/book-slot-atomic-anon-revoke.test.ts</files>
  <action>
Antes de esta tarea: confirmar que el Supabase LOCAL está levantado (`npx supabase status`) y que la
suite apunta ahí — `.env.test.local` (override de `vitest.setup.ts`) tiene que resolver
`NEXT_PUBLIC_SUPABASE_URL` a `127.0.0.1`. Si resuelve al proyecto remoto, PARAR: esta suite escribe
filas reales en `appointments`.

Crear `test/book-slot-atomic-anon-revoke.test.ts`, suite DB-backed con el molde de
`test/concurrency.test.ts` + `test/agenda-save-blocks-rpc.test.ts`:

- `import { hasSupabaseCreds } from './env'` y `describe.skipIf(!hasSupabaseCreds)(...)`. El import es
  OBLIGATORIO: es el marcador por el que `vitest.config.mts` clasifica la suite en el proyecto `db`
  (serializado). Sin él, el caso 4 de `test/suite-split.test.ts` falla nombrando este archivo.
- `beforeAll`: `seedOneTenant({ bufferMinutes: 0, serviceDurationMinutes: 30 })` +
  `seedTimeBlock(t)` (default lunes 08:00–20:00, que envuelve los horarios de los casos). Después,
  con `t.admin`, actualizar el negocio a `max_advance_days: 7` y `plan_status: 'cancelled'`. Esos dos
  valores son el corazón del caso: convierten al payload en uno que el route handler público
  rechazaría DOS veces (400 `date_out_of_window` por la fecha `2031-03-03`, que está a años del
  corte; y 403 `plan_inactive` por el estado del plan). Todo el sembrado es con service-role: sembrar
  no es asertar.
- Fecha fija `2031-03-03` (lunes → `EXTRACT(dow) = 1`, alineada con el `day_of_week` del bloque
  sembrado, misma constante que `test/concurrency.test.ts`).
- Cliente de ataque: `createClient(url, anonKey, { auth: { persistSession: false } })` SIN
  `signInWithPassword`. Ese cliente es literalmente lo que corre en el navegador de cualquier persona
  que abra `/[slug]`.
- Dos guards anti-falso-verde que TIRAN antes de la primera aserción (molde de
  `agenda-save-blocks-rpc.test.ts`): (1) `anonKey !== process.env.SUPABASE_SERVICE_ROLE_KEY`;
  (2) `getSession()` del cliente de ataque devuelve sesión nula — si tuviera sesión el rol efectivo
  sería `authenticated` y el test mediría otra cosa.
- Helper `bookAs(client, time)` que invoca `client.rpc('book_slot_atomic', { ... })` con los CATORCE
  parámetros que pasa `lib/booking-core.ts:499` (`p_business_id`, `p_professional_id`, `p_service_id`,
  `p_location_id`, `p_date`, `p_time`, `p_duration`, `p_client_id`, `p_client_name`, `p_client_phone`,
  `p_client_email`, `p_notes`, `p_status`, `p_expires_at`). PostgREST resuelve la función por el
  conjunto exacto de argumentos: si falta uno, el error es "función no encontrada" y el test daría un
  falso verde por el motivo equivocado.
- Helper `countAppointmentsAt(time)`: lectura INDEPENDIENTE con `t.admin` sobre `appointments`
  filtrando por `business_id` + `date` + `time`. Las aserciones de efecto se hacen contra la base, no
  contra el valor de retorno del RPC.
- Casos, cada uno en su propio horario para que no se pisen entre sí:
  1. **`anon` (09:00) — el caso que muerde.** `bookAs(anonClient, '09:00')` tiene que devolver error
     Y `countAppointmentsAt('09:00')` tiene que ser 0. HOY este caso FALLA (crea el turno): esa
     falla ES la reproducción del bypass.
  2. **Control positivo `service_role` (10:00).** El MISMO payload vía `t.admin` tiene que crear el
     turno (`countAppointmentsAt('10:00') === 1`). Sin este control, el caso 1 podría ponerse verde
     por un payload inválido en vez de por la falta de privilegio, y el fix parecería funcionar
     cuando en realidad no probó nada.
  3. **Control positivo `authenticated` (11:00).** Un segundo cliente anon-key que SÍ hace
     `signInWithPassword` con `t.email` / `t.password` (rol efectivo `authenticated`, el del alta
     manual del dueño) tiene que crear el turno. Este caso es el que detecta un revoke que se pasa de
     rosca y rompe `app/api/appointments/create/route.ts`.
- `afterAll`: `teardownOneTenant(t)`.

Comentarios en español explicando el PORQUÉ, como el resto del repo. En particular: por qué el
cliente de ataque no tiene sesión, y por qué las aserciones de efecto van contra la base.

**GATES DE EVIDENCIA — se miden ahora y tienen poder de veto sobre el camino elegido:**

- **G1 (el bypass es real).** El caso 1 falla creando el turno. Además, corroborarlo por el camino
  del todo original: `SET LOCAL ROLE anon` + llamada + `ROLLBACK` en una sola transacción contra el
  Postgres local, vía `docker exec supabase_db_forjo-app psql -U postgres -d postgres -f <archivo>`
  (el nombre del contenedor sale de `project_id = "forjo-app"` en `supabase/config.toml`; si no
  coincide, sacarlo de `docker ps`). El script tiene que imprimir el id del turno creado y terminar
  en `ROLLBACK` — no puede quedar nada escrito. Si docker/psql no está disponible en esta máquina,
  registrarlo en el SUMMARY y seguir con la reproducción por HTTP, que es estrictamente más cercana
  a producción (es la superficie real, PostgREST con el JWT anónimo).
  → Si G1 NO se reproduce por ninguna de las dos vías: **PARAR**. No hay agujero que tapar y una
  migración escrita sobre una premisa falsa es peor que no hacer nada.
- **G2 (no hay caller anónimo).** `grep -rn "book_slot_atomic" app/ lib/ components/ scripts/` tiene
  que devolver EXACTAMENTE un call site de producción (`lib/booking-core.ts`). Abrir los tres callers
  y confirmar el cliente que le pasan: `app/api/booking/create/route.ts` → `createAdminClient()`
  (service_role); `app/api/appointments/create/route.ts` → `createClient()` con cookies
  (authenticated); `lib/abono-generation.ts` → recibe el cliente del caller (service_role).
  → Si aparece cualquier superficie que lo llame con la anon key sin sesión: **PARAR y escalar**. El
  revoke rompería el booking público y el otro camino vuelve a estar sobre la mesa.
- **G3 (los otros dos roles hoy pueden).** Los casos 2 y 3 pasan HOY, antes de tocar nada. Son la
  línea de base contra la que se mide que el fix no se pasó de rosca.

Registrar en el SUMMARY la salida literal de los tres gates (el mensaje de falla del caso 1, el id
del turno del ROLLBACK, y el listado de call sites). Sin eso, la Tarea 2 no arranca.

NO tocar `lib/`, `app/`, `supabase/` ni `.planning/` en esta tarea. Es medición pura.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run test/book-slot-atomic-anon-revoke.test.ts</automated>
    <automated>./node_modules/.bin/vitest run test/suite-split.test.ts</automated>
    <automated>grep -rn "book_slot_atomic" app/ lib/ components/ scripts/</automated>
    <automated>git diff --stat -- supabase/ lib/ app/</automated>
  </verify>
  <done>
El primer comando sale con código distinto de 0: el caso `anon` está en ROJO —creó el turno— y los
casos 2 y 3 en verde. Esa combinación exacta es el resultado esperado de esta tarea; un verde total
significaría que el test no muerde y hay que arreglar el test antes de seguir.
`test/suite-split.test.ts` pasa 5/5 (la suite nueva quedó clasificada en el proyecto `db`).
El grep devuelve un solo call site de producción, en `lib/booking-core.ts`.
`git diff --stat` sobre `supabase/`, `lib/` y `app/` sale VACÍO: esta tarea no tocó código de
producción.
G1, G2 y G3 quedan registrados con su salida literal en el SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migración 076 — el rol anónimo deja de poder ejecutar el RPC (GREEN)</name>
  <files>supabase/migrations/076_book_slot_atomic_revoke_anon.sql</files>
  <action>
Crear `supabase/migrations/076_book_slot_atomic_revoke_anon.sql`. El separador después del número es
**GUION BAJO**, sin excepción: con un guion medio `supabase db reset` saltea el archivo EN SILENCIO y
el reset quedaría verde sin haber aplicado nada.

Contenido, con la cabecera densa en español que usa este repo (molde 075 para la forma, molde 074
para el bloque de privilegios — es el ÚNICO precedente de revocar la ejecución de una función):

1. **Cabecera.** Contexto del hallazgo (X-16-A, `secure-phase` de la Phase 16; riesgo aceptado RA-05
   que las migraciones 074 y `schema.sql:4449` dejaron explícitamente abierto). La medición de la
   Tarea 1, con sus números reales. La tabla de cobertura de los dos caminos y por qué se revoca en
   vez de llevar la ventana adentro de la función: mover `isDateOutOfWindow` a la base cierra uno de
   los tres controles y deja el gate de plan y el anti-bot igual de bypasseables, además de duplicar
   una regla de negocio que hoy tiene una sola fuente de verdad en `lib/booking-window.ts`.
   Dejar dicho qué NO hace: no toca el CUERPO de la función (el motor de reservas no se redefine
   acá), no toca ninguna tabla, columna, índice, policy ni vista, y no toca ninguna migración previa.
2. **Por qué esta migración NO está acoplada a un deploy.** Los tres callers usan service_role o
   authenticated (medido en G2), así que revocar el privilegio del rol anónimo no cambia el
   comportamiento de ninguna superficie viva. Se puede aplicar sola, antes o después del deploy. Es
   lo contrario de la 074, que sí estaba acoplada. Decirlo explícito ahorra una coordinación que no
   hace falta.
3. **Cuerpo, dentro de `BEGIN; ... COMMIT;`** (los tres statements de privilegio tienen que ser
   atómicos: si el `GRANT` fallara después de un `REVOKE` autocommiteado, el alta manual del dueño se
   quedaría sin poder ejecutar la función):
   - `REVOKE EXECUTE ON FUNCTION ... FROM PUBLIC;`
   - `REVOKE EXECUTE ON FUNCTION ... FROM "anon";`
   - `GRANT EXECUTE ON FUNCTION ... TO "authenticated";`
   - `GRANT EXECUTE ON FUNCTION ... TO "service_role";`
   Los dos REVOKE son necesarios y NO son redundantes: uno saca la concesión implícita que Postgres
   le da a PUBLIC en toda función nueva, el otro saca la concesión EXPLÍCITA a `anon` de las
   migraciones 041/042/058/062/063/064/068/069. Sacar solo uno deja el privilegio en pie por la otra
   vía. Los dos GRANT van DESPUÉS de los REVOKE porque `REVOKE ... FROM PUBLIC` también les saca a
   esos roles lo que tuvieran por herencia de PUBLIC; re-otorgarlos explícitamente los deja
   independientes de esa herencia.
   La firma de los CATORCE argumentos va COMPLETA y comillada, copiada byte a byte de la línea 489 de
   `069_shared_capacity_agenda_and_space_gates.sql`. El nombre solo no identifica una función: una
   firma con una letra distinta apunta a otra función (o a ninguna) y el REVOKE no revoca nada
   mientras la migración sale verde. Verificarlo con un diff antes de dar la tarea por hecha.
4. **Guard de post-estado en un `DO $$`, DENTRO de la misma transacción**, justo antes del `COMMIT`.
   Con `has_function_privilege('anon', '<firma>'::regprocedure, 'EXECUTE')` y sus equivalentes para
   `authenticated` y `service_role`: si el primero no quedó en falso, o si alguno de los otros dos no
   quedó en verdadero, `RAISE EXCEPTION` con `ERRCODE = 'P0001'` y un mensaje de dominio que diga qué
   rol quedó mal. La transacción entera se revierte. Es una migración que se verifica a sí misma: se
   aplica A MANO en producción y una "consulta posterior recomendada" se saltea; un `RAISE` no.
5. **`NOTIFY pgrst, 'reload schema';` al final, FUERA de la transacción.** PostgREST cachea el schema
   junto con los privilegios: sin el reload, el revoke puede tardar en verse desde la API y la
   verificación posterior daría un falso negativo.
6. **Runbook de producción**, en la cabecera, mismo nivel de detalle que el de la 075:
   - (a) **Pre-flight**, a correr en el SQL editor de prod ANTES de aplicar: una query que liste, para
     los tres roles, `has_function_privilege(rol, '<firma>'::regprocedure, 'EXECUTE')`. Esperado
     antes de aplicar: `anon = t`, `authenticated = t`, `service_role = t`. Si `anon` ya viniera en
     falso, la migración es un no-op y no hace falta aplicarla.
   - (b) **Chequeo de consumidores** —la condición que el todo exigía, re-verificada en el momento de
     aplicar, no solo en el momento de planificar—: confirmar que ninguna integración externa llame
     al RPC con la anon key. Los tres callers de la app usan service_role/authenticated (G2). El
     agente de WhatsApp consume `/api/agent/context` por HTTP, no PostgREST, así que no se ve
     afectado. Si apareciera un consumidor nuevo con anon key desde que se escribió este plan, NO
     aplicar y reabrir la decisión.
   - (c) Pegar y ejecutar el archivo **COMPLETO, de una sola vez** — nunca statement por statement:
     partirlo rompe la atomicidad del BEGIN/COMMIT y puede dejar la función sin GRANT para el dueño.
   - (d) **Verificación posterior**: la misma query del pre-flight. Esperado: `anon = f`,
     `authenticated = t`, `service_role = t`. Y una prueba de humo funcional: crear un turno desde el
     link público del negocio y otro desde el alta manual del panel — los dos tienen que funcionar.
   - (e) **RECIÉN DESPUÉS de aplicarla**, espejar `supabase/schema.sql` a mano y de forma quirúrgica
     (nunca con `db dump`): borrar la línea `GRANT ALL ON FUNCTION "public"."book_slot_atomic"(...)
     TO "anon";` (~`schema.sql:2884`, la de `anon` ÚNICAMENTE — las de `authenticated` y
     `service_role` se quedan) y actualizar la nota de `schema.sql:4449`, que hoy dice que el RPC
     conserva su grant al rol anónimo y que RA-05 sigue abierto. Antes de aplicarla, `schema.sql` NO
     se toca: es el espejo del estado REAL de producción, y reflejar una migración no aplicada lo
     convierte en mentira.
   - (f) **Orden**: producción tiene aplicada hasta la **075** inclusive. La 075 se aplicó el
     2026-08-31 y quedó verificada con evidencia cruda del operador (`pg_constraint` devuelve una
     sola fila, `tb_location_same_tenant`, y `time_blocks_location_id_fkey` ya no existe). La
     afirmación contraria que había acá salía de leer el SUMMARY de la 075, que se escribió ANTES
     de aplicarla — corregido por el orquestador. Las migraciones se aplican en orden numérico sin
     excepción: la 076 es la siguiente y no tiene nada delante. Re-verificar igual el estado real
     antes de aplicar.

Validar en local: `npx supabase db reset`. El reset replaya el baseline numerado en orden y tiene que
terminar sin errores, con la 076 EN el listado de migraciones aplicadas (si no aparece, el nombre del
archivo está mal). Después, volver a correr la suite de la Tarea 1: el caso `anon` pasa a VERDE (la
llamada falla y no queda ninguna fila) y los casos 2 y 3 SIGUEN verdes.

**NO aplicar nada a producción.** Eso lo hace el usuario a mano con el runbook.
**NO editar ninguna migración 041–075.** Ya corrieron. El fix es una migración nueva.
**NO tocar `supabase/schema.sql`** en esta tarea (paso (e) del runbook: es post-aplicación).
  </action>
  <verify>
    <automated>ls supabase/migrations/076_book_slot_atomic_revoke_anon.sql</automated>
    <automated>npx supabase db reset</automated>
    <automated>./node_modules/.bin/vitest run test/book-slot-atomic-anon-revoke.test.ts</automated>
    <automated>git diff --stat -- supabase/schema.sql supabase/migrations/</automated>
    <automated>grep -cE 'REVOKE EXECUTE ON FUNCTION' supabase/migrations/076_book_slot_atomic_revoke_anon.sql</automated>
  </verify>
  <done>
El archivo existe con separador de guion bajo. `npx supabase db reset` termina OK y lista la 076
entre las migraciones aplicadas. La suite de la Tarea 1 pasa 3/3: el caso `anon` ahora falla la
llamada y deja `appointments` en 0, y los dos controles positivos siguen creando su turno.
`git diff --stat` sobre `supabase/schema.sql` y `supabase/migrations/` sale VACÍO (la 076 es un
archivo nuevo sin trackear; ninguna migración vieja ni el schema tienen diff).
El último grep devuelve 2 (los dos REVOKE: PUBLIC y el rol anónimo).
La firma de 14 argumentos de la 076 es byte-idéntica a la de `069:489` (verificado con diff).
  </done>
</task>

<task type="auto">
  <name>Task 3: Regresión de los tres caminos de alta + cierre del todo</name>
  <files>.planning/workstreams/motor-reservas/todos/pending/2026-08-18-book-slot-atomic-es-ejecutable-por-anon.md</files>
  <action>
El riesgo real de este cambio no es el revoke: es romper alguno de los tres caminos que SÍ tienen que
seguir creando turnos. Verificarlos, en este orden:

1. **Suite completa**: `npm test`. Tiene que quedar verde. Las suites que cubren los tres caminos son
   `test/booking-public-regression.test.ts` (público), `test/manual-booking.test.ts` (alta manual),
   `test/abono-generation.test.ts` (abonos), más `test/concurrency.test.ts` y
   `test/booking-core.test.ts`, que ejercen el RPC de verdad contra la base. Comparar el total de
   tests pasados contra el de antes de la Tarea 2: tiene que ser el mismo + los 3 nuevos. Un total
   que BAJA es una regresión aunque no haya rojo (una suite que dejó de correr).
2. **Tipos**: `./node_modules/.bin/tsc --noEmit`. Usar el binario local: `npx tsc` sale 0 siempre en
   este repo (falso verde) y no sirve como gate.
3. **Lint**: `./node_modules/.bin/eslint test/book-slot-atomic-anon-revoke.test.ts`. Tarda más de 3
   minutos incluso sobre un solo archivo (medido) — correrlo con timeout ≥ 300000 ms o en background.
   El gate es CERO hallazgos NUEVOS respecto del archivo; hallazgos preexistentes de otros archivos
   no cuentan.

Después, cerrar el todo: mover
`.planning/workstreams/motor-reservas/todos/pending/2026-08-18-book-slot-atomic-es-ejecutable-por-anon.md`
a `.planning/workstreams/motor-reservas/todos/completed/` con `git mv` (preserva el historial; un
copy+delete lo pierde). Antes de moverlo, agregarle al final una sección `## Resolución` con: la
fecha, el quick id `260902-h6m`, el camino elegido y su justificación de cobertura (los tres
controles vs uno), el archivo de la migración, el archivo del test que lo deja cerrado, y —bien
visible— que la migración **todavía NO está aplicada en producción** y que el runbook vive en la
cabecera del archivo 076. Un todo movido a `completed/` mientras el fix no llegó a prod es una
mentira si no dice esa última línea.

⚠ NO tocar `.planning/workstreams/motor-reservas/phases/`: la fase 19 está cerrada y la 20 sin
empezar.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/eslint test/book-slot-atomic-anon-revoke.test.ts</automated>
    <automated>ls .planning/workstreams/motor-reservas/todos/completed/2026-08-18-book-slot-atomic-es-ejecutable-por-anon.md</automated>
    <automated>git status --porcelain -- .planning/workstreams/motor-reservas/phases/</automated>
  </verify>
  <done>
`npm test` verde, con el total de tests igual al baseline + 3. `tsc --noEmit` sin errores (con el
binario local, no con npx). eslint sin hallazgos nuevos sobre el archivo de test. El todo está en
`todos/completed/` con su sección de resolución, y ya no está en `todos/pending/`. El último comando
sale VACÍO: no se tocó ni un archivo de `phases/`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → PostgREST (anon key) | La anon key viaja en el bundle. Cualquiera puede firmar requests con ella y llamar cualquier función que el rol `anon` pueda ejecutar. Es el borde que este plan cierra. |
| navegador → `/api/booking/create` (service_role adentro) | El camino legítimo del booking público: acá viven la ventana, el gate de plan y el reCAPTCHA. |
| dueño autenticado → PostgREST (JWT `authenticated`) | El alta manual. Tiene que seguir funcionando: es el control positivo del caso 3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-h6m-01 | Elevation of Privilege | `public.book_slot_atomic` ejecutable por `anon` | mitigate | Migración 076: `REVOKE EXECUTE` a PUBLIC y al rol anónimo; el único camino público queda `/api/booking/create`, donde corren los tres controles. |
| T-h6m-02 | Tampering | Ventana de reserva (`isDateOutOfWindow`, solo en el route handler) | mitigate | Cubierto por T-h6m-01: sin ejecución anónima, la única entrada pública pasa por el backstop de ventana. El caso 1 del test usa una fecha fuera de ventana justamente para probarlo. |
| T-h6m-03 | Spoofing | reCAPTCHA salteable llamando el RPC directo (alta masiva automatizada) | mitigate | Cubierto por T-h6m-01. |
| T-h6m-04 | Repudiation / abuso comercial | Gate de plan (`plan_status`) salteable → un negocio suspendido sigue captando turnos | mitigate | Cubierto por T-h6m-01. El caso 1 siembra `plan_status: 'cancelled'` para que el payload viole también este control. |
| T-h6m-05 | Denial of Service | Un revoke de más deja al dueño sin poder dar de alta turnos a mano | mitigate | Guard de post-estado dentro de la transacción de la 076 + caso 3 del test (`authenticated` sigue pudiendo) + prueba de humo funcional en el runbook. |
| T-h6m-06 | Tampering | Regresión futura: alguien vuelve a conceder EXECUTE al rol anónimo en una migración nueva | mitigate | El caso 1 de `test/book-slot-atomic-anon-revoke.test.ts` se pone rojo. Refuerzo estructural ya presente: la 074 revocó el default de FUNCTIONS para el rol anónimo, así que una función nueva no nace concedida. |
| T-h6m-07 | Information Disclosure | Los parámetros del RPC (`business_id`, `service_id`) son públicos vía `public_services` | accept | No es lo que se arregla acá y no cambia: son datos que la página pública necesita. Dejan de ser aprovechables cuando el rol anónimo no puede ejecutar la función. |
</threat_model>

<verification>
1. `npx supabase db reset` termina OK con la 076 en el listado de migraciones aplicadas.
2. `npm test` verde, con el total = baseline + 3.
3. `./node_modules/.bin/tsc --noEmit` sin errores.
4. `git diff --stat -- supabase/schema.sql supabase/migrations/` vacío: ninguna migración aplicada ni
   el schema tienen diff.
5. `git status --porcelain -- .planning/workstreams/motor-reservas/phases/` vacío.
6. El SUMMARY registra la salida literal de G1 (bypass reproducido), G2 (inventario de call sites) y
   G3 (línea de base de los otros dos roles).
</verification>

<success_criteria>
- Un cliente con la anon key y sin sesión NO puede crear un turno llamando `book_slot_atomic`: la
  llamada falla y `appointments` queda en 0.
- El booking público, el alta manual del dueño y la generación de abonos siguen creando turnos.
- Existe un test permanente que se pone rojo si se revierte el fix.
- La migración 076 está validada en local y NO aplicada a producción, con el runbook completo escrito
  en su cabecera.
- Ninguna migración 041–075 ni `supabase/schema.sql` tienen una sola línea de diff.
- El todo está en `todos/completed/` con su resolución, diciendo explícitamente que falta aplicar la
  migración en producción.
</success_criteria>

<output>
Create `.planning/quick/260902-h6m-cerrar-el-bypass-anon-de-book-slot-atomi/260902-h6m-SUMMARY.md` when done
</output>
