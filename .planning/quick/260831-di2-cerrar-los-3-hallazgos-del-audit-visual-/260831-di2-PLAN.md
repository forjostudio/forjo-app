---
phase: quick-260831-di2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/agenda/agenda-client.tsx
  - .planning/workstreams/motor-reservas/phases/19-el-panel/19-UI-SPEC.md
  - .planning/workstreams/motor-reservas/phases/19-el-panel/19-UI-REVIEW.md
autonomous: true
requirements: ["BLOCKER-01", "WARNING-03", "WARNING-01"]

must_haves:
  truths:
    - "Cambiar la duracion del turno o el descanso entre turnos enciende 'Cambios sin guardar', igual que tocar un horario o un chip."
    - "Con el guardado en vuelo, los dos Select de duracion/descanso estan deshabilitados: no se puede meter un cambio que el UPDATE con valores capturados se lleve puesto."
    - "Si el UPDATE de duracion/descanso falla despues de que el RPC de horarios salio bien, el indicador de cambios sin guardar VUELVE a encenderse: la copy dice que esa mitad no se guardo y la senal lo acompana."
    - "Lo que persiste el boton 'Guardar horarios' en la tabla businesses vive en UN solo objeto de estado, y ese objeto es el mismo que lee el UPDATE: agregar un campo ahi lo hace ensuciar el estado sin tocar nada mas."
    - "Las dos live regions existen en el DOM desde el montaje del componente y solo cambia su contenido, que es la condicion para que un lector de pantalla las locute."
    - "La linea de chips no cambia de alto NI se desplaza horizontalmente al marcar el primer servicio: el nodo que se agrega siempre-montado esta fuera del flujo (position absolute) y no es un item del flex."
    - "El trigger colapsado dice 'Ver todos (7)' cuando el negocio tiene 7 servicios en la linea, haya comodin o no; el comodin sigue contando para decidir si la linea colapsa."
    - "19-UAT.md no se modifica: la UAT de la fase 19 sigue abierta y con sus 3 escenarios como estaban."
    - "Cero logica de negocio nueva: ni una query, ni un RPC, ni una migracion, ni un componente, ni una dependencia."
  artifacts:
    - path: "app/(dashboard)/agenda/agenda-client.tsx"
      provides: "Los dos Select de duracion/descanso dentro del contrato de guardado (ensucian + se congelan), las dos live regions siempre montadas, y el contador del trigger sin el comodin"
      contains: "hoursConfig"
    - path: ".planning/workstreams/motor-reservas/phases/19-el-panel/19-UI-SPEC.md"
      provides: "Enmienda fechada 2026-08-31: la fila Guardando pasa de enumeracion a REGLA, el mecanismo de las live regions y el numero del trigger"
      contains: "Enmienda 2026-08-31"
    - path: ".planning/workstreams/motor-reservas/phases/19-el-panel/19-UI-REVIEW.md"
      provides: "Seccion Resolucion con la disposicion de los 3 priorizados, los 6 menores no aplicados y el check humano que queda pendiente"
      contains: "## Resolución"
  key_links:
    - from: "saveHours (el UPDATE a businesses)"
      to: "el objeto de estado hoursConfig"
      via: "el UPDATE lee hoursConfig.slotDuration / hoursConfig.bufferMinutes — la enumeracion de lo que se persiste y la fuente del estado son el MISMO objeto"
      pattern: "default_slot_duration: hoursConfig\\.slotDuration"
    - from: "los dos Select de duracion/descanso"
      to: "hoursDirty + savingHours"
      via: "onValueChange pasa por updateHoursConfig (que prende hoursDirty) y el Select lleva disabled={savingHours}"
      pattern: "updateHoursConfig\\("
    - from: "la live region del comodin"
      to: "el layout de la linea de chips"
      via: "sr-only es position:absolute, asi que el nodo siempre montado NO es un item del flex y no consume el gap-x-2"
      pattern: "role=\"status\" className=\"sr-only\""
---

<objective>
Cerrar los **3 hallazgos priorizados** del audit visual 6-pilares de la fase 19 (`19-UI-REVIEW.md`, 18/24):
el BLOCKER de estado sucio/congelamiento en los dos `Select` de duracion y descanso, los dos `role="status"`
que se montan junto con su contenido (y por lo tanto probablemente no anuncian nada), y el contador de
`Ver todos (N)` que cuenta el chip comodin.

Purpose: el BLOCKER es **la misma perdida silenciosa de CR-01** en los dos controles que aquel fix no barrio.
El dueno cambia la duracion del turno, se va de la pantalla, y nadie le avisa; o la cambia con el RPC en vuelo
y el UPDATE escribe el valor viejo mientras `setHoursDirty(false)` apaga la unica senal. Ademas se corrige la
CAUSA de raiz: la fase enumero "los seis gestos que ensucian" en vez de derivar la regla *"todo lo que este
boton persiste tiene que ensuciar y congelarse"*, y esa enumeracion es la que dejo el agujero.

Output: `app/(dashboard)/agenda/agenda-client.tsx` (unico archivo de codigo) + la enmienda del contrato en
`19-UI-SPEC.md` + la seccion de Resolucion en `19-UI-REVIEW.md`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/STATE.md
@CLAUDE.md
@AGENTS.md
@.claude/CLAUDE.md
@.claude/skills/convenciones-forjo/SKILL.md

# La fuente de esta tarea: los 3 hallazgos con sus file:line.
@.planning/workstreams/motor-reservas/phases/19-el-panel/19-UI-REVIEW.md

# El contrato de diseno (aprobado 6/6). Leer la ENMIENDA FECHADA 2026-08-26 de la fila
# `Guardando (savingHours)` (~:347-359): dice "todo el grid", y es lo que hace que el hallazgo 1
# sea un bug de CODIGO y no del contrato.
@.planning/workstreams/motor-reservas/phases/19-el-panel/19-UI-SPEC.md

# Unico archivo de codigo. LEER LOS COMENTARIOS antes de editar: el docblock de `saveHours`
# (~:760-800) explica por que `savingHours` congela la grilla entera, y el de `BlockServicesLine`
# (~:255-320) explica el comodin, el umbral y el `min-h-11`. Los tres cambios de este plan viven
# adentro de razonamientos ya escritos: se EXTIENDEN, no se reemplazan.
@app/(dashboard)/agenda/agenda-client.tsx
</context>

<hallazgos_de_lectura>
Verificado contra el codigo real antes de escribir este plan.

1. **El BLOCKER se confirma exactamente como lo describe el audit.** `saveHours` persiste los dos valores en
   `:835` (`.update({ default_slot_duration: slotDuration, buffer_minutes: bufferMinutes })`), pero
   `setSlotDuration` (`:1311`) y `setBufferMinutes` (`:1329`) son setters inline crudos: no pasan por ningun
   mutador que prenda `hoursDirty`, y ninguno de los dos `Select` lleva `disabled`.

2. **Hay un segundo agujero en el mismo hallazgo, que el audit no nombra.** `setHoursDirty(false)` esta en `:826`,
   **antes** del UPDATE de `:835`. Si el RPC sale bien y el UPDATE falla, el toast dice la verdad ("los horarios se
   guardaron, pero no se pudo guardar la duracion ni el descanso") pero la senal visual ya se apago: el dueno se
   queda con configuracion sin guardar y sin ningun indicador. Es el mismo defecto de clase y se cierra en la
   misma tarea (Paso 4).

3. **Los sitios de estado son solo 6 y estan todos localizados** (`grep` sobre `slotDuration|bufferMinutes`):
   `:530`, `:531` (declaracion), `:835` (UPDATE), `:1311`, `:1329`, `:1331` (JSX). Nada mas del archivo los lee.
   Por eso el refactor a un objeto unico es de 6 puntos, todos mecanicos y todos cubiertos por `tsc`.

4. **El patron in-repo para un `Select` deshabilitado es el prop en la RAIZ, no en el trigger.**
   `app/(crm)/admin/negocios/[id]/ficha-client.tsx:407` hace `<Select ... disabled={!canChangePlan}>`.
   `Select` = `SelectPrimitive.Root` de `@base-ui/react/select`, y `SelectTrigger` ya trae
   `disabled:cursor-not-allowed disabled:opacity-50` en su className base (`components/ui/select.tsx:45`), asi que
   el apagado visual sale solo. El audit sugeria ponerlo en el `SelectTrigger`: **se hace en la raiz**, que es el
   precedente vivo del repo.

5. **La correccion que sugiere el audit para las live regions introduce un defecto de layout.** Su propuesta es
   montar siempre el contenedor VISIBLE del comodin (`inline-flex min-h-11 items-center`) y condicionar solo el
   hijo. Pero ese contenedor es un **item del flex** `gap-x-2` del grupo: vacio mide 0 de ancho pero **igual
   consume su gap de 8px**, asi que la fila entera de chips se correria 8px a la derecha cada vez que el comodin
   esta presente y volveria al marcar el primer servicio. Eso es un desplazamiento nuevo justo en la interaccion
   que el **escenario #2 de la UAT ya aprobo en navegador el 2026-08-27**. La solucion de este plan desacopla:
   la live region va **`sr-only`** (que en Tailwind es `position:absolute` ⇒ esta **fuera del flujo** y por
   definicion **no es item del flex**: cero gap, cero alto, en los dos estados) y el chip visible se queda
   **byte-identico**, solo que `aria-hidden` para no leerse dos veces.

6. **Para el indicador de estado sucio no hace falta desacoplar.** Su `<span>` es el **ultimo** hijo de un
   `flex items-center gap-4` que ademas es un `div` de ancho completo: montado y vacio agrega 16px de gap despues
   del boton y **nada visible cambia**, porque no hay nada a su derecha. Ahi alcanza con montar siempre el mismo
   nodo y condicionar el **texto**.

7. **Baselines medidos** (de esto dependen los gates):
   - `eslint` sobre `agenda-client.tsx` da hoy **exactamente** `1 problem (1 error, 0 warnings)`: el
     `react-hooks/purity` de `Date.now()` en `:1000` (vista semanal, **D-13, fuera de alcance por constraint**).
   - Ocurrencias actuales en el archivo: `disabled={savingHours}` **8** · `sr-only` **0** · `role="status"` **3**
     (dos en JSX y **una dentro de un comentario**, `:314`) · `aria-hidden="true"` **2**.
   - `npm test` (`vitest run`, proyectos `pure`/`db`) corre verde de punta a punta desde el quick `260827-ion`.
     Este plan no agrega tests: el cambio vive entero en un componente cliente `.tsx` y el repo no tiene
     infraestructura de component testing. La verificacion automatizada es `tsc` + eslint acotado + gates
     estructurales; la suite corre como control de no-regresion.

**Restriccion de comentarios (los gates cuentan ocurrencias literales en TODO el archivo):** no escribir
`disabled={savingHours}` ni `role="status" className="sr-only"` dentro de comentarios. Referirse a ellos por
concepto ("el prop de congelamiento", "la live region invisible"). El comentario de `:314` que hoy menciona el
rol ARIA hay que reescribirlo igual (ver Task 2), porque describe un mecanismo que deja de ser cierto.
</hallazgos_de_lectura>

<decisiones_ya_tomadas_no_reabrir>
- **Fuera de alcance, por eleccion explicita del usuario:** las 6 recomendaciones menores del audit (copies
  `not_deployed`/`reload`, `text-red-400` → `--destructive`, `pl-0.5`, el import muerto de `Minus`, el toast del
  inactivo). No tocarlas ni "de paso".
- **NO re-litigar:** WR-05 (gate de canchas) y WR-06 (`hasChipCatalog` inline). Aceptados con riesgo documentado.
- **NO tocar** el `react-hooks/purity` de la vista semanal (D-13, ya en `deferred-items.md`).
- **NO tocar `19-UAT.md`.** La UAT de la fase 19 esta abierta (1 de 3 pasada; las otras 2 dependen del deploy).
- Que el comodin cuente para el **umbral** `CHIPS_COLLAPSED_MAX` es **correcto y no se toca**. Lo unico que
  cambia es el numero que se le muestra al dueno.
- El chip comodin sigue siendo **informativo y no clickeable** (D-16). No convertirlo en boton.
</decisiones_ya_tomadas_no_reabrir>

<tasks>

<task type="auto">
  <name>Task 1: Meter duracion y descanso adentro del contrato de guardado, y dejar la invariante como estructura</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx</files>
  <action>
Cierra el **BLOCKER-01** del audit y su causa de raiz. Alcance: `:530-531`, el bloque de `saveHours` `:816-845`,
y los dos bloques de `Select` `:1305-1340`. Nada mas del archivo.

**Paso 0 — baseline de lint, ANTES de editar nada.** Correr
`./node_modules/.bin/eslint "app/(dashboard)/agenda/agenda-client.tsx" 2>&1 | tail -3` y guardar la salida en
`.planning/quick/260831-di2-cerrar-los-3-hallazgos-del-audit-visual-/eslint-baseline.txt`. Tiene que decir
`1 problem (1 error, 0 warnings)` (el `react-hooks/purity` de `:1000`). Si dice otra cosa, **PARAR y reportar**:
el gate de "cero hallazgos nuevos" depende de ese numero.

**Paso 1 — un solo objeto para lo que persiste el boton.** Reemplazar los dos `useState` sueltos de `:530-531`
por un unico estado `hoursConfig` con la misma inicializacion (`business.default_slot_duration ?? 60` y
`business.buffer_minutes ?? 0` como propiedades `slotDuration` y `bufferMinutes`), y agregar inmediatamente
debajo una funcion `updateHoursConfig(patch: Partial<typeof hoursConfig>)` que haga **primero**
`setHoursDirty(true)` y despues el merge inmutable (`setHoursConfig(prev => ({ ...prev, ...patch }))`).
`setHoursDirty(true)` va primero para replicar exactamente la forma de los seis mutadores que ya existen
(`toggleDay :615`, `addBlock :629`, `removeBlock :645`, `updateBlock :660`, `toggleBlockService :676`,
`applyCopyDay :736`).

Ese objeto **es la invariante hecha estructura**, y es el punto del ejercicio: lo que `saveHours` persiste en
`businesses` y lo que el editor tiene en memoria dejan de ser dos listas que hay que acordarse de sincronizar y
pasan a ser **el mismo objeto**. Un campo nuevo agregado ahi ensucia el estado sin que nadie tenga que acordarse
de nada. Escribir esa invariante como comentario **arriba del `useState`**, en dos mitades y diciendo cual es
automatica y cual no:
  - mitad automatica: todo lo que este objeto contiene ensucia el estado, porque el unico camino de escritura es
    `updateHoursConfig`;
  - mitad manual: **congelarse durante el guardado sigue siendo por control** (`disabled={savingHours}`), y por
    eso el control nuevo que lea de este objeto tambien lo necesita.
Ese comentario reemplaza a la enumeracion: no listar "los seis gestos", listar la REGLA.

**Paso 2 — el UPDATE lee del objeto.** En `:835`, el `.update({ ... })` pasa a
`default_slot_duration: hoursConfig.slotDuration, buffer_minutes: hoursConfig.bufferMinutes`. **Literal**: el gate
cuenta esas dos cadenas exactas. El comentario largo que precede al UPDATE (por que estos dos quedan fuera de la
transaccion, y T-19-27) **no se toca**.

**Paso 3 — los dos `Select` entran al contrato.** En `:1311` y `:1329`:
  - `onValueChange` pasa por el mutador: `v => updateHoursConfig({ slotDuration: Number(v) })` y
    `v => updateHoursConfig({ bufferMinutes: Number(v) })`.
  - `value` pasa a `String(hoursConfig.slotDuration)` / `String(hoursConfig.bufferMinutes)`, y el
    `<SelectValue>` de `:1331` a `hoursConfig.bufferMinutes === 0 ? 'Sin descanso' : ...`.
  - agregar `disabled={savingHours}` **en la raiz `<Select>`**, no en el `SelectTrigger`: es el patron in-repo
    (`app/(crm)/admin/negocios/[id]/ficha-client.tsx:407`) y el trigger ya trae `disabled:opacity-50` en su
    className base, asi que el apagado visual queda consistente con los otros 8 controles congelados de la card.

**Paso 4 — la senal no puede apagarse por una mitad que no se guardo.** En el `if (bizError)` de `:837-841`,
agregar `setHoursDirty(true)` junto al `toast.error` existente, con un comentario de una linea explicando el
porque: `setHoursDirty(false)` de `:826` es correcto para el RPC (los horarios SI se guardaron), pero si el
UPDATE de duracion/descanso falla queda configuracion sin persistir y el dueno se quedaria sin ninguna senal —
justo el modo de falla que el indicador existe para cubrir. La copy del toast **no cambia**: ya dice la verdad
completa.

**Paso 5 — extender el docblock de `saveHours` (`:778-789`), no reescribirlo.** Hoy enumera los controles que
congela. Agregarle una linea que suba de la enumeracion a la regla: *lo que este boton persiste — los bloques de
la grilla y el objeto de configuracion de arriba — tiene que (a) ensuciar y (b) congelarse; la primera mitad la
garantiza el objeto, la segunda es por control*. No borrar el razonamiento de CR-01 ni el de "deshabilitar y no
`if (savingHours) return`".

Cero logica nueva: ni una query, ni un RPC, ni un import, ni un componente.
  </action>
  <verify>
    <automated>export PATH="/c/Program Files/Git/usr/bin:$PATH"; cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && F="app/(dashboard)/agenda/agenda-client.tsx" && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint "$F" 2>&1 | grep -q '1 problem (1 error, 0 warnings)' && [ "$(grep -cF 'default_slot_duration: hoursConfig.slotDuration' "$F")" = "1" ] && [ "$(grep -cF 'buffer_minutes: hoursConfig.bufferMinutes' "$F")" = "1" ] && [ "$(grep -cF 'disabled={savingHours}' "$F")" = "10" ] && [ "$(grep -cE 'updateHoursConfig\(' "$F")" -ge 3 ] && [ "$(grep -cE 'setHoursDirty\(true\)' "$F")" -ge 8 ] && echo GATE_OK</automated>
  </verify>
  <done>
`tsc` limpio con el binario local. eslint sobre el archivo sigue en `1 problem (1 error, 0 warnings)`.
El UPDATE lee del objeto (`default_slot_duration: hoursConfig.slotDuration` y `buffer_minutes: hoursConfig.bufferMinutes`,
1 vez cada uno). `disabled={savingHours}` pasa de 8 a **10** ocurrencias. `updateHoursConfig(` aparece al menos 3
veces (definicion + los dos `onValueChange`). `setHoursDirty(true)` pasa de 6 a **8** (los seis mutadores +
`updateHoursConfig` + la rama de `bizError`).
  </done>
</task>

<task type="auto">
  <name>Task 2: Las dos live regions existen antes de tener contenido, sin mover un pixel</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx</files>
  <action>
Cierra **WARNING-03**. Una live region tiene que estar en el DOM **antes** de que cambie su contenido para que un
lector de pantalla la locute; si el nodo entero aparece de golpe, la mayoria de los lectores no dicen nada. Hoy
los dos unicos anuncios de la fase se montan junto con su texto, asi que el contrato afirma algo que el codigo no
hace.

**Sitio A — el chip comodin (`:317-326`).** Se **desacopla** el anuncio del dibujo, por el motivo del hallazgo 5
de este plan (montar siempre el contenedor visible lo deja como item del flex `gap-x-2` y corre la fila 8px):
  - Agregar, como **primer hijo** del `div role="group"` y antes del bloque `{wildcard && ...}`, una live region
    invisible y **siempre montada**, con este markup exacto:
    `<span role="status" className="sr-only">{wildcard ? 'Cualquier servicio' : ''}</span>`
    En Tailwind `sr-only` es `position:absolute`, asi que el nodo esta **fuera del flujo** y **no es item del
    flex**: no consume el `gap-x-2`, no aporta alto, y el layout es identico en los dos estados. Es la unica
    forma de montar siempre la region sin tocar el layout que la UAT ya aprobo.
  - El `<span>` exterior del chip visible (hoy `role="status" className="inline-flex min-h-11 items-center"`)
    pasa a `aria-hidden="true" className="inline-flex min-h-11 items-center"` — pierde el rol y gana el
    `aria-hidden`, para que el texto no se lea dos veces (una por la region, otra por el chip). **El resto del
    chip no se toca**: se conserva el `min-h-11` (es el ancla de altura de D-17), el `<span>` interior con su
    `border-dashed`, el `Asterisk aria-hidden`, el texto y el `whitespace-nowrap`, todo byte-identico.
  - Reescribir la parte del comentario de `:312-314` que hoy explica el rol ARIA sobre el contenedor visible:
    tiene que decir que el anuncio lo hace una region invisible siempre montada (porque una region que aparece
    con su texto adentro no locuta), que el `min-h-11` sigue siendo el que evita el salto de altura, y **por que
    la region va fuera del flujo** (si fuera un item del flex, la fila se correria un gap al aparecer y
    desaparecer el comodin). No borrar la explicacion de D-16 ni la de "nunca coexiste con un chip marcado".

**Sitio B — "Cambios sin guardar" (`:1488-1490`).** Aca **no** hace falta desacoplar: el `<span>` es el ultimo
hijo de un `flex items-center gap-4` de ancho completo, asi que vacio no cambia nada visible. Reemplazar el
`{hoursDirty && (<span .../>)}` por un unico nodo siempre montado con el texto condicionado:
`<span role="status" className="text-xs text-warning">{hoursDirty ? 'Cambios sin guardar' : ''}</span>`
Sigue siendo el **unico** lugar de la fase que usa el token `--warning`, como exige el contrato, y sigue siendo
`role="status"` (polite), nunca `role="alert"`.

**Prohibido:** animar la aparicion/desaparicion (D-17 pide "al instante"), agregar `aria-live` ademas del rol
(redundante), usar `hidden`/`display:none` en la region (un nodo oculto no se anuncia), o cambiar cualquier clase
del chip visible.
  </action>
  <verify>
    <automated>export PATH="/c/Program Files/Git/usr/bin:$PATH"; cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && F="app/(dashboard)/agenda/agenda-client.tsx" && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint "$F" 2>&1 | grep -q '1 problem (1 error, 0 warnings)' && [ "$(grep -cF 'role="status" className="sr-only"' "$F")" = "1" ] && [ "$(grep -cF '<span aria-hidden="true" className="inline-flex min-h-11 items-center">' "$F")" = "1" ] && [ "$(grep -cF "wildcard ? 'Cualquier servicio' : ''" "$F")" = "1" ] && [ "$(grep -cF "hoursDirty ? 'Cambios sin guardar' : ''" "$F")" = "1" ] && [ "$(grep -cF 'border-dashed border-border px-3 text-xs font-medium text-muted-foreground whitespace-nowrap' "$F")" = "1" ] && echo GATE_OK</automated>
    <human-check>
**NO es automatizable y NO es un `checkpoint:human-verify`.** Va como `human-check` a proposito: un checkpoint
seria auto-aprobado por `auto_advance: true` sin que nadie abra nada (trampa ya documentada del proyecto). Como
`human-check`, el ejecutor **no lo ejecuta ni lo aprueba**: lo deja registrado como PENDIENTE en el SUMMARY y en
la tabla de Resolucion de la Task 3. Lo hace una persona, despues del deploy.

Con un lector de pantalla real (Windows: NVDA gratuito, o el Narrador; Mac: VoiceOver con Cmd+F5):
1. Entrar al panel → **Agenda** → la card de la grilla semanal, en un negocio con servicios cargados.
2. En una franja que tenga al menos un servicio marcado, apagar el **ultimo** chip marcado. El lector tiene que
   locutar **"Cualquier servicio"** cuando reaparece el comodin. Hoy, con el nodo montandose junto con su texto,
   lo esperable es que no diga nada.
3. Volver a marcar un servicio: el comodin desaparece y el lector **no** tiene que anunciar nada nuevo.
4. Cambiar un horario (o la duracion del turno): el lector tiene que locutar **"Cambios sin guardar"**.
5. **No-regresion de layout (esto conserva el escenario #2 de la UAT, ya PASADO el 2026-08-27):** con el
   navegador a **375px** de ancho, mirar fijo la fila de chips de una franja en comodin y marcar el primer
   servicio. La fila **no** puede saltar de alto **ni correrse horizontalmente**. Si se corre, el fix reintrodujo
   un item de flex donde no debia haberlo.
6. Con el guardado en vuelo ("Guardando..."), los dos `Select` de duracion y descanso tienen que verse apagados
   igual que el resto de la card, y no abrirse al clickearlos.
    </human-check>
  </verify>
  <done>
`tsc` limpio, eslint en `1 problem (1 error, 0 warnings)`. Existe exactamente **una** live region `sr-only` y
**un** contenedor de chip comodin con `aria-hidden`. Los dos textos anunciables estan condicionados **adentro**
del nodo, no envolviendolo. El markup interior del chip comodin quedo intacto (la cadena larga de clases del pill
sigue apareciendo 1 vez). El `human-check` queda registrado como **PENDIENTE**, no aprobado.
  </done>
</task>

<task type="auto">
  <name>Task 3: El contador deja de contar el comodin, y el contrato queda al dia</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx, .planning/workstreams/motor-reservas/phases/19-el-panel/19-UI-SPEC.md, .planning/workstreams/motor-reservas/phases/19-el-panel/19-UI-REVIEW.md</files>
  <action>
**Parte A — el numero (`WARNING-01`, `:352`).** Cambiar la etiqueta del trigger de `Ver todos (${total})` a
`Ver todos (${shown.length})`. `total` **se queda tal cual** y sigue siendo lo unico que decide `collapsible`
(`:287`): que el comodin cuente para el **umbral** es correcto y esta pedido por el spec. Lo que estaba mal es
que un negocio con 7 servicios y ninguno marcado leyera "Ver todos (8)": ese numero significa "tus servicios".
Ampliar el comentario de `:284-286` en una linea para dejar escrita la distincion **umbral vs numero mostrado**,
sin repetir literales de codigo que los gates cuentan.

**Parte B — enmendar `19-UI-SPEC.md`.** Cuatro ediciones quirurgicas, todas marcadas como una unica
**`> **Enmienda 2026-08-31 (post UI-REVIEW).**`** al pie de la seccion de estados (justo debajo de la enmienda
del 2026-08-26 que ya existe, sin borrarla), mas los retoques puntuales:
  1. **Fila `Guardando (savingHours)` (~:347)** — de enumeracion a REGLA. El texto pasa a decir que se congela
     **todo control cuyo valor persista el boton "Guardar horarios"** (que hoy son: los inputs de hora, la ×, el
     toggle de dia, "Agregar bloque", "Copiar a otros dias", los chips, **y los dos `Select` de duracion del turno
     y descanso entre turnos**), y que la lista es un ejemplo, no la definicion. En la enmienda explicar por que:
     la version anterior enumeraba, y una enumeracion no cubre el control que se agrega despues — que es
     exactamente como los dos `Select` quedaron afuera. **Excepcion declarada:** el trigger "Ver todos" **no** se
     congela, porque el colapso no toca configuracion y no hay nada que perder (cierra el WR-07 documental del
     audit: sin esta linea, el proximo lector "arregla" el codigo en la direccion equivocada).
  2. **Snippet del chip comodin (~:281-291)** — reemplazar el bloque de codigo por el par nuevo: la live region
     `sr-only` siempre montada + el chip visible con `aria-hidden`. Reescribir la vineta de `role="status"`
     (~:291) para que diga **por que**: una region que se monta junto con su texto no se locuta, y la region
     tiene que ir **fuera del flujo** para no consumir el `gap-x-2` de la linea. Mantener intacta la vineta del
     `min-h-11` / cero layout shift.
  3. **Snippet de "Cambios sin guardar" (~:374-377)** — el nodo se monta siempre y lo que se condiciona es el
     texto.
  4. **Trigger (~:266) y checklist de accesibilidad (~:479) y la fila de D-16 (~:546)** — el snippet del trigger
     pasa a `Ver todos (${shown.length})` con una linea aclarando que el comodin cuenta para el **umbral** pero
     no para el numero; el item del checklist y la fila de D-16 dejan de decir que el chip comodin lleva
     `role="status"` y pasan a decir que el comodin tiene **una live region asociada** (polite, nunca
     `role="alert"`). Las filas ~:249 y ~:254 y el diagrama ~:185 **no cambian**: sus ejemplos (10 servicios con
     7 marcados ⇒ "(10)"; 1 marcado ⇒ "(8)") ya son consistentes con el numero nuevo, porque ahi no hay comodin.

**Parte C — cerrar `19-UI-REVIEW.md` con su Resolucion.** Agregar una seccion `## Resolución (2026-08-31, quick
260831-di2)` **inmediatamente despues del bloque de encabezado** (la linea `**UAT visual:** ...`) y **antes** de
`## Pillar Scores`, con el mismo formato de tabla que usa `19-REVIEW.md` §"Resolución" (`| Hallazgo | Estado |
Commit / destino |`). Filas:
  - `BLOCKER-01` → **FIXED**, con el commit de la Task 1, mencionando que ademas se corrigio la causa de raiz
    (objeto unico de configuracion persistida) y el apagado indebido de la senal ante `bizError`.
  - `WARNING-03` (live regions) → **FIXED en codigo, VERIFICACION HUMANA PENDIENTE**, con el commit de la Task 2
    y el detalle de que el anuncio real solo se confirma con un lector de pantalla despues del deploy (los 6
    puntos del `human-check`).
  - `WARNING-01` (`Ver todos (N)`) → **FIXED**, con el commit de esta tarea.
  - `WARNING-02`, `WARNING-04`, `WARNING-05`, `WARNING-06`, `WARNING-08` y el import muerto de `Minus` →
    **NO APLICADOS** — el usuario eligio los 3 priorizados. Una linea por grupo, sin re-discutirlos.
  - `WR-05` / `WR-06` → **fuera de alcance**, aceptados con riesgo documentado (no se re-litigan).
  - `D-13` (`react-hooks/purity` de la vista semanal) → **diferido**, ya en `deferred-items.md`.
  - Los `NO VERIFICADO (necesita ojos)` de los pilares 2, 3, 5 y 6 → siguen abiertos y dependen del deploy; los
    cubre la UAT de la fase, que **no se toca**.
  Cerrar la seccion con una linea de verificacion, igual que `19-REVIEW.md`: `tsc` limpio con binario local,
  eslint acotado sin hallazgos nuevos, suite verde.

**No tocar `19-UAT.md`** ni ningun otro archivo de la fase 19.
  </action>
  <verify>
    <automated>export PATH="/c/Program Files/Git/usr/bin:/c/Program Files/Git/cmd:$PATH"; cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && F="app/(dashboard)/agenda/agenda-client.tsx" && D=".planning/workstreams/motor-reservas/phases/19-el-panel" && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint "$F" 2>&1 | grep -q '1 problem (1 error, 0 warnings)' && [ "$(grep -cF 'Ver todos (${shown.length})' "$F")" = "1" ] && [ "$(grep -cF 'Ver todos (${shown.length})' "$D/19-UI-SPEC.md")" = "1" ] && [ "$(grep -cF 'Enmienda 2026-08-31' "$D/19-UI-SPEC.md")" -ge 1 ] && [ "$(grep -cF 'sr-only' "$D/19-UI-SPEC.md")" -ge 1 ] && [ "$(grep -cF '## Resolución' "$D/19-UI-REVIEW.md")" = "1" ] && [ -z "$(git status --porcelain "$D/19-UAT.md")" ] && npm test 2>&1 | tail -5 && echo GATE_OK</automated>
  </verify>
  <done>
El trigger muestra la cantidad de servicios del negocio y `total` sigue decidiendo el colapso. `19-UI-SPEC.md`
tiene la enmienda fechada del 2026-08-31 con la fila `Guardando` expresada como regla + la excepcion del trigger,
y sus tres snippets al dia. `19-UI-REVIEW.md` tiene su seccion `## Resolución` con los 3 priorizados dispuestos y
el resto declarado explicitamente. `19-UAT.md` sin cambios en `git status`. `npm test` verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueno autenticado → `businesses` (UPDATE de duracion/descanso) | El unico cruce del plan. El UPDATE ya existe, ya lleva `.eq('id', business.id)` y no cambia: lo unico que cambia es **de donde lee** los dos valores (de un objeto de estado en vez de dos `useState`). |
| dueno autenticado → RPC `save_agenda_blocks` | No se toca en absoluto (ni el payload, ni la firma, ni el manejo de error). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-di2-01 | Tampering | `saveHours` — el UPDATE de `default_slot_duration` / `buffer_minutes` | mitigate | Es el BLOCKER: hoy un cambio hecho con el RPC en vuelo hace que el UPDATE escriba el valor **viejo** capturado en la clausura, y `setHoursDirty(false)` borra la senal ⇒ configuracion perdida en silencio, indistinguible de "nunca se configuro". Se cierra con `disabled={savingHours}` en los dos `Select` (el control no puede cambiar mientras la llamada esta en vuelo) + gate automatizado que cuenta 10 ocurrencias del congelamiento. |
| T-di2-02 | Repudiation | Indicador de estado sucio ante fallo parcial del guardado | mitigate | `setHoursDirty(false)` corre **antes** del UPDATE: si el UPDATE falla, el toast dice que esa mitad no se guardo pero la senal visual ya se apago. Paso 4 de la Task 1 vuelve a prender `hoursDirty` en la rama de `bizError`, para que el estado de la pantalla no contradiga al mensaje. |
| T-di2-03 | Information Disclosure | Live region `sr-only` del comodin | accept | Su unico contenido posible es la cadena fija `'Cualquier servicio'` o vacio. No interpola nombres de servicio, ni ids, ni datos del negocio. Superficie nula. |
| T-di2-04 | Elevation of Privilege | Chip comodin | accept | Sigue siendo `<span>` informativo, no clickeable (D-16). El `aria-hidden` que se le agrega no expone ninguna accion nueva; el unico camino para volver al comodin sigue siendo apagar todos los chips (D-17). |
| T-di2-05 | Denial of Service | — | accept | Cambio de estado y JSX en un componente cliente: sin loops nuevos, sin fetch nuevo, sin efectos. `updateHoursConfig` hace un merge inmutable de un objeto de dos claves. |
| T-di2-06 | Spoofing | — | accept | No se toca autenticacion, sesion, RLS ni ninguna ruta publica. El archivo es un componente del dashboard, detras de la sesion del dueno. |

Sin instalaciones de paquetes en este plan (npm/pip/cargo): no aplica el gate de legitimidad ni el checkpoint bloqueante.
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` sale 0. **Usar el binario local**: `npx tsc` es falso verde en este repo.
2. eslint **acotado al archivo tocado** sigue en `1 problem (1 error, 0 warnings)`. El gate es **cero hallazgos
   NUEVOS**, no cero hallazgos: el unico es el `react-hooks/purity` de `:1000` (D-13, fuera de alcance).
   **No correr `npm run lint` completo**: no puede dar exit 0 y se corta por timeout a los 2 minutos.
3. `npm test` (`vitest run`) verde de punta a punta, como control de no-regresion.
4. `git diff --stat` toca **tres** archivos y ninguno mas: `app/(dashboard)/agenda/agenda-client.tsx`,
   `19-UI-SPEC.md`, `19-UI-REVIEW.md`.
5. `git status --porcelain` sobre `19-UAT.md` sale **vacio**.
6. `git diff` sobre el `.tsx` no muestra ningun cambio en `:1000` (vista semanal), ni en `lib/`, ni en la
   construccion del payload del RPC, ni en `classifySaveHoursError`, ni en las copies de rechazo.
7. `git diff` no agrega ni quita ninguna llamada a `supabase.rpc`, `buildSaveHoursPayload`,
   `buildDayStatesFromRows` ni `validateBlocks`.
8. El `human-check` de la Task 2 queda **registrado como pendiente** en el SUMMARY y en la tabla de Resolucion.
   No se marca aprobado: `auto_advance: true` no lo pide ni lo auto-aprueba, y solo lo cierra una persona con un
   lector de pantalla despues del deploy.
</verification>

<success_criteria>
- Cambiar la duracion del turno o el descanso enciende "Cambios sin guardar", igual que cualquier otro gesto del
  editor.
- Los dos `Select` se apagan durante el guardado, junto con los otros 8 controles de la card.
- Un fallo del UPDATE de duracion/descanso deja el indicador **encendido**, coherente con su toast.
- La enumeracion "los seis gestos que ensucian" queda reemplazada por una regla con soporte estructural: lo que el
  boton persiste vive en un objeto unico que es el mismo que lee el UPDATE.
- Las dos live regions existen desde el montaje y solo cambia su contenido.
- La linea de chips no se mueve —ni de alto ni de lado— al marcar el primer servicio a 375px.
- `Ver todos (7)` con 7 servicios, con o sin comodin; el comodin sigue contando para decidir el colapso.
- `19-UI-SPEC.md` enmendado y `19-UI-REVIEW.md` con su Resolucion; `19-UAT.md` intacto.
- Un solo archivo de codigo, cero componentes, cero dependencias, cero logica de negocio.
</success_criteria>

<output>
Create `.planning/quick/260831-di2-cerrar-los-3-hallazgos-del-audit-visual-/260831-di2-SUMMARY.md` when done.

En el SUMMARY dejar registrado, para el proximo que toque esta pantalla:
- que el objeto de configuracion persistida **es** el contrato: agregarle un campo lo hace ensuciar solo, pero el
  control nuevo igual necesita su `disabled` — la mitad del congelamiento sigue siendo manual y esa es la unica
  parte que hay que recordar;
- que la live region del comodin va `sr-only` **a proposito** y no puede volver a envolver al chip visible: si es
  item del flex, la fila se corre un gap y rompe el escenario #2 de la UAT que ya paso;
- que el `human-check` del lector de pantalla quedo **PENDIENTE** y solo se cierra despues del deploy.
</output>
