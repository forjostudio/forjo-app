---
quick_id: 260722-n3a
slug: cerrar-pendientes-de-la-uat-de-la-fase-0
date: 2026-07-22
status: planned
type: execute
workstream: motor-reservas
phase_ref: 07-cancelaci-n-del-abono-mail-panel
autonomous: true
files_modified:
  - "app/(dashboard)/abonos/abonos-client.tsx"
  - ".planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-CONTEXT.md"
  - "app/api/abonos/cancel/route.ts"
  - ".planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-SECURITY.md"
must_haves:
  truths:
    - "Ante un fallo de RED al pedir el link de baja, el dueño ve un toast de error (no una promesa rechazada en consola) y el portapapeles queda intacto"
    - "D-14 y D-15 dicen lo que el código realmente hace: el aviso al dueño sale SOLO cuando la baja la inicia el cliente"
    - "El comentario de la vía panel enuncia el criterio del recibo y ya no cita decisiones truncadas"
    - "La disposición de T-07-19 para la vía panel está escrita: el rastro ahí es de datos (cancelled_at + status + fila en Archivados)"
  artifacts:
    - path: "app/(dashboard)/abonos/abonos-client.tsx"
      provides: "copyCancelLink con catch de red"
    - path: ".planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-CONTEXT.md"
      provides: "D-14 y D-15 enmendadas con rastro fechado"
    - path: "app/api/abonos/cancel/route.ts"
      provides: "comentario (8) alineado con la decisión"
    - path: ".planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-SECURITY.md"
      provides: "T-07-19 con disposición explícita por vía + audit trail 2026-07-22"
  key_links:
    - from: "07-CONTEXT.md D-14/D-15"
      to: "app/api/abonos/cancel/route.ts:109"
      via: "el comentario cita las decisiones enmendadas"
---

<objective>
Cerrar los 4 pendientes que dejó la UAT de la Phase 07 (`07-UAT.md`, tests 4 y 11).

Uno es código (un `catch` faltante). Los otros tres son registro: alinear las decisiones y la
disposición de seguridad con el comportamiento que el usuario ya decidió que es el correcto.

Purpose: que el estado escrito de la fase coincida con el código, y que el dueño reciba feedback
cuando el pedido del link de baja falla por red.
Output: 1 fix de UI + 3 documentos alineados, en 3 commits.
</objective>

<context>
El diagnóstico YA está hecho y verificado durante la UAT. **No re-diagnostiques nada**: cada tarea
trae archivo, línea y el arreglo exacto.

@.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-UAT.md
@.planning/workstreams/motor-reservas/STATE.md
@.claude/skills/convenciones-forjo/SKILL.md

**Decisión del usuario (2026-07-22) que gobierna las tareas 2 y 3:** el aviso al dueño es un
**recibo de algo que le OCURRIÓ**, no de algo que él ejecutó.

| Vía | Quién inicia | Mails |
|-----|--------------|-------|
| Link público (`/abono/cancelar/[token]`) | el CLIENTE | mail al cliente + aviso al dueño |
| Panel (`POST /api/abonos/cancel`) | el DUEÑO | solo mail al cliente |

Esto **ya está implementado así** y es lo correcto. Lo único que falta es el registro.
</context>

<constraints>
- **Comandos de verificación (regla dura del proyecto):** usar SIEMPRE los binarios locales
  `./node_modules/.bin/tsc --noEmit`, `./node_modules/.bin/eslint`, `./node_modules/.bin/vitest`.
  **NUNCA `npx tsc`**: resuelve un paquete equivocado del registry que SIEMPRE sale exit 0 (falso
  verde). Ya provocó una verificación inválida en esta misma fase.
- NO agregar dependencias. NO correr `npm install` ni `npm ci`.
- El **único** cambio de comportamiento permitido es el `catch` de la Tarea 1. Todo lo demás es
  documentación y comentarios.
- NO tocar `lib/abono-cancel.ts` ni `app/api/abonos/cancel/[token]/route.ts` (la ruta pública).
- Comentarios en español explicando el *por qué*, según la convención del repo.
- Un commit por tarea.
</constraints>

<tasks>

<task type="auto">
  <name>Tarea 1: catch de red en copyCancelLink (el único cambio de código)</name>
  <files>app/(dashboard)/abonos/abonos-client.tsx</files>
  <action>
En `copyCancelLink` (~líneas 144-168) el bloque es hoy `try { ... } finally { setCopyingLink(false) }`,
SIN `catch`. El `toast.error('No se pudo obtener el link de baja. Probá de nuevo.')` de la línea ~153
solo cubre el fallo HTTP (`!res.ok || !data?.ok || !url`). Cuando el fallo es de RED (offline, datos
móviles cortados, DNS caído) el propio `fetch` rechaza antes de devolver `res`: la promesa queda sin
capturar (`Uncaught (in promise) TypeError: Failed to fetch`, reproducido en la UAT con DevTools →
Offline) y el dueño no ve NINGÚN mensaje.

Agregar un bloque `catch` entre el `try` y el `finally` que muestre EXACTAMENTE el mismo
`toast.error('No se pudo obtener el link de baja. Probá de nuevo.')` que ya usa la rama HTTP. El
mensaje del toast es genérico a propósito: no interpolar el error capturado.

Sumar arriba del `catch` un comentario breve en español que diga por qué existe: el fallo de red
rechaza el `fetch` antes de que haya `res`, así que la rama HTTP no lo alcanza; sin este bloque el
dueño se queda sin feedback y el botón parece no haber hecho nada.

Lo que NO se toca y NO se puede romper:
- El `finally { setCopyingLink(false) }` se queda tal cual (ya devuelve el botón a su estado; en la
  UAT esa parte funcionó).
- El `try/catch` interno del portapapeles (líneas ~156-164) queda intacto: ya maneja su propio caso
  y NO re-lanza, así que el `catch` nuevo no lo va a duplicar.
- Ante fallo NO se escribe nada al portapapeles. Eso ya funciona bien y es deliberado (D-17): el
  dueño reintenta antes que pegarle a su cliente un link roto.
- Ninguna otra función del archivo. Nada de `confirmCancel`, nada del listado, nada de los tabs.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
    <automated>./node_modules/.bin/eslint "app/(dashboard)/abonos/abonos-client.tsx"</automated>
    <automated>grep -c "No se pudo obtener el link de baja" "app/(dashboard)/abonos/abonos-client.tsx"  # debe imprimir 2 (rama HTTP + catch de red)</automated>
    <automated>grep -c "setCopyingLink(false)" "app/(dashboard)/abonos/abonos-client.tsx"  # debe seguir imprimiendo 1 (el finally intacto)</automated>
    <human-check>Re-correr el test 4 de la UAT: `npm run dev` → /abonos → DevTools → Network → Offline → "Copiar link de baja". Debe aparecer el toast de error, la consola NO debe mostrar `Uncaught (in promise)`, el botón vuelve de "Copiando..." y el portapapeles conserva lo que tenía.</human-check>
  </verify>
  <done>
`tsc --noEmit` y `eslint` en exit 0; el literal del toast aparece 2 veces; el `finally` sigue siendo
uno solo; en offline el dueño ve el toast y la consola queda sin promesas rechazadas.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: enmendar D-14/D-15 y realinear el comentario que las cita</name>
  <files>.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-CONTEXT.md, app/api/abonos/cancel/route.ts</files>
  <action>
Las dos partes van juntas en un commit: la enmienda y el comentario que la cita están acoplados.

**(a) `07-CONTEXT.md`, bloque "Mails (política anti-avalancha)", líneas ~69-77.**
Hoy D-14 (marcada LOCKED) dice "La baja manda UN solo mail de baja de serie al cliente y UN aviso al
dueño" sin distinguir por qué vía se inició, y D-15 cierra con "Esto mantiene las dos vías 100%
consistentes también en notificaciones". Las dos contradicen la decisión del 2026-07-22.

Enmendar AMBAS **dejando rastro**: no reescribas la historia en silencio. Conservá el texto original
de cada decisión y agregale debajo, como sub-bullet, una línea que empiece con el marcador literal
`**ENMENDADA 2026-07-22**` (una en D-14 y otra en D-15, dos en total) que diga:
- el criterio del recibo (el aviso al dueño informa algo que le OCURRIÓ; por el panel la baja la
  ejecuta él mismo, así que avisarle sería redundante);
- por vía: cliente → mail al cliente + aviso al dueño; dueño → solo mail al cliente;
- qué parte de la decisión original sigue en pie. En D-14: el anti-avalancha (UN mail por baja,
  NUNCA uno por turno cancelado) sigue LOCKED e intacto — la enmienda solo precisa el destinatario
  según la vía. En D-15: el cliente sigue recibiendo SIEMPRE su mail cuando el dueño da de baja; lo
  que se cae es la afirmación de paridad total de notificaciones;
- el origen: UAT de la fase, test 11 (`07-UAT.md`, sección `## Gaps`, entrada
  `resolved_by_decision`), decisión del usuario del 2026-07-22;
- que el CÓDIGO no cambia porque ya hace lo correcto;
- que la enmienda ALINEA D-14/D-15 con D-13, que ya decía "cuando el **cliente** da de baja, se manda
  mail a `notification_email`" — o sea, D-13 nunca prometió el aviso por la vía del panel.

No toques ninguna otra decisión del archivo (D-01..D-13, D-16..D-21 quedan como están).

**(b) `app/api/abonos/cancel/route.ts`, comentario del bloque (8), línea ~109.**
Hoy arranca: "(8) Baja efectiva → UN solo mail al cliente (D-14/D-15), best-effort en after()...".
Cita D-14/D-15 para justificar justo lo contrario de lo que esas decisiones decían.

Reescribir ese comentario para que enuncie el criterio: por el panel la baja la ejecuta el propio
dueño, así que avisarle sería redundante; el aviso al dueño existe solo en la vía pública, donde la
baja la inicia el cliente. Mantener la cita a D-14/D-15 (después de la enmienda de (a) ya dicen esto)
y conservar lo que el comentario ya explicaba bien: best-effort en `after()` para no demorar la
respuesta, captura previa en consts de todo lo que necesita el closure, y que si el mail falla se
loguea sin romper la baja.

**Es SOLO el comentario: ni una línea de lógica.** No se agrega ni se quita ningún envío, ningún
import, ninguna condición. `sendAbonoCancelledAdminNotification` NO entra en este archivo.
  </action>
  <verify>
    <automated>grep -c "ENMENDADA 2026-07-22" ".planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-CONTEXT.md"  # debe imprimir 2 (D-14 y D-15)</automated>
    <automated>grep -c "D-14/D-15" "app/api/abonos/cancel/route.ts"  # debe seguir imprimiendo 1</automated>
    <automated>git diff HEAD -U0 -- "app/api/abonos/cancel/route.ts" | grep -E "^[+-][^+-]" | grep -vcE "^[+-]\s*(//|\*|/\*)" || true  # debe imprimir 0: TODAS las líneas cambiadas son comentario</automated>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <done>
D-14 y D-15 llevan cada una su marca `**ENMENDADA 2026-07-22**` con criterio, vías, origen y qué
sigue vigente; el comentario (8) de la ruta del panel enuncia el criterio del recibo; el diff de
`route.ts` no contiene una sola línea que no sea comentario; `tsc --noEmit` en exit 0.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: disposición explícita de T-07-19 para la vía del panel</name>
  <files>.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-SECURITY.md</files>
  <action>
T-07-19 (Repudiation — "baja sin rastro para el negocio", tabla del plan 07-03, línea ~86) hoy
declara como mitigación "D-13: aviso a `notification_email` + `cancelled_at` visible en el panel".
Eso describe solo la vía pública. En la vía del PANEL ese mail no existe **por diseño** (ver Tarea 2),
así que el rastro ahí es de DATOS.

**(a)** Editar la fila de T-07-19 EN SU LUGAR (misma tabla, misma fila) para que la disposición quede
por vía:
- vía pública (cliente): aviso a `businesses.notification_email` (D-13) + el rastro en datos;
- vía panel (dueño): la baja la ejecuta el propio dueño, así que el rastro es
  `abonos.cancelled_at` + `abonos.status = 'cancelled'`, más la fila visible bajo el tab
  **Archivados** de `/abonos`.

Evidencia a citar (ya verificada, no la re-investigues): `cancelled_at` y el `status` se escriben en
`lib/abono-cancel.ts:211-216`; `app/(dashboard)/abonos/page.tsx:39` los lee; el tab Archivados filtra
`status !== 'active'` en `app/(dashboard)/abonos/abonos-client.tsx:129`.

La `Disposition` sigue siendo `mitigate` y el `Status` sigue siendo `closed`. No se está abriendo una
amenaza nueva: se está precisando la disposición de una ya cerrada.

**(b)** Agregar una fila al `## Security Audit Trail` con fecha `2026-07-22`, `Run By` =
`quick 260722-n3a (Claude)`, y en la columna `Closed` una nota corta que explique el origen: la UAT de
la fase (test 11) + la decisión del usuario del 2026-07-22 precisaron la disposición de T-07-19 para
la vía del panel; el rastro ahí es de datos, no de mail.

**RESTRICCIÓN DURA del frontmatter:** `threats_open: 0` NO puede subir. Si tu edición NO agrega filas
al registro de amenazas — que es el caso esperado, porque (a) edita una fila existente — entonces
`threats_total: 68` y `threats_closed: 68` quedan **exactamente igual**. Solo tocalos si realmente
agregaste filas nuevas al registro, y en ese caso `total` y `closed` suben juntos y `open` sigue en 0.

No toques el `Accepted Risks Log`, ni R-01/R-02/R-03, ni los `Unregistered Flags`, ni el `Sign-Off`,
ni ninguna otra fila del registro.
  </action>
  <verify>
    <automated>grep -cE "^threats_(total|closed): 68" ".planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-SECURITY.md"  # debe imprimir 2 (sin cambios)</automated>
    <automated>grep -c "^threats_open: 0" ".planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-SECURITY.md"  # debe imprimir 1</automated>
    <automated>grep -c "260722-n3a" ".planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-SECURITY.md"  # debe imprimir 1 (la fila nueva del audit trail)</automated>
    <automated>grep -n "T-07-19" ".planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-SECURITY.md"  # la fila del registro debe mencionar "panel" y "cancelled_at", con Disposition mitigate y Status closed</automated>
  </verify>
  <done>
La fila de T-07-19 distingue las dos vías y nombra el rastro de datos del panel con su evidencia; el
audit trail tiene la entrada del 2026-07-22 explicando el origen; el frontmatter conserva
`threats_open: 0` y no infló los contadores.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño autenticado → `GET /api/abonos/cancel-link/[id]` | superficie que ya existe; esta tarea solo cambia el manejo del error del lado del cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q1-01 | Information Disclosure | toast del `catch` nuevo en `copyCancelLink` | mitigate | el toast usa el mensaje genérico ya existente; NO se interpola el error capturado (un `TypeError` de red o un cuerpo de respuesta no deben llegar a la UI del dueño) |
| T-Q1-02 | Repudiation | fallo silencioso del pedido de la credencial de baja | mitigate | el `catch` garantiza feedback explícito ante fallo de red; el portapapeles sigue sin ensuciarse (D-17) |
| T-Q1-03 | Tampering | edición de un documento de seguridad ya verificado | mitigate | `threats_open: 0` no sube; se precisa la disposición de una amenaza cerrada y se deja entrada fechada en el audit trail |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | esta tarea NO instala paquetes: `package.json` y `package-lock.json` no se tocan |
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` → exit 0 (NUNCA `npx tsc`).
2. `./node_modules/.bin/eslint "app/(dashboard)/abonos/abonos-client.tsx"` → exit 0.
3. `git diff HEAD --name-only` al final = exactamente los 4 archivos declarados en `files_modified`.
4. El diff de `app/api/abonos/cancel/route.ts` contiene únicamente líneas de comentario.
5. `lib/abono-cancel.ts` y `app/api/abonos/cancel/[token]/route.ts` NO aparecen en el diff.
</verification>

<success_criteria>
- En offline, "Copiar link de baja" muestra el toast de error y no deja una promesa sin capturar.
- D-14 y D-15 dicen lo mismo que hace el código, con el rastro de la enmienda fechado.
- El comentario de la vía panel enuncia el criterio del recibo.
- T-07-19 tiene su disposición escrita por vía, con `threats_open: 0` intacto.
- 3 commits: uno de código, uno de la enmienda + comentario, uno de seguridad.
</success_criteria>

<output>
Crear `.planning/quick/260722-n3a-cerrar-pendientes-de-la-uat-de-la-fase-0/260722-n3a-SUMMARY.md` al terminar.
</output>
