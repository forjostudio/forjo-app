---
phase: quick-260723-p76
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/appointments/appointments-client.tsx
autonomous: false
requirements:
  - QUICK-P76
must_haves:
  truths:
    - "El control de fecha muestra 'Fecha' cuando no hay filtro (nunca un input nativo roto/vacío)"
    - "Al abrir el control se ven 4 opciones: Hoy, Esta semana, Este mes, Elegir fecha…"
    - "Elegir 'Hoy' deja solo los turnos de la fecha de hoy"
    - "Elegir 'Esta semana' deja los turnos de la semana actual lunes a domingo"
    - "Elegir 'Este mes' deja los turnos del mes calendario actual"
    - "Elegir 'Elegir fecha…' despliega el calendario de la app en español y filtra por el día elegido"
    - "El filtro de fecha se combina (AND) con los tabs Próximos/Pasados/Todos y con Profesional/Estado"
    - "El botón 'Limpiar' vuelve el control a 'Fecha' y resetea también Profesional/Estado"
  artifacts:
    - path: "app/(dashboard)/appointments/appointments-client.tsx"
      provides: "Selector de rango de fecha (Hoy/Semana/Mes/Elegir fecha) reemplazando el input nativo"
      contains: "dateMode"
  key_links:
    - from: "app/(dashboard)/appointments/appointments-client.tsx (trigger + dropdown)"
      to: "app/(dashboard)/appointments/appointments-client.tsx (estado dateMode/filterDate)"
      via: "los clicks de las opciones setean dateMode/filterDate y disparan el re-filtrado"
      pattern: "setDateMode"
    - from: "app/(dashboard)/appointments/appointments-client.tsx (filtro)"
      to: "date-fns (startOfWeek/endOfWeek/startOfMonth/endOfMonth)"
      via: "helper de rango que compara a.date (string 'yyyy-MM-dd') por comparación lexicográfica"
      pattern: "startOfWeek|endOfWeek|startOfMonth|endOfMonth"
---

<objective>
Reemplazar el `<input type="date">` "Filtrar por fecha" de la página de Turnos por un control de rango relativo a HOY: **Hoy · Esta semana · Este mes · Elegir fecha…**, con el calendario de la app desplegado en "Elegir fecha…" y la lógica de filtrado por rango detrás.

Purpose: el input nativo se ve roto/vacío y solo filtra por fecha exacta. Un selector de rango con presets cubre el uso real (ver la agenda de hoy / de la semana / del mes) sin depender del date-picker del navegador.

Output: `app/(dashboard)/appointments/appointments-client.tsx` modificado. Un solo archivo de producción. Sin dependencias nuevas.
</objective>

<context>
@app/(dashboard)/appointments/appointments-client.tsx
@app/(dashboard)/agenda/agenda-client.tsx
@app/(dashboard)/finances/finances-client.tsx
@components/ui/calendar.tsx

Notas de contexto (ya verificadas):
- `date-fns` YA está importado en el archivo: `import { format, parseISO } from 'date-fns'` (línea 4) y `import { es } from 'date-fns/locale'` (línea 5). Falta agregar `startOfWeek, endOfWeek, startOfMonth, endOfMonth` al import de `date-fns`.
- El componente `Calendar` (`@/components/ui/calendar`) NO está importado todavía en este archivo — hay que agregar el import. YA trae `locale = es` por default; NO pasar `locale` y NO tocar `components/ui/calendar.tsx`.
- Patrón EXACTO de uso del Calendar (copiar de `agenda-client.tsx` ~860-865): `<Calendar mode="single" selected={filterDate ? parseISO(filterDate) : undefined} onSelect={...} />`.
- Patrón de dropdown sin primitivo Popover (de `finances-client.tsx` ~105): `absolute z-50 mt-1 bg-popover border border-border rounded-md shadow-lg`.
- Comparación de fechas por string ISO ('yyyy-MM-dd') = comparación de fechas; el archivo YA lo usa en las líneas 104-105 (`a.date < today`, `a.date >= today`).
- Windows + PowerShell: la ruta del archivo tiene paréntesis; citarla siempre entre comillas en los comandos.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estado y lógica de filtrado por rango de fecha</name>
  <files>app/(dashboard)/appointments/appointments-client.tsx</files>
  <action>
Agregar el modelo de estado y la lógica de filtrado, SIN tocar todavía la UI del control (eso es Task 2).

1) Imports:
   - En el import de `date-fns` (línea 4) agregar `startOfWeek, endOfWeek, startOfMonth, endOfMonth` junto a `format, parseISO`. NO duplicar `format`/`parseISO`.
   - Agregar el import del calendario: `import { Calendar } from '@/components/ui/calendar'` (junto a los demás imports de `@/components/ui/*`). NO pasar `locale` cuando se use más adelante — el componente ya lo trae.

2) Estado (junto a los `useState` existentes, ~línea 90). Conservar `filterDate` tal cual (string 'yyyy-MM-dd') pero ahora solo representa la fecha puntual del modo custom. Agregar:
   - `const [dateMode, setDateMode] = useState<'none' | 'today' | 'week' | 'month' | 'custom'>('none')`
   - `const [dateOpen, setDateOpen] = useState(false)` — abre/cierra el dropdown del control.
   - `const [calView, setCalView] = useState(false)` — dentro del dropdown, alterna entre la lista de opciones y el calendario.

3) Helper de rango (a nivel módulo, fuera del componente, junto a los otros helpers de módulo como `StatusBadge`/`RowActions`). Firma: `function dateRangeFor(mode: 'today' | 'week' | 'month', ref: Date): { from: string; to: string }`. Devuelve strings 'yyyy-MM-dd':
   - `'today'` → from = to = `format(ref, 'yyyy-MM-dd')`.
   - `'week'` → `startOfWeek(ref, { weekStartsOn: 1 })` .. `endOfWeek(ref, { weekStartsOn: 1 })` (lunes a domingo), cada extremo formateado con `format(..., 'yyyy-MM-dd')`.
   - `'month'` → `startOfMonth(ref)` .. `endOfMonth(ref)`, mismo formateo.
   NO usar fenced code; describir con estos identificadores.

4) Reemplazar EXACTAMENTE la línea 106 actual (`if (filterDate && a.date !== filterDate) return false`) por la lógica según `dateMode`, manteniendo el resto del `.filter(...)` intacto:
   - Si `dateMode === 'custom'`: si `filterDate` está seteado y `a.date !== filterDate`, `return false`.
   - Si `dateMode === 'today' | 'week' | 'month'`: calcular `const r = dateRangeFor(dateMode, new Date())` y si `a.date < r.from || a.date > r.to`, `return false` (comparación lexicográfica de strings ISO, igual patrón que las líneas 104-105).
   - Si `dateMode === 'none'`: no filtra por fecha.
   Este bloque se SUMA (AND) a los checks de `tab`, `filterPro` y `filterStatus` que ya existen — NO tocarlos.

No cambiar todavía el JSX de `<div className="relative">…</div>` (líneas ~254-261) ni el botón "Limpiar" — eso es Task 2. El objetivo de Task 1 es que compile con el estado y la lógica nuevos aunque `dateMode` siga en `'none'` (el input nativo sigue presente pero ya no participa del filtro; quedará huérfano hasta Task 2, aceptable como paso intermedio ya que Task 2 es obligatoria en el mismo plan).
  </action>
  <verify>
    <automated>Desde la raíz del repo: `./node_modules/.bin/tsc --noEmit` sale 0 (sin errores de tipos por el nuevo union `dateMode` ni por los imports de date-fns). NUNCA usar `npx tsc`.</automated>
  </verify>
  <done>Compila sin errores de TypeScript. Existen `dateMode`, `dateOpen`, `calView` y el helper `dateRangeFor`. La línea de filtro por fecha exacta fue reemplazada por la lógica por `dateMode`. Los imports de `date-fns` incluyen `startOfWeek/endOfWeek/startOfMonth/endOfMonth` y está importado `Calendar`.</done>
</task>

<task type="auto">
  <name>Task 2: Control de UI (trigger + dropdown + calendario) y botón Limpiar</name>
  <files>app/(dashboard)/appointments/appointments-client.tsx</files>
  <action>
Reemplazar el `<div className="relative">…</div>` que hoy contiene el `<Input type="date">` con overlay (líneas ~254-261) por el nuevo control de rango, y actualizar el botón "Limpiar". Mantener la barra pareja con los `<Select>` vecinos de Profesional/Estado (mismo alto/ancho `w-44`).

1) Trigger: un `<button type="button">` con el mismo look que un `SelectTrigger className="w-44"` de los selects vecinos (mismo alto, borde `border-input`, `rounded-md`, texto `text-sm`, padding horizontal, `flex items-center justify-between`, con un chevron `ChevronDown` de `lucide-react` a la derecha — agregar `ChevronDown` al import de `lucide-react` de la línea 14). Al hacer click: `setDateOpen(v => !v)` y `setCalView(false)` (siempre abre en la lista de opciones).
   - Label del trigger según `dateMode`: `'none'` → "Fecha"; `'today'` → "Hoy"; `'week'` → "Esta semana"; `'month'` → "Este mes"; `'custom'` → la fecha formateada en español, ej. `format(parseISO(filterDate), "d 'de' MMM", { locale: es })`. Cuando `dateMode === 'none'` el texto va en `text-muted-foreground` (placeholder), en el resto en color de texto normal — imitando el contraste de los selects vecinos.

2) Dropdown: envolver trigger + panel en un `<div className="relative">`. El panel se muestra si `dateOpen` con el patrón de finances: `absolute z-50 mt-1 bg-popover border border-border rounded-md shadow-lg` (ancho suficiente para las opciones; para el calendario usar el ancho natural del `Calendar`). Cierre al click-afuera: usar un `ref` sobre el `<div className="relative">` y un `useEffect` con listener `mousedown` en `document` que hace `setDateOpen(false)` si el click cae fuera del ref (limpiar el listener en el cleanup). Alternativa aceptable: un backdrop `fixed inset-0 z-40` detrás del panel que cierra al click — elegir la opción más limpia; preferir el patrón `ref` + `mousedown`.

3) Contenido del panel:
   - Si `!calView`: lista de 4 botones full-width (`w-full text-left px-3 py-2 text-sm hover:bg-accent transition-colors`, patrón de finances):
     - "Hoy" → `setDateMode('today'); setDateOpen(false)`.
     - "Esta semana" → `setDateMode('week'); setDateOpen(false)`.
     - "Este mes" → `setDateMode('month'); setDateOpen(false)`.
     - "Elegir fecha…" → `setCalView(true)` (NO cierra; cambia el panel al calendario).
   - Si `calView`: renderizar el calendario copiando el patrón EXACTO de `agenda-client.tsx` ~860-865:
     `<Calendar mode="single" selected={filterDate ? parseISO(filterDate) : undefined} onSelect={d => { if (d) { setFilterDate(format(d, 'yyyy-MM-dd')); setDateMode('custom') } setDateOpen(false); setCalView(false) }} />`.
     NO pasar `locale`. Envolver en el borde/panel del popover como en agenda (`inline-block rounded-lg`… opcional, mantener consistencia visual). NO agregar `disabled` (a diferencia de agenda, acá se puede filtrar por fechas pasadas — los tabs Pasados/Todos las muestran).

4) Botón "Limpiar" (líneas ~280-284): cambiar la condición de visibilidad de `(filterDate || filterPro !== 'all' || filterStatus !== 'all')` a `(dateMode !== 'none' || filterPro !== 'all' || filterStatus !== 'all')`, y el `onClick` para resetear también la fecha: además de `setFilterPro('all'); setFilterStatus('all')`, agregar `setDateMode('none'); setFilterDate('')` (y cerrar el control: `setDateOpen(false); setCalView(false)`).

5) Eliminar por completo el `<Input type="date">` con su `<span>` overlay. Si `Input` deja de usarse en el resto del archivo, quitar su import (línea 10) para no dejar import sin usar (verificar con eslint). Si `Input` se sigue usando en otra parte del archivo, dejarlo.

No tocar los tabs Próximos/Pasados/Todos ni los `<Select>` de Profesional/Estado (salvo que ya quedan alineados con el nuevo trigger).
  </action>
  <verify>
    <automated>Desde la raíz del repo, los tres verdes: (1) `./node_modules/.bin/tsc --noEmit` sale 0; (2) `./node_modules/.bin/eslint "app/(dashboard)/appointments/appointments-client.tsx"` sin errores (incluido no-unused-vars: si se quitó `Input`, que no quede su import; si se usa `ChevronDown`, que esté importado); (3) `npm run build` completa sin errores. NUNCA usar `npx tsc`. Correr además `./node_modules/.bin/vitest run --no-file-parallelism` para confirmar que la suite existente sigue verde (este componente no tiene tests; se corre solo como no-regresión).</automated>
  </verify>
  <done>El input nativo de fecha ya no existe. El trigger muestra "Fecha" con `dateMode='none'` y el label correcto en cada modo. Al abrir aparecen las 4 opciones; Hoy/Semana/Mes cierran y filtran; "Elegir fecha…" muestra el calendario en español y al elegir un día filtra por esa fecha y el trigger muestra la fecha. Click-afuera cierra. "Limpiar" resetea fecha + profesional + estado y solo aparece cuando hay algún filtro activo. `tsc`, `eslint`, `npm run build` verdes; suite de vitest sin regresiones.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
Selector de rango de fecha en la página de Turnos (`/appointments`) que reemplaza el input nativo roto: opciones Hoy · Esta semana · Este mes · Elegir fecha… (relativas a HOY), con el calendario de la app en "Elegir fecha…" y filtrado por rango. El executor ya cerró tsc/eslint/build/vitest; falta la validación visual en el navegador, que NO puede autocerrar el executor.
  </what-built>
  <how-to-verify>
Con la app corriendo (`npm run dev`) e iniciada sesión en un negocio con turnos, ir a la página de Turnos:
1. El control de filtro de fecha muestra "Fecha" (no un campo vacío/roto) y está alineado con los selects Profesional/Estado.
2. Abrir el control: aparecen exactamente 4 opciones — Hoy, Esta semana, Este mes, Elegir fecha…
3. "Hoy": la lista queda solo con turnos de la fecha de hoy; el trigger muestra "Hoy".
4. "Esta semana": quedan los turnos de esta semana de LUNES a DOMINGO; el trigger muestra "Esta semana".
5. "Este mes": quedan los turnos del mes calendario actual; el trigger muestra "Este mes".
6. "Elegir fecha…": se despliega el calendario en español (meses/días en es); elegir un día → la lista filtra por esa fecha y el trigger muestra la fecha (ej. "5 de ago").
7. Click fuera del control lo cierra sin romper nada.
8. Combinación: con un modo de fecha activo, cambiar los tabs Próximos/Pasados/Todos y los selects Profesional/Estado → el filtro de fecha se mantiene y se combina (AND) con el resto.
9. "Limpiar": vuelve el control a "Fecha" y resetea también Profesional/Estado; el botón "Limpiar" solo aparece cuando hay algún filtro activo.
10. Revisar en mobile (~375px) que la barra de filtros no se rompe y el dropdown se ve completo.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" o describí los problemas encontrados.</resume-signal>
</task>

</tasks>

<verification>
- `./node_modules/.bin/tsc --noEmit` → 0 errores.
- `./node_modules/.bin/eslint "app/(dashboard)/appointments/appointments-client.tsx"` → sin errores ni imports huérfanos.
- `npm run build` → build exitoso.
- `./node_modules/.bin/vitest run --no-file-parallelism` → suite existente verde (no-regresión; el componente no tiene tests propios).
- Checkpoint humano (bloqueante): validación visual del control en el navegador según los 10 pasos.
</verification>

<success_criteria>
- El `<input type="date">` fue eliminado y reemplazado por el selector de rango.
- `dateMode` con default `'none'` y label del trigger correcto por modo; `filterDate` conservado solo para el modo custom.
- Filtros: Hoy (día exacto), Esta semana (lunes-domingo), Este mes (mes calendario), Elegir fecha (día puntual) — todos por comparación de strings ISO y sumados (AND) a tabs + Profesional + Estado.
- Calendario de la app en español dentro del dropdown, sin tocar `components/ui/calendar.tsx` y sin pasar `locale`.
- "Limpiar" resetea fecha + profesional + estado y aparece solo cuando hay filtro activo.
- Sin dependencias nuevas. Un solo archivo de producción tocado.
- tsc / eslint / build / vitest verdes (cierre del executor) + checkpoint humano aprobado.
</success_criteria>

<output>
Crear `.planning/quick/260723-p76-turnos-selector-de-rango-de-fecha-hoy-se/260723-p76-SUMMARY.md` al terminar.
</output>
