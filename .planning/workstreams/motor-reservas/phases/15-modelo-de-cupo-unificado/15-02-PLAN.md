---
phase: 15-modelo-de-cupo-unificado
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/(dashboard)/settings/settings-client.tsx
  - test/helpers/booking-fixtures.ts
  - test/concurrency.test.ts
  - test/booking-cualquiera-public.test.ts
autonomous: true
requirements: [CUPO-06, CUPO-08]

must_haves:
  truths:
    - "El editor de servicios no puede producir una combinación que el CHECK de coherencia rechace"
    - "Crear un servicio nuevo sin tocar nada lo deja en modo individual con cupo 1"
    - "Elegir clase grupal o recurso simultáneo desde individual sube el cupo a 2 automáticamente"
    - "El rechazo de CUPO-08 llega al dueño como copy propia, nunca como el texto crudo del error"
    - "Ninguna escritura de test viola el CHECK de coherencia, en ninguno de sus dos sentidos"
    - "Los cuatro afterEach y los tres simultaneos de cupo 1 quedan legales y las dos suites verdes"
  artifacts:
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "Tercera opción de modo, defaults en individual, piso de cupo por modo y mapeo del rechazo"
      contains: "individual"
    - path: "test/helpers/booking-fixtures.ts"
      provides: "seedGroupClassService y los comentarios de los fixtures corregidos"
      contains: "seedGroupClassService"
  key_links:
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "supabase/migrations/068_service_capacity_unified_and_mode_gate.sql"
      via: "el piso de cupo por modo espeja el CHECK de coherencia y el mapeo espeja el código del gate"
      pattern: "service_mode_has_future_appointments"
---

<objective>
Sumar **lo mínimo** para que la 068 no rompa nada que hoy funciona: el editor de servicios no puede
producir una combinación ilegal, y los cuatro `afterEach` de la suite dejan de escribir una que ahora
lo es.

Purpose: `settings-client.tsx` fuerza hoy `capacity = 1` para todo lo que no sea recurso simultáneo.
Con el CHECK de coherencia de D-06 (`group_class ⇒ cupo >= 2`), elegir "Clase grupal" en el editor de
hoy **rebota contra el constraint** y el dueño ve un toast genérico sobre una pantalla que antes
funcionaba.

Y del lado de los tests, el CHECK vuelve ilegales **dos** combinaciones, no una: cuatro escrituras de
clase grupal con cupo 1 (en `afterEach` y dentro de casos) **y** tres siembras de recurso simultáneo con
cupo 1 — estas últimas a través de un helper que hace `throw`, así que no degradan: **rompen el caso
entero con `23514`**. Sin tocar las siete, **la suite de la propia fase se cae** apenas la 068 esté
aplicada en local.

**Lo que este plan NO hace:** la UX completa del editor —el copy que explique la diferencia real entre
grupal (se cuenta por hora de inicio: todos arrancan juntos) y simultáneo (se cuenta por solape: entran
escalonados), el badge de modo en la lista, el cupo deshabilitado con estado propio— sigue siendo
**CUPO-09, Phase 16**. Este guard no es la feature: es el mínimo para que nada quede roto en ningún
commit (D-10).

Output: el editor coherente con la base, los fixtures de test legales, y las dos suites que los usan
verdes contra el Postgres local.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/15-modelo-de-cupo-unificado/15-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/15-modelo-de-cupo-unificado/15-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/15-modelo-de-cupo-unificado/15-01-SUMMARY.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<constraints>
- **Ejecución sobre `main`, SIN worktree** (los tests necesitan `node_modules` y los `.env*`).
- **NUNCA `npx tsc`** — usar `./node_modules/.bin/tsc --noEmit`.
- **`npx vitest run` completo NO es un gate útil** en esta máquina. Correr suites puntuales con
  `./node_modules/.bin/vitest run <archivo>` y declarar cuáles.
- La 068 ya está aplicada **en local** (`supabase db reset` del plan 15-01). Si el `db reset` no se
  corrió en esta sesión, correrlo antes de tocar los tests o van a fallar por el motivo equivocado.
- Este plan **no toca el motor**: ni `book_slot_atomic`, ni `lib/booking-core.ts`, ni
  `app/api/booking/availability/route.ts`. Esas son 15-03 y 15-04.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Guard mínimo del editor de servicios (D-10 punto 1, D-06)</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - `app/(dashboard)/settings/settings-client.tsx` líneas 120-240 (`CapacityMode`,
      `normalizeCapacity`, `spacesBlockSimultaneous` y el componente `CapacityModeFields` completo,
      incluidos el párrafo de ayuda y el input de cupo), 515-525 (estado inicial de `newService`),
      600-712 (`addService`, `deleteService` — el molde del mapeo de un rechazo de la base a copy
      propia —, `openEditService` y `saveEditService`).
    - `lib/types.ts` — el campo `capacity_mode` ya con los tres valores (viene de 15-01).
  </read_first>
  <action>
Seis cambios acotados. Ninguno agrega estado nuevo ni componentes nuevos.

1. **Piso de cupo por modo.** Al lado de `normalizeCapacity`, agregar un helper que devuelva el mínimo
   legal de cada modo: 1 para el modo individual, 2 para los otros dos. Extender `normalizeCapacity`
   para aceptar un mínimo opcional (default 1) y usar ese mínimo en el `Math.max`. Comentar que el piso
   **espeja el CHECK `services_capacity_matches_mode_chk` de la migr. 068**: la autoridad es la base y
   esto es su espejo de UX, no un reemplazo.

2. **Tercera opción en el segmented control.** El array `opts` de `CapacityModeFields` pasa a tener las
   tres, con la individual **primera** (es el DEFAULT de la base y el caso real del 100 % de los
   servicios de producción): etiqueta `Individual`, después `Clase grupal`, después
   `Recurso simultáneo`. Los labels siguen siendo FIJOS para todos los rubros (D-10 de la Phase 12):
   NO se rutean por `lib/use-terminology`.

3. **El cambio de modo no puede dejar un cupo ilegal.** En el `onClick` del radio, el patch pasa a
   llevar también el cupo: al modo individual se le manda cupo 1; a los otros dos, el cupo actual
   normalizado con piso 2. Así, pasar de individual a grupal/simultáneo **sube el cupo a 2** y el
   `UPDATE` no rebota (D-06).

4. **El campo del número deja de ser exclusivo del simultáneo.** Hoy solo se muestra cuando el modo es
   `simultaneous_resource`; ahora se muestra siempre que el modo **no** sea el individual, porque desde
   la 068 `services.capacity` es la única fuente del número para los **tres** modos. `min` del input
   pasa a 2 y el `onChange` normaliza con el piso del modo. La etiqueta del campo pasa a una neutra
   ("Cuántos lugares") — el copy que explique cada eje de conteo es Phase 16.
   Además: el párrafo de ayuda de abajo afirma hoy que en la clase grupal el número se pone en la
   grilla de la agenda. Eso deja de ser cierto con la 068 y un texto que miente es peor que ninguno:
   quitar esa afirmación y dejar una línea mínima que nombre los tres modos sin explayarse. La
   redacción completa es CUPO-09.

5. **Defaults en el modo individual — son CUATRO sitios, no tres.** Localizarlos con
   `grep -n "group_class" "app/(dashboard)/settings/settings-client.tsx"`; hoy caen en estas líneas y
   **los cuatro** pasan a inicializar/caer al modo individual con cupo 1:
   - `:523` — `useState` de `newService` (estado inicial del form de alta).
   - `:622` — el reset que corre **después** de `addService`.
   - `:675` — `useState` de `editSvcForm` (estado inicial del form de edición). **Es el que más se
     olvida**, porque su valor lo pisa `openEditService` al abrir el diálogo: igual hay que cambiarlo,
     o el form arranca en un modo que ya no es el default de la base.
   - `:686` — el fallback de `openEditService` para filas viejas en memoria. Tiene la forma
     `s.capacity_mode ?? 'group_class'`, así que **no lo matchea** el grep del literal de asignación:
     lleva su propio criterio de aceptación.
   Además, en `addService` y en `saveEditService` el cálculo del cupo pasa de "simultáneo ? N : 1" a
   "individual ? 1 : normalizado con piso 2".
   Las dos ocurrencias restantes de `group_class` en el archivo (`:122`, un comentario que describe los
   modos, y `:176`, la entrada del array `opts`) **no** son defaults: la de `:176` se conserva —es la
   opción del control— y la de `:122` se reescribe junto con el resto de los comentarios que la 068
   deja mintiendo.

6. **Mapeo del rechazo de CUPO-08 (criterio 3 del ROADMAP).** En `saveEditService`, antes del
   `toast.error` genérico, discriminar el rechazo del gate nuevo con el **molde exacto de
   `deleteService`**: `error.code === 'P0001'` **y** `error.message?.includes(...)` con el código de
   dominio `service_mode_has_future_appointments` — siempre los dos, el code primero. El toast que se
   emite es **copy propia**, en español, accionable, y **nunca** interpola el texto del error ni el
   nombre del servicio: algo del tenor de "No se puede cambiar cómo se ocupa el cupo: el servicio tiene
   turnos futuros reservados. Cancelalos o esperá a que pasen, y volvé a intentar." (lección T-14-14 /
   T-13-09). El `.eq('business_id', business.id)` del `update` ya está y **no se toca**: es defensa en
   profundidad.

No tocar `spacesBlockSimultaneous` ni el aviso amarillo del espacio: la 068 no cambia esa regla.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; ./node_modules/.bin/tsc --noEmit &amp;&amp; ./node_modules/.bin/eslint app/\(dashboard\)/settings/settings-client.tsx &amp;&amp; echo "LINT_OK"</automated>
    <human-check>
      En `/servicios` (dev local, con la 068 ya aplicada por `supabase db reset`):
      1. Crear un servicio nuevo sin tocar el control de cupo → se guarda sin error y queda en
         **Individual**.
      2. Abrirlo, elegir **Clase grupal** → aparece el campo de número con **2** y se guarda sin error.
      3. Subirlo a 10 y guardar → persiste 10.
      4. Volverlo a **Individual** y guardar → persiste con cupo 1.
      Ninguno de los cuatro pasos puede terminar en el toast genérico "Error al guardar".
    </human-check>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit 0.
    - `grep -cF "capacity_mode: 'group_class'" "app/(dashboard)/settings/settings-client.tsx"` devuelve
      **0** (hoy son 3: los `useState` de alta y de edición, y el reset del alta).
    - `grep -cF "?? 'group_class'" "app/(dashboard)/settings/settings-client.tsx"` devuelve **0**
      (hoy es 1: el fallback de `openEditService`. El grep del literal de asignación **no** lo ve, así
      que sin este criterio los dos anteriores se cumplen con el sitio sin tocar).
    - `grep -cF "capacity_mode: 'individual'" "app/(dashboard)/settings/settings-client.tsx"` devuelve
      **&gt;= 3**.
    - `grep -cF "key: 'individual'" "app/(dashboard)/settings/settings-client.tsx"` devuelve **1**.
    - `grep -c "service_mode_has_future_appointments" "app/(dashboard)/settings/settings-client.tsx"`
      devuelve **1**.
    - `grep -cF "error.code === 'P0001'" "app/(dashboard)/settings/settings-client.tsx"` devuelve
      **&gt;= 3** (los dos de `deleteService` más el del cambio de modo).
    - El input de cupo dejó de tener piso 1: `git diff -U0 -- "app/(dashboard)/settings/settings-client.tsx" | grep -cF "+            min={2}"`
      devuelve **1** (el atributo del input dentro de `CapacityModeFields`; ajustar la indentación del
      patrón al diff real si el bloque se reindentó).
  </acceptance_criteria>
  <done>El editor de hoy no puede producir una combinación que la base rechace, y si la base rechaza
  igual (cambio de modo con turnos futuros), el dueño lee una explicación propia y accionable.</done>
</task>

<task type="auto">
  <name>Task 2: Fixtures de test alineados a la fuente única del cupo</name>
  <files>test/helpers/booking-fixtures.ts</files>
  <read_first>
    - `test/helpers/booking-fixtures.ts` líneas 90-175 (`seedTimeBlock`, `seedProfessional`,
      `seedService`, `seedExpiredHold`) y 230-260 (`seedSimultaneousService` y su comentario).
  </read_first>
  <action>
1. **Agregar `seedGroupClassService(seeded, { capacity, serviceId? })`**, molde literal de
   `seedSimultaneousService`: cliente `tenant.admin` (service-role), `UPDATE` con `capacity_mode` en
   clase grupal y el `capacity` recibido, filtrado por `id` **y** `business_id`, sin retorno, `throw`
   con mensaje contextualizado si hay error.
   ⚠ El `serviceId` **opcional** (default `seeded.serviceId`) **no es adorno**: el plan 15-03 necesita
   poner en modo grupal un **segundo** servicio del mismo tenant —el que devuelve `seedService`— para
   el caso que prueba que dos servicios grupales distintos siguen coexistiendo en el mismo slot. Sin
   ese parámetro, ese caso no se puede armar.
   Documentar en su comentario: (a) desde la migr. 068 el cupo del grupal vive en `services.capacity`,
   no en el bloque de agenda, así que este helper es el **único** modo de armar un escenario grupal;
   (b) el CHECK de coherencia exige `capacity >= 2` — llamarlo con 1 rebota con 23514 y eso es correcto;
   (c) ⚠ el tenant y el service se comparten entre los tests del archivo, así que quien lo llame
   **debe** devolver el service al DEFAULT (modo individual, cupo 1) en el `afterEach`.

2. **Reescribir el comentario de `seedSimultaneousService`.** Hoy afirma que el cupo del recurso
   simultáneo no vive en la capacidad del bloque "porque esa es la fuente de la clase grupal". Esa
   segunda mitad muere con la 068: ahora **los tres** modos leen `services.capacity` y lo que distingue
   al simultáneo es el **eje de conteo** (solape de intervalos), no la fuente del número. La forma del
   helper no cambia; el comentario sí.

3. **Reescribir el comentario de `seedTimeBlock`.** El bloque sigue definiendo el **día y la ventana**
   (`day_of_week` 1 = lunes, para alinear con la fecha fija `2031-03-03` de la suite; ventana
   08:00-20:00), pero su `capacity` **deja de decidir el cupo**. Dejar escrito que a partir de la 068
   ese parámetro sirve como **control negativo**: un bloque de cupo 3 con un servicio individual tiene
   que seguir dando cupo 1, y ese es el test que detecta una lectura que quedó leyendo la columna vieja.

4. **No tocar** `seedExpiredHold` (escribe `seat 0` + `is_group false` a mano, que sigue siendo lo que
   el motor escribiría para un turno individual) ni `seedService` (nace con los DEFAULT de la base, que
   desde la 068 son modo individual y cupo 1 — actualizar solo su comentario, que hoy nombra los
   DEFAULT de la 062).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; ./node_modules/.bin/tsc --noEmit &amp;&amp; echo "TSC_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function seedGroupClassService" test/helpers/booking-fixtures.ts` devuelve **1**.
    - `grep -cF "capacity_mode: 'group_class'" test/helpers/booking-fixtures.ts` devuelve **1**
      (únicamente el `update` del helper nuevo).
    - `grep -cF "capacity_mode: 'simultaneous_resource'" test/helpers/booking-fixtures.ts` devuelve **1**
      (sin cambios: el `update` de `seedSimultaneousService`).
    - `./node_modules/.bin/tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>La suite tiene un helper para declarar un escenario grupal por servicio, y ningún comentario de
  los fixtures sigue afirmando que el cupo grupal vive en el bloque de agenda.</done>
</task>

<task type="auto">
  <name>Task 3: Toda escritura de test que la 068 vuelve ilegal (los cuatro afterEach + los tres simultaneos de cupo 1)</name>
  <files>test/concurrency.test.ts, test/booking-cualquiera-public.test.ts</files>
  <read_first>
    - `test/concurrency.test.ts` líneas 40-58 (el `afterEach` que devuelve el service a los defaults), y
      los **tres** casos que siembran un simultáneo de cupo 1: localizarlos con
      `grep -n "seedSimultaneousService(t, { capacity: 1 })" test/concurrency.test.ts` (hoy `:462`,
      `:555` y `:844`) y leer solo esas regiones.
    - `test/booking-cualquiera-public.test.ts` líneas 115-135 (su `afterEach`), y los dos restablecidos
      **dentro** de casos: buscar con `grep -n "capacity_mode: 'group_class'" test/booking-cualquiera-public.test.ts`
      (hoy son 3 en ese archivo y 1 en `concurrency.test.ts`).
    - `test/helpers/booking-fixtures.ts` — `seedSimultaneousService`: **hace `throw` ante error**, así
      que un cupo ilegal no degrada, **rompe el caso entero**.
  </read_first>
  <action>
**El CHECK de coherencia (D-06) vuelve ilegales DOS combinaciones, no una.** El plan cubre las dos acá,
porque las dos dependen del CHECK (que ya está aplicado por 15-01) y **ninguna** depende del motor: son
cambios de test puros y tienen que entrar en el mismo commit que la migración, o la suite queda roja
por construcción (D-10).

**(A) Las cuatro escrituras de clase grupal con cupo 1** (`group_class ⇒ cupo >= 2`) pasan al DEFAULT
nuevo: modo individual con cupo 1.

Actualizar el comentario que acompaña a cada una: ya no se está "volviendo al DEFAULT de la 062" sino
al DEFAULT de la 068, que es el modo individual, y el motivo de fondo sigue siendo el mismo (el tenant
y el service son compartidos por todo el archivo, así que un caso que muta el modo contamina a los
siguientes si no lo restituye).

⚠ En `booking-cualquiera-public.test.ts` hay dos restablecidos **dentro de casos**, no en el
`afterEach`, y uno de ellos sostiene un assert de cero regresión: el mismo POST forjado que se rechaza
con un servicio simultáneo tiene que **entrar** con un servicio que no lo sea, porque lo que gatea es
el **modo**, no el flag. Con el modo individual ese assert se sostiene igual (individual no es
simultáneo) — ajustar el comentario para que hable de "un modo que no sea el simultáneo" en vez de
nombrar la clase grupal, y **no** cambiar ninguna expectativa.

**(B) Los tres casos que siembran un recurso simultáneo de cupo 1**
(`simultaneous_resource ⇒ cupo >= 2`). `seedSimultaneousService` hace `throw` ante error, así que estos
tres **mueren con `23514`** apenas la 068 esté aplicada en local. Ninguno se borra en silencio: cada uno
migra o se convierte en guard, y la resolución se declara en el SUMMARY.

- `simultáneo cupo 1 — la 2ª reserva solapada se rechaza (409) sin sobre-reserva` → el escenario ya no
  existe **por construcción del modelo** (un "recurso simultáneo" de un solo lugar era la misma
  ambigüedad que la fase elimina). **Convertirlo en el guard de que sigue sin existir**: asertar que
  poner el servicio en modo simultáneo con cupo 1 rebota con `23514` (check_violation) y que el
  servicio **queda en su modo anterior** (leer la fila después; no confiar en el error). Como el helper
  tira, el intento va con un `update` directo por `t.admin`, no por `seedSimultaneousService`. El
  comportamiento que el caso probaba —la 2ª reserva solapada de un cupo 1 se rechaza— ya lo cubren
  `CONC-02` y el caso del `23P01`. Renombrar el `it(...)` para que diga lo que ahora prueba.
- `CR-01 — un hold VENCIDO no consume cupo del carril del recurso simultáneo` → subir el escenario a
  cupo **2** y correr los conteos un lugar: con el hold vencido sembrado, las **dos** reservas vivas
  entran (si el hold contara, la 2ª moriría con `slot_full`) y una **tercera** solapada muere con
  `slot_full`. El invariante y el fixture `seedExpiredHold` no cambian.
- `CR-04 (b) — availability simultánea: el espacio tomado por una agenda hermana va a full` → con cupo
  >= 2 obligatorio, **todo** servicio simultáneo sobre una agenda con espacio mapeado es una
  configuración imposible desde la 064 (gap 3, `simultaneous_space_conflict`), así que el sub-caso que
  este test aislaba desaparece **por construcción del modelo, no por regresión**. Reencuadrarlo para
  que asierte lo que sí queda: con cupo 2 y espacio mapeado, el read-path devuelve **todos** los
  start-times en `full`. Si al reencuadrarlo queda redundante con el caso `gap 3`, fusionarlos — pero
  dejando escrito en el comentario del caso superviviente qué escenario se perdió y por qué.
  **Prohibido borrar el caso sin esa nota.**

**Lo que NO entra acá:** los casos grupales de `concurrency.test.ts` que hoy declaran el cupo en el
**bloque de agenda** (`seedTimeBlock` con cupo > 1). Mientras el motor siga leyendo el bloque, esos
casos pasan tal cual; su migración a `seedGroupClassService` es del plan **15-03**, en la misma unidad
que el cambio del RPC. La línea divisoria es exacta: acá va **todo lo que el CHECK vuelve ilegal**
(15-01), allá va **todo lo que el cambio de fuente del cupo vuelve incorrecto** (15-03).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; ./node_modules/.bin/vitest run test/concurrency.test.ts test/booking-cualquiera-public.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "capacity_mode: 'group_class'" test/concurrency.test.ts` devuelve **0**.
    - `grep -cF "capacity_mode: 'group_class'" test/booking-cualquiera-public.test.ts` devuelve **0**.
    - `grep -cF "capacity_mode: 'individual'" test/concurrency.test.ts` devuelve **&gt;= 1** (el
      `afterEach`; el guard del CHECK que reemplaza al caso del simultáneo de cupo 1 suma escrituras).
    - `grep -cF "capacity_mode: 'individual'" test/booking-cualquiera-public.test.ts` devuelve **3**.
    - `grep -cE "seedSimultaneousService\(t, \{ capacity: 1 \}\)" test/concurrency.test.ts` devuelve
      **0** (hoy son 3, y el helper hace `throw`: con la 068 aplicada esos tres casos mueren con
      `23514` antes de llegar a su primera aserción).
    - `grep -cF "'23514'" test/concurrency.test.ts` devuelve **&gt;= 1** (el guard que reemplaza al caso
      del simultáneo de cupo 1 asierta el `check_violation`).
    - `./node_modules/.bin/vitest run test/concurrency.test.ts test/booking-cualquiera-public.test.ts --no-file-parallelism`
      termina con **0 fallos** y sin ningún caso caído por `23514` **no esperado** (el único lugar donde
      ese código puede aparecer es como aserción del guard). Declarar en el SUMMARY el conteo exacto de
      casos pasados por archivo, y que **no bajó** respecto de la corrida previa a la 068.
  </acceptance_criteria>
  <done>Las dos suites que escriben `capacity_mode` corren enteras contra la base con la 068 aplicada:
  ninguna escritura viola el CHECK de coherencia, en ninguno de sus dos sentidos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador del dueño (anon + RLS) → `services` | el panel escribe `capacity_mode`/`capacity` por PostgREST; la UI no es un control de seguridad |
| error de Postgres → pantalla del dueño | el `message` del `RAISE` y el de un `CHECK` llegan al cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | escritura de `capacity_mode`/`capacity` desde el panel | mitigate | El piso por modo es espejo de UX; la **autoridad es el CHECK de la base** (migr. 068), inalcanzable desde el cliente. Se conserva el `.eq('business_id', business.id)` en el `update` como defensa en profundidad |
| T-15-11 | Information Disclosure | toast del rechazo de CUPO-08 | mitigate | El toast es copy propia fija; **no** interpola `error.message` ni el nombre del servicio. Discriminación por `error.code === 'P0001'` + `message.includes(<código de dominio>)`, molde `deleteService` (T-14-14 / T-13-09) |
| T-15-12 | Elevation of Privilege | cliente Supabase del panel | mitigate | Sin cambios: `settings-client.tsx` sigue usando el browser client (anon + RLS), nunca service-role. Este plan no agrega ninguna llamada nueva a la base |
| T-15-13 | Tampering | fixtures de test con service-role | accept | `test/helpers/booking-fixtures.ts` usa service-role a propósito y solo en `test/`; es el criterio ya declarado del archivo y no viaja a producción |
| T-15-SC | Tampering | supply chain | mitigate | Cero dependencias npm nuevas: `package.json` no está en `files_modified` |
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` exit 0.
2. `./node_modules/.bin/vitest run test/concurrency.test.ts test/booking-cualquiera-public.test.ts --no-file-parallelism`
   con 0 fallos, contra el Postgres local con la 068 aplicada.
3. `git diff --stat` toca exactamente cuatro archivos y **ninguno** de `supabase/`, `lib/booking-core.ts`
   ni `app/api/`.
4. La verificación humana del Task 1 queda registrada en el SUMMARY (modo `end-of-phase`: se ejecuta al
   cierre de la fase, no bloquea este plan).
</verification>

<success_criteria>
- El editor ofrece el modo individual, arranca en él y sube el cupo a 2 al pasar a grupal o simultáneo
  (D-10 punto 1).
- El rechazo de CUPO-08 llega al dueño como copy propia y accionable, sin el texto crudo del error.
- **Ninguna** escritura de test viola el CHECK de coherencia: ni las cuatro de clase grupal con cupo 1,
  ni las tres de recurso simultáneo con cupo 1. Las dos suites quedan verdes (D-10 punto 2).
- Ningún caso se borró sin reemplazo: los escenarios que el modelo vuelve imposibles quedan como guards
  de que siguen siéndolo, con su nota de qué se perdió y por qué.
- Nada de la UX completa del editor (los tres modos explicados, el badge de la lista) se adelantó: eso
  sigue siendo CUPO-09 / Phase 16.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/15-modelo-de-cupo-unificado/15-02-SUMMARY.md` when done
</output>
