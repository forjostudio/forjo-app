---
phase: 19-el-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/time-block-services.ts
  - test/time-block-services.test.ts
  - lib/agenda-hours-payload.ts
  - test/agenda-hours-payload.test.ts
autonomous: true
requirements: [AGENDA-05, AGENDA-06]

must_haves:
  truths:
    - "El panel decide 'esta franja es comodin' con la MISMA funcion que el motor, nunca con un filtro inline en el JSX (AGENDA-02, AGENDA-06, D-16)"
    - "Una franja cuyo unico mapeo es a un servicio INACTIVO NO es comodin: el motor tampoco la trata como comodin (D-11)"
    - "El payload del guardado se arma con el set COMPLETO de bloques (7 dias x todos los consultorios): guardar desde una sede no puede borrar los bloques de la otra (P-03)"
    - "El cupo del bloque no viaja en el payload del guardado (D-12)"
    - "Guardar dos veces seguidas sin recargar no duplica ningun bloque: el estado local se re-deriva de las filas que devuelve la base (P-01)"
    - "Los dos modulos son puros: sin React, sin Supabase, sin next/ — testeables sin base de datos"
  artifacts:
    - path: "lib/time-block-services.ts"
      provides: "las 2 funciones nuevas de la pregunta inversa: que declara la franja y si es comodin"
      exports: ["servicesOfBlock", "isBlockWildcard"]
      contains: "isBlockWildcard"
    - path: "lib/agenda-hours-payload.ts"
      provides: "el contrato del guardado por diff: como se arma el payload y como se re-deriva el estado local desde las filas persistidas"
      exports: ["buildSaveHoursPayload", "buildDayStatesFromRows"]
      min_lines: 80
    - path: "test/agenda-hours-payload.test.ts"
      provides: "los controles negativos de P-01 (doble guardado no duplica) y P-03 (el set es completo)"
      contains: "buildSaveHoursPayload"
    - path: "test/time-block-services.test.ts"
      provides: "los casos de las 2 funciones nuevas con control negativo, incluido el servicio inactivo mapeado"
      contains: "isBlockWildcard"
  key_links:
    - from: "lib/agenda-hours-payload.ts"
      to: "lib/time-block-services.ts"
      via: "NINGUNO — los dos modulos son independientes; el cableado lo hace agenda-client (Plan 19-04)"
      pattern: "^import type"
    - from: "test/agenda-hours-payload.test.ts"
      to: "lib/agenda-hours-payload.ts"
      via: "import desde el alias @/lib, sin Supabase ni credenciales"
      pattern: "@/lib/agenda-hours-payload"
---

<objective>
Dejar **fuera del componente** las dos decisiones que la fase no puede permitirse tomar en el JSX:
(a) *"¿que servicios declara esta franja y es comodin?"* (AGENDA-02 / AGENDA-06) y (b) *"¿que se le
manda a la base al guardar y como queda el estado local despues?"* (D-01 / P-01 / P-03 / D-12).

Purpose: la regla del comodin ya vive en `lib/time-block-services.ts` desde la Phase 18 y su cabecera
dice por que: tres capas que la interpretan distinto derivan. El panel es la tercera capa. Y el
guardado por diff introduce dos modos de falla nuevos que se cierran **con tests** en vez de **con
cuidado**: el segundo guardado que duplica (P-01) y el payload por consultorio que borra las otras
sedes (P-03). Los dos son medibles sobre objetos planos; no hace falta base ni navegador.

Output: 2 funciones nuevas en `lib/time-block-services.ts`, el modulo nuevo
`lib/agenda-hours-payload.ts` con 2 funciones + 3 tipos, y sus suites puras.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/19-el-panel/19-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/19-el-panel/19-PATTERNS.md
@lib/time-block-services.ts
@test/time-block-services.test.ts
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<constraints>
- **Pureza no negociable.** Los dos modulos no importan Supabase, ni React, ni nada de `next/`. Sus
  entradas son objetos planos. Si te encontras importando `@/lib/supabase/...`, te fuiste de alcance.
- **Contrato D-16 (el caller filtra ANTES).** Ninguna funcion de `lib/time-block-services.ts` filtra
  por negocio ni por vigencia, y las nuevas tampoco. El aislamiento vive en la RLS (migr. 071/073) y
  en las queries de los consumidores.
- **No tocar las 4 funciones existentes** de `lib/time-block-services.ts` ni sus tests: la
  disponibilidad y el backstop de la Phase 18 estan asegurados con `threats_open: 0`.
- **NUNCA `npx tsc`** (baja un paquete que no es el compilador y siempre sale 0) →
  `./node_modules/.bin/tsc --noEmit`.
- **`npx vitest run` completo NO es un gate util** en esta maquina (las suites de abono son flaky en
  paralelo). Correr **suites nombradas**.
- Comentarios densos **en espanol**, explicando el *por que*, no el *que*.
- Cero dependencias nuevas: `git diff -- package.json package-lock.json` tiene que quedar vacio.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: La pregunta inversa — que declara la franja y si es comodin</name>
  <files>lib/time-block-services.ts, test/time-block-services.test.ts</files>
  <read_first>
    - `lib/time-block-services.ts` **integro (193 lineas)**. La cabecera `:1-28` declara el contrato
      del modulo (pureza + D-16 + la regla del comodin) y `blocksForService` `:87-97` es el molde
      exacto de forma: JSDoc que explica el POR QUE y el caso trampa, generica sobre el shape de la
      fila, un solo `import type`.
    - `test/time-block-services.test.ts` **:1-30** — las factories `block()` y `map()` que ya existen
      (NO crear otras) y el parrafo del CONTROL NEGATIVO, que es el estandar del workstream.
    - `19-RESEARCH.md` §"Pattern 3" y §"El default cualquiera en la base" — por que el comodin es
      AUSENCIA de filas (3 columnas, todas NOT NULL, sin sentinel) y por que un mapeo a un servicio
      desactivado sigue restringiendo la franja.
    - `19-UI-SPEC.md` §"El chip Cualquier servicio" — la frase que fija el matiz: si queda un
      inactivo mapeado, la franja no es comodin y el chip no se muestra.
  </read_first>
  <behavior>
    - `servicesOfBlock(blockId, bridge)` → los `service_id` que declara esa franja.
      · franja sin filas ⇒ `[]` (y `[]` significa comodin, no "sin servicios")
      · las filas de OTRA franja no entran
      · las filas de otro negocio se interpretan igual: el modulo no filtra por tenant (D-16)
      · el orden de salida es el orden de `bridge` (estable, sin ordenar por nada mas)
    - `isBlockWildcard(blockId, bridge)` → `true` si y solo si esa franja no tiene NINGUNA fila.
      · franja con 1 fila ⇒ `false`, **aunque ese servicio este desactivado**: el motor tampoco
        distingue (la columna `active` es de `services`, no de la puente)
      · franja sin filas, con otras franjas mapeadas alrededor ⇒ sigue `true`
      · por construccion equivale a `servicesOfBlock(...).length === 0`: una sola fuente
  </behavior>
  <action>
    En `lib/time-block-services.ts`, **al final del archivo** (sin tocar las 4 funciones existentes),
    agregar las dos funciones descritas en `<behavior>`, con el mismo idioma que el resto del modulo:
    JSDoc en espanol que dice **por que** existen y cual es el caso que muerde, firma que recibe
    `blockId: string` y `bridge: TimeBlockService[]`, cero estado, cero efectos.

    Puntos que el JSDoc tiene que dejar escritos, porque son los que se pierden al leer el codigo:

    1. **Son la pregunta INVERSA de `blocksForService`.** El helper de la Phase 18 responde "en que
       franjas se da el servicio X"; el panel necesita "que declara la franja Y". Citar que el
       consumidor es el panel de la Phase 19 (la cabecera del modulo `:9-10` ya lo anticipa) y que
       AGENDA-02 prohibe reimplementarlo en el componente.
    2. **`[]` es un estado DECLARADO, no un hueco.** La ausencia de filas ES la regla del comodin
       (D-01): no hay sentinel ni columna nullable donde leerla, se computa.
    3. **El matiz del servicio desactivado** (D-11): `isBlockWildcard` mira **todas** las filas,
       incluidas las de servicios que hoy nadie puede reservar. Si se filtrara por vigencia, el panel
       pintaria "Cualquier servicio" sobre una franja que el motor sigue restringiendo — o sea, el
       panel mentiria sobre lo que ve el publico. Ese es exactamente el modo de falla que este modulo
       existe para prevenir.
    4. `isBlockWildcard` se implementa sobre la misma travesia que `servicesOfBlock` (o delegando en
       ella) para que no puedan divergir, igual que `isServiceScheduled` se apoya en
       `blocksForService` (`:116`).

    Despues, en `test/time-block-services.test.ts`, agregar **dos `describe` nuevos** al final,
    titulados con el formato del archivo (`'nombreFn — <que congela> (D-xx)'`), reusando las
    factories `block()` y `map()` que ya estan al tope. Casos obligatorios:

    `servicesOfBlock`
    1. franja sin filas ⇒ arreglo vacio.
    2. franja con dos filas ⇒ exactamente esos dos ids. — *control negativo de 1*
    3. las filas de otra franja no contaminan el resultado. — *detecta olvidarse de filtrar por
       franja: pasa el caso 2 y falla este*
    4. filas con otro `business_id` se interpretan igual (D-16 congelado).

    `isBlockWildcard`
    5. franja sin filas ⇒ `true`.
    6. franja con una fila ⇒ `false`. — *control negativo de 5*
    7. **el caso mordedor de la fase**: franja cuyo unico mapeo es a un servicio que el negocio tiene
       desactivado ⇒ `false`. El `it(...)` tiene que decir explicitamente por que (el motor tampoco
       la trata como comodin). Como el modulo no conoce `active`, el caso se arma pasando el id de un
       servicio que el test declara inactivo en un comentario: lo que se congela es que la funcion
       **no acepta** ninguna nocion de vigencia.
    8. una franja comodin rodeada de franjas mapeadas sigue siendo comodin.

    Correr solo esta suite, no la bateria completa.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && npx vitest run test/time-block-services.test.ts && grep -cE "^export function " lib/time-block-services.ts && grep -cE "^import " lib/time-block-services.ts && grep -cE "^describe\(" test/time-block-services.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `npx vitest run test/time-block-services.test.ts` exit **0**, 0 failed, 0 skipped.
    - `grep -cE "^export function " lib/time-block-services.ts` = **6** (las 4 de la Phase 18 + las 2
      nuevas).
    - `grep -cE "^import " lib/time-block-services.ts` = **1**, y sigue siendo `import type`
      (`grep -cE "^import type " lib/time-block-services.ts` = **1**): pureza intacta.
    - `grep -cE "^describe\(" test/time-block-services.test.ts` = **6**.
    - `grep -cF "isBlockWildcard" lib/time-block-services.ts` >= 1 y
      `grep -cF "servicesOfBlock" lib/time-block-services.ts` >= 1.
    - `git diff -- lib/time-block-services.ts | grep -c "^-[^-]"` = **0**: sobre el modulo solo se
      agregan lineas, no se toca ni una de las 4 funciones existentes.
    - Los casos 3 y 7 existen y su texto declara explicitamente que son controles negativos.
    - **Prueba de mordida** (a mano, se reporta en el SUMMARY y se restaura): hacer que
      `isBlockWildcard` filtre las filas por un campo inventado de vigencia y confirmar que el caso 7
      se pone **rojo**.
  </acceptance_criteria>
  <done>
    Las dos funciones existen, son puras, estan documentadas con el POR QUE, y la suite las congela
    con control negativo — incluido el caso del servicio desactivado que sigue mapeado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: El contrato del guardado por diff — payload completo y re-derivacion del estado</name>
  <files>lib/agenda-hours-payload.ts, test/agenda-hours-payload.test.ts</files>
  <read_first>
    - `app/(dashboard)/agenda/agenda-client.tsx` **:170-180** (`LocalBlock` / `defaultBlock`),
      **:254-262** (el `useState(initializer)` que construye `dayStates` desde `initialTimeBlocks`) y
      **:366-390** (`saveHours` de hoy, con el `.delete()` por negocio, el drop silencioso de los
      bloques sin consultorio en `:377` y el `capacity` en `:379`). Las tres reglas que este modulo
      encierra estan HOY enterradas ahi.
    - `lib/agenda-occupancy.ts` — el molde de forma de "modulo puro que decide, UI que solo pinta":
      cabecera con el por que, tipos propios, cero dependencias.
    - `19-RESEARCH.md` §"El write path HOY" (los 4 hechos verificados) y Pitfalls **P-01** y **P-03**.
    - `19-RESEARCH.md` §Testing — la tabla que nombra estas dos funciones y su caso mordedor.
  </read_first>
  <behavior>
    - `buildSaveHoursPayload(days, { hasLocations })` → el arreglo que viaja como `p_blocks`.
      · recorre los **7** dias y **todos** los bloques de cada dia, sin filtrar por consultorio
      · saltea los dias con `enabled: false`
      · con `hasLocations: true` descarta los bloques sin consultorio (es la regla de hoy, `:377`)
      · con `hasLocations: false` los conserva y su consultorio viaja nulo
      · `id` ausente ⇒ viaja `null` (bloque nuevo); `id` presente ⇒ viaja tal cual
      · `label` vacio o solo espacios ⇒ `null`; consultorio vacio ⇒ `null`
      · `service_ids` se deduplica y se descartan los vacios, conservando el orden de entrada
      · **ningun elemento del payload lleva el cupo del bloque** (D-12)
    - `buildDayStatesFromRows(rows)` → los 7 dias del editor, re-derivados de las filas persistidas.
      · siempre devuelve 7 entradas, en indice 0..6
      · `enabled` = ese dia tiene al menos un bloque
      · el horario se normaliza a `'HH:MM'` (Postgres devuelve `time` con segundos)
      · `label`/`location_id` nulos se normalizan a cadena vacia (lo que esperan los inputs)
      · `service_ids` se **clona** por bloque: dos bloques nunca comparten el mismo arreglo (P-04)
      · una fila con dia fuera de 0..6 se descarta en vez de romper
    - **La garantia que cierra P-01:** estado → payload → (la base asigna ids) → re-derivacion →
      payload otra vez ⇒ el segundo payload tiene la misma cantidad de elementos que el primero y
      **ninguno** con `id` nulo.
  </behavior>
  <action>
    Crear `lib/agenda-hours-payload.ts` con una cabecera al estilo de `lib/agenda-occupancy.ts` que
    explique las **tres** cosas que este modulo existe para que no se puedan olvidar:

    1. **El set es COMPLETO** (P-03). El editor opera por consultorio activo, pero el guardado es por
       negocio: el RPC borra los bloques del negocio que no vengan en el payload. Armar el payload
       con la lista ya filtrada por consultorio borraria las otras sedes — una regresion que solo
       aparece en un negocio multi-sede y que nadie ve en local. Por eso la funcion recibe los 7 dias
       enteros y **no acepta** ningun parametro de consultorio.
    2. **El cupo del bloque no viaja** (D-12). La columna dejo de decidir en la migr. 068 y
       arrastrarla al codigo nuevo la volveria a legitimar. Omitirla del payload es seguro: la
       columna es NOT NULL con default y el UPDATE que no la menciona conserva su valor.
    3. **El retorno de la base ES el estado nuevo** (P-01). El editor inicializa `dayStates` con
       `useState(initializer)` y **nunca** vuelve a derivarlo de las props: no hay ningun `useEffect`
       que lo re-sincronice, y `router.refresh()` no sirve porque el inicializador ya corrio. Con el
       guardado por diff eso duplicaria cada bloque nuevo en el segundo guardado. La salida elegida
       es la mas barata de sostener: el RPC devuelve el set resultante y el cliente **re-deriva**
       `dayStates` de esas filas con esta misma funcion, la misma que usa el inicializador. Al no
       existir correlacion entre el payload y el retorno, tampoco existe la clase de bug de
       correlacion. (`19-RESEARCH.md` Open Question 2 recomendaba una clave `tmp_key`; se descarta
       por innecesaria — la re-derivacion completa es estrictamente mas simple y elimina el modo de
       falla en vez de administrarlo. Anotarlo en el SUMMARY.)

    Exportar los tipos: el borrador de bloque del editor (id opcional, horas, etiqueta, consultorio y
    los ids de servicio), el dia del editor (bandera de abierto + sus bloques), la fila persistida que
    devuelve el RPC (id, dia, horas, etiqueta, consultorio y los ids de servicio) y el elemento del
    payload. El borrador va **generico** en `buildSaveHoursPayload` (cualquier objeto que lo extienda)
    para que `agenda-client` pueda sumarle su campo de error sin castear — mismo criterio por el que
    `blocksForService` es generica sobre el shape de la franja.

    Despues crear `test/agenda-hours-payload.test.ts` espejando `test/time-block-services.test.ts`:
    `describe/it/expect` de Vitest, import por el alias `@/lib/...`, factories minimas al tope, cero
    Supabase. Casos obligatorios:

    `buildSaveHoursPayload`
    1. dos consultorios con bloques en el mismo dia ⇒ el payload trae **los dos**. — *el control de
       P-03: una implementacion que filtre por consultorio pasa cualquier otro caso y falla este*
    2. un dia con `enabled: false` no aporta bloques.
    3. con consultorios cargados, un bloque sin consultorio se descarta; sin consultorios cargados,
       el mismo bloque viaja con consultorio nulo. — *congela la regla de hoy*
    4. bloque sin id ⇒ el elemento lleva id nulo; bloque con id ⇒ lo conserva.
    5. etiqueta con solo espacios ⇒ viaja nula.
    6. ids de servicio repetidos ⇒ se deduplican conservando el orden.
    7. **ningun elemento del payload tiene una clave de cupo** (assert sobre las claves del objeto,
       no sobre el valor). — *el control de D-12*

    `buildDayStatesFromRows`
    8. siempre 7 dias; los dias sin filas quedan cerrados.
    9. `'09:00:00'` se normaliza a `'09:00'`.
    10. etiqueta y consultorio nulos ⇒ cadena vacia.
    11. dos bloques distintos no comparten el arreglo de servicios: mutar el de uno no toca el del
        otro. — *el control de P-04*

    Ida y vuelta (el caso que cierra P-01)
    12. armar un estado con un bloque **sin id**, generar el payload, simular la base con un ayudante
        local del test que le asigne un id a los elementos sin id y devuelva las filas resultantes,
        re-derivar el estado y **volver a generar el payload**: la segunda vez tiene la misma
        cantidad de elementos y ninguno con id nulo. El texto del `it(...)` tiene que decir que este
        caso es el que evita que el segundo "Guardar horarios" duplique todo.

    Correr solo esta suite.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && npx vitest run test/agenda-hours-payload.test.ts && grep -cE "^export function " lib/agenda-hours-payload.ts && grep -cE "^import .*(supabase|react|next/)" lib/agenda-hours-payload.ts && grep -cE "^  it\(" test/agenda-hours-payload.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `npx vitest run test/agenda-hours-payload.test.ts` exit **0**, 0 failed, 0 skipped.
    - `grep -cE "^export function " lib/agenda-hours-payload.ts` = **2**.
    - `grep -cE "^import .*(supabase|react|next/)" lib/agenda-hours-payload.ts` = **0** y
      `grep -cE "^import .*(supabase|@supabase)" test/agenda-hours-payload.test.ts` = **0**: los dos
      archivos son puros.
    - `grep -cE "^  it\(" test/agenda-hours-payload.test.ts` >= **12**.
    - `grep -ciE "\bcapacity\b" lib/agenda-hours-payload.ts` = **0**: el modulo no conoce el cupo del
      bloque ni para excluirlo.
    - El caso 12 existe y su `it(...)` nombra el doble guardado.
    - **Prueba de mordida** (a mano, se reporta en el SUMMARY y se restaura): hacer que
      `buildSaveHoursPayload` reciba y aplique un filtro por consultorio, y confirmar que el caso 1
      se pone **rojo**.
    - `git diff -- package.json package-lock.json` vacio.
  </acceptance_criteria>
  <done>
    Las tres reglas del guardado (set completo, sin cupo, re-derivacion desde la base) viven en un
    modulo puro con suite propia, y las dos trampas caras de la fase (P-01, P-03) tienen un test que
    las muerde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filas leidas de la base → los modulos puros | reciben filas ya acotadas por `business_id`; no pueden verificar esa acotacion por si mismos (contrato D-16) |
| estado del editor → payload que cruza a la base | el payload se arma en el browser: todo lo que sale de aca es entrada NO confiable para el RPC del Plan 19-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | `isBlockWildcard` filtrando por vigencia haria que el panel pinte "comodin" sobre una franja que el motor sigue restringiendo (panel y publico divergen) | mitigate | La funcion no acepta ninguna nocion de `active`; caso de test 7 (franja con unico mapeo a servicio desactivado ⇒ `false`) + prueba de mordida |
| T-19-02 | Information Disclosure | filtrar por `business_id` adentro de los modulos puros daria una falsa sensacion de aislamiento en codigo que recibe filas de un caller y no puede validar su origen | accept | Contrato D-16 explicito en el JSDoc + caso de test 4 (filas de otro negocio se interpretan igual). El aislamiento real vive en la RLS (071/073) y en las queries de los Planes 19-03/19-04 |
| T-19-03 | Tampering (auto-infligido) | `buildSaveHoursPayload` armado desde la lista ya filtrada por consultorio ⇒ el RPC borra los bloques de las otras sedes | mitigate | La firma **no acepta** consultorio; caso de test 1 con dos sedes + prueba de mordida (agregar el filtro pone el caso rojo) |
| T-19-04 | Repudiation | el segundo "Guardar horarios" duplica los bloques y el dueno no tiene forma de saber que paso (P-01) | mitigate | `buildDayStatesFromRows` es la unica derivacion del estado, compartida por el inicializador y por el post-guardado; caso de test 12 (ida y vuelta) |
| T-19-SC | Tampering (supply chain) | dependencias | accept | **Cero** paquetes nuevos: `git diff -- package.json package-lock.json` vacio |
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` exit **0**.
2. `npx vitest run test/time-block-services.test.ts test/agenda-hours-payload.test.ts` exit **0**,
   0 failed, 0 skipped.
3. `grep -cE "^export function " lib/time-block-services.ts` = **6**;
   `grep -cE "^export function " lib/agenda-hours-payload.ts` = **2**.
4. `grep -cE "^import " lib/time-block-services.ts` = **1** (sigue siendo `import type`);
   `grep -cE "^import .*(supabase|react|next/)" lib/agenda-hours-payload.ts` = **0**.
5. `grep -ciE "\bcapacity\b" lib/agenda-hours-payload.ts` = **0**.
6. `git diff -- lib/time-block-services.ts | grep -c "^-[^-]"` = **0** (solo lineas agregadas).
7. Las dos pruebas de mordida (vigencia en `isBlockWildcard`, filtro por consultorio en
   `buildSaveHoursPayload`) estan documentadas en el SUMMARY con su resultado rojo.
8. `git diff --name-only` toca **exactamente** los 4 archivos del frontmatter.
9. `git diff -- package.json package-lock.json` vacio.
</verification>

<success_criteria>
- El panel va a poder responder "esta franja es comodin" con la misma funcion que el motor, no con un
  filtro inline (AGENDA-02 / AGENDA-06).
- Las dos trampas caras del guardado por diff (duplicar al segundo guardado, borrar las otras sedes)
  fallan en rojo en la suite si alguien las reintroduce.
</success_criteria>

## Artifacts this phase produces

| Artefacto | Plan | Estado al cerrar este plan |
|---|---|---|
| `lib/time-block-services.ts` → `servicesOfBlock`, `isBlockWildcard` | **19-01** | creado |
| `lib/agenda-hours-payload.ts` → `buildSaveHoursPayload`, `buildDayStatesFromRows`, tipos `AgendaBlockDraft` / `AgendaDayDraft` / `SavedAgendaBlock` / `AgendaBlockPayload` | **19-01** | creado |
| `test/agenda-hours-payload.test.ts` | **19-01** | creado, verde |
| `supabase/migrations/074_save_agenda_blocks.sql` → funcion `public.save_agenda_blocks(uuid, jsonb)` | 19-02 | — |
| `app/(dashboard)/settings/settings-client.tsx` → campo `blocks` en el pre-check de borrado | 19-03 | — |
| `app/[slug]/booking-client.tsx` → rama `service_not_scheduled` | 19-03 | — |
| `app/(dashboard)/agenda/page.tsx` → props `initialTimeBlockServices` y `serviceCatalog` | 19-04 | — |
| `app/(dashboard)/agenda/agenda-client.tsx` → tipo `ServiceCatalogItem`, `service_ids` en `LocalBlock`, `hoursDirty` | 19-04 | — |
| `app/(dashboard)/agenda/agenda-client.tsx` → `ServiceChip`, `BlockServicesLine`, `saveHours` sobre el RPC | 19-05 | — |
| `supabase/schema.sql` reflejando la 074 | 19-06 | — |

<output>
Create `.planning/workstreams/motor-reservas/phases/19-el-panel/19-01-SUMMARY.md` when done
</output>
