---
phase: 19-el-panel
plan: 06
type: execute
wave: 4
depends_on: ["19-02", "19-05"]
files_modified:
  - supabase/schema.sql
autonomous: false
requirements: [AGENDA-05]

must_haves:
  truths:
    - "El estado real de las migraciones 071/072/073 en PRODUCCION esta MEDIDO, no supuesto: los artefactos de la Phase 18 se contradicen entre si"
    - "La 074 esta aplicada en produccion ANTES de que se deployee el codigo que la invoca: al reves, cada guardado de horarios falla"
    - "Despues de aplicar se recarga el cache de esquema de PostgREST: sin eso la funcion existe pero no se expone (P-05)"
    - "La lista de privilegios de la funcion en PRODUCCION se verifico: el rol anonimo no puede ejecutarla (P-02)"
    - "supabase/schema.sql refleja la 074 y se edito quirurgicamente: solo lineas agregadas, nunca por dump"
  artifacts:
    - path: "supabase/schema.sql"
      provides: "el estado base del schema reflejando la funcion de la 074 y sus privilegios"
      contains: "save_agenda_blocks"
  key_links:
    - from: "supabase/schema.sql"
      to: "supabase/migrations/074_save_agenda_blocks.sql"
      via: "reflejo quirurgico de la funcion y de sus tres sentencias de privilegios, sin reordenar el archivo"
      pattern: "save_agenda_blocks"
---

<objective>
Cerrar el **pendiente operativo** de la fase: aplicar la migracion **074** a **produccion**, en el
orden correcto, con la verificacion previa del estado real de las migraciones de la Phase 18, la
recarga del cache de PostgREST y la verificacion posterior de privilegios — y reflejar la funcion en
`supabase/schema.sql`.

Purpose: `tsc`, `lint` y la suite pasan **igual** sin la migracion aplicada. Es un falso verde: el
panel deployado contra una base sin la 074 devuelve error en **cada** guardado de horarios, y el
sintoma (una copy generica) es indistinguible de un problema de red. Ademas hay **evidencia
contradictoria** sobre el estado de prod que **no se puede resolver leyendo artefactos**:
`18-VERIFICATION.md:152` dice que las 071 y 072 **no** estan aplicadas (prod en la 070);
`18-SECURITY.md` §9, posterior, dice que **si** lo estan y que faltaba la 073; y la memoria del
proyecto dice que las tres estan. **Sin la 071 y la 073 la 074 no puede funcionar** — no hay tabla que
escribir ni FK que valide la pertenencia al tenant.

Y hay una leccion propia del workstream que aplica literal: la **068 se aplico fuera de orden** y
dejo roto el alta de servicios mientras corrio el codigo viejo. El orden acá es: **migracion primero,
deploy despues.**

Output: la 074 aplicada y verificada en produccion, y `supabase/schema.sql` actualizado.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/19-el-panel/19-02-SUMMARY.md
@supabase/migrations/README.md
@.planning/workstreams/motor-reservas/phases/18-el-modelo-y-la-disponibilidad/18-01-SUMMARY.md
</context>

<constraints>
- **La aplicacion a produccion la hace UNA PERSONA, a mano, en el editor SQL de Supabase.** Nada de
  `supabase db push`, `supabase link` ni `supabase migration up --linked`: es la regla del proyecto
  (`.claude/CLAUDE.md`) y esta fase no la cambia.
- **Orden duro: migracion ANTES del deploy.** La 074 es aditiva (crea una funcion nueva que nadie
  llama todavia), asi que aplicarla antes es inofensivo; aplicarla despues deja el panel roto para
  todos los negocios mientras tanto.
- **`supabase/schema.sql` se edita QUIRURGICAMENTE, nunca por `supabase db dump`** (que reordena el
  archivo entero y vuelve el diff ilegible). Es el criterio que ya uso el Plan 18-01.
- **No se modifica la 074.** Si la verificacion revela que algo falta, se corrige con una migracion
  **nueva** (la 075), no editando un archivo que ya corrio en produccion.
- **Nada de este plan toca codigo de la app.**
</constraints>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Medir el estado real de prod, aplicar lo que falte en orden, aplicar la 074 y verificar</name>
  <what-built>
    El Plan 19-02 dejo `supabase/migrations/074_save_agenda_blocks.sql` escrita, validada con
    `supabase db reset` en el Postgres local PG17 y cubierta por `test/agenda-save-blocks-rpc.test.ts`
    (aislamiento cross-tenant + atomicidad + el rol anonimo sin ejecucion). El Plan 19-05 dejo el
    panel invocandola. Lo unico que falta es que exista en **produccion**.

    Esto no lo puede hacer Claude: por convencion del proyecto las migraciones se aplican a mano, en
    orden, desde el editor SQL de Supabase, y ademas hay que **medir** antes cual es el estado real —
    los artefactos de la Phase 18 se contradicen sobre si la 071/072/073 estan aplicadas.
  </what-built>
  <how-to-verify>
    **Paso 1 — Medir el estado real (no asumir).** En el editor SQL de **produccion**, correr:

    ```sql
    select to_regclass('public.time_block_services') is not null                                     as tabla_071,
           (select count(*) from pg_views
             where schemaname = 'public' and viewname = 'public_time_block_services') = 1            as vista_071,
           exists (select 1 from pg_constraint where conname = 'tbs_block_same_tenant')              as fk_073_bloque,
           exists (select 1 from pg_constraint where conname = 'tbs_service_same_tenant')            as fk_073_servicio,
           (select count(*) from information_schema.role_table_grants
             where table_schema = 'public' and table_name like 'public\_%'
               and grantee in ('anon','authenticated') and privilege_type <> 'SELECT')               as grants_de_mas_072;
    ```

    Lectura esperada si las tres estan aplicadas: las cuatro primeras en `true` y la ultima en `0`.

    **Paso 2 — Aplicar lo que falte, EN ORDEN y sin saltear numeros.** Si alguna da `false` (o la
    ultima da distinto de 0), pegar y correr los archivos faltantes **en este orden**:
    `071_time_block_services.sql` → `072_public_views_read_only.sql` →
    `073_tenant_integrity_and_default_privs.sql`. Son idempotentes: correr una que ya estaba aplicada
    no rompe nada.

    **Paso 3 — Aplicar la 074.** Pegar y correr `supabase/migrations/074_save_agenda_blocks.sql`
    entera (funcion + los tres statements de privilegios + el bloque de default privileges).

    **Paso 4 — Recargar el cache de esquema de PostgREST.** En la misma sesion:

    ```sql
    NOTIFY pgrst, 'reload schema';
    ```

    Sin esto la funcion existe en la base pero PostgREST no la expone, y **cada** guardado de horarios
    devuelve el error de funcion inexistente (P-05).

    **Paso 5 — Verificar privilegios (es la evidencia de P-02).** Correr:

    ```sql
    select p.proname, p.prosecdef, coalesce(array_to_string(p.proacl, ','), '') as acl
      from pg_proc p join pg_namespace n on n.oid = p.pronamespace
     where n.nspname = 'public' and p.proname = 'save_agenda_blocks';
    ```

    Tiene que devolver **una** fila, con `prosecdef = false` (corre con los privilegios de quien la
    invoca) y un `acl` que **no** contenga una entrada para el rol anonimo y **si** contenga la
    ejecucion para `authenticated`. Si el rol anonimo aparece, **parar**: es RA-05 otra vez, ahora
    sobre la configuracion del negocio.

    **Paso 6 — Verificar el default de funciones futuras.** Correr:

    ```sql
    select pg_get_userbyid(defaclrole) as creador, defaclacl::text as acl
      from pg_default_acl d join pg_namespace n on n.oid = d.defaclnamespace
     where n.nspname = 'public' and d.defaclobjtype = 'f';
    ```

    El rol anonimo ya no tiene que figurar con ejecucion. Si el bloque de `supabase_admin` emitio un
    `NOTICE` de permiso insuficiente, esta bien: es la degradacion prevista, y el creador que importa
    en este proyecto es `postgres`.

    **Paso 7 — Confirmar el orden respecto del deploy.** La 074 queda aplicada **antes** de deployar
    el codigo de la fase. No deployar hasta que los pasos 3 a 6 esten hechos.
  </how-to-verify>
  <resume-signal>
    Pega en el chat: (a) la fila del Paso 1 tal cual salio, (b) que migraciones hubo que aplicar en el
    Paso 2 (o "ninguna"), (c) la fila del Paso 5 con el `acl` completo, y (d) la salida del Paso 6.
    Despues escribi "aplicada" — o describi que salio mal.
  </resume-signal>
  <action>
    **Esto es un checkpoint bloqueante: Claude no ejecuta nada de este task.** Presentarle a la
    persona los 7 pasos de `<how-to-verify>` en orden, esperar el `<resume-signal>`, y **no avanzar**
    al Task 2 hasta tenerlo. Cuando llegue la respuesta, transcribir las salidas crudas de los pasos
    1, 5 y 6 al SUMMARY sin reinterpretarlas, y contrastarlas contra los criterios de aceptacion de
    abajo. Si la lista de privilegios de la funcion incluye al rol anonimo, o si el default de
    funciones sigue abierto, el checkpoint **no se aprueba**: se corrige con una migracion **075**
    nueva, nunca editando la 074 que ya corrio en produccion.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && git diff --stat -- supabase/migrations/074_save_agenda_blocks.sql && test -f supabase/migrations/074_save_agenda_blocks.sql && echo "074 sin modificar en el repo"</automated>
  </verify>
  <done>
    La persona confirmo con "aplicada" y pego las tres salidas; produccion tiene la 071/072/073/074
    aplicadas en orden, el cache de esquema recargado, y la funcion no es ejecutable por el rol
    anonimo. La 074 del repo no se toco.
  </done>
  <acceptance_criteria>
    - Las cuatro banderas del Paso 1 quedan en `true` y `grants_de_mas_072` en `0` **despues** del
      Paso 2 (si hizo falta aplicar algo, se vuelve a correr la consulta del Paso 1 para confirmarlo).
    - El Paso 5 devuelve exactamente **una** fila, con `prosecdef = false`, sin entrada para el rol
      anonimo en el `acl` y con la ejecucion concedida a `authenticated`.
    - El Paso 6 muestra que el default de FUNCTIONS del schema `public` ya no concede ejecucion al rol
      anonimo para el creador `postgres`.
    - El `NOTIFY` del Paso 4 se corrio **despues** de la 074 y **antes** del deploy.
    - Las salidas crudas de los pasos 1, 5 y 6 quedan pegadas en el SUMMARY: son la evidencia que
      cierra la contradiccion entre `18-VERIFICATION.md` y `18-SECURITY.md`, y la que `secure-phase`
      va a leer.
    - **No se modifico** `supabase/migrations/074_save_agenda_blocks.sql` durante este plan
      (`git diff -- supabase/migrations/074_save_agenda_blocks.sql` vacio).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Reflejar la 074 en `supabase/schema.sql`, quirurgicamente</name>
  <files>supabase/schema.sql</files>
  <read_first>
    - `18-01-SUMMARY.md` — el criterio y el resultado del reflejo quirurgico de la 071 (85 lineas
      agregadas, 0 borradas) y la razon de no usar el dump.
    - `supabase/schema.sql` **:213-250** — donde viven las definiciones de funciones y con que formato
      quedan escritas en el dump (quoting, `SET search_path`, `ALTER FUNCTION ... OWNER TO`).
    - `supabase/schema.sql` **:4190-4200** — el bloque de `GRANT EXECUTE ON FUNCTION`, para poner las
      tres sentencias nuevas donde el archivo ya las agrupa.
    - `supabase/migrations/074_save_agenda_blocks.sql` — la fuente de verdad de lo que hay que
      reflejar.
  </read_first>
  <action>
    Reflejar en `supabase/schema.sql` **exactamente** lo que la 074 agrega, insertando en las
    secciones que el archivo ya tiene para cada tipo de objeto (no al final del archivo, y sin
    reordenar nada):

    1. La definicion de la funcion, con el mismo formato con el que el dump escribe las demas
       funciones del schema, en la seccion de funciones y respetando el orden alfabetico si el archivo
       lo mantiene en esa zona.
    2. La sentencia de propietario de la funcion, si el archivo la emite para las funciones vecinas.
    3. Las **tres** sentencias de privilegios de la funcion, en el bloque donde el archivo ya agrupa
       los privilegios de ejecucion.
    4. La sentencia de default privileges sobre funciones, junto a las otras del mismo tipo.

    **Nunca** correr `supabase db dump`: reordena el archivo entero y vuelve el diff imposible de
    revisar. El objetivo es un diff de **solo lineas agregadas**.

    Cerrar con el commit de documentacion de la fase, mencionando en el mensaje que la 074 quedo
    aplicada en produccion (con la fecha) y que la proxima migracion libre del proyecto es la **075**.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && grep -cF "save_agenda_blocks" supabase/schema.sql && git diff -- supabase/schema.sql | grep -c "^-[^-]" && ./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF "save_agenda_blocks" supabase/schema.sql` >= **4** (definicion + propietario + los
      dos `REVOKE` + el `GRANT`; el minimo tolerado es 4 por si el archivo no emite la sentencia de
      propietario para esa zona).
    - `git diff -- supabase/schema.sql | grep -c "^-[^-]"` = **0**: **solo lineas agregadas**. Si hay
      lineas borradas, se uso un dump o se reordeno el archivo: revertir y volver a hacerlo a mano.
    - `git diff --stat -- supabase/schema.sql` reporta menos de **200** lineas agregadas.
    - `grep -cE "^[[:space:]]*SECURITY[[:space:]]+DEFINER" supabase/schema.sql` **no aumento**
      respecto de antes del plan (`git diff -- supabase/schema.sql | grep -cE "^\+.*SECURITY DEFINER"`
      = **0**): lo reflejado no es una funcion con privilegios de owner.
    - `git diff --name-only` toca **exactamente** `supabase/schema.sql` (mas los artefactos de
      planificacion del SUMMARY).
    - El SUMMARY deja escrito, en una linea propia y facil de encontrar: **la proxima migracion libre
      del proyecto es la 075**.
  </acceptance_criteria>
  <done>
    `supabase/schema.sql` refleja la funcion de la 074 y sus privilegios, con un diff de solo lineas
    agregadas, y el repo queda alineado con lo que hay en produccion.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persona operando el editor SQL de produccion → base de produccion | es la unica via de cambio de esquema del proyecto; no hay pipeline que la valide |
| base de produccion → PostgREST | una funcion que existe pero no esta en el cache de esquema es invisible para la app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-31 | Elevation of Privilege | aplicar la funcion sin su bloque de privilegios (por pegar solo la seccion 1 de la migracion) la deja ejecutable por el rol anonimo en produccion | mitigate | El Paso 3 exige pegar el archivo **entero**, y el Paso 5 verifica la lista de privilegios efectiva contra `pg_proc` antes de dar el plan por cerrado. Si el rol anonimo aparece, el checkpoint no se aprueba |
| T-19-32 | Denial of Service (operacional) | aplicar sin recargar el cache de esquema: la funcion existe pero PostgREST no la expone y **todos** los guardados de horarios fallan | mitigate | Paso 4 obligatorio dentro del mismo runbook, y el codigo del Plan 19-05 registra ese caso con prefijo de modulo para que el sintoma sea diagnosticable (P-05) |
| T-19-33 | Tampering (integridad de despliegue) | deployar el codigo antes de la migracion — es literalmente lo que paso con la **068** y dejo roto el alta de servicios mientras corrio el codigo viejo | mitigate | Paso 7 explicito: migracion primero, deploy despues. La 074 es aditiva, asi que el orden correcto no tiene costo |
| T-19-34 | Tampering | aplicar la 074 sobre una base sin la 071/073: no hay tabla que escribir ni FK que valide la pertenencia al tenant | mitigate | Paso 1 mide el estado real y el Paso 2 aplica lo que falte **en orden**, en vez de confiar en artefactos que se contradicen |
| T-19-35 | Repudiation | dar la fase por cerrada sin evidencia de que produccion quedo como se cree | mitigate | El `resume-signal` exige pegar las salidas crudas de los pasos 1, 5 y 6, y quedan en el SUMMARY |
| T-19-SC | Tampering (supply chain) | dependencias | accept | **Cero** paquetes nuevos: este plan no toca codigo de la app |
</threat_model>

<verification>
1. Las cuatro banderas del Paso 1 en `true` y `grants_de_mas_072` en `0`, medidas **despues** de
   aplicar lo que faltara.
2. `pg_proc` en produccion devuelve una fila para la funcion, con `prosecdef = false`, sin el rol
   anonimo en su lista de privilegios y con la ejecucion concedida a `authenticated`.
3. `pg_default_acl` para funciones en `public` ya no concede ejecucion al rol anonimo.
4. El `NOTIFY` se corrio despues de la 074 y antes del deploy.
5. `grep -cF "save_agenda_blocks" supabase/schema.sql` >= **4**.
6. `git diff -- supabase/schema.sql | grep -c "^-[^-]"` = **0**.
7. `git diff -- supabase/migrations/074_save_agenda_blocks.sql` vacio: la migracion aplicada y la del
   repo son el mismo archivo.
8. El SUMMARY contiene las salidas crudas de los pasos 1, 5 y 6, y la linea que dice que la proxima
   migracion libre es la **075**.
</verification>

<success_criteria>
- La contradiccion sobre el estado de las migraciones de la Phase 18 en produccion queda **resuelta
  con una medicion**, no con una suposicion.
- La 074 esta en produccion, expuesta por PostgREST, y **no** ejecutable por el rol anonimo.
- El repo (`supabase/schema.sql`) refleja lo que hay en la base, con un diff de solo lineas agregadas.
</success_criteria>

## Artifacts this phase produces

| Artefacto | Plan | Estado al cerrar este plan |
|---|---|---|
| `lib/time-block-services.ts` → `servicesOfBlock`, `isBlockWildcard` | 19-01 | creado |
| `lib/agenda-hours-payload.ts` → `buildSaveHoursPayload`, `buildDayStatesFromRows` | 19-01 | creado |
| `supabase/migrations/074_save_agenda_blocks.sql` → `public.save_agenda_blocks(uuid, jsonb)` | 19-02 | **aplicada en produccion y verificada** |
| `test/agenda-save-blocks-rpc.test.ts` | 19-02 | verde |
| `app/(dashboard)/settings/settings-client.tsx` → 5o count + frase de D-07 | 19-03 | creado |
| `app/[slug]/booking-client.tsx` → rama `service_not_scheduled` | 19-03 | creado |
| `app/(dashboard)/agenda/page.tsx` → props `initialTimeBlockServices` y `serviceCatalog` | 19-04 | creadas |
| `app/(dashboard)/agenda/agenda-client.tsx` → `ServiceCatalogItem`, `service_ids`, `hoursDirty`, toggle de servicio, fila sin stepper | 19-04 | creados |
| `app/(dashboard)/agenda/agenda-client.tsx` → `ServiceChip`, `BlockServicesLine`, `CHIPS_COLLAPSED_MAX`, "Cambios sin guardar", `saveHours` sobre el RPC | 19-05 | creados |
| `supabase/schema.sql` reflejando la 074 | **19-06** | actualizado |

<output>
Create `.planning/workstreams/motor-reservas/phases/19-el-panel/19-06-SUMMARY.md` when done
</output>
