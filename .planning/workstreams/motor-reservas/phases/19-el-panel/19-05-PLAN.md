---
phase: 19-el-panel
plan: 05
type: execute
wave: 3
depends_on: ["19-01", "19-02", "19-04"]
files_modified:
  - app/(dashboard)/agenda/agenda-client.tsx
autonomous: true
requirements: [AGENDA-05, AGENDA-06]

must_haves:
  truths:
    - "Debajo de cada franja del editor se ve, SIN abrir nada, que servicios se dan en ella (AGENDA-05)"
    - "Una franja sin servicios asignados muestra un chip 'Cualquier servicio': un estado DECLARADO, no un hueco ni un placeholder (AGENDA-06, D-16)"
    - "Apagar el ultimo chip devuelve la franja a comodin y el chip 'Cualquier servicio' reaparece al instante, sin animacion y sin mover el layout (D-17)"
    - "Un servicio desactivado que sigue mapeado se muestra marcado y con la palabra 'inactivo': la franja NO es comodin y el panel no miente sobre lo que ve el publico (D-11)"
    - "Un servicio marcado nunca se colapsa detras de 'Ver todos': lo unico que se colapsa son las opciones NO elegidas"
    - "Se guarda con el boton que ya existe, en una sola llamada todo-o-nada, y el estado local queda re-derivado de lo que devolvio la base (D-03, D-04, P-01)"
    - "El rechazo de la base se traduce a copy propia por codigo de error: el mensaje de Postgres nunca llega a la pantalla (T-14-25 / T-13-09)"
    - "El editor deja de escribir time_blocks directamente: la unica escritura de horarios es la llamada al RPC"
    - "En el vertical canchas la linea no se renderiza, y esas franjas quedan comodin igual: cero regresion"
  artifacts:
    - path: "app/(dashboard)/agenda/agenda-client.tsx"
      provides: "la linea de servicios por franja (chip, grupo, comodin, colapso), el indicador de cambios sin guardar y el guardado atomico sobre el RPC"
      contains: "save_agenda_blocks"
      exports: ["AgendaClient"]
  key_links:
    - from: "app/(dashboard)/agenda/agenda-client.tsx"
      to: "public.save_agenda_blocks"
      via: "una sola invocacion rpc con el business_id y el set completo de franjas"
      pattern: "save_agenda_blocks"
    - from: "app/(dashboard)/agenda/agenda-client.tsx"
      to: "lib/time-block-services.ts"
      via: "isBlockWildcard decide si se pinta el chip comodin; el componente solo pinta"
      pattern: "isBlockWildcard"
    - from: "app/(dashboard)/agenda/agenda-client.tsx"
      to: "lib/agenda-hours-payload.ts"
      via: "buildSaveHoursPayload arma el payload y buildDayStatesFromRows re-deriva el estado con lo que devolvio la base"
      pattern: "buildSaveHoursPayload"

  prohibitions:
    - statement: "La linea de servicios no abre nada: ni modal, ni drawer, ni popover — AGENDA-05 pide ver sin abrir"
      status: resolved
      verification: "el diff no agrega ninguna etiqueta JSX de dialogo, cajon o popover"
    - statement: "El acento de la paleta no entra en la linea de servicios: a 12px sobre la superficie clara falla AA en 3 de las 5 paletas, incluida la default"
      status: resolved
      verification: "el diff no agrega ninguna clase de texto con el token de acento"
    - statement: "Ningun chip se atenua con opacidad: baja el contraste por debajo de AA. La senal es forma y palabra"
      status: resolved
      verification: "el diff no agrega clases de opacidad"
    - statement: "No se valida que una franja tenga servicios: la franja sin servicios es el estado valido por defecto del 100% de los negocios"
      status: resolved
      verification: "validateBlocks no cambia de tamano ni menciona los ids de servicio"
    - statement: "El mensaje crudo de Postgres no se muestra en ningun toast"
      status: resolved
      verification: "ninguna llamada de toast recibe una propiedad de mensaje de un error"
---

<objective>
El corazon de la fase: que el dueno **vea y cambie** que se da en cada franja desde el editor de
horarios, que el caso por defecto se lea como **"cualquiera"**, y que todo eso se persista con el
boton que ya existe, en **una sola transaccion**.

Purpose: AGENDA-05 pide que la grilla **muestre** que se da en cada franja **sin abrir nada** — de
ahi que sea una segunda linea con chips y no un boton que abre un modal. AGENDA-06 pide que la franja
sin servicios se lea como un estado **declarado**: el dia del deploy el **100% de los negocios** la
ve asi, y si eso pareciera un campo sin llenar, la pantalla entera se leeria como un tablero de
pendientes. Y el guardado tiene que ser todo-o-nada porque el estado intermedio —horarios nuevos con
mapeo viejo— **lo ve el publico** a traves de la disponibilidad.

El contrato visual esta **cerrado y aprobado** en `19-UI-SPEC.md` (6/6): este plan lo materializa,
no lo re-deriva.

Output: `ServiceChip` y `BlockServicesLine` definidas fuera del componente, la linea insertada bajo
cada franja, el indicador de cambios sin guardar, y `saveHours` reescrita sobre el RPC de la 074.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/19-el-panel/19-UI-SPEC.md
@.planning/workstreams/motor-reservas/phases/19-el-panel/19-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/19-el-panel/19-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/19-el-panel/19-02-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/19-el-panel/19-04-SUMMARY.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<constraints>
- **`19-UI-SPEC.md` manda y esta LOCKED.** Clases, tokens, copy, umbral, `aria-*` y estados salen de
  ahi tal cual. Si algo te parece mejorable, se anota en el SUMMARY; no se cambia acá.
- **Cero componentes nuevos de `@/components/ui`** y **prohibido `npx shadcn add`**. La linea se arma
  con dos funciones locales, hermanas de `OccupancyBadge`, **definidas fuera** del componente para no
  recrearse en cada render (se renderizan dentro de un `map` de 7 dias x N bloques).
- **Cero dependencias nuevas.** Los dos iconos salen de la libreria que ya usa el archivo.
- **Usar `Edit`, nunca `Write`**: el archivo tiene ~1300 lineas y el cambio es parcial.
- **No se toca la vista semanal de turnos** (D-13), ni el encabezado del dia colapsado (D-15), ni el
  calendario de dias especiales, ni la ventana de reserva, ni el alta manual.
- **`validateBlocks` no cambia** (P-10): sigue validando solo horas y solapamientos. Una franja sin
  servicios **no** es un error, ni un warning, ni un asterisco — es el estado valido por defecto.
- **La regla del comodin no se reimplementa** (AGENDA-02 / P-07): sale del helper puro.
- **NUNCA `npx tsc`** → `./node_modules/.bin/tsc --noEmit`.
- Comentarios densos **en espanol** explicando el POR QUE.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: La linea de servicios bajo cada franja (Bloque A del UI-SPEC)</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx</files>
  <read_first>
    - `19-UI-SPEC.md` **§Bloque A completo** (ubicacion exacta, gates de visibilidad, anatomia,
      contrato del chip, orden y umbral, chip comodin, servicio desactivado, empty state, linea guia,
      estados) + **§Color**, **§Typography**, **§Spacing**, **§Accesibilidad**, **§Responsive** y
      **§Copywriting Contract**. Es el contrato: no re-derivar nada de esto.
    - `app/(dashboard)/agenda/agenda-client.tsx` **:110-145** (`OccupancyBadge`: el molde de helper
      de presentacion fuera del componente, con props tipadas inline y `cn()` con clases base +
      condicionales), **:884-1000** (el selector de consultorio, el `map` de dias, la fila de la
      franja ya sin stepper, el parrafo de error y los botones de agregar/copiar) y el estado que
      dejo el Plan 19-04 (bloque local con ids de servicio, mutador de toggle, estado sucio).
    - `components/crm/tag-chip.tsx` **:42-64** — el molde primario: boton externo de 44px que
      envuelve un pill visual mas bajo, con `aria-pressed` y anillo de foco.
    - `app/(dashboard)/settings/settings-client.tsx` **:2740-2800** — el segundo molde: el grupo con
      su etiqueta accesible interpolando la terminologia del vertical, el icono de tilde condicional,
      y el estado inline que evita el salto de layout. **De acá NO se copia el color** (falla AA) ni
      la escritura inmediata.
    - `app/(dashboard)/settings/settings-client.tsx` **:955** y `app/(dashboard)/equipo/page.tsx`
      **:18** — el precedente del gate por vertical.
    - `19-RESEARCH.md` Pitfall **P-09** (el parrafo de error tiene que quedar pegado a los inputs).
  </read_first>
  <action>
    ⚠ **Dos decisiones LOCKED gobiernan la forma de todo este task y no se reabren:**
    **D-08** — la linea va como **segunda linea bajo la fila del bloque**, nunca inline junto a las
    horas (rompe la linea a 375px) y nunca detras de un boton que abre algo (para saber que se da
    habria que **abrir**, que es lo que AGENDA-05 prohibe). **D-09** — el control es un **chip
    toggleable** por servicio: cero clicks para VER el estado, un click para cambiarlo, y hace
    evidente que "ninguno prendido" es un estado **valido** y no un campo sin llenar, que es
    exactamente lo que AGENDA-06 necesita.

    **(a) Las dos funciones locales, fuera del componente.** Definirlas antes de `AgendaClient`, al
    lado de `OccupancyBadge` y con su mismo idioma.

    - **El chip**: un boton de tipo explicito con el estado presionado expuesto por `aria-pressed`,
      area tactil minima de 44 puntos en alto y ancho, anillo de foco visible, y **adentro** un pill
      de 28 puntos de alto con borde, pill redondeado, padding horizontal de 12 y texto de 12px en
      peso medio, sin quiebre de linea. El tratamiento de color sale del UI-SPEC §Color y es **todo
      neutro**: el estado marcado se distingue por relleno + color de texto + el icono de tilde, o
      sea **tres portadores**, nunca el color solo. Recibe ademas una bandera de "inactivo" que suma
      borde punteado, y una etiqueta accesible opcional para el caso en que el texto visible no
      alcance.
    - **La linea**: un contenedor con rol de grupo y etiqueta accesible que nombra la franja por su
      horario, con envoltura de linea y el espaciado del UI-SPEC (la separacion vertical entre filas
      de chips la da el area tactil, no un espacio; agregarlo romperia el peso secundario de D-14).
      Adentro decide, en este orden: si la franja es comodin, el chip informativo; si no, los chips.

    Documentar arriba de las dos, con un comentario denso, **por que van fuera del componente**: se
    renderizan dentro de un `map` de 7 dias x N bloques y redefinirlas por render las recrearia en
    cada tecla del input de hora.

    **(b) El chip comodin (D-16 / AGENDA-06).** Se renderiza **si y solo si** la franja no tiene
    ningun servicio asignado, y esa decision sale de la funcion pura de `lib/time-block-services.ts`
    — **prohibido** un filtro inline en el JSX (P-07). Es **informativo, no clickeable**: un control
    cuyo unico estado posible es el no-op ensena mal la regla, y si fuera clickeable se leeria como
    una opcion mas entre los servicios, que es justo la lectura que D-16 evita. Va con rol de estado
    (para que un lector de pantalla lo anuncie cuando reaparece, nunca como alerta), borde punteado,
    el icono de asterisco de la libreria de iconos del proyecto, y la misma altura minima de 44 que
    los chips **aunque no sea clickeable**: asi la altura de la linea es identica con o sin el, y no
    hay salto de layout cuando aparece o desaparece (D-17 pide que reaparezca **al instante**).

    Comentar el punto que se pierde al leer el codigo: **volver a comodin es borrar filas**, no
    escribir un estado vacio; la ausencia ES el estado. Y que este chip **nunca coexiste** con un chip
    marcado, incluidos los de servicios desactivados.

    **(c) Que chips se muestran, y en que orden.** Los servicios **activos** del catalogo (D-11), mas
    los **inactivos que siguen mapeados a esa franja**. Un inactivo no mapeado no aparece nunca. El
    orden es el del catalogo —fecha de creacion ascendente, que ya viene ordenado del servidor— y
    **esta prohibido reordenar por seleccionados**: los chips saltarian de lugar al togglear.

    **(d) El servicio desactivado mapeado (D-11).** Se muestra **marcado**, con borde punteado y el
    sufijo de palabra que fija el UI-SPEC. **No se oculta** (ocultarlo mentiria: la franja sigue
    restringida y el dueno no tendria forma de sacarlo desde ninguna pantalla) y **no se atenua con
    opacidad** (baja el contraste por debajo de AA). Lleva etiqueta accesible completa. Al tocarlo se
    quita el mapeo y el chip desaparece de la linea; **es el unico toggle de toda la linea que emite
    un aviso**, y es informativo, con la copy exacta del UI-SPEC — porque es la unica accion de la
    linea que no se puede deshacer desde esa pantalla. Ningun otro toggle emite aviso: el chip que se
    pinta ES el feedback.

    **(e) El colapso (D-10).** Declarar una constante de modulo con el umbral **6** y un nombre
    explicito, con un comentario que diga de donde sale (a 375px el ancho util es ~295px y un chip
    promedio mide ~85px ⇒ 3 por fila; 6 son las ~2 filas de D-10, expresadas sin medir el DOM). Con
    6 chips o menos se muestran todos y **no hay** disparador. Con mas, en estado colapsado se
    muestran **todos los marcados** (que nunca se ocultan: colapsar un servicio declarado romperia
    AGENDA-05), mas el chip comodin si corresponde, mas los no marcados hasta llegar a 6 en total, y
    el disparador con el total entre parentesis. Expandido, todos los chips y el disparador inverso.
    La expansion es **in situ**, en la misma linea: nada de modal, cajon ni popover. El estado de
    expandido es **por franja**, indexado por dia e indice, y se resetea al eliminar un bloque (los
    indices se corren) y al cambiar de consultorio. No persiste entre recargas. El disparador es
    neutro y subrayado (es navegacion dentro de la linea, no una accion de configuracion) y tambien
    llega a 44 de alto.

    **(f) Los gates de visibilidad, en este orden.**
    1. Vertical **canchas**: la linea **no se renderiza** en ninguna franja, y la linea guia tampoco.
       En ese vertical un "servicio" ES una cancha con su propia agenda, y ofrecer "que canchas se dan
       en esta franja" duplicaria ese eje con otro que no lo decide. Sin el gate esas franjas quedan
       comodin igual (0 filas), asi que el gate no cambia comportamiento: solo saca ruido. Mismo
       precedente que ya usa el proyecto para gatear por vertical.
    2. Catalogo vacio: **no** se renderiza una linea por franja. En su lugar, **una sola** linea en la
       card, en la misma posicion que la linea guia (son mutuamente excluyentes), con la copy del
       UI-SPEC y un enlace a la pantalla de servicios. Repetir catorce veces "todavia no cargaste
       servicios" convierte una pantalla sana en un tablero de pendientes.
    3. Resto: se renderiza en **todas** las franjas del consultorio activo.

    **(g) La linea guia.** Cuando si hay servicios, **una sola** linea de ayuda arriba de la lista de
    dias, con la copy del UI-SPEC (que interpola la terminologia del vertical y evita el articulo
    antes de la interpolacion, regla de genero heredada de la Phase 8). Es el **unico** texto
    explicativo de toda la fase: ensena la regla del comodin una vez, no una vez por franja.

    **(h) La insercion en el JSX (P-09).** La linea va como **tercer** hijo del contenedor de la
    franja, **despues** del parrafo de error del bloque. El error tiene que quedar pegado a los
    inputs que lo causaron; los chips van abajo de todo. **No** se envuelve la fila existente en
    contenedores nuevos ni se cambia su espaciado.

    **(i) Movimiento y carga.** Solo transicion de color. **Prohibido** animar la aparicion o
    desaparicion del chip comodin, la expansion del colapso, o cualquier propiedad que dispare
    layout. Sin skeleton y sin spinner: los datos llegan por props server-rendered. Los chips **no**
    se deshabilitan mientras se guarda (deshabilitar solo los chips y no los inputs de hora seria una
    inconsistencia nueva; el feedback ya lo da el boton).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && npm run lint && grep -cE "^function (ServiceChip|BlockServicesLine)" "app/(dashboard)/agenda/agenda-client.tsx" && grep -cF "isBlockWildcard" "app/(dashboard)/agenda/agenda-client.tsx" && grep -cF "aria-pressed" "app/(dashboard)/agenda/agenda-client.tsx" && grep -cF "min-h-11" "app/(dashboard)/agenda/agenda-client.tsx" && grep -cE "\.filter\([^)]*time_block_id" "app/(dashboard)/agenda/agenda-client.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0** y `npm run lint` exit **0**.
    - `grep -cE "^function (ServiceChip|BlockServicesLine)" "app/(dashboard)/agenda/agenda-client.tsx"`
      = **2**: las dos estan **fuera** del componente (empiezan en columna 0, como `OccupancyBadge`).
    - `grep -cF "isBlockWildcard" "app/(dashboard)/agenda/agenda-client.tsx"` >= **1** y
      `grep -cE "\.filter\([^)]*time_block_id" "app/(dashboard)/agenda/agenda-client.tsx"` = **0**:
      la regla sale del helper puro (AGENDA-02 / P-07).
    - `grep -cF "aria-pressed" "app/(dashboard)/agenda/agenda-client.tsx"` >= **1**;
      `grep -cF 'role="group"' "app/(dashboard)/agenda/agenda-client.tsx"` >= **1**;
      `grep -cF 'role="status"' "app/(dashboard)/agenda/agenda-client.tsx"` >= **1**;
      `grep -cF 'role="alert"' "app/(dashboard)/agenda/agenda-client.tsx"` = **0**.
    - `grep -cF "min-h-11" "app/(dashboard)/agenda/agenda-client.tsx"` >= **3** (chip, chip comodin y
      disparador del colapso) y `grep -cF "min-w-11" ...` >= **1**.
    - `grep -cF "Asterisk" "app/(dashboard)/agenda/agenda-client.tsx"` >= **2** (import + uso) y
      `git diff -- package.json` vacio: el icono sale de la libreria que ya esta.
    - `grep -cF "CHIPS_COLLAPSED_MAX" "app/(dashboard)/agenda/agenda-client.tsx"` >= **2** (la
      constante de modulo y su uso), y su valor es **6**.
    - `grep -cF "'canchas'" "app/(dashboard)/agenda/agenda-client.tsx"` >= **1**: el gate de vertical
      existe.
    - **Sin abrir nada** (AGENDA-05):
      `git diff -- "app/(dashboard)/agenda/agenda-client.tsx" | grep -cE "^\+.*<(Dialog|Drawer|Popover|Sheet)"` = **0**.
    - **Contraste y atenuacion**:
      `git diff -- "app/(dashboard)/agenda/agenda-client.tsx" | grep -cE "^\+.*(text-primary|opacity-)"` = **0**.
    - **Sin colores crudos**: `git diff -- "app/(dashboard)/agenda/agenda-client.tsx" | grep -cE "^\+.*(#[0-9a-fA-F]{3,8}\b|text-neutral-|bg-neutral-)"` = **0**.
    - **Tipografia**: `git diff -- "app/(dashboard)/agenda/agenda-client.tsx" | grep -cE "^\+.*(text-base|text-lg|font-semibold|font-bold|text-\[1[01]px\]|text-\[9px\])"` = **0**.
    - **`validateBlocks` intacta**: `git diff -- "app/(dashboard)/agenda/agenda-client.tsx" | grep -cE "^[-+].*validateBlocks"` refleja **solo** su invocacion desde el guardado, no cambios en su cuerpo (verificado leyendo el diff y anotado en el SUMMARY) — P-10.
    - `git status --porcelain components/ui | wc -l` = **0** y `git diff -- components.json` vacio.
    - **UAT visual reportada en el SUMMARY, con capturas o descripcion paso a paso, a 375px y en
      desktop:** (1) franja sin servicios ⇒ se ve el chip comodin y **no** se lee como un campo sin
      llenar; (2) marcar un servicio ⇒ el chip comodin desaparece y la linea **no** cambia de altura;
      (3) apagar el ultimo ⇒ el chip comodin reaparece al instante; (4) negocio con 8 servicios ⇒ el
      disparador aparece, los marcados nunca se esconden; (5) servicio desactivado mapeado ⇒ chip
      marcado con borde punteado y la palabra, y al quitarlo aparece el aviso; (6) negocio sin
      servicios ⇒ **una sola** linea con el enlace, no una por franja; (7) vertical canchas ⇒ no hay
      ninguna linea de servicios; (8) navegacion por teclado: hora inicio → hora fin → eliminar →
      chips, con anillo de foco visible en todo.
  </acceptance_criteria>
  <done>
    El dueno ve que se da en cada franja sin abrir nada, y la franja sin servicios se lee como
    "cualquiera" — un estado declarado, no un hueco.
  </done>
</task>

<task type="auto">
  <name>Task 2: El guardado atomico sobre el RPC + el indicador de cambios sin guardar</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx</files>
  <read_first>
    - `app/(dashboard)/agenda/agenda-client.tsx` **:366-390** — `saveHours` tal como quedo despues
      del Plan 19-04, y **:1002-1007** — el bloque del boton de guardar.
    - `19-02-SUMMARY.md` — la firma **real** con la que quedo la funcion de la 074 (nombre de los dos
      parametros, columnas del retorno) y los cuatro codigos de dominio que puede lanzar. No
      adivinarlos: leerlos de ahi.
    - `lib/agenda-hours-payload.ts` — las firmas reales de las dos funciones del Plan 19-01.
    - `app/(dashboard)/settings/settings-client.tsx` **:1280-1297** — el molde **exacto** del mapeo de
      rechazos: se inspecciona el codigo y el mensaje, se devuelve un codigo de dominio, y la copy la
      pone el call site. El mensaje **se inspecciona, nunca se muestra**.
    - `lib/booking-core.ts` **:499-514** — la unica invocacion de `.rpc()` que hoy tiene el repo, para
      el estilo de los parametros.
    - `19-RESEARCH.md` §"Pattern 2" (la tabla de rechazos → copy), Pitfalls **P-01**, **P-03** y
      **P-05**, y §"El write path HOY" punto 4 (por que la actualizacion de la duracion del turno
      queda **fuera** del RPC).
    - `19-UI-SPEC.md` §"Cambios sin guardar" y §Copywriting Contract (las copys de guardado).
  </read_first>
  <behavior>
    - Al tocar el boton que ya existe: si la validacion de horas falla, se avisa y no se llama a la
      base (comportamiento de hoy, intacto).
    - Si pasa, se arma el payload con el set **completo** de franjas (todos los dias, todos los
      consultorios) y se hace **una** llamada a la funcion de la 074.
    - Con exito: el estado local se **re-deriva** de las filas devueltas, el estado sucio se apaga y
      se avisa que se guardo. Recien despues se persiste la duracion del turno y el descanso, y **su**
      error se chequea y se avisa aparte.
    - Con error: copy propia segun el codigo, un registro en consola con el prefijo del modulo, y el
      estado local **no** se toca (lo que el dueno ve sigue siendo lo que quiso guardar).
    - El boton vuelve a estar disponible pase lo que pase.
  </behavior>
  <action>
    **(a) Reescribir `saveHours` entera.**

    1. Validar horas como hoy. Si falla, mismo aviso y salir.
    2. Marcar el guardado en curso.
    3. Armar el payload con la funcion del Plan 19-01, pasandole los **7 dias completos** y la senal
       de si el negocio tiene consultorios cargados. ⚠ **Nunca** la lista ya filtrada por consultorio
       activo: el RPC borra lo que no venga en el payload, asi que un payload por sede borraria las
       otras (P-03). Comentarlo en el codigo, porque es una regresion que solo aparece en un negocio
       multi-sede y que nadie ve en local.
    4. Una sola invocacion de la funcion de la 074 con el id del negocio y el payload. **No** hacer
       borrados ni inserciones directas sobre las tablas: la atomicidad es el punto (D-04).
    5. Si hubo error: mapearlo a un codigo de dominio con el molde del borrado de servicios
       —inspeccionar el codigo de error y, cuando hace falta distinguir dos casos sobre el mismo
       codigo, el contenido del mensaje— y de ahi a **copy propia**. La tabla de correspondencias
       esta en `19-RESEARCH.md` §Pattern 2, ampliada con los codigos que la 074 define: el de autoria
       y el de violacion de clave foranea comparten sintoma para el dueno (recargar la pagina); el de
       franja no encontrada dice que los horarios cambiaron desde otra pestana o sesion y hay que
       recargar; los de payload o bloque invalido caen en el aviso de corregir antes de guardar; el
       de funcion no expuesta por PostgREST cae en la copy generica **pero ademas** deja un registro
       en consola con el prefijo del modulo, porque sin eso el sintoma es indistinguible de un
       problema de red y el diagnostico real es "la migracion no esta aplicada o falto recargar el
       cache" (P-05); cualquier otro cae en la copy generica de conexion del UI-SPEC.
       **Prohibido** pasar el mensaje de la base a la pantalla (T-14-25 / T-13-09). Salir sin tocar el
       estado local.
    6. Si salio bien: **re-derivar** `dayStates` de las filas devueltas con la funcion del Plan 19-01
       (es la misma que usa el inicializador), apagar el estado sucio y avisar el exito. Comentar por
       que este paso **no es opcional**: el editor inicializa su estado una sola vez y no vuelve a
       derivarlo de las props, asi que sin la re-derivacion el segundo guardado volveria a insertar
       cada bloque nuevo (P-01) — y recargar la ruta del servidor **no** alcanza.
    7. Recien despues, persistir la duracion del turno y el descanso entre turnos con la
       actualizacion que ya existe, y **chequear su error** (hoy no se chequea). Si falla, un aviso
       propio que diga la verdad: los horarios si se guardaron. Comentar por que queda **fuera** de la
       transaccion: esos dos valores no participan de ninguna invariante con el mapeo, y meterlos
       agrandaria la firma del RPC sin cerrar ningun estado inconsistente que el publico pueda ver.
    8. Devolver el boton a su estado normal en todos los caminos.

    **(b) El indicador de cambios sin guardar.** Al lado del boton de guardar, dentro de un contenedor
    en fila con separacion, mostrar —solo cuando hay cambios pendientes— el texto corto del UI-SPEC,
    con rol de estado y el token de advertencia (el **unico** lugar de la fase donde ese token se
    usa). Sin bloqueo de navegacion, sin aviso al cerrar la pestana, sin modal: fuera de alcance.

    **(c) El sexto encendido.** Confirmar que los seis mutadores prenden el estado sucio (los cinco
    del Plan 19-04 mas el toggle de servicio) y que `saveHours` es el **unico** que lo apaga, y solo
    tras un guardado exitoso. Un indicador de estado sucio que miente es peor que no tenerlo.

    **(d) Documentar los dos limites que la fase acepta**, con comentarios cortos donde corresponda:
    los dias con horario especial no tienen mapeo posible y siguen ofreciendo todos los servicios
    (D-06, el caveat que la Phase 18 acepto como RA-04), y la lectura de la disponibilidad todavia no
    filtra por sede (D-19 / WR-04): el bloque ya lleva su consultorio y un dueno multi-sede **puede**
    mapear por sede, lo que no discrimina por sede es la lectura publica. Cerrarlo tocaria el helper
    puro y los dos consumidores que la Phase 18 dejo con cero amenazas abiertas, para un caso con
    **cero** negocios afectados hoy.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && npm run lint && grep -cF "save_agenda_blocks" "app/(dashboard)/agenda/agenda-client.tsx" && grep -cE "from\('time_blocks'\)" "app/(dashboard)/agenda/agenda-client.tsx" && grep -cF "buildSaveHoursPayload" "app/(dashboard)/agenda/agenda-client.tsx" && grep -cF "buildDayStatesFromRows" "app/(dashboard)/agenda/agenda-client.tsx" && grep -cE "toast\.(error|success|info|warning)\([^)]*\b(err|error)\b[^)]*\.(message|details|hint)" "app/(dashboard)/agenda/agenda-client.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0** y `npm run lint` exit **0**.
    - `grep -cF "save_agenda_blocks" "app/(dashboard)/agenda/agenda-client.tsx"` = **1**: una sola
      invocacion.
    - `grep -cE "from\('time_blocks'\)" "app/(dashboard)/agenda/agenda-client.tsx"` = **0**: el
      editor dejo de escribir la tabla directamente.
    - `grep -cF "buildSaveHoursPayload" "app/(dashboard)/agenda/agenda-client.tsx"` = **1** y
      `grep -cF "buildDayStatesFromRows" "app/(dashboard)/agenda/agenda-client.tsx"` = **2** (el
      inicializador + la re-derivacion post-guardado). **Si da 1, P-01 sigue abierto.**
    - `grep -cE "toast\.(error|success|info|warning)\([^)]*\b(err|error)\b[^)]*\.(message|details|hint)" "app/(dashboard)/agenda/agenda-client.tsx"` = **0**.
    - `grep -cF "[agenda/save-hours]" "app/(dashboard)/agenda/agenda-client.tsx"` >= **1**: el
      registro en consola lleva el prefijo de modulo.
    - `grep -cF "setHoursDirty(false)" "app/(dashboard)/agenda/agenda-client.tsx"` = **1** y
      `grep -cF "setHoursDirty(true)" ...` = **6**.
    - `grep -cF "Cambios sin guardar" "app/(dashboard)/agenda/agenda-client.tsx"` = **1** y esa linea
      usa el token de advertencia.
    - `grep -cF "Guardar horarios" "app/(dashboard)/agenda/agenda-client.tsx"` = **1**: la fase **no**
      agrega ningun CTA nuevo (D-03).
    - La actualizacion de la duracion del turno chequea su error:
      `grep -B2 -A2 "default_slot_duration" "app/(dashboard)/agenda/agenda-client.tsx" | grep -cF "error"` >= **1**.
    - **UAT funcional reportada en el SUMMARY, contra la base local con la 074 aplicada:**
      (1) mapear servicios a una franja, guardar, **recargar** y confirmar que el mapeo sigue;
      (2) **guardar dos veces seguidas sin recargar** y confirmar que no se duplico ningun bloque —
      es la senal temprana de P-01;
      (3) cambiarle el horario a una franja mapeada, guardar, y confirmar que **el mapeo quedo
      intacto** (D-02);
      (4) en un negocio con dos consultorios, guardar desde el tab A y confirmar que los bloques del
      tab B **siguen ahi** (P-03);
      (5) apagar todos los chips de una franja, guardar y confirmar que la puente quedo sin filas
      para esa franja y el chip comodin volvio;
      (6) copiar un dia con servicios a otros dos y confirmar que el aviso nombra los servicios y que
      togglear un chip de un dia destino **no** cambia el otro (P-04);
      (7) forzar un error (por ejemplo renombrando temporalmente la funcion en la base local) y
      confirmar que el toast es copy propia, que la consola registra con el prefijo, y que **el
      estado en pantalla no cambio**.
  </acceptance_criteria>
  <done>
    "Guardar horarios" persiste franjas y mapeo en una sola transaccion, el estado local queda
    sincronizado con la base, y ningun rechazo de la base llega crudo a la pantalla.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| estado del editor (browser) → funcion de la 074 | el payload lo arma el cliente: ids de negocio, de franja y de servicio son entrada no confiable para la base |
| rechazo de la base → pantalla del dueno | el mensaje de Postgres trae nombres de tabla y de constraint; solo puede cruzar un codigo |
| escritura del panel → superficie publica de disponibilidad | lo que se guarda acá es lo que decide que horarios ve un cliente anonimo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-24 | Information Disclosure | mostrar el mensaje crudo del rechazo en un toast filtraria nombres de tabla, de constraint y detalles del schema | mitigate | Mapeo por codigo de error al molde del borrado de servicios → codigo de dominio → copy propia del call site. Criterio que cuenta en 0 las llamadas de toast con una propiedad de mensaje de error (T-14-25 / T-13-09) |
| T-19-25 | Tampering | payload armado desde la lista filtrada por consultorio activo ⇒ el RPC borra los bloques de las otras sedes | mitigate | El payload sale de la funcion pura que **no acepta** consultorio (Plan 19-01), y la UAT incluye el escenario de dos consultorios (P-03) |
| T-19-26 | Elevation of Privilege | invocar la funcion desde la consola del browser con el id de otro negocio | mitigate | La defensa no vive acá: es el guard de autoria + la RLS + las FK compuestas dentro del RPC (Plan 19-02, T-19-06/07/08). El cliente manda el id del negocio de la sesion y no lo toma de ningun input |
| T-19-27 | Repudiation | guardado que falla mudo: el dueno cree que configuro y el publico ve otra cosa | mitigate | Se chequea el error del RPC **y** el de la actualizacion de la duracion del turno (hoy sin chequear), con registro en consola con prefijo de modulo y copy distinta para cada caso |
| T-19-28 | Repudiation | el indicador de cambios sin guardar que miente (un mutador que no lo prende) es peor que no tenerlo | mitigate | Seis encendidos, un solo apagado y solo tras exito; criterio de aceptacion que los cuenta |
| T-19-29 | Denial of Service (auto-infligido) | validar "esta franja no tiene servicios" como error convertiria el estado valido del 100% de los negocios en un tablero de pendientes | mitigate (por prohibicion) | `validateBlocks` no cambia (P-10); criterio de aceptacion que verifica que su cuerpo no se toco |
| T-19-30 | Denial of Service (operacional) | codigo deployado contra una base sin la 074 aplicada o sin recargar el cache de PostgREST ⇒ ningun guardado de horarios funciona | mitigate | El codigo mapea ese caso a copy propia **con registro diagnosticable**, y el Plan 19-06 hace la aplicacion y la verificacion contra produccion antes del deploy (P-05) |
| T-19-SC | Tampering (supply chain) | dependencias y componentes de registry | accept | **Cero** paquetes nuevos y **cero** componentes instalados: `git diff -- package.json package-lock.json components.json` vacio y `git status --porcelain components/ui` vacio |
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` exit **0** y `npm run lint` exit **0**.
2. `grep -cE "^function (ServiceChip|BlockServicesLine)" "app/(dashboard)/agenda/agenda-client.tsx"` = **2**.
3. `grep -cF "save_agenda_blocks" ...` = **1**; `grep -cE "from\('time_blocks'\)" ...` = **0**.
4. `grep -cF "buildSaveHoursPayload" ...` = **1**; `grep -cF "buildDayStatesFromRows" ...` = **2**.
5. `grep -cF "isBlockWildcard" ...` >= **1**; `grep -cE "\.filter\([^)]*time_block_id" ...` = **0**.
6. `grep -cE "toast\.(error|success|info|warning)\([^)]*\b(err|error)\b[^)]*\.(message|details|hint)" ...` = **0**;
   `grep -cF "[agenda/save-hours]" ...` >= **1**.
7. `grep -cF "setHoursDirty(true)" ...` = **6**; `grep -cF "setHoursDirty(false)" ...` = **1**.
8. `git diff -- "app/(dashboard)/agenda/agenda-client.tsx" | grep -cE "^\+.*(<(Dialog|Drawer|Popover|Sheet)|text-primary|opacity-|#[0-9a-fA-F]{3,8}\b|text-base|text-lg|font-semibold|font-bold)"` = **0**.
9. `grep -cF "min-h-11" ...` >= **3**; `grep -cF "aria-pressed" ...` >= **1**;
   `grep -cF 'role="group"' ...` >= **1**; `grep -cF 'role="alert"' ...` = **0**.
10. `git status --porcelain components/ui | wc -l` = **0**; `git diff -- package.json package-lock.json components.json` vacio.
11. Las dos UAT (visual del Task 1, funcional del Task 2) estan reportadas en el SUMMARY con sus 8 y
    7 escenarios respectivamente, cada uno con su resultado.
12. `git diff --name-only` toca **exactamente** `app/(dashboard)/agenda/agenda-client.tsx`.
</verification>

<success_criteria>
- **AGENDA-05:** el dueno asigna servicios a una franja desde Agenda y la grilla muestra que se da en
  cada una **sin abrir nada**.
- **AGENDA-06:** una franja sin servicios asignados se ve y se lee como **"cualquiera"**.
- El guardado es todo-o-nada y el estado local queda sincronizado con lo que quedo en la base: guardar
  dos veces seguidas no duplica nada y guardar desde una sede no borra la otra.
</success_criteria>

## Artifacts this phase produces

| Artefacto | Plan | Estado al cerrar este plan |
|---|---|---|
| `lib/time-block-services.ts` → `servicesOfBlock`, `isBlockWildcard` | 19-01 | consumido acá |
| `lib/agenda-hours-payload.ts` → `buildSaveHoursPayload`, `buildDayStatesFromRows` | 19-01 | consumido acá |
| `supabase/migrations/074_save_agenda_blocks.sql` → `public.save_agenda_blocks(uuid, jsonb)` | 19-02 | validada en LOCAL; **pendiente en prod (Plan 19-06)** |
| `app/(dashboard)/settings/settings-client.tsx` → 5o count + frase de D-07 | 19-03 | creado |
| `app/[slug]/booking-client.tsx` → rama `service_not_scheduled` | 19-03 | creado |
| `app/(dashboard)/agenda/page.tsx` → props `initialTimeBlockServices` y `serviceCatalog` | 19-04 | creadas |
| `app/(dashboard)/agenda/agenda-client.tsx` → `ServiceCatalogItem`, `service_ids`, `hoursDirty`, toggle de servicio, fila sin stepper | 19-04 | creados |
| `app/(dashboard)/agenda/agenda-client.tsx` → `ServiceChip`, `BlockServicesLine`, `CHIPS_COLLAPSED_MAX`, indicador "Cambios sin guardar", `saveHours` sobre el RPC | **19-05** | creados |
| `supabase/schema.sql` reflejando la 074 | 19-06 | — |

<output>
Create `.planning/workstreams/motor-reservas/phases/19-el-panel/19-05-SUMMARY.md` when done
</output>
