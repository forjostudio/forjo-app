---
phase: 01-turnos-manuales
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/booking-core.ts
  - app/api/booking/create/route.ts
  - test/booking-core.test.ts
  - test/booking-public-regression.test.ts
  - test/helpers/booking-fixtures.ts
autonomous: true
requirements: [MANUAL-01, MANUAL-03]
tags: [booking, refactor, multi-tenant, anti-double-booking, vitest]

must_haves:
  truths:
    - "lib/booking-core.ts exporta createAppointmentCore que re-valida service/professional/location por business_id antes de insertar"
    - "createAppointmentCore inserta el turno y traduce 23505/23P01 a slot_taken (409) — anti-doble-booking intacto, mismo error que el booking público (D-07)"
    - "createAppointmentCore con requireDeposit=false inserta status='confirmed' y expires_at=null"
    - "createAppointmentCore devuelve cancelledHoldIds (no manda mails desde el core)"
    - "app/api/booking/create/route.ts consume createAppointmentCore y mantiene reCAPTCHA, gate de plan, seña, holds-emails y GCal en el caller"
    - "La suite Vitest existente (TEST-01 isolation + webhooks) sigue verde tras el refactor"
  prohibitions:
    - "lib/booking-core.ts NO importa lib/email ni lib/recaptcha ni getBusinessSecrets (señal de que migró lógica que no le corresponde — RESEARCH Pitfall 2)"
    - "lib/booking-core.ts NO crea su propio cliente Supabase (lo recibe por parámetro, rol-agnóstico)"
    - "El refactor del endpoint público NO cambia el comportamiento con seña (status pending_payment + expires_at + mail de seña permanecen en el caller)"
  artifacts:
    - path: "lib/booking-core.ts"
      provides: "Core rol-agnóstico de creación de turno (anti-tampering + re-check solapamiento/buffer + liberación de holds + insert + traducción de constraint)"
      exports: ["createAppointmentCore", "CreateAppointmentInput", "CreateAppointmentResult"]
      min_lines: 90
    - path: "test/booking-core.test.ts"
      provides: "Tests del core: overlap/buffer, anti-tampering, 23505/23P01 -> slot_taken, confirmed directo"
    - path: "test/booking-public-regression.test.ts"
      provides: "No-regresión del endpoint público tras consumir el core (seña/holds intactos)"
  key_links:
    - from: "app/api/booking/create/route.ts"
      to: "lib/booking-core.ts"
      via: "import { createAppointmentCore } from '@/lib/booking-core'"
      pattern: "createAppointmentCore"
---

<objective>
Extraer la cadena de validación + insert de turno de `app/api/booking/create/route.ts` a un helper compartido y rol-agnóstico `lib/booking-core.ts`, y refactorizar el endpoint público para consumirlo SIN regresión. Es el fundamento que el alta manual autenticada (Plan 02) reusa.

Purpose: Una sola fuente de verdad para la validación anti-tampering de tenant, el re-check de solapamiento con buffer, la liberación de holds vencidos, el insert y la traducción atómica de constraint (23505/23P01 -> slot_taken). Cubre MANUAL-01 (pipeline reusable) y MANUAL-03 (respeta disponibilidad real, no sobre-reserva). Es la parte de mayor riesgo de la fase (RESEARCH Pitfall 2): el refactor del endpoint público en producción no puede regresar.

Output: `lib/booking-core.ts` (nuevo), `app/api/booking/create/route.ts` (refactorizado), tests del core + no-regresión del público.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-PATTERNS.md

# Pipeline a extraer (núcleo de la fase)
@app/api/booking/create/route.ts
@test/isolation.test.ts
@test/helpers/supabase-fixtures.ts
@.claude/skills/supabase-multitenant-rls/SKILL.md
</context>

## Artifacts this phase produces (Plan 01 scope)

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `lib/booking-core.ts` | new module | exporta `createAppointmentCore` + tipos `CreateAppointmentInput`, `CreateAppointmentResult` |
| `createAppointmentCore(input)` | async function | rol-agnóstico (recibe el cliente Supabase ya construido); devuelve `{ ok, appointmentId, cancelToken, status, serviceName, durationMinutes, cancelledHoldIds }` o `{ ok:false, error, status }` |
| Error codes (reusados) | strings snake_case | `invalid_service`, `invalid_professional`, `slot_taken` (409), `insert_failed` (500) |
| `app/api/booking/create/route.ts` | refactor | consume el core; mantiene reCAPTCHA/seña/holds-emails/GCal en el caller |
| `test/booking-core.test.ts` | new test | core: overlap/buffer/anti-tampering/23505->slot_taken/confirmed directo |
| `test/booking-public-regression.test.ts` | new test | no-regresión del endpoint público |
| `test/helpers/booking-fixtures.ts` | new helper | siembra business+service+professional+location para los tests del core (molde de `test/helpers/supabase-fixtures.ts`) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extraer createAppointmentCore a lib/booking-core.ts</name>
  <files>lib/booking-core.ts</files>
  <read_first>
    - app/api/booking/create/route.ts (todo el archivo — es la fuente de la extracción; líneas clave 8-14 SENTINEL/timeToMinutes, 81-134 anti-tampering + re-check, 140-177 liberación de holds, 179-241 insert + traducción de error)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-PATTERNS.md (sección "lib/booking-core.ts" con los excerpts exactos a copiar y la firma)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-RESEARCH.md (Pattern 1 con la firma propuesta + Pitfall 2 "qué NO migrar")
    - .claude/skills/supabase-multitenant-rls/SKILL.md (re-validación por business_id en toda entidad)
  </read_first>
  <behavior>
    - Anti-tampering de service: un serviceId que NO es del business -> { ok:false, error:'invalid_service', status:400 }
    - Anti-tampering de service: un service con active=false -> invalid_service (400)
    - Anti-tampering de professional: un professionalId que NO es del business -> invalid_professional (400); professionalId === 'none' o null -> proId=null (bucket SENTINEL)
    - Re-check de solapamiento: un turno que se solapa (considerando buffer) con un confirmed existente del mismo bucket -> { ok:false, error:'slot_taken', status:409 }
    - Insert exitoso con requireDeposit=false -> { ok:true, status:'confirmed' }, fila con expires_at=null
    - Insert que choca con constraint 011 (23505) o 013 (23P01) -> slot_taken (409)
    - El core devuelve cancelledHoldIds con los ids de holds vencidos que liberó, y NO manda mails
  </behavior>
  <action>
    Crear `lib/booking-core.ts` (named export, kebab-case file, runtime Node — NO 'use client'). Definir `const SENTINEL = '00000000-0000-0000-0000-000000000000'` y `function timeToMinutes(t)` copiados de `route.ts:9,11-14`. Declarar los tipos `CreateAppointmentInput` y `CreateAppointmentResult` con la firma de PATTERNS/RESEARCH Pattern 1: el input incluye `supabase: SupabaseClient` (rol-agnóstico, recibido por parámetro), `business: { id, buffer_minutes }`, `serviceId`, `professionalId`, `locationId`, `date`, `time`, `clientId`, `clientName`, `clientPhone`, `clientEmail`, `notes`, y `requireDeposit?`/`depositExpiryHours?`. El result discrimina por `ok`.

    Implementar `createAppointmentCore`: (1) re-valida service por `.eq('id', serviceId).eq('business_id', business.id)` (de aquí sale `duration_minutes` y `name`, nunca del cliente); (2) re-valida professional igual, con el branch `'none'`; (3) re-check de solapamiento con buffer + sentinela (extraer `route.ts:106-134`); (4) liberación de holds vencidos (extraer `route.ts:140-149`) devolviendo `cancelledHoldIds` SIN mandar mails; (5) re-validación de `locationId` por business + fallback al `service.location_id` (extraer `route.ts:197-211`); (6) cálculo de `status`/`expires_at` a partir de `requireDeposit` (false -> `'confirmed'`/`null`; true -> `'pending_payment'`/ahora+`depositExpiryHours`); (7) INSERT con `.select('id, cancel_token')` y traducción `23505`/`23P01` -> `slot_taken` (409), resto -> `insert_failed` (500) con `console.error('[booking-core] insert error:', ...)`.

    NO importar `lib/email`, `lib/recaptcha`, ni `getBusinessSecrets`. NO crear el cliente Supabase adentro (se recibe). El core NO inserta el cliente (`clients`) — recibe `clientId` y los campos ya resueltos por el caller. Mantener los comentarios densos en español del original que explican la concurrencia/constraints.
  </action>
  <acceptance_criteria>
    - source: `lib/booking-core.ts` exporta `export async function createAppointmentCore`
    - source: `lib/booking-core.ts` exporta los tipos `CreateAppointmentInput` y `CreateAppointmentResult`
    - source: `grep -L "lib/email\|lib/recaptcha\|business-secrets" lib/booking-core.ts` confirma que NINGUNO de esos imports está presente
    - source: `lib/booking-core.ts` NO contiene `createClient(` ni `createAdminClient(` (recibe el cliente por parámetro)
    - cli: `npx tsc --noEmit` pasa sin errores nuevos
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`lib/booking-core.ts` existe, exporta `createAppointmentCore` + tipos, no importa email/recaptcha/secrets, no crea su cliente, y typecheck pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests del core (overlap/buffer/anti-tampering/23505) + helper de fixtures</name>
  <files>test/booking-core.test.ts, test/helpers/booking-fixtures.ts</files>
  <read_first>
    - test/isolation.test.ts (molde de test de integración: describe.skipIf(!hasSupabaseCreds), seed en beforeAll, teardown en afterAll, anon-key para aserciones, service-role solo para seed)
    - test/helpers/supabase-fixtures.ts (seedTwoTenants/teardown — molde para el nuevo helper de un solo tenant con service+professional+location+appointment)
    - test/env.ts (hasSupabaseCreds)
    - lib/booking-core.ts (el módulo bajo test — Task 1)
  </read_first>
  <behavior>
    - Test A (anti-tampering): createAppointmentCore con un serviceId de OTRO business -> error 'invalid_service'
    - Test B (slot_taken por re-check JS): sembrar un appointment confirmed; llamar al core sobre un horario solapado (mismo profesional/bucket) -> error 'slot_taken' (409)
    - Test C (confirmed directo): core con requireDeposit=false sobre slot libre -> ok:true, status 'confirmed', y la fila insertada tiene expires_at null
    - Test D (constraint atómica): forzar un insert que viole 011/013 (mismo business+pro+date+time confirmed) -> error 'slot_taken' (mapeo 23505/23P01)
    - Test E (buffer): con buffer_minutes>0, un turno que NO se solapa nominalmente pero cae dentro del buffer -> slot_taken
  </behavior>
  <action>
    Crear `test/helpers/booking-fixtures.ts`: seedOneTenant() que crea (service-role, solo para seed) 1 usuario auth + 1 business (con buffer_minutes) + 1 service activo (duration_minutes) + 1 professional activo + 1 location, y devuelve sus ids + admin para teardown; reusar el patrón de prefijo único `__test_<uuid8>` y `email_confirm:true` de `supabase-fixtures.ts`. teardownOneTenant() con try/finally que borra el business (CASCADE) y el usuario auth.

    Crear `test/booking-core.test.ts` con `describe.skipIf(!hasSupabaseCreds)`. En beforeAll sembrar y construir un cliente Supabase (puede ser el admin del helper, o un anon autenticado — el core es rol-agnóstico; usar el admin para aislar el test del core de la cuestión RLS, que ya cubre isolation.test.ts). Implementar los tests A-E llamando a `createAppointmentCore({ supabase, business, ... })`. Para el test D, insertar primero el appointment ocupante y luego invocar el core con el mismo slot/pro para provocar el choque de constraint. Limpiar appointments creados entre tests si hace falta.
  </action>
  <acceptance_criteria>
    - source: `test/booking-core.test.ts` importa `createAppointmentCore` de `@/lib/booking-core`
    - source: `test/booking-core.test.ts` usa `describe.skipIf(!hasSupabaseCreds)` (no rompe CI sin secrets)
    - behavior: con creds Supabase locales, `npm run test -- booking-core` pasa los 5 casos (A-E)
    - source: `test/helpers/booking-fixtures.ts` usa el prefijo `__test_` y borra con `auth.admin.deleteUser` en `finally`
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- booking-core</automated>
  </verify>
  <done>El core tiene cobertura de overlap, buffer, anti-tampering y traducción de constraint; los tests pasan localmente y se skipean sin creds.</done>
</task>

<task type="auto">
  <name>Task 3: Refactorizar el endpoint público para consumir el core, sin regresión</name>
  <files>app/api/booking/create/route.ts, test/booking-public-regression.test.ts</files>
  <read_first>
    - app/api/booking/create/route.ts (el archivo a refactorizar — identificar qué se queda: reCAPTCHA :74-79, gate de plan :63-65, getBusinessSecrets :69, holds-emails :152-176, mail de seña :271-294, GCal :247-265)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-PATTERNS.md (sección "app/api/booking/create/route.ts (MODIFY)" — lista exacta de líneas que se quedan fuera del core)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-RESEARCH.md (Pitfall 2 — el core devuelve cancelledHoldIds; los mails de holds quedan en el after() del caller público)
    - lib/booking-core.ts (la firma del core — Task 1)
    - test/webhook-deposit.test.ts (molde de test de webhook/seña para la no-regresión)
  </read_first>
  <action>
    En `app/api/booking/create/route.ts`: mantener intactos el parseo del body, el lookup de business por slug, el gate de plan (:63-65), `getBusinessSecrets` (:69), `requireDeposit` (:71) y reCAPTCHA (:74-79). Reemplazar la cadena inline `:81-241` (anti-tampering service/professional/location + re-check + liberación de holds + insert + traducción de error) por una llamada a `createAppointmentCore({ supabase, business, serviceId, professionalId, locationId, date, time, clientId: null, clientName, clientPhone, clientEmail, notes, requireDeposit, depositExpiryHours: Number(business.deposit_expiry_hours) || 1 })`.

    IMPORTANTE — el público SIGUE insertando un cliente nuevo SIEMPRE (no dedupe): mantener el insert de `clients` (:188-192) ANTES de llamar al core y pasar el `client_id` resultante al core (o, si la firma del core no inserta el cliente, dejar el insert del cliente en el caller y pasar su id). El comportamiento público de "siempre cliente nuevo" NO se toca (CONTEXT D-04). Tras `result`, si `!result.ok` devolver `Response.json({ ok:false, error: result.error }, { status: result.status })`. Usar `result.cancelledHoldIds` para manejar los mails de holds vencidos en el `after()` de ESTE archivo (extraer los ids cancelados; el lookup de los datos de cada hold para el mail puede quedar en el caller). Mantener el `after()` de GCal (solo si confirmed) y el `after()` del mail de seña (solo si requireDeposit), sin cambios de comportamiento. La respuesta final mantiene `{ ok:true, appointmentId, cancelToken, requiresPayment }`.

    Crear `test/booking-public-regression.test.ts` (`describe.skipIf(!hasSupabaseCreds)`) que verifique, contra un business fixture con seña, que un POST al pipeline público crea un `pending_payment` con `expires_at` no nulo (la rama de seña sobrevive al refactor), y contra un business sin seña que crea un `confirmed`. Reusar el helper de fixtures. Si testear el route handler completo es inviable sin servidor, testear el contrato del core con `requireDeposit:true` (pending_payment + expires_at) como proxy de la no-regresión de la rama de seña, y dejar explícito en un comentario que TEST-01 (isolation + webhooks) es el guard de la suite existente.
  </action>
  <acceptance_criteria>
    - source: `app/api/booking/create/route.ts` contiene `import { createAppointmentCore } from '@/lib/booking-core'` y una llamada a `createAppointmentCore(`
    - source: `app/api/booking/create/route.ts` SIGUE importando `verifyRecaptcha`, `sendPendingPaymentEmail`, `sendExpiredHoldEmail`, `createCalendarEvent`, `getBusinessSecrets` (la lógica del caller no migró al core)
    - source: el archivo sigue conteniendo el gate de plan (`plan_inactive`) y el branch reCAPTCHA
    - behavior: `npm run test` (suite completa) queda verde — TEST-01 isolation + webhook-deposit + webhook-subscription siguen pasando
    - cli: `npx tsc --noEmit` pasa
  </acceptance_criteria>
  <verify>
    <automated>npm run test && npx tsc --noEmit</automated>
  </verify>
  <done>El endpoint público consume el core; reCAPTCHA/seña/holds-emails/GCal permanecen en el caller; la suite Vitest completa (incl. TEST-01 y webhooks) sigue verde; typecheck pasa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente público (anónimo) -> endpoint /api/booking/create | input no confiable cruza acá; el tenant se resuelve por slug y la conexión usa service role |
| caller -> lib/booking-core.ts | el caller pasa `business` ya resuelto y el cliente Supabase; el core re-valida toda entidad por business_id |
| código JS (re-check) -> Postgres (constraints 011/013) | el re-check en JS es UX; la única defensa atómica ante la carrera son los constraints DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | createAppointmentCore (serviceId/professionalId/locationId del cliente) | mitigate | Re-validar cada id con `.eq('business_id', business.id)` antes de usar; `duration_minutes` sale del service re-validado, nunca del cliente (verify: test A del core) |
| T-01-02 | Tampering | doble-booking por carrera concurrente | mitigate | El core mantiene el re-check JS + el INSERT confía en los constraints 011/013 como respaldo atómico; traducción 23505/23P01 -> slot_taken (verify: test B y D del core) |
| T-01-03 | Information Disclosure | regresión del refactor: core que importe email/recaptcha/secrets | mitigate | El core NO importa lib/email, lib/recaptcha ni getBusinessSecrets; esos quedan en el caller (verify: grep -L de acceptance Task 1) |
| T-01-04 | Tampering | regresión de la rama de seña al extraer el core | mitigate | El core devuelve cancelledHoldIds y NO manda mails; el caller público conserva la lógica de seña/holds; la suite TEST-01 + webhooks debe seguir verde (verify: npm run test en Task 3) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes nuevos (RESEARCH Package Legitimacy Audit = N/A); sin superficie de supply-chain nueva |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores.
- `npm run test` (suite completa) verde — incluye isolation (TEST-01), webhook-deposit, webhook-subscription (no-regresión del refactor) + los nuevos booking-core tests.
- `grep` confirma que `lib/booking-core.ts` no importa email/recaptcha/secrets y no crea su cliente Supabase.
- `app/api/booking/create/route.ts` consume `createAppointmentCore` y conserva reCAPTCHA/seña/holds/GCal.
</verification>

<success_criteria>
- `lib/booking-core.ts` es la única fuente de verdad de la validación+insert de turno, rol-agnóstico, sin lógica de email/recaptcha/secrets.
- El endpoint público quedó refactorizado sin cambio de comportamiento (suite verde).
- Cobertura de tests del core: overlap, buffer, anti-tampering, 23505/23P01 -> slot_taken, confirmed directo.
- MANUAL-01 (pipeline reusable) y MANUAL-03 (respeta disponibilidad real) quedan respaldados por el core + sus tests.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-01-SUMMARY.md` when done
</output>
