---
phase: 01-turnos-manuales
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - app/api/appointments/create/route.ts
  - test/manual-booking.test.ts
autonomous: true
requirements: [MANUAL-01, MANUAL-02, MANUAL-03]
tags: [booking, route-handler, auth, multi-tenant, dedupe, vitest]

must_haves:
  truths:
    - "POST /api/appointments/create devuelve 401 cuando no hay sesión autenticada"
    - "El route handler resuelve el business por owner_id (NO por slug, NO por business_id del cliente)"
    - "El route handler reusa createAppointmentCore con requireDeposit:false -> turno confirmed, expires_at null"
    - "Cliente nuevo con teléfono o email que ya existe en el negocio reusa el cliente existente (dedupe D-04)"
    - "El turno queda asociado vía client_id + copia client_name/client_phone/client_email a la fila (D-05)"
    - "Un slot ocupado devuelve 409 { ok:false, error:'slot_taken' } igual que el booking público"
    - "El evento de Google Calendar se crea best-effort en after() si el negocio tiene google_refresh_token; NO se manda mail"
  prohibitions:
    - "El route handler NO usa createAdminClient() para escribir el turno ni el cliente (corre con la sesión del dueño, anon+RLS)"
    - "El route handler NO maneja seña/pago/reCAPTCHA (D-01/D-02 — manual siempre confirmed, sin captcha)"
    - "El route handler NO confía en business_id ni duration_minutes que vengan del cliente"
    - "El service-role SOLO se usa para leer google_refresh_token vía getBusinessSecrets dentro del after() (excepción acotada), nunca para el insert"
  artifacts:
    - path: "app/api/appointments/create/route.ts"
      provides: "Alta de turno autenticada: auth.getUser + business por owner_id + dedupe cliente + core + GCal en after()"
      exports: ["POST"]
      min_lines: 70
    - path: "test/manual-booking.test.ts"
      provides: "Tests del dedupe (D-04) + alta autenticada confirmed + 401 sin sesión"
  key_links:
    - from: "app/api/appointments/create/route.ts"
      to: "lib/booking-core.ts"
      via: "import { createAppointmentCore } from '@/lib/booking-core'"
      pattern: "createAppointmentCore"
    - from: "app/api/appointments/create/route.ts"
      to: "lib/supabase/server"
      via: "createClient() anon+RLS (sesión del dueño), NO admin"
      pattern: "createClient\\(\\)"
---

<objective>
Crear el route handler autenticado `app/api/appointments/create/route.ts` que el dashboard usa para el alta manual de turnos. Corre con la sesión del dueño (anon key + RLS), resuelve el business por `owner_id`, resuelve/crea el cliente con dedupe (D-04), llama a `createAppointmentCore` (Plan 01) con seña desactivada (D-01), y crea el evento de Google Calendar best-effort en `after()` sin mandar mail.

Purpose: MANUAL-01 (alta desde sesión autenticada reusando el pipeline), MANUAL-02 (elegir/crear cliente con dedupe + asociación por client_id), MANUAL-03 (respeta disponibilidad real vía el core). Cierra el gap del alta client-side actual de `appointments-client.tsx` (que confía en el cliente y salta anti-tampering/buffer/holds/GCal).

Output: `app/api/appointments/create/route.ts` (nuevo) + `test/manual-booking.test.ts`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-PATTERNS.md

# Analogs del route handler autenticado
@app/api/booking/create/route.ts
@app/(dashboard)/clients/page.tsx
@lib/booking-core.ts
@lib/business-secrets.ts
@.claude/skills/supabase-multitenant-rls/SKILL.md
</context>

## Artifacts this phase produces (Plan 02 scope)

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `app/api/appointments/create/route.ts` | new route handler | `export async function POST(request)` — runtime Node |
| Error codes | strings snake_case | `unauthorized` (401), `not_found` (404), `bad_request`/`missing_fields` (400), reusa `slot_taken` (409) / `invalid_service` / `invalid_professional` / `insert_failed` del core |
| `test/manual-booking.test.ts` | new test | dedupe (D-04), alta confirmed, 401 sin sesión |

<tasks>

<task type="auto">
  <name>Task 1: Route handler autenticado app/api/appointments/create/route.ts</name>
  <files>app/api/appointments/create/route.ts</files>
  <read_first>
    - app/(dashboard)/clients/page.tsx (líneas 5-32 — patrón auth.getUser + business por owner_id + carga de clients por business_id)
    - app/api/booking/create/route.ts (parseo defensivo del body :22-43, estructura del after() de GCal :247-265, getBusinessSecrets :69)
    - lib/booking-core.ts (firma de createAppointmentCore + CreateAppointmentInput — Plan 01)
    - lib/business-secrets.ts (getBusinessSecrets server-only, service-role, para el token de Google)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-PATTERNS.md (sección "app/api/appointments/create/route.ts" — imports, auth+tenant, defensive parse, core call, GCal after)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-RESEARCH.md (Pattern 2 + Pattern 3 dedupe + anti-pattern "no usar service role en el alta manual")
    - .claude/skills/supabase-multitenant-rls/SKILL.md
  </read_first>
  <action>
    Crear `app/api/appointments/create/route.ts` con `export async function POST(request: Request)` (runtime Node por defecto). Imports: `after` de `next/server`, `createClient` de `@/lib/supabase/server` (anon+RLS — NO admin), `getBusinessSecrets`, `createCalendarEvent`, `createAppointmentCore`.

    Secuencia: (1) `const supabase = await createClient()`; `const { data:{ user } } = await supabase.auth.getUser()`; si no hay user -> `Response.json({ ok:false, error:'unauthorized' }, { status:401 })`. (2) business por `owner_id` (`select('id, name, address, buffer_minutes').eq('owner_id', user.id).single()`); si no hay -> 404 `not_found`. (3) parseo defensivo del body (molde `route.ts:22-43`): `try { raw = await request.json() } catch { return 400 'bad_request' }`, narrowing `typeof body.x === 'string' ? ... : default` para serviceId, professionalId, locationId, date, time, clientId, clientName, clientPhone, clientEmail, notes. Validar requeridos (clientName + serviceId + date + time) -> 400 `missing_fields` si faltan.

    (4) Resolver/crear cliente con dedupe (D-04, ver Task 2 para el helper de dedupe; en este task implementarlo inline o llamar a un helper local): si llegó `clientId`, validarlo por `.eq('id', clientId).eq('business_id', business.id)` y usarlo. Si llegaron datos nuevos, normalizar (email lowercase, teléfono solo dígitos) y buscar match dentro del business (`(business_id + phone normalizado)` o `(business_id + email lowercase)`); si hay match, reusar ese client_id; si no, INSERT del cliente nuevo via la sesión (anon+RLS) y usar su id. El insert/select de `clients` corre con `supabase` (NO admin).

    (5) Llamar `createAppointmentCore({ supabase, business, serviceId, professionalId, locationId, date, time, clientId: resolvedClientId, clientName, clientPhone, clientEmail, notes, requireDeposit: false })`. Si `!result.ok` -> `Response.json({ ok:false, error: result.error }, { status: result.status })`.

    (6) GCal best-effort en `after()` (copiar estructura de `route.ts:247-265`): leer `getBusinessSecrets(business.id)` (única excepción de service-role permitida — lectura de un secreto del propio tenant ya resuelto), y si hay `google_refresh_token` crear el evento y guardar `google_event_id` con `supabase.from('appointments').update(...).eq('id', appointmentId)`. NO mandar mail (D-01). Envolver en try/catch con `console.error('[appointments/create] gcal FALLÓ ...')`.

    (7) `return Response.json({ ok:true, appointmentId: result.appointmentId })`. Errores siempre `{ ok:false, error:'<snake>' }`.
  </action>
  <acceptance_criteria>
    - source: `app/api/appointments/create/route.ts` exporta `export async function POST`
    - source: importa `createClient` de `@/lib/supabase/server` y NO importa `createAdminClient` para el insert
    - source: contiene `auth.getUser()` y devuelve `unauthorized` con status 401
    - source: resuelve el business con `.eq('owner_id', user.id)` (no por slug)
    - source: llama a `createAppointmentCore` con `requireDeposit: false`
    - source: NO importa `verifyRecaptcha` ni `sendPendingPaymentEmail` (sin captcha ni mail — D-01/D-02)
    - source: el único uso de `getBusinessSecrets`/service-role está dentro del `after()` para el token de Google
    - cli: `npx tsc --noEmit` pasa
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>El route handler autenticado existe, valida sesión (401), resuelve tenant por owner_id, dedupea el cliente, reusa el core con seña off, crea GCal en after() sin mail, y typecheck pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests del dedupe (D-04) + alta confirmed + 401 sin sesión</name>
  <files>test/manual-booking.test.ts</files>
  <read_first>
    - test/isolation.test.ts (molde: describe.skipIf(!hasSupabaseCreds), anon-key autenticado como dueño para aserciones, service-role solo para seed)
    - test/helpers/supabase-fixtures.ts y test/helpers/booking-fixtures.ts (helper de seed — el de Plan 01 si ya cubre service+professional+location; si no, extenderlo)
    - app/api/appointments/create/route.ts (el handler bajo test — Task 1)
  </read_first>
  <behavior>
    - Test 401: invocar la lógica de alta sin sesión (anon sin signIn, o probar el contrato del handler) -> debe denegar / no insertar
    - Test dedupe por teléfono: con un cliente existente de teléfono "11 2233-4455", crear un turno con cliente nuevo cuyo teléfono normaliza al mismo valor -> NO se crea un cliente duplicado; el turno queda asociado al cliente existente
    - Test dedupe por email: idem con email en distinta capitalización -> reusa el cliente existente
    - Test sin match: cliente nuevo con teléfono/email que no existe -> se crea un cliente nuevo y queda asociado
    - Test confirmed: el alta manual sobre slot libre crea un appointment con status 'confirmed' y expires_at null (D-01)
  </behavior>
  <action>
    Crear `test/manual-booking.test.ts` (`describe.skipIf(!hasSupabaseCreds)`). Sembrar un tenant (reusar `booking-fixtures.ts` de Plan 01; si le falta el `clients` previo para el dedupe, sembrar un cliente existente con teléfono+email conocidos). Construir un cliente anon-key autenticado como el dueño (patrón `isolation.test.ts` beforeAll). Si testear el route handler HTTP end-to-end es inviable sin servidor, extraer la lógica de dedupe a una función pura testeable o testear la secuencia (resolver cliente -> core) replicando lo que hace el handler con el `supabase` autenticado. Asertar: normalización de teléfono (solo dígitos) y email (lowercase) coincide -> reusa id; sin match -> id nuevo; el appointment final tiene `client_id` apuntando al cliente correcto y `status='confirmed'`/`expires_at` null. Para el caso 401, asertar que sin sesión el insert anon a `appointments` no escribe un turno del business (RLS) o que el contrato del handler devuelve unauthorized. Limpiar con teardown.
  </action>
  <acceptance_criteria>
    - source: `test/manual-booking.test.ts` usa `describe.skipIf(!hasSupabaseCreds)`
    - behavior: con creds locales, `npm run test -- manual-booking` pasa los casos de dedupe (teléfono + email + sin-match), confirmed directo, y denegación sin sesión
    - source: las aserciones de dedupe usan el cliente autenticado del dueño (anon+RLS), no el service-role
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- manual-booking</automated>
  </verify>
  <done>El dedupe (D-04) está cubierto por teléfono y email, el alta confirmed y la denegación sin sesión también; los tests pasan localmente y se skipean sin creds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser del dueño -> /api/appointments/create | input no confiable; el actor es el dueño autenticado (cookie de sesión) |
| sesión del dueño -> Postgres | conexión anon+RLS; RLS + filtro por business_id garantizan que solo escriba en SU negocio |
| after() -> getBusinessSecrets (service role) | única lectura privilegiada permitida: el google_refresh_token del propio tenant ya resuelto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Spoofing / Elevation | endpoint sin auth | mitigate | `auth.getUser()` -> 401 si no hay sesión; el proxy ya cubre /api/* con refresh de sesión (verify: test 401 + acceptance source) |
| T-01-06 | Tampering / Elevation | cross-tenant write (insertar turno/cliente en negocio ajeno) | mitigate | business resuelto por `owner_id` (no por slug ni por business_id del cliente) + RLS FOR ALL USING + WITH CHECK (migración 040, Plan 03) + filtro `.eq('business_id', business.id)` en cliente/IDs (verify: isolation.test.ts cross-write sigue verde) |
| T-01-07 | Tampering | ID tampering (service/professional/location de otro negocio) | mitigate | El core re-valida cada id por business_id (Plan 01 T-01-01); el handler valida también el clientId por business_id antes de usarlo |
| T-01-08 | Information Disclosure | service-role leak al cliente | mitigate | El alta NO usa createAdminClient; getBusinessSecrets corre server-only dentro del after(); el token nunca sale del server (verify: acceptance source — único uso en after()) |
| T-01-09 | Tampering | doble-booking por carrera | mitigate | Reusa el core (re-check JS + constraints 011/013 atómicos); mismo slot_taken (409) que el público (verify: core tests B/D del Plan 01) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Sin paquetes nuevos en esta fase (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores.
- `npm run test -- manual-booking` verde (dedupe + confirmed + 401).
- `npm run test` (suite completa) sigue verde — isolation.test.ts cross-write/cross-insert no regresiona.
- `grep` confirma: el handler no usa `createAdminClient` para el insert; sin `verifyRecaptcha`/`sendPendingPaymentEmail`.
</verification>

<success_criteria>
- Existe un endpoint autenticado de alta de turno que corre con la sesión del dueño, reusa el core, y aplica dedupe de cliente.
- MANUAL-01/02/03 quedan implementados server-side; el camino manual no abre bypass del re-check de disponibilidad.
- Cero uso de service-role para escribir; única excepción documentada = lectura del token de Google en after().
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-02-SUMMARY.md` when done
</output>
