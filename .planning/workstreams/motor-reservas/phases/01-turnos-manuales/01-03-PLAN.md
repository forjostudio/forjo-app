---
phase: 01-turnos-manuales
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/040-appointments-clients-insert-with-check.sql
  - supabase/schema.sql
  - .planning/workstreams/motor-reservas/REQUIREMENTS.md
autonomous: false
requirements: [MANUAL-01, MANUAL-04]
tags: [migration, rls, supabase, multi-tenant, hardening, traceability]

must_haves:
  truths:
    - "Existe supabase/migrations/040-*.sql con FOR INSERT WITH CHECK (business_id = negocios del dueño) para appointments y clients"
    - "La migración 040 sigue el patrón exacto de fixed_expenses (baseline:1272-1274)"
    - "supabase db reset aplica el baseline + 040 limpio (validación local sobre PG17)"
    - "Un INSERT cross-tenant autenticado (dueño A insertando en business de B) sigue fallando tras 040"
    - "REQUIREMENTS.md Traceability marca MANUAL-04 como Deferred / fuera de Phase 1, citando D-01"
  prohibitions:
    - "La migración 040 NO modifica los constraints 011/013 ni la policy 'business member access' existente (solo AGREGA policies FOR INSERT)"
    - "La migración 040 NO se aplica a producción automáticamente (prod se aplica a mano, coordinado con el deploy)"
    - "El alta manual NO renderiza ningún control de seña ni inserta status pending_payment (MANUAL-04 diferido por D-01)"
  artifacts:
    - path: "supabase/migrations/040-appointments-clients-insert-with-check.sql"
      provides: "Policies FOR INSERT WITH CHECK explícitas para appointments y clients (hardening defensa-en-profundidad)"
      contains: "FOR INSERT WITH CHECK"
    - path: "supabase/schema.sql"
      provides: "Schema regenerado reflejando las nuevas policies (si el repo mantiene este artefacto)"
  key_links:
    - from: "supabase/migrations/040-appointments-clients-insert-with-check.sql"
      to: "supabase/migrations/00000000000000_baseline.sql"
      via: "patrón fixed_expenses FOR INSERT WITH CHECK (baseline:1272)"
      pattern: "FOR INSERT WITH CHECK"
---

<objective>
Agregar la migración `040` que formaliza con `FOR INSERT ... WITH CHECK (business_id = negocios del dueño)` el INSERT autenticado del dueño sobre `appointments` y `clients` (defensa en profundidad pedida por la skill de RLS), validarla localmente con `supabase db reset`, regenerar `supabase/schema.sql`, y actualizar la Traceability de REQUIREMENTS.md para marcar MANUAL-04 como diferido a v2 (D-01).

Purpose: Cierre del aislamiento por tenant para el alta manual (MANUAL-01, defensa-en-profundidad) + cobertura-como-diferido de MANUAL-04. NOTA (RESEARCH Pitfall 1): el INSERT del dueño YA funciona tenant-safe hoy porque la policy `FOR ALL USING(...)` reusa el USING como check de filas nuevas; la migración 040 es hardening de claridad, NO un bloqueante funcional. El valor crítico de este plan es la VALIDACIÓN LOCAL ([BLOCKING] task) que cierra el falso-positivo de verificación (build/types pasan sin la migración aplicada porque los tipos salen de config, no de la DB viva).

Output: `supabase/migrations/040-appointments-clients-insert-with-check.sql` (nuevo), `supabase/schema.sql` (regenerado), `REQUIREMENTS.md` (Traceability actualizada).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-PATTERNS.md
@.planning/workstreams/motor-reservas/REQUIREMENTS.md

# Patrón de policy + estado actual de las tablas
@.claude/skills/supabase-multitenant-rls/SKILL.md
# baseline: fixed_expenses FOR INSERT WITH CHECK (líneas 1272-1274); policies appointments/clients (1203, 1215)
</context>

## Artifacts this phase produces (Plan 03 scope)

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `supabase/migrations/040-appointments-clients-insert-with-check.sql` | new migration | 2 policies `FOR INSERT WITH CHECK` (appointments + clients), patrón fixed_expenses |
| `supabase/schema.sql` | regenerated | refleja las nuevas policies (solo si el repo mantiene este artefacto sincronizado) |
| REQUIREMENTS.md Traceability row | edit | MANUAL-04 -> Deferred (v2), citando D-01 |

<tasks>

<task type="auto">
  <name>Task 1: Escribir migración 040 (FOR INSERT WITH CHECK para appointments y clients)</name>
  <files>supabase/migrations/040-appointments-clients-insert-with-check.sql</files>
  <read_first>
    - supabase/migrations/00000000000000_baseline.sql (líneas 1203-1217: policy "business member access" actual de appointments y clients; líneas 1266-1288: bloque fixed_expenses con FOR DELETE/INSERT/SELECT/UPDATE — el patrón EXACTO a copiar para el INSERT WITH CHECK)
    - .claude/skills/supabase-multitenant-rls/SKILL.md (reglas de WITH CHECK en INSERT, defensa en profundidad)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-PATTERNS.md (sección "supabase/migrations/040-*.sql" con el bloque fixed_expenses citado y la nota de permissive policies)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-RESEARCH.md (Pitfall 1 — la migración es hardening, no funcional; Open Question 3)
  </read_first>
  <action>
    Crear `supabase/migrations/040-appointments-clients-insert-with-check.sql`. Numeración 040 (primera migración sobre el baseline replayable, per MEMORY infra-testing-roadmap). Agregar DOS policies nuevas, una por tabla, copiando literal el patrón de `fixed_expenses tenant insert` (baseline:1272-1274), adaptando el nombre de policy y la tabla:

    Policy 1 — `appointments`: `CREATE POLICY "appointments tenant insert" ON "public"."appointments" FOR INSERT WITH CHECK (("business_id" IN ( SELECT "businesses"."id" FROM "public"."businesses" WHERE ("businesses"."owner_id" = ( SELECT "auth"."uid"() AS "uid")))));`

    Policy 2 — `clients`: misma forma con `"clients tenant insert"` sobre `"public"."clients"`.

    Encabezar el archivo con un comentario SQL en español que explique: que es hardening de defensa-en-profundidad (la policy `FOR ALL USING` ya cubría el INSERT por la semántica de Postgres), que NO modifica la policy existente ni los constraints 011/013, y que es una policy PERMISSIVE adicional (se OR-ea con la existente; ambas expresan la misma regla de tenant). NO escribir DROP de la policy existente. NO tocar appointments_no_double_booking (011) ni appointments_no_overlap (013).
  </action>
  <acceptance_criteria>
    - source: el archivo existe en `supabase/migrations/` con prefijo `040`
    - source: contiene `CREATE POLICY "appointments tenant insert" ... FOR INSERT WITH CHECK` y `CREATE POLICY "clients tenant insert" ... FOR INSERT WITH CHECK`
    - source: NO contiene `DROP POLICY`, NO contiene `appointments_no_double_booking`, NO contiene `appointments_no_overlap`, NO contiene `ALTER ... CONSTRAINT`
    - source: el WITH CHECK usa `businesses.owner_id = auth.uid()` (mismo subselect que fixed_expenses)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "FOR INSERT WITH CHECK" supabase/migrations/040-appointments-clients-insert-with-check.sql</automated>
  </verify>
  <done>La migración 040 agrega las 2 policies FOR INSERT WITH CHECK siguiendo fixed_expenses, sin tocar policies existentes ni constraints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Validación local de la migración (supabase db reset) + regen schema.sql</name>
  <what-built>
    La migración 040 (Task 1). Esta validación replaya el baseline + 040 sobre el Postgres LOCAL (PG17) y confirma que aplica limpio, y regenera `supabase/schema.sql`. Es obligatoria: build y typecheck pasan SIN la migración aplicada (los tipos salen de config, no de la DB viva), lo que produciría una verificación de falso-positivo. El reset local es el guard del proyecto contra eso (MEMORY infra-testing-roadmap).
  </what-built>
  <read_first>
    - supabase/migrations/040-appointments-clients-insert-with-check.sql (la migración a validar — Task 1)
    - supabase/config.toml (config del Supabase CLI local; analytics/studio/storage off en Windows per MEMORY)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-PATTERNS.md (nota: verificar con supabase db reset + cross-tenant insert que debe fallar)
  </read_first>
  <how-to-verify>
    Requiere Docker / Supabase CLI local (interactivo en Windows). Pasos:
    1. Levantar el stack local si no está: `supabase start` (o confirmar que ya corre).
    2. Correr `supabase db reset` — replaya el baseline (00000000000000) + la migración 040. Debe terminar SIN error (output limpio, "Finished supabase db reset" / sin "ERROR:" de SQL).
    3. Regenerar el dump del schema: `supabase db dump --local -f supabase/schema.sql` (o el comando que el repo use para mantener schema.sql; si el repo NO mantiene schema.sql sincronizado tras el baseline replayable, omitir este paso y dejarlo registrado en el SUMMARY).
    4. (Recomendado) Verificación cross-tenant manual: con un cliente autenticado como dueño A, intentar `INSERT INTO appointments(business_id, ...)` con el business_id de B -> debe FALLAR por RLS/WITH CHECK. Esto confirma que 040 no abrió escritura cross-tenant.
    5. NO aplicar a producción (prod se aplica a mano, coordinado con el deploy — constraint del proyecto).
  </how-to-verify>
  <action>Checkpoint humano: correr `supabase db reset` localmente para replayar baseline + migración 040, confirmar aplicación limpia, regenerar `supabase/schema.sql` (si el repo lo mantiene) y verificar manualmente que un INSERT cross-tenant autenticado falla. NO aplicar a producción.</action>
  <acceptance_criteria>
    - cli: `supabase db reset` aplica el baseline + 040 sin error de SQL (output limpio)
    - source: si el repo mantiene schema.sql, `supabase/schema.sql` quedó regenerado reflejando las 2 policies nuevas; si no lo mantiene, queda documentado en el SUMMARY
    - behavior: un INSERT cross-tenant autenticado (dueño A en business de B) falla tras 040 (sin regresión de aislamiento)
  </acceptance_criteria>
  <resume-signal>Escribí "approved" cuando `supabase db reset` aplicó limpio y (si aplica) schema.sql quedó regenerado; o describí el error del reset.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Marcar MANUAL-04 como diferido en la Traceability de REQUIREMENTS.md</name>
  <files>.planning/workstreams/motor-reservas/REQUIREMENTS.md</files>
  <read_first>
    - .planning/workstreams/motor-reservas/REQUIREMENTS.md (sección Traceability líneas 61-85 — la fila MANUAL-04 y la tabla Phase -> Requirements)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-CONTEXT.md (D-01 + bloque <deferred> — la autoridad de la decisión)
  </read_first>
  <action>
    Editar SOLO las filas relevantes de REQUIREMENTS.md (Edit scoped, NO reescribir el archivo): en la tabla Traceability cambiar la fila `| MANUAL-04 | Phase 1 | Pending |` por `| MANUAL-04 | Deferred (v2) | Out of Phase 1 |` (o equivalente que deje claro que sale de Phase 1), y agregar una nota al pie de la tabla o en la sección citando: "MANUAL-04 diferido a v2 por decisión D-01 (CONTEXT de Phase 1, bloque <deferred>): el alta manual no maneja seña; el turno siempre queda confirmed. Reconsiderar en un milestone futuro." Actualizar también la fila de la tabla "Phase -> Requirements" de Phase 1 si conviene reflejar el count (de 4 a 3 activos + 1 diferido), dejando MANUAL-04 marcado como diferido para no romper el conteo de cobertura. NO tocar las filas de Phase 2/3.
  </action>
  <acceptance_criteria>
    - source: REQUIREMENTS.md ya NO muestra `MANUAL-04 | Phase 1 | Pending`
    - source: REQUIREMENTS.md cita la decisión D-01 al diferir MANUAL-04
    - source: las filas de CUPOS/ESPACIO/CONC quedan intactas (Edit scoped)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "MANUAL-04" .planning/workstreams/motor-reservas/REQUIREMENTS.md</automated>
  </verify>
  <done>La Traceability refleja MANUAL-04 como diferido a v2 citando D-01; el resto del documento intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sesión del dueño (anon+RLS) -> appointments/clients INSERT | la policy WITH CHECK valida que la fila nueva pertenezca a un negocio del dueño |
| migración local -> migración producción | la validación corre en PG17 local; prod se aplica a mano (nunca auto) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Elevation / Tampering | INSERT cross-tenant en appointments/clients | mitigate | Policy `FOR INSERT WITH CHECK (business_id = negocios del dueño)` explícita (040) + la `FOR ALL USING` existente; ambas permissive, misma regla (verify: cross-tenant insert falla en el reset local) |
| T-01-11 | Tampering | migración debilita constraints 011/013 o la policy existente | mitigate | 040 solo AGREGA policies FOR INSERT; no toca constraints ni la policy USING (verify: grep -L de DROP/constraint en acceptance Task 1) |
| T-01-12 | Repudiation / falso-positivo de verificación | build pasa sin la migración aplicada (tipos de config, no de DB viva) | mitigate | [BLOCKING] supabase db reset local replaya 040 y confirma aplicación limpia + aislamiento cross-tenant (Task 2) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Sin paquetes nuevos en esta fase (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `supabase db reset` aplica baseline + 040 limpio (PG17 local).
- INSERT cross-tenant autenticado falla tras 040 (sin regresión de aislamiento).
- `supabase/schema.sql` regenerado (si el repo lo mantiene) o decisión documentada.
- REQUIREMENTS.md Traceability marca MANUAL-04 diferido a v2 citando D-01.
</verification>

<success_criteria>
- El INSERT del dueño sobre appointments/clients tiene una policy WITH CHECK explícita (hardening), sin regresión de aislamiento.
- La migración fue validada localmente (no se confía en que build/types pasen como prueba de la DB).
- MANUAL-04 queda cubierto-como-diferido en la Traceability, citando la decisión D-01.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-03-SUMMARY.md` when done
</output>
