---
phase: 01-turnos-manuales
plan: 04
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - components/dashboard/nuevo-turno-form.tsx
  - app/(dashboard)/appointments/appointments-client.tsx
  - app/(dashboard)/appointments/page.tsx
  - app/(dashboard)/agenda/agenda-client.tsx
  - app/(dashboard)/agenda/page.tsx
autonomous: false
requirements: [MANUAL-01, MANUAL-02, MANUAL-03]
tags: [ui, dashboard, modal, drawer, combobox, agenda, appointments]

must_haves:
  truths:
    - "components/dashboard/nuevo-turno-form.tsx renderiza Dialog en desktop (>=768px) y Drawer (vaul) en mobile (<768px)"
    - "El form tiene un combobox de clientes que filtra en memoria los clients del negocio + opción crear nuevo inline (D-03)"
    - "El form hace fetch POST a /api/appointments/create (NO insert directo a supabase)"
    - "Tras crear con éxito: toast 'Turno agregado' + cierre + router.refresh()"
    - "Botón 'Nuevo turno' presente en Agenda y en Turnos (D-08)"
    - "Click en un día del resumen semanal de la Agenda pre-llena la FECHA del form (D-08 acotado)"
    - "appointments-client usa el form compartido vía fetch (reemplaza el handleCreate client-side directo)"
    - "agenda/page.tsx y appointments/page.tsx cargan clients/services/professionals/locations por business_id para el form"
  prohibitions:
    - "El form NO renderiza ningún control de seña / link de pago / toggle de seña (MANUAL-04 diferido por D-01)"
    - "El form NO hace supabase.from('appointments').insert() directo (todo va por el endpoint del Plan 02)"
    - "NO se construye una grilla hora x dia clickeable en la Agenda (D-08 acotado: solo click-en-día pre-llena fecha — RESEARCH Pitfall 3)"
    - "NO se agregan dependencias nuevas (combobox = Input + filtro en memoria; command.tsx NO existe; drawer/dialog ya vendoreados)"
  artifacts:
    - path: "components/dashboard/nuevo-turno-form.tsx"
      provides: "Form compartido modal/drawer de alta de turno con combobox de cliente + crear-inline"
      min_lines: 120
    - path: "app/(dashboard)/appointments/page.tsx"
      provides: "Carga de clients (+ locations) para el form"
      contains: "clients"
    - path: "app/(dashboard)/agenda/page.tsx"
      provides: "Carga de services/professionals/clients/locations para el form"
      contains: "professionals"
  key_links:
    - from: "components/dashboard/nuevo-turno-form.tsx"
      to: "app/api/appointments/create/route.ts"
      via: "fetch('/api/appointments/create', { method: 'POST' })"
      pattern: "fetch\\(['\\\"]/api/appointments/create"
    - from: "app/(dashboard)/agenda/agenda-client.tsx"
      to: "components/dashboard/nuevo-turno-form.tsx"
      via: "render del NuevoTurnoForm + click-en-día pre-llena fecha"
      pattern: "NuevoTurnoForm"
    - from: "app/(dashboard)/appointments/appointments-client.tsx"
      to: "components/dashboard/nuevo-turno-form.tsx"
      via: "reemplazo del modal inline por el form compartido (fetch al endpoint)"
      pattern: "NuevoTurnoForm"
---

<objective>
Construir el form compartido `components/dashboard/nuevo-turno-form.tsx` (modal en desktop / drawer en mobile, con combobox de clientes + crear-nuevo inline) y cablearlo en las dos superficies del dashboard: Turnos (`appointments-client.tsx`, reemplazando el insert client-side directo por el fetch al endpoint del Plan 02) y Agenda (`agenda-client.tsx`, con botón "Nuevo turno" + click-en-día que pre-llena la fecha). Cargar en ambos `page.tsx` los datos que el form necesita.

Purpose: Superficie UI de MANUAL-01/02/03 (D-03 combobox, D-08 disparos, D-09 responsive). Cierra el bug del alta actual: el alta pasa a correr el pipeline server-side completo vía el endpoint autenticado en vez del insert directo desde el browser.

Output: `components/dashboard/nuevo-turno-form.tsx` (nuevo) + edits a appointments-client/page y agenda-client/page.

Scope reduction note (LOCKED): D-08 se ACOTA — la Agenda NO tiene grilla hora x dia (RESEARCH Pitfall 3). "Click en slot" se reinterpreta como "click en un DÍA del resumen semanal pre-llena la FECHA" (no la hora). NO construir la grilla horaria (mejora futura, fuera de alcance). El UI-SPEC menciona "empty grid slot click-to-prefill (hour)"; en esta fase se reduce a click-en-día -> fecha.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-UI-SPEC.md

# Analogs de UI
@app/(dashboard)/appointments/appointments-client.tsx
@app/(dashboard)/appointments/page.tsx
@app/(dashboard)/agenda/agenda-client.tsx
@app/(dashboard)/agenda/page.tsx
@app/(dashboard)/clients/clients-client.tsx
@components/ui/drawer.tsx
@components/ui/dialog.tsx
</context>

## Artifacts this phase produces (Plan 04 scope)

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `components/dashboard/nuevo-turno-form.tsx` | new component | export `NuevoTurnoForm` ('use client'); modal/drawer responsive + combobox + crear-inline + fetch al endpoint |
| client-side handler | function | submit hace `fetch('/api/appointments/create')` + toast + `router.refresh()` (reemplaza el insert directo) |
| `app/(dashboard)/appointments/page.tsx` | edit | agrega carga de `clients` (y `locations` si el form los usa) por business_id |
| `app/(dashboard)/agenda/page.tsx` | edit | agrega carga de `services`/`professionals`/`clients`/`locations` por business_id |
| `app/(dashboard)/appointments/appointments-client.tsx` | edit | usa el form compartido vía fetch; conserva la traducción 23505/23P01 -> toast |
| `app/(dashboard)/agenda/agenda-client.tsx` | edit | botón "Nuevo turno" + click-en-día pre-llena fecha |

<tasks>

<task type="auto">
  <name>Task 1: Componente compartido NuevoTurnoForm (modal/drawer + combobox + fetch)</name>
  <files>components/dashboard/nuevo-turno-form.tsx</files>
  <read_first>
    - app/(dashboard)/appointments/appointments-client.tsx (líneas 545-639: markup del modal actual a reproducir visualmente — grid-cols-2 gap-3, Label+Input/Select, space-y-3 mt-2; líneas 288-330: el handleCreate que se reemplaza por fetch)
    - app/(dashboard)/clients/clients-client.tsx (líneas 176-191: filtro/detección de duplicados en memoria — email lowercase, teléfono solo dígitos — molde del combobox y del dedupe optimista)
    - components/ui/drawer.tsx y components/ui/dialog.tsx (las primitivas responsive — D-09)
    - app/(dashboard)/agenda/agenda-client.tsx (líneas 83-86: patrón fetch + toast + router.refresh)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-UI-SPEC.md (Copywriting Contract: todos los textos en español; Interaction States; Color/Typography/Spacing; touch targets 44px; labels siempre visibles)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-PATTERNS.md (sección "components/dashboard/nuevo-turno-form.tsx": shell responsive, combobox Input+filter, dedupe normalization, submit por fetch)
  </read_first>
  <action>
    Crear `components/dashboard/nuevo-turno-form.tsx` ('use client'). Export `NuevoTurnoForm` con props: `open`, `onOpenChange`, `clients`, `services`, `professionals`, `locations` (tipados con los tipos de `@/lib/types`), `prefill?: { date?: string; professionalId?: string }`, y un callback `onCreated?` (o usar `router.refresh()` internamente). Shell responsive (D-09): usar `Dialog` (`@/components/ui/dialog`) en `>=768px` y `Drawer` (`@/components/ui/drawer`, vaul) en `<768px`; detectar con una media query / hook simple (o renderizar ambos y mostrar según breakpoint con clases — seguir el patrón del repo si existe; si no, un hook `useMediaQuery`-style mínimo sin dependencias).

    Campos (reproducir el layout exacto del modal actual `appointments-client.tsx:551-633`, sin el control de seña): Cliente (combobox), Servicio (Select), Profesional (Select, opción "Sin preferencia"), Consultorio/location si el negocio tiene locations, Fecha (`Input type="date"`), Hora. IMPORTANTE (D-06): la hora se ingresa libre (`Input type="time"` o select de slots), permitiendo cualquier hora — incluir el texto auxiliar del UI-SPEC: "Podés agendar a cualquier hora libre, aunque esté fuera de tu horario de atención." NO restringir a los slots de `/api/booking/availability`.

    Combobox de cliente (D-03/D-04, command.tsx NO existe): `Input` de búsqueda + lista filtrada en memoria sobre `clients` (ya cargados por business_id), filtrando por nombre/teléfono/email. Placeholder "Buscá un cliente o creá uno nuevo". Empty state "Sin clientes que coincidan" + acción "Crear nuevo cliente". Rama crear-nuevo inline: campos "Nombre" (requerido) + "Teléfono o email" (al menos uno). Dedupe optimista en UI (normalizar email lowercase / teléfono solo dígitos como `clients-client.tsx:180-181` y mostrar "Ya tenés un cliente con ese teléfono: {nombre}. ¿Usar el existente?" + "Usar existente"); la autoridad del dedupe es el server (Plan 02).

    Submit: `fetch('/api/appointments/create', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ clientId, clientName, clientPhone, clientEmail, serviceId, professionalId, locationId, date, time, notes }) })`. Botón "Agregar turno" con estado loading "Agregando..." y disabled (anti doble-submit). Mapear errores del endpoint a los toasts del UI-SPEC: `slot_taken` -> "Ese horario ya está ocupado. Elegí otro.", `invalid_service`/`invalid_professional` -> "Revisá el servicio o el profesional seleccionado.", `missing_fields` -> "Completá el cliente, el servicio y el horario.", `insert_failed` -> "No se pudo guardar el turno. Probá de nuevo.". Éxito -> `toast.success('Turno agregado')` + `onOpenChange(false)` + `router.refresh()`. NO renderizar control de seña (D-01). NO hacer insert directo a supabase.
  </action>
  <acceptance_criteria>
    - source: `components/dashboard/nuevo-turno-form.tsx` exporta `NuevoTurnoForm` y empieza con `'use client'`
    - source: importa `Drawer` de `@/components/ui/drawer` y `Dialog` de `@/components/ui/dialog`
    - source: contiene `fetch('/api/appointments/create'` con method POST
    - source: NO contiene `supabase.from('appointments').insert` ni `.from('clients').insert` (todo por el endpoint)
    - source: NO contiene strings/controles de seña (grep -i "seña\|deposit\|pending_payment" no matchea controles de UI)
    - source: NO importa `@/components/ui/command`
    - source: copy en español del UI-SPEC presente ("Agregar turno", "Buscá un cliente o creá uno nuevo", "Turno agregado")
    - cli: `npx tsc --noEmit` pasa
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>El form compartido existe, es responsive (dialog/drawer), tiene combobox Input+filtro + crear-inline, hace fetch al endpoint, sin control de seña ni insert directo; typecheck pasa.</done>
</task>

<task type="auto">
  <name>Task 2: Cablear el form en Turnos (appointments) — reemplazar handleCreate por el endpoint</name>
  <files>app/(dashboard)/appointments/appointments-client.tsx, app/(dashboard)/appointments/page.tsx</files>
  <read_first>
    - app/(dashboard)/appointments/page.tsx (carga actual: appointments/professionals/services/timeBlocks por business_id — agregar clients y locations)
    - app/(dashboard)/appointments/appointments-client.tsx (líneas 288-330 handleCreate directo a reemplazar; 545-640 el modal inline a reemplazar por NuevoTurnoForm; 345-347 el botón "Nuevo turno" existente)
    - components/dashboard/nuevo-turno-form.tsx (la API del form — Task 1)
    - app/(dashboard)/clients/page.tsx (patrón de carga de clients por business_id)
  </read_first>
  <action>
    En `app/(dashboard)/appointments/page.tsx`: agregar al `Promise.all` la carga de `clients` (`select('*').eq('business_id', business.id)`) y, si el form los usa, `locations` (`.eq('business_id', business.id)`). Pasar `clients` (y `locations`) como props al `AppointmentsClient`.

    En `app/(dashboard)/appointments/appointments-client.tsx`: aceptar las nuevas props (`clients`, `locations`). Reemplazar el modal inline (`:545-640`) por `<NuevoTurnoForm open={dialogOpen} onOpenChange={setDialogOpen} clients={clients} services={services} professionals={professionals} locations={locations} />`. Eliminar el `handleCreate` directo (`:288-330`) — el alta ahora la hace el form vía fetch. Conservar el botón "Nuevo turno" (`:345-347`) que setea `dialogOpen`. Tras crear, el refresh lo maneja el form (`router.refresh()`); si el componente mantenía estado local optimista de `appointments`, dejar que `router.refresh()` recargue los datos del server component (más simple y consistente). Mantener intacto todo lo demás (tabs, lista, delete, editar).
  </action>
  <acceptance_criteria>
    - source: `app/(dashboard)/appointments/page.tsx` carga `clients` por `.eq('business_id', business.id)` y lo pasa al client
    - source: `appointments-client.tsx` renderiza `<NuevoTurnoForm` y ya NO contiene el `handleCreate` con `supabase.from('appointments').insert`
    - source: el botón "Nuevo turno" sigue presente
    - cli: `npx tsc --noEmit` pasa
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Turnos usa el form compartido; el insert directo client-side fue eliminado; page.tsx provee clients/locations; typecheck pasa.</done>
</task>

<task type="auto">
  <name>Task 3: Cablear el form en Agenda — botón "Nuevo turno" + click-en-día pre-llena fecha</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx, app/(dashboard)/agenda/page.tsx</files>
  <read_first>
    - app/(dashboard)/agenda/page.tsx (carga actual: timeBlocks/locations/exceptions/appointments — agregar services/professionals/clients)
    - app/(dashboard)/agenda/agenda-client.tsx (líneas 412-449: resumen semanal de 7 días — cada celda de día es donde va el click-en-día; líneas 83-86: patrón fetch+toast+router.refresh; header de la página para el botón)
    - components/dashboard/nuevo-turno-form.tsx (la API del form, props prefill.date — Task 1)
    - .planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-RESEARCH.md (Pitfall 3 — NO hay grilla horaria; D-08 acotado a click-en-día -> fecha)
  </read_first>
  <action>
    En `app/(dashboard)/agenda/page.tsx`: agregar al `Promise.all` la carga de `services` (`.eq('business_id', business.id).eq('active', true)`), `professionals` (`.eq('business_id', business.id).eq('active', true)`) y `clients` (`.eq('business_id', business.id)`). Pasarlos como props al `AgendaClient`.

    En `app/(dashboard)/agenda/agenda-client.tsx`: aceptar las nuevas props. Agregar estado `nuevoTurnoOpen` + `prefillDate`. Agregar un botón "Nuevo turno" (icono `Plus`, ya importado) en el header de la página (junto al título / controles de Google), que abre el form con `prefillDate` vacío. En el resumen semanal (`:424-447`), hacer que cada celda de día sea un `<button>` semántico (focusable, con `aria-label` que describa la fecha) que al click setea `prefillDate = format(d,'yyyy-MM-dd')` y abre el form. Aplicar el hover `hover:border-primary` del UI-SPEC a la celda (mirrors calendar-day). Renderizar `<NuevoTurnoForm open={nuevoTurnoOpen} onOpenChange={setNuevoTurnoOpen} prefill={{ date: prefillDate }} clients={clients} services={services} professionals={professionals} locations={initialLocations} />`. NO construir grilla hora x dia (D-08 acotado). Tras crear, el form hace `router.refresh()`. No romper el editor de horario semanal existente (time_blocks/excepciones) ni el resumen de chips de turnos.
  </action>
  <acceptance_criteria>
    - source: `app/(dashboard)/agenda/page.tsx` carga `services`, `professionals` y `clients` por `.eq('business_id', business.id)` y los pasa al client
    - source: `agenda-client.tsx` renderiza `<NuevoTurnoForm` y tiene un botón "Nuevo turno"
    - source: las celdas de día del resumen semanal son `<button>` con `aria-label` y setean la fecha del prefill
    - source: NO hay una grilla hora x dia nueva (sin filas horarias clickeables — solo click-en-día)
    - cli: `npx tsc --noEmit` pasa
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Agenda tiene botón "Nuevo turno" + click-en-día que pre-llena la fecha del form compartido; page.tsx provee services/professionals/clients; sin grilla horaria; typecheck pasa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificación visual del alta manual (desktop + mobile)</name>
  <what-built>
    El form compartido "Nuevo turno" (modal desktop / drawer mobile) con combobox de cliente + crear-inline, cableado en Turnos y Agenda, que crea el turno vía el endpoint autenticado (anti-tampering + anti-doble-booking + dedupe) y refresca la vista.
  </what-built>
  <how-to-verify>
    1. `npm run dev`, loguearse como un negocio con servicios/profesionales/clientes cargados.
    2. En **Turnos**: click "Nuevo turno" -> el modal abre (desktop). Buscar un cliente existente en el combobox -> se filtra. Crear uno nuevo inline (nombre + teléfono). Elegir servicio + fecha + hora libre. "Agregar turno" -> toast "Turno agregado", el turno aparece en la lista (router.refresh).
    3. Repetir eligiendo una hora que YA tiene un turno del mismo profesional -> toast "Ese horario ya está ocupado. Elegí otro." (slot_taken del endpoint).
    4. Dedupe: crear "nuevo" cliente con el teléfono de uno existente -> confirmar que NO se duplicó el cliente (verlo en /clients).
    5. En **Agenda**: click "Nuevo turno" -> abre con fecha vacía. Click en un DÍA del resumen semanal -> abre con esa fecha pre-llenada. Confirmar que NO hay grilla horaria clickeable (solo el resumen de días).
    6. Mobile (DevTools <768px): el form abre como **drawer** (vaul), no modal. Touch targets cómodos. Labels visibles.
    7. Confirmar que NO aparece ningún control de seña / link de pago en el form (D-01).
  </how-to-verify>
  <action>Checkpoint humano: correr `npm run dev` y verificar manualmente el alta manual en desktop (modal) y mobile (drawer) según los pasos de how-to-verify: combobox + crear-inline, dedupe que no duplica, slot ocupado rechazado, click-en-día que pre-llena fecha, y ausencia de control de seña.</action>
  <resume-signal>Escribí "approved" si el alta funciona en desktop+mobile, el dedupe no duplica, slot ocupado se rechaza, click-en-día pre-llena fecha y no hay control de seña; o describí los problemas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (form) -> /api/appointments/create | el form solo manda datos; toda la validación/aislamiento la hace el endpoint (Plan 02) |
| datos cargados en page.tsx (server) -> client component | clients/services/professionals/locations ya filtrados por business_id en el server component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Tampering | el form como única defensa (bypass del pipeline si se inserta directo) | mitigate | El form NO inserta directo a supabase; todo va por el endpoint autenticado que re-valida server-side (verify: grep -L de .from('appointments').insert en el form) |
| T-01-14 | Information Disclosure | exponer datos de otro tenant en los combos | mitigate | clients/services/professionals/locations se cargan en page.tsx con `.eq('business_id', business.id)` (server component con sesión del dueño) |
| T-01-15 | Tampering | doble-booking desde el alta manual | mitigate | El endpoint reusa el core (re-check + constraints 011/013); el form muestra el slot_taken; no hay bypass UI (verify: checkpoint paso 3) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Sin paquetes nuevos: combobox = Input+filtro, drawer/dialog ya vendoreados (verify: acceptance Task 1 — sin import de command, sin nuevas deps) |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores.
- Verificación visual (Task 4): alta funciona desktop (modal) + mobile (drawer); dedupe no duplica; slot ocupado rechazado; click-en-día pre-llena fecha; sin control de seña.
- `grep` confirma: el form hace fetch al endpoint y NO inserta directo; no importa command.tsx; page.tsx cargan los datos por business_id.
</verification>

<success_criteria>
- El alta manual de turnos es operable desde Turnos y Agenda, responsive, con combobox de cliente + crear/dedupe, corriendo el pipeline server-side completo vía el endpoint.
- D-08 acotado entregado (botón + click-en-día -> fecha; sin grilla horaria).
- MANUAL-01/02/03 quedan operables end-to-end desde la UI; MANUAL-04 ausente de la UI (D-01).
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/01-turnos-manuales/01-04-SUMMARY.md` when done
</output>
