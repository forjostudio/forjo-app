---
phase: 02-cupos-grupales
plan: 05
type: tdd
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - test/concurrency.test.ts
autonomous: true
requirements: [CONC-01, CONC-02, CUPOS-03, CUPOS-02]
user_setup: []

must_haves:
  truths:
    - "CONC-01: dos createAppointmentCore en paralelo (Promise.all) sobre el último lugar de un bloque capacity=2 → exactamente 1 ok + 1 slot_full; la DB queda con exactamente 2 filas que ocupan (no 3)."
    - "CONC-02: en un bloque capacity=1, la 2ª reserva del mismo slot → slot_taken (NO slot_full) — no-regresión del anti-doble-booking."
    - "CUPOS-03: un bloque capacity=N admite N reservas y rechaza la (N+1)ª con slot_full."
    - "CUPOS-02: la respuesta de /api/booking/availability no contiene el conteo de ocupantes por slot (solo busy/full) — el público no puede inferir lugares restantes."
    - "La suite completa (npm test) sigue verde — booking-core.test.ts (cupo 1) no regresó."
  prohibitions:
    - "Los tests NUNCA pasan en falso: CONC-01 debe asertar exactamente 1 ok + 1 full Y verificar el estado de la DB con t.admin (no 3 filas)."
    - "CONC-02 NUNCA acepta slot_full como resultado de la 2ª reserva cupo 1 — debe ser slot_taken."
    - "La clave del slot en los tests NUNCA mezcla professional_id null y la sentinela en distintas reservas del mismo slot (Pitfall 1)."
  artifacts:
    - path: "test/concurrency.test.ts"
      provides: "CONC-01, CONC-02, CUPOS-03, CUPOS-02 con cuerpos reales (no placeholder)"
      contains: "slot_full"
      min_lines: 60
  key_links:
    - from: "test/concurrency.test.ts"
      to: "createAppointmentCore + book_slot_atomic"
      via: "Promise.all de dos altas sobre el último lugar; el advisory lock serializa en la DB → determinista"
      pattern: "Promise.all"
    - from: "test/concurrency.test.ts"
      to: "seedTimeBlock"
      via: "siembra un time_block capacity=N para que el RPC resuelva la capacity"
      pattern: "seedTimeBlock"
---

<objective>
Llenar el scaffold de `test/concurrency.test.ts` (creado en Plan 01) con los cuerpos reales de CONC-01 (anti-sobrecupo concurrente), CONC-02 (no-regresión cupo 1), CUPOS-03 (admite hasta capacity, rechaza el excedente) y un test de CUPOS-02 (availability no filtra lugares restantes). Estos tests son el CRITERIO DE ÉXITO DURO de la fase.

Purpose: La garantía atómica anti-sobrecupo y la cero-regresión cupo 1 viven en la DB; los tests son la única prueba de que funcionan bajo concurrencia real. El advisory lock del RPC (Plan 01) serializa en la DB independientemente del orden de llegada, lo que hace los tests DETERMINISTAS (siempre 1 ok + 1 full) en vez de flaky (Pitfall 1 / RESEARCH §CONC-01 caveat). El test de CUPOS-02 guardea que el público no puede inferir lugares restantes (D-06).
Output: `test/concurrency.test.ts` con 4 tests verdes + la suite completa verde.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-PATTERNS.md

# El scaffold a llenar + el molde + el fixture
@test/concurrency.test.ts
@test/booking-core.test.ts
@test/helpers/booking-fixtures.ts
@lib/booking-core.ts
# Forma exacta del RPC y de la respuesta de availability (de plans previos)
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-01-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-02-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-03-SUMMARY.md
</context>

<feature>
  <name>Tests de concurrencia anti-sobrecupo + no-regresión cupo 1 + non-leak de availability</name>
  <files>test/concurrency.test.ts</files>
  <behavior>
    CONC-01 (capacity=2, 1 seat ya ocupado):
      - input: dos createAppointmentCore en Promise.all sobre el mismo slot '09:00' (el último lugar)
      - output: exactamente 1 resultado ok:true + exactamente 1 ok:false con error 'slot_full'
      - verificación DB: exactamente 2 filas en el slot en estados confirmed/pending_payment (no 3)
    CONC-02 (capacity=1):
      - input: 1ª reserva ok; 2ª reserva del mismo slot '10:00'
      - output: 2ª → ok:false, error 'slot_taken' (NO 'slot_full')
    CUPOS-03 (capacity=N, ej. 3):
      - input: N reservas secuenciales sobre el slot + 1 extra
      - output: las primeras N → ok:true; la (N+1)ª → ok:false, error 'slot_full'
    CUPOS-02 (availability non-leak):
      - input: un slot con M inscriptos (M < capacity), consultar la respuesta de availability/disponibilidad
      - output: la respuesta NO contiene el conteo por slot ni `remaining`; solo busy/full
  </behavior>
  <read_first>
    - test/concurrency.test.ts (scaffold con los 3 it placeholder de Plan 01)
    - test/booking-core.test.ts líneas 1-57 (molde: skipIf, beforeAll/afterAll/afterEach, baseInput, DATE) + Test B/D (no-regresión cupo 1, carrera) + Test C (verificación independiente con t.admin)
    - test/helpers/booking-fixtures.ts (seedOneTenant + seedTimeBlock de Plan 01)
    - 02-RESEARCH.md §CONC-01 (molde Promise.all + verificación DB length===2 + caveat determinismo), §CONC-02 (molde slot_taken), §Validation Architecture (Test Map, comandos -t)
    - 02-PATTERNS.md §test/concurrency.test.ts (Analog A/B/C/D)
    - 02-02-SUMMARY.md (mapeo slot_full vs slot_taken en el core) + 02-03-SUMMARY.md (forma de la respuesta de availability { ok, busy, full })
  </read_first>
  <implementation>
RED→GREEN: el scaffold ya existe con los `it` pendientes (fallan). Reemplazar cada cuerpo placeholder por el real; correr; deben pasar contra Supabase local (con 041 aplicada por `supabase db reset`).

1. En `beforeAll`: sembrar el tenant (`seedOneTenant`) y, para los tests de cupo > 1, sembrar un time_block con capacity vía `seedTimeBlock` (cubriendo el day_of_week de `DATE` y los horarios de test). Para CONC-02 (cupo 1), un time_block capacity=1 (o el default). Usar `t.admin` (service-role) como el `supabase` del core (molde de booking-core.test.ts: el core es rol-agnóstico). `afterEach` limpia appointments por business_id.

2. CONC-01 — `it('CONC-01 — anti-sobrecupo: dos reservas concurrentes sobre el último lugar, solo una confirma', ...)`: con un time_block capacity=2, crear 1 inscripto (ocupa seat 0). Luego `const [a, b] = await Promise.all([createAppointmentCore({...baseInput(), time:'09:00'}), createAppointmentCore({...baseInput(), time:'09:00'})])`. Asertar `[a,b].filter(r => r.ok).length === 1` y `[a,b].filter(r => !r.ok && r.error === 'slot_full').length === 1`. Verificación independiente: `t.admin.from('appointments').select('id').eq('business_id', t.businessId).eq('date', DATE).eq('time','09:00').in('status',['confirmed','pending_payment'])` → length === 2 (no 3). Comentar en español por qué es determinista (el advisory lock serializa en la DB).

3. CONC-02 — `it('CONC-02 — no-regresion: capacity=1 sigue rechazando la 2ª con slot_taken', ...)`: con time_block capacity=1, 1ª reserva `time:'10:00'` → `expect(first.ok).toBe(true)`; 2ª reserva mismo slot → `expect(second.ok).toBe(false)` y `if (!second.ok) expect(second.error).toBe('slot_taken')` (explícitamente NO slot_full). Es la guarda de cero-regresión.

4. CUPOS-03 — `it('CUPOS-03 — admite hasta capacity y rechaza el excedente con slot_full', ...)`: con time_block capacity=3, crear 3 reservas secuenciales sobre `time:'11:00'` (todas ok), luego 1 más → `expect(extra.ok).toBe(false)` y `expect(extra.error).toBe('slot_full')`. Verificación DB: exactamente 3 filas que ocupan.

5. CUPOS-02 — `it('CUPOS-02 — availability no filtra lugares restantes', ...)`: crear M < capacity inscriptos en un slot. Llamar al endpoint de disponibilidad (o a la función que lo computa) y asertar que la respuesta NO tiene una propiedad con el conteo por slot ni `remaining`; que `full` (si presente) es un array de strings y `busy` no expone un número de lugares restantes. Si invocar el route handler HTTP es costoso en el test, asertar la forma de la respuesta `{ ok, busy, full }` documentada en 02-03-SUMMARY y que ninguna clave numérica revela ocupación restante. Mantener el test simple y determinista.

Correr `npx vitest run test/concurrency.test.ts` (debe pasar los 4) y luego `npm test` (suite completa verde — booking-core.test.ts no regresó). Si las creds de Supabase faltan, los tests se skipean (no fallan) — documentar que el gate real requiere las 3 creds + 041 aplicada.

Commits TDD: `test(02-05): completar tests de concurrencia (CONC-01/02, CUPOS-03/02)` tras GREEN.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → Supabase local (service-role) | los tests usan service-role solo para sembrar/verificar fixtures; aíslan la lógica del core de RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | test flaky deja pasar sobrecupo sin detectarlo | mitigate | CONC-01 verifica el estado de la DB con t.admin (exactamente 2 filas), no solo los resultados; el advisory lock lo hace determinista. |
| T-02-17 | Tampering | CONC-02 pasaría con slot_full y enmascararía una regresión | mitigate | CONC-02 asierta explícitamente error === 'slot_taken' (NO slot_full) para la 2ª reserva cupo 1. |
| T-02-18 | Information Disclosure | el test no detecta una fuga de lugares restantes | mitigate | CUPOS-02 asierta que la respuesta de availability no contiene conteo/remaining por slot. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Sin paquetes nuevos (Vitest ya instalado). Sin checkpoint de legitimidad necesario. |
</threat_model>

<verification>
- `npx vitest run test/concurrency.test.ts` → 4 tests verdes (CONC-01, CONC-02, CUPOS-03, CUPOS-02) con las 3 creds de Supabase + 041 aplicada.
- `npm test` → suite completa verde (booking-core.test.ts sin regresión).
- CONC-01 verifica el estado de la DB (exactamente 2 filas), no solo los resultados.
- CONC-02 asierta slot_taken (no slot_full) para cupo 1.
</verification>

<success_criteria>
- Los 4 tests existen con cuerpos reales y pasan contra Supabase local.
- La suite completa sigue verde (cero regresión cupo 1).
- El gate de la fase (suite verde + supabase db reset aplica 041) se cumple antes de /gsd:verify-work.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-05-SUMMARY.md` when done.
Registrar el conteo final de tests de la suite (npm test) para la memoria del proyecto.
</output>
