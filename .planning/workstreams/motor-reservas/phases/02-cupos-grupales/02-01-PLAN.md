---
phase: 02-cupos-grupales
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/041_time_blocks_capacity_and_seat.sql
  - supabase/schema.sql
  - test/helpers/booking-fixtures.ts
  - test/concurrency.test.ts
autonomous: false
requirements: [CUPOS-01, CUPOS-03, CONC-01, CONC-02]
user_setup: []

must_haves:
  truths:
    - "La migración 041 aplica limpia con `supabase db reset` local (replay baseline + 040 + 041) sin error."
    - "time_blocks gana columna capacity smallint NOT NULL DEFAULT 1 con CHECK (capacity >= 1)."
    - "appointments gana columna seat smallint NOT NULL DEFAULT 0 y columna is_group boolean NOT NULL DEFAULT false."
    - "El índice único appointments_no_double_booking incluye seat como última columna del bucket."
    - "El EXCLUDE gist appointments_no_overlap solo aplica cuando is_group = false (cupo 1 conserva el anti-solape)."
    - "Existe la función book_slot_atomic SECURITY DEFINER con pg_advisory_xact_lock por slot, count vs capacity e INSERT condicional que RAISE 'slot_full' cuando está lleno."
    - "El fixture seedOneTenant puede sembrar un time_block con capacity configurable (seedTimeBlock)."
    - "test/concurrency.test.ts existe como scaffold con los describe/it de CONC-01, CONC-02, CUPOS-03 (skeleton, llenado en Plan 05)."
  prohibitions:
    - "La migración NUNCA usa `supabase db push` — solo `supabase db reset` local valida."
    - "La redefinición del índice 011 NUNCA deja de rechazar la 2ª fila cuando capacity = 1 (seat 0 único)."
    - "El EXCLUDE 013 NUNCA se elimina ni se vuelve siempre-permisivo globalmente (solo se condiciona a NOT is_group)."
    - "book_slot_atomic NUNCA confía en IDs del cliente: filtra TODO por p_business_id resuelto por el caller; SET search_path = public."
    - "La migración NUNCA expone roster/ocupación a anon (appointments sigue sin read público para anon)."
  artifacts:
    - path: "supabase/migrations/041_time_blocks_capacity_and_seat.sql"
      provides: "capacity en time_blocks + seat/is_group en appointments + redefinición índice 011 + EXCLUDE 013 condicional + función book_slot_atomic + GRANT EXECUTE + policy WITH CHECK de time_blocks"
      contains: "book_slot_atomic"
      min_lines: 80
    - path: "supabase/schema.sql"
      provides: "snapshot regenerado tras supabase db reset reflejando 041"
    - path: "test/helpers/booking-fixtures.ts"
      provides: "seedTimeBlock({ capacity }) o parámetro capacity en seedOneTenant"
    - path: "test/concurrency.test.ts"
      provides: "scaffold de CONC-01/CONC-02/CUPOS-03 con skipIf(!hasSupabaseCreds)"
  key_links:
    - from: "book_slot_atomic (041)"
      to: "appointments (índice único seat)"
      via: "INSERT con seat := count() — respaldo atómico si dos requests compiten el seat"
      pattern: "pg_advisory_xact_lock"
    - from: "book_slot_atomic (041)"
      to: "time_blocks.capacity"
      via: "SELECT capacity del bloque que cubre el slot (day_of_week + ventana start_time/end_time)"
      pattern: "time_blocks"
---

<objective>
Construir el ESPINAZO DE INTEGRIDAD de la fase: la migración 041 que agrega `capacity` a `time_blocks`, `seat`/`is_group` a `appointments`, redefine el índice único 011 a capacity-aware (columna `seat`), condiciona el EXCLUDE gist 013 a `NOT is_group`, y crea la función `book_slot_atomic` (`SECURITY DEFINER` + `pg_advisory_xact_lock`) que reemplazará el INSERT directo del core. Más el scaffold de tests (`test/concurrency.test.ts`) y la extensión del fixture para sembrar `time_blocks` con `capacity`.

Purpose: Sin la migración aplicada y validada con `supabase db reset` local, nada de la fase funciona — la garantía atómica anti-sobrecupo y la cero-regresión cupo 1 viven enteramente en la DB (D-02, CONC-01, CONC-02). El scaffold de tests es el Wave 0 que el resto de la fase llena.
Output: `supabase/migrations/041_time_blocks_capacity_and_seat.sql`, `supabase/schema.sql` regenerado, `test/helpers/booking-fixtures.ts` extendido, `test/concurrency.test.ts` scaffold.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-PATTERNS.md
@.claude/skills/supabase-multitenant-rls/SKILL.md

# Migración precedente (estilo de comentario denso + policy WITH CHECK por tenant)
@supabase/migrations/040_appointments_clients_insert_with_check.sql
# Constraints actuales a redefinir + tabla time_blocks/appointments (leer las líneas citadas en RESEARCH §Current Integrity Baseline)
@supabase/migrations/00000000000000_baseline.sql
# Fixture a extender
@test/helpers/booking-fixtures.ts
# Molde del suite de tests
@test/booking-core.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 041 — capacity + seat/is_group + redefinición de constraints + book_slot_atomic</name>
  <files>supabase/migrations/041_time_blocks_capacity_and_seat.sql</files>
  <read_first>
    - supabase/migrations/040_appointments_clients_insert_with_check.sql (estilo de cabecera en español: qué hace / qué NO hace / invariantes; forma exacta de la policy WITH CHECK por tenant a copiar literal)
    - supabase/migrations/00000000000000_baseline.sql líneas 633-642 (tabla time_blocks: day_of_week, start_time, end_time, label, location_id; NO tiene date/time), 648-649 (EXCLUDE appointments_no_overlap), 796-797 (índice appointments_no_double_booking), 93-101 (columnas appointments incl. cancel_token uuid DEFAULT gen_random_uuid())
    - 02-RESEARCH.md §Current Integrity Baseline + Pattern 1 (DDL de book_slot_atomic) + Pattern 2 (seat) + Pattern 3 Opción A (EXCLUDE condicional vía is_group) + §Runtime State Inventory
    - 02-PATTERNS.md §migration (Analog A/B/C) + §No Analog Found (RPC)
    - .claude/skills/supabase-multitenant-rls/SKILL.md (regla 3: una policy por operación con WITH CHECK)
  </read_first>
  <action>
Crear `supabase/migrations/041_time_blocks_capacity_and_seat.sql` con separador underscore (post-baseline, después de 040). Cabecera de comentario denso en ESPAÑOL al estilo de 040 explicando el *por qué* (capacity-aware con cero regresión cupo 1, anti-sobrecupo atómico) y declarando explícitamente que ESTA migración SÍ redefine los constraints 011/013 (invirtiendo la frase de 040 que decía que NO los tocaba) y por qué es seguro. El DDL, en orden:

1. `ALTER TABLE public.time_blocks ADD COLUMN capacity smallint NOT NULL DEFAULT 1;` seguido de `ALTER TABLE public.time_blocks ADD CONSTRAINT time_blocks_capacity_positive CHECK (capacity >= 1);`. Comentar: plantilla semanal, aplica a todos los slots de la ventana del bloque; el backfill DEFAULT 1 deja los bloques existentes con comportamiento idéntico al actual.

2. `ALTER TABLE public.appointments ADD COLUMN seat smallint NOT NULL DEFAULT 0;` y `ALTER TABLE public.appointments ADD COLUMN is_group boolean NOT NULL DEFAULT false;`. Comentar: `seat` = posición 0..capacity-1 que vuelve único el índice por slot; `is_group` desnormaliza si el slot es grupal (lo escribe el RPC) para condicionar el EXCLUDE 013. El backfill DEFAULT 0 / false deja las filas vivas como individuales (seat 0, no grupal) — consistente con que hoy cada slot tiene a lo sumo 1 fila (lo garantiza el índice 011 actual).

3. Redefinir el índice 011: `DROP INDEX IF EXISTS public.appointments_no_double_booking;` luego recrearlo IDÉNTICO al baseline pero agregando `, seat` como última columna: `CREATE UNIQUE INDEX appointments_no_double_booking ON public.appointments USING btree (business_id, COALESCE(professional_id, '00000000-0000-0000-0000-000000000000'::uuid), date, time, seat) WHERE (status = ANY (ARRAY['confirmed'::text, 'pending_payment'::text]));`. Preservar el sentinel `00000000-0000-0000-0000-000000000000` EXACTO. Comentar: cupo 1 solo asigna seat=0 → el índice rechaza la 2ª fila igual que hoy (23505 → slot_taken), CERO regresión (CONC-02); cupo N permite seats 0..N-1 distintos.

4. Condicionar el EXCLUDE 013: `ALTER TABLE public.appointments DROP CONSTRAINT IF EXISTS appointments_no_overlap;` luego recrearlo IDÉNTICO al baseline (líneas 648-649: business_id WITH =, COALESCE(professional_id, sentinel) WITH =, tsrange((date+time),(date+time)+make_interval(mins=>COALESCE(duration_minutes,30))) WITH &&) pero agregando `AND NOT is_group` al WHERE: `WHERE (status = ANY (ARRAY['confirmed'::text, 'pending_payment'::text]) AND NOT is_group)`. Comentar: cupo 1 (is_group=false) conserva el anti-solape de duración variable INTACTO (cero regresión); cupo N (is_group=true) lo evita porque los inscriptos comparten el mismo rango exacto (D-03) y el índice de seat ya cubre la unicidad.

5. Crear `book_slot_atomic` siguiendo el DDL de 02-RESEARCH.md Pattern 1: `CREATE OR REPLACE FUNCTION public.book_slot_atomic(...)` con firma que reciba `p_business_id uuid, p_professional_id uuid, p_service_id uuid, p_location_id uuid, p_date date, p_time time, p_duration int, p_client_id uuid, p_client_name text, p_client_phone text, p_client_email text, p_notes text, p_status text, p_expires_at timestamptz` y `RETURNS TABLE (id uuid, cancel_token uuid)`. `LANGUAGE plpgsql SECURITY DEFINER SET search_path = public`. Cuerpo: (a) `v_bucket := COALESCE(p_professional_id, '00000000-0000-0000-0000-000000000000'::uuid)`; (b) `PERFORM pg_advisory_xact_lock(hashtextextended(p_business_id::text || v_bucket::text || p_date::text || p_time::text, 0));` — la clave del lock usa EXACTAMENTE el mismo COALESCE que el índice; (c) `SELECT COALESCE(MAX(tb.capacity), 1) INTO v_capacity FROM time_blocks tb WHERE tb.business_id = p_business_id AND tb.day_of_week = EXTRACT(dow FROM p_date) AND p_time >= tb.start_time AND p_time < tb.end_time;`; (d) `SELECT count(*) INTO v_occupied FROM appointments a WHERE a.business_id = p_business_id AND COALESCE(a.professional_id, '00000000-0000-0000-0000-000000000000'::uuid) = v_bucket AND a.date = p_date AND a.time = p_time AND a.status IN ('confirmed','pending_payment');`; (e) `IF v_occupied >= v_capacity THEN RAISE EXCEPTION 'slot_full' USING ERRCODE = 'P0001'; END IF;`; (f) `v_seat := v_occupied;` y `RETURN QUERY INSERT INTO appointments (business_id, client_id, client_name, client_phone, client_email, service_id, professional_id, location_id, date, time, duration_minutes, seat, is_group, notes, status, expires_at) VALUES (..., v_seat, (v_capacity > 1), ..., p_status, p_expires_at) RETURNING appointments.id, appointments.cancel_token;`. El `is_group` insertado = `v_capacity > 1` (grupal cuando el bloque tiene cupo). Filtrar TODO por `p_business_id` (Pitfall 2). Comentar denso en español el por qué del advisory lock (serializa solo el slot peleado, multi-instancia en Vercel) y por qué `SECURITY DEFINER` necesita re-imponer tenant internamente.

6. `GRANT EXECUTE ON FUNCTION public.book_slot_atomic(uuid, uuid, uuid, uuid, date, time, int, uuid, text, text, text, text, text, timestamptz) TO anon, authenticated, service_role;` (la firma exacta del GRANT debe coincidir con la de la función).

7. Hardening RLS de `time_blocks` al estilo 040: agregar `CREATE POLICY "time_blocks tenant insert" ON public.time_blocks FOR INSERT WITH CHECK (business_id IN (SELECT businesses.id FROM businesses WHERE businesses.owner_id = (SELECT auth.uid())));` y la equivalente `FOR UPDATE USING/WITH CHECK` — copiando literal el patrón de 040, para que editar `capacity` no abra un vector cross-tenant. NO hacer DROP de la policy "business member access" ni de la policy public read existentes. Comentar que la policy public read de time_blocks (baseline:1354) queda intacta: anon puede leer `capacity` (tope estático, aceptable bajo D-06; NO es ocupación viva).

NO emitir `supabase db push` en ningún comentario ni instrucción. La validación es Task 2.
  </action>
  <verify>
    <automated>grep -v '^--' "supabase/migrations/041_time_blocks_capacity_and_seat.sql" | grep -c 'book_slot_atomic' | { read n; [ "$n" -ge 2 ] && echo OK || { echo "FALTA book_slot_atomic"; exit 1; }; }</automated>
  </verify>
  <acceptance_criteria>
    - El archivo existe con capacity (CHECK >= 1), seat, is_group, índice 011 redefinido con seat, EXCLUDE 013 condicionado a NOT is_group, función book_slot_atomic con advisory lock, GRANT EXECUTE, policy WITH CHECK de time_blocks.
    - El sentinel 00000000-0000-0000-0000-000000000000 aparece idéntico en el índice, la clave del advisory lock y el count de ocupación.
    - No aparece la cadena `supabase db push` en el archivo.
  </acceptance_criteria>
  <done>La migración 041 está escrita con todos los objetos DDL y la función book_slot_atomic; preserva el sentinel y el bucket COALESCE consistentes.</done>
</task>

<task type="auto">
  <name>Task 2: Validar la migración con `supabase db reset` local + regenerar schema.sql</name>
  <files>supabase/schema.sql</files>
  <read_first>
    - 02-RESEARCH.md §Environment Availability (Supabase CLI local PG17 configurado) + §Validation Architecture (gate: supabase db reset aplica 041 sin error)
    - commit 546e6a0 (precedente: "regenerar supabase/schema.sql (migración 040)" — dump --local sobre baseline + migraciones)
    - supabase/config.toml (config local)
  </read_first>
  <action>
Ejecutar el replay LOCAL de migraciones para validar que 041 aplica limpio sobre baseline + 040: `supabase db reset` (PG17 local). Debe terminar sin error — replaya baseline + 040 + 041 en orden. Si el reset falla por el predicado del EXCLUDE condicional o por la firma de la función, corregir la migración (Task 1) y re-correr. Esta es la ÚNICA validación válida del proyecto: NO usar `supabase db push`.

Tras el reset exitoso, regenerar `supabase/schema.sql` con el mismo patrón del precedente 040 (`supabase db dump --local` o el comando que usó el commit 546e6a0). El delta esperado en schema.sql: columna `capacity` en time_blocks, columnas `seat`/`is_group` en appointments, el índice 011 con seat, el EXCLUDE 013 con `AND NOT is_group`, la función book_slot_atomic + su GRANT, las policies nuevas de time_blocks.

Este task es `autonomous: false` a nivel de plan: requiere Supabase local corriendo (Docker). Si el entorno headless no puede levantar Supabase local, el executor debe PARAR y pedir intervención humana para correr `supabase db reset` + dump — el build/tsc/lint pasan sin esto (los tipos vienen de config, no de la DB viva), así que saltearlo crearía un falso verde de verificación.

Prod se aplica A MANO después, coordinado con el deploy (constraint del proyecto) — NO en este plan.
  </action>
  <verify>
    <automated>grep -c 'book_slot_atomic\|"capacity"\|"seat"\|"is_group"' supabase/schema.sql | { read n; [ "$n" -ge 3 ] && echo OK || { echo "schema.sql no refleja 041 — correr supabase db reset + dump"; exit 1; }; }</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` local replaya baseline + 040 + 041 sin error (registrado en el SUMMARY).
    - supabase/schema.sql regenerado contiene capacity, seat, is_group, book_slot_atomic.
    - No se ejecutó `supabase db push`; prod queda pendiente de aplicación manual.
  </acceptance_criteria>
  <done>La migración 041 aplica limpia en local y supabase/schema.sql refleja los nuevos objetos.</done>
</task>

<task type="auto">
  <name>Task 3: Extender fixture seedTimeBlock + scaffold de test/concurrency.test.ts</name>
  <files>test/helpers/booking-fixtures.ts, test/concurrency.test.ts</files>
  <read_first>
    - test/helpers/booking-fixtures.ts líneas 39-88 (seedOneTenant: business+location+service+professional; NO siembra time_blocks hoy — A4) y 94-101 (teardown cascada)
    - test/booking-core.test.ts líneas 1-57 (molde del suite: skipIf(!hasSupabaseCreds), seedOneTenant en beforeAll, teardown en afterAll, afterEach limpia appointments, baseInput(), DATE futura fija)
    - 02-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map; CONC-01/CONC-02/CUPOS-03) + §CONC-01/CONC-02 (moldes)
    - 02-PATTERNS.md §test/concurrency.test.ts (Analog A/B/C/D)
  </read_first>
  <action>
1. En `test/helpers/booking-fixtures.ts`: agregar un helper `seedTimeBlock(seeded, opts)` que inserte una fila en `time_blocks` con `business_id`, `day_of_week`, `start_time`, `end_time`, `location_id` (del fixture) y `capacity` (parámetro, default 1) usando `seeded.admin` (service-role). `day_of_week` debe corresponder al `EXTRACT(dow FROM DATE)` de la fecha de test (la fecha `2031-03-03` es un día concreto — calcularlo para que el bloque cubra el slot del RPC). `start_time`/`end_time` deben envolver los horarios de test (ej. '08:00'..'20:00'). Comentar en español por qué el RPC/availability necesitan el time_block sembrado (resuelven `capacity` por day_of_week + ventana). NO romper la firma de `seedOneTenant` existente (los tests actuales dependen de ella); agregar `seedTimeBlock` como helper aparte. El teardown por CASCADE de business ya borra los time_blocks (no requiere cambio).

2. Crear `test/concurrency.test.ts` como SCAFFOLD (los cuerpos los llena Plan 05): copiar el molde de `booking-core.test.ts` (import de `hasSupabaseCreds`, `seedOneTenant`/`teardownOneTenant`/`seedTimeBlock`, `createAppointmentCore`; `describe.skipIf(!hasSupabaseCreds)('concurrencia: cupos grupales', ...)`; `DATE` futura fija; `beforeAll` que siembra tenant + time_block; `afterAll` teardown; `afterEach` limpia appointments). Dentro del describe, declarar TRES `it` con el nombre exacto que el Test Map espera y cuerpo placeholder que falla explícito hasta Plan 05:
   - `it('CONC-01 — anti-sobrecupo: dos reservas concurrentes sobre el último lugar, solo una confirma', ...)`
   - `it('CONC-02 — no-regresion: capacity=1 sigue rechazando la 2ª con slot_taken', ...)`
   - `it('CUPOS-03 — admite hasta capacity y rechaza el excedente con slot_full', ...)`
   Cada cuerpo placeholder: `expect.fail('pendiente: Plan 05')` (o `throw new Error`) — el scaffold DEBE fallar/pendiente, no pasar en falso. Comentar arriba del archivo que el scaffold es Wave 0 y que Plan 05 lo completa con los moldes de 02-RESEARCH.md §CONC-01/CONC-02.

NO inventar la lógica de concurrencia acá — solo el esqueleto + el seed. La firma de los `it` debe coincidir con `-t "anti-sobrecupo"`, `-t "no-regresion"`, `-t "hasta capacity"` del Test Map para que los gates de Plan 05 corran.
  </action>
  <verify>
    <automated>grep -c 'seedTimeBlock' test/helpers/booking-fixtures.ts | { read n; [ "$n" -ge 1 ] && echo OK || { echo "falta seedTimeBlock"; exit 1; }; } && npx vitest run test/concurrency.test.ts 2>&1 | grep -qiE 'pendiente|fail|pending' && echo "scaffold-presente" || { echo "scaffold de concurrency no detectado"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - test/helpers/booking-fixtures.ts exporta seedTimeBlock({ capacity }) sin romper la firma de seedOneTenant.
    - test/concurrency.test.ts existe con los 3 `it` (CONC-01, CONC-02, CUPOS-03) y cuerpos placeholder que NO pasan en falso (expect.fail/throw).
    - Los nombres de los `it` permiten filtrarlos con `-t "anti-sobrecupo"`, `-t "no-regresion"`, `-t "hasta capacity"`.
  </acceptance_criteria>
  <done>El fixture puede sembrar un time_block con capacity y el scaffold de concurrencia existe (pendiente, no verde) para que Plan 05 lo complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente público → /api/booking/create (service-role) | input no confiable cruza al RPC; el tenant se resuelve por slug |
| dueño autenticado → /api/appointments/create (anon+RLS) | input del dueño; el RPC corre SECURITY DEFINER con privilegios elevados |
| anon → time_blocks (public read) | anon lee capacity (tope estático), NUNCA la ocupación viva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | book_slot_atomic (SECURITY DEFINER) cross-tenant | mitigate | La función filtra TODO por `p_business_id` resuelto por el caller; `SET search_path = public`; el anti-tampering de service/professional/location lo hace el core ANTES del RPC. Nunca confía en IDs del cliente. |
| T-02-02 | Tampering | regresión anti-doble-booking cupo 1 | mitigate | Índice único 011 con `seat` (seat=0 único para capacity 1) + EXCLUDE 013 condicional a `NOT is_group` preservado; CONC-02 lo guardea en Plan 05. |
| T-02-03 | Tampering | sobrecupo bajo carrera (TOCTOU count→insert) | mitigate | `pg_advisory_xact_lock(hash del slot+bucket)` dentro del RPC serializa solo el slot peleado + índice único seat como respaldo; CONC-01 en Plan 05. |
| T-02-04 | Elevation of Privilege | GRANT EXECUTE de la función a anon/authenticated | mitigate | GRANT acotado a anon, authenticated, service_role; la función re-impone tenant internamente — el grant no es un vector porque el `p_business_id` lo resuelve el caller, no el cliente. |
| T-02-05 | Information Disclosure | capacity expuesto a anon vía public read de time_blocks | accept | `capacity` es el tope estático (no la ocupación viva). D-06 solo prohíbe "lugares restantes"; la ocupación vive en appointments (sin read público para anon). Aceptado por research A3. |
| T-02-06 | Tampering | reasignar capacity de un bloque a otro tenant | mitigate | Policy `FOR INSERT/UPDATE WITH CHECK (business_id IN <negocios del dueño>)` en time_blocks al estilo 040. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Esta fase NO instala paquetes (RESEARCH §Standard Stack: sin dependencias nuevas; btree_gist es extensión nativa ya habilitada). Sin checkpoint de legitimidad necesario. |
</threat_model>

<verification>
- `supabase db reset` local replaya baseline + 040 + 041 sin error (gate duro de la fase).
- `grep` confirma capacity/seat/is_group/book_slot_atomic en la migración y en schema.sql regenerado.
- El sentinel `00000000-0000-0000-0000-000000000000` es idéntico en índice, advisory lock y count.
- `test/concurrency.test.ts` existe (pendiente, no verde) + `seedTimeBlock` disponible.
</verification>

<success_criteria>
- Migración 041 escrita y aplicada limpia en local; schema.sql regenerado.
- Índice 011 capacity-aware (seat) y EXCLUDE 013 condicional (NOT is_group) preservan la cero-regresión cupo 1.
- book_slot_atomic existe con advisory lock + count vs capacity + INSERT condicional + RAISE 'slot_full'.
- Fixture extendido + scaffold de tests listo para Plan 05.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-01-SUMMARY.md` when done.
Registrar en el SUMMARY: el resultado de `supabase db reset` local, la firma exacta de book_slot_atomic, y que prod queda PENDIENTE de aplicación manual.
</output>