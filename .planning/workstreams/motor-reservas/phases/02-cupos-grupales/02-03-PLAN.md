---
phase: 02-cupos-grupales
plan: 03
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - app/api/booking/availability/route.ts
  - app/[slug]/booking-client.tsx
autonomous: true
requirements: [CUPOS-02]
user_setup: []

must_haves:
  truths:
    - "availability cuenta ocupantes por slot (mismo bucket COALESCE) y los compara contra la capacity del time_block que cubre el slot."
    - "availability devuelve `full: string[]` (horarios llenos) además del `busy` actual, computado en el server con service-role."
    - "booking-client marca un slot como ocupado cuando está en `full` (count >= capacity), no solo por solapamiento suelto."
    - "Para capacity = 1, full y el comportamiento de busy coinciden (1 ocupante = lleno) — cero cambio visible."
  prohibitions:
    - "availability NUNCA devuelve el conteo de ocupantes ni una entrada por inscripto que permita inferir lugares restantes (D-06)."
    - "El público NUNCA recibe cuántos lugares quedan — solo libre/lleno."
    - "El client NUNCA computa la ocupación contra capacity por su cuenta — la autoridad del count vive en el server (anon no lee appointments)."
  artifacts:
    - path: "app/api/booking/availability/route.ts"
      provides: "count por slot vs capacity → full:string[], sin filtrar lugares restantes"
      contains: "full"
    - path: "app/[slug]/booking-client.tsx"
      provides: "marca slot ocupado por `full` del endpoint"
  key_links:
    - from: "app/api/booking/availability/route.ts"
      to: "time_blocks.capacity"
      via: "lee time_blocks del negocio (day_of_week + ventana) para resolver capacityFor(time)"
      pattern: "time_blocks"
    - from: "app/[slug]/booking-client.tsx"
      to: "app/api/booking/availability/route.ts"
      via: "fetch de availability → usa `full` para marcar slots ocupados"
      pattern: "availability"
---

<objective>
Volver capacity-aware el endpoint de disponibilidad pública: agrupar ocupantes por slot, leer `time_blocks.capacity`, y devolver SOLO `full: string[]` (horarios donde `count >= capacity`) — NUNCA el conteo ni una entrada por inscripto (D-06, CUPOS-02). Ajustar `booking-client.tsx` para marcar un slot ocupado cuando está en `full`.

Purpose: El público debe ver "disponible" hasta que el cupo se llena, sin poder inferir cuántos lugares quedan (D-06 LOCKED). El `anon` NO puede leer `appointments` (RLS) — la única vía pública a la ocupación es este endpoint (service-role), que colapsa el count a un booleano por slot. Filtrar la lista cruda de ocupantes filtraría lugares restantes (Pitfall 4).
Output: `app/api/booking/availability/route.ts` con `full`; `app/[slug]/booking-client.tsx` que marca slots por `full`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-PATTERNS.md

# Endpoint a modificar (self/exact analog)
@app/api/booking/availability/route.ts
# Generación de slots en el client (self/exact analog)
@app/[slug]/booking-client.tsx
---
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: availability capacity-aware — count por slot vs capacity → full:string[] (non-leak D-06)</name>
  <files>app/api/booking/availability/route.ts</files>
  <read_first>
    - app/api/booking/availability/route.ts COMPLETO: línea 11 (SENTINEL), 36-42 (query de appointments confirmed/pending_payment), 49-59 (filtro por bucket + descarte de holds vencidos + construcción de `busy` + Response.json con `busy`)
    - 02-RESEARCH.md §Availability capacity-aware (Code Examples: countByTime + capacityFor(time) → full) + Pitfall 4 (no filtrar lugares restantes) + Open Question #3 (mantener busy + agregar full)
    - 02-PATTERNS.md §availability/route.ts (colapsar a full por slot, mismo SENTINEL/bucket, mantener busy para capacity=1)
    - supabase/migrations/00000000000000_baseline.sql líneas 633-642 (time_blocks: day_of_week, start_time, end_time — para resolver capacityFor)
  </read_first>
  <action>
1. Agregar una query de `time_blocks` del negocio en el mismo endpoint (service-role, filtrada por `business_id`): `select('day_of_week, start_time, end_time, capacity')`. Comentar en español: la capacity vive en la plantilla semanal; se resuelve por slot vía day_of_week (de la `date` consultada) + ventana start_time/end_time.

2. Construir `capacityFor(time)`: para el `date` consultado, calcular su `day_of_week` (con la zona fija de Argentina, consistente con el resto del proyecto), y buscar el `time_block` cuyo `day_of_week` coincide y cuya ventana `[start_time, end_time)` contiene el `time`. Devolver su `capacity` (o 1 si no hay bloque). Si hay varios bloques que cubren el slot, usar el MAX (consistente con el `COALESCE(MAX(tb.capacity), 1)` del RPC en Plan 01).

3. Agrupar los ocupantes por `time` dentro del bucket ya filtrado (mismo `busy` que hoy, mismo `COALESCE(professional_id, SENTINEL)`, mismo descarte de holds vencidos): `const countByTime = new Map<string, number>()` sumando 1 por ocupante. Computar `const full = [...countByTime.entries()].filter(([time, n]) => n >= capacityFor(time)).map(([time]) => time)`.

4. Cambiar el `Response.json` (línea 59) a `Response.json({ ok: true, busy, full }, { headers: { 'Cache-Control': 'no-store' } })`. MANTENER `busy` (compatibilidad: para capacity=1, full y busy coinciden; minimiza el blast radius en el client) y AGREGAR `full`. NUNCA devolver `countByTime`, ni `remaining`, ni una entrada por inscripto que exponga el número (D-06 / Pitfall 4): `full` es solo la lista de horarios llenos.

Conservar `export const dynamic = 'force-dynamic'` y el header `Cache-Control: no-store` (disponibilidad siempre fresca). Conservar el comentario que explica que anon no lee appointments y por qué este endpoint usa service-role.
  </action>
  <verify>
    <automated>grep -c "full" app/api/booking/availability/route.ts | { read n; [ "$n" -ge 2 ] && echo OK || { echo "falta full"; exit 1; }; } && grep -c "time_blocks" app/api/booking/availability/route.ts | { read n; [ "$n" -ge 1 ] && echo OK || { echo "no lee time_blocks"; exit 1; }; } && ! grep -qE "remaining|countByTime\s*\}" app/api/booking/availability/route.ts && echo "no-leak-ok" && npx tsc --noEmit 2>&1 | grep -q "availability" && { echo "tsc error"; exit 1; } || echo tsc-ok</automated>
  </verify>
  <acceptance_criteria>
    - El endpoint lee time_blocks.capacity y resuelve capacityFor(time) por day_of_week + ventana.
    - La respuesta incluye `full: string[]` (horarios llenos) y conserva `busy`.
    - La respuesta NO contiene el conteo de ocupantes, `remaining`, ni una entrada por inscripto que permita inferir lugares restantes.
    - `npx tsc --noEmit` limpio en availability/route.ts.
  </acceptance_criteria>
  <done>availability cuenta por slot vs capacity y devuelve solo libre/lleno sin filtrar lugares restantes.</done>
</task>

<task type="auto">
  <name>Task 2: booking-client marca slots ocupados por `full` del endpoint</name>
  <files>app/[slug]/booking-client.tsx</files>
  <read_first>
    - app/[slug]/booking-client.tsx líneas 225-258 (generación de slots desde time_blocks; el `conflict` por solapamiento de `busy` líneas 241-246; el fetch de availability que llena `busy`)
    - 02-RESEARCH.md §Public booking client + Open Question #3 (para capacity=1 full y busy coinciden)
    - 02-PATTERNS.md §availability/route.ts (el client marca ocupado solo los slots en `full`)
  </read_first>
  <action>
Donde el client consume la respuesta de `/api/booking/availability` (el fetch que hoy llena `busy`), capturar también `full` (array de horarios). En la generación de slots (loop líneas 235-253), marcar un slot como ocupado/no-ofrecido cuando su `time` está en `full` — en vez de (o además de) el `conflict` por solapamiento de `busy`. Recomendación segura (D-06): para un bloque grupal, el endpoint ya decidió "lleno" contando contra capacity; el client confía en `full` y NO recomputa contra capacity (no tiene ni debe tener el count). Para capacity=1 `full` y el `conflict` por `busy` coinciden, así que la lógica sigue correcta para el caso individual.

Concretamente: tras obtener `{ busy, full }`, agregar `if (full.includes(time)) continue;` (saltea el slot lleno) en el loop de generación, conservando el `conflict` por solapamiento existente para el caso de duraciones variables del cupo 1 (ambos co-existen sin romperse). Comentar en español que `full` viene del server (autoridad del count) y que el client NUNCA conoce cuántos lugares quedan (D-06).

NO cambiar el copy/UX más allá de no ofrecer el slot lleno (un slot lleno simplemente no aparece como disponible, igual que hoy un slot ocupado). Si el endpoint no devuelve `full` (respuesta vieja), tratar `full` como `[]` (defensivo).
  </action>
  <verify>
    <automated>grep -c "full" app/[slug]/booking-client.tsx | { read n; [ "$n" -ge 1 ] && echo OK || { echo "client no usa full"; exit 1; }; } && npx tsc --noEmit 2>&1 | grep -q "booking-client" && { echo "tsc error"; exit 1; } || echo tsc-ok</automated>
  </verify>
  <acceptance_criteria>
    - El client lee `full` de la respuesta de availability y saltea los slots llenos en la generación.
    - El client trata `full` ausente como `[]` (defensivo, no rompe respuestas viejas).
    - El client no recomputa la ocupación contra capacity ni muestra lugares restantes.
    - `npx tsc --noEmit` limpio en booking-client.tsx.
  </acceptance_criteria>
  <done>El público ve un slot grupal como disponible hasta que el server lo reporta `full`, sin ver cuántos lugares quedan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (público) → /api/booking/availability (service-role) | el endpoint colapsa la ocupación a un booleano por slot; el anon NO lee appointments |
| servidor → cliente público | la respuesta NO debe filtrar lugares restantes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information Disclosure | fuga de lugares restantes vía availability | mitigate | El endpoint devuelve `full: string[]` (solo horarios llenos), nunca el count ni `remaining` ni una entrada por inscripto. El client confía en `full` y no recomputa. (D-06 / Pitfall 4) |
| T-02-11 | Information Disclosure | capacity (tope) inferible vía la respuesta | accept | El tope es dato estático ya legible en time_blocks (public read del baseline); D-06 solo prohíbe la OCUPACIÓN/lugares restantes, que sí se oculta. Consistente con T-02-05 de Plan 01. |
| T-02-12 | Tampering | el endpoint cuenta sobre el bucket equivocado | mitigate | Mismo `COALESCE(professional_id, SENTINEL)` y mismo descarte de holds vencidos que hoy; la capacity se resuelve con el MAX consistente con el RPC. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Sin paquetes nuevos en este plan. Sin checkpoint de legitimidad necesario. |
</threat_model>

<verification>
- `grep` confirma `full` + lectura de time_blocks en availability, y `full` consumido en el client.
- `grep` confirma que availability NO devuelve `remaining`/count crudo.
- `npx tsc --noEmit` limpio en ambos archivos.
- Verificación funcional (Plan 05 / UAT): un test asierta que la respuesta de availability no contiene el conteo por slot (CUPOS-02).
</verification>

<success_criteria>
- availability cuenta por slot vs capacity y devuelve solo libre/lleno.
- El público no puede inferir lugares restantes.
- El client marca slots llenos por `full`; capacity=1 sin cambio visible.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-03-SUMMARY.md` when done.
Registrar la forma EXACTA de la respuesta de availability (`{ ok, busy, full }`) para el test de no-leak de Plan 05.
</output>
