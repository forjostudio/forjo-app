---
phase: 02-cupos-grupales
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/booking-core.ts
  - lib/types.ts
autonomous: true
requirements: [CUPOS-03, CUPOS-05]
user_setup: []

must_haves:
  truths:
    - "createAppointmentCore invoca supabase.rpc('book_slot_atomic', {...}) en vez del INSERT directo."
    - "El tipo de retorno error del core incluye 'slot_full' además de 'slot_taken'/'invalid_service'/'invalid_professional'/'insert_failed'."
    - "Un slot grupal con lugar libre (count < capacity) NO es rechazado por el re-check JS (capacity-aware o delegado al RPC)."
    - "El choque del índice único de seat (23505/23P01) sigue mapeando a slot_taken; el RAISE 'slot_full' del RPC mapea a slot_full (409)."
    - "lib/types.ts: TimeBlock gana capacity: number; Appointment gana seat?: number e is_group?: boolean."
    - "La seña por servicio (requireDeposit) sigue rigiendo status/expires_at exactamente como hoy (CUPOS-05, sin cambio)."
  prohibitions:
    - "El core NUNCA vuelve a usar supabase.from('appointments').insert(...) directo para el alta (lo reemplaza el RPC)."
    - "El re-check JS NUNCA rechaza al 2º+ inscripto de un bloque grupal con cupo libre (Pitfall 5)."
    - "El advisory lock del RPC NUNCA usa una clave distinta a COALESCE(professional_id, sentinel) — el bucket debe ser idéntico al de hoy (Pitfall 1)."
    - "El core NUNCA deja de re-validar service/professional/location por business_id ANTES del RPC (anti-tampering intacto)."
  artifacts:
    - path: "lib/booking-core.ts"
      provides: "alta vía book_slot_atomic + mapeo de slot_full + re-check capacity-aware"
      contains: "book_slot_atomic"
    - path: "lib/types.ts"
      provides: "capacity en TimeBlock; seat/is_group en Appointment"
      contains: "capacity"
  key_links:
    - from: "lib/booking-core.ts"
      to: "book_slot_atomic (migración 041)"
      via: "supabase.rpc('book_slot_atomic', { p_business_id, p_professional_id, ... })"
      pattern: "rpc\\(['\"]book_slot_atomic"
    - from: "lib/booking-core.ts"
      to: "app/api/booking/create + app/api/appointments/create"
      via: "ambos callers heredan slot_full por el mapeo genérico result.error → HTTP (sin tocar los route handlers)"
      pattern: "error: result.error"
---

<objective>
Reemplazar el INSERT directo de `createAppointmentCore` (líneas 184-203) por una llamada a `supabase.rpc('book_slot_atomic', {...})`, agregar el código de error de dominio `slot_full` (409) al mapeo y al tipo de retorno, y volver capacity-aware el re-check JS para que no rechace inscriptos válidos de un bloque grupal. Extender `lib/types.ts` con `capacity` (TimeBlock) y `seat`/`is_group` (Appointment).

Purpose: El core es la ÚNICA fuente de verdad reusada por el booking público (service-role) y el alta manual autenticada (Phase 1). Agregar el anti-sobrecupo atómico acá una sola vez hace que AMBOS callers lo hereden sin duplicar (D-discreción "reúso del core"). El mapeo genérico `result.error → HTTP` ya existente en los dos route handlers propaga `slot_full` sin tocarlos (PATTERNS: callers self/exact). CUPOS-05 (seña por servicio) no se toca: el flag `requireDeposit` sigue rigiendo status/expires_at.
Output: `lib/booking-core.ts` con RPC + slot_full + re-check capacity-aware; `lib/types.ts` extendido.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-PATTERNS.md

# El core a refactorizar (self/exact analog)
@lib/booking-core.ts
# Tipos de dominio
@lib/types.ts
# Callers que heredan slot_full sin cambio (referencia, NO se tocan)
@app/api/booking/create/route.ts
@app/api/appointments/create/route.ts
# La firma del RPC (depende del SUMMARY de Plan 01)
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reemplazar el INSERT directo por book_slot_atomic + mapear slot_full + re-check capacity-aware</name>
  <files>lib/booking-core.ts</files>
  <behavior>
    - Un alta sobre un slot con cupo libre (count < capacity) → ok: true, appointmentId/cancelToken poblados (vía RPC).
    - Un alta sobre un slot lleno (count >= capacity) → ok: false, error: 'slot_full', status: 409.
    - Un alta sobre un slot cupo 1 ya ocupado → ok: false, error: 'slot_taken', status: 409 (no slot_full).
    - El 2º inscripto de un bloque grupal con cupo libre NO recibe slot_taken del re-check JS.
    - El anti-tampering (service/professional/location por business_id) corre ANTES del RPC, igual que hoy.
  </behavior>
  <read_first>
    - lib/booking-core.ts COMPLETO: línea 15 (SENTINEL), 49-61 (tipo de retorno; línea 61 = unión de errores), 106-134 (re-check JS de solapamiento → taken → slot_taken), 136-153 (liberación de holds vencidos, corre ANTES del insert), 181-212 (INSERT directo + traducción de constraint a reemplazar)
    - 02-RESEARCH.md §Code Examples (mapeo de error nuevo con rpcErr?.message?.includes('slot_full')) + Pitfall 1 (clave del lock = mismo COALESCE) + Pitfall 5 / A5 (re-check JS capacity-aware o delegado al RPC)
    - 02-PATTERNS.md §lib/booking-core.ts (cambios 1-5: tipo, mapeo, re-check, sentinel, holds antes del RPC)
    - 02-01-SUMMARY.md (firma EXACTA de book_slot_atomic y orden de parámetros p_*)
  </read_first>
  <action>
1. Tipo de retorno (línea 61): agregar `'slot_full'` a la unión de errores → `error: 'invalid_service' | 'invalid_professional' | 'slot_taken' | 'slot_full' | 'insert_failed'`.

2. Re-check JS capacity-aware (líneas 121-134): hoy `taken` se marca con CUALQUIER ocupante del bucket que solape (`sameBucket.some(...)`). Para no rechazar inscriptos válidos de un slot grupal, el re-check debe volverse capacity-aware: en vez de `some`, contar los ocupantes del MISMO slot exacto (mismo `time`, mismo bucket, estados que ocupan, holds no vencidos) y compararlos contra la `capacity` del bloque. La capacity ya se decide en el RPC (autoridad real); el re-check JS es solo UX, así que la opción más robusta (Pitfall 5 / A5) es: dejar el re-check JS SOLO para el caso obvio "cupo 1 ya ocupado" o delegar la decisión al RPC. Implementación recomendada: mantener el re-check de solapamiento como hoy PERO solo devolver `slot_taken` temprano cuando el bloque es cupo 1 (la `capacity` no es conocida en el core sin un query extra; la decisión segura es DELEGAR al RPC y NO rechazar de más en el path JS — el RPC ya devuelve slot_full/slot_taken con la autoridad). Documentar en español el por qué: el RPC es la autoridad atómica; el re-check JS no debe bloquear un slot grupal con lugar. Conservar la liberación de holds vencidos (136-153) ANTES del RPC sin cambio (el RPC cuenta sobre el estado ya limpio).

3. Reemplazar el bloque INSERT directo (181-203) por la llamada al RPC con la firma del SUMMARY de Plan 01: `const { data: appt, error: rpcErr } = await supabase.rpc('book_slot_atomic', { p_business_id: business.id, p_professional_id: proId, p_service_id: service.id, p_location_id: validLocationId, p_date: date, p_time: time, p_duration: Number(service.duration_minutes || 30), p_client_id: clientId, p_client_name: clientName, p_client_phone: clientPhone, p_client_email: clientEmail, p_notes: notes, p_status: initialStatus, p_expires_at: expiresAt }).single()`. Mantener el comentario denso en español explicando que el RPC es el respaldo atómico (advisory lock por slot) y reemplaza el INSERT autocommit que no podía serializar la carrera.

4. Mapeo de error (reemplaza líneas 205-212), en este orden: (a) `if (rpcErr?.message?.includes('slot_full')) return { ok: false, error: 'slot_full', status: 409 }` — el RAISE 'slot_full' (ERRCODE P0001) llega en `message`; (b) `if (rpcErr?.code === '23505' || rpcErr?.code === '23P01') return { ok: false, error: 'slot_taken', status: 409 }` — choque del índice de seat o del EXCLUDE (cupo 1: doble-reserva clásica); (c) `if (rpcErr || !appt) { console.error('[booking-core] rpc error:', rpcErr?.message); return { ok: false, error: 'insert_failed', status: 500 } }`. Conservar el prefijo de log `[booking-core]`.

5. La construcción del objeto de retorno ok (líneas 214-222) usa `appt.id` y `appt.cancel_token` igual que hoy (el RPC los devuelve vía RETURNING). NO tocar el cálculo de `initialStatus`/`expiresAt` (CUPOS-05 / D-05: la seña se configura por servicio, independiente de individual/grupal, y sigue rigiendo status/expires_at exactamente como hoy — no se agrega pricing ni lógica de seña nueva por capacidad).

NO inline el código completo: seguir los identificadores exactos (nombre del RPC, p_*, slot_full, 409, SENTINEL).
  </action>
  <verify>
    <automated>grep -c "book_slot_atomic" lib/booking-core.ts | { read n; [ "$n" -ge 1 ] && echo OK || { echo "falta rpc"; exit 1; }; } && grep -c "slot_full" lib/booking-core.ts | { read n; [ "$n" -ge 2 ] && echo OK || { echo "falta slot_full"; exit 1; }; } && ! grep -q "from('appointments')\s*$" lib/booking-core.ts && npx tsc --noEmit 2>&1 | grep -q "booking-core" && { echo "tsc error en core"; exit 1; } || echo "tsc-ok"</automated>
  </verify>
  <acceptance_criteria>
    - createAppointmentCore llama supabase.rpc('book_slot_atomic', {...}) con los p_* de la firma del SUMMARY de Plan 01.
    - El tipo de retorno error incluye 'slot_full'; el mapeo distingue slot_full (P0001/message) de slot_taken (23505/23P01).
    - El INSERT directo a appointments fue eliminado del path de alta.
    - El re-check JS no rechaza un slot grupal con cupo libre; el anti-tampering por business_id corre antes del RPC.
    - `npx tsc --noEmit` no reporta errores en booking-core.ts.
  </acceptance_criteria>
  <done>El core crea turnos vía el RPC atómico, mapea slot_full (409) y slot_taken (409) por separado, y no bloquea inscriptos grupales válidos.</done>
</task>

<task type="auto">
  <name>Task 2: Extender lib/types.ts con capacity (TimeBlock) y seat/is_group (Appointment)</name>
  <files>lib/types.ts</files>
  <read_first>
    - lib/types.ts líneas 81-91 (interface TimeBlock) y 175-206 (interface Appointment)
    - 02-PATTERNS.md §lib/types.ts (TimeBlock +capacity: number; Appointment +seat?: number, +is_group?: boolean; convención snake_case, opcionales con ?)
  </read_first>
  <action>
1. En `interface TimeBlock` (81-91): agregar `capacity: number` (no opcional — la columna es NOT NULL DEFAULT 1, siempre presente tras la migración). Comentar en español que es el cupo del bloque (default 1 = comportamiento individual de siempre).

2. En `interface Appointment` (175-206): agregar `seat?: number` y `is_group?: boolean` (opcionales con `?` siguiendo la convención del repo, ya que el `select` puede no traerlos siempre). Comentar: `seat` = posición 0..capacity-1 que vuelve único el índice; `is_group` = desnormalización que condiciona el EXCLUDE 013 (lo escribe el RPC).

Mantener snake_case en los campos (reflejan columnas DB), igual que el resto del archivo.
  </action>
  <verify>
    <automated>grep -c "capacity" lib/types.ts | { read n; [ "$n" -ge 1 ] && echo OK || exit 1; } && grep -c "is_group" lib/types.ts | { read n; [ "$n" -ge 1 ] && echo OK || exit 1; } && npx tsc --noEmit 2>&1 | grep -q "types.ts" && { echo "tsc error en types"; exit 1; } || echo "tsc-ok"</automated>
  </verify>
  <acceptance_criteria>
    - TimeBlock tiene capacity: number (no opcional).
    - Appointment tiene seat?: number e is_group?: boolean.
    - `npx tsc --noEmit` no reporta errores en types.ts.
  </acceptance_criteria>
  <done>Los tipos de dominio reflejan las columnas nuevas de la migración 041.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (público/manual) → createAppointmentCore | input no confiable; el core re-valida toda entidad por business_id antes del RPC |
| createAppointmentCore → book_slot_atomic | el core pasa IDs ya validados; el RPC (SECURITY DEFINER) re-impone tenant internamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | re-check JS debilitado abre bypass del cupo | mitigate | El re-check JS es solo UX; la autoridad atómica es el RPC (advisory lock + índice seat). El JS no rechaza de más (slot grupal) ni de menos (el RPC siempre decide). |
| T-02-08 | Tampering | anti-tampering de tenant omitido al refactorizar | mitigate | El re-validado de service/professional/location por business_id (líneas 83-104, 164-179) se conserva ANTES del RPC; el RPC recibe IDs ya validados. |
| T-02-09 | Information Disclosure | filtrar detalle de error de DB del RPC al cliente | mitigate | Solo se devuelve el código de dominio snake_case (slot_full/slot_taken/insert_failed); el mensaje crudo del RPC va a console.error, nunca al cliente. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Sin paquetes nuevos en este plan (solo edición de TS). Sin checkpoint de legitimidad necesario. |
</threat_model>

<verification>
- `grep` confirma book_slot_atomic + slot_full en el core y capacity/is_group en types.
- `npx tsc --noEmit` limpio en booking-core.ts y types.ts.
- El INSERT directo a appointments fue eliminado del path de alta del core.
- Verificación funcional real: los tests de Plan 05 (CONC-01/02, CUPOS-03) + la suite del core existente (booking-core.test.ts) deben seguir verdes.
</verification>

<success_criteria>
- El core crea turnos vía book_slot_atomic; ambos callers heredan slot_full sin tocarse.
- slot_full (409) y slot_taken (409) se distinguen correctamente en el mapeo.
- El re-check JS es capacity-aware / delega al RPC y no bloquea slots grupales con cupo.
- Los tipos reflejan capacity/seat/is_group.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-02-SUMMARY.md` when done.
Registrar el shape exacto del objeto pasado a supabase.rpc (los p_*) para que Plan 03/05 lo referencien.
</output>
