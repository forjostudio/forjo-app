---
phase: 02-cupos-grupales
plan: 04
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - app/(dashboard)/agenda/agenda-client.tsx
  - app/(dashboard)/agenda/page.tsx
autonomous: false
requirements: [CUPOS-01, CUPOS-04]
user_setup: []

must_haves:
  truths:
    - "El editor de horarios (grilla de time_blocks en agenda-client.tsx) tiene un campo `cupo` por bloque (Input number, min 1, default 1)."
    - "El campo `capacity` se persiste en el delete-all + insert de time_blocks (saveHours)."
    - "Al hacer click en un slot grupal de la agenda, se abre un overlay (Dialog en desktop / Drawer vaul en mobile) con el contador de ocupación (ej. 8/15) y el roster (nombre, contacto, estado confirmado/seña pendiente)."
    - "El contador y el roster se computan de los appointments ya cargados por slot vs la capacity del bloque, filtrados por business_id."
  prohibitions:
    - "NUNCA se introduce una librería nueva para el overlay — se reusa @/components/ui/dialog (desktop) y @/components/ui/drawer (vaul, ya instalado) (D-04)."
    - "El campo `cupo` NUNCA vive en settings-client.tsx (ahí no se editan time_blocks; corrección de PATTERNS sobre canonical_refs)."
    - "Las queries del roster NUNCA omiten `.eq('business_id', business.id)` (aislamiento por tenant)."
  artifacts:
    - path: "app/(dashboard)/agenda/agenda-client.tsx"
      provides: "campo cupo por bloque + persistencia capacity + overlay roster con contador"
      contains: "capacity"
    - path: "app/(dashboard)/agenda/page.tsx"
      provides: "query de appointments con client_phone/client_email para el roster, por business_id"
  key_links:
    - from: "app/(dashboard)/agenda/agenda-client.tsx (saveHours)"
      to: "time_blocks.capacity"
      via: "delete-all + insert con capacity en el objeto toInsert"
      pattern: "capacity"
    - from: "app/(dashboard)/agenda/agenda-client.tsx (roster)"
      to: "initialAppointments + time_blocks.capacity"
      via: "count de inscriptos del slot vs capacity del bloque → contador 8/15 + lista"
      pattern: "initialAppointments"
---

<objective>
Agregar el campo "cupo" (`capacity`) por bloque en la grilla de horarios de `agenda-client.tsx` (CUPOS-01, D-01) y el roster del admin: al hacer click en un slot grupal, un overlay (Dialog desktop / Drawer vaul mobile) muestra el contador de ocupación (8/15) + la lista de inscriptos con nombre, contacto y estado (CUPOS-04, D-04).

Purpose: El dueño define cuántos lugares tiene cada bloque y ve quién está inscripto. El campo va en el editor REAL de time_blocks — `agenda-client.tsx` líneas 555-589 (CORRECCIÓN de PATTERNS sobre canonical_refs: `settings-client.tsx` NO edita time_blocks). El roster reusa el patrón Dialog ya presente en el mismo archivo (líneas 736-766) + el componente drawer vaul ya instalado, SIN dependencias nuevas (D-04). El contador/roster es dato EXCLUSIVO del admin (corre con sesión anon+RLS del dueño, `.eq('business_id', ...)`).
Output: `agenda-client.tsx` con campo cupo + roster; `page.tsx` con los campos de contacto en la query.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-PATTERNS.md

# Editor de time_blocks + Dialog analog (self/exact)
@app/(dashboard)/agenda/agenda-client.tsx
# Query del server (self/exact)
@app/(dashboard)/agenda/page.tsx
# Tipos (capacity ya agregado en Plan 02)
@lib/types.ts
---
@.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campo "cupo" por bloque en la grilla + persistencia de capacity</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx</files>
  <read_first>
    - app/(dashboard)/agenda/agenda-client.tsx: línea 49 (type LocalBlock), 52-56 (defaultBlock), 130-138 (mapeo de initialTimeBlocks a dayStates), 241-264 (saveHours: delete-all + insert con el objeto toInsert), 555-589 (fila de edición del bloque: Inputs controlados con updateBlock)
    - 02-PATTERNS.md §agenda-client.tsx (campo capacity en la fila 557-583; extender LocalBlock, defaultBlock, mapeo, toInsert)
    - lib/types.ts (TimeBlock.capacity tras Plan 02)
  </read_first>
  <action>
1. Extender el type `LocalBlock` (línea 49): agregar `capacity: number`.

2. `defaultBlock` (52-56): cada rama devuelve `capacity: 1` (default = comportamiento individual).

3. Mapeo de `initialTimeBlocks` a `dayStates` (líneas 130-138, dentro del `.map(b => ({...}))`): agregar `capacity: b.capacity ?? 1`.

4. En la fila de edición del bloque (entre los Inputs de label y el botón de eliminar, ~líneas 571-583): agregar un `<Input type="number" min={1} value={block.capacity} onChange={e => updateBlock(day, idx, 'capacity', Math.max(1, Number(e.target.value) || 1))} className="w-20 text-sm" title="Cupo (lugares)" placeholder="Cupo" />` con un `<span>` o label accesible "Cupo" cerca. Validación inline: nunca < 1 (consistente con el CHECK de la migración). Reusar el patrón visual de los Inputs hermanos (mismas clases de tamaño/estilo) para consistencia.

5. `saveHours` (241-264): el objeto `toInsert` (línea 247 declara el tipo; 253 hace el push) debe incluir `capacity`. Agregar `capacity: number` al tipo del array y `capacity: b.capacity || 1` al objeto pusheado. El delete-all + insert ya borra y reinserta todos los bloques; capacity viaja en el insert.

6. `updateBlock` debe aceptar la key `'capacity'` con valor numérico (revisar su firma/genérico; si es `(day, idx, key, value)` con value string, ajustar para number o castear en el handler). Comentar en español que cupo 1 = bloque individual (cero cambio de comportamiento).

NO tocar `settings-client.tsx` (no edita time_blocks).
  </action>
  <verify>
    <automated>grep -c "capacity" "app/(dashboard)/agenda/agenda-client.tsx" | { read n; [ "$n" -ge 4 ] && echo OK || { echo "capacity insuficiente en agenda-client"; exit 1; }; } && npx tsc --noEmit 2>&1 | grep -q "agenda-client" && { echo "tsc error"; exit 1; } || echo tsc-ok</automated>
  </verify>
  <acceptance_criteria>
    - LocalBlock, defaultBlock, el mapeo de initialTimeBlocks y toInsert incluyen capacity.
    - La fila de edición tiene un Input number (min 1) para el cupo con validación >= 1.
    - saveHours persiste capacity en el insert de time_blocks.
    - `npx tsc --noEmit` limpio en agenda-client.tsx.
  </acceptance_criteria>
  <done>El dueño edita el cupo por bloque y se guarda; cupo 1 es el default e idéntico al comportamiento actual.</done>
</task>

<task type="auto">
  <name>Task 2: Roster del admin — overlay con contador 8/15 + lista de inscriptos</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx, app/(dashboard)/agenda/page.tsx</files>
  <read_first>
    - app/(dashboard)/agenda/page.tsx líneas 28-43 (Promise.all; el select de appointments línea 32-38 con client_name/status, por business_id)
    - app/(dashboard)/agenda/agenda-client.tsx líneas 736-766 (Dialog "Copiar horario": open/onOpenChange + DialogContent/DialogHeader/DialogTitle — patrón a espejar) + la grilla donde se renderizan los appointments del día (el click target del slot)
    - components/ui/drawer.tsx (Drawer/DrawerContent/DrawerHeader/DrawerTitle de vaul, ya instalado, sin estrenar) y components/ui/dialog.tsx
    - 02-CONTEXT.md D-04 (drawer mobile / panel desktop; contador + nombre/contacto/estado) + 02-PATTERNS.md §agenda-client.tsx (roster: espejar Dialog 736-766; usar drawer en mobile; computar de initialAppointments vs capacity)
  </read_first>
  <action>
1. En `page.tsx`: extender el `select` de appointments (línea 33) para incluir los campos de contacto que el roster muestra: agregar `client_phone, client_email` (el roster pide contacto, D-04). Conservar `.eq('business_id', business.id)` y el resto de la query SIN cambio. Actualizar el tipo `AgendaAppt` (en agenda-client.tsx, exportado) con `client_phone?: string | null` y `client_email?: string | null` si no los tiene.

2. En `agenda-client.tsx`: agregar estado `const [rosterSlot, setRosterSlot] = useState<{ date: string; time: string; locationId: string | null; professionalId?: string | null } | null>(null)`. Al hacer click en un slot que tiene inscriptos (o que pertenece a un bloque con capacity > 1), setear `rosterSlot` con la info del slot. El click target es la celda/slot de la grilla de agenda donde hoy se muestran los turnos del día.

3. Computar el contador y el roster del slot seleccionado a partir de `initialAppointments` (ya cargados por el server, filtrados por business_id): filtrar los appointments cuyo `date`/`time` (y bucket de profesional si aplica) coinciden con `rosterSlot`, en estados que ocupan (`confirmed`/`pending_payment`). El contador = `inscriptos.length` / `capacity del bloque que cubre el slot` (ej. "8/15"). Resolver la capacity buscando el `time_block` del día/ventana en `initialTimeBlocks` (mismo criterio que el RPC: day_of_week + ventana, MAX).

4. Renderizar el overlay espejando el patrón Dialog de las líneas 736-766: `<Dialog open={rosterSlot !== null} onOpenChange={open => { if (!open) setRosterSlot(null) }}>` con `<DialogContent>`, `<DialogHeader><DialogTitle>` mostrando el horario + contador "8/15", y el cuerpo = lista de inscriptos (cada uno: nombre `client_name`, contacto `client_phone`/`client_email`, y un badge de estado confirmado/seña pendiente derivado de `status` — reusar la función de color de estado ya presente arriba del archivo). Para mobile, usar el `Drawer` de `@/components/ui/drawer` (vaul, ya instalado) con la MISMA estructura de estado (`rosterSlot !== null` → abierto) — un patrón responsive (Dialog en desktop, Drawer en mobile) o el Drawer para ambos, a discreción del executor mirando cómo el repo distingue mobile (no introducir librería nueva, D-04).

5. Empty state del roster: si el slot no tiene inscriptos, mostrar "0/{capacity} · sin inscriptos aún" (no dejar el overlay vacío). El contador refleja la ocupación real del slot.

Todo filtrado por `business_id` (los appointments ya vienen filtrados del server). NO exponer datos de otro tenant. El roster es dato exclusivo del admin (corre con la sesión del dueño).
  </action>
  <verify>
    <automated>grep -cE "rosterSlot|Drawer|DialogTitle" "app/(dashboard)/agenda/agenda-client.tsx" | { read n; [ "$n" -ge 2 ] && echo OK || { echo "falta overlay roster"; exit 1; }; } && grep -c "client_phone" "app/(dashboard)/agenda/page.tsx" | { read n; [ "$n" -ge 1 ] && echo OK || { echo "page no trae contacto"; exit 1; }; } && npx tsc --noEmit 2>&1 | grep -qE "agenda-client|agenda/page" && { echo "tsc error"; exit 1; } || echo tsc-ok</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx trae client_phone/client_email en el select de appointments, por business_id.
    - Al click en un slot con inscriptos, se abre un overlay (Dialog/Drawer) con contador "N/capacity" + lista (nombre, contacto, estado).
    - El overlay reusa @/components/ui/dialog y/o @/components/ui/drawer — sin dependencias nuevas.
    - Empty state presente para slots sin inscriptos.
    - `npx tsc --noEmit` limpio en ambos archivos.
  </acceptance_criteria>
  <done>El dueño hace click en un slot grupal y ve el contador de ocupación + el roster (nombre, contacto, estado).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual del campo cupo + roster (desktop + mobile)</name>
  <what-built>El campo "cupo" por bloque en el editor de horarios de /agenda y el overlay de roster (contador 8/15 + lista de inscriptos) al hacer click en un slot grupal.</what-built>
  <how-to-verify>
1. Iniciar el dev server (`npm run dev`) y entrar a `/agenda` como dueño.
2. En el editor de horarios, verificar que cada bloque tiene un campo "Cupo" (number, min 1, default 1). Cambiar el cupo de un bloque a 15 y Guardar; recargar y confirmar que el 15 persiste.
3. Crear (o tener) varios turnos en un mismo slot grupal (mismo día/hora) desde "Nuevo turno".
4. Hacer click en ese slot en la agenda: debe abrirse un overlay con el contador "N/15" y la lista de inscriptos (nombre, contacto, estado confirmado/seña pendiente).
5. En mobile (DevTools responsive 375px), el overlay debe verse como drawer/panel usable (touch targets >= 44px), no roto.
6. Verificar que el público (/[slug]) NO ve el contador ni cuántos lugares quedan — solo "disponible" hasta llenarse.
  </how-to-verify>
  <action>Checkpoint humano: correr `npm run dev` y verificar manualmente en /agenda (desktop + mobile) que el campo cupo persiste tras guardar+recargar, que el click en un slot grupal abre el overlay con contador "N/capacity" + roster (nombre, contacto, estado), que el overlay es usable en mobile (drawer), y que el público no ve lugares restantes — según los pasos de how-to-verify.</action>
  <verify><human-check>El dueño confirma que el cupo persiste, el roster muestra contador + inscriptos, el overlay es usable en mobile y el público no ve lugares restantes.</human-check></verify>
  <done>El campo cupo persiste, el roster funciona en desktop y mobile, y el público no ve lugares restantes.</done>
  <resume-signal>Escribir "approved" o describir los problemas visuales/funcionales.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño autenticado → agenda (anon+RLS) | el roster y el campo cupo corren con la sesión del dueño, filtrados por business_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Information Disclosure | el roster filtra inscriptos de otro tenant | mitigate | initialAppointments ya viene filtrado por `.eq('business_id', business.id)` en page.tsx; el roster solo filtra en memoria sobre ese set. |
| T-02-14 | Tampering | editar capacity de un bloque de otro negocio | mitigate | saveHours hace delete-all + insert con `.eq('business_id', business.id)`; la policy WITH CHECK de time_blocks (Plan 01) lo refuerza en la DB. |
| T-02-15 | Information Disclosure | exponer contacto del cliente innecesariamente | accept | El contacto (phone/email) es dato propio del negocio sobre SUS clientes; el roster es del admin. Aceptable (mismo criterio que la agenda actual que ya muestra client_name). |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | Sin paquetes nuevos (D-04: reusa dialog/drawer ya instalados). Sin checkpoint de legitimidad necesario. |
</threat_model>

<verification>
- `grep` confirma capacity en la grilla + persistencia, y rosterSlot/Drawer/DialogTitle en el overlay.
- `grep` confirma client_phone en la query del server.
- `npx tsc --noEmit` limpio.
- Checkpoint humano: el campo cupo persiste, el roster muestra contador + lista, mobile usable, el público no ve lugares restantes.
</verification>

<success_criteria>
- El dueño edita el cupo por bloque y persiste.
- El roster muestra contador 8/15 + inscriptos (nombre, contacto, estado) en un overlay reusado.
- Sin dependencias nuevas; aislamiento por tenant intacto.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/02-cupos-grupales/02-04-SUMMARY.md` when done.
</output>
