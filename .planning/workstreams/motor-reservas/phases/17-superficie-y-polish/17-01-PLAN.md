---
phase: 17-superficie-y-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/settings/settings-client.tsx
autonomous: true
requirements: [CUPO-09]

must_haves:
  truths:
    - "El dueño ve, sin tocar ningun control, el eje de conteo + un ejemplo + que sale mal de cada modo de cupo (D-01)"
    - "Las tres explicaciones estan en pantalla A LA VEZ: leer no escribe en el formulario (D-02)"
    - "A 375px las tres opciones de modo se ven una por fila, sin caja dentada, con 44px de alto (D-13)"
    - "El campo 'Cuantos lugares' se puede vaciar y tipear otro numero; se corrige solo al SALIR del campo (D-06)"
    - "Los labels siguen siendo Individual / Clase grupal / Recurso simultaneo y ahora viven en UN solo lugar (D-03)"
    - "Los ejemplos son fijos para todos los verticales: no se agrega ninguna clave a lib/verticals.ts (D-04)"
  artifacts:
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "CAPACITY_MODE_HELP (fuente unica de label+eje+ejemplo+advertencia), el explicador de tres grupos, el radiogroup en grid y el input de cupo con commit en onBlur"
      contains: "CAPACITY_MODE_HELP"
  key_links:
    - from: "CAPACITY_MODE_HELP"
      to: "el radiogroup de modo"
      via: "los labels de los toggles salen del MISMO array que las explicaciones (D-03: un label, un lugar)"
      pattern: "CAPACITY_MODE_HELP"
    - from: "cada role=radio del radiogroup"
      to: "el grupo del explicador de ese modo"
      via: "aria-describedby apunta al id del grupo, para que el lector de pantalla lea la explicacion al enfocar la opcion"
      pattern: "cap-mode-help-"
---

<objective>
Que el editor de servicio **explique** la diferencia entre "Clase grupal" y "Recurso simultáneo" en
vez de nombrarlas, y cerrar dos de los tres defectos que levantó la UAT: el campo de cupo que no se
puede editar con el teclado y los toggles desacomodados.

Purpose: la Phase 15 volvió el modo **declarable** y la 070 lo dejó bloqueado en la dirección
correcta, pero en pantalla los dos modos de cupo compartido siguen metidos en la misma bolsa
(*"varios lugares por turno"*). El dueño elige a ciegas, y elegir mal tiene consecuencias reales que
la base ya no puede corregir sola: alguien se suma a mitad de clase, o se le llena la agenda antes de
tiempo. La explicación es el requisito, no el adorno.

Output: `CapacityModeFields` reescrito — radiogroup en grid determinista, bloque explicativo de tres
grupos siempre visibles, y el input de cupo con commit en `onBlur`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-UI-SPEC.md
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-PATTERNS.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<constraints>
- **Ejecución sobre `main`, sin worktree.** Este plan toca **un solo archivo**:
  `app/(dashboard)/settings/settings-client.tsx`. Si te encontrás editando `agenda-client.tsx`,
  `finances-client.tsx`, `components/ui/dialog.tsx` o `lib/verticals.ts`, te fuiste de alcance.
- **`17-CONTEXT.md` (D-01…D-13) y `17-UI-SPEC.md` están LOCKED.** Se implementan, no se rediscuten.
  La redacción fina de la prosa es discreción tuya; las tres capas, los ejemplos y los labels **no**.
- **Alcance del realineado: SOLO el bloque de modo (D-13).** El grupo "Se ofrece en" comparte el
  patrón de pills envolviendo y **queda exactamente como está**, en el alta y en el diálogo.
- **NUNCA `npx tsc`** (baja un paquete que no es el compilador y siempre sale 0) →
  `./node_modules/.bin/tsc --noEmit`.
- **`npx vitest run` completo NO es un gate útil** en esta máquina (las suites de abono son flaky en
  paralelo). Este plan no toca `lib/` ni `test/`: no hay suite que correr.
- `grep -ciF` aborta en este Git Bash (exit 134). Usar `-cF` **o** `-ciE`, nunca los tres flags.
- **Cero hex suelto**: todo color por token del design system (`--warning`, `--foreground`,
  `--muted-foreground`, `--primary`, `--secondary`, `--border`).
- **Mobile-first 375px**, targets ≥ 44px en mobile (`min-h-11`, liberado con `sm:` donde el UI-SPEC lo
  autoriza), contraste WCAG AA, `lucide-react` como único sistema de iconos.
- Comentarios densos **en español**, explicando el *por qué*, no el *qué*.
- ⚠ **No reproduzcas en un comentario el texto de la copy vieja que borrás.** Un criterio de
  aceptación de la Task 2 cuenta ese texto y un comentario que lo cite lo hace fallar sobre código
  correcto. Referite a la línea vieja por su ubicación, no por su contenido.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: El radiogroup pasa a grid determinista y los labels dejan de vivir sueltos</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - `settings-client.tsx:215-300` — `CapacityModeFields` entero: el array local de opciones, el
      contenedor del radiogroup, el `onClick` que patchea modo + cupo juntos, el guard `blocked` del
      espacio compartido y el aviso `--warning` de abajo.
    - `17-UI-SPEC.md` §2.1 — las clases exactas del grid y de los botones (están completas ahí).
    - `17-CONTEXT.md` D-03 y D-13 — por qué los labels no se renombran y por qué solo se realinea
      este bloque.
  </read_first>
  <action>
    Dos cambios, en este orden.

    **(a) Fuente única de los tres modos.** A nivel de módulo, junto a `minCapacityFor` /
    `MAX_CAPACITY`, declarar una constante `CAPACITY_MODE_HELP`: un array tipado de tres entradas en
    el orden `individual` → `group_class` → `simultaneous_resource` (el mismo de hoy: individual va
    primero porque es el DEFAULT de la base y el 100 % de producción). Cada entrada lleva `key`
    (`CapacityMode`), `label`, `axis`, `example` y `warning` opcional. Los valores de texto salen
    **literales** de la tabla de `17-UI-SPEC.md` §7.1; `individual` va **sin** `warning` (D-01: no
    tiene contra qué equivocarse) pero **sí** con `example`.

    Comentar arriba por qué existe: es la aplicación a la pantalla de la misma lección que la Phase 15
    aplicó en la base — el label vive en UN lugar. Hoy está duplicado de hecho (array de opciones +
    la línea de copy de abajo) y D-03 lo prohíbe renombrar justamente porque arrastra la copy del
    rechazo del gate, el aviso de espacio compartido y los comentarios del código.

    Borrar el array local de opciones de `CapacityModeFields` y mapear el radiogroup sobre
    `CAPACITY_MODE_HELP`, leyendo `key` y `label`. **El `onClick` no cambia**: sigue patcheando modo y
    cupo juntos (subir el cupo al piso legal en el mismo estado es lo que evita rebotar contra
    `services_capacity_matches_mode_chk`). El guard `blocked` del espacio compartido tampoco cambia.

    **(b) Grid determinista (D-13).** El contenedor del radiogroup deja de ser un flex que envuelve y
    pasa a la forma prescriptiva del UI-SPEC §2.1: `grid grid-cols-1 gap-1 rounded-md border
    border-border p-1 sm:grid-cols-3`, conservando `role="radiogroup"` y su `aria-label`. Los botones
    pasan a `w-full min-h-11 sm:min-h-0 sm:h-9 px-3 rounded text-sm font-medium` y conservan el
    `bg-primary text-primary-foreground` en activo, el `text-muted-foreground hover:text-foreground`
    en reposo, el `transition-colors`, el `disabled:opacity-60`, el `cursor-not-allowed` del bloqueado
    y el anillo de foco que ya tienen.

    Agregar a cada botón `aria-describedby={`cap-mode-help-${key}`}` — el destino lo crea la Task 2.
    Dejar anotado en un comentario que el destino llega en el bloque de abajo, para que el atributo no
    parezca huérfano mientras se lee el diff.

    A 375px esto da tres filas de 44px, lectura vertical que rima con los tres grupos del explicador;
    en desktop tres columnas iguales donde "Recurso simultáneo" entra en una línea.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF 'CAPACITY_MODE_HELP' "app/(dashboard)/settings/settings-client.tsx"` >= **2**
      (la declaración + el map del radiogroup; sube a 3 con la Task 2).
    - `grep -cF "label: 'Clase grupal'" "app/(dashboard)/settings/settings-client.tsx"` == **1** —
      D-03: el label existe una sola vez en el archivo.
    - `grep -cF 'grid-cols-1 gap-1 rounded-md border border-border p-1 sm:grid-cols-3' "app/(dashboard)/settings/settings-client.tsx"` == **1**.
    - `grep -cF 'inline-flex flex-wrap gap-1 rounded-md' "app/(dashboard)/settings/settings-client.tsx"` == **0** — el contenedor viejo del radiogroup ya no existe.
    - `grep -cF 'cap-mode-help-' "app/(dashboard)/settings/settings-client.tsx"` >= **1**.
    - `grep -cF 'role="radiogroup"' "app/(dashboard)/settings/settings-client.tsx"` == **1** y
      `grep -cF 'role="radio"' "app/(dashboard)/settings/settings-client.tsx"` == **1** (sigue siendo
      un solo map).
    - El grupo "Se ofrece en" **no** se tocó: `git diff -U0 -- "app/(dashboard)/settings/settings-client.tsx" | grep -cF 'Se ofrece en'` == **0**.
  </acceptance_criteria>
  <done>Los tres modos salen de un solo array, el radiogroup es un grid que no puede dentarse, y cada opción ya apunta a su explicación.</done>
</task>

<task type="auto">
  <name>Task 2: El bloque explicativo — tres grupos paralelos, los tres visibles (D-01/D-02/D-04)</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - `17-UI-SPEC.md` §2.2 — el markup de un grupo está escrito ahí carácter por carácter (riel,
      tamaños, tokens, orden de las capas) y §7.1 tiene la tabla de copy.
    - `settings-client.tsx:270-277` — el aviso `--warning` del espacio compartido: **ese** es el molde
      exacto de la capa de advertencia (mismas clases, mismo icono, mismo tamaño). No inventes otro.
    - `17-CONTEXT.md` D-01, D-02, D-04 — las tres capas, por qué los tres se ven a la vez, y por qué
      los ejemplos son fijos para todos los rubros.
  </read_first>
  <action>
    Insertar el bloque explicativo **debajo** del radiogroup y del aviso de espacio compartido, en el
    lugar que hoy ocupa la línea de copy que nombra los tres modos y dice de dónde sale el número
    (`settings-client.tsx:281-284`). **Esa línea se borra entera**: el bloque nuevo la reemplaza, no
    la acompaña. No cites su texto en ningún comentario (ver constraints).

    Contenedor: `rounded-md border border-border bg-secondary/30 p-3 space-y-2.5`. Adentro, un map
    sobre `CAPACITY_MODE_HELP` que renderiza **los tres grupos siempre**, en el orden del array
    (mismo orden que los toggles). Cada grupo:

    - `<div id={`cap-mode-help-${key}`} className={cn('border-l-2 pl-3 space-y-0.5', activo ? 'border-l-primary' : 'border-l-border')}>`
    - Label: `text-sm font-medium`, `text-foreground` si es el modo activo, `text-muted-foreground` si no.
    - Eje: `text-xs text-muted-foreground`.
    - Ejemplo: `text-xs text-muted-foreground`, con el prefijo `Ej:` en `font-medium text-foreground/80`.
      Sin itálica (la marca no la contempla).
    - Advertencia, **solo si la entrada tiene `warning`**: el molde de `settings-client.tsx:270-277` —
      `flex items-start gap-1.5 text-xs text-warning` + `<TriangleAlert aria-hidden="true" className="w-3.5 h-3.5 flex-shrink-0 mt-px" />` + el texto en un `<span>`.

    Tres cosas que son parte del requisito y no detalles:

    1. **El bloque es descriptivo, NO interactivo.** Sin `onClick`, sin `role="button"`, sin
       `tabIndex`. D-02 lo pide explícito: cada toque de un toggle **escribe** en el formulario (el
       handler manda modo + cupo juntos), así que si la explicación viviera detrás de un click, leer
       tendría efecto de escritura. Dejalo escrito en un comentario, porque es exactamente el atajo
       que va a proponer el próximo que lea esto.
    2. **El estado activo se marca SOLO con color** (riel + label). No cambia peso ni tamaño → cero
       reflow al cambiar de modo.
    3. **Los pasos de 2px (`space-y-0.5`) y 10px (`space-y-2.5`) son deliberados.** Son la excepción
       de escala que `17-UI-SPEC.md` declara y mide (79 + 57 usos en el repo, 31 en los dos archivos
       de esta fase): la distancia entre grupos es 5× la distancia interna, y es eso lo que hace que
       nueve líneas se lean como tres grupos. **No los "corrijas" a múltiplos de 4.**

    `individual` queda con 3 líneas y los otros dos con 4 (D-01 le saca solo la advertencia, no el
    ejemplo): se ve como una familia, no como un huérfano.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF 'varios lugares por turno' "app/(dashboard)/settings/settings-client.tsx"` == **0** —
      la línea que metía los dos modos de cupo compartido en la misma bolsa ya no existe (ni en código
      ni citada en un comentario).
    - `grep -cF 'border-l-2 pl-3' "app/(dashboard)/settings/settings-client.tsx"` == **1** — un solo
      template para los tres grupos, no tres bloques copiados.
    - `grep -cF 'bg-secondary/30' "app/(dashboard)/settings/settings-client.tsx"` == **1**.
    - `grep -cF 'CAPACITY_MODE_HELP' "app/(dashboard)/settings/settings-client.tsx"` >= **3**
      (declaración + radiogroup + explicador).
    - `grep -cF 'TriangleAlert' "app/(dashboard)/settings/settings-client.tsx"` == **6** (los 5 de hoy
      + la capa de advertencia del explicador).
    - `grep -cF 'cap-mode-help-' "app/(dashboard)/settings/settings-client.tsx"` == **2** — el
      `aria-describedby` de la Task 1 y el `id` de acá, los dos derivados de la misma `key`.
    - `grep -cF 'yoga de 9:00' "app/(dashboard)/settings/settings-client.tsx"` == **1** y
      `grep -cF '3 camillas' "app/(dashboard)/settings/settings-client.tsx"` == **1** — los ejemplos
      fijos de D-04 están, una sola vez cada uno.
    - `git diff -- lib/verticals.ts` **vacío**: D-04 prohíbe agregar claves por vertical.
    - Leído a ojo sobre el diff: dentro del bloque explicativo no hay `onClick`, `role=` ni `tabIndex`.
  </acceptance_criteria>
  <done>Los tres modos se explican con eje + ejemplo + qué sale mal, los tres a la vez, sin que leer escriba nada.</done>
</task>

<task type="auto">
  <name>Task 3: El campo "Cuántos lugares" se deja vaciar y se corrige al salir (D-06)</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - `settings-client.tsx:284-297` — el campo de cupo actual: el handler del input normaliza en cada
      tecla, y **esa** llamada es la causa del defecto que levantó la UAT (vaciarlo lo reescribe al
      piso en la misma tecla).
    - `app/(dashboard)/web/_sections/section-forms.tsx:120-185` — `NumberField`: el molde literal de
      texto local + commit en `onBlur`. **Portar la lógica, no reescribirla.**
    - `settings-client.tsx:135-150` — `minCapacityFor` y `MAX_CAPACITY`, ya existentes.
    - `17-UI-SPEC.md` §2.3.
  </read_first>
  <action>
    Dentro de `CapacityModeFields`, el campo de cupo pasa a tener **estado de texto local** como
    fuente de verdad mientras el foco está adentro:

    - `const [capacityText, setCapacityText] = useState(String(capacity))`.
    - El handler de cambio escribe **siempre** el string crudo al estado local. Si el string está
      vacío o no parsea a número finito, **no** propaga nada al formulario. Si parsea, propaga el
      número **tal cual, sin clampear**. ⚠ Propagar el valor clampeado es exactamente lo que reescribe
      el campo en la misma tecla: el padre cambiaría el prop y el input se resincronizaría encima de
      lo que se está tipeando. El clamp vive en el `onBlur` y, como última línea, en el payload del
      guardado (`saveEditService` / `addService` ya normalizan con el piso del modo).
    - `onBlur`: normaliza lo que quedó tipeado con `normalizeCapacity(n, minCapacityFor(value))` —
      vacío o basura vuelven al valor vigente del prop —, escribe el resultado al estado local y lo
      propaga al formulario.
    - **Resincronización desde afuera**: el toggle de modo sube el cupo a su piso, así que el texto
      tiene que seguir al prop cuando el cambio viene de otro control. Usar un `useRef<boolean>` que
      marque "el input tiene el foco" (se prende en `onFocus`, se apaga en `onBlur`) y un `useEffect`
      sobre `capacity` que **salte** la resincronización mientras ese ref está en `true`. Sin ese
      guard, tipear `007` se reescribe a `7` bajo el cursor.
    - Conservar el `onFocus` que selecciona el contenido.

    Atributos del input (UI-SPEC §2.3): `inputMode="numeric"` para el teclado numérico en mobile,
    `tabular-nums`, `h-11 sm:h-8`, spinners nativos ocultos
    (`[appearance:textfield] [&::-webkit-inner-spin-button]:appearance-none [&::-webkit-outer-spin-button]:appearance-none`),
    `max-w-[11rem]` en el contenedor y el `Label` visible como hoy. Mantener `min`/`max`/`step` como
    pista del navegador, sabiendo que **no** son la validación: la autoridad sigue siendo el CHECK de
    la 068 y su espejo de UX es `minCapacityFor`.

    Dejar comentado el porqué del `onBlur`: es el patrón de validación inline que el proyecto ya usa
    en sus formularios, y es la diferencia entre "no me deja borrar el 2" y "puedo escribir el número
    que quiero".
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cE 'normalizeCapacity\(parseInt' "app/(dashboard)/settings/settings-client.tsx"` == **0** —
      la normalización por tecla del campo de cupo ya no existe.
    - `grep -cF 'onBlur' "app/(dashboard)/settings/settings-client.tsx"` == **1** (hoy hay **0**).
    - `grep -cF 'inputMode="numeric"' "app/(dashboard)/settings/settings-client.tsx"` == **1**.
    - `grep -cF 'minCapacityFor' "app/(dashboard)/settings/settings-client.tsx"` >= **3** — el piso por
      modo sigue siendo el que decide, ahora al salir del campo.
    - `grep -cF 'useRef' "app/(dashboard)/settings/settings-client.tsx"` == **4** (los 3 de hoy + el
      guard de foco).
    - Prueba manual mínima en `npm run dev`, anotada en el SUMMARY: en el diálogo de un servicio
      grupal, borrar el contenido del campo deja el campo **vacío** (no vuelve a `2` bajo el cursor);
      tipear `6` deja `6`; hacer Tab afuera con el campo vacío lo devuelve a `2`.
  </acceptance_criteria>
  <done>Se puede borrar el número y escribir otro; el piso del modo se aplica al salir, no bajo el cursor.</done>
</task>

</tasks>

<artifacts>
## Artifacts que produce este plan

| Símbolo / elemento | Dónde vive | Qué provee |
|---|---|---|
| `CAPACITY_MODE_HELP` | `settings-client.tsx` (módulo) | Fuente única de `key` + `label` + `axis` + `example` + `warning?` de los tres modos (D-01/D-03/D-04) |
| Bloque explicativo de tres grupos | `settings-client.tsx` → `CapacityModeFields` | Las tres capas de D-01, siempre visibles (D-02), con riel de paralelismo |
| Radiogroup en grid | `settings-client.tsx` → `CapacityModeFields` | El realineado de D-13, con `aria-describedby` hacia el explicador |
| `capacityText` + guard de foco | `settings-client.tsx` → `CapacityModeFields` | El commit en `onBlur` de D-06 |
| ids `cap-mode-help-{modo}` | `settings-client.tsx` | Enlace a11y radio → explicación |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño autenticado → su propio `services` | esta fase **no** cambia el camino de escritura del diálogo; `saveEditService` sigue siendo el único write path de este plan |
| copy de la base → pantalla del dueño | el texto de un `RAISE` de un gate no puede llegar crudo a la UI (T-14-25 / T-13-09) |
| formulario → CHECK `services_capacity_matches_mode_chk` | el editor no puede producir una combinación modo↔cupo que la base rechace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | el input de cupo deja de clampear en cada tecla (D-06) ⇒ el formulario puede sostener transitoriamente un cupo ilegal para su modo | mitigate | El clamp no se elimina, se **mueve**: `normalizeCapacity(n, minCapacityFor(value))` corre en `onBlur` y, como última línea, en el payload de `saveEditService`/`addService`, que ya lo hacen hoy y **no se tocan en este plan**. La autoridad sigue siendo el CHECK de la 068; el helper es su espejo de UX, nunca su reemplazo |
| T-17-02 | Tampering | `MAX_CAPACITY` (99) evita que un número pegado viaje al UPDATE y vuelva como `22003 smallint out of range` | mitigate | El techo se conserva en la misma llamada de `onBlur` y en el payload; el criterio de grep sobre `minCapacityFor` prueba que el piso siguió atado al modo |
| T-17-03 | Information Disclosure | la copy nueva del explicador podría describir el rechazo del gate con palabras propias y divergir de la copy real | mitigate | El explicador **no habla del gate**: describe el eje de conteo, un ejemplo y el error de elección. La copy del rechazo (`settings-client.tsx:809`) **no se toca** y sigue siendo la única cadena de ese caso |
| T-17-04 | Tampering | el `onClick` del toggle patchea modo + cupo juntos; separarlos reintroduce el rebote contra el CHECK que 15-02 cerró | mitigate | El handler **no se modifica** en este plan; el criterio de aceptación de la Task 1 exige que el radiogroup siga teniendo un solo `role="radio"` derivado del mismo array, y el diff del handler queda a la vista |
| T-17-SC | Tampering (supply chain) | dependencias | accept | **Cero** paquetes nuevos: `tw-animate-css`, `lucide-react` y shadcn ya están instalados. `git diff -- package.json package-lock.json` debe quedar **vacío**; no aplica el gate de legitimidad |
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` exit **0**.
2. `git diff --name-only` toca **exactamente** `app/(dashboard)/settings/settings-client.tsx`.
3. `git diff -- package.json package-lock.json lib/verticals.ts components/ui/dialog.tsx` **vacío**.
4. Los conteos de grep de las tres tasks dan los valores exactos declarados.
5. A 375px (devtools, `npm run dev`, `/settings` → tab Servicios): las tres opciones de modo se ven
   una por fila con 44px de alto, y los tres grupos del explicador se leen como tres bloques
   paralelos, no como nueve líneas sueltas.
6. El campo de cupo se puede vaciar; al salir vuelve al piso del modo.
</verification>

<success_criteria>
- El editor **explica** la diferencia entre los dos modos de cupo compartido con eje + ejemplo + qué
  sale mal, y las tres explicaciones se ven a la vez sin tocar nada.
- Los labels no cambiaron y ahora viven en un solo lugar del archivo.
- El campo de cupo se puede editar con el teclado.
- El bloque de modo quedó alineado a 375px; el grupo "Se ofrece en" no se tocó.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-01-SUMMARY.md` when done
</output>
