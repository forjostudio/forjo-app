---
phase: 17-superficie-y-polish
plan: 05
type: execute
wave: 2
depends_on: ["17-04"]
files_modified:
  - app/(dashboard)/agenda/agenda-client.tsx
autonomous: true
requirements: [POLISH-09]

must_haves:
  truths:
    - "La grilla de la agenda calcula la ocupacion desde services.capacity, la misma fuente que el motor (POLISH-09)"
    - "Ninguna lectura de la columna del dia decide el cupo o el modo por los bloques de horario"
    - "El modo se lee de services.capacity_mode; la deduccion por el numero ya no existe (D-11)"
    - "Una clase grupal ocupa UNA sola linea por horario, con un contador 3/6 y un solo elemento clickeable (D-10)"
    - "El contador se ve SIEMPRE, no solo al llenarse, y una vez por slot (D-10)"
    - "El grupal y el simultaneo muestran su ocupacion con el MISMO componente y los mismos tokens (POLISH-09)"
    - "Un turno sin servicio resoluble se ve como hoy: sin contador y sin roster (D-12)"
    - "El roster y la linea de grupo leen el MISMO objeto: no pueden mostrar numeros distintos"
  artifacts:
    - path: "app/(dashboard)/agenda/agenda-client.tsx"
      provides: "entriesByDate (consume buildDayEntries), OccupancyBadge compartido por los dos modos, la linea de grupo colapsada y el roster por servicio"
      contains: "OccupancyBadge"
  key_links:
    - from: "app/(dashboard)/agenda/agenda-client.tsx"
      to: "lib/agenda-occupancy.ts"
      via: "import de buildDayEntries / computeOverlapFull / capacityOf — la unica fuente del cupo y del modo"
      pattern: "@/lib/agenda-occupancy"
    - from: "la linea de grupo de la columna del dia"
      to: "el roster del slot"
      via: "rosterSlot lleva date + time + serviceId y el roster recupera LA MISMA entrada de grupo que se renderizo"
      pattern: "serviceId"
---

<objective>
Cablear la columna del día de la agenda al módulo puro de 17-04: borrar las dos lecturas viejas,
colapsar cada clase grupal en **una** línea con su contador, y darle al grupal el mismo tratamiento de
ocupación que ya tiene el simultáneo.

Purpose: acá se cierra POLISH-09 de verdad. Hoy `capacityFor()` (`agenda-client.tsx:467`) decide el
cupo con `time_blocks.capacity` —la columna que la 068 jubiló— y la línea siguiente deduce el modo del
número, a un renglón de `isSimultaneous`, que ya lo lee bien. Y la asimetría que el requisito viene a
cerrar no es que al grupal le falte un badge: es que **el simultáneo tiene tratamiento de ocupación y
el grupal no tiene ninguno**, aunque el grupal es el modo donde el dueño más necesita saber cuántos
lugares quedan.

⚠ **La agrupación por slot no tiene analog en este repo** (`17-PATTERNS.md` lo dice explícito): hoy
cada turno es su propio item del `.map` y cada chip grupal es clickeable por separado. Es cambio de
**estructura** de la columna, no un badge más, y es uno de los dos lugares de la fase donde el
ejecutor va a improvisar. Por eso los criterios de aceptación de este plan son los más explícitos de
la fase.

Output: `agenda-client.tsx` consumiendo `lib/agenda-occupancy.ts`, con `OccupancyBadge` y la línea de
grupo colapsada.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-UI-SPEC.md
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-PATTERNS.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<constraints>
- **Depende de 17-04**, que ya dejó `lib/agenda-occupancy.ts` con sus tests en verde. Este plan
  **consume** ese módulo; no reimplementa nada de su lógica ni la copia.
- Este plan toca **un solo archivo**: `app/(dashboard)/agenda/agenda-client.tsx`. **No** se toca
  `app/(dashboard)/agenda/page.tsx`: la consulta del server ya trae la tabla `services` completa, así
  que `capacity` y `capacity_mode` ya están del lado del cliente. **No** se toca `lib/agenda-occupancy.ts`
  (si aparece un caso que el módulo no cubre, se corrige ahí **con su test**, no con una excepción en
  el componente).
- **Fuera de alcance, explícito:** "Copiar horario" (`agenda-client.tsx:1130`) y el diálogo del roster
  de desktop (`:1110`) tienen el mismo problema de alto potencial que 17-02 resolvió en el editor de
  servicio. **Quedan anotados, no tocados.**
- **Fuera de alcance, explícito:** "la agenda no sabe qué servicio se da en cada franja" es del tamaño
  de un milestone y ya está registrado como todo propio. Esta fase lo roza sin resolverlo: **no
  dejarse arrastrar.**
- **NUNCA `npx tsc`** → `./node_modules/.bin/tsc --noEmit`. Este plan cierra el archivo, así que
  además corre `npm run build`.
- **`npx vitest run` completo NO es un gate útil** acá (suites de abono flaky desde antes). La suite
  que importa es la de 17-04:
  `./node_modules/.bin/vitest run test/agenda-occupancy.test.ts --no-file-parallelism`, que tiene que
  seguir en verde **sin tocarla**.
- `grep -ciF` aborta en este Git Bash (exit 134). Usar `-cF` **o** `-ciE`, nunca los tres flags.
- Si dejás un comentario histórico nombrando la función vieja de cupo por bloque, **escribí el nombre
  sin paréntesis**: un criterio de aceptación cuenta las llamadas y un comentario con `()` lo hace
  fallar sobre código correcto.
- Mobile-first: a 375px la grilla es `grid-cols-2` (~170px por columna). Todo lo que se agregue tiene
  que caber ahí. Tokens del design system, cero hex suelto, `lucide-react` como único sistema de iconos.
- Comentarios densos **en español**, explicando el *por qué*.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Borrar las dos lecturas viejas y pasar a consumir el módulo puro (D-11 / D-12)</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx</files>
  <read_first>
    - `lib/agenda-occupancy.ts` tal como quedó en 17-04 — la firma exacta de `buildDayEntries`,
      `computeOverlapFull`, `capacityOf` y `occupiesSeat`, y la forma de `DayEntry`.
    - `agenda-client.tsx:60-80` — `OCCUPYING_STATUSES` y `timeToMin` locales.
    - `agenda-client.tsx:445-540` — `apptsByDate`, el `useCallback` del cupo por bloque, `serviceById`,
      `overlapFullById` y el `roster`.
    - `agenda-client.tsx:600-612` — dónde se consume `apptsByDate` en la grilla semanal.
    - `17-CONTEXT.md` D-11 y D-12 · `17-UI-SPEC.md` §4.1.
  </read_first>
  <action>
    **(a) Se van cuatro cosas del archivo.** Todas se reemplazan por el módulo de 17-04, ninguna se
    reescribe:

    1. El `useCallback` de `agenda-client.tsx:467` que calcula el cupo recorriendo los bloques de
       horario. **Se borra entero.** Esto es lo que cierra el desacuerdo que la Phase 15 dejó
       registrado como último residuo: era la **cuarta** lectura del cupo y la única que quedaba
       decidiendo por una columna que la 068 jubiló.
    2. `timeToMin` local (`:69`) — la versión buena vive ahora en el módulo. Si el archivo la sigue
       necesitando, se **importa**; no se deja una copia.
    3. `OCCUPYING_STATUSES` local (`:66`) — misma regla: sale del módulo.
    4. El cuerpo del `useMemo` de la ocupación por solape, que pasa a ser una llamada a
       `computeOverlapFull(initialAppointments, serviceById, Date.now())`. El nombre de la variable
       resultante puede quedar igual: lo que cambia es de dónde sale el cálculo.

    **(b) `entriesByDate` reemplaza a `apptsByDate`.** Un `useMemo` que agrupa los turnos por fecha y
    pasa cada día por `buildDayEntries(dayAppts, serviceById, nowMs)`. El `nowMs` se calcula **una vez**
    dentro del memo y se comparte con el cálculo de solape: si cada llamada leyera el reloj por su
    cuenta, dos partes de la misma pantalla podrían discrepar sobre si un hold ya venció.

    La grilla semanal pasa a leer `entriesByDate.get(ds)`, y el caso "sin turnos" mira la longitud de
    **las entradas**, no la de los turnos crudos (un día con turnos siempre produce al menos una
    entrada, así que el texto vacío no puede aparecer de más).

    **(c) El roster deja de leer el bloque y pasa a ser por servicio.** `rosterSlot` pasa a llevar
    `{ date, time, serviceId }`. El `roster` **no recalcula nada**: recupera de `entriesByDate` la
    entrada de grupo de esa fecha con esa hora y ese servicio, y usa su `occupied`, su `capacity` y su
    lista de turnos.

    Esto es deliberado y hay que dejarlo comentado: la línea de grupo y el roster leen **el mismo
    objeto**, así que es estructuralmente imposible que muestren números distintos. Dos consecuencias
    que van también al SUMMARY:
    - El roster ahora **filtra por servicio**, no solo por fecha y hora. Antes, dos clases distintas a
      la misma hora se mezclaban en la misma lista; con el cupo por servicio eso ya no puede ser.
    - La lista del roster puede tener **más filas que el contador** cuando hay holds vencidos o turnos
      que no ocupan lugar. Es correcto: el chip de estado de cada fila lo explica, y el contador dice
      cuántos lugares están realmente tomados. Anotarlo para la UAT.

    El título del roster suma el **nombre del servicio** además de la fecha y la hora.

    **(d) D-12, explícito.** Nada de esto se aplica a un turno sin `service_id` o cuyo servicio no
    resuelve: el módulo ya lo devuelve como entrada individual, y en pantalla eso es el chip de hoy,
    sin contador y sin roster. No agregues un fallback en el componente.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF "@/lib/agenda-occupancy" "app/(dashboard)/agenda/agenda-client.tsx"` == **1**.
    - `grep -cE 'capacityFor\s*\(' "app/(dashboard)/agenda/agenda-client.tsx"` == **0** — ninguna
      llamada ni definición de la función vieja de cupo por bloque sobrevive.
    - `grep -cF 'initialTimeBlocks' "app/(dashboard)/agenda/agenda-client.tsx"` == **4** (hoy **6**):
      las dos ocurrencias que desaparecen son el recorrido de los bloques y su array de dependencias.
      Las 4 que quedan son el tipo del prop, la desestructuración, el editor de horarios y `openDays`
      — ninguna decide cupo.
    - `grep -cF 'function timeToMin' "app/(dashboard)/agenda/agenda-client.tsx"` == **0** y
      `grep -cF 'const OCCUPYING_STATUSES' "app/(dashboard)/agenda/agenda-client.tsx"` == **0** — no
      quedó una segunda copia de ninguna de las dos.
    - `grep -cF 'entriesByDate' "app/(dashboard)/agenda/agenda-client.tsx"` >= **3** y
      `grep -cF 'const apptsByDate' "app/(dashboard)/agenda/agenda-client.tsx"` == **0** — el memo
      viejo ya no se declara (el criterio mira la **declaración**, así que un comentario histórico que
      lo nombre no lo rompe).
    - `grep -cF 'serviceId' "app/(dashboard)/agenda/agenda-client.tsx"` >= **2** — el roster viaja con
      el servicio.
    - `grep -cF 'Date.now()' "app/(dashboard)/agenda/agenda-client.tsx"` == **1** — un solo reloj para
      toda la pantalla.
    - `./node_modules/.bin/vitest run test/agenda-occupancy.test.ts --no-file-parallelism` sigue en
      **0 failed**, y `git diff -- lib/agenda-occupancy.ts test/agenda-occupancy.test.ts` está
      **vacío**: este plan consume el módulo, no lo ajusta para que le cierre.
  </acceptance_criteria>
  <done>La columna del día decide el cupo y el modo por el servicio; en el archivo no queda ninguna lectura del cupo por bloque ni ninguna copia de los helpers.</done>
</task>

<task type="auto">
  <name>Task 2: `OccupancyBadge` — un solo componente de ocupación para los dos modos</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx</files>
  <read_first>
    - `agenda-client.tsx:645-656` — el badge `N/M lleno` que ya existe: `Badge variant="outline"`,
      `h-4 gap-0.5 px-1 py-0 text-[9px] font-medium`, tokens `border-warning/30 bg-warning/10
      text-warning`, icono `Users` con `size-2.5!`, cifras con `tabular-nums`. **Es la extracción
      literal**: hoy solo lo consume el recurso simultáneo y solo cuando está lleno.
    - `17-UI-SPEC.md` §4.4 (la tabla de las cuatro situaciones y sus tokens) y §7.4 (la copy exacta).
    - `17-CONTEXT.md` D-10 — por qué el contador se ve siempre.
  </read_first>
  <action>
    Extraer `OccupancyBadge` como componente **interno de este archivo** (es uno de los dos componentes
    nuevos que el UI-SPEC autoriza en la fase). Props: `occupied`, `capacity`, `pendingDeposit` y un
    discriminador de alcance para el texto del `title` (el slot grupal habla del horario; el
    simultáneo, de los turnos que se pisan).

    **Un único badge, hasta dos segmentos, un solo color** — así el aviso no se duplica ni se abarata:

    | Situación | Contenido | Tokens |
    |---|---|---|
    | Normal | `3/6` con el icono | `border-border bg-secondary text-muted-foreground` |
    | Lleno (`occupied >= capacity`) | `6/6 lleno` | `border-warning/30 bg-warning/10 text-warning` |
    | Hay seña pendiente (umbral **>= 1**) | `3/6 · 1 sin seña` | warning |
    | Las dos | `6/6 lleno · 1 sin seña` | warning |

    - **Orden fijo: primero el cupo, después la plata.** El cupo es la pregunta de todos los días; el
      aviso de seña es la excepción.
    - `sin seña` es invariable (no se pluraliza), igual que `lugares` en el control de la tarjeta.
    - El color **nunca** es el único portador: el badge siempre lleva el icono `Users` y, cuando
      corresponde, la palabra. El texto va en `--warning` **puro**, nunca a opacidad (el 9px en negrita
      es texto pequeño y necesita el contraste completo).
    - `title` compuesto según §7.4, y la **misma información repetida en el `aria-label` del botón del
      grupo** (Task 3): en mobile no hay hover, así que el `title` no puede ser el único canal.
    - `tabular-nums` en las cifras: pasar de 9 a 10 no puede mover nada al lado.

    **Por qué el segmento "sin seña" es parte de la decisión y no un extra:** colapsar el grupo esconde
    el color ámbar por-persona que hoy se ve en cada chip `pending_payment`. Sin este segmento, la fase
    **perdería información que hoy está en pantalla**. Dejalo comentado.

    Consumidores, los dos:
    - La línea de grupo (Task 3): **siempre visible** (D-10).
    - El recurso simultáneo: **conserva su comportamiento actual** — el badge aparece solo cuando el
      intervalo ya alcanzó el cupo, porque el solape no se lee de un vistazo. Lo que cambia es que
      ahora sale del mismo componente y con los mismos tokens: eso es exactamente la armonización que
      pide POLISH-09. Su `title` mantiene el matiz de que el conteo es por turnos que se pisan.

    El badge `Fijo` del abono **no se toca** y **no aplica** a la línea de grupo: es propiedad de un
    turno, no del slot, y se ve en el roster.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF 'function OccupancyBadge' "app/(dashboard)/agenda/agenda-client.tsx"` == **1** y
      `grep -cF '<OccupancyBadge' "app/(dashboard)/agenda/agenda-client.tsx"` == **2** — lo consumen
      **los dos** modos, que es el punto del requisito.
    - `grep -cF 'text-[9px]' "app/(dashboard)/agenda/agenda-client.tsx"` == **2** (el badge extraído +
      el badge `Fijo`, que no se toca): la ocupación dejó de tener dos maquetas.
    - `grep -cF 'sin seña' "app/(dashboard)/agenda/agenda-client.tsx"` >= **1**.
    - `grep -cF 'tabular-nums' "app/(dashboard)/agenda/agenda-client.tsx"` >= **1**.
    - `grep -cF 'size-2.5!' "app/(dashboard)/agenda/agenda-client.tsx"` == **2** (el icono del badge de
      ocupación + el del badge `Fijo`).
    - `grep -cE 'bg-warning/10|border-warning/30' "app/(dashboard)/agenda/agenda-client.tsx"` >= **1** y
      `grep -cE '#[0-9a-fA-F]{6}' "app/(dashboard)/agenda/agenda-client.tsx"` == **0** — todo por
      token, cero hex suelto.
  </acceptance_criteria>
  <done>Existe un solo componente de ocupación, lo usan el grupal y el simultáneo, y el aviso de seña pendiente no se perdió al colapsar.</done>
</task>

<task type="auto">
  <name>Task 3: La línea de grupo colapsada — una fila, un contador, un solo clickeable (D-10)</name>
  <files>app/(dashboard)/agenda/agenda-client.tsx</files>
  <read_first>
    - `agenda-client.tsx:612-680` — el `.map` de la columna del día completo: el `chipClass`, el
      `chipBody`, la rama que hoy convierte los chips grupales en botones que abren el roster, y el
      `<div>` de los demás.
    - `17-UI-SPEC.md` §4.2 y §4.3 — la estructura y el markup exactos de la línea de grupo.
    - `17-CONTEXT.md` D-10 — por qué es una sola unidad clickeable y por qué el contador va una vez por
      slot.
  </read_first>
  <action>
    Reescribir el `.map` de la columna para que itere **entradas**, no turnos:

    **Entrada individual** (`kind === 'appt'`): el chip de hoy, tal cual — misma superficie
    (`statusChip(status)`), misma hora, mismo nombre, mismo subtítulo de servicio, mismo badge `Fijo`,
    y el badge de ocupación **solo** si el cálculo de solape marcó ese turno. **Deja de ser un
    `<button>` en todos los casos**: la rama que abría el roster desde un chip suelto desaparece,
    porque un turno que no pertenece a un grupo no tiene roster que abrir (D-12).

    **Entrada de grupo** (`kind === 'group'`): **una sola fila**, con la forma del UI-SPEC §4.3:

    - Un `<button>` con `chipClass` construido sobre `statusChip('confirmed')` —el grupo es una unidad,
      y el estado por persona ahora vive en el badge y en el roster— más
      `flex w-full items-center gap-1.5 text-left cursor-pointer hover:brightness-95` y el anillo de
      foco que ya usan los chips.
    - Contenido, en este orden: la hora en `font-semibold`, el nombre del servicio en un
      `min-w-0 flex-1 truncate`, y el `OccupancyBadge` con `flex-shrink-0`.
    - `aria-label` **completo**: servicio, hora, día y ocupación, incluyendo el `N de M lugares` y,
      si corresponde, el `1 sin seña`. El `3/6` visual tiene que estar acompañado de texto en el label
      accesible.
    - `onClick` → `setRosterSlot({ date, time, serviceId })`.

    **Alto fijo: una fila, sin importar si hay 3 o 15 inscriptos.** Esa es la razón de ser de la
    decisión: a 375px la grilla es de dos columnas (~170px cada una) y seis chips apilados hacían
    impracticable la semana. El nombre del servicio va `truncate`; el contador **nunca** se recorta.

    Tres cosas que hay que respetar y que es fácil perder al reescribir un `.map`:

    1. **Orden cronológico mezclado.** Los grupos y los chips comparten la misma línea de tiempo: el
       módulo ya devuelve el grupo en la posición de su primer miembro, así que **renderizá las
       entradas en el orden en que vienen**. No reordenes, no separes grupos arriba y chips abajo.
    2. **Un slot cuyos turnos son todos no-ocupantes igual se renderiza**, con su contador en `0/N`
       sobre la superficie neutra que devuelve `statusChip` para un estado no activo. Colapsar no puede
       hacer desaparecer un día que hoy muestra algo.
    3. **Un solo elemento clickeable por slot** (D-10). Si te encontrás poniendo un `onClick` por
       inscripto, volviste al modelo viejo.

    El `key` de React de la entrada de grupo sale de la clave del grupo (fecha + hora + servicio), no
    del id de un turno: si sale el primer inscripto, la fila no puede remontarse entera.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0** y `npm run build` exit **0**.
    - `grep -cE 'const isGroup' "app/(dashboard)/agenda/agenda-client.tsx"` == **0** — la deducción del
      modo por el número ya no existe (D-11).
    - `grep -cE 'capacity_mode\s*===' "app/(dashboard)/agenda/agenda-client.tsx"` == **0** — el
      componente **no** re-implementa la decisión de modo: eso lo resolvió el módulo puro y llega como
      `kind`. Hoy ese conteo vale **1** (la línea de `isSimultaneous`), que también se va porque el
      simultáneo llega como entrada individual con su cálculo de solape ya hecho. Si el conteo no da
      0, alguien duplicó la regla en la pintura.
    - `grep -cF "entry.kind === 'group'" "app/(dashboard)/agenda/agenda-client.tsx"` >= **1**.
    - `grep -cF 'min-w-0 flex-1 truncate' "app/(dashboard)/agenda/agenda-client.tsx"` >= **1** y
      `grep -cF 'flex-shrink-0' "app/(dashboard)/agenda/agenda-client.tsx"` >= **1**.
    - `grep -cF 'Ver inscriptos de' "app/(dashboard)/agenda/agenda-client.tsx"` == **1** — un solo
      `aria-label` de apertura de roster, o sea un solo clickeable por slot.
    - `./node_modules/.bin/vitest run test/agenda-occupancy.test.ts --no-file-parallelism` en
      **0 failed**, con `git diff -- lib/ test/` **vacío**.
    - **Prueba manual obligatoria** en `npm run dev`, con el negocio de prueba local, anotada paso por
      paso en el SUMMARY. Hace falta un servicio `group_class` de cupo 6 y al menos tres turnos suyos
      en el mismo horario:
      1. A **375px**, ese horario ocupa **una sola fila** en la celda del día, con `09:00`, el nombre
         del servicio truncado y el contador `3/6` sin recortar.
      2. El contador se ve **aunque no esté lleno** (D-10).
      3. Tocar la fila abre el roster con los tres inscriptos y el título con el nombre del servicio.
      4. Un cuarto turno del **mismo** horario pero de **otro** servicio grupal produce una **segunda**
         fila con su propio contador — no se fusionan.
      5. Un turno individual de ese día se sigue viendo como hoy y **no** abre nada.
      6. Un turno `pending_payment` de la clase suma el segmento `· 1 sin seña` al badge.
      7. Subir el cupo de ese servicio a 8 desde `/servicios` (el control inline de 17-03) y recargar:
         el contador pasa a `3/8`. **Esta es la prueba de POLISH-09**: el número que muestra la grilla
         cambia porque cambió `services.capacity`, no un bloque de horario.
  </acceptance_criteria>
  <done>Cada clase grupal ocupa una fila con su contador, la columna sigue siendo una línea de tiempo, y el número que muestra sale del servicio.</done>
</task>

</tasks>

<artifacts>
## Artifacts que produce este plan

| Símbolo / elemento | Dónde vive | Qué provee |
|---|---|---|
| `OccupancyBadge` | `agenda-client.tsx` (componente interno) | El contador de ocupación de **los dos** modos, con el segmento de seña pendiente (POLISH-09 / D-10) |
| `entriesByDate` | `agenda-client.tsx` (memo) | Reemplaza a `apptsByDate`; consume `buildDayEntries` con un solo reloj por render |
| Línea de grupo colapsada | `agenda-client.tsx` | Una fila por slot grupal, un solo clickeable (D-10) |
| `rosterSlot` con `serviceId` | `agenda-client.tsx` (estado) | El roster filtra por servicio y lee la misma entrada que la línea |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos del server → grilla del panel | los turnos y los servicios llegan filtrados por `business_id` en la página de agenda (T-02-13); el componente no hace fetch propio |
| panel autenticado → dato mostrado | la ocupación es dato **exclusivo del admin**; el público nunca ve lugares restantes (decisión histórica del workstream, ajena a esta fase) |
| lectura del panel ↔ decisión del motor | la grilla y `book_slot_atomic` tienen que decidir el cupo con la misma fuente |
| slot de un servicio ↔ slot de otro servicio a la misma hora | dos clases distintas comparten fecha y hora pero **no** comparten cupo ni inscriptos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-22 | Tampering (integridad del dato mostrado) | la columna del día decidía el cupo por el bloque de horario, columna que la 068 jubiló ⇒ el panel dice "lleno" con un número que el motor ignora | mitigate | Se borra la lectura vieja **entera** (criterio: cero llamadas, y las ocurrencias del prop de bloques bajan de 6 a 4) y el cupo pasa a salir de `services.capacity` vía el módulo de 17-04. Es el cierre del último residuo que la Phase 15 dejó anotado |
| T-17-23 | Tampering | la línea de grupo y el roster podrían calcular la ocupación por separado y divergir | mitigate | El roster **no recalcula**: recupera la **misma** entrada de grupo que se renderizó (misma fecha, hora y servicio). Estructuralmente no pueden mostrar números distintos |
| T-17-24 | Information Disclosure | el roster filtrado solo por fecha y hora mezcla inscriptos de dos clases distintas del mismo horario | mitigate | `rosterSlot` viaja con `serviceId` y la entrada de grupo tiene el `service_id` en su clave. El caso 4 de la prueba manual lo verifica en pantalla, y el caso 4 de la suite de 17-04 lo congela por mutación |
| T-17-25 | Information Disclosure | colapsar el grupo esconde el color ámbar por-persona de los `pending_payment` que hoy se ve en cada chip ⇒ la fase **perdería** información que ya está en pantalla | mitigate | El segundo segmento del badge (`· N sin seña`) es obligatorio, con umbral >= 1, y aparece también en el `aria-label` del botón del grupo. Criterio de grep + caso 6 de la prueba manual |
| T-17-26 | Denial of Service (UX) | un slot con 15 inscriptos hacía impracticable la semana a 375px (grilla de dos columnas de ~170px) | mitigate | Alto fijo de **una fila** por slot, nombre del servicio con `truncate` dentro de un `min-w-0 flex-1` y contador `flex-shrink-0` que nunca se recorta. Verificado a 375px en la prueba manual |
| T-17-27 | Tampering | un turno sin servicio resoluble podría inducir a inventar un cupo o a abrir un roster vacío | mitigate | D-12 lo resuelve **en el módulo puro** (entrada individual, sin contador y sin roster) y el componente tiene prohibido agregar un fallback propio: criterio de grep de que no re-implementa la decisión de modo |
| T-17-28 | Elevation of Privilege | ninguna: este plan **no** agrega ni modifica ningún camino de escritura | accept | La columna del día es solo lectura sobre datos ya filtrados por tenant en el server. `git diff` no toca `page.tsx`, no hay `supabase.from(...).update/insert/delete` nuevo, y el único efecto de un click es abrir un diálogo local |
| T-17-SC | Tampering (supply chain) | dependencias | accept | **Cero** paquetes nuevos: `Badge`, `Users` y `Dialog`/`Drawer` ya están. `git diff -- package.json package-lock.json` vacío |
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` exit **0** y `npm run build` exit **0**.
2. `./node_modules/.bin/vitest run test/agenda-occupancy.test.ts --no-file-parallelism` ⇒ **0 failed**,
   **sin haber tocado** ni el módulo ni su suite (`git diff -- lib/ test/` vacío). **No** se corre la
   suite completa: las de abono son flaky desde antes de esta fase.
3. `git diff --name-only` toca **exactamente** `app/(dashboard)/agenda/agenda-client.tsx`.
4. `git diff -- "app/(dashboard)/agenda/page.tsx" package.json package-lock.json` **vacío**.
5. Los conteos de grep de las tres tasks dan los valores exactos declarados — en particular
   `initialTimeBlocks` == **4**, cero llamadas a la función vieja de cupo por bloque, y
   `<OccupancyBadge` == **2**.
6. Los **7 pasos** de la prueba manual de la Task 3 corridos a 375px, con su resultado anotado en el
   SUMMARY. El paso 7 es la evidencia de POLISH-09: el contador cambia porque cambió el cupo del
   **servicio**.

**Rojo conocido al cerrar este plan:** ninguno. Este plan cierra POLISH-09 y con él la fase; lo único
pendiente después es la **UAT visual** del guion de abajo, que no es un criterio que un ejecutor pueda
satisfacer solo.
</verification>

<uat_script>
## Guion de la UAT visual de la fase (para `/gsd:verify-work`)

⚠ **Esto NO lo cierra el ejecutor.** Los checkpoints de este workstream se auto-aprueban con
`auto_advance` sin que nadie abra el navegador, y tres de las siete observaciones humanas de la
Phase 14 reportaron defectos que el pipeline verde no veía. El guion queda escrito acá para que la
UAT tenga pasos concretos; hasta que un humano lo corra, la fase se declara **PENDIENTE DE UAT**.

**Preparación — YA HECHA en el Supabase local** (sembrada por el ejecutor de 17-05 el 2026-08-20, en
`Negocio de Prueba` / `negocio-prueba`, login `test@forjo.local` / `Forjo1234!`). No hay que cargar
nada a mano: abrí `/agenda` en la semana del **viernes 21 de agosto de 2026** y vas a encontrar

- **`Yoga grupal`** — `group_class`, cupo **6**, con 3 turnos a las **09:00** (Ana Gomez y Bruno Diaz
  confirmados, Carla Ruiz con la **seña pendiente** y el hold vivo).
- **`Pilates reformer`** — `group_class`, cupo **4**, con 1 turno a las **09:00** — el MISMO horario
  que Yoga, a propósito: son la prueba de que dos clases distintas **no se fusionan**.
- **`Corte`** — `individual`, 1 turno a las **11:00** (Elsa Mora).
- **`Color`** — `simultaneous_resource`, cupo 2, ya venía en el seed.

⚠ Si corriste `supabase db reset` después de esa fecha, la siembra se perdió (`seed.sql` recrea el
negocio pero no estos turnos) y hay que rehacerla a mano.

⚠ **Los pasos 1 a 6 miran pantallas de `17-01`/`17-02`/`17-03`. `17-03` (el control inline de cupo en
la tarjeta de servicio, wave 3) todavía NO estaba ejecutado cuando cerró `17-05`**: si al correr la
UAT la tarjeta del grupal no muestra el stepper, no es un defecto — es que 17-03 no corrió todavía.
Los pasos 7 y 8 sí son de 17-05 y son verificables ya.

1. **375px · `/settings` → Servicios → editar un servicio.** ¿Se leen tres bloques paralelos (no nueve
   líneas)? ¿Se entiende la diferencia entre grupal y simultáneo sin abrir nada más?
2. **375px · mismo diálogo.** ¿El "Guardar" se alcanza siempre? ¿El título queda fijo arriba?
3. **375px · mismo diálogo.** ¿Se puede borrar el número de lugares y escribir otro?
4. **375px · lista de servicios.** ¿El servicio `individual` se ve igual que antes (sin badge)? ¿El
   grupal muestra `Clase grupal · [−] 6 [+] lugares`?
5. **375px · tarjeta.** Cambiar el cupo, guardar, ver el toast. Mientras guarda uno, ¿el stepper del
   otro servicio sigue usable?
6. **375px · alta de servicio.** ¿El botón "Agregar servicio" está al final y rotulado?
7. **375px · `/agenda`, semana del vie 21 de ago.** En la celda del viernes tiene que haber **DOS
   filas de las 09:00**, una por clase: `09:00 · Pilates reformer · 👥 1/4` (badge neutro) y
   `09:00 · Yoga grupal · 👥 3/6 · 1 sin seña` (badge ámbar). ¿Cada una ocupa **una sola** fila?
   ¿El contador se ve **aunque no esté lleno**? ¿El nombre del servicio se trunca sin recortar el
   contador? ¿La semana sigue siendo legible? A las 11:00 el turno individual de Elsa Mora se ve como
   siempre y **no** es clickeable.
8. **375px · `/agenda`.** Tocar la fila de Yoga: ¿el título del roster dice
   `Yoga grupal · vie 21 de ago · 09:00` y lista a **Ana, Bruno y Carla** (y NO a Dora Paz, que es de
   Pilates)? ¿El contador del diálogo dice `3/6` y **lugares ocupados**? — **Este paso es el único de
   17-05 que el ejecutor no pudo medir**: la apertura del roster es JS de cliente y no había navegador
   en el entorno de ejecución. Todo lo demás de los pasos 7 y 8 se verificó contra el HTML renderizado
   con datos reales.
9. **375px · `/finances` → Turnos.** ¿Se ve el servicio bajo el nombre del cliente? ¿La fecha, el
   precio y el botón quedaron donde estaban?
10. **Modo oscuro y otra paleta.** Repetir 4, 7 y 9: ¿algún texto pierde contraste?
</uat_script>

<success_criteria>
- La grilla de la agenda calcula la ocupación desde `services.capacity` y no queda ninguna lectura del
  cupo por bloque de horario en el archivo.
- El modo se lee; la deducción por el número desapareció.
- Una clase grupal ocupa una fila por horario, con su contador siempre visible y un solo clickeable.
- El grupal y el simultáneo muestran su ocupación con el mismo componente y los mismos tokens.
- El roster y la línea de grupo no pueden divergir: leen el mismo objeto.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-05-SUMMARY.md` when done
</output>
