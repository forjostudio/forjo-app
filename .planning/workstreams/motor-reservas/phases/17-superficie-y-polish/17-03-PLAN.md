---
phase: 17-superficie-y-polish
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - app/(dashboard)/settings/settings-client.tsx
autonomous: true
requirements: [POLISH-08]

must_haves:
  truths:
    - "La lista de /servicios muestra el modo de cada servicio de cupo compartido sin abrirlo (POLISH-08, D-07)"
    - "Los servicios individual NO llevan badge: la linea de datos se ve igual que hoy (D-07)"
    - "Desde la tarjeta se cambia el numero de lugares y se guarda, sin abrir el modal (D-08)"
    - "Desde la tarjeta NO se puede cambiar el modo: el label es texto, no un control (D-09)"
    - "El estado de guardado es POR TARJETA: guardar un servicio no congela los demas (D-08)"
    - "Un rechazo del guardado devuelve el numero al valor anterior y avisa con copy PROPIA, sin interpolar el mensaje de la base (T-14-25 / T-13-09)"
    - "El UPDATE inline lleva .eq('business_id', ...) ademas de la RLS, igual que saveEditService"
    - "El payload inline es exactamente { capacity }: no manda capacity_mode"
  artifacts:
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "CapacityInlineControl (badge + stepper + guardado diferido), saveCapacityInline (segundo write path sobre services), savingCapacityId (estado por tarjeta) y la linea de datos de la tarjeta convertida en contenedor flexible"
      contains: "CapacityInlineControl"
  key_links:
    - from: "CapacityInlineControl"
      to: "saveCapacityInline"
      via: "onSave devuelve true/false; false revierte el numero al valor guardado y marca el stepper"
      pattern: "saveCapacityInline"
    - from: "saveCapacityInline"
      to: "services (PostgREST)"
      via: "update({ capacity }).eq('id').eq('business_id') — defensa en profundidad, molde saveEditService"
      pattern: "\\.eq\\('business_id', business\\.id\\)"
    - from: "saveCapacityInline y saveEditService"
      to: "la copy del rechazo del gate de modo"
      via: "los dos leen LA MISMA constante; no hay dos redacciones del mismo caso"
      pattern: "GATE_MODE_CHANGE_MESSAGE"
---

<objective>
Que la tarjeta de `/servicios` muestre el modo de cupo **y** deje ajustar el número de lugares sin
abrir el modal.

Purpose: ⚠ **Esto es la ampliación de alcance de D-08, y es capacidad NUEVA, no presentación.** Cambia
el patrón de la pantalla (hoy: *ver en la lista, editar en el modal*) y abre un **segundo camino de
escritura sobre `services`**. El UI-SPEC lo llama "la pieza de más riesgo de la fase" y no existe nada
igual en el design system: un dato de la línea de datos que además escribe. Tres consecuencias que hay
que tratar como trabajo real y no como un `+` y un `−`: el estado de guardado es **por tarjeta** (no
hay un solo `saving*` singleton que alcance, y no hay precedente de estado-por-fila-en-lista en este
repo), el rechazo del gate tiene que mapear a **copy propia** sin interpolar el mensaje de la base, y
el piso por modo tiene que respetarse igual que en el modal.

D-07 y D-08 son **el mismo elemento**, no dos: `Clase grupal · [−] 6 [+] lugares`. Un solo lugar donde
vive el número en la tarjeta — la misma lección que la Phase 15 aplicó en la base, aplicada a la
pantalla.

Output: `CapacityInlineControl`, `saveCapacityInline`, `savingCapacityId` y la línea de datos de la
tarjeta convertida en contenedor flexible.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-UI-SPEC.md
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-PATTERNS.md
@.claude/skills/convenciones-forjo/SKILL.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
</context>

<constraints>
- **Depende de 17-02.** Leé `settings-client.tsx` como quedó después de los dos planes anteriores.
- Este plan toca **un solo archivo**: `app/(dashboard)/settings/settings-client.tsx`.
- **Desde la tarjeta NO se cambia el modo (D-09).** El label de modo es un `<span>`, no un botón. Si
  te encontrás poniendo un `onClick` en el label, parás: cambiar de modo es la operación peligrosa
  (es la que el gate de la 070 bloquea) y vive en el modal, donde hay lugar para explicarla.
- **El payload del guardado inline es exactamente `{ capacity }`.** Es **higiene, no un requisito de
  corrección** — está medido en `17-UI-SPEC.md` §1.5 contra el Postgres local: el guard de no-cambio
  del trigger (`IS NOT DISTINCT FROM`, migr. **070** líneas 338-342) hace que el payload amplio pase
  igual, y de hecho `saveEditService` manda el modo en cada guardado hoy, en producción, y no rebota.
  No escribas un comentario que venda el payload mínimo como arreglo de un bug: es evitar despachar
  un trigger `SECURITY DEFINER` que solo puede terminar en `RETURN NEW`, por cada `+` y cada `−`.
- **La copy que ve el dueño es propia y fija. NUNCA se interpola `error.message`, el código de la base
  ni el nombre del servicio** (T-14-25 / T-13-09).
- **Prohibido insertar un `<p>` de error dentro de la tarjeta**: empuja la cobertura y las pills de
  sede de todas las tarjetas de abajo. El canal es `toast.error` + una marca en el propio stepper.
- **NUNCA `npx tsc`** → `./node_modules/.bin/tsc --noEmit`. Este plan cierra el archivo, así que
  además corre `npm run build`.
- `grep -ciF` aborta en este Git Bash (exit 134). Usar `-cF` **o** `-ciE`, nunca los tres flags.
- Mobile-first 375px, targets ≥ 44px en mobile, tokens del design system (cero hex suelto),
  `lucide-react` como único sistema de iconos.
- Comentarios densos **en español**, explicando el *por qué*.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: `CapacityInlineControl` — el badge que además escribe (D-07 + D-08)</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - `17-UI-SPEC.md` §1 completo (§1.1 a §1.5) — es **prescriptivo**: composición, clases, los cuatro
      estados de la fila, targets táctiles por viewport y el contrato de escritura.
    - `app/(dashboard)/agenda/agenda-client.tsx:781-800` — el stepper de cupo por bloque: el molde
      visual (contenedor `overflow-hidden rounded-md border`, input central con bordes laterales,
      spinners nativos ocultos, `disabled:pointer-events-none disabled:opacity-30`). ⚠ **NO copiar su
      handler de cambio**: normaliza en cada tecla, que es justo el defecto que 17-01 corrigió.
    - `settings-client.tsx` → `CapacityModeFields`, tal como quedó en 17-01 — la disciplina de texto
      local + commit en `onBlur` ya está implementada ahí; **replicá esa forma**, no inventes otra.
    - `settings-client.tsx:135-150` — `minCapacityFor`, `MAX_CAPACITY`, `normalizeCapacity`.
    - `settings-client.tsx` → `CAPACITY_MODE_HELP` (creada en 17-01) — de ahí sale el label del modo.
  </read_first>
  <action>
    Crear `CapacityInlineControl`, componente **interno de este archivo** (no un archivo nuevo, no un
    componente del design system: no hay precedente de "badge que además escribe" y el UI-SPEC lo
    autoriza explícitamente como uno de los dos componentes nuevos de la fase).

    **Firma:** recibe el `service` (necesita `id`, `name`, `capacity`, `capacity_mode`), un booleano
    `saving` (que el padre calcula por tarjeta) y un `onSave(capacity: number): Promise<boolean>` que
    devuelve `true` si la base aceptó y `false` si rechazó. El componente **no** habla con Supabase:
    ese es el trabajo de la Task 2.

    **Estado interno:** el valor mostrado + el texto local del input + un flag de rechazo. El valor
    guardado (`saved`) sale del prop, normalizado con el piso del modo. `dirty` es `valor !== saved`.
    Un `useEffect` sobre `saved` resincroniza valor y texto: cuando el padre confirma el guardado, la
    fila se limpia sola y el botón desaparece — sin que el componente tenga que saber que guardó.

    **Composición** (UI-SPEC §1.2), como **un solo item flex** del contenedor de la línea de datos —
    envolver todo en un `<span className="inline-flex flex-wrap items-center gap-x-2 gap-y-1">` para
    que a 375px el control baje entero a su propia línea en vez de partirse entre el label y el
    stepper:

    - Label de modo: `<span className="font-medium text-foreground">` con el `label` que sale de
      `CAPACITY_MODE_HELP` (D-03: los labels viven en un solo lugar). **Es texto, no un control** (D-09).
    - Stepper: `<div role="group" aria-label={`Lugares de ${service.name}`}>` con
      `inline-flex items-center overflow-hidden rounded-md border bg-background transition-colors`,
      más `border-primary/50` mientras está sucio y `border-destructive` + `aria-invalid="true"`
      mientras dura el estado rechazado.
      - Botón `−`: `h-11 w-11 sm:h-8 sm:w-8`, icono `Minus` (hay que **agregarlo al import de
        `lucide-react`**), `aria-label="Un lugar menos"`, `disabled` en el piso del modo con
        `title="El mínimo de este modo es 2 lugares"`, `disabled:pointer-events-none disabled:opacity-30`.
      - Input: `h-11 w-14 sm:h-8 sm:w-10`, `border-x`, `text-center tabular-nums`,
        `inputMode="numeric"`, `aria-label="Cantidad de lugares"`, spinners nativos ocultos, y la
        **misma disciplina de 17-01**: el texto local es la verdad mientras el foco está adentro, se
        acepta el vacío, no se clampea en cada tecla, y en `onBlur` se normaliza con
        `normalizeCapacity(n, minCapacityFor(mode))` (vacío o basura → vuelve al valor vigente).
      - Botón `+`: espejo del `−`, icono `Plus`, `aria-label="Un lugar más"`, `disabled` en
        `MAX_CAPACITY` con `title="El máximo es 99 lugares"`.
      - Los dos `title` no son decoración: un `disabled` sin explicación es un callejón.
    - Sufijo: `<span>lugares</span>` (invariable, el piso es 2 así que nunca es "lugar").
    - Botón Guardar, **solo si está sucio**: `<Button size="sm" className="min-h-11 sm:min-h-0 min-w-24">`,
      con `animate-in fade-in-0 slide-in-from-left-1 duration-150 motion-reduce:animate-none` a la
      entrada. **Sin animación de salida** (unmount directo): animar la salida exige presencia montada
      y no paga — dejalo escrito como decisión, no como olvido. El `min-w-24` (96px) evita que la fila
      se reacomode cuando la etiqueta cambia a `Guardando…`.

    **Los cuatro estados** (UI-SPEC §1.4), todos obligatorios:

    1. **Limpio**: sin botón en el DOM; stepper con `border-border`, iconos `text-muted-foreground` y
       `hover:bg-secondary hover:text-foreground` con `transition-colors`.
    2. **Sucio**: aparece Guardar; el stepper marca el pendiente **sin mover nada** (solo color).
       **Revertir = volver al número guardado**: no hay botón "Cancelar". Además, `Escape` con el foco
       dentro del grupo restaura el valor guardado y saca el botón.
    3. **Guardando**: botón `disabled` + `aria-busy="true"` + texto `Guardando…`; stepper e input
       `disabled` (una tarjeta = un request en vuelo). **Sin spinner**: el cambio de etiqueta más el
       `disabled` ya es el feedback y no mete un elemento animado nuevo en una línea de 12px.
    4. **Rechazado**: el número **vuelve al valor guardado** apenas llega el `false`, así que la fila
       queda limpia y el botón desaparece (no hay estado zombi "sucio pero fallado"). El stepper toma
       `border-destructive` y `aria-invalid="true"` durante ~4 s (mismo lifetime que el toast) y
       vuelve solo con `transition-colors`. El `setTimeout` se limpia en el unmount.

    Foco visible en los tres interactivos con el idioma que el archivo ya usa
    (`focus-visible:ring-2 ring-ring ring-offset-1 ring-offset-background`).

    **Comentario de cabecera obligatorio** explicando por qué el control existe y por qué el label es
    texto: la tarjeta ajusta el cupo, que es la operación cotidiana; el modo vive en el modal porque
    es la operación que el gate bloquea y la que necesita las tres explicaciones de D-01.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF 'CapacityInlineControl' "app/(dashboard)/settings/settings-client.tsx"` >= **1**
      (sube a >= 2 con la Task 3).
    - `grep -cF 'Minus' "app/(dashboard)/settings/settings-client.tsx"` >= **2** (import + uso); hoy
      vale **0**.
    - `grep -cF 'aria-invalid' "app/(dashboard)/settings/settings-client.tsx"` == **1** (hoy **0**).
    - `grep -cF 'min-w-24' "app/(dashboard)/settings/settings-client.tsx"` == **1** (hoy **0**).
    - `grep -cF 'animate-in fade-in-0 slide-in-from-left-1' "app/(dashboard)/settings/settings-client.tsx"` == **1**
      y `grep -cF 'motion-reduce:animate-none' "app/(dashboard)/settings/settings-client.tsx"` == **1**.
    - `grep -cF 'onBlur' "app/(dashboard)/settings/settings-client.tsx"` == **2** (el de 17-01 + el de
      este control): la disciplina se replicó, no se abandonó.
    - `grep -cF 'aria-busy' "app/(dashboard)/settings/settings-client.tsx"` == **1**.
    - `grep -cF 'h-11 w-11 sm:h-8 sm:w-8' "app/(dashboard)/settings/settings-client.tsx"` == **2**
      (los botones `−` y `+`): el piso táctil de 44px en mobile, liberado en desktop.
    - Leído a ojo sobre el diff: el label de modo es un `<span>` sin `onClick`, sin `role` y sin
      `tabIndex` (D-09).
  </acceptance_criteria>
  <done>El control existe, tiene sus cuatro estados, cumple 44px en mobile y no permite tocar el modo.</done>
</task>

<task type="auto">
  <name>Task 2: `saveCapacityInline` — el segundo camino de escritura sobre `services`</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - `settings-client.tsx:775-813` — `saveEditService` completo: el `.eq('id').eq('business_id')`, el
      mapeo del rechazo del gate por `code` + `message.includes(...)`, la copy fija con el comentario
      que prohíbe interpolar, y la actualización optimista local **después** de la confirmación.
    - `17-UI-SPEC.md` §1.5 (contrato de escritura, con la tabla de lo medido) y §7.3 (la tabla de copy
      de resultados y errores).
    - `17-PATTERNS.md` §"Shared Patterns" — anti-tampering de tenant y copy del rechazo del gate.
  </read_first>
  <action>
    **(a) La copy del rechazo del gate deja de estar escrita dos veces.** Extraer la cadena que hoy
    vive dentro de `saveEditService` a una constante de módulo `GATE_MODE_CHANGE_MESSAGE`, y hacer que
    **los dos** caminos de escritura la lean. La cadena **no se reescribe ni una coma**: es la versión
    post-WR-02 de la Phase 16, que dice la verdad sobre la salida (un turno marcado `completed` no se
    puede cancelar desde el panel). Conservar su comentario explicativo, movido junto con ella.

    **(b) Estado por tarjeta.** Agregar `savingCapacityId: string | null`. **No** se puede copiar el
    shape de `savingEditSvc` (un booleano singleton) porque solo hay un diálogo abierto a la vez pero
    hay **muchas tarjetas** en pantalla: con un booleano global, guardar un servicio congelaría los
    steppers de todos los demás. Dejalo comentado — es el único punto donde el molde de
    `saveEditService` no alcanza tal cual.

    **(c) `saveCapacityInline(svc, capacity): Promise<boolean>`**, replicando el molde:

    - Prende `savingCapacityId` con el id del servicio y lo apaga en **todas** las salidas.
    - Normaliza antes de mandar: `normalizeCapacity(capacity, minCapacityFor(svc.capacity_mode ?? 'individual'))`.
      El control ya clampea, pero el piso por modo y el techo `MAX_CAPACITY` se aplican también acá —
      defensa en profundidad, y `MAX_CAPACITY` es lo que evita que un número pegado vuelva como
      `22003 smallint out of range`, que el panel no sabría explicar.
    - `supabase.from('services').update({ capacity }).eq('id', svc.id).eq('business_id', business.id)`.
      El `.eq('business_id', ...)` es **obligatorio**: la RLS es la segunda capa, no la única. El
      payload lleva **una sola clave**.
    - Error `P0001` cuyo `message` incluye `service_mode_has_future_appointments` →
      `toast.error(GATE_MODE_CHANGE_MESSAGE)` y `return false`. Comentar que por D-09 **este camino no
      manda el modo**, así que este rechazo no debería llegar nunca desde la tarjeta: queda como
      **fail-safe**, y si aparece significa que alguien empezó a mandar un `capacity_mode` **distinto**
      — es un bug del cliente, no un camino feliz.
    - Cualquier otro error → `toast.error('No pudimos guardar el cupo. Volvimos al valor anterior. Intentá de nuevo.')`
      y `return false`. **Cadena fija**: nada de `error.message`, ni el código, ni el nombre del servicio.
    - Éxito → `setServices(prev => prev.map(...))` con el cupo nuevo (misma actualización local
      post-confirmación que `saveEditService`), `toast.success('Cupo actualizado')`, `return true`.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF "No se puede cambiar cómo se ocupa el cupo" "app/(dashboard)/settings/settings-client.tsx"` == **1**
      — una sola redacción del rechazo, compartida por los dos caminos.
    - `grep -cF 'GATE_MODE_CHANGE_MESSAGE' "app/(dashboard)/settings/settings-client.tsx"` == **3**
      (declaración + `saveEditService` + `saveCapacityInline`).
    - `grep -cF ".eq('business_id', business.id)" "app/(dashboard)/settings/settings-client.tsx"` == **16**
      (hoy **15**): el camino nuevo suma el suyo.
    - `grep -cF '.update({ capacity })' "app/(dashboard)/settings/settings-client.tsx"` == **1** — el
      payload inline tiene exactamente una clave.
    - `grep -cE 'toast\.error\([^)]*error\.' "app/(dashboard)/settings/settings-client.tsx"` == **1** —
      la única interpolación de un error en un toast sigue siendo la preexistente de la subida de foto
      (fuera de alcance). El camino nuevo **no** agrega otra.
    - `grep -cF "toast.success('Cupo actualizado')" "app/(dashboard)/settings/settings-client.tsx"` == **1**.
    - `grep -cF 'savingCapacityId' "app/(dashboard)/settings/settings-client.tsx"` >= **3**.
    - `grep -cF 'service_mode_has_future_appointments' "app/(dashboard)/settings/settings-client.tsx"` == **2**
      (el mapeo del diálogo + el fail-safe del camino inline).
  </acceptance_criteria>
  <done>El segundo write path filtra por tenant, manda una sola clave, y traduce el rechazo con la misma cadena que el modal.</done>
</task>

<task type="auto">
  <name>Task 3: La línea de datos de la tarjeta admite el control como tercer dato (D-07)</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - `settings-client.tsx` — la tarjeta de servicio: el bloque del nombre + la pill "Sin cobertura" y,
      justo debajo, el `<p className="text-xs text-muted-foreground">` con duración y precio.
    - `17-UI-SPEC.md` §1.1 y la tabla de "Foco visual" — por qué el nombre tiene que seguir dominando
      la tarjeta y el control tiene que ceder peso.
    - `17-CONTEXT.md` D-07 — por qué el modo va en la línea de datos y no en una pill junto al nombre.
  </read_first>
  <action>
    Convertir la línea de datos en un contenedor flexible:
    `<div className="flex flex-wrap items-center gap-x-2 gap-y-1 text-xs text-muted-foreground">` con
    la duración, el precio y —**solo si el modo del servicio no es `individual`**— el
    `CapacityInlineControl`, separados por `<span aria-hidden="true">·</span>` (el separador no puede
    leerse dos veces en el lector de pantalla).

    Resolver el modo con el mismo fallback que usa el diálogo al abrir (`capacity_mode ?? 'individual'`):
    cubre filas viejas que quedaron en memoria sin el modo resuelto.

    **Los servicios `individual` no llevan nada** (D-07): son el default de la base y el 100 % de
    producción, así que badgearlos sería ruido en todas las tarjetas de hoy. **Sin badge, el badge se
    vuelve señal.** Visualmente la línea de un servicio individual tiene que verse igual que hoy.

    Pasarle al control el `saving` derivado por tarjeta (`savingCapacityId === s.id`) y el `onSave`
    que llama a `saveCapacityInline` con ese servicio.

    **La pill de alarma "Sin cobertura" no se toca**: sigue arriba, junto al nombre. D-07 separa los
    dos registros a propósito — el modo es un **dato**, mismo registro que duración y precio, y las
    pills junto al nombre están reservadas para advertencias. Mezclarlos le sube el volumen a un dato
    normal. Dejalo comentado, porque es la primera "mejora" que va a proponer el próximo que lea esto.

    Cuidado con el foco visual: el nombre del servicio (14px/500 en `--foreground`) tiene que seguir
    dominando la tarjeta. El control vive en la línea de 12px `--muted-foreground` y su única parte en
    `--foreground` es el label de modo; el stepper aporta un borde de 1px, **no una superficie llena**,
    y **no lleva `bg-primary` en reposo**. Una lista de servicios tiene que seguir leyéndose como
    lista, no como un panel de configuración. La única excepción es deliberada: cuando la fila está
    sucia, el "Guardar" (que es `bg-primary`) pasa a dominar esa tarjeta — hay una acción pendiente y
    tiene que verse; al guardar, el foco vuelve al nombre.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0** y `npm run build` exit **0**.
    - `grep -cF 'CapacityInlineControl' "app/(dashboard)/settings/settings-client.tsx"` >= **2**
      (definición + uso en la tarjeta).
    - `grep -cF 'aria-hidden="true">·<' "app/(dashboard)/settings/settings-client.tsx"` >= **1** — los
      separadores de la línea de datos no se leen en voz alta.
    - `grep -cF 'flex flex-wrap items-center gap-x-2 gap-y-1' "app/(dashboard)/settings/settings-client.tsx"` == **1**.
    - `grep -cF 'Sin cobertura' "app/(dashboard)/settings/settings-client.tsx"` == **1** y la pill
      sigue en el bloque del nombre (verificable en el diff: su `<span>` no se movió).
    - `grep -cF 'savingCapacityId === s.id' "app/(dashboard)/settings/settings-client.tsx"` == **1** —
      el estado de guardado se deriva por tarjeta en el call-site.
    - Prueba manual en `npm run dev` a **375px**, anotada en el SUMMARY, con **dos** servicios de cupo
      compartido activos:
      1. Un servicio `individual` no muestra ningún badge ni stepper.
      2. Subir el cupo de uno hace aparecer "Guardar"; bajarlo al valor original lo hace desaparecer
         sin guardar nada.
      3. Guardar muestra "Guardando…", después el toast `Cupo actualizado`, y la fila queda limpia.
      4. Mientras uno guarda, el stepper del **otro** servicio sigue usable.
      5. El control baja a su propia línea completo (label + stepper + sufijo), no partido.
  </acceptance_criteria>
  <done>La tarjeta muestra el modo y deja ajustar el cupo sin abrir el modal, sin robarle el foco al nombre del servicio.</done>
</task>

</tasks>

<artifacts>
## Artifacts que produce este plan

| Símbolo / elemento | Dónde vive | Qué provee |
|---|---|---|
| `CapacityInlineControl` | `settings-client.tsx` (componente interno) | Badge de modo + stepper + guardado diferido con sus cuatro estados (D-07 + D-08) |
| `saveCapacityInline(svc, capacity)` | `settings-client.tsx` | Segundo write path sobre `services`, payload `{ capacity }`, filtrado por tenant, con mapeo de rechazo |
| `savingCapacityId` | `settings-client.tsx` (estado) | Guardado **por tarjeta** — el shape que `savingEditSvc` no podía dar |
| `GATE_MODE_CHANGE_MESSAGE` | `settings-client.tsx` (módulo) | Cadena única del rechazo del gate de modo, compartida por los dos write paths |
| Línea de datos flexible de la tarjeta | `settings-client.tsx` | Contenedor `flex-wrap` con separadores `aria-hidden` |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño autenticado (navegador) → `services` de su propio negocio | **segundo** camino de escritura, en paralelo al del diálogo; entra por PostgREST con anon key + RLS |
| PATCH forjado sin la UI → `services` | el control no es la defensa: el piso por modo lo impone el CHECK de la 068 y el modo lo gatea el trigger de la 070 |
| mensaje de un `RAISE` de la base → pantalla del dueño | el texto del gate no puede llegar crudo a la UI |
| tarjeta A ↔ tarjeta B de la misma lista | varias tarjetas escriben sobre la misma tabla desde la misma pantalla |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-09 | Tampering | el `UPDATE` de `saveCapacityInline` sin filtro explícito por tenant confiaría **solo** en la RLS | mitigate | `.eq('id', svc.id).eq('business_id', business.id)`, molde literal de `saveEditService` (`settings-client.tsx:790`). Criterio de conteo exacto: el archivo pasa de **15** a **16** ocurrencias del filtro |
| T-17-10 | Information Disclosure | interpolar `error.message`, el código SQLSTATE o el nombre del servicio en el toast filtra internals de la base al dueño (T-14-25 / T-13-09) | mitigate | Dos cadenas **fijas**: `GATE_MODE_CHANGE_MESSAGE` (compartida, no duplicada) y la genérica del UI-SPEC §7.3. Criterio: `toast.error` con una propiedad de `error` sigue valiendo **1** en todo el archivo — la preexistente de la subida de foto, fuera de alcance |
| T-17-11 | Tampering | el camino inline podría escribir un `capacity` que viola `services_capacity_matches_mode_chk` o desborda el `smallint` | mitigate | `normalizeCapacity(n, minCapacityFor(mode))` se aplica **dos veces**: en el `onBlur` del control y otra vez en `saveCapacityInline` antes de mandar. `MAX_CAPACITY = 99` es el techo. La autoridad sigue siendo el CHECK de la 068; esto es su espejo de UX, nunca su reemplazo |
| T-17-12 | Elevation of Privilege | si el payload inline llevara `capacity_mode`, la tarjeta se volvería un camino para cambiar el modo **sin** las explicaciones de D-01 y sin el aviso de espacio compartido | mitigate | El payload es **exactamente `{ capacity }`** (criterio de grep) y el label de modo es un `<span>` sin `onClick` (D-09). Como red de seguridad, un `capacity_mode` **distinto** seguiría rebotando contra el trigger de la 070 y su rechazo mapea a `GATE_MODE_CHANGE_MESSAGE`: **fail-closed**, no fail-open. ⚠ Ese trigger es la última línea, no la primera — el gate real es que la UI no ofrezca el control |
| T-17-13 | Denial of Service (UX) | un `saving` global congelaría todas las tarjetas y podría dejar steppers bloqueados si un request queda colgado | mitigate | `savingCapacityId` es **por tarjeta** (criterio de grep en el call-site) y se apaga en todas las salidas de la función. Una tarjeta = un request en vuelo; las demás siguen usables |
| T-17-14 | Repudiation | el estado "sucio pero fallado" haría que el dueño crea que guardó un número que la base rechazó | mitigate | Ante `false` el control **revierte al valor guardado** y el botón desaparece: no queda ningún número en pantalla que no esté en la base. La marca de error es transitoria (`border-destructive` + `aria-invalid` ~4 s) y el canal del mensaje es el toast |
| T-17-15 | Tampering | insertar un `<p>` de error dentro de la tarjeta empuja el layout de todas las tarjetas de abajo | mitigate | Prohibido por constraint: el canal es `toast.error` (cero desplazamiento) + una marca en el propio stepper |
| T-17-SC | Tampering (supply chain) | dependencias | accept | **Cero** paquetes nuevos: `tw-animate-css` y `lucide-react` ya son dependencias. `git diff -- package.json package-lock.json` vacío |
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` exit **0** y `npm run build` exit **0**.
2. `git diff --name-only` toca **exactamente** `app/(dashboard)/settings/settings-client.tsx`.
3. `git diff -- package.json package-lock.json components/ui/` **vacío**.
4. Los conteos de grep de las tres tasks dan los valores exactos declarados — en particular
   `.eq('business_id', business.id)` == **16** y `.update({ capacity })` == **1**.
5. Los 5 pasos manuales de la Task 3 corridos a 375px, con su resultado anotado en el SUMMARY.
6. Rechazo probado a mano (opcional pero recomendado, anotar el resultado): con el servicio en un
   estado que el gate rechace, el número vuelve al anterior y el toast dice la copy propia, sin
   ningún fragmento del mensaje de Postgres.
</verification>

<success_criteria>
- La lista de `/servicios` muestra el modo de cada servicio de cupo compartido sin abrirlo, y los
  `individual` se ven igual que hoy.
- Se puede ajustar y guardar el cupo desde la tarjeta, con estado por tarjeta y sin poder tocar el modo.
- El segundo write path filtra por tenant, manda una sola clave y traduce el rechazo con la misma
  cadena que el modal — sin interpolar nada de la base.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-03-SUMMARY.md` when done
</output>
