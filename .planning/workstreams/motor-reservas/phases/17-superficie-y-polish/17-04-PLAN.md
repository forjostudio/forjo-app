---
phase: 17-superficie-y-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agenda-occupancy.ts
  - test/agenda-occupancy.test.ts
  - app/(dashboard)/finances/finances-client.tsx
autonomous: true
requirements: [POLISH-09, POLISH-10]

must_haves:
  truths:
    - "La ocupacion de un slot y su agrupacion son funciones PURAS, testeables sin DB y sin navegador"
    - "El cupo sale de services.capacity — la misma fuente que el motor — y nunca de time_blocks (POLISH-09)"
    - "El modo se LEE de services.capacity_mode; en ningun lado se deduce del numero (D-11)"
    - "Dos clases distintas a la misma hora NO se fusionan: el service_id es parte de la clave del grupo (D-10)"
    - "Un turno sin service_id, o cuyo servicio no resuelve, se trata como individual: sin contador y sin roster (D-12)"
    - "Un hold con la sena vencida no ocupa lugar, igual que en el motor y en availability"
    - "Finanzas en mobile muestra el servicio de cada turno sin desplazar ningun otro dato (POLISH-10)"
  artifacts:
    - path: "lib/agenda-occupancy.ts"
      provides: "buildDayEntries (agrupacion por slot + ocupacion), computeOverlapFull (movido desde agenda-client), capacityOf, occupiesSeat y timeToMin — modulo puro, cero imports"
      exports: ["buildDayEntries", "computeOverlapFull", "capacityOf", "occupiesSeat", "timeToMin", "OCCUPYING_STATUSES"]
      min_lines: 120
    - path: "test/agenda-occupancy.test.ts"
      provides: "Los casos que congelan D-10/D-11/D-12 y el descarte del hold vencido, con dos casos discriminantes verificados por mutacion"
      contains: "buildDayEntries"
      min_lines: 150
    - path: "app/(dashboard)/finances/finances-client.tsx"
      provides: "El servicio como segunda linea bajo el nombre del cliente, solo en mobile"
      contains: "sm:hidden"
  key_links:
    - from: "buildDayEntries"
      to: "services.capacity / services.capacity_mode"
      via: "el cupo y el modo salen del mapa de servicios que le pasa el caller; el helper no conoce time_blocks"
      pattern: "capacity_mode"
    - from: "test/agenda-occupancy.test.ts"
      to: "lib/agenda-occupancy.ts"
      via: "import por el alias @/lib/..., sin Supabase y sin gate de entorno"
      pattern: "@/lib/agenda-occupancy"
---

<objective>
Sacar de la grilla de la agenda las dos cosas que **se pueden probar** —el cálculo de ocupación y el
agrupamiento por horario— a un módulo puro con tests, y cerrar POLISH-10 (Finanzas mobile).

Purpose: POLISH-09 **no es cosmético**. `capacityFor()` (`agenda-client.tsx:467`) calcula la ocupación
con `time_blocks.capacity`, columna que **desde la migración 068 no decide nada**: la grilla del panel
dice "lleno" con un número que el motor ignora. Hoy no se nota porque todo vale 1; **miente en cuanto
se declare la primera clase de cupo > 1**, que es justo lo que esta fase invita a hacer al explicar
los modos. Y la línea de abajo (`:638`) deduce el modo del número, estando a un renglón de
`isSimultaneous` (`:637`), que ya lo lee bien — el molde correcto está pegado al defecto.

Este plan **no toca `agenda-client.tsx`**: construye y prueba la lógica que 17-05 va a consumir. Esa
separación no es burocracia — la composición visual no se puede testear en este repo (el runner corre
con `environment: 'node'` y no renderiza JSX), pero la ocupación y el agrupamiento sí, y son
exactamente donde puede aparecer un número equivocado.

Output: `lib/agenda-occupancy.ts` (módulo puro), `test/agenda-occupancy.test.ts` y la fila de Finanzas
mobile con el servicio.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-UI-SPEC.md
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-PATTERNS.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<constraints>
- **Este plan NO modifica `app/(dashboard)/agenda/agenda-client.tsx`.** Ese archivo es de 17-05. Si te
  encontrás editándolo, te fuiste de alcance.
- **Rojo conocido y declarado:** al terminar este plan, `lib/agenda-occupancy.ts` queda **sin ningún
  consumidor** y `agenda-client.tsx` sigue con su lógica vieja adentro. Es **esperado**, no es código
  muerto ni una duplicación olvidada: **el plan 17-05 la borra de `agenda-client.tsx` y pasa a
  importar este módulo.** No lo "arregles" adelantando el cableado.
- **El módulo tiene que ser PURO: cero imports.** Ni React, ni Next, ni Supabase, ni `date-fns`. Recibe
  datos y devuelve datos, y por eso se puede testear sin DB y sin navegador. `timeToMin` se define
  adentro (hoy vive en `agenda-client.tsx:69`); 17-05 borrará la copia de allá.
- **NO ampliar ningún `select` server-side.** `agenda/page.tsx:48` ya trae la tabla `services`
  completa, así que `capacity` y `capacity_mode` **ya están del lado del cliente** y `serviceById`
  (`agenda-client.tsx:482`) los tiene. Un plan que toque `page.tsx` está haciendo trabajo de más.
- **NUNCA `npx tsc`** (baja un paquete que no es el compilador y siempre sale 0) →
  `./node_modules/.bin/tsc --noEmit`.
- **`npx vitest run` completo NO es un gate útil** en esta máquina: las suites de abono son flaky en
  paralelo desde antes de esta fase. Correr **la suite específica**:
  `./node_modules/.bin/vitest run test/agenda-occupancy.test.ts --no-file-parallelism`.
- `grep -ciF` aborta en este Git Bash (exit 134). Usar `-cF` **o** `-ciE`, nunca los tres flags.
- Comentarios densos **en español**, explicando el *por qué*, no el *qué*.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/agenda-occupancy.ts — la ocupación y el agrupamiento, puros</name>
  <files>lib/agenda-occupancy.ts</files>
  <read_first>
    - `app/(dashboard)/agenda/agenda-client.tsx:60-80` — `OCCUPYING_STATUSES` y `timeToMin`.
    - `app/(dashboard)/agenda/agenda-client.tsx:486-525` — `overlapFullById` completo: el guard de hold
      vivo, los carriles por servicio y día, y el conteo por intersección de intervalos. **Se mueve
      tal cual**: la lógica está validada en producción desde la Phase 12 y este plan la hace
      testeable, no la rediseña.
    - `app/(dashboard)/agenda/agenda-client.tsx:527-537` — el `roster`, que hoy saca el cupo del bloque.
    - `app/(dashboard)/agenda/agenda-client.tsx:634-640` — `isSimultaneous` (lee el modo, molde
      correcto) e `isGroup` (lo deduce del número, el defecto).
    - `lib/client-status.ts` — el precedente de la casa: regla que vivía duplicada dentro de un
      componente, extraída a un módulo puro con su test.
    - `17-CONTEXT.md` D-10, D-11, D-12 · `17-UI-SPEC.md` §4.1 y §4.2.
  </read_first>
  <behavior>
    Contrato que los tests de la Task 2 tienen que congelar, escrito antes de implementar:

    - `capacityOf(svc)` → el `capacity` del servicio, con piso 1. Servicio ausente, `capacity` nula o
      0 ⇒ **1**. Fail-safe: sub-ofrecer esconde, sobre-ofrecer miente.
    - `occupiesSeat(appt, nowMs)` ⇒ `true` solo si el estado está en `OCCUPYING_STATUSES`
      (`confirmed` / `pending_payment`) **y** el hold sigue vivo (`confirmed`, o `expires_at` nulo, o
      `expires_at` en el futuro respecto de `nowMs`).
    - `buildDayEntries(dayAppts, serviceById, nowMs)`:
      · Un turno cuyo servicio resuelve a `capacity_mode === 'group_class'` entra a un grupo con clave
        `date | time(HH:MM) | service_id`. Todo lo demás sale como entrada individual, **en su lugar**.
      · El grupo aparece en la posición de su **primer** miembro; los siguientes se suman a él sin
        crear otra entrada ⇒ la columna sigue leyéndose como línea de tiempo, con grupos y chips
        mezclados cronológicamente.
      · `occupied` = miembros con `occupiesSeat`; `pendingDeposit` = miembros `pending_payment` con el
        hold vivo; `capacity` = `capacityOf(servicio)`; `serviceName` = nombre del servicio o `null`.
      · **Un grupo cuyos miembros son todos no-ocupantes igual existe**, con `occupied: 0`: colapsar no
        puede hacer desaparecer algo que hoy se ve en pantalla.
      · **D-12**: `service_id` nulo, o servicio que no está en el mapa, ⇒ entrada individual. No se
        inventa un número ni se abre una lista que no corresponde a ninguna clase.
    - `computeOverlapFull(appts, serviceById, nowMs)`: idéntico al de hoy — solo los servicios
      `simultaneous_resource`, carriles por `service_id | date`, conteo por intersección de intervalos
      con la duración por defecto de 30 cuando falta, y solo entran al mapa los turnos cuyo intervalo
      **ya alcanzó** el cupo. Un grupal nunca aparece en este mapa.
  </behavior>
  <action>
    Crear `lib/agenda-occupancy.ts` con los tipos estructurales mínimos (`OccupancyAppt`,
    `OccupancyService`) y las funciones del bloque de arriba.

    **Genéricos, no casteos:** `buildDayEntries<A extends OccupancyAppt>(dayAppts: A[], ...)` devuelve
    entradas que conservan el tipo concreto `A`, para que 17-05 pueda leer `client_name`, `abono_id`,
    el join del nombre del servicio y el resto de `AgendaAppt` sin un solo casteo. Mismo criterio en
    `computeOverlapFull`.

    `DayEntry<A>` es una unión discriminada por `kind`:
    - `{ kind: 'appt'; appt: A }`
    - `{ kind: 'group'; key: string; date: string; time: string; serviceId: string; serviceName: string | null; appts: A[]; occupied: number; capacity: number; pendingDeposit: number }`

    `nowMs` entra **por parámetro**, nunca leyendo el reloj adentro: es lo que vuelve determinista el
    caso del hold vencido.

    Tres comentarios que son parte del entregable, no adorno:

    1. **Por qué el cupo sale de `services.capacity`**: es la fuente del motor desde la 068. La
       columna de cupo de los bloques se conserva pero **dejó de decidir**, y este módulo no la conoce
       ni la recibe. Nombrarlo acá es lo que evita que alguien la vuelva a enchufar "porque estaba a
       mano".
    2. **Por qué el modo se lee y no se deduce**: deducirlo del número es la misma clase de error que
       el review de la 069 propuso en la base y que se descartó midiéndolo. `isSimultaneous` ya lo
       hacía bien; este módulo aplica el mismo criterio a los tres modos.
    3. **Por qué el `service_id` está en la clave del grupo** (D-10): el cupo es del **servicio**, así
       que dos clases distintas a la misma hora en agendas distintas no se pueden fusionar — el
       contador mentiría.

    ⚠ **Consecuencia que hay que dejar escrita en el módulo y repetir en el SUMMARY:** el caller le
    pasa el mapa de servicios **activos** (la página de agenda filtra por activo). Un turno de una
    clase grupal cuyo servicio fue **desactivado** no resuelve y cae, por D-12, en el tratamiento
    individual: chip suelto, sin contador y sin roster. Es el comportamiento **literal** de D-12 y
    **no se corrige acá** ampliando la consulta del server — es un cambio de data flow que esta fase
    no tiene en alcance. Anotalo, no lo arregles.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -c '^import' lib/agenda-occupancy.ts` == **0** — el módulo es puro: cero dependencias.
    - `grep -cF 'export function buildDayEntries' lib/agenda-occupancy.ts` == **1**,
      `grep -cF 'export function computeOverlapFull' lib/agenda-occupancy.ts` == **1**,
      `grep -cF 'export function capacityOf' lib/agenda-occupancy.ts` == **1**,
      `grep -cF 'export function occupiesSeat' lib/agenda-occupancy.ts` == **1**,
      `grep -cF 'export function timeToMin' lib/agenda-occupancy.ts` == **1**.
    - `grep -nF 'time_blocks' lib/agenda-occupancy.ts` — si aparece, **toda** línea que lo mencione
      empieza con `//` o `*`. El módulo no recibe bloques ni los conoce como dato.
    - `grep -cF 'Date.now()' lib/agenda-occupancy.ts` == **0** — el reloj entra por parámetro.
    - `grep -cE '\bas\b [A-Z]' lib/agenda-occupancy.ts` == **0** — genéricos, no casteos.
    - `git diff --name-only` **no** incluye `app/(dashboard)/agenda/agenda-client.tsx` ni
      `app/(dashboard)/agenda/page.tsx`.
  </acceptance_criteria>
  <done>Existe un módulo puro que decide la ocupación desde el cupo del servicio, lee el modo en vez de deducirlo, y agrupa por servicio y horario.</done>
</task>

<task type="auto">
  <name>Task 2: Los tests que congelan D-10, D-11 y D-12, con dos casos discriminantes probados por mutación</name>
  <files>test/agenda-occupancy.test.ts</files>
  <read_first>
    - `test/client-status.test.ts` — el molde de una suite pura de este repo: `describe/it/expect`,
      import por el alias `@/lib/...`, sin Supabase, sin credenciales y **sin ningún gate de entorno
      que saltee casos** (así corre siempre, haya o no un Supabase local levantado).
    - `lib/agenda-occupancy.ts` tal como quedó en la Task 1.
    - El bloque de comportamiento de la Task 1: cada viñeta de ahí tiene que tener al menos un caso.
  </read_first>
  <action>
    Escribir `test/agenda-occupancy.test.ts` con un helper local mínimo para fabricar turnos y
    servicios (objetos literales, sin fixtures de DB — esta suite **no** toca Supabase). Cubrir, como
    mínimo:

    **buildDayEntries**
    1. Tres `confirmed` de un grupal de cupo 6 ⇒ una sola entrada de grupo, `occupied: 3`,
       `capacity: 6`, `pendingDeposit: 0`, con los 3 turnos adentro.
    2. Hold **vencido**: 2 `confirmed` + 1 `pending_payment` con vencimiento en el pasado ⇒
       `occupied: 2`, `pendingDeposit: 0`, y el turno **sigue** en la lista del grupo (el roster lo
       muestra, pero no ocupa lugar).
    3. El mismo caso con vencimiento en el futuro ⇒ `occupied: 3`, `pendingDeposit: 1`.
    4. **Dos servicios grupales distintos a la misma hora** ⇒ **dos** entradas de grupo, cada una con
       su propio contador. No se fusionan.
    5. El mismo servicio en dos horarios ⇒ dos entradas de grupo.
    6. `service_id` nulo ⇒ entrada individual (D-12).
    7. `service_id` que **no está** en el mapa —el caso real del servicio desactivado o borrado— ⇒
       entrada individual, sin contador (D-12).
    8. Servicio `simultaneous_resource` ⇒ entrada individual (no agrupa).
    9. Servicio `individual` ⇒ entrada individual.
    10. **Orden**: una lista ordenada por hora donde los miembros de un grupo están intercalados con
        turnos individuales ⇒ el grupo aparece en la posición de su **primer** miembro y los
        individuales conservan la suya.
    11. Grupo cuyos miembros son **todos** no ocupantes (por ejemplo `completed`) ⇒ la entrada de
        grupo existe igual, con `occupied: 0`.
    12. `capacity` nula o 0 en el servicio ⇒ `capacity: 1`.

    **computeOverlapFull**
    13. Dos turnos que se pisan sobre un simultáneo de cupo 2 ⇒ los dos en el mapa, con
        `count: 2, capacity: 2`.
    14. Dos turnos escalonados que **no** se pisan sobre cupo 2 ⇒ mapa vacío.
    15. Un hold vencido no cuenta hacia el solape.
    16. Un servicio `group_class` **nunca** aparece en este mapa.

    **Prueba de mutación (obligatoria, y va anotada en el SUMMARY con su salida).** Dos de estos casos
    tienen que ser **discriminantes**, no decorativos — la regla del workstream es que un test que pasa
    con y sin la garantía no cuenta como verificación:

    - Sacá temporalmente el `service_id` de la clave del grupo ⇒ el caso **4** tiene que **fallar**.
    - Sacá temporalmente el guard de hold vivo de `occupiesSeat` ⇒ el caso **2** tiene que **fallar**.

    Restaurá el código y volvé a correr la suite en verde. Pegá en el SUMMARY las dos salidas en rojo
    y la final en verde.

    Comentario de cabecera de la suite: qué congela cada bloque y por qué (mismo estilo que
    `test/client-status.test.ts`).
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run test/agenda-occupancy.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/vitest run test/agenda-occupancy.test.ts --no-file-parallelism` ⇒
      **0 failed**, con **>= 16** casos.
    - `grep -cF "from '@/lib/agenda-occupancy'" test/agenda-occupancy.test.ts` == **1**.
    - `grep -ciE "supabase|createclient|process\.env" test/agenda-occupancy.test.ts` == **0** — la
      suite no depende de la DB ni del entorno, así que no puede entrar al conjunto de suites flaky.
    - `grep -ciE "\.skip\(|\.todo\(" test/agenda-occupancy.test.ts` == **0**.
    - Las dos mutaciones produjeron **rojo en el caso previsto** y la restauración volvió a verde, con
      las tres salidas anotadas en el SUMMARY.
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
  </acceptance_criteria>
  <done>La ocupación y el agrupamiento están congelados por tests que corren siempre, y dos de ellos están probados por mutación.</done>
</task>

<task type="auto">
  <name>Task 3: Finanzas en mobile muestra el servicio (POLISH-10)</name>
  <files>app/(dashboard)/finances/finances-client.tsx</files>
  <read_first>
    - `app/(dashboard)/finances/finances-client.tsx:886-900` — la fila de turnos del tab
      `appointments`: el contenedor flex, el nombre del cliente, y el `<span>` del servicio que hoy
      queda escondido en mobile.
    - `lib/appointment-service.ts:56` — `apptServiceName(row, fallback)`: el fallback snapshot → join
      vive ahí y **no se reimplementa** en el call-site.
    - `17-UI-SPEC.md` §6 — el markup está prescriptivo ahí.
  </read_first>
  <action>
    A 375px la fila es `[fecha 80px] [cliente flex-1] [precio] [Pagado | Cobrar]` y el servicio
    directamente no está. **El servicio baja como segunda línea bajo el nombre del cliente, solo en
    mobile.** Ningún dato se desplaza ni se saca:

    - El `<span>` del nombre del cliente pasa a estar envuelto en un `<div className="min-w-0 flex-1">`,
      con el nombre como `block truncate font-medium`.
    - Debajo, **solo si el servicio no es vacío**, un `<span className="block truncate text-xs text-muted-foreground sm:hidden">`
      con el mismo `apptServiceName(appt, '')` que ya usa la fila.
    - La columna que hoy solo se ve desde 640px **se mantiene**: en desktop la fila de una línea ya
      funciona y no hay motivo para tocarla. El `sm:hidden` de la línea nueva y el `hidden sm:block`
      de la columna son complementarios — nunca se ven los dos a la vez.
    - Si el servicio es vacío (turno sin servicio resoluble), **no se renderiza la línea**: nada de
      filas con altura fantasma.

    La fila crece ~16px **solo en mobile**; fecha, precio y la acción quedan donde están. El nombre del
    cliente sigue siendo el punto de entrada de la fila: la línea nueva es 12px `--muted-foreground`,
    o sea se suma un dato sin mover el foco.

    Como pide la discreción del CONTEXT: si al implementarlo resultara inevitable desplazar otro dato,
    **decidilo y anotalo en el SUMMARY** — no inventes una pantalla nueva. Con este markup no hace
    falta: dejá escrito en el SUMMARY que no hubo que desplazar nada.
  </action>
  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF 'sm:hidden' "app/(dashboard)/finances/finances-client.tsx"` == **1** (hoy **0**).
    - `grep -cF 'hidden sm:block' "app/(dashboard)/finances/finances-client.tsx"` == **1** — la columna
      de desktop **sigue existiendo**, no se reemplazó.
    - `grep -cF 'min-w-0 flex-1' "app/(dashboard)/finances/finances-client.tsx"` == **1** (hoy **0**).
    - `grep -cF 'apptServiceName' "app/(dashboard)/finances/finances-client.tsx"` == **4** (los 3 de
      hoy + la línea nueva): el nombre del servicio sigue saliendo del helper compartido, no de un
      ternario nuevo en el call-site.
    - Prueba manual en `npm run dev`, anotada en el SUMMARY: a **375px** cada fila de `/finances` →
      tab Turnos muestra el servicio bajo el nombre del cliente, y la fecha, el precio y el botón
      quedan en la misma posición que antes; a **≥ 640px** la fila se ve **exactamente** como hoy (una
      sola línea, el servicio en su columna).
  </acceptance_criteria>
  <done>El servicio se ve en mobile como segunda línea, el desktop quedó intacto y ningún dato se desplazó.</done>
</task>

</tasks>

<artifacts>
## Artifacts que produce este plan

| Símbolo / archivo | Dónde vive | Qué provee |
|---|---|---|
| `lib/agenda-occupancy.ts` | archivo nuevo | Módulo puro, cero imports |
| `buildDayEntries<A>` | `lib/agenda-occupancy.ts` | Agrupación por `date` + hora + `service_id`, con ocupación y seña pendiente (D-10/D-11/D-12) |
| `computeOverlapFull<A>` | `lib/agenda-occupancy.ts` | La ocupación por solape del recurso simultáneo, movida desde `agenda-client.tsx` y ahora testeable |
| `capacityOf`, `occupiesSeat`, `timeToMin`, `OCCUPYING_STATUSES` | `lib/agenda-occupancy.ts` | Piezas compartidas por las dos anteriores y por el roster de 17-05 |
| `DayEntry<A>` | `lib/agenda-occupancy.ts` | Unión discriminada por `kind` que consume la columna del día |
| `test/agenda-occupancy.test.ts` | archivo nuevo | 16+ casos, 2 discriminantes probados por mutación |
| Segunda línea del servicio en mobile | `finances-client.tsx` | POLISH-10 |
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos del server → cómputo en memoria del panel | los turnos y los servicios llegan **ya filtrados por `business_id`** en la página de agenda (T-02-13); el helper corre sobre lo que recibe |
| panel autenticado → dato mostrado | la ocupación es dato **exclusivo del admin**: el público nunca ve lugares restantes (decisión histórica del workstream, ajena a esta fase) |
| lectura del panel ↔ decisión del motor | la grilla y `book_slot_atomic` tienen que decidir el cupo con la **misma** fuente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-16 | Tampering (integridad del dato mostrado) | la grilla calcula "lleno" desde el cupo del bloque, que la 068 jubiló ⇒ el panel puede decir "lleno" sobre un horario reservable, o al revés | mitigate | El módulo nuevo **no conoce** los bloques: recibe el mapa de servicios y lee `capacity` / `capacity_mode`. Criterio de grep: cualquier aparición del nombre de esa tabla en el archivo es **comentario**. El cierre real (borrar la lectura vieja) es de 17-05 y está declarado |
| T-17-17 | Tampering | deducir el modo del número reintroduce la clase de error que el review de la 069 propuso en la base y que se descartó midiéndolo | mitigate | El agrupamiento se decide por `capacity_mode === 'group_class'`, nunca por el número. Los casos 8 y 9 de la suite (simultáneo e individual ⇒ entrada individual) fallan si alguien vuelve a deducir |
| T-17-18 | Information Disclosure | un contador que **fusione** dos clases distintas de la misma hora mostraría, en la agenda de un servicio, inscriptos de otro | mitigate | El `service_id` es parte de la clave del grupo, y el caso **4** lo prueba **por mutación**: sacarlo del key hace fallar el test |
| T-17-19 | Tampering | un hold con la seña vencida contado como ocupante hace que el panel avise "lleno" sobre horarios que siguen reservables (precedente CR-01) | mitigate | `occupiesSeat` replica el guard del RPC (migr. 063) y de `availability`; el caso **2** lo prueba por mutación |
| T-17-20 | Information Disclosure | el helper podría retener o loguear datos del cliente (nombre, teléfono, mail) | accept | Recibe los turnos por parámetro y **no** los persiste, no los serializa y no los loguea: devuelve conteos y las mismas referencias que le pasaron. Es puro y sin imports (criterio de grep), así que no puede hablar con nada |
| T-17-21 | Tampering | ampliar una consulta del server "para resolver los servicios desactivados" cambiaría el data flow fuera de alcance | accept | D-12 cubre el caso por diseño: servicio irresoluble ⇒ tratamiento individual, sin contador y sin roster. Queda **anotado** en el módulo y en el SUMMARY, no arreglado. **Reabrir si** el dueño reporta que desactivar un servicio le rompe la lectura de una clase todavía viva |
| T-17-SC | Tampering (supply chain) | dependencias | accept | **Cero** paquetes nuevos: el plan escribe TypeScript puro y JSX con componentes ya instalados. `git diff -- package.json package-lock.json` vacío |
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` exit **0**.
2. `./node_modules/.bin/vitest run test/agenda-occupancy.test.ts --no-file-parallelism` ⇒ **0 failed**,
   >= **16** casos. **No** se corre la suite completa: las de abono son flaky en paralelo desde antes
   de esta fase y su rojo no dice nada sobre este trabajo.
3. Las dos mutaciones (clave sin `service_id`; `occupiesSeat` sin el guard de hold vivo) produjeron
   rojo en el caso previsto, y la restauración volvió a verde. Las tres salidas van al SUMMARY.
4. `grep -c '^import' lib/agenda-occupancy.ts` == **0**.
5. `git diff --name-only` toca **exactamente** `lib/agenda-occupancy.ts`,
   `test/agenda-occupancy.test.ts` y `app/(dashboard)/finances/finances-client.tsx`.
6. `git diff -- package.json package-lock.json "app/(dashboard)/agenda/"` **vacío**.
7. Finanzas verificada a 375px y a ≥ 640px, con el resultado anotado en el SUMMARY.

**Rojo conocido al cerrar este plan:** ninguna suite en rojo. Lo que **queda abierto a propósito** es
que `agenda-client.tsx` sigue leyendo el cupo del bloque y deduciendo el modo: eso lo cierra **17-05**,
que borra las dos lecturas viejas y pasa a importar este módulo. No es un criterio que este plan pueda
satisfacer.
</verification>

<success_criteria>
- La ocupación y el agrupamiento por slot son funciones puras, sin imports, que deciden por
  `services.capacity` y `services.capacity_mode`.
- Dos clases distintas a la misma hora no se fusionan, y un hold vencido no ocupa lugar — las dos
  cosas probadas por mutación, no solo por un test verde.
- Un turno sin servicio resoluble cae en el tratamiento individual, sin inventar un número.
- Finanzas en mobile muestra el servicio sin desplazar ningún otro dato.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-04-SUMMARY.md` when done
</output>
