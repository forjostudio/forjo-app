---
phase: 17-superficie-y-polish
plan: 06
type: execute
wave: 1
gap_closure: true
depends_on: []
files_modified:
  - app/(dashboard)/settings/settings-client.tsx
autonomous: true
requirements: [POLISH-08]

must_haves:
  truths:
    - "A 375px ningun boton de accion se monta sobre el texto de la tarjeta de servicio (G-02)"
    - "El nombre del servicio gana ancho: la fila del nombre ya no comparte ancho con la linea de datos"
    - "El control de cupo vive en SU PROPIA linea, con el ancho completo de la tarjeta (271px), no en una columna de 85px"
    - "Con la fila sucia, el boton Guardar entra en la MISMA linea que el stepper (G-02b)"
    - "El label del modo sigue en la linea de datos junto a duracion y precio (D-07)"
    - "La tarjeta de un servicio individual sigue sin badge y sin control (D-07)"
    - "Desde la tarjeta se sigue cambiando el numero, nunca el modo: el label sigue siendo texto (D-09)"
    - "El segundo write path no cambia: payload { capacity } y filtro por tenant intactos (D-08)"
  artifacts:
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "La fila de la tarjeta de servicio recompuesta (nombre+acciones arriba, linea de datos a ancho completo abajo) y CapacityInlineControl partido en dos items de flex: el label como dato, el stepper en su propia linea"
      contains: "CapacityInlineControl"
  key_links:
    - from: "la linea de datos de la tarjeta"
      to: "CapacityInlineControl"
      via: "el control aporta DOS items al contenedor flex: el label de modo inline y el bloque del stepper con basis-full"
      pattern: "basis-full"
    - from: "CapacityInlineControl"
      to: "saveCapacityInline"
      via: "onSave sigue devolviendo Promise<boolean>; este plan NO toca el write path"
      pattern: "saveCapacityInline"
---

<objective>
Cerrar **G-02** y **G-02b**: que la tarjeta de `/servicios` deje de superponer botones sobre el texto a
375px, y que el control de cupo entre en una sola línea con su botón `Guardar`.

Purpose: la UAT lo diagnosticó y el diagnóstico se confirmó midiendo. La fila de la tarjeta es
`flex items-center` con una columna `min-w-0 flex-1` (nombre + línea de datos) y, al lado, `Desactivar`
más dos botones de icono. **A 375px la tarjeta tiene 271px de ancho interior** (375 − 32 del `p-4` del
layout − 48 del `Card p-6` − 24 del `p-3` de la tarjeta). Las acciones se comen ~186px, así que a la
columna izquierda le quedan **~85px** — y el stepper solo mide **146px**. No hay superposición
"visual": hay 61px de desborde real de un `inline-flex` que no puede encoger, montándose bajo unos
botones centrados verticalmente contra un bloque que creció de alto. El servicio `individual` no está
afectado, que es la prueba de que lo introduce el control (regresión de 17-03).

**La regla de composición de este cierre de gaps, y es la misma para los tres gaps:** a este ancho, un
dato nuevo **no le pide ancho al que ya estaba: se lleva su propia línea.** Es literalmente lo que hizo
POLISH-10 en Finanzas —el servicio bajó como segunda línea bajo el nombre del cliente— y Finanzas es la
única de las tres superficies de la fase que pasó la UAT sin un solo issue. G-02 y G-03 hicieron lo
contrario y los dos se rompieron.

Output: la fila de la tarjeta recompuesta y `CapacityInlineControl` partido en dos items de flex.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-UAT.md
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-UI-SPEC.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<constraints>
- Este plan toca **un solo archivo**: `app/(dashboard)/settings/settings-client.tsx`.
- **No se toca `saveCapacityInline`.** El write path pasó la UAT (test 5: guarda, revierte, no congela
  las otras tarjetas). Este plan es composición, no lógica de guardado.
- **No se toca el diálogo "Editar servicio"** (D-05 pasó la UAT, test 2) ni el campo de cupo del modal
  (D-06 pasó, test 3) ni el explicador de modos (eso es 17-08).
- **No se toca `components/ui/*`, `package.json` ni `lib/verticals.ts`.** Se mantuvieron intactos toda
  la fase y siguen así.
- **Desde la tarjeta se cambia el número, nunca el modo (D-09).** El label de modo se **mueve de
  posición**, no de naturaleza: sigue siendo un `span` de texto plano.
- **Las tarjetas de profesionales no se tocan.** Comparten la clase base de la fila (`p-3 rounded-lg
  bg-secondary/50 space-y-2`) pero nadie las reportó, y ampliar el arreglo es superficie extra para
  volver a mirar en la UAT.
- Piso táctil: los botones del stepper conservan **`h-11 w-11` en mobile** (44px, regla dura del
  proyecto). El ancho se recupera de otro lado, no del target.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Las acciones salen de la fila del dato — la línea de datos recupera los 271px</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - `app/(dashboard)/settings/settings-client.tsx` líneas **2108-2165** — la tarjeta de servicio
      completa: el `div` contenedor (`p-3 rounded-lg bg-secondary/50 space-y-2`), la fila
      `flex items-center gap-3`, la columna `flex-1 min-w-0` con el bloque del nombre y la línea de
      datos, y los tres botones de acción (`Desactivar`, `Pencil`, `Trash2`).
    - `17-UAT.md` → **G-02** (`root_cause` y `note`).
  </read_first>
  <action>
    Recomponer **solo** la tarjeta de servicio (la del `.map` de servicios, la que declara
    `const capMode: CapacityMode` justo arriba). Es un cambio de estructura JSX; no se agrega ni se
    quita un solo dato.

    **Hoy** el contenedor de la tarjeta tiene un único hijo-fila con `items-center` y `gap-3`, y dentro
    de él una columna `flex-1 min-w-0` que anida DOS cosas: el bloque del nombre y la línea de datos.
    Esa columna es la que creció de alto al recibir el control, y es la que sólo dispone de ~85px.

    **Queda así**, con dos hijos hermanos del contenedor (el `space-y-2` del contenedor ya los separa):

    1. **Fila A — nombre y acciones.** Sigue siendo `flex items-center`, pero baja el gap a `gap-2` y
       pierde la línea de datos. Su primer hijo es el bloque del nombre tal cual está hoy
       (`flex items-center gap-2 min-w-0` con el `p` del nombre y la pill de cobertura), envuelto en el
       `flex-1 min-w-0` que ya existe. Su segundo hijo es **nuevo**: un contenedor
       `flex shrink-0 items-center gap-1` que agrupa los TRES botones de acción sin cambiarles una sola
       clase por dentro.
       Aritmética del cambio, para que se entienda por qué se agrupan: las acciones pasan de
       86 + 32 + 32 + 3 gaps de 12px = **186px** a 86 + 32 + 32 + 2 gaps de 4px + 1 gap de 8px =
       **166px**, y el nombre pasa de ~85px a **~105px**. Es la parte de G-02 que dice "el nombre del
       servicio se trunca mientras los botones ocupan ancho al lado".
       `items-center` se **conserva**: con la línea de datos afuera, la fila vuelve a ser un renglón
       corto y centrar es correcto otra vez. No cambiarlo a `items-start`: sería tocar la alineación de
       las tarjetas `individual`, que son el 100 % de producción hoy y no tienen ningún defecto.

    2. **Fila B — la línea de datos.** El `div` que hoy vive dentro de la columna
       (`flex flex-wrap items-center gap-x-2 gap-y-1 text-xs text-muted-foreground`, con
       `{s.duration_minutes}min · ${...}`, el separador `aria-hidden` y `CapacityInlineControl`) se
       **mueve tal cual** a hermano de la fila A, después de ella. No se le cambia ninguna clase ni
       ningún hijo: sólo cambia de padre. Ahí adentro pasa a disponer de los 271px completos.

    Todo lo que va debajo (el bloque "Se ofrece en" y el de cobertura) queda intacto y en el mismo
    orden.

    Dejá un comentario en español arriba de la fila A explicando **el porqué medido**: que las acciones
    y el dato dejan de competir por el mismo renglón porque a 375px ese renglón mide 271px y el control
    solo necesita 146px, y que el precedente es la fila de Finanzas de POLISH-10 —el único de los tres
    cambios de superficie de esta fase que no se rompió— donde el dato nuevo bajó a su propia línea en
    vez de pelear por ancho horizontal. No incluyas listas de clases de Tailwind dentro del comentario.
  </action>
  <acceptance_criteria>
    - El control quedó DESPUÉS de los botones de acción en el archivo (prueba estructural del
      movimiento):
      `F="app/(dashboard)/settings/settings-client.tsx"; CTL=$(grep -nF '<CapacityInlineControl' "$F" | cut -d: -f1); DEL=$(grep -nF 'openDeleteService(s)' "$F" | cut -d: -f1); test "$CTL" -gt "$DEL" && echo OK`
      → imprime `OK`. (Antes de este plan: control en 2145, borrar en 2160 — el orden inverso.)
    - La fila de la tarjeta de servicio dejó de usar el gap viejo, y las dos tarjetas de profesionales
      siguen intactas: `grep -cF 'className="flex items-center gap-3"' "$F"` → **2** (base: 3).
    - Las acciones quedaron agrupadas: `grep -cF 'flex shrink-0 items-center gap-1' "$F"` → **1**.
    - Los tres botones de acción no cambiaron de contenido:
      `grep -cF 'openEditService(s)' "$F"` → **1** y `grep -cF 'openDeleteService(s)' "$F"` → **1**.
    - El write path sigue idéntico (scope acotado a la función, inmune a comentarios de otras partes):
      `R=$(sed -n '/async function saveCapacityInline/,/^  }$/p' "$F"); printf '%s\n' "$R" | grep -cF '.update({ capacity })'` → **1**
      y `printf '%s\n' "$R" | grep -cF ".eq('business_id', business.id)"` → **1**.
    - `./node_modules/.bin/tsc --noEmit` → exit 0. (`npx tsc` siempre sale 0 en esta máquina y no
      sirve como gate.)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && F="app/(dashboard)/settings/settings-client.tsx" && CTL=$(grep -nF '<CapacityInlineControl' "$F" | cut -d: -f1) && DEL=$(grep -nF 'openDeleteService(s)' "$F" | cut -d: -f1) && test "$CTL" -gt "$DEL" && test "$(grep -cF 'className="flex items-center gap-3"' "$F")" -eq 2 && test "$(grep -cF 'flex shrink-0 items-center gap-1' "$F")" -eq 1 && echo TASK1_OK</automated>
  </verify>
  <done>La tarjeta tiene dos filas hermanas: nombre + acciones agrupadas arriba, línea de datos a ancho completo abajo. Los tipos compilan y las tarjetas de profesionales no se tocaron.</done>
</task>

<task type="auto">
  <name>Task 2: El control se parte en dos — el modo es dato, el stepper es línea</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - `app/(dashboard)/settings/settings-client.tsx` líneas **466-625** — `CapacityInlineControl`
      completo: el estado (`value`, `text`, `rejected`), `apply`/`revert`/`handleSave`, el `span` raíz
      con el `onKeyDown` de Escape, el label, el grupo del stepper, el sufijo y el botón `Guardar`.
    - `17-UAT.md` → **G-02b**.
    - `17-UI-SPEC.md` §1.2 (composición) y §1.3 (targets táctiles).
  </read_first>
  <action>
    Recomponer el **retorno** de `CapacityInlineControl`. La máquina de estados no se toca: `value`,
    `text`, `saved`, `dirty`, `apply`, `revert`, `handleSave`, el timer del rechazo y los `useEffect`
    quedan **exactamente** como están. Todo lo que cambia es cómo se reparte el ancho.

    **La aritmética que manda** (tarjeta = 271px de ancho interior a 375px, medido):

    | Pieza | mobile |
    |---|---|
    | grupo del stepper (`h-11 w-11` + input `w-14` + borde) | **146px** |
    | sufijo `lugares` | ~40px |
    | `Guardar` (`min-w-24`) | **96px** |
    | label del modo (`Clase grupal`) | ~74px |

    Todo junto en un renglón: 74 + 146 + 40 + 96 + gaps = **~372px**. Por eso hoy cae en tres niveles.
    Sacando el label del renglón y dejando que el sufijo ceda cuando aparece el botón: **146 + 8 + 96 =
    250px ≤ 271** ✓. Ése es el renglón que el dueño pidió.

    **Cambios, en orden:**

    1. El componente pasa a devolver un **fragmento con dos items** para el contenedor flex de la línea
       de datos (que es `flex-wrap`, así que dos items es exactamente lo que necesita):
       - **Item 1 — el label del modo**, el mismo `span` de hoy (`font-medium text-foreground`, texto
         de `CAPACITY_MODE_HELP`, sin manejadores). Queda inline con `30min · $5.000 ·`, que es
         literalmente lo que pide D-07: el modo es el **tercer dato** de la línea, mismo registro que
         duración y precio. La línea completa mide 92 + 8 + 6 + 8 + 74 = **188px ≤ 271** ✓.
       - **Item 2 — el bloque del control**: un `span` con
         `flex basis-full items-center gap-x-2 gap-y-1`. `basis-full` es lo que fuerza su propia línea
         de forma **determinista**, en vez de depender de dónde caiga el wrap. Adentro van, en este
         orden: el grupo del stepper tal cual está hoy, el sufijo y el botón `Guardar`.
    2. El `onKeyDown` que restaura con Escape **se muda al item 2** (el label no es focusable, así que
       no pierde nada) y tiene que seguir cubriendo el stepper **y** el botón `Guardar`.
    3. El sufijo `lugares` recibe `hidden sm:inline` **sólo cuando la fila está sucia**
       (condicionalo sobre `dirty` con `cn`). En reposo —que es el 99 % del tiempo— el sufijo se ve y la
       línea dice `[−] 6 [+] lugares` (194px). Cuando hay un guardado pendiente, la unidad le cede sus
       40px al botón, que es el elemento que **tiene** que verse. La unidad no se pierde para nadie: el
       input conserva su `aria-label` de cantidad de lugares, el grupo conserva el suyo, y a los ≥640px
       el sufijo vuelve porque ahí el stepper baja a 104px y entran los dos.
    4. El label del modo **no cambia de naturaleza**: sigue sin manejador de click, sin rol de botón y
       sin `tabIndex`. Cambiar de modo sigue viviendo sólo en el modal (D-09).

    Dejá comentado el porqué de `basis-full` (determinismo: la línea propia no puede quedar sujeta a
    dónde caiga el wrap) y el porqué del sufijo que cede (la aritmética de arriba, con los números).
  </action>
  <acceptance_criteria>
    - El bloque del control se lleva su propia línea de forma determinista:
      `grep -cE 'basis-full' "$F"` → **1** (base: 0).
    - El sufijo cede ancho sólo en mobile: `grep -cE 'hidden sm:inline' "$F"` → **1** (base: 0).
    - El piso táctil no se tocó: `grep -cF 'h-11 w-11 sm:h-8 sm:w-8' "$F"` → **2** (los dos botones del
      stepper).
    - El botón conserva su ancho mínimo anti-reflow: `grep -cE 'min-w-24' "$F"` → **1**.
    - El label del modo sigue sin ser un control (gate por construcción, no por palabra: los
      comentarios del archivo mencionan estos atributos en prosa y no deben poder invalidar el gate):
      `grep -cE 'tabIndex=' "$F"` → **0** y `grep -cE 'role="button"' "$F"` → **0**.
    - El write path sigue intacto:
      `R=$(sed -n '/async function saveCapacityInline/,/^  }$/p' "$F"); printf '%s\n' "$R" | grep -cF '.update({ capacity })'` → **1**.
    - `./node_modules/.bin/tsc --noEmit` → exit 0.
    - `npm run build` → compila (tarda ~3 min; corrélo una sola vez, al final del plan).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && F="app/(dashboard)/settings/settings-client.tsx" && test "$(grep -cE 'basis-full' "$F")" -eq 1 && test "$(grep -cE 'hidden sm:inline' "$F")" -eq 1 && test "$(grep -cF 'h-11 w-11 sm:h-8 sm:w-8' "$F")" -eq 2 && test "$(grep -cE 'tabIndex=' "$F")" -eq 0 && test "$(grep -cE 'role="button"' "$F")" -eq 0 && echo TASK2_OK</automated>
  </verify>
  <done>El label del modo es el tercer dato de la línea; el stepper, el sufijo y `Guardar` viven en una línea propia de 250px como máximo. Ni el estado del componente ni el write path cambiaron.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño autenticado (navegador) → `services` de su propio negocio | el segundo camino de escritura de D-08 sigue existiendo; este plan lo **rodea** sin tocarlo |
| texto de la tarjeta → control de escritura | el riesgo es que una recomposición convierta en interactivo algo que era texto (D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-29 | Tampering | mover el label de modo a otro item del flex podría tentar a "aprovechar" y hacerlo clickeable para cambiar de modo desde la tarjeta, salteando el explicador de D-01 y el aviso de espacio compartido | mitigate | El label sigue siendo un `span` de texto. Gate por construcción (`tabIndex=` y `role="button"` en 0) en vez de por palabra suelta, porque el archivo ya menciona esos atributos en comentarios en prosa y un conteo del token se auto-invalidaría |
| T-17-30 | Tampering | recomponer el JSX alrededor de `saveCapacityInline` podría alterar el payload o perder el filtro por tenant sin que nadie lo note | mitigate | Gate acotado por `sed` a la función: dentro de `saveCapacityInline` el payload sigue siendo de una sola clave y el `.eq('business_id', …)` sigue presente. El scope evita el falso positivo de contar ocurrencias en todo el archivo |
| T-17-31 | Denial of Service (UX) | esconder el sufijo mientras la fila está sucia podría dejar al dueño sin la unidad del número | accept | La unidad viaja por los `aria-label` del input y del grupo, vuelve a los ≥640px, y en reposo —el estado normal— está siempre visible. El intercambio dura lo que dura un guardado |
| T-17-32 | Information Disclosure | la copy del rechazo del gate podría reescribirse al recomponer | mitigate | Este plan no toca el manejo de errores; la cadena compartida y la prohibición de interpolar el mensaje de la base (T-14-25 / T-13-09) quedan como están |
| T-17-SC | Tampering | instalar paquetes para "resolver" el layout | mitigate | La fase no agrega dependencias: `git diff -- package.json package-lock.json` vacío. Sin instalaciones, no hay checkpoint de legitimidad que abrir |
</threat_model>

## Artifacts this phase produces

| Artifact | Qué aporta |
|---|---|
| `app/(dashboard)/settings/settings-client.tsx` → fila de la tarjeta de servicio | Nombre + acciones agrupadas en una fila corta; línea de datos a ancho completo abajo (cierra G-02) |
| `app/(dashboard)/settings/settings-client.tsx` → `CapacityInlineControl` | Label de modo como dato inline + bloque del stepper con `basis-full` y sufijo que cede ante `Guardar` (cierra G-02b) |

<verification>
- `./node_modules/.bin/tsc --noEmit` → exit 0.
- `npm run build` → compila.
- `git diff --stat` → **un solo archivo de código**: `app/(dashboard)/settings/settings-client.tsx`.
- `git diff -- package.json package-lock.json components/ui/ lib/verticals.ts "app/(dashboard)/agenda/" "app/(dashboard)/finances/"` → vacío.
- La suite pura de la agenda no se toca en este plan, pero sigue verde:
  `npx vitest run test/agenda-occupancy.test.ts --no-file-parallelism` → 20 passed.
  (⚠ `npx vitest run` completo es flaky por las suites de abono — nombrá siempre la suite.)
- ⚠ La confirmación visual a 375px **no es tarea de este plan**: va a la segunda ronda de UAT
  (guion en `17-08-PLAN.md`).
</verification>

<success_criteria>
1. A 375px ningún botón de acción puede montarse sobre el texto: no comparten renglón con la línea de
   datos (probado estructuralmente: el control quedó después de los botones en el archivo).
2. El control dispone de los 271px de la tarjeta en vez de los ~85px de la columna.
3. Con la fila sucia, `[−] 7 [+] [Guardar]` suma 250px y entra en un renglón.
4. El servicio `individual` sigue sin badge y sin control (`capMode !== 'individual'` intacto).
5. `saveCapacityInline` sigue mandando una sola clave y filtrando por `business_id`.
6. `tsc --noEmit` en 0 y `npm run build` compila.
</success_criteria>

<output>
Crear `.planning/workstreams/motor-reservas/phases/17-superficie-y-polish/17-06-SUMMARY.md` al terminar.
Anotá en el SUMMARY, explícitamente, la **desviación medida del UI-SPEC §1.1/§1.2**: el spec pedía que
el control bajara *entero* (label + stepper + sufijo) a su propia línea, y eso mide ~278px contra los
271px reales de la tarjeta — no entra ni siquiera en reposo. Por eso el label se queda arriba como
dato y sólo baja el bloque del stepper. Es una corrección medida del contrato, no un incumplimiento.
</output>
