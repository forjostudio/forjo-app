---
phase: 13-borrado-de-servicio-preservando-historial
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - components/crm/confirm-dialog.tsx
  - components/crm/confirm-dialog.test.tsx
  - app/(dashboard)/settings/settings-client.tsx
  - lib/canchas.ts
autonomous: true
requirements: [HIST-01, HIST-02]

must_haves:
  truths:
    - "Al tocar el tacho de un servicio con turnos futuros, se abre un modal que explica cuántos turnos lo bloquean y desde qué fecha, y ofrece 'Desactivar' como acción primaria (HIST-02, D-11, D-13)"
    - "El botón 'Desactivar' del modal desactiva el servicio ahí mismo, cierra el diálogo y confirma con un toast (D-12)"
    - "Al tocar el tacho de un servicio sin turnos futuros, el modal aclara cuántos turnos se conservan en el historial y permite eliminar"
    - "Tras eliminar, el servicio desaparece de la lista del panel (HIST-01)"
    - "Si alguien reserva entre el pre-check y el confirm, el modal NO se cierra: pasa a estado bloqueado con el mensaje correcto (backstop del trigger)"
    - "Los servicios desactivados salen de la lista principal y viven en un tab 'Desactivados' con contador (D-14)"
    - "Desactivar un servicio de otro negocio es imposible: toggleService filtra por business_id"
    - "Borrar una cancha bloqueada por un abono activo muestra el copy correcto, no el toast genérico"
  artifacts:
    - path: "components/crm/confirm-dialog.tsx"
      provides: "Props aditivas para el diálogo de dos estados (segundo botón + ocultar confirmar + handler de error)"
      contains: "secondaryAction"
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "Pre-check, modal de dos estados, deleteService discriminado, toggleService endurecido, tabs Activos/Desactivados"
      contains: "service_has_future_appointments"
    - path: "lib/canchas.ts"
      provides: "Mapeo del rechazo P0001 del trigger a has_appointments"
      contains: "P0001"
  key_links:
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "public.services_block_delete_trg"
      via: "mapeo del error P0001 del DELETE a los dos estados del modal"
      pattern: "service_has_(future_appointments|active_abono)"
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "components/crm/confirm-dialog.tsx"
      via: "props secondaryAction / hideConfirm / onConfirmError"
      pattern: "secondaryAction"
    - from: "lib/canchas.ts"
      to: "public.services_block_delete_trg"
      via: "deleteCancha reconoce P0001 y lo mapea a has_appointments"
      pattern: "has_appointments"
---

<objective>
Construir la UX del borrado: pre-check al abrir el tacho, un solo modal con dos estados
(bloqueado / confirmable), "Desactivar" como salida ejecutable, tabs Activos/Desactivados en la lista de
servicios, y el endurecimiento de `toggleService`. Además, cerrar la regresión de canchas que abre la 065.

Purpose: HIST-01 y HIST-02. Implementa D-11, D-12, D-13 y D-14, y evita que el rechazo del trigger
nuevo caiga en el toast genérico de canchas.

Output: `ConfirmDialog` extendido con 3 props aditivas, `settings-client.tsx` con el flujo completo de
borrado/desactivación, y `lib/canchas.ts` reconociendo `P0001`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/13-borrado-de-servicio-preservando-historial/13-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/13-borrado-de-servicio-preservando-historial/13-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/13-borrado-de-servicio-preservando-historial/13-01-SUMMARY.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender `ConfirmDialog` con las 3 props aditivas del diálogo de dos estados</name>

  <files>components/crm/confirm-dialog.tsx, components/crm/confirm-dialog.test.tsx</files>

  <read_first>
    - `components/crm/confirm-dialog.tsx` COMPLETO — contrato `ConfirmDialogProps`, los helpers puros exportados (`computeConfirmState`, `buildSubmitGuard`, `confirmButtonClass`), `handleConfirm` (cierra si `onConfirm` no lanza; toast genérico si lanza) y el `DialogFooter` fijo `[Cancelar] [confirmLabel]`.
    - `components/crm/confirm-dialog.test.tsx` — convención de tests: `environment node`, solo lógica PURA exportada, nunca render del componente.
    - `components/ui/button.tsx` — variantes disponibles (`default`, `outline`, …) para el botón secundario.
  </read_first>

  <behavior>
    - `computeFooterLayout({})` devuelve `{ showConfirm: true, showSecondary: false, secondaryVariant: 'outline' }` — comportamiento actual intacto para los ~10 call-sites existentes.
    - `computeFooterLayout({ hasSecondary: true })` devuelve `{ showConfirm: true, showSecondary: true, secondaryVariant: 'outline' }`.
    - `computeFooterLayout({ hideConfirm: true, hasSecondary: true })` devuelve `{ showConfirm: false, showSecondary: true, secondaryVariant: 'default' }` — sin confirmar, el secundario pasa a ser el primario visual.
    - `computeFooterLayout({ hideConfirm: true })` devuelve `{ showConfirm: false, showSecondary: false, secondaryVariant: 'outline' }` — footer con solo "Cancelar" (estado "verificando…").
    - `computeConfirmState` sigue devolviendo exactamente lo mismo que hoy para todas las entradas ya testeadas (cero regresión).
  </behavior>

  <action>
Sumar a `ConfirmDialogProps` tres props OPCIONALES y aditivas, cada una con el mismo estilo de JSDoc que
ya usa `minReasonLength` (explicando que es aditiva y que quien no la pasa conserva el comportamiento
previo):

1. `secondaryAction?: { label: string; onClick: () => void | Promise<void> }` — renderiza un botón extra
   en el `DialogFooter`, entre "Cancelar" y el de confirmar. Se deshabilita mientras `loading`.
2. `hideConfirm?: boolean` — oculta el botón de confirmar. Sirve al estado *bloqueado* de D-11: ofrecer
   un "Eliminar" que se sabe que el trigger va a rechazar es mal UX.
3. `onConfirmError?: (err: unknown) => void` — si se pasa, REEMPLAZA el `toast.error` genérico del
   `catch` de `handleConfirm`. El dialog sigue quedando abierto igual que hoy. Permite que el caller
   muestre el mensaje preciso del rechazo del trigger en vez de "No se pudo completar la acción".

Exportar un helper PURO nuevo `computeFooterLayout(input: { hideConfirm?: boolean; hasSecondary?: boolean }):
{ showConfirm: boolean; showSecondary: boolean; secondaryVariant: 'default' | 'outline' }` con la
semántica de `<behavior>`, y usarlo desde el JSX del footer. La regla del componente es que la lógica de
qué se muestra/habilita vive en helpers puros testeados, no inline en el render.

En `handleConfirm`, en la rama `catch`: conservar el `console.error` actual y, si `onConfirmError` está
definido, llamarlo en lugar del `toast.error` genérico.

NO tocar `computeConfirmState`, `buildSubmitGuard` ni `confirmButtonClass`. NO cambiar el orden de las
props existentes ni sus defaults. Ningún call-site existente puede necesitar cambios.

En `components/crm/confirm-dialog.test.tsx`: agregar un `describe` para `computeFooterLayout` con los 4
casos de `<behavior>`, siguiendo el estilo del archivo.
  </action>

  <verify>
    <automated>npx vitest run components/crm/confirm-dialog.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "secondaryAction" components/crm/confirm-dialog.tsx` devuelve al menos 3 (tipo, destructuring, JSX).
    - `grep -c "hideConfirm" components/crm/confirm-dialog.tsx` devuelve al menos 3.
    - `grep -c "onConfirmError" components/crm/confirm-dialog.tsx` devuelve al menos 3.
    - `grep -c "export function computeFooterLayout" components/crm/confirm-dialog.tsx` devuelve 1.
    - `grep -c "computeFooterLayout" components/crm/confirm-dialog.test.tsx` devuelve al menos 1.
    - `npx vitest run components/crm/confirm-dialog.test.tsx` sale con exit code 0 y TODOS los tests preexistentes del archivo siguen pasando.
    - `git diff --stat components/crm/confirm-dialog.tsx` reporta menos de 70 líneas cambiadas (extensión aditiva, no reescritura).
    - `./node_modules/.bin/tsc --noEmit` sale con exit code 0 (los ~10 call-sites existentes compilan sin cambios).
  </acceptance_criteria>

  <done>`ConfirmDialog` soporta un segundo botón de acción, ocultar el confirmar y un handler de error propio, sin romper ningún call-site.</done>
</task>

<task type="auto">
  <name>Task 2: Pre-check, modal de dos estados, `deleteService` discriminado y `toggleService` endurecido</name>

  <files>app/(dashboard)/settings/settings-client.tsx</files>

  <read_first>
    - `app/(dashboard)/settings/settings-client.tsx` líneas 500-560 — `deleteService` (~521-533) y `toggleService` (~534-537) en su estado actual, y el patrón `.eq('business_id', business.id)` de `deleteProfessional`.
    - `app/(dashboard)/settings/settings-client.tsx` líneas 1425-1500 — la lista de servicios dentro de la rama `!isCanchas`, incluido el `onClick={() => setDelService(s)}` del tacho (~1488) y el gate `isCanchas` (~322 y ~1434).
    - `app/(dashboard)/settings/settings-client.tsx` líneas 2295-2330 — el call-site actual del `ConfirmDialog` de borrado.
    - `components/dashboard/canchas-manager.tsx` líneas 155-205 — MOLDE EXACTO del pre-check (`openDelete`: par de estados `target` + `pending: number | null` donde `null` = "contando…", "hoy AR" con `toLocaleDateString('en-CA', { timeZone: 'America/Argentina/Buenos_Aires' })`, `.eq('business_id', …)`, `count ?? 0`) y de la descripción multi-estado (`delDescription`: constante derivada FUERA del JSX, ternario anidado, `undefined` cuando no hay target).
    - `lib/booking-core.ts` líneas 340-375 — MOLDE del mapeo de errores de Postgres a dominio: `message.includes('<codigo_dominio>')` primero, `code` después, una rama por caso, cada una con su comentario citando la migración de origen.
    - `components/crm/confirm-dialog.tsx` (tras el Task 1) — firmas de `secondaryAction`, `hideConfirm` y `onConfirmError`.
    - `supabase/migrations/065_service_snapshot_and_delete_gate.sql` — los dos messages exactos del `RAISE` y la semántica del gate (`date >= hoy AR`, `status IS DISTINCT FROM 'cancelled'`, abono `active`).
  </read_first>

  <action>
**Pre-check (D-11/D-13).** Reemplazar el estado `delService` suelto por el par del molde de canchas:
`delService: Service | null` y `delInfo: { future: number; nextDate: string | null; activeAbono: boolean;
history: number } | null`, donde `delInfo === null` significa "contando…".

Crear `openDeleteService(s: Service)`: setea `delService`, pone `delInfo` en `null` y lanza tres queries
con el cliente browser, TODAS con `.eq('business_id', business.id)` y `.eq('service_id', s.id)`:
- turnos futuros que bloquean: `.from('appointments').select('date', { count: 'exact' }).gte('date', today)
  .or('status.is.null,status.neq.cancelled').order('date').limit(1)` — de ahí salen el conteo (`count`)
  y la fecha del próximo turno (`data?.[0]?.date`).
- abono activo: `.from('abonos').select('id', { count: 'exact', head: true }).eq('status', 'active')`.
- total histórico para el copy confirmable: `.from('appointments').select('id', { count: 'exact', head: true })`.
`today` se deriva con `new Date().toLocaleDateString('en-CA', { timeZone: 'America/Argentina/Buenos_Aires' })`.

**Semántica de estado — regla dura.** El filtro `.or('status.is.null,status.neq.cancelled')` es el
equivalente exacto en PostgREST del `status IS DISTINCT FROM 'cancelled'` del trigger. NO usar
`.neq('status','cancelled')` a secas: PostgREST descarta también las filas con `status` NULL y el
pre-check diría "podés borrar" donde el trigger rechaza. Dejarlo comentado en español.

Cablear el tacho de la lista de servicios a `openDeleteService(s)` en lugar de `setDelService(s)`.

**Descripción de tres estados**, como constante derivada fuera del JSX (molde `delDescription` de
canchas), con `delBlocked = !!delInfo && (delInfo.future > 0 || delInfo.activeAbono)`:
- `delInfo === null`: `Vas a eliminar "<nombre>". Verificando turnos…`
- bloqueado: armar la frase con los números reales — cuando hay futuros, `"<nombre>" tiene N turno(s)
  reservado(s) a partir del <d/M>`; sumar ` y un abono activo` cuando `activeAbono`; cuando hay abono
  activo pero cero futuros, la frase arranca por el abono. Cerrar siempre con
  ` Desactivalo para dejar de ofrecerlo y conservar el historial.`
  La fecha se formatea con `date-fns` (`format(parseISO(nextDate), 'd/M')`), que ya se usa en el repo.
- confirmable: `Vas a eliminar "<nombre>". Se conservan sus M turnos en el historial (Finanzas y ficha
  del cliente) con su nombre y su precio. Esta acción no se puede deshacer.`

**Call-site del `ConfirmDialog`**: mantener `title`, `risk="alto"`, `confirmLabel="Eliminar"` y
`destructive`; sumar `hideConfirm={delInfo === null || delBlocked}` y, cuando `delBlocked`,
`secondaryAction={{ label: 'Desactivar', onClick: … }}` cuyo handler llama `toggleService(delService.id,
false)`, cierra el diálogo y limpia `delInfo` (D-12: desactiva ahí mismo, no manda a buscar el switch).
El `onOpenChange` limpia `delService` y `delInfo`.

**`deleteService` pasa a devolver un discriminado** en vez de tragarse el error con un toast:
tipo `{ ok: true } | { ok: false; error: 'has_future_appointments' | 'has_active_abono' | 'unknown' }`.
Conserva `.eq('id', id).eq('business_id', business.id)` y suma `.select('id')` para poder detectar el
caso "RLS filtró la fila": si no hubo error pero volvieron 0 filas, devolver `{ ok: false, error: 'unknown' }`
en vez de mentir "Servicio eliminado" (Pitfall 8, endurecimiento barato).
Mapeo del error, molde `booking-core`, cada rama con su comentario citando la migración 065:
- `code === 'P0001'` y `message` incluye `service_has_future_appointments` → `has_future_appointments`.
- `code === 'P0001'` y `message` incluye `service_has_active_abono` → `has_active_abono`.
- `code === '23503'` → `has_future_appointments` (fallback defensivo: quedó algún FK en NO ACTION).
- cualquier otro → `unknown`.
En éxito: filtrar el estado local y `toast.success('Servicio eliminado')`. En error NO emite toast: el
toast lo decide el caller (el modal), que tiene el contexto del estado.

**`onConfirm` del diálogo (backstop del trigger, D-10/D-11).** Llama a `deleteService`; si devuelve
`ok: false`, vuelve a correr el pre-check (para refrescar conteos y fecha) y **lanza** — porque
`ConfirmDialog` cierra el diálogo si `onConfirm` NO lanza, y ahí el rechazo del trigger se tragaría en
silencio. Con el throw, el modal queda abierto y se re-renderiza en estado bloqueado. `onConfirmError`
muestra el `toast.error` correcto según el motivo (turnos futuros / abono activo / genérico).
Dejar comentado en español por qué hace falta lanzar.

**`toggleService` endurecido** (D-12 lo asciende a acción primaria del modal): sumar
`.eq('business_id', business.id)` (hoy NO lo tiene — un UUID conocido de otro negocio se podría
desactivar si la policy lo permitiera), capturar `error` y devolver temprano con
`toast.error(...)`, y confirmar con `toast.success('Servicio desactivado' / 'Servicio activado')` recién
después de mutar el estado local. Comentar que se alinea al patrón de `deleteService`/`deleteProfessional`.

No cambiar el copy de los toasts de sede (~886) ni de profesional (~715): ya están shipeados (commit
`8e34b00`) y `REQUIREMENTS.md §Out of Scope` los declara fuera de alcance.
  </action>

  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "openDeleteService" "app/(dashboard)/settings/settings-client.tsx"` devuelve al menos 2 (definición + call-site del tacho).
    - `grep -c "status.is.null,status.neq.cancelled" "app/(dashboard)/settings/settings-client.tsx"` devuelve 1.
    - `grep -c "America/Argentina/Buenos_Aires" "app/(dashboard)/settings/settings-client.tsx"` devuelve al menos 1.
    - `grep -c "service_has_future_appointments" "app/(dashboard)/settings/settings-client.tsx"` devuelve 1.
    - `grep -c "service_has_active_abono" "app/(dashboard)/settings/settings-client.tsx"` devuelve 1.
    - `grep -c "secondaryAction" "app/(dashboard)/settings/settings-client.tsx"` devuelve 1.
    - `grep -c "hideConfirm" "app/(dashboard)/settings/settings-client.tsx"` devuelve 1.
    - `grep -c "onConfirmError" "app/(dashboard)/settings/settings-client.tsx"` devuelve 1.
    - En la definición de `toggleService`, la query encadena `.eq('business_id', business.id)` — verificar con `grep -A3 "async function toggleService"`.
    - `grep -A6 "async function toggleService" "app/(dashboard)/settings/settings-client.tsx"` contiene `if (error)`.
    - `grep -A8 "async function deleteService" "app/(dashboard)/settings/settings-client.tsx"` contiene `.select('id')`.
    - `git diff "app/(dashboard)/settings/settings-client.tsx"` NO modifica las líneas del toast de sede ni las del toast de profesional (verificarlo leyendo el diff y registrarlo en el SUMMARY).
    - `./node_modules/.bin/tsc --noEmit` sale con exit code 0 y `npm run lint` sale con exit code 0.
  </acceptance_criteria>

  <done>El tacho abre un modal que anticipa el resultado con números reales, ofrece "Desactivar" ejecutable cuando bloquea, elimina cuando se puede, y el rechazo tardío del trigger reabre el estado bloqueado en vez de cerrar en silencio.</done>
</task>

<task type="auto">
  <name>Task 3: Tabs Activos/Desactivados en servicios + mapeo `P0001` en `deleteCancha`</name>

  <files>app/(dashboard)/settings/settings-client.tsx, lib/canchas.ts</files>

  <read_first>
    - `app/(dashboard)/abonos/abonos-client.tsx` líneas 51-58, 143-156 y 262-297 — MOLDE LITERAL a replicar: tipo + constante de tabs a nivel módulo, filtro y contadores con `useMemo` usando el MISMO predicado (el comentario del archivo explica por qué), render de las píldoras (clases exactas `px-2.5 py-1 rounded-full text-xs font-medium transition-colors`, `aria-pressed`, activo `bg-primary text-primary-foreground`, inactivo `bg-secondary/50 text-muted-foreground hover:text-foreground`, etiqueta `{label} ({count})`) y el empty state por tab (`rounded-lg border border-dashed border-border p-8 text-center space-y-2` + icono `mx-auto h-6 w-6 text-muted-foreground`).
    - `app/(dashboard)/settings/settings-client.tsx` líneas 1425-1500 — la lista `services.map(...)` dentro de la rama `!isCanchas`, y el `line-through` condicional del nombre (~1472).
    - `lib/canchas.ts` líneas 178-200 — `deleteCancha`: borra `professionals` primero (FK todavía en NO ACTION → sigue dando `23503`) y después `services`, mapeando `23503 → 'has_appointments'`.
    - `components/dashboard/canchas-manager.tsx` líneas 175-190 — el consumidor del error: `'has_appointments'` muestra el copy correcto, cualquier otro cae al genérico "No se pudo eliminar la cancha".
  </read_first>

  <action>
**Tabs (D-14)** en `app/(dashboard)/settings/settings-client.tsx`, replicando el molde de `/abonos`:
1. A nivel módulo: `type ServiceTab = 'activos' | 'desactivados'` y
   `const SERVICE_TABS: { key: ServiceTab; label: string }[] = [{ key: 'activos', label: 'Activos' },
   { key: 'desactivados', label: 'Desactivados' }]`.
2. Estado `serviceTab` con default `'activos'`.
3. `visibleServices` y `serviceTabCounts` con `useMemo`, usando el MISMO predicado `!!s.active` para
   filtrar y para contar. Comentarlo en español citando el motivo del molde: si cada uno decidiera por
   su cuenta, el tab podría decir "Activos (1)" sobre una lista vacía.
4. Renderizar las píldoras arriba de la lista, con las clases EXACTAS de `abonos-client.tsx` (no
   inventar variantes) y `aria-pressed`. Etiqueta `{label} ({count})`.
5. La lista pasa a iterar `visibleServices` en vez de `services`.
6. Empty state por tab con el molde de `/abonos` y copy distinto por tab: en "Activos" invita a crear un
   servicio; en "Desactivados" explica que ahí van los servicios que se conservan pero ya no se ofrecen.
7. Quitar el `line-through` condicional del nombre: con el tab, tachar todos los nombres de la lista
   "Desactivados" no aporta información y sí ruido visual. Es la mitad de D-14 que vuelve usable la
   salida que ofrece D-12.
8. Todo esto va DENTRO de la rama `!isCanchas`: en el vertical canchas la pantalla renderiza
   `CanchasManager` y no debe ganar tabs.

**Mapeo `P0001` en `lib/canchas.ts` (regresión que abre la 065).** En `deleteCancha`, la rama que
maneja el error del `DELETE FROM services` hoy solo reconoce `23503`. Tras la 065 ese código ya no se
produce (el FK de `appointments` pasó a `SET NULL` y el de `abonos` también), y el rechazo llega como
`P0001` con el message del trigger. Ampliar la condición: además de `code === '23503'`, devolver
`{ ok: false, error: 'has_appointments' }` cuando `code === 'P0001'` y el `message` incluye
`service_has_future_appointments` o `service_has_active_abono`. Comentar en español de qué migración
viene el `RAISE` y por qué se mapea al mismo `'has_appointments'` que ya consume `canchas-manager`.
NO tocar la rama de `professionals` (su FK sigue en NO ACTION) ni el copy del toast del manager: el
modal de D-11 NO se extiende a canchas (fuera de alcance), solo el mapeo del error.
  </action>

  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "SERVICE_TABS" "app/(dashboard)/settings/settings-client.tsx"` devuelve al menos 2.
    - `grep -c "visibleServices" "app/(dashboard)/settings/settings-client.tsx"` devuelve al menos 2 (definición + uso en el map).
    - `grep -c "aria-pressed" "app/(dashboard)/settings/settings-client.tsx"` devuelve al menos 1.
    - `grep -c "px-2.5 py-1 rounded-full text-xs font-medium transition-colors" "app/(dashboard)/settings/settings-client.tsx"` devuelve 1 (clases copiadas literal del molde de /abonos).
    - `grep -cE "!s\.active && '[a-z-]*line-through" "app/(dashboard)/settings/settings-client.tsx"` devuelve 0 (el tachado condicional del nombre del servicio ya no existe; el criterio matchea la expresión, no la palabra suelta, así que un comentario explicativo no lo invalida).
    - `grep -c "P0001" lib/canchas.ts` devuelve al menos 1.
    - `grep -cE "service_has_future_appointments|service_has_active_abono" lib/canchas.ts` devuelve al menos 1.
    - `grep -c "'has_appointments'" lib/canchas.ts` devuelve al menos 2 (rama de professionals + rama de services).
    - `git diff components/dashboard/canchas-manager.tsx` está vacío (no se toca el copy del toast).
    - `./node_modules/.bin/tsc --noEmit` sale con exit code 0 y `npm run lint` sale con exit code 0.
  </acceptance_criteria>

  <done>Los servicios desactivados viven en su propio tab con contador, y borrar una cancha bloqueada por el trigger nuevo muestra el copy correcto en vez del genérico.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño autenticado (browser, anon key + RLS) → PostgREST → `services` | El DELETE y el UPDATE de `active` salen de acá con la sesión del dueño |
| pre-check del cliente → decisión de UX | El cliente NO autoriza: solo anticipa; la garantía vive en el trigger de la DB |
| `lib/canchas.ts` (server, service-role o sesión) → `services` | El borrado de cancha también choca contra el trigger nuevo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-15 | Elevation of Privilege | `toggleService` en `settings-client.tsx` — hoy sin `.eq('business_id', …)` | mitigate | Agregar el filtro por tenant + chequeo de `error`, alineándolo a `deleteService`/`deleteProfessional`; RLS queda como segunda capa |
| T-13-16 | Information Disclosure | Las 3 queries del pre-check leen `appointments` y `abonos` | mitigate | `.eq('business_id', business.id)` en las tres, además de `.eq('service_id', s.id)`; RLS `"business member access"` como respaldo |
| T-13-17 | Tampering | Saltear el modal y pegarle directo al endpoint de PostgREST para borrar un servicio con turnos futuros | mitigate | El modal es UX/refuerzo; el gate autoritativo es `services_block_delete_trg` (D-10), que no se puede evitar desde el cliente. Documentado en comentario |
| T-13-18 | Spoofing | El `DELETE` filtrado por RLS devuelve `error === null` y 0 filas, y la UI declara "Servicio eliminado" sin haber borrado | mitigate | `.select('id')` en el DELETE + tratar "0 filas devueltas" como `{ ok: false }` |
| T-13-19 | Repudiation | El rechazo tardío del trigger cierra el modal en silencio y el dueño cree que borró | mitigate | `deleteService` devuelve discriminado, `onConfirm` lanza para que `ConfirmDialog` no cierre, y `onConfirmError` muestra el motivo real |
| T-13-20 | Information Disclosure | El modal muestra conteo y fecha del próximo turno | accept | Son datos del propio tenant, obtenidos con queries filtradas por `business_id`; es exactamente lo que pide D-13 |
| T-13-21 | Denial of Service | El rechazo `P0001` del trigger cae al toast genérico de canchas y el operador no entiende por qué no puede borrar | mitigate | `deleteCancha` reconoce `P0001` + los dos messages y lo mapea a `has_appointments` |
| T-13-SC | Tampering | Instalación de paquetes | accept | Este plan no instala nada |

**ASVS level:** L1 · **Bloquea en severidad:** high
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 13-03)

**Símbolos TS nuevos**
- `computeFooterLayout(input)` — export puro de `components/crm/confirm-dialog.tsx`
- Props nuevas de `ConfirmDialogProps`: `secondaryAction`, `hideConfirm`, `onConfirmError`
- `openDeleteService(s)` — función local de `app/(dashboard)/settings/settings-client.tsx`
- Estado `delInfo` (`{ future, nextDate, activeAbono, history } | null`) y `delBlocked` — locales de `settings-client.tsx`
- `ServiceTab`, `SERVICE_TABS`, `serviceTab`, `visibleServices`, `serviceTabCounts` — locales de `settings-client.tsx`
- Tipo de retorno discriminado de `deleteService`: `{ ok: true } | { ok: false; error: 'has_future_appointments' | 'has_active_abono' | 'unknown' }`

**Firmas modificadas**
- `deleteService(id)` pasa de `Promise<void>` a devolver el discriminado de arriba
- `toggleService(id, active)` suma filtro por `business_id`, manejo de `error` y toasts
- `deleteCancha` (`lib/canchas.ts`) amplía el mapeo de errores a `P0001`

**UI nueva**
- Píldoras de filtro "Activos / Desactivados" con contador en la sección Servicios de Ajustes
- Empty state por tab en la lista de servicios
- Segundo botón "Desactivar" en el diálogo de borrado de servicio
</artifacts_this_phase_produces>

<verification>
1. `npx vitest run components/crm/confirm-dialog.test.tsx` verde, incluidos todos los tests preexistentes.
2. `./node_modules/.bin/tsc --noEmit` exit 0 — prueba que los ~10 call-sites de `ConfirmDialog` compilan sin cambios.
3. `npm run lint` exit 0.
4. `npm test` completo verde.
5. `git diff --name-only` de este plan lista exactamente: `components/crm/confirm-dialog.tsx`, `components/crm/confirm-dialog.test.tsx`, `app/(dashboard)/settings/settings-client.tsx`, `lib/canchas.ts`.
</verification>

<success_criteria>
- HIST-02: el modal bloquea, explica con números y ofrece "Desactivar" que ejecuta.
- HIST-01: sin turnos futuros el borrado se completa y el servicio sale de la lista.
- D-14: los desactivados viven en su tab con contador.
- Cero regresión en el borrado de canchas.
</success_criteria>

<output>
Crear `.planning/workstreams/motor-reservas/phases/13-borrado-de-servicio-preservando-historial/13-03-SUMMARY.md` al terminar, con el copy final de los tres estados del modal transcrito literal.
</output>
