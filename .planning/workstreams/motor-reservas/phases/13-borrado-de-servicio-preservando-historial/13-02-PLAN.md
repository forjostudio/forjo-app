---
phase: 13-borrado-de-servicio-preservando-historial
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - lib/appointment-service.ts
  - lib/types.ts
  - test/appointment-service.test.ts
  - app/(dashboard)/finances/finances-client.tsx
  - app/api/export/finances/route.ts
  - app/(dashboard)/dashboard/page.tsx
  - app/(dashboard)/clients/clients-client.tsx
  - app/(dashboard)/appointments/appointments-client.tsx
  - app/(dashboard)/abonos/page.tsx
  - app/(dashboard)/abonos/abonos-client.tsx
autonomous: true
requirements: [HIST-03]

must_haves:
  truths:
    - "Un turno pasado de un servicio ya borrado se muestra en Finanzas con su nombre y su precio (no '—' ni $0)"
    - "El CSV de Finanzas exporta ese mismo turno con concepto y monto correctos"
    - "La ficha del cliente muestra ese turno con nombre y precio en su historial"
    - "La pantalla de Turnos lo muestra igual en desktop y en mobile"
    - "El total de facturación del mes del Dashboard coincide con el de Finanzas para el mismo período tras borrar un servicio"
    - "Una serie de abono archivada de un servicio borrado sigue mostrando el nombre del servicio"
    - "Un turno de un servicio de precio 0 sigue mostrando 0 y no cae al join"
  artifacts:
    - path: "lib/appointment-service.ts"
      provides: "Fallback snapshot -> join, puro y compartido (D-05)"
      exports: ["apptServiceName", "apptServicePrice", "apptServicePriceOrNull"]
      min_lines: 30
    - path: "test/appointment-service.test.ts"
      provides: "Tests unitarios puros del fallback (sin DB)"
    - path: "lib/types.ts"
      provides: "Campos service_name / service_price en Appointment y service_name en Abono"
      contains: "service_price"
  key_links:
    - from: "app/(dashboard)/finances/finances-client.tsx"
      to: "lib/appointment-service.ts"
      via: "import de apptServiceName / apptServicePrice"
      pattern: "from '@/lib/appointment-service'"
    - from: "app/api/export/finances/route.ts"
      to: "lib/appointment-service.ts"
      via: "import del helper en el armado del CSV"
      pattern: "from '@/lib/appointment-service'"
    - from: "app/(dashboard)/clients/clients-client.tsx"
      to: "lib/appointment-service.ts"
      via: "getApptPrice/getApptService reemplazados por el helper compartido"
      pattern: "from '@/lib/appointment-service'"
---

<objective>
Centralizar el fallback **snapshot → join** de D-05 en un helper puro y consumirlo desde TODOS los
read-paths de historial, para que un turno de un servicio borrado siga mostrando su nombre y su precio.

Purpose: HIST-03. Hoy el fallback está duplicado 12 veces inline (`(a.services as {...})?.price || 0`);
si se migra una copia y se olvida otra, el bug se ve como "el gráfico bajó", no como un error.

Output: `lib/appointment-service.ts` + sus tests puros, y los 8 read-paths migrados (Finanzas, CSV,
Dashboard, ficha del cliente, Turnos desktop+mobile, Abonos).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/13-borrado-de-servicio-preservando-historial/13-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/13-borrado-de-servicio-preservando-historial/13-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/13-borrado-de-servicio-preservando-historial/13-01-SUMMARY.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear `lib/appointment-service.ts` (helper puro del fallback) + tipos + tests unitarios</name>

  <files>lib/appointment-service.ts, lib/types.ts, test/appointment-service.test.ts</files>

  <read_first>
    - `lib/booking-window.ts` líneas 1-40 — molde exacto de "helper puro compartido por client y server": bloque de cabecera en español explicando el *por qué*, tipo estructural mínimo como parámetro, named exports, cero import de React o Supabase.
    - `app/(dashboard)/clients/clients-client.tsx` líneas 140-155 — `getApptPrice` / `getApptService`, el fallback actual que este helper reemplaza (usan `||`, el nuevo usa `??`).
    - `lib/types.ts` líneas 243-285 (`Appointment`) — molde de cómo se documentan los campos aditivos de una migración: `?` + `| null` + comentario con número de migración, quién la escribe y por qué es opcional. Buscar además la interfaz `Abono` en el mismo archivo.
    - `supabase/migrations/065_service_snapshot_and_delete_gate.sql` — nombres exactos de las columnas (`service_name`, `service_price`).
    - `test/env.ts` — el patrón de skip; este test NO lo necesita (es puro), pero conviene ver la convención del directorio.
  </read_first>

  <behavior>
    - `apptServiceName({ service_name: 'Corte' })` devuelve `'Corte'` (snapshot manda).
    - `apptServiceName({ service_name: null, services: { name: 'Corte' } })` devuelve `'Corte'` (fallback al join).
    - `apptServiceName({ service_name: null, services: null })` devuelve `'—'` (fallback por defecto).
    - `apptServiceName({ service_name: null, services: null }, 'Sin servicio')` devuelve `'Sin servicio'`.
    - `apptServiceName({ service_name: 'Snapshot', services: { name: 'Actual' } })` devuelve `'Snapshot'` — el snapshot GANA aunque el join exista (D-03: foto inmutable).
    - `apptServicePrice({ service_price: 0, services: { price: 5000 } })` devuelve `0` — un precio 0 legítimo NO cae al join (el bug de `||` que hay hoy).
    - `apptServicePrice({ service_price: null, services: { price: 5000 } })` devuelve `5000`.
    - `apptServicePrice({ service_price: null, services: null })` devuelve `0`.
    - `apptServicePrice({ service_price: '1234.50' })` devuelve `1234.5` (numeric de PostgREST puede llegar como string).
    - `apptServicePriceOrNull({ service_price: null, services: null })` devuelve `null` (para los renders que distinguen "sin precio" de "$0").
    - `apptServicePriceOrNull({ service_price: 0 })` devuelve `0`.
  </behavior>

  <action>
Crear `lib/appointment-service.ts` con el molde de `lib/booking-window.ts`: bloque de cabecera en
español que explique que es la fuente ÚNICA del fallback de D-05 (snapshot primero, join de red de
seguridad), que las funciones son PURAS (sin React ni Supabase) para poder consumirse desde Server
Components, Client Components y route handlers, y que el snapshot lo escribe el trigger
`appointments_service_snapshot_trg` de la migración 065.

Definir un **tipo estructural mínimo** exportado (no importar `Appointment` entero, así lo cumplen
también las filas acotadas de PostgREST de Finanzas y las de `abonos`):
campos `service_name?: string | null`, `service_price?: number | string | null`,
`services?: { name?: string | null; price?: number | string | null } | null`.

Named exports:
- `apptServiceName(row, fallback = '—'): string` — `row.service_name ?? row.services?.name ?? fallback`.
- `apptServicePriceOrNull(row): number | null` — primer valor no nulo entre `row.service_price` y
  `row.services?.price`, pasado por `Number(...)`; si no hay ninguno, `null`.
- `apptServicePrice(row): number` — `apptServicePriceOrNull(row) ?? 0`.

Usar SIEMPRE `??` y nunca `||`: con `||` un precio 0 legítimo colapsa con `null` y cae al join. Dejarlo
comentado en el código, porque es exactamente el bug que tiene la versión inline actual.
El mismo `apptServiceName` sirve para `abonos` (misma forma estructural: columna `service_name` +
embed `services(name)`) — documentarlo en la cabecera para que nadie cree un segundo helper.

En `lib/types.ts`: sumar a `Appointment` los campos `service_name?: string | null` y
`service_price?: number | null`, y a la interfaz `Abono` el campo `service_name?: string | null`,
cada bloque con el comentario al estilo del repo (migración 065, lo escribe el trigger
`*_service_snapshot_trg`, opcional porque no todo `select` lo trae, foto inmutable de D-03).

Crear `test/appointment-service.test.ts` con Vitest: describe + it por cada caso de `<behavior>`.
Es un test PURO — no importa `test/env.ts`, no toca Supabase, no usa `describe.skipIf`.
  </action>

  <verify>
    <automated>npx vitest run test/appointment-service.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Existe `lib/appointment-service.ts` y exporta `apptServiceName`, `apptServicePrice` y `apptServicePriceOrNull`.
    - `grep -cE "^import .*(react|@supabase)" lib/appointment-service.ts` devuelve 0 (helper puro: sin React ni Supabase).
    - `grep -cE "\?\?" lib/appointment-service.ts` devuelve al menos 4 (todas las cadenas de fallback usan el operador nullish, no el OR lógico).
    - `npx vitest run test/appointment-service.test.ts` sale con exit code 0 y reporta al menos 11 tests pasando.
    - El test incluye explícitamente el caso `service_price: 0` con join presente y asierta `0`.
    - `grep -c 'service_price' lib/types.ts` devuelve al menos 1 y `grep -c 'service_name' lib/types.ts` devuelve al menos 2.
    - `./node_modules/.bin/tsc --noEmit` sale con exit code 0.
  </acceptance_criteria>

  <done>El helper existe, es puro, distingue precio 0 de null, y los tipos de `Appointment` y `Abono` incluyen los campos del snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar Finanzas (7 sitios + selects acotados), el CSV de Finanzas y el `monthRevenue` del Dashboard</name>

  <files>app/(dashboard)/finances/finances-client.tsx, app/api/export/finances/route.ts, app/(dashboard)/dashboard/page.tsx</files>

  <read_first>
    - `lib/appointment-service.ts` (creado en el Task 1) — firmas exactas del helper.
    - `app/(dashboard)/finances/finances-client.tsx` — los 7 sitios: select principal (~219), select del período anterior (~246) y su reduce (~250), select del chart de 6 meses (~268) y su reduce (~273), `apptRevenue` (~288), ranking por servicio (~310-318), `buildDailyCashflow` (~518) y el render de fila (~881-887).
    - `app/api/export/finances/route.ts` — el select acotado (~48) y el armado de `movimientos` (~65-72).
    - `app/(dashboard)/dashboard/page.tsx` — el select de `monthData` (`select('*, services(price)')`) y el reduce de `monthRevenue` (~83-85).
    - `.planning/workstreams/motor-reservas/phases/13-borrado-de-servicio-preservando-historial/13-PATTERNS.md` §"Read-paths de historial" — las dos formas del cast inline (A: reduce; B: variable `svc` + render) que hay que reemplazar.
  </read_first>

  <action>
**Regla que evita el bug silencioso:** hay dos selects **acotados** en Finanzas y uno en el CSV que NO
traen `*`. Si se cambia solo el reduce y no el select, el snapshot llega `undefined` y el período
anterior / el chart de 6 meses caen a 0 para servicios borrados — se ve como "el gráfico bajó", no como
un error. Ampliar el select ES parte del cambio.

`app/(dashboard)/finances/finances-client.tsx`:
1. Importar `apptServiceName` y `apptServicePrice` desde `@/lib/appointment-service`.
2. Select del período anterior: cambiar `select('services(price)')` por
   `select('service_price, services(price)')`.
3. Select del chart de 6 meses: mismo cambio, `select('service_price, services(price)')`.
4. El select principal y el resto usan `select('*, …')`: el `*` ya trae `service_name` y
   `service_price`, así que NO cambian.
5. Reemplazar los 4 reduces inline (`pServices`, el `ingresos` del chart, `apptRevenue` y el de
   `buildDailyCashflow`) por `apptServicePrice(x)`.
6. Ranking por servicio: `const label = apptServiceName(a, 'Sin servicio')` y
   `cur.total += apptServicePrice(a)` — conservar el texto `'Sin servicio'` que ya usa el ranking.
7. Render de fila: el nombre pasa a `apptServiceName(a)` y el monto a `fmtARS(apptServicePrice(a))`.
   No cambiar clases ni layout: es una migración de datos, no de diseño. **D-07: un turno de un servicio
   borrado NO se distingue visualmente del resto** — nada de badges, iconos, tooltips ni colores nuevos
   para marcar "servicio eliminado". Se lee igual que cualquier otro turno.
8. Borrar los casts `as { price?: number } | null` / `as { name?: string; price?: number } | null` que
   quedan sin uso.

`app/api/export/finances/route.ts` (D-06 ampliado: es Finanzas; si se omite, el CSV de un servicio
borrado sale "—" / 0 y contradice HIST-03):
9. Ampliar el select a `select('date, service_name, service_price, services(name, price)')`.
10. En el armado de `movimientos`: `concepto: apptServiceName(a)` y `monto: apptServicePrice(a)`.
11. Mantener intacto el `.eq('business_id', …)` de la query.

`app/(dashboard)/dashboard/page.tsx` (decisión del dueño de esta sesión, corrige la premisa fáctica de
D-06: `monthRevenue` suma la facturación del mes en curso, o sea también turnos ya pasados; si no se
migra, Dashboard y Finanzas muestran números distintos tras borrar un servicio a mitad de mes):
12. Importar el helper y reemplazar el reduce de `monthRevenue` por `apptServicePrice(a)`.
13. El select de `monthData` ya es `select('*, services(price)')` — el `*` trae el snapshot, así que
    NO hace falta tocarlo. Verificarlo antes de asumirlo.
14. NO migrar el resto del Dashboard (turnos de hoy, próximos): son turnos vivos, D-06 los deja fuera.
Dejar un comentario en español al lado de `monthRevenue` explicando por qué ESTE número sí se migra y
el resto del Dashboard no.
  </action>

  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "from '@/lib/appointment-service'" "app/(dashboard)/finances/finances-client.tsx"` devuelve 1.
    - `grep -c "from '@/lib/appointment-service'" "app/api/export/finances/route.ts"` devuelve 1.
    - `grep -c "from '@/lib/appointment-service'" "app/(dashboard)/dashboard/page.tsx"` devuelve 1.
    - `grep -c "select('service_price, services(price)')" "app/(dashboard)/finances/finances-client.tsx"` devuelve 2.
    - `grep -c "service_price" "app/api/export/finances/route.ts"` devuelve al menos 1 (el select ampliado).
    - `grep -cE "(a|x)\.services as \{" "app/(dashboard)/finances/finances-client.tsx"` devuelve 0 (no queda ningún cast inline del fallback viejo en código; si el executor deja una nota, debe describir el patrón sin reproducirlo).
    - `grep -cE "apptServicePrice|apptServiceName" "app/(dashboard)/finances/finances-client.tsx"` devuelve al menos 7.
    - `grep -c "apptServicePrice" "app/(dashboard)/dashboard/page.tsx"` devuelve 1 (solo `monthRevenue`).
    - `./node_modules/.bin/tsc --noEmit` sale con exit code 0.
    - `npm run lint` sale con exit code 0.
  </acceptance_criteria>

  <done>Finanzas (UI + CSV) y el total mensual del Dashboard leen el snapshot con fallback al join, y los dos selects acotados fueron ampliados.</done>
</task>

<task type="auto">
  <name>Task 3: Migrar la ficha del cliente, Turnos (desktop + mobile) y Abonos</name>

  <files>app/(dashboard)/clients/clients-client.tsx, app/(dashboard)/appointments/appointments-client.tsx, app/(dashboard)/abonos/page.tsx, app/(dashboard)/abonos/abonos-client.tsx</files>

  <read_first>
    - `lib/appointment-service.ts` (Task 1) — firmas exactas.
    - `app/(dashboard)/clients/clients-client.tsx` líneas 140-160 — `getApptPrice` / `getApptService` y todos sus call-sites en el archivo.
    - `app/(dashboard)/appointments/appointments-client.tsx` — el select de `refresh()` (~169) y los DOS renders: tabla desktop (~405-415) y tarjetas mobile (~436-445).
    - `app/(dashboard)/abonos/page.tsx` — el select de la serie (~39), que hoy pide `services(name)`.
    - `app/(dashboard)/abonos/abonos-client.tsx` líneas 31-47 — el tipo `AbonoRow` con `services: { name: string } | null`, y todos los sitios del archivo que leen `services?.name`.
  </read_first>

  <action>
`app/(dashboard)/clients/clients-client.tsx` (acá vive el fallback REAL de la ficha del cliente, no en
`page.tsx`):
1. Borrar las funciones locales `getApptPrice` y `getApptService` y reemplazarlas por
   `import { apptServiceName, apptServicePrice } from '@/lib/appointment-service'`.
2. Actualizar todos los call-sites: donde llamaba `getApptPrice(a)` va `apptServicePrice(a)`, donde
   llamaba `getApptService(a)` va `apptServiceName(a)` (el fallback por defecto `'—'` es el mismo texto
   que devuelve hoy `getApptService`, así que no cambia nada visible).
3. El select de `app/(dashboard)/clients/page.tsx` ya es `select('*, services(name, price)')` — el `*`
   trae el snapshot. NO tocar ese archivo; solo verificar que el `*` esté ahí.

`app/(dashboard)/appointments/appointments-client.tsx` — **son DOS renders**, migrar los dos (la
regresión clásica es tocar solo el de desktop y dejar el mobile mostrando "—" en 375px):
4. Importar `apptServiceName` y `apptServicePriceOrNull`.
5. Render desktop (celdas de la tabla): el nombre pasa a `apptServiceName(a)`; el precio usa
   `apptServicePriceOrNull(a)` y conserva el formato exacto actual, incluido el `'—'` cuando el valor
   es `null` y el `Number(...).toLocaleString('es-AR')` cuando no.
6. Render mobile (tarjetas): mismos dos reemplazos, conservando el separador ` · $` y la condición de
   "solo si hay precio".
7. El select de `refresh()` y el de `page.tsx` usan `select('*, …')`: el `*` trae el snapshot, no
   cambian. Verificarlo antes de asumirlo.

`app/(dashboard)/abonos/` (D-09: una serie archivada de un servicio borrado no puede quedar sin nombre):
8. `page.tsx`: agregar `service_name` a la lista de columnas del select de la serie, dejando intacto el
   resto del select y el `.eq('business_id', business.id)`.
9. `abonos-client.tsx`: sumar `service_name?: string | null` al tipo `AbonoRow` y reemplazar todos los
   sitios que leen `services?.name` por `apptServiceName(a, <el fallback que ya usa ese sitio>)`.
   Conservar literalmente el texto de fallback que cada sitio ya muestra; si hoy no tiene fallback,
   usar `'—'`.

Ningún cambio de layout, clases ni copy en estos cuatro archivos: es migración de origen de datos.
**D-07 aplica igual acá:** un turno de un servicio borrado se lee exactamente como cualquier otro; no
agregar ninguna marca visual que delate que el servicio ya no existe.
  </action>

  <verify>
    <automated>./node_modules/.bin/tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE "function getAppt(Price|Service)" "app/(dashboard)/clients/clients-client.tsx"` devuelve 0 (las funciones locales fueron eliminadas, no solo dejadas de usar).
    - `grep -cE "getAppt(Price|Service)\(" "app/(dashboard)/clients/clients-client.tsx"` devuelve 0 (no quedan call-sites).
    - `grep -c "from '@/lib/appointment-service'" "app/(dashboard)/clients/clients-client.tsx"` devuelve 1.
    - `grep -c "apptServiceName" "app/(dashboard)/appointments/appointments-client.tsx"` devuelve 2 (desktop + mobile).
    - `grep -c "apptServicePriceOrNull" "app/(dashboard)/appointments/appointments-client.tsx"` devuelve 2 (desktop + mobile).
    - `grep -c "service_name" "app/(dashboard)/abonos/page.tsx"` devuelve al menos 1.
    - `grep -c "service_name" "app/(dashboard)/abonos/abonos-client.tsx"` devuelve al menos 1.
    - `grep -cE "\{a\.services\?\.name|\{abono\.services\?\.name|= *[a-z]+\.services\?\.name" "app/(dashboard)/abonos/abonos-client.tsx"` devuelve 0 (ningún render ni asignación lee el join directo).
    - `grep -c "apptServiceName" "app/(dashboard)/abonos/abonos-client.tsx"` devuelve al menos 1.
    - `git diff "app/(dashboard)/clients/page.tsx"` está vacío (no se tocó).
    - `./node_modules/.bin/tsc --noEmit` sale con exit code 0.
    - `npm run lint` sale con exit code 0.
  </acceptance_criteria>

  <done>La ficha del cliente, los dos renders de Turnos y el listado de Abonos leen el snapshot con fallback al join; no queda ningún cast inline del fallback viejo en esos archivos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño autenticado (browser/RSC, anon key + RLS) → PostgREST → `appointments`/`abonos` | Todos los read-paths migrados cruzan acá; el aislamiento lo dan RLS + `.eq('business_id', …)` |
| route handler del CSV (`/api/export/finances`) → PostgREST | Select ampliado: no puede perder el scoping por tenant |
| vistas públicas `public_services` / `public_canchas` → rol `anon` | No deben ganar columnas nuevas por este plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-10 | Information Disclosure | select ampliado de `app/api/export/finances/route.ts` | mitigate | Solo se agregan `service_name`/`service_price` de `appointments`; el `.eq('business_id', …)` de la query queda intacto y se verifica en el diff |
| T-13-11 | Information Disclosure | selects ampliados de `finances-client.tsx` (período anterior y chart) | mitigate | Se agrega únicamente `service_price` de la propia fila; ambas queries conservan `.eq('business_id', businessId)` |
| T-13-12 | Information Disclosure | `public_services` / `public_canchas` (rol `anon`) | mitigate | Las columnas nuevas viven en `appointments`/`abonos`, no en `services`; verificar con grep que ninguna de las dos vistas se toca en este plan |
| T-13-13 | Tampering | El helper cae al join y muestra el precio ACTUAL en vez del histórico, alterando la facturación mostrada | mitigate | Orden `snapshot ?? join` con `??` (no `||`) y test que asierta que el snapshot gana aunque el join exista (D-03) |
| T-13-14 | Repudiation | Precio 0 legítimo colapsa con "sin precio" y desaparece del reporte | mitigate | `apptServicePriceOrNull` distingue `0` de `null`; test unitario dedicado |
| T-13-SC | Tampering | Instalación de paquetes | accept | Este plan no instala nada |

**ASVS level:** L1 · **Bloquea en severidad:** high
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 13-02)

**Archivos nuevos**
- `lib/appointment-service.ts`
- `test/appointment-service.test.ts`

**Símbolos TS nuevos**
- `apptServiceName(row, fallback?)` — export de `lib/appointment-service.ts`
- `apptServicePrice(row)` — export de `lib/appointment-service.ts`
- `apptServicePriceOrNull(row)` — export de `lib/appointment-service.ts`
- El tipo estructural mínimo exportado por `lib/appointment-service.ts` para las filas con snapshot

**Campos nuevos en tipos existentes**
- `Appointment.service_name`, `Appointment.service_price` (`lib/types.ts`)
- `Abono.service_name` (`lib/types.ts`)
- `AbonoRow.service_name` (`app/(dashboard)/abonos/abonos-client.tsx`)

**Símbolos eliminados**
- `getApptPrice`, `getApptService` (locales de `app/(dashboard)/clients/clients-client.tsx`) — promovidos al helper compartido
</artifacts_this_phase_produces>

<verification>
1. `npx vitest run test/appointment-service.test.ts` verde con al menos 11 casos.
2. `./node_modules/.bin/tsc --noEmit` exit 0.
3. `npm run lint` exit 0.
4. `npm test` completo verde (ninguna regresión en los 40+ archivos de test existentes).
5. `git diff --name-only` no lista `lib/booking-core.ts` ni ningún archivo bajo `app/api/booking/` (el write-path NO se toca, D-02).
</verification>

<success_criteria>
- Un turno cuyo `service_id` es NULL pero con snapshot poblado se ve con nombre y precio en Finanzas, CSV, ficha del cliente, Turnos (ambos renders) y Dashboard.
- Una serie de abono archivada sin `service_id` muestra el nombre del servicio.
- Cero ocurrencias del cast inline del fallback viejo en los archivos migrados.
</success_criteria>

<output>
Crear `.planning/workstreams/motor-reservas/phases/13-borrado-de-servicio-preservando-historial/13-02-SUMMARY.md` al terminar, listando los sitios migrados con su archivo y su forma (reduce / render / select).
</output>
