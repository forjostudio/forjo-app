---
phase: 06-modelo-del-abono-alta-manual-generaci-n-forward
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - app/api/cron/cancel-expired/route.ts
  - test/abono-cron.test.ts
autonomous: true
requirements: [ABONO-06, ABONO-02]
must_haves:
  truths:
    - "El cron DIARIO existente (cancel-expired) extiende la ventana rolling de cada abono activo hacia adelante — sin agregar ningún cron nuevo (Vercel Hobby)"
    - "La extensión genera solo la cola nueva (desde generated_until hasta hoy + abono_window_weeks) y avanza generated_until (idempotente)"
    - "La generación del cron pasa por el mismo motor atómico (Plan 02); una ocurrencia en conflicto se saltea y se acumula en skipped_occurrences"
    - "El cron sigue validando el secreto de cron existente; la extensión es best-effort en su propio try/catch y no aborta cancel-expired"
  artifacts:
    - path: "app/api/cron/cancel-expired/route.ts"
      provides: "Bloque de extensión de ventana de abonos, piggyback en el cron diario (como el MRR snapshot)"
      contains: "generateAbonoOccurrences"
  key_links:
    - from: "app/api/cron/cancel-expired/route.ts"
      to: "lib/abono-generation.ts"
      via: "por cada abono activo genera la cola de la ventana con generateAbonoOccurrences"
      pattern: "generateAbonoOccurrences"
---

<objective>
Enganchar la **extensión de la ventana rolling** en el cron DIARIO existente (`app/api/cron/cancel-expired`, Vercel Hobby = 1 cron diario, NO sumar crons — D-05/D-06/ABONO-06). Cada corrida, por cada abono `active`, genera la cola nueva de ocurrencias (desde `generated_until` hasta hoy + `abono_window_weeks`) con el motor del Plan 02, avanza `generated_until` y acumula los `skipped_occurrences`. Best-effort en su propio try/catch (mismo criterio que el snapshot de MRR ya presente en ese handler): un fallo de la extensión NO aborta el cancel-expired.

Purpose: Es lo que mantiene la ventana "hacia adelante" con el tiempo sin intervención (ABONO-06). El alta genera la primera tanda (Plan 03); el cron la va extendiendo.
Output: bloque de extensión en `app/api/cron/cancel-expired/route.ts` + `test/abono-cron.test.ts`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/phases/06-modelo-del-abono-alta-manual-generaci-n-forward/06-CONTEXT.md

# El ÚNICO cron diario — patrón piggyback (ver writeMonthlySnapshot)
@app/api/cron/cancel-expired/route.ts
@lib/abono-generation.ts
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bloque de extensión de ventana de abonos en cancel-expired (piggyback, best-effort)</name>
  <read_first>
    - app/api/cron/cancel-expired/route.ts — el gate de secreto (`authorization` === `Bearer ${CRON_SECRET}` → 401), el uso de createAdminClient() (service-role, no hay sesión en el cron), y el patrón `writeMonthlySnapshot`: función best-effort en su propio try/catch que corre al final y NO aborta el flujo aunque falle.
    - lib/abono-generation.ts — generateAbonoOccurrences (rol-agnóstico: recibe el supabase; acá se le pasa el admin client).
    - lib/booking-window.ts (si existe) o el patrón de "hoy en hora AR" usado en el repo — para calcular la fecha de hoy en America/Argentina/Buenos_Aires (UTC-3) de forma consistente.
  </read_first>
  <files>app/api/cron/cancel-expired/route.ts</files>
  <action>
    Agregar una función `extendAbonoWindows(supabase)` (espejo de `writeMonthlySnapshot`: best-effort, su propio try/catch, devuelve un contador para el JSON de respuesta) y llamarla en el GET tras la lógica de cancel-expired (en ambas ramas de salida — la de "sin holds vencidos" y la del final — igual que hoy se llama writeMonthlySnapshot). Lógica de `extendAbonoWindows`:
    (1) Traer todos los abonos con `status = 'active'` (join con businesses para obtener `id, buffer_minutes, abono_window_weeks` por negocio — el business es el tenant del abono). Con service-role, el filtro por business_id lo aplica el motor internamente; acá se itera por abono.
    (2) Por cada abono: calcular hoy (hora AR) y `toDate = hoy + abono_window_weeks*7` (default 8). `fromDate` = el mayor entre hoy y (`generated_until` + 1 día) si generated_until existe, si no hoy. Si fromDate > toDate, no hay cola nueva → saltear ese abono. Si hay cola: `generateAbonoOccurrences({ supabase, business: { id, buffer_minutes }, abono, fromDate, toDate })`, luego UPDATE de la fila del abono: `generated_until = toDate` y `skipped_occurrences = <existentes> ++ result.skipped` (append, no reemplazo — acumular; acotar el array a un tope razonable si crece mucho), filtrando el UPDATE por id + business_id.
    (3) Acumular contadores (abonos procesados, turnos generados, ocurrencias salteadas) y devolverlos; sumarlos al JSON de respuesta del cron (`{ cancelled, ..., abonosExtended, abonoOccurrencesGenerated }`).
    Mantener intacto el gate del secreto de cron (D-10) y el resto de cancel-expired. La extensión NO debe agregar un cron nuevo ni cambiar `vercel.json`.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && grep -cE "generateAbonoOccurrences" app/api/cron/cancel-expired/route.ts</automated>
  </verify>
  <done>El cron diario extiende cada abono activo por su ventana, avanza generated_until, acumula skipped, mantiene el gate del secreto y no aborta cancel-expired si la extensión falla. No se tocó vercel.json. Compila.</done>
</task>

<task type="auto">
  <name>Task 2: Tests de la extensión del cron (frontera rolling, idempotencia, secreto, aislamiento)</name>
  <read_first>
    - test/manual-booking.test.ts / test/concurrency.test.ts — patrón de test contra la DB local + fixtures.
    - app/api/cron/cancel-expired/route.ts — cómo se invoca el GET con el header de autorización en un test.
  </read_first>
  <files>test/abono-cron.test.ts</files>
  <action>
    Crear `test/abono-cron.test.ts`:
    (1) Extensión: sembrar un abono active con generated_until en el pasado cercano → una corrida del cron genera la cola nueva hasta hoy+ventana, y generated_until avanza a esa fecha.
    (2) Idempotencia: correr el cron dos veces seguidas → la segunda no crea duplicados (generated_until ya cubre la ventana; no hay cola nueva).
    (3) Conflicto: si una ocurrencia de la cola choca con un turno pre-existente → se saltea y se acumula en skipped_occurrences del abono, sin pisar el turno ajeno, y el resto se genera.
    (4) Secreto: un GET sin `Bearer ${CRON_SECRET}` correcto → 401 y no se genera nada.
    (5) Aislamiento: dos abonos de negocios distintos → cada uno genera solo en su propio business (sin cruce).
    Limpieza por business_id entre tests.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx vitest run test/abono-cron.test.ts</automated>
  </verify>
  <done>Los casos de frontera rolling, idempotencia, conflicto-no-pisa, gate de secreto y aislamiento pasan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| invocador HTTP → cron | Solo Vercel Cron con el CRON_SECRET puede disparar la extensión |
| cron (service-role) → abonos/appointments | Sin sesión: RLS no protege; el aislamiento lo da el filtro por business_id en el motor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14 | Spoofing | GET /api/cron/cancel-expired | mitigate | gate existente `authorization === Bearer ${CRON_SECRET}` → 401; la extensión no lo relaja (D-10) |
| T-06-15 | Tampering | generación del cron → appointments | mitigate | pasa por generateAbonoOccurrences → createAppointmentCore (atómico); conflicto se saltea, nunca pisa (D-06) |
| T-06-16 | Information Disclosure / cross-tenant | service-role sin RLS | mitigate | el motor filtra toda query por business_id del abono; los UPDATE del abono llevan `.eq('business_id', ...)`; test de aislamiento |
| T-06-17 | Denial of Service | cron largo / muchos abonos | mitigate | por abono solo se genera la COLA (generated_until→hoy+ventana), típicamente ~1 semana nueva por corrida; best-effort en try/catch (un abono que falla no frena el resto ni cancel-expired) |
| T-06-18 | Availability | agregar un cron más frecuente | mitigate | se hace piggyback en el cron diario existente; NO se toca vercel.json (Vercel Hobby = 1/día) |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | esta fase NO instala paquetes nuevos |
</threat_model>

<verification>
- `npx tsc --noEmit` pasa.
- `npx vitest run test/abono-cron.test.ts` verde.
- `vercel.json` sin cambios (sigue con el único cron diario).
</verification>

<success_criteria>
El cron diario extiende la ventana de cada abono activo por el motor atómico, es idempotente y aislado por tenant, respeta el secreto de cron y no agrega crons; los tests lo prueban.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/06-modelo-del-abono-alta-manual-generaci-n-forward/06-04-SUMMARY.md` when done
</output>
