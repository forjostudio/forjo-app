---
phase: 06-modelo-del-abono-alta-manual-generaci-n-forward
plan: 06
type: execute
wave: 5
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05]
gap_closure: true
files_modified:
  - supabase/migrations/054_abonos.sql
  - supabase/schema.sql
  - lib/types.ts
  - lib/abono-generation.ts
  - test/abono-generation.test.ts
  - app/api/abonos/create/route.ts
  - test/abono-create.test.ts
  - app/api/cron/cancel-expired/route.ts
  - test/abono-cron.test.ts
  - components/dashboard/nuevo-abono-form.tsx
  - app/(dashboard)/abonos/abonos-client.tsx
  - app/(dashboard)/abonos/page.tsx
requirements: [ABONO-02, ABONO-07]
must_haves:
  truths:
    - "El abono NO se saltea por horario semanal (out_of_hours eliminado); sólo saltea por día cerrado (excepción closed=true) o por conflicto real del core (slot_taken/slot_full/espacio) — D-06′"
    - "Cada abono es indefinido (total_occurrences null) o finito de N sesiones; al llegar al turno N pasa a status='completed' y el cron deja de extenderlo — D-07′ / ABONO-07"
    - "Un choque NO consume sesión: el finito busca N turnos REALES (maxCreated = N − generados), reusando el rolling window; idempotencia + cron convergen a N"
    - "El anti-doble-booking del core queda intacto: quitar la guarda de horario no relaja 011/013/cupo/espacio; total_occurrences no participa de ninguna constraint"
    - "El detalle del abono muestra 'Último: <fecha del último turno real>' (no generated_until) y, si es finito, 'Sesiones: X de N'"
  artifacts:
    - path: "supabase/migrations/054_abonos.sql"
      provides: "abonos.total_occurrences int null + status check con 'completed' (migración EDITADA, aún no en prod)"
      contains: "total_occurrences"
    - path: "lib/abono-generation.ts"
      provides: "motor sin skip out_of_hours (sólo day_closed) + parámetro maxCreated para finitos"
      exports: ["generateAbonoOccurrences"]
---

<objective>
Ajustes post-UAT del abono (Plan de cierre de la Phase 6, D-06′/D-07′/D-09′). Tres cambios:
(1) el abono del dueño deja de gatearse por horario semanal (se quita el skip `out_of_hours`; queda
sólo `day_closed`), (2) cada abono es **indefinido o finito de N sesiones** (nueva columna
`total_occurrences`, estado `completed`, el motor acepta `maxCreated`), (3) el detalle muestra el
**último turno real** y las **sesiones X de N**. El núcleo anti-doble-booking NO se toca.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/06-modelo-del-abono-alta-manual-generaci-n-forward/06-CONTEXT.md
@lib/abono-generation.ts
@lib/booking-core.ts
@app/api/abonos/create/route.ts
@app/api/cron/cancel-expired/route.ts
@app/(dashboard)/abonos/abonos-client.tsx
@components/dashboard/nuevo-abono-form.tsx
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 054 (editar) — total_occurrences + status 'completed' + schema + tipos</name>
  <action>
    La migración **054 NO está en prod** (última en prod = 053) → se EDITA en su lugar (no una migración nueva).
    En `supabase/migrations/054_abonos.sql`:
    - Agregar a la tabla `abonos` la columna `total_occurrences int` (NULLABLE; null=indefinido, N=finito)
      con `check (total_occurrences is null or total_occurrences > 0)`. Comentario: cantidad total de
      sesiones del abono finito; null = indefinido (rolling).
    - Cambiar el check de `status` para incluir `'completed'`: `status in ('active','cancelled','completed')`
      (el abono finito pasa a completed al generar su último turno). Mantener default 'active'.
    Regenerar `supabase/schema.sql` (patrón quirúrgico del repo: insertar sólo el diff de la 054, no
    reordenar todo el dump). En `lib/types.ts`: `interface Abono` suma `total_occurrences: number | null`
    y el union de `status` pasa a `'active' | 'cancelled' | 'completed'`.
    Verify: `supabase db reset` (replaya baseline+040..054 e idempotente) + `npx tsc --noEmit`.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && supabase db reset && npx tsc --noEmit && grep -c "total_occurrences" supabase/migrations/054_abonos.sql</automated>
  </verify>
  <done>054 tiene total_occurrences + status 'completed', idempotente; schema.sql y types reflejan el cambio; tsc pasa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Motor — quitar skip out_of_hours (dejar day_closed) + parámetro maxCreated</name>
  <read_first>
    - lib/abono-generation.ts líneas ~95-193: la guarda (3) actual (excepciones + grilla semanal) y el loop.
  </read_first>
  <files>lib/abono-generation.ts, test/abono-generation.test.ts</files>
  <action>
    (1) **Quitar el skip `out_of_hours`** (D-06′): eliminar la lectura de `time_blocks` (weeklyBlocks/withinWeeklyGrid),
    el narrowing de horario especial `closed=false` (líneas ~142-153) y la rama `else` de la grilla semanal
    (~154-161). La guarda (3) queda reducida a: traer las `schedule_exceptions` de esa fecha aplicables
    (global o del location del abono) y **saltear con reason `'day_closed'` SOLO si `closed === true`**.
    Cualquier otra fecha (incluso fuera del horario semanal, o con excepción `closed=false`) sigue de largo
    hacia el core. El core (anti-doble-booking) NO se toca: un slot tomado sigue devolviendo slot_taken.
    (2) **maxCreated (D-07′/finito):** sumar `maxCreated?: number` al input. Si viene, cuando
    `created.length >= maxCreated` **cortar el loop** (break). El motor sigue siendo puro/range-based: el
    caller pasa `maxCreated = N − generados` para el finito. Un choque NO cuenta (sólo `created` suma), así
    que se buscan N turnos reales dentro del rango.
    Actualizar el comentario denso del "por qué" (el dueño no se gatea por horario; el finito no consume sesión al chocar).
    Tests (`test/abono-generation.test.ts`): (a) **reemplazar** el caso de horario especial `out_of_hours`
    por uno que verifique que una ocurrencia FUERA de la grilla semanal (ej. 21:00 sin time_block) **SÍ se
    genera** ahora; (b) día cerrado (`closed=true`) sigue salteando con `day_closed`; (c) nuevo: `maxCreated=2`
    sobre un rango de 5 semanas → genera exactamente 2 turnos y corta; (d) mantener iteración/no-pisa/idempotencia/aislamiento.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npx vitest run test/abono-generation.test.ts --no-file-parallelism</automated>
  </verify>
  <done>El motor no saltea por horario semanal (sólo day_closed), acepta maxCreated y corta al alcanzarlo; los tests lo prueban (incluye el 21:00 que ahora genera). Sin insert directo ni RPC (regla dura intacta).</done>
</task>

<task type="auto">
  <name>Task 3: Alta + cron — abono finito/indefinido (total_occurrences, maxCreated, completed)</name>
  <read_first>
    - app/api/abonos/create/route.ts — parseo del body, persistencia de generated_until + skipped_occurrences, llamada al motor.
    - app/api/cron/cancel-expired/route.ts — extendAbonoWindows (piggyback best-effort).
  </read_first>
  <files>app/api/abonos/create/route.ts, test/abono-create.test.ts, app/api/cron/cancel-expired/route.ts, test/abono-cron.test.ts</files>
  <action>
    **Alta** (`/api/abonos/create`): aceptar `totalOccurrences` en el body (narrowing defensivo: entero > 0 → finito,
    ausente/null/0 → indefinido). Persistir en la fila `abonos.total_occurrences`. Al generar la primera tanda:
    - Indefinido: como hoy (horizonte = business.abono_window_weeks, sin maxCreated).
    - Finito N: `maxCreated = N` (aún no hay generados), horizonte = el mismo rolling window; tras generar,
      contar los turnos reales del abono (appointments no cancelados con ese abono_id); si `count >= N` →
      `status='completed'`. (El cron completa el resto si N no entró en la primera ventana.)
    **Cron** (`extendAbonoWindows`): procesar sólo abonos `status='active'`. Por cada uno:
    - Contar generados reales (appointments no cancelados con abono_id) = `gen`.
    - Finito (`total_occurrences = N`): si `gen >= N` → marcar `completed` y saltear. Si no,
      `maxCreated = N − gen`; generar con ese tope; recontar; si llega a N → `status='completed'`.
    - Indefinido: como hoy (sin maxCreated). Mantener el cap `.slice(-50)` de skipped_occurrences, el gate
      del CRON_SECRET, el try/catch best-effort y el aislamiento por business_id.
    Tests: alta finita (crea N, o marca completed si N entró); cron completa un finito hasta N y lo marca
    completed; cron NO toca abonos completed; indefinido sigue rolling. Mantener los casos de secreto/aislamiento.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npx vitest run test/abono-create.test.ts test/abono-cron.test.ts --no-file-parallelism</automated>
  </verify>
  <done>El alta persiste total_occurrences y genera respetando finito/indefinido; el cron completa los finitos y los marca completed sin re-tocarlos; los tests pasan.</done>
</task>

<task type="auto">
  <name>Task 4: UI — Duración (Indefinido/N sesiones) + detalle 'Último'/'Sesiones X de N'</name>
  <read_first>
    - components/dashboard/nuevo-abono-form.tsx — dónde arma el body del POST y los campos.
    - app/(dashboard)/abonos/abonos-client.tsx — el detalle (línea del "Hasta" con generated_until) y la lista.
    - app/(dashboard)/abonos/page.tsx — la carga de abonos (para sumar el último turno real por abono).
  </read_first>
  <files>components/dashboard/nuevo-abono-form.tsx, app/(dashboard)/abonos/abonos-client.tsx, app/(dashboard)/abonos/page.tsx</files>
  <action>
    (1) **Form** (`nuevo-abono-form`): agregar un control **"Duración"** con dos opciones — **Indefinido**
    (default) y **N sesiones** (muestra un input numérico ≥1 cuando se elige). Enviar `totalOccurrences: N`
    (finito) o `null`/omitir (indefinido) en el POST. Reusar los componentes/tokens existentes (radio/segmented
    del design system, label visible, estados). Copy claro: "Indefinido (se repite hasta cancelar)" / "N sesiones (termina)".
    (2) **page.tsx**: al cargar los abonos, incluir por abono la **fecha del último turno real** (max `date` de
    appointments no cancelados con ese abono_id) y el **conteo** de generados, filtrando por business_id. Pasar al client.
    (3) **abonos-client** detalle: cambiar el campo **"Hasta" → "Último"** mostrando esa fecha real del último
    turno (no `generated_until`); si el abono es **finito**, agregar una línea **"Sesiones: X de N"** (X=generados,
    N=total_occurrences). En la lista, para finitos mostrar "N sesiones" y para completed un chip "Completado".
    Reword del helper del control de ventana a nivel negocio: aclarar "(se aplica a los abonos indefinidos)".
    Todo con el design system, WCAG AA, mobile 375px, sin hardcodear hex.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npx eslint app/\(dashboard\)/abonos components/dashboard/nuevo-abono-form.tsx</automated>
  </verify>
  <done>El form permite elegir Indefinido/N sesiones; el detalle muestra "Último: <fecha real>" y "Sesiones X de N" para finitos; tsc + eslint limpios.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-22 | Tampering | motor sin guarda de horario | mitigate | quitar el skip out_of_hours NO relaja el core: 011/013/cupo/espacio siguen atómicos; day_closed se conserva; test 21:00-genera + no-pisa |
| T-06-23 | Tampering | total_occurrences / maxCreated | mitigate | columna metadata (no participa de constraints); maxCreated sólo ACOTA la generación (nunca la fuerza); finito converge a N por idempotencia |
| T-06-24 | DoS | finito con muchos choques | mitigate | maxCreated corta al llegar a N; el rango por corrida está acotado por el rolling window; el cron avanza de a una ventana por día |
| T-06-25 | Information Disclosure | totalOccurrences/last-date del cliente | mitigate | toda query por business_id; totalOccurrences lo re-valida el endpoint; el último-turno se lee acotado por business_id |
</threat_model>

<verification>
- `supabase db reset` + `npx tsc --noEmit` limpios.
- `npx vitest run test/abono-generation.test.ts test/abono-create.test.ts test/abono-cron.test.ts --no-file-parallelism` verde.
- `grep -E "out_of_hours" lib/abono-generation.ts` → vacío (el skip por horario semanal se eliminó; queda day_closed).
- Regla dura intacta: sin `.rpc('book_slot_atomic')` ni `.from('appointments').insert` en el motor.
</verification>

<output>
Create `.planning/workstreams/motor-reservas/phases/06-modelo-del-abono-alta-manual-generaci-n-forward/06-06-SUMMARY.md` when done
</output>
