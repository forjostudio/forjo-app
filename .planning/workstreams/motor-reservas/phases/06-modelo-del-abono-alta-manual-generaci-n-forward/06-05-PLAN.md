---
phase: 06-modelo-del-abono-alta-manual-generaci-n-forward
plan: 05
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - app/(dashboard)/abonos/page.tsx
  - app/(dashboard)/abonos/abonos-client.tsx
  - components/dashboard/nuevo-abono-form.tsx
  - app/(dashboard)/agenda/agenda-client.tsx
autonomous: false
requirements: [ABONO-01]
user_setup: []
must_haves:
  truths:
    - "El dueño arma un abono desde el panel (cliente + servicio/cancha + profesional/consultorio según vertical + día de la semana + hora) reusando los patrones del form 'Nuevo turno'"
    - "El dueño configura la ventana de generación (abono_window_weeks) a nivel negocio desde la UI (D-07)"
    - "El turno que viene de un abono se distingue con un badge 'fijo/abono' en la agenda (vía abono_id, D-09)"
    - "El detalle del abono muestra la serie + las ocurrencias salteadas por conflicto (skipped_occurrences, D-06)"
  artifacts:
    - path: "components/dashboard/nuevo-abono-form.tsx"
      provides: "Form de alta del abono (día de la semana + hora, sin fecha puntual) que postea a /api/abonos/create"
      min_lines: 40
    - path: "app/(dashboard)/abonos/abonos-client.tsx"
      provides: "Lista de abonos + detalle (serie + ocurrencias salteadas) + control de ventana"
    - path: "app/(dashboard)/agenda/agenda-client.tsx"
      provides: "Badge 'fijo/abono' en los turnos con abono_id"
      contains: "abono"
  key_links:
    - from: "components/dashboard/nuevo-abono-form.tsx"
      to: "app/api/abonos/create/route.ts"
      via: "fetch POST al crear el abono"
      pattern: "abonos/create"
    - from: "app/(dashboard)/agenda/agenda-client.tsx"
      to: "appointments.abono_id"
      via: "renderiza el badge cuando el turno tiene abono_id"
      pattern: "abono_id"
---

<objective>
Superficie de UI del abono (D-04/D-07/D-09/D-06): (1) una página en el panel para armar el abono reusando los patrones del form "Nuevo turno" (Dialog/Drawer + combobox de cliente + selects de servicio/profesional/consultorio), pero pidiendo **día de la semana + hora** en vez de una fecha puntual, posteando a `/api/abonos/create`; (2) un control para configurar la **ventana de generación** (`abono_window_weeks`) a nivel negocio; (3) el **badge "fijo/abono"** en la agenda para los turnos con `abono_id`; (4) el **detalle del abono** con la serie y las ocurrencias salteadas por conflicto (`skipped_occurrences`). Cierra con checkpoint humano de verificación visual/funcional.

Purpose: Es la cara visible de la fase para el dueño. Reusa el design system del proyecto (no hay lenguaje visual nuevo).
Output: página + client de abonos, `nuevo-abono-form.tsx`, badge en `agenda-client.tsx`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/phases/06-modelo-del-abono-alta-manual-generaci-n-forward/06-CONTEXT.md

# Patrón de form a reusar (Dialog/Drawer responsive, combobox de cliente, selects por vertical)
@components/dashboard/nuevo-turno-form.tsx
@app/(dashboard)/agenda/agenda-client.tsx
@app/(dashboard)/agenda/page.tsx
@lib/types.ts

# Convenciones de UI/marca del proyecto
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Página de abonos + form de alta (día de la semana + hora) + control de ventana</name>
  <read_first>
    - components/dashboard/nuevo-turno-form.tsx — estructura COMPLETA: shell responsive Dialog(desktop)/Drawer(mobile), combobox de cliente con dedupe/creación inline, selects de service/professional/location, manejo por vertical, estados de saving, fetch al endpoint. Reusar estos patrones tal cual (NO inventar UI nueva).
    - app/(dashboard)/agenda/page.tsx — cómo la page server carga clients/services/professionals/locations + business (por owner_id) y pasa props al client. Replicar para la page de abonos.
    - lib/types.ts — Abono, Business.abono_window_weeks.
  </read_first>
  <files>app/(dashboard)/abonos/page.tsx, app/(dashboard)/abonos/abonos-client.tsx, components/dashboard/nuevo-abono-form.tsx</files>
  <action>
    (1) `app/(dashboard)/abonos/page.tsx` — Server Component async (patrón agenda/page.tsx): supabase = await createClient(); auth.getUser() → redirect('/login') si no hay; business por owner_id; cargar en paralelo (Promise.all) clients, services, professionals, locations, y los abonos del negocio (`.eq('business_id', business.id)`, con joins a client/service/professional para mostrarlos), más `business.abono_window_weeks`. Pasar todo al client. Aislamiento por tenant en toda query (`.eq('business_id', business.id)`).
    (2) `components/dashboard/nuevo-abono-form.tsx` — clon adaptado de nuevo-turno-form: MISMO shell Dialog/Drawer + combobox de cliente + selects de service/professional/location por vertical, PERO el campo temporal es **día de la semana** (select 0..6 con labels en español, convención domingo..sábado) + **hora** (mismo input de hora que el form de turno), SIN date-picker de fecha puntual. En el vertical canchas, el select es de cancha (professional con service_id) igual que el form de turno lo maneja. Al enviar: `fetch('/api/abonos/create', { method:'POST', body: JSON con clientId/clientName/clientPhone/clientEmail, serviceId, professionalId, locationId, dayOfWeek, time })`; toast de éxito/error (sonner) y refresco de la lista; estado `saving` para evitar doble submit. Copy: dejar claro que es un turno FIJO semanal indefinido.
    (3) `app/(dashboard)/abonos/abonos-client.tsx` — 'use client': botón "Nuevo abono" que abre el form; lista de abonos activos (cliente, servicio, día+hora, cantidad de turnos generados). Un control de **ventana de generación** (input numérico "generar N semanas hacia adelante", default 8) que persiste `businesses.abono_window_weeks` reusando EXACTAMENTE el mismo mecanismo owner-autenticado con que Ajustes persiste `max_advance_days` (Phase 4) — localizar ese patrón y espejarlo (server action o endpoint de update de business por owner_id); no crear un mecanismo nuevo si ya existe uno. Feedback con toast.
    Registrar la ruta /abonos en el menú/sidebar del dashboard donde corresponde (mismo patrón que las demás secciones), gateando por vertical si aplica (el abono aplica a todos los verticales que reservan).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npx eslint app/\(dashboard\)/abonos components/dashboard/nuevo-abono-form.tsx</automated>
  </verify>
  <done>Existe la sección /abonos con la lista, el form de alta (día de la semana + hora, no fecha) que postea a /api/abonos/create, y el control de ventana que persiste abono_window_weeks por el mismo camino que max_advance_days. tsc + eslint limpios.</done>
</task>

<task type="auto">
  <name>Task 2: Badge 'fijo/abono' en la agenda + detalle del abono (serie + ocurrencias salteadas)</name>
  <read_first>
    - app/(dashboard)/agenda/agenda-client.tsx — dónde se renderiza cada turno (la tarjeta/celda del turno) y cómo llegan los appointments; agregar el badge sin romper el layout existente. Verificar que la query que alimenta la agenda traiga `abono_id` (si no, sumarlo al select en agenda/page.tsx — archivo ya en scope del Plan por el badge; si el select vive en page.tsx, incluir abono_id ahí).
    - lib/types.ts — Appointment.abono_id y Abono.skipped_occurrences.
    - components/ui/badge.tsx (o el patrón de badge/chip usado en el proyecto) — reusar el componente existente, no crear uno nuevo.
  </read_first>
  <files>app/(dashboard)/agenda/agenda-client.tsx, app/(dashboard)/abonos/abonos-client.tsx</files>
  <action>
    (1) En `agenda-client.tsx`: cuando un turno tiene `abono_id` no nulo, renderizar un badge/chip sutil "Fijo" (o "Abono") reusando el componente de badge existente del design system (colores/tokens del proyecto, sin hardcodear hex). El badge NO debe alterar el layout ni tapar información; ubicarlo de forma consistente con otros indicadores de la tarjeta del turno. Asegurar que `abono_id` llega en los datos del turno (ajustar el select en agenda/page.tsx si hace falta — solo agregar la columna, sin tocar el resto).
    (2) En `abonos-client.tsx`: al abrir un abono, mostrar el detalle: la serie (día+hora, cliente, servicio, estado) + la lista de **ocurrencias salteadas** leídas de `skipped_occurrences` (fecha + razón traducida a español: slot ocupado / cupo lleno / día cerrado / fuera de horario / conflicto de espacio). Esto le avisa al dueño qué semanas no se pudieron reservar (D-06). Reusar Dialog/Drawer y los patrones de lista existentes.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && grep -cE "abono_id" app/\(dashboard\)/agenda/agenda-client.tsx</automated>
  </verify>
  <done>Los turnos de abono muestran el badge en la agenda (reusando el badge del design system) y el detalle del abono lista las ocurrencias salteadas con razón en español. tsc pasa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Verificación visual/funcional del flujo de abonos</name>
  <what-built>Sección /abonos (lista + form de alta día-de-la-semana + hora + control de ventana), badge "fijo/abono" en la agenda, y detalle del abono con las ocurrencias salteadas.</what-built>
  <how-to-verify>
    1. Correr `npm run dev` y entrar como un negocio de prueba.
    2. Ir a /abonos → "Nuevo abono": elegir un cliente, un servicio (o cancha en el vertical canchas), profesional/consultorio si aplica, un día de la semana y una hora. Guardar.
    3. Confirmar el toast de éxito y que el abono aparece en la lista con la cantidad de turnos generados.
    4. Ir a la Agenda y verificar que aparecen los turnos generados en el día/hora elegidos en las próximas semanas, cada uno con el badge "Fijo/Abono".
    5. En /abonos, abrir el detalle del abono y verificar que se ve la serie; si sembraste un conflicto (un turno manual en uno de esos slots antes de crear el abono), esa semana debe figurar como "salteada" con su razón.
    6. Cambiar el control de "ventana" (ej. de 8 a 4 semanas) y confirmar que persiste (recargar la página).
    7. Verificación visual (reglas del proyecto): contraste WCAG AA del badge, form legible en mobile (375px), estados de hover/focus/disabled en botones, un solo CTA claro por pantalla.
  </how-to-verify>
  <action>Checkpoint humano: correr `npm run dev`, crear un abono desde /abonos, verificar la generación de turnos con badge en la agenda, el detalle con ocurrencias salteadas y la persistencia del control de ventana; más el chequeo visual (WCAG AA, mobile 375px, estados interactivos). No hay pasos automatizables adicionales: es verificación de UI en vivo.</action>
  <acceptance_criteria>
    - behavior: crear un abono genera N turnos en el día/hora elegidos, cada uno con badge "Fijo/Abono" en la agenda
    - behavior: el detalle del abono lista las ocurrencias salteadas (con razón) cuando hay conflicto sembrado
    - behavior: el control de ventana (abono_window_weeks) persiste tras recargar
    - visual: contraste WCAG AA del badge, form usable en mobile 375px, estados hover/focus/disabled presentes
  </acceptance_criteria>
  <resume-signal>Escribí "aprobado" o describí los ajustes visuales/funcionales necesarios.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño (client) → /api/abonos/create | El form postea datos que el server re-valida por business_id (la UI solo sugiere) |
| page server → datos del negocio | Toda query de la page filtra por business_id resuelto por owner_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-19 | Tampering | inputs del form (serviceId/professionalId/clientId/dayOfWeek/time) | mitigate | la autoridad es el endpoint (Plan 03), que re-valida por business_id; la UI no es fuente de verdad de aislamiento |
| T-06-20 | Information Disclosure | page.tsx de abonos + select de agenda | mitigate | toda query `.eq('business_id', business.id)`; no se expone skipped_occurrences ni abonos a otro tenant; abono_id es un id opaco sin datos sensibles |
| T-06-21 | Tampering | control de ventana (abono_window_weeks) | mitigate | persiste por el mismo camino owner-autenticado que max_advance_days (business por owner_id + RLS owner-only); no es columna admin protegida |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | esta fase NO instala paquetes nuevos (se reusan componentes/design system existentes) |
</threat_model>

<verification>
- `npx tsc --noEmit` y `npx eslint` limpios en los archivos nuevos/tocados.
- Checkpoint humano aprobado (flujo de alta + badge + detalle + persistencia de ventana + verificación visual WCAG/mobile).
</verification>

<success_criteria>
El dueño crea y ve abonos desde el panel con los patrones existentes, configura la ventana, distingue los turnos de abono por el badge en la agenda y ve las ocurrencias salteadas; verificado por checkpoint humano.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/06-modelo-del-abono-alta-manual-generaci-n-forward/06-05-SUMMARY.md` when done
</output>
