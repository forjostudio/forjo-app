---
phase: 06-modelo-del-abono-alta-manual-generaci-n-forward
plan: 08
type: execute
wave: 7
depends_on: [06-06, 06-07]
gap_closure: true
security_gap: GAP-01
closes_threats: [T-06-08, T-06-17, T-06-24]
files_modified:
  - supabase/migrations/055_abono_window_bounds.sql
  - supabase/schema.sql
  - lib/abono-generation.ts
  - app/api/abonos/create/route.ts
  - app/api/cron/cancel-expired/route.ts
  - app/(dashboard)/abonos/abonos-client.tsx
  - test/abono-generation.test.ts
must_haves:
  truths:
    - "El servidor NUNCA usa abono_window_weeks sin acotar: los dos callers lo normalizan a un entero en 1..52 (default 8) antes de calcular el rango"
    - "El motor no puede loopear sin fin: valida el formato de fromDate/toDate y tiene un tope duro de iteraciones como backstop"
    - "La DB rechaza valores fuera de 1..52 (CHECK), con los valores existentes NORMALIZADOS antes de crear la constraint"
    - "Un dueño no puede degradar el cron diario compartido por todos los tenants"
---

<objective>
Cerrar **GAP-01** (blocker de la auditoría de seguridad): `businesses.abono_window_weeks` es
owner-writable y **no tiene techo en ninguna capa**, y dimensiona el loop del motor **dentro del único
cron diario compartido por todos los negocios**. Con `999999` son ~1.000.000 de iteraciones (2 queries
cada una); con `2147483647` el `toDate` queda `'NaN-NaN-NaN'`, la guarda `fromDate > toDate`
(comparación de STRINGS: `'2026-…' > 'NaN-…'` es `false`) no dispara y **el loop no termina**.
Cierra T-06-08, T-06-17 y T-06-24.

Defensa en profundidad en 3 capas: (1) clamp server-side en los callers — la corrección real, (2)
endurecer el motor para que ningún rango inválido pueda loopear, (3) `CHECK` en la DB.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Clamp server-side en los dos callers (la corrección real)</name>
  <read_first>
    - app/api/abonos/create/route.ts:191 — `const windowWeeks = Number(business.abono_window_weeks) || 8`
    - app/api/cron/cancel-expired/route.ts:141 — idéntico, sobre `biz.abono_window_weeks`
    - Ambos archivos ya duplican helpers chicos por convención (SKIPPED_CAP, toISODate, countAbonoAppointments) — seguir esa convención, no crear un lib nuevo.
  </read_first>
  <files>app/api/abonos/create/route.ts, app/api/cron/cancel-expired/route.ts</files>
  <action>
    En CADA route agregar un helper `clampWindowWeeks(raw: unknown): number` con la misma semántica en los
    dos: `Math.trunc(Number(raw))`; si no es finito o es < 1 → default **8**; si es > **52** → **52**.
    Devuelve siempre un entero en 1..52. Reemplazar los dos `Number(...) || 8` por este helper.
    Comentario en español explicando el POR QUÉ (dimensiona el loop del motor dentro del cron diario
    COMPARTIDO: un valor absurdo de un solo dueño degradaba la generación de todos los tenants — GAP-01,
    T-06-08/17/24). El servidor no confía en la columna aunque la DB tenga CHECK: la constraint es
    defensa en profundidad, no la única barrera.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && grep -c "clampWindowWeeks" app/api/abonos/create/route.ts app/api/cron/cancel-expired/route.ts</automated>
  </verify>
  <done>Ningún caller usa el valor crudo; los dos lo acotan a 1..52 antes de calcular el rango.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Endurecer el motor — rango válido + tope duro de iteraciones</name>
  <read_first>
    - lib/abono-generation.ts:76-82 (la guarda `if (fromDate > toDate) return`) y el `for` (~:109).
  </read_first>
  <files>lib/abono-generation.ts, test/abono-generation.test.ts</files>
  <action>
    El motor es la última línea: debe ser imposible que loopee sin fin **con cualquier entrada**, venga de
    donde venga (hoy la guarda es una comparación de strings, frágil ante cualquier fecha rara).
    (1) **Validar formato**: si `fromDate` o `toDate` no matchean `/^\d{4}-\d{2}-\d{2}$/`, devolver
    `{ created: [], skipped: [] }` inmediatamente (no lanzar: el caller es best-effort). Esto sola ya mata
    el caso `'NaN-NaN-NaN'`.
    (2) **Tope duro de iteraciones**: constante a nivel módulo (ej. `MAX_OCCURRENCES_PER_RUN = 520`, 10
    años de semanas) y cortar el `for` al alcanzarlo, aunque el rango pidiera más. Backstop puro: en
    operación normal (ventana ≤52) nunca se toca.
    Comentario denso en español: por qué el string-compare no alcanzaba y por qué el tope existe.
    NO cambiar la semántica de generación normal (mismos created/skipped para rangos válidos).
    Tests: (a) `fromDate`/`toDate` inválidos (incl. `'NaN-NaN-NaN'`) → devuelve vacío sin tocar la DB y
    SIN colgarse; (b) un rango absurdamente largo (ej. 50 años) termina y no supera el tope; (c) los casos
    existentes siguen verdes (no hay cambio de comportamiento en rangos normales).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npx vitest run test/abono-generation.test.ts --no-file-parallelism</automated>
  </verify>
  <done>Con entradas inválidas el motor devuelve vacío al toque; ningún rango puede superar el tope de iteraciones; los casos previos siguen pasando.</done>
</task>

<task type="auto">
  <name>Task 3: Migración 055 — normalizar + CHECK 1..52</name>
  <read_first>
    - supabase/migrations/054_abonos.sql — cabecera/estilo idempotente e invariantes del repo.
    - supabase/schema.sql:392 — `"abono_window_weeks" integer DEFAULT 8` (sin CHECK).
  </read_first>
  <files>supabase/migrations/055_abono_window_bounds.sql, supabase/schema.sql</files>
  <action>
    ⚠ La **054 YA ESTÁ EN PROD** (2026-07-21) → NO se edita en el lugar. Esta es una migración **NUEVA**:
    `055_abono_window_bounds.sql`, idempotente, NO aplicada por el flujo (se coordina a mano).
    Orden OBLIGATORIO (si no, la constraint no puede crearse):
    (1) **Normalizar primero** los valores existentes fuera de rango:
        `UPDATE public.businesses SET abono_window_weeks = LEAST(GREATEST(abono_window_weeks, 1), 52)
         WHERE abono_window_weeks IS NOT NULL AND (abono_window_weeks < 1 OR abono_window_weeks > 52);`
        (clampea preservando la intención en vez de resetear a 8). Esto importa: durante la auditoría se
        persistieron 5000 / 999999 / 2147483647 en la DB local.
    (2) **Después** agregar la constraint, idempotente (DO block / `IF NOT EXISTS` según el patrón del repo):
        `CHECK (abono_window_weeks IS NULL OR abono_window_weeks BETWEEN 1 AND 52)`
        nombre `businesses_abono_window_weeks_range`. Debe permitir NULL (la columna es nullable con default 8).
    Cabecera al estilo 054: qué hace, por qué (GAP-01), que es defensa en profundidad del clamp
    server-side, y las invariantes (validar con `supabase db reset` local; prod a mano + `NOTIFY pgrst, 'reload schema';`).
    NO tocar la tabla `abonos`, ni `book_slot_atomic`, ni ninguna policy. Regenerar `supabase/schema.sql`
    quirúrgicamente (sólo el diff de la 055), sin reordenar el dump.
    NO incluir la FK compuesta de UF-02: `appointments` es una tabla caliente con constraints existentes
    (011/013) y el riesgo no lo justifica hoy (ninguna lectura cruza tenants). Queda anotado como diferido.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && supabase db reset && npx tsc --noEmit && grep -c "abono_window_weeks" supabase/migrations/055_abono_window_bounds.sql</automated>
  </verify>
  <done>Existe la 055 idempotente que normaliza y luego crea el CHECK 1..52 permitiendo NULL; `supabase db reset` replaya limpio; schema.sql refleja sólo ese diff.</done>
</task>

<task type="auto">
  <name>Task 4: Tope en el input de la UI</name>
  <read_first>
    - app/(dashboard)/abonos/abonos-client.tsx:~218 — el input de la ventana (hoy sólo `min={1}`) y su handler de guardado.
  </read_first>
  <files>app/(dashboard)/abonos/abonos-client.tsx</files>
  <action>
    Agregar `max={52}` al input numérico y clampear en el handler antes de persistir (1..52), con un mensaje
    de error claro si el usuario escribe fuera de rango (patrón de toasts ya usado en el archivo). Es UX +
    coherencia con el server: la autoridad sigue siendo el clamp server-side, no el input.
    Actualizar el texto de ayuda para indicar el rango permitido.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npx eslint app/\(dashboard\)/abonos</automated>
  </verify>
  <done>El input no deja pasar valores fuera de 1..52 y el helper explica el rango.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Categoría | Componente | Disposición | Mitigación |
|-----------|-----------|-----------|-------------|------------|
| T-06-08 | DoS | rango del motor | mitigate | validación de formato + tope duro de iteraciones → ningún rango puede loopear sin fin |
| T-06-17 | DoS | cron diario compartido | mitigate | clamp 1..52 server-side en el cron → un tenant no puede degradar la corrida de los demás |
| T-06-24 | DoS | finito con muchos choques | mitigate | el rango por corrida queda acotado por el clamp + el tope del motor |
| T-06-28 | Tampering | abono_window_weeks en DB | mitigate | CHECK 1..52 (migración 055) tras normalizar los valores existentes; defensa en profundidad del clamp |
</threat_model>

<verification>
- `supabase db reset` limpio (baseline + 040..055) y `npx tsc --noEmit` OK.
- `npx vitest run --no-file-parallelism` verde (suite completa).
- Ningún caller usa `abono_window_weeks` crudo; el motor rechaza rangos inválidos y tiene tope.
- Re-correr `/gsd:secure-phase 6 --ws motor-reservas` debe dar `threats_open: 0`.
</verification>

<output>
Create `.planning/workstreams/motor-reservas/phases/06-modelo-del-abono-alta-manual-generaci-n-forward/06-08-SUMMARY.md` when done
</output>
