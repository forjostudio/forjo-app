---
phase: 06-modelo-del-abono-alta-manual-generaci-n-forward
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/054_abonos.sql
  - supabase/schema.sql
  - lib/types.ts
autonomous: true
requirements: [ABONO-01, ABONO-02, ABONO-03]
must_haves:
  truths:
    - "La tabla abonos existe con RLS habilitada y un negocio no puede leer/escribir abonos de otro (owner-only)"
    - "appointments tiene abono_id nullable con FK a abonos, para vincular cada turno a su serie"
    - "businesses tiene abono_window_weeks (default 8) como setting de generación a nivel negocio"
    - "El modelo del abono admite sumar cobro recurrente / pagá-o-liberá a futuro sin re-migrar (columnas nullable ya presentes)"
    - "supabase db reset local replaya el baseline + 040..054 en orden, limpio"
  artifacts:
    - path: "supabase/migrations/054_abonos.sql"
      provides: "Tabla abonos + FK appointments.abono_id + businesses.abono_window_weeks + RLS owner-only"
      contains: "create table if not exists"
    - path: "lib/types.ts"
      provides: "interface Abono + abono_id en Appointment + abono_window_weeks en Business"
      contains: "interface Abono"
  key_links:
    - from: "supabase/migrations/054_abonos.sql"
      to: "supabase/schema.sql"
      via: "schema.sql regenerado tras validar la migración local (patrón del repo)"
      pattern: "abonos"
---

<objective>
Crear el espinazo de datos del abono recurrente: migración **054** (idempotente, numerada, NO auto-aplicada) que crea la tabla `abonos` (D-01), el vínculo `appointments.abono_id` (D-03), el setting de ventana `businesses.abono_window_weeks` (D-07), todo con RLS owner-only y aislamiento por `business_id`/`owner_id` (D-10). El modelo se diseña **extensible sin re-migrar** (D-02): columnas nullable ya presentes para el cobro futuro y el flujo pagá-o-liberá de v0.25, sin construir nada de eso. Más los tipos TS espejo de las filas.

Purpose: Todo lo que sigue (motor de generación, endpoint de alta, cron, UI) depende de este esquema. Es la base de la fase; se planifica primero y sola (Wave 1).
Output: `supabase/migrations/054_abonos.sql`, `supabase/schema.sql` regenerado, tipos en `lib/types.ts`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/06-modelo-del-abono-alta-manual-generaci-n-forward/06-CONTEXT.md

# Skills de dominio (LEER antes de escribir SQL)
@.claude/skills/supabase-multitenant-rls/SKILL.md
@.claude/skills/convenciones-forjo/SKILL.md

# Migraciones de referencia (formato + invariantes del repo)
@supabase/migrations/052_booking_window.sql
@supabase/migrations/053_mp_connection_status.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 054 — tabla abonos + FK abono_id + businesses.abono_window_weeks (RLS owner-only, extensible)</name>
  <read_first>
    - supabase/migrations/052_booking_window.sql y 053_mp_connection_status.sql — cabecera, estilo idempotente, invariantes ("NO se aplica vía push; validar con supabase db reset local; prod a mano + NOTIFY pgrst").
    - supabase/schema.sql líneas ~340-361 (tabla businesses, ver max_advance_days como columna aditiva owner-updatable) y la CREATE POLICY "owner access" ON businesses (línea ~1594) — el vínculo tenant = businesses.owner_id = auth.uid().
    - .claude/skills/supabase-multitenant-rls/SKILL.md — reglas duras: enable RLS en la MISMA migración, una policy por operación, with check en insert/update.
  </read_first>
  <files>supabase/migrations/054_abonos.sql</files>
  <action>
    Crear la migración numerada 054 (idempotente; última en prod = 053). Cabecera en español al estilo 052/053: qué hace, qué NO hace (no se aplica por el flujo; validar con `supabase db reset` local; prod a mano coordinado con el deploy + `NOTIFY pgrst, 'reload schema';`; regenerar schema.sql después). Contenido:

    (1) `create table if not exists public.abonos` con columnas base (D-01): `id uuid primary key default gen_random_uuid()`; `business_id uuid not null` con FK a `businesses(id) on delete cascade`; `client_id uuid` FK a `clients(id) on delete set null`; `service_id uuid` FK a `services(id) on delete restrict` (evita orfandad de generación); `professional_id uuid` FK a `professionals(id) on delete set null` (NULLABLE — bucket sentinela/"sin profesional" según vertical); `location_id uuid` FK a `locations(id) on delete set null` (NULLABLE); `day_of_week smallint not null check (day_of_week between 0 and 6)` (convención EXTRACT(dow): 0=domingo..6=sábado, idéntica a time_blocks.day_of_week y a book_slot_atomic); `start_time time without time zone not null`; `duration_minutes int` (snapshot al crear; la generación usa la duración VIVA del service igual, esto es solo referencia); `status text not null default 'active' check (status in ('active','cancelled'))`; `cancel_token uuid not null default gen_random_uuid()` (token a NIVEL SERIE — lo consume el link de cancelación de Phase 7); `generated_until date` (frontera de la ventana rolling — hace idempotente la generación forward); `skipped_occurrences jsonb not null default '[]'::jsonb` (registro de ocurrencias salteadas por conflicto, D-06: array de objetos {date, reason}); `created_at timestamptz not null default now()`; `cancelled_at timestamptz`.

    (2) Columnas EXTENSIBLES para lo diferido (D-02) — NULLABLE, presentes pero SIN lógica en v0.24: `reminder_lead_hours int` (v0.25: lead-time del recordatorio pagá-o-liberá); `deposit_amount numeric` (futuro: seña por ocurrencia); `billing_subscription_id text` (futuro: referencia a la suscripción de cobro por cliente). Comentar en SQL que son placeholders del modelo extensible y que v0.24 NO los usa.

    (3) `create index if not exists` sobre `abonos(business_id)` y sobre `abonos(business_id, status)` (el cron itera abonos activos por negocio).

    (4) `alter table public.abonos enable row level security;` (misma migración — regla dura del skill). Policies owner-only, UNA por operación, con `drop policy if exists` antes de cada `create policy` (idempotencia): SELECT `using (business_id in (select id from public.businesses where owner_id = (select auth.uid())))`; INSERT `with check (...misma subselect...)`; UPDATE `using (...)` **y** `with check (...)` (impide reasignar a otro tenant); DELETE `using (...)`. Sin policy para `anon` (D-10: nada de abonos al público; las columnas sensibles de D-02 quedan cerradas por no existir read anon).

    (5) `alter table public.appointments add column if not exists abono_id uuid references public.abonos(id) on delete set null;` (D-03 — vínculo turno→serie; `on delete set null` = borrar un abono no borra sus turnos ya generados). `create index if not exists` sobre `appointments(abono_id)`. NO se agrega policy nueva a appointments (ya tiene RLS + policies; la columna es aditiva).

    (6) `alter table public.businesses add column if not exists abono_window_weeks integer default 8;` (D-07 — ventana de generación a nivel negocio, default 8 semanas; el trigger businesses_protect_admin_columns NO la protege → owner-updatable, igual que max_advance_days). NO tocar la vista public_businesses (el público NO necesita este dato; a diferencia de max_advance_days, no viaja al anon).

    NO tocar `book_slot_atomic` en esta migración: el vínculo `abono_id` se setea con un UPDATE acotado JUSTO DESPUÉS del insert atómico (Plan 02) — `abono_id` es una etiqueta que no participa de ninguna constraint, así que setearla fuera del RPC NO relaja el anti-doble-booking y evita un DROP/recreate del SECURITY DEFINER endurecido (menor riesgo al núcleo, D-10).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && supabase db reset</automated>
  </verify>
  <done>La migración 054 existe, es idempotente (correr `supabase db reset` dos veces no falla), crea `abonos` con RLS habilitada + 4 policies owner-only, agrega `appointments.abono_id` (FK on delete set null) y `businesses.abono_window_weeks` (default 8). `book_slot_atomic` queda intacto. `supabase db reset` replaya baseline + 040..054 limpio en PG17 local.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerar schema.sql + tipos TS (interface Abono, abono_id en Appointment, abono_window_weeks en Business)</name>
  <read_first>
    - lib/types.ts — interfaces Appointment (línea ~216) y Business (línea ~1); estilo snake_case espejo de la fila DB, comentarios densos en español sobre semántica/origen y sobre qué NO va al cliente.
    - El patrón de regeneración de schema.sql tras aplicar una migración (mencionado en las cabeceras de 052/053: "regenerar supabase/schema.sql, igual que 037/039/042/043").
  </read_first>
  <files>supabase/schema.sql, lib/types.ts</files>
  <action>
    (1) Regenerar `supabase/schema.sql` para reflejar la 054 (dump local tras `supabase db reset`, patrón del repo). El diff debe contener SOLO lo nuevo: la tabla `abonos` con sus policies/índices, `appointments.abono_id` + índice, `businesses.abono_window_weeks`. Verificar que NO cambió `book_slot_atomic` ni ninguna otra definición existente.

    (2) En `lib/types.ts`: agregar `export interface Abono` con los campos snake_case espejo de la fila (id, business_id, client_id, service_id, professional_id, location_id, day_of_week, start_time, duration_minutes, status como `'active' | 'cancelled'`, cancel_token, generated_until, skipped_occurrences tipado como `{ date: string; reason: string }[]`, created_at, cancelled_at) y los campos extensibles como opcionales (`reminder_lead_hours?: number | null`, `deposit_amount?: number | null`, `billing_subscription_id?: string | null`) con comentario "diferido a v0.25 / cobro futuro — v0.24 NO los usa" (D-02). Agregar `abono_id?: string | null` a `interface Appointment` con comentario "FK a la serie del abono (migr. 054, D-03); marca el turno como 'fijo' en la agenda (D-09)". Agregar `abono_window_weeks?: number | null` a `interface Business` con comentario "ventana de generación forward del abono en semanas (D-07); default 8 en la DB".
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && grep -cE "interface Abono" lib/types.ts && grep -cE "abono_id" lib/types.ts</automated>
  </verify>
  <done>`schema.sql` refleja la 054 sin tocar definiciones existentes. `lib/types.ts` exporta `Abono` y suma `abono_id` a Appointment + `abono_window_weeks` a Business. `tsc --noEmit` pasa limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (authenticated) → abonos/appointments/businesses | El dueño solo puede tocar filas de SU negocio; RLS + filtro por business_id lo contienen |
| anon → abonos | El público NUNCA lee ni escribe abonos (sin read policy anon) |
| migración → prod | La 054 se aplica a mano coordinada con el deploy, NO por el flujo GSD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | tabla abonos (client_id, deposit_amount, billing_subscription_id) | mitigate | RLS habilitada en la MISMA migración; policies owner-only por operación (business_id in businesses del owner); SIN read policy anon → nada de abonos al público |
| T-06-02 | Tampering | abonos INSERT/UPDATE cross-tenant | mitigate | `with check (business_id in ...owner...)` en insert y update → un dueño no puede crear ni reasignar un abono a otro business_id |
| T-06-03 | Elevation of Privilege | businesses.abono_window_weeks / abono_id | mitigate | columnas aditivas; businesses ya tiene RLS owner-only; el trigger businesses_protect_admin_columns no aplica (no es columna admin); appointments.abono_id `on delete set null` no habilita borrado en cascada de turnos |
| T-06-04 | Tampering | book_slot_atomic (núcleo anti-doble-booking) | mitigate | la migración 054 NO toca el RPC endurecido; abono_id se setea fuera del RPC como etiqueta no-constraint (Plan 02) → cero relajación de 011/013/cupos/espacio |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | esta fase NO instala paquetes nuevos (solo SQL + TS con deps existentes) |
</threat_model>

<verification>
- `supabase db reset` replaya baseline + 040..054 limpio (idempotente: correrlo dos veces no falla).
- `npx tsc --noEmit` pasa.
- `grep -n "book_slot_atomic" supabase/schema.sql` muestra la MISMA definición previa (la 054 no la tocó).
</verification>

<success_criteria>
Existe la migración 054 idempotente con `abonos` (RLS owner-only), `appointments.abono_id` y `businesses.abono_window_weeks`; schema.sql y los tipos TS reflejan el cambio; `supabase db reset` y `tsc` pasan; el núcleo anti-doble-booking queda intacto.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/06-modelo-del-abono-alta-manual-generaci-n-forward/06-01-SUMMARY.md` when done
</output>
