---
phase: 21-lo-que-el-negocio-declara
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - lib/onboarding-agenda.ts
  - test/onboarding-agenda.test.ts
  - app/(onboarding)/onboarding/page.tsx
autonomous: true
requirements: [AGENDA-08]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Un servicio que el dueno cargo en el paso 2 y que ninguna franja cubre aparece nombrado en un aviso al pie del paso Horarios, anticipando la frase exacta que el publico ya ve desde la Phase 20: 'Sin horarios disponibles' (D-07)"
    - "El aviso NO bloquea: Finalizar sigue habilitado y handleFinish no lo consulta — un servicio sin franja es un estado LEGAL (el dueno esta a mitad de configurar, D-06 de la Phase 18) con consecuencia publica real, no un campo mal llenado"
    - "Con CERO franjas abiertas (el dueno cerro los 7 dias, que es un click) el aviso no dice nada: la pregunta no tiene sujeto. Eso lo garantiza hasScheduleCoverage y NO isServiceScheduled, que con cero franjas devuelve false para TODO servicio (CR-01 del code review de la Phase 20)"
    - "El aviso solo aparece con el toggle prendido: con el toggle apagado se persiste comodin (D-10), asi que ningun servicio queda sin cobertura y el aviso mentiria"
    - "La cuenta de servicios sin cobertura sale de lib/time-block-services (hasScheduleCoverage sobre filas sinteticas, molde verbatim del adaptador del panel), nunca de un filtro escrito en el JSX del alta (AGENDA-02)"
  prohibitions:
    - "El aviso de servicios sin cobertura no puede impedir terminar el alta ni convertirse en una validacion bloqueante: un negocio a mitad de configurar tiene que poder entrar al dashboard"
    - "El aviso no puede presentarse como un error de formulario: lo que se le esta diciendo al dueno es que un cliente no va a poder reservar ese servicio, no que lleno mal un campo"
  artifacts:
    - path: "lib/onboarding-agenda.ts"
      provides: "servicesWithoutCoverage + onboardingDraftBlocks: el dato del aviso de D-07, computado con hasScheduleCoverage sobre filas sinteticas"
      exports: ["servicesWithoutCoverage", "onboardingDraftBlocks"]
    - path: "app/(onboarding)/onboarding/page.tsx"
      provides: "el aviso no bloqueante al pie del paso Horarios, en una region role=status siempre montada"
      contains: "Sin horarios disponibles"
    - path: "test/onboarding-agenda.test.ts"
      provides: "los casos del aviso, incluido el discriminante de cero franjas (la guarda CR-01)"
      contains: "servicesWithoutCoverage"
  key_links:
    - from: "lib/onboarding-agenda.ts"
      to: "lib/time-block-services.ts"
      via: "hasScheduleCoverage(serviceId, draftBlocks, draftBridge) — la fuente unica de la regla del comodin (AGENDA-02)"
      pattern: "hasScheduleCoverage"
    - from: "app/(onboarding)/onboarding/page.tsx"
      to: "lib/onboarding-agenda.ts"
      via: "servicesWithoutCoverage(services, dayStates) alimenta el aviso del paso 4"
      pattern: "servicesWithoutCoverage"
---

<objective>
Cerrar D-07: que el dueño que mapea franjas y deja un servicio **sin ninguna que lo cubra** se entere
**antes** de terminar el alta, y que se entere como consecuencia y no como error de validación.

Lo que cambió y hace que esto valga la pena: hasta la Phase 20 un servicio sin franja era **invisible**
en la página pública. Desde la Phase 20 aparece deshabilitado con la frase `Sin horarios disponibles`.
El aviso del alta anticipa esa frase textual para que el dueño reconozca el efecto cuando lo vea en su
propia página.

Purpose: es la última pieza de AGENDA-08 y la única que mira al estado mal configurado. No bloquea —el
estado es legal— pero deja de ser silencioso.

Output: `servicesWithoutCoverage` en `lib/onboarding-agenda.ts` con sus casos en la suite pura, y el
aviso al pie del paso Horarios de `app/(onboarding)/onboarding/page.tsx`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/21-lo-que-el-negocio-declara/21-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/21-lo-que-el-negocio-declara/21-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/21-lo-que-el-negocio-declara/21-01-SUMMARY.md
@lib/time-block-services.ts
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<constraints>
- **Depende del Plan 21-01.** Este plan asume que ya existen `lib/onboarding-agenda.ts`, el toggle
  `perFranja`, `chipCatalog`, `showServicesToggle` y `HourBlock.service_ids`. Si algo de eso falta, el
  Plan 21-01 no cerró y este no arranca.
- **AGENDA-02 — `hasScheduleCoverage`, jamás una reimplementación.** Prohibido un filtro sobre el mapeo
  escrito en el JSX o en el módulo. Y prohibida la función cruda para este aviso: es la trampa que ya
  mordió una vez (CR-01 de la Phase 20).
- **Contrato D-16** (`lib/time-block-services.ts:21-24`): el caller filtra por tenant ANTES de llamar.
  En el alta se cumple trivialmente —todas las filas son del negocio que se está creando— y por eso las
  filas sintéticas llevan `business_id` vacío, igual que ya hace el panel.
- **D-07 es no bloqueante, literalmente.** `handleFinish` no puede consultar el aviso, ni directa ni
  indirectamente, y el botón Finalizar no puede quedar condicionado por él.
- **Cero migraciones, cero dependencias, cero endpoints.** Igual que el Plan 21-01.
- **NUNCA `npx tsc`** → `./node_modules/.bin/tsc --noEmit`.
- **C-05 — `Edit`, nunca `Write`,** sobre los dos archivos existentes.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: servicesWithoutCoverage — el dato del aviso, con la guarda de cero franjas</name>
  <files>lib/onboarding-agenda.ts, test/onboarding-agenda.test.ts</files>
  <read_first>
    - `lib/onboarding-agenda.ts` **íntegro** (creado en el Plan 21-01) — los tipos `OnboardingDayDraft` /
      `OnboardingBlockDraft` que se reusan y el estilo de cabecera del módulo.
    - `lib/time-block-services.ts` `:100-160` (`hasScheduleCoverage` y la función cruda que envuelve, con
      el comentario que explica la trampa de cero franjas), `:21-24` (contrato D-16) y `:240-285`.
    - `lib/types.ts` `:180-184` — la forma de la fila de la puente
      (`business_id` / `time_block_id` / `service_id`), que es lo que se fabrica sintéticamente.
    - `components/agenda/block-services-line.tsx` — el adaptador de filas sintéticas que se movió ahí en
      el Plan 21-01 (origen: `agenda-client.tsx:191-200`); es el molde verbatim a extender a toda la
      grilla.
    - `test/onboarding-agenda.test.ts` **íntegro** — la suite del Plan 21-01, a la que se le suman los
      casos nuevos sin tocar los existentes.
  </read_first>
  <behavior>
    - Con una franja que declara el servicio A y otra que declara el B: ningún servicio queda sin
      cobertura, la lista sale vacía.
    - Con una franja que declara sólo A y un servicio B cargado en el paso 2: la lista sale con B, y
      **sólo** con B.
    - Con **cero** franjas abiertas (los 7 días cerrados) y dos servicios cargados: la lista sale
      **vacía**. Es el caso discriminante: apoyado en la función cruda en vez de en la que envuelve la
      guarda, este test se pone rojo porque diría que los dos servicios quedaron sin horario.
    - Con al menos una franja comodín (sin ningún servicio declarado): la lista sale vacía aunque otras
      franjas estén mapeadas — una franja comodín cubre todo.
    - Las filas de servicio con el nombre vacío se ignoran: el dueño no puede recibir un aviso sobre una
      fila que ni cargó (asunción A3 de RESEARCH, hecha explícita acá).
    - Un día con `enabled: false` no aporta franjas al cálculo, pero un día abierto con bloques sí, aunque
      esté en el medio de días cerrados.
  </behavior>
  <action>
    Agregar a `lib/onboarding-agenda.ts` (sigue siendo un módulo **puro**; se le suma un solo import,
    `hasScheduleCoverage` de `@/lib/time-block-services`):

    - `export function onboardingDraftBlocks(days: OnboardingDayDraft[]): { id: string; service_ids: string[] }[]`
      — las franjas abiertas de la grilla del alta, keyeadas por `${day}-${idx}`, que es la misma clave
      estable que ya usa el estado de colapso. Sólo los días con `enabled: true`.
    - `export function servicesWithoutCoverage<S extends { id: string; name: string }>(services: S[], days: OnboardingDayDraft[]): S[]`
      — construye las filas sintéticas de la puente a partir de esas franjas (una fila por cada id
      declarado, con el `time_block_id` igual a la clave del bloque y el `business_id` en cadena vacía,
      molde verbatim del adaptador del panel) y devuelve los servicios con `name.trim()` no vacío para los
      que `hasScheduleCoverage(s.id, draftBlocks, draftBridge)` da `false`.

    Comentarios obligatorios, en español y explicando el *por qué*:
    1. **Por qué la función que envuelve la guarda y no la cruda**, con nombre y apellido: la cruda filtra
       las franjas, así que con cero franjas devuelve lista vacía y da `false` para **todo** servicio; en
       el alta cerrar los 7 días es un click, y el aviso pasaría de informar a mentirle al dueño sobre
       todo su catálogo. Es literalmente CR-01 del code review de la Phase 20, y el CONTEXT de esta fase
       (`21-CONTEXT.md:82` y `:112`) nombra la función equivocada — la corrección queda registrada acá.
    2. **Por qué el `business_id` va en cadena vacía**: el contrato D-16 dice que el caller ya filtró por
       tenant, y en el alta se cumple trivialmente porque todas las filas son del negocio que se está
       creando. Mismo criterio que usa el panel.
    3. **Por qué se ignoran las filas sin nombre**: `handleFinish` ya las descarta al insertar, así que
       avisar sobre ellas sería un aviso sobre un servicio que no va a existir.

    En `test/onboarding-agenda.test.ts`, agregar un `describe` nuevo con los seis comportamientos
    declarados arriba, **sin tocar** los `it(...)` de la suite del Plan 21-01. Importar la función real
    del módulo; cero mocks.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && npx vitest run test/onboarding-agenda.test.ts && grep -c "  it(" test/onboarding-agenda.test.ts && grep -cF "hasScheduleCoverage" lib/onboarding-agenda.ts</automated>
    <fails_when>exit distinto de 0; o el resumen de vitest dice "0 passed" / "No test files found"; o el conteo de `it(` es menor a 12 (los 6 del Plan 21-01 más los 6 nuevos); o el último `grep -c` imprime 0 y corta la cadena con exit 1.</fails_when>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && grep -c "isServiceScheduled" lib/onboarding-agenda.ts</automated>
    <fails_when>el número impreso es distinto de 0 — el aviso quedó apoyado en la función cruda y con cero franjas va a apagar el catálogo entero (CR-01).</fails_when>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/onboarding-agenda.test.ts` sale **0** con **≥ 12 tests pasados**, 0 failed,
      0 skipped.
    - `git diff -- test/onboarding-agenda.test.ts` **agrega** casos y no modifica ninguna línea con
      `expect(` de las que ya existían.
    - `grep -cF "hasScheduleCoverage" lib/onboarding-agenda.ts` ≥ **1** y
      `grep -c "isServiceScheduled" lib/onboarding-agenda.ts` = **0**.
    - `grep -cE "\.filter\(r => r\.time_block_id|serviceIds\.length === 0" lib/onboarding-agenda.ts` = **0**
      — la regla del comodín no se reimplementó (AGENDA-02).
    - `grep -cF "business_id: ''" lib/onboarding-agenda.ts` ≥ **1** — las filas sintéticas siguen el molde
      del panel y el contrato D-16.
    - `grep -cE "^import " lib/onboarding-agenda.ts` = **2** — el módulo sigue puro: sólo
      `@/lib/agenda-hours-payload` y `@/lib/time-block-services`, ni React ni Supabase.
    - **Prueba de mutación (obligatoria, se registra en el SUMMARY):** cambiar a mano la función envolvente
      por la cruda en el módulo pone **rojo** el caso de cero franjas; se revierte y la suite vuelve a
      verde. Si ese caso sobrevive a la mutación, no está probando la guarda que importa.
  </acceptance_criteria>
  <done>
    El dato del aviso de D-07 sale de una función pura con suite propia, apoyada en la fuente única de la
    regla del comodín, y el caso de cero franjas está probado por mutación.
  </done>
</task>

<task type="auto">
  <name>Task 2: El aviso al pie del paso Horarios — consecuencia, no validación</name>
  <files>app/(onboarding)/onboarding/page.tsx</files>
  <read_first>
    - `app/(onboarding)/onboarding/page.tsx` `:806-880` — el render del paso Horarios tal como quedó
      después del Plan 21-01 (el toggle, la lista de días, la línea de chips por bloque) y dónde termina
      la lista de días, que es donde va el aviso.
    - `app/(onboarding)/onboarding/page.tsx` `:293-330` — el inicio de `handleFinish`, para confirmar que
      el aviso **no** entra ahí, y `:885-900` — el botón Finalizar y su `disabled`.
    - `lib/onboarding-agenda.ts` — la firma exacta de la función que alimenta el aviso.
    - `components/agenda/block-services-line.tsx` — la región viva del comodín: el comentario explica por
      qué una región `role="status"` tiene que estar **siempre montada** y por qué va fuera del flujo del
      flex. El aviso hereda la primera mitad de ese criterio.
    - `app/[slug]/booking-client.tsx` `:620-640` — la frase pública verbatim que el aviso anticipa.
    - `.planning/workstreams/motor-reservas/phases/21-lo-que-el-negocio-declara/21-CONTEXT.md`
      bloque `<specifics>` — el tono pedido: consecuencia, no error de validación.
  </read_first>
  <action>
    En el bloque `step === 4`, **después** de la lista de días (el cierre del contenedor `space-y-2` que
    envuelve los siete días) y antes del cierre del paso, renderizar el aviso de D-07.

    **Forma.** Una región **siempre montada** cuyo contenido cambia, nunca un nodo que aparece y
    desaparece: un `<p>` con `role="status"` y `className="text-xs text-muted-foreground"`. El motivo es
    el mismo que ya está escrito en el componente de la línea de chips — una región que se monta junto
    con su contenido no la locuta ningún lector de pantalla, porque el nodo aparece ya con el texto
    adentro y no hay cambio que anunciar. Cuando no hay nada que avisar, el contenido es cadena vacía.

    **Gate.** El aviso se calcula y se muestra **sólo** cuando `showServicesToggle && perFranja`. Con el
    toggle apagado se persiste comodín (D-10), así que ningún servicio queda sin cobertura y un aviso ahí
    sería falso. En canchas no aparece nunca, porque `showServicesToggle` ya lo cubre (D-03).

    **Dato.** Una constante calculada en el cuerpo del componente, junto a los otros booleanos derivados:
    `showServicesToggle && perFranja ? servicesWithoutCoverage(services, dayStates) : []`. La función es
    la del módulo puro; no se escribe ningún filtro sobre el mapeo en el JSX (AGENDA-02).

    **Copy** (discreción de Claude, con la restricción del bloque `<specifics>` del CONTEXT: tiene que
    sonar a consecuencia). Un solo servicio:
    `Cerámica no se da en ninguna franja: en tu página de reservas va a aparecer como «Sin horarios disponibles».`
    Varios (nombres unidos por coma, en el orden del paso 2):
    `Estos servicios no se dan en ninguna franja y van a aparecer como «Sin horarios disponibles» en tu página de reservas: Yoga, Masaje.`
    La frase entre comillas angulares es **verbatim** la que el público ya muestra desde la Phase 20
    (`app/[slug]/booking-client.tsx:630`): el objetivo es que el dueño reconozca el efecto cuando lo vea
    en su propia página. Los nombres salen del estado del paso 2, tal como el dueño los escribió.

    **Tratamiento visual.** `text-muted-foreground`, **nunca** el color de error, ni `aria-invalid`, ni un
    icono de alerta: no es un error de formulario, es información sobre lo que un cliente va a ver.
    Tampoco es un `toast` — el aviso tiene que quedar en pantalla mientras el dueño ajusta los chips.

    **No bloquea, y eso es estructural, no una promesa.** `handleFinish` no consulta esta constante, el
    botón Finalizar conserva su `disabled={loading}` y nada más, y `validateHours()` no cambia.
    Comentario obligatorio explicando por qué: un servicio sin franja es un estado **legal** —el dueño
    está a mitad de configurar (D-06 de la Phase 18)— sólo que desde la Phase 20 tiene consecuencia
    pública real, y por eso se informa en vez de impedir.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && npx vitest run test/onboarding-agenda.test.ts && grep -cF "Sin horarios disponibles" "app/(onboarding)/onboarding/page.tsx" && grep -cF "servicesWithoutCoverage" "app/(onboarding)/onboarding/page.tsx" && grep -cF "role=\"status\"" "app/(onboarding)/onboarding/page.tsx"</automated>
    <fails_when>exit distinto de 0; o el resumen de vitest dice "0 passed" / "No test files found"; o cualquiera de los tres `grep -c` imprime 0 y corta la cadena con exit 1.</fails_when>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && awk '/async function handleFinish/{f=1} f{print} f&&/^  \}$/{exit}' "app/(onboarding)/onboarding/page.tsx" | grep -cE "sinCobertura|servicesWithoutCoverage"</automated>
    <fails_when>el número impreso es distinto de 0 — el aviso se coló dentro del submit y D-07 dejó de ser no bloqueante.</fails_when>
    <human-check>UAT visual en local con el negocio semilla, a 375px y en desktop: (1) el paso Horarios con 3 servicios cargados y el toggle apagado no muestra ni chips ni aviso; (2) prendido, marcar "Cerámica" sólo en el martes deja "Yoga" y "Masaje" nombrados en el aviso del pie, en gris y sin ícono de error; (3) el botón Finalizar sigue habilitado con el aviso en pantalla y el alta termina; (4) cerrar los 7 días hace desaparecer el aviso en vez de listar todo el catálogo; (5) en un negocio de rubro canchas no aparece ni el toggle ni el aviso.</human-check>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` sale **0** y `npm run lint` no agrega errores.
    - `grep -cF "Sin horarios disponibles" "app/(onboarding)/onboarding/page.tsx"` = **2** (la frase en las
      dos variantes de copy, singular y plural).
    - `grep -cF "servicesWithoutCoverage" "app/(onboarding)/onboarding/page.tsx"` = **2** (el import + el
      cálculo).
    - `grep -cF "role=\"status\"" "app/(onboarding)/onboarding/page.tsx"` = **1** — la región del aviso,
      siempre montada.
    - Dentro del cuerpo de `handleFinish` no aparece ninguna referencia al aviso:
      `awk '/async function handleFinish/{f=1} f{print} f&&/^  \}$/{exit}' "app/(onboarding)/onboarding/page.tsx" | grep -cE "sinCobertura|servicesWithoutCoverage"` = **0**.
    - El botón Finalizar sigue con una sola condición de deshabilitado:
      `grep -cF "onClick={handleFinish} disabled={loading}" "app/(onboarding)/onboarding/page.tsx"` = **1**.
    - `git status --porcelain -- lib/onboarding-agenda.ts test/onboarding-agenda.test.ts` vacío en esta tarea (los dos
      archivos son del Task 1) y `git status --porcelain` (árbol de trabajo, antes del commit) toca **exactamente**
      `app/(onboarding)/onboarding/page.tsx`.
    - `git status --porcelain -- package.json package-lock.json` (árbol de trabajo, antes del commit) vacío.
  </acceptance_criteria>
  <done>
    El paso Horarios nombra los servicios que ninguna franja cubre, con la misma frase que el cliente va a
    ver en la página pública, sin bloquear el alta y sin parecer un error de formulario.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Estado local del wizard → cálculo del aviso | Todo el plan es **lectura**: no agrega ninguna escritura, ninguna query y ningún parámetro nuevo al RPC. Las filas que alimentan el cálculo son sintéticas y nunca salen del navegador. |
| Nombres de servicio cargados por el dueño → texto en pantalla | El aviso interpola nombres que el propio dueño escribió, dentro de su propia sesión. React escapa el contenido de texto por defecto; no hay `dangerouslySetInnerHTML` en ningún punto de este plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-08 | Information Disclosure | El aviso nombrando servicios | low | accept | Los nombres son del propio negocio y sólo se muestran al dueño en su sesión, en una pantalla que no es pública. No hay dato de otro tenant al alcance: las filas del cálculo se fabrican desde el estado local del wizard |
| T-21-09 | Tampering | La copy del aviso interpolando texto del usuario | low | mitigate | Se interpola como **texto** de React (escapado por defecto), nunca como HTML. Prohibido `dangerouslySetInnerHTML` en este plan |
| T-21-10 | Denial of Service (usabilidad) | Un aviso que bloquee el alta | medium | mitigate | Es estructural y verificable: `handleFinish` no referencia el aviso (gate con `awk` region-scoped) y el botón Finalizar conserva su única condición de deshabilitado. Bloquear dejaría al dueño encerrado en el wizard por un estado que la Phase 18 declaró legal |
| T-21-11 | Repudiation / fallo mudo | El aviso apagado al revés con cero franjas | medium | mitigate | `hasScheduleCoverage` con su guarda de negocio sin franjas; el caso está probado por mutación en el Task 1. Sin eso, el dueño ve su catálogo entero marcado como sin horario y aprende a ignorar el aviso |
| T-21-SC | Tampering (supply chain) | npm/pip/cargo installs | high | mitigate | Cero paquetes nuevos. Gate: `git status --porcelain -- package.json package-lock.json` (árbol de trabajo, antes del commit) vacío |

**Hereda sin cambios** el registro del Plan 21-01 (T-21-01 a T-21-07): este plan no toca el write path,
no agrega parámetros al RPC y no modifica ninguna policy.
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` sale **0**.
2. `npx vitest run` completo sale **0**; `test/onboarding-agenda.test.ts` reporta **≥ 12 tests pasados**.
3. `grep -c "isServiceScheduled" lib/onboarding-agenda.ts` = **0** (la guarda CR-01 está en su lugar).
4. `grep -cF "Sin horarios disponibles" "app/(onboarding)/onboarding/page.tsx"` = **2**.
5. `awk '/async function handleFinish/{f=1} f{print} f&&/^  \}$/{exit}' "app/(onboarding)/onboarding/page.tsx" | grep -cE "sinCobertura|servicesWithoutCoverage"` = **0** — D-07 sigue siendo no bloqueante.
6. `git status --porcelain -- package.json package-lock.json` (árbol de trabajo, antes del commit) vacío y `ls supabase/migrations | grep -c "^077"` = **0**.
7. `git status --porcelain` (árbol de trabajo, antes del commit) toca **exactamente** los 3 archivos del frontmatter.
8. `npm run lint` sin errores nuevos.
9. La UAT visual del `human-check` del Task 2 queda registrada en el SUMMARY con su resultado por paso
   (el modo de verificación humana de este proyecto es `end-of-phase`, así que no hay checkpoint
   bloqueante en el medio: el registro es la evidencia).
</verification>

<success_criteria>
- Un dueño que mapea franjas y deja "Yoga" sin ninguna lo ve nombrado antes de terminar el alta, con la
  misma frase que su cliente va a leer en la página pública.
- El aviso no impide terminar el alta, no se lee como un error de formulario, y con los 7 días cerrados
  no dice nada.
- El dato sale de la fuente única de la regla del comodín, con el caso de cero franjas probado por
  mutación.
</success_criteria>

## Artifacts this phase produces

| Artefacto / símbolo | Plan | Estado al cerrar este plan |
|---|---|---|
| `components/agenda/block-services-line.tsx` → `BlockServicesLine`, `ServiceCatalogItem` | 21-01 | ya creado |
| `lib/onboarding-agenda.ts` → `buildOnboardingAgendaPayload`, `OnboardingDayDraft`, `OnboardingBlockDraft` | 21-01 | ya creado |
| `app/(onboarding)/onboarding/page.tsx` → `newServiceId()`, `Service.id`, `HourBlock.service_ids`, `perFranja`, `canMapServices`, `chipCatalog`, `showServicesToggle`, `expandedChips`, `toggleChipsExpanded`, `toggleBlockService`, submit por `save_agenda_blocks` | 21-01 | ya creado |
| `test/onboarding-agenda-rpc.test.ts` | 21-01 | ya creado |
| `lib/onboarding-agenda.ts` → `servicesWithoutCoverage`, `onboardingDraftBlocks` | **21-02** | creado |
| `test/onboarding-agenda.test.ts` → el `describe` del aviso (6 casos nuevos) | **21-02** | creado, verde |
| `app/(onboarding)/onboarding/page.tsx` → la constante del aviso + la región `role="status"` al pie del paso Horarios | **21-02** | creado |

<!-- planner-discipline-allow: isServiceScheduled -->
<!-- planner-discipline-allow: servicesWithoutCoverage -->
<!-- planner-discipline-allow: sinCobertura -->

<output>
Create `.planning/workstreams/motor-reservas/phases/21-lo-que-el-negocio-declara/21-02-SUMMARY.md` when done
</output>
