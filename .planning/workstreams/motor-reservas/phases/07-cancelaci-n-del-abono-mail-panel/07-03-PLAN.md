---
phase: 07-cancelaci-n-del-abono-mail-panel
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - app/api/abonos/cancel/[token]/route.ts
  - app/abono/cancelar/[token]/page.tsx
  - app/abono/cancelar/[token]/abono-cancel-client.tsx
  - app/api/abonos/create/route.ts
autonomous: true
requirements: [ABONO-04]
must_haves:
  truths:
    - "El cliente recibe en el mail de alta del abono un link de baja, y abrirlo da de baja la SERIE COMPLETA, no un turno suelto (ABONO-04, criterio 1 del ROADMAP)"
    - "La página pública muestra, ANTES de confirmar, el negocio con su branding, el servicio, el día/hora fijos, el conteo de turnos a cancelar y la fecha del último (D-03/D-11)"
    - "Confirmar la baja pone la serie en cancelled y cancela sus turnos futuros llamando a cancelAbonoSeries — la misma función que usa el panel (D-07)"
    - "Un token inexistente o con formato inválido devuelve 404 genérico sin revelar si la serie existe (D-22)"
    - "Abrir el link con la serie ya dada de baja muestra el mensaje de estado sin botón de acción (D-08)"
    - "Re-POSTear el token sobre una serie ya cancelada no re-dispara mails ni vuelve a tocar turnos (D-05)"
    - "El cancel_token no caduca ni se rota tras usarse: el link deja de operar por ESTADO, no por vencimiento, para que la página informativa post-baja siga funcionando (D-09)"
    - "La baja por el cliente manda UN mail al cliente y UN aviso al dueño a notification_email (D-13/D-14)"
    - "La regla de las 24h del cancel de turno suelto no aplica a esta ruta (D-06)"
  prohibitions:
    - "No se modifica app/cancelar/[token]/ ni app/api/cancel/[token]/route.ts: la ruta es NUEVA, no se agrega branching a los flujos vivos de turno suelto (D-10)"
    - "No se manda un mail por turno cancelado: uno solo al cliente y uno solo al dueño (D-14)"
    - "Ninguna ruta ni respuesta reactiva un abono cancelled (D-04)"
    - "Ninguna query de cancelación masiva propia en esta ruta: toda la baja pasa por cancelAbonoSeries (D-24)"
    - "No se agregan prefijos a lib/auth/route-lists.ts: la ruta nueva hereda el mismo tratamiento de proxy que /cancelar/[token]"
    - "Ninguna rama de código escribe, rota ni invalida abonos.cancel_token después de usarlo (D-09)"
    - "La respuesta de error nunca distingue token inexistente de token de otro tenant"
  artifacts:
    - path: "app/api/abonos/cancel/[token]/route.ts"
      provides: "Endpoint público de baja de serie resuelto por cancel_token"
      exports: ["POST"]
    - path: "app/abono/cancelar/[token]/page.tsx"
      provides: "Página pública server-side con branding del tenant y preview de la baja"
    - path: "app/abono/cancelar/[token]/abono-cancel-client.tsx"
      provides: "Componente client con la máquina de estados y la confirmación"
      exports: ["AbonoCancelClient"]
    - path: "app/api/abonos/create/route.ts"
      provides: "cancelUrl del mail de alta apuntando a la ruta nueva (D-16)"
      contains: "/abono/cancelar/"
  key_links:
    - from: "app/api/abonos/cancel/[token]/route.ts"
      to: "lib/abono-cancel.ts"
      via: "delega toda la baja en cancelAbonoSeries con el businessId resuelto por el token"
      pattern: "cancelAbonoSeries"
    - from: "app/api/abonos/create/route.ts"
      to: "app/abono/cancelar/[token]/page.tsx"
      via: "el mail de alta lleva el cancelUrl construido con abonos.cancel_token"
      pattern: "abono/cancelar"
---

<objective>
Construir la **vía del cliente**: el link que viaja en el mail de alta del abono y la página pública que lo resuelve. Ruta NUEVA (D-10, LOCKED) — `app/abono/cancelar/[token]/` + `POST /api/abonos/cancel/[token]` — copiando el patrón de `/cancelar/[token]` sin tocar ni un byte de esos archivos, que están vivos en producción para el cancel de turno suelto.

El token (`abonos.cancel_token`, uuid, ya existe en la migración 054) es la ÚNICA autorización: resuelve server-side con service role la serie y su tenant, y de ahí sale el `businessId` que se le pasa a `cancelAbonoSeries` (Plan 01). Sin match, 404 genérico. Antes de confirmar, el cliente ve cuántos turnos se cancelan y hasta cuándo (D-03/D-11): es una acción destructiva e irreversible, nunca un click a ciegas.

Cierra además D-16: el `cancelUrl` de `sendAbonoConfirmation` — que ya existe como parámetro sin uso — pasa a llenarse en el alta del abono con la URL de esta ruta.

Purpose: cubre ABONO-04 y el criterio 1 del ROADMAP §Phase 7 completo (mail → link → baja de la serie entera).
Output: 3 archivos nuevos + el wiring del `cancelUrl` en el alta.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-01-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-02-SUMMARY.md

# Analogs a copiar SIN modificar
@app/api/cancel/[token]/route.ts
@app/cancelar/[token]/page.tsx
@app/cancelar/[token]/cancel-client.tsx
</context>

## Artifacts this phase produces (Plan 03)

| Artefacto | Tipo | Firma / forma |
|---|---|---|
| `app/api/abonos/cancel/[token]/route.ts` | route handler nuevo | `export async function POST(_req: NextRequest, { params }: { params: Promise<{ token: string }> })` |
| respuestas del endpoint | contrato HTTP | `{ ok: true, cancelledCount, lastDate }` \| `{ ok: false, reason: 'invalid' \| 'not_found' \| 'already_cancelled' \| 'error' }` |
| `app/abono/cancelar/[token]/page.tsx` | page server nueva | `export default async function AbonoCancelarPage({ params }: { params: Promise<{ token: string }> })` + `export const metadata` |
| `app/abono/cancelar/[token]/abono-cancel-client.tsx` | componente client nuevo | `export function AbonoCancelClient(props)` con props `{ token, clientName, service, dayLabel, time, cancelledCount, lastDate, businessName, businessSlug, logoUrl, accent, initialState }` |
| ruta pública | URL | `/abono/cancelar/<cancel_token>` |
| `cancelUrl` del mail de alta | wiring | `${base}/abono/cancelar/${abono.cancel_token}` en `app/api/abonos/create/route.ts` |

<tasks>

<task type="auto">
  <name>Task 1: POST /api/abonos/cancel/[token] — baja pública por token (D-05/D-06/D-08/D-13/D-14/D-22)</name>
  <read_first>
    - app/api/cancel/[token]/route.ts — el analog EXACTO, de punta a punta: la cabecera doctrinal sobre por qué el token es la única autorización, `params` como Promise que hay que await (Next 16), `createAdminClient()`, el join anidado a `businesses(...)` con columnas NO secretas, el Pitfall E (los secretos van aparte por `getBusinessSecrets`), el 404 genérico, el log con prefijo de módulo, y sobre todo el comentario de por qué los mails se hacen con `await` y no con `after()` en la vía pública. NO MODIFICAR ESTE ARCHIVO.
    - lib/abono-cancel.ts — la firma de `cancelAbonoSeries` y la unión que devuelve.
    - lib/email.ts — las firmas de `sendAbonoCancelledEmail` y `sendAbonoCancelledAdminNotification` creadas en el Plan 02.
    - lib/business-secrets.ts — `getBusinessSecrets(businessId)`.
    - app/api/abonos/create/route.ts líneas 44-48 — la constante `DAY_LABELS` con la convención EXTRACT(dow) 0=domingo..6=sábado que arma el `dayLabel` del mail.
  </read_first>
  <files>app/api/abonos/cancel/[token]/route.ts</files>
  <action>
    Crear `app/api/abonos/cancel/[token]/route.ts` con `export async function POST(_req: NextRequest, { params }: { params: Promise<{ token: string }> })`, abriendo con cabecera doctrinal en español que enuncie: que da de baja la SERIE ENTERA y no una ocurrencia; que el token es la única autorización y de él sale el tenant; que la regla de las 24h del cancel de turno suelto NO rige (D-06); que manda un solo mail al cliente y un solo aviso al dueño (D-14); y que es una ruta nueva creada a propósito para no meter branching en el cancel de turno suelto vivo en producción (D-10).

    Secuencia:
    (1) `await params`; sin token → `Response.json({ ok: false, reason: 'invalid' }, { status: 400 })`.
    (2) `createAdminClient()` y resolución de la serie: `.from('abonos')` con `.select('id, business_id, status, day_of_week, start_time, clients(name, email, phone), services(name), businesses(id, name, slug, primary_color, logo_url, notification_email, whatsapp)')`, `.eq('cancel_token', token)`, `.maybeSingle()`. Comentar que `cancel_token` es una columna uuid: un token con formato inválido hace fallar el casteo en Postgres y devuelve data nula — ese camino también tiene que terminar en el 404 genérico, nunca en un 500 que revele que el input llegó a la base.
    (3) Sin fila → `Response.json({ ok: false, reason: 'not_found' }, { status: 404 })`. Mismo cuerpo y mismo status para "no existe" y para cualquier otro fallo de resolución: no se distingue (D-22).
    (4) Si `status === 'cancelled'` → `Response.json({ ok: false, reason: 'already_cancelled' })` sin status custom, igual que el analog. No se toca ningún turno y no se manda ningún mail (D-05). El estado `completed` NO corta acá: sigue de largo, porque una serie completada puede tener turnos futuros por delante y se puede dar de baja igual (D-21, consistente con el panel).
    (5) Llamar `cancelAbonoSeries({ supabase, businessId: <business_id de la fila>, abonoId: <id de la fila> })`. El `businessId` sale SIEMPRE de la fila resuelta por el token, jamás de la request. Esta ruta no ejecuta ninguna query de cancelación propia.
    (6) Si el resultado es `ok: false` → `console.error` con prefijo `[abonos/cancel]` incluyendo el id del abono, y `Response.json({ ok: false, reason: 'error' }, { status: 500 })`. Si el resultado trae `alreadyCancelled: true` (carrera con otra request) → devolver `{ ok: false, reason: 'already_cancelled' }` sin mandar mails.
    (7) Mails con **await**, no con `after()`, replicando el comentario del analog sobre por qué (en serverless el fetch a Resend se corta al hacer return en esta superficie). Primero `getBusinessSecrets(business.id)`. Después, cada uno en su propio try/catch con `console.error('[abonos/cancel] ...')` y el ternario `e instanceof Error ? e.message : e`:
        - si el cliente de la serie tiene email → `sendAbonoCancelledEmail` con el `dayLabel` derivado de `day_of_week` con la constante de etiquetas de día (misma convención que el alta), la hora, `cancelledCount` y `lastDate` del resultado, y el branding del negocio;
        - si `businesses.notification_email` está cargado → `sendAbonoCancelledAdminNotification` con el nombre y contacto del cliente y los mismos números (D-13).
      Un fallo de mail se loguea y NO rompe la baja, que ya está confirmada.
    (8) `Response.json({ ok: true, cancelledCount, lastDate })`.

    El handler NO escribe, rota ni invalida `abonos.cancel_token` en ningún punto (D-09): el link deja de operar por ESTADO — la serie pasa a `cancelled` y el paso (4) lo corta — y no por vencimiento, para que el cliente que vuelva a abrir el link siga viendo la pantalla informativa en vez de una pantalla de error.

    NO copiar del analog el bloque de Google Calendar: los turnos generados por abono no crean eventos (`lib/abono-generation.ts` no los crea), así que no hay nada que borrar.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && git status --porcelain app/api/cancel app/cancelar</automated>
  </verify>
  <acceptance_criteria>
    - `app/api/abonos/cancel/[token]/route.ts` existe y exporta POST: `grep -cE "^export async function POST" "app/api/abonos/cancel/[token]/route.ts"` devuelve 1.
    - La baja se delega en el motor compartido: `grep -cE "\bcancelAbonoSeries\s*\(" "app/api/abonos/cancel/[token]/route.ts"` es ≥ 1.
    - No hay cancelación masiva propia en la ruta: `grep -cE "from\('appointments'\)" "app/api/abonos/cancel/[token]/route.ts"` devuelve 0.
    - Como máximo un envío por destinatario: `grep -cE "sendAbonoCancelledEmail\s*\(" "app/api/abonos/cancel/[token]/route.ts"` devuelve 1 y `grep -cE "sendAbonoCancelledAdminNotification\s*\(" "app/api/abonos/cancel/[token]/route.ts"` devuelve 1.
    - Los flujos vivos del cancel de turno suelto quedan intactos: `git status --porcelain app/api/cancel app/cancelar` no imprime nada.
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>
  <done>El endpoint público da de baja la serie por token con aislamiento resuelto server-side, 404 genérico, idempotencia y exactamente dos mails como máximo; no ejecuta ninguna query de cancelación propia y no toca las rutas vivas.</done>
</task>

<task type="auto">
  <name>Task 2: Página pública /abono/cancelar/[token] — preview + confirmación (D-08/D-11/D-12)</name>
  <read_first>
    - app/cancelar/[token]/page.tsx — el shell server completo (59 líneas): resolución por token con service role, pantalla de "Link inválido" genérica, derivación del estado inicial server-side, `PaletteScript` con `palette`/`theme`/`font` del negocio y el fallback de color `'#d94a2b'`. NO MODIFICAR ESTE ARCHIVO.
    - app/cancelar/[token]/cancel-client.tsx — el componente client completo (147 líneas): helper local `fmtDate` con los arrays `DAYS` y `MONTHS` (sin date-fns en la superficie pública), la máquina de estados con `useState`, el `fetch` al endpoint con manejo por `reason`, el header de marca con logo o inicial, la card con el bloque de detalle de borde izquierdo acentuado, el botón con `style={{ backgroundColor: accent }}` y la pantalla de éxito con el link a `/${businessSlug}`. NO MODIFICAR ESTE ARCHIVO.
    - lib/abono-cancel.ts — `previewAbonoCancellation` y `todayISOInAR`.
    - components/palette-script.tsx — las props que consume `PaletteScript`.
  </read_first>
  <files>app/abono/cancelar/[token]/page.tsx, app/abono/cancelar/[token]/abono-cancel-client.tsx</files>
  <action>
    **(A) `app/abono/cancelar/[token]/page.tsx`** — Server Component, `export const metadata = { title: 'Cancelar turno fijo' }`. Copiar la estructura del analog y adaptar:
    - Resolver la serie con `createAdminClient()` sobre `abonos` por `.eq('cancel_token', token).maybeSingle()`, seleccionando `id, business_id, status, day_of_week, start_time, clients(name), services(name), businesses(name, slug, primary_color, logo_url, theme, palette, font)`. El select del business SIEMPRE incluye `theme`, `palette` y `font` porque los consume `PaletteScript`.
    - Sin fila → la misma pantalla genérica "Link inválido" del analog, con el copy adaptado al turno fijo y sin revelar si la serie existe.
    - Calcular el preview server-side con `previewAbonoCancellation({ supabase, businessId, abonoId })` (D-03/D-11). Si la serie ya está cancelada, el preview no hace falta: pasar `cancelledCount: 0` y `lastDate: null`.
    - Derivar `initialState` con solo dos valores: `'cancelled'` si el status de la serie es `cancelled`, si no `'active'` (D-08). El estado `completed` cae en `'active'` a propósito (D-21). No se calcula ninguna condición de horario: la regla de las 24h no existe en esta superficie (D-06).
    - Derivar el `dayLabel` a partir de `day_of_week` con un array local de nombres de día en la convención 0=domingo..6=sábado, para pasar al client una frase del estilo "todos los martes".
    - Render: `PaletteScript` con `palette`/`theme`/`font` del negocio, seguido de `AbonoCancelClient` con todas las props de la tabla de artefactos; `accent` con el mismo fallback del analog.

    **(B) `app/abono/cancelar/[token]/abono-cancel-client.tsx`** — `'use client'`, `export function AbonoCancelClient(props)`. Copiar el shell del analog (helper `fmtDate` local con `DAYS`/`MONTHS`, header de marca, card, botón acentuado, pantalla de éxito) y adaptar:
    - Máquina de estados: `view` de tipo `'active' | 'cancelled' | 'done'` inicializado con `initialState`, más `loading` y `error`. No existen los estados de turno pasado ni de fuera de plazo.
    - `confirmCancel()` hace `fetch` con método POST a `/api/abonos/cancel/${token}`, y mapea la respuesta: `ok` → `'done'`; `reason` de serie ya cancelada → `'cancelled'`; `reason` de token no encontrado → mensaje de enlace inválido; cualquier otra cosa → mensaje genérico de reintento. Mismo manejo de `catch` de red que el analog.
    - Bloque de detalle (D-11): "Servicio" (si viene), "Se repite" con el `dayLabel` en la frase del estilo "todos los martes", "Hora" con `time.slice(0, 5)` y el sufijo `hs`, "A nombre de" con el cliente, y — cuando `cancelledCount` es mayor a 0 — una línea de advertencia con el número de turnos futuros que se van a cancelar y, si hay `lastDate`, la fecha del último renderizada con el `fmtDate` local. No se lista fecha por fecha: con la ventana de generación la lista es larga en mobile.
    - Vista `'active'`: párrafo que aclara que se da de baja el turno fijo COMPLETO (todas las semanas) y que la acción no se puede deshacer; botón con la etiqueta de dar de baja el turno fijo, deshabilitado mientras `loading`.
    - Vista `'cancelled'` (D-08): mensaje de que ese turno fijo ya fue dado de baja, SIN botón de acción, más el link a `/${businessSlug}`.
    - Vista `'done'` (D-12): confirmación de la baja, aclaración de que los turnos futuros quedaron liberados, y link a la página pública del negocio para reservar un turno suelto.
    - Mantener las clases y los estilos inline del analog tal cual para que el branding por tenant se vea idéntico al cancel de turno.

    No agregar ningún prefijo a `lib/auth/route-lists.ts`: la ruta nueva cae, igual que `/cancelar/[token]`, en el `NextResponse.next()` del proxy (sin `updateSession`, que es justo lo que queremos en una superficie anónima) y no queda exenta de mantenimiento — exactamente el mismo tratamiento que ya tiene el cancel de turno.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npx eslint "app/abono/cancelar/[token]" && git status --porcelain lib/auth/route-lists.ts proxy.ts</automated>
  </verify>
  <acceptance_criteria>
    - Existen `app/abono/cancelar/[token]/page.tsx` y `app/abono/cancelar/[token]/abono-cancel-client.tsx`.
    - La página usa el branding del tenant: `grep -cE "<PaletteScript" "app/abono/cancelar/[token]/page.tsx"` devuelve 1.
    - El preview sale del motor compartido: `grep -cE "previewAbonoCancellation\s*\(" "app/abono/cancelar/[token]/page.tsx"` es ≥ 1.
    - El client apunta al endpoint nuevo: `grep -cE "/api/abonos/cancel/" "app/abono/cancelar/[token]/abono-cancel-client.tsx"` devuelve 1.
    - La máquina de estados no tiene ramas de plazo vencido: `grep -cE "too_late" "app/abono/cancelar/[token]/abono-cancel-client.tsx"` devuelve 0.
    - Las listas de ruteo del Edge quedan intactas: `git status --porcelain lib/auth/route-lists.ts proxy.ts` no imprime nada.
    - `npx tsc --noEmit` y `npx eslint` sobre el directorio nuevo salen 0.
  </acceptance_criteria>
  <done>La página pública resuelve la serie por token con el branding del negocio, muestra el conteo y la última fecha antes de confirmar, distingue el estado ya-cancelado sin botón, y al confirmar deja al cliente en la pantalla de éxito con el link para reservar.</done>
</task>

<task type="auto">
  <name>Task 3: D-16 — llenar el cancelUrl del mail de alta del abono</name>
  <read_first>
    - app/api/abonos/create/route.ts líneas 186-206 — el insert del abono con su `.select(...)`, que YA trae `cancel_token`.
    - app/api/abonos/create/route.ts líneas 260-287 — el bloque `after()` que manda `sendAbonoConfirmation`, incluyendo cómo se captura `abonoId` en un const ANTES del closure.
    - lib/email.ts líneas 335-372 y 419-421 — `sendAbonoConfirmation` ya acepta `cancelUrl` y ya renderiza el botón condicional cuando viene con contenido; no hay que tocar el template.
    - lib/email.ts línea 117 — el patrón de construcción de la base de URL usado en todo el módulo.
  </read_first>
  <files>app/api/abonos/create/route.ts</files>
  <action>
    En `app/api/abonos/create/route.ts`, dentro del bloque que manda el mail de alta:
    - capturar el token de baja en un const junto al `abonoId` que ya se captura antes del `after()` (el closure no debe depender del objeto `abono` completo);
    - construir la URL de baja con la base `process.env.NEXT_PUBLIC_APP_URL` con fallback `'https://gestion.forjo.studio'`, sin barra final duplicada, más el path `/abono/cancelar/` y el token;
    - pasarla como `cancelUrl` en la llamada a `sendAbonoConfirmation`.

    Agregar un comentario breve en español explicando que este es el link que el cliente usa para dar de baja la serie completa (D-16/ABONO-04) y que el template ya lo contemplaba desde Phase 6 con el botón condicional. Si el `cancel_token` viniera vacío por cualquier motivo, no pasar `cancelUrl` (el template degrada sin botón) en vez de mandar una URL rota.

    No cambiar nada más del handler: ni la validación, ni el anti-tampering, ni la generación de la primera tanda.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npx vitest run test/abono-create.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "abono/cancelar" app/api/abonos/create/route.ts` es ≥ 1.
    - `grep -cE "cancelUrl" app/api/abonos/create/route.ts` es ≥ 1.
    - `git diff -U0 app/api/abonos/create/route.ts` no muestra cambios fuera del bloque del mail (ninguna línea modificada por encima de la llamada a `generateAbonoOccurrences`).
    - `npx vitest run test/abono-create.test.ts` sigue verde (cero regresión en el alta).
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>
  <done>El mail de alta del abono lleva el link de baja apuntando a la ruta nueva, sin ningún otro cambio en el alta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet anónimo → `/abono/cancelar/[token]` | Superficie pública sin sesión: el único credencial es el token de la URL |
| internet anónimo → `POST /api/abonos/cancel/[token]` | Mutación destructiva sin sesión; corre con service role, RLS no protege |
| token → tenant | El `business_id` se deriva de la fila resuelta por el token, nunca de la request |
| app → Resend | Salida a un tercero con los secretos del tenant resuelto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Spoofing | `POST /api/abonos/cancel/[token]` | mitigate | resolución por `.eq('cancel_token', token)` contra la columna uuid indexada de la migración 054 (uuid v4 = ~122 bits de entropía, no adivinable). Se replica el patrón vigente de `app/api/cancel/[token]/route.ts`: la comparación la hace Postgres sobre la columna, NUNCA un `===` en JS contra un secreto ya conocido — no existe en el proceso un canal de temporización sobre el token |
| T-07-13 | Information Disclosure | enumeración de tokens | mitigate | token inexistente, token con formato inválido (falla el casteo uuid) y cualquier otro fallo de resolución devuelven el MISMO 404 con `reason: 'not_found'`; la página muestra la misma pantalla genérica de link inválido (D-22) |
| T-07-14 | Elevation of Privilege | cross-tenant | mitigate | el `businessId` que recibe `cancelAbonoSeries` sale de la fila resuelta por el token; la ruta no acepta ningún identificador de negocio ni de abono por request y no ejecuta ninguna query sobre `appointments` (verificado por grep) |
| T-07-15 | Denial of Service / abuso | re-POST del mismo token | mitigate | idempotencia D-05: el gate atómico del Plan 01 hace que la segunda llamada devuelva `already_cancelled` sin tocar turnos ni disparar mails; un atacante con el token no puede usarlo como amplificador de mails |
| T-07-16 | Tampering | flujo vivo de cancel de turno suelto | mitigate | ruta NUEVA (D-10): `app/cancelar/[token]/` y `app/api/cancel/[token]/route.ts` no se abren ni se modifican; verificado con `git status --porcelain` en el verify de la Task 1 |
| T-07-17 | Tampering | listas de ruteo del Edge | mitigate | no se agregan prefijos a `lib/auth/route-lists.ts`: la ruta hereda el tratamiento de `/cancelar/[token]` (sin `updateSession`, sin exención de mantenimiento), evitando que la superficie anónima vea cookies del dueño |
| T-07-18 | Information Disclosure | contenido de la página pública | mitigate | la página solo renderiza nombre del cliente, servicio, día/hora y el resumen numérico de la propia serie; no expone `business_id`, `abono_id` ni datos de otros clientes |
| T-07-19 | Repudiation | baja sin rastro para el negocio | mitigate | D-13: cuando la baja la hace el cliente se manda el aviso a `businesses.notification_email` y la serie queda `cancelled` con `cancelled_at` visible en el panel |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | este plan NO instala paquetes; si apareciera uno, slopcheck + checkpoint humano bloqueante antes del install |
</threat_model>

<verification>
- `npx tsc --noEmit` pasa.
- `npx eslint "app/abono/cancelar/[token]"` pasa.
- `npx vitest run test/abono-create.test.ts` verde (cero regresión en el alta).
- `git status --porcelain app/cancelar app/api/cancel lib/auth/route-lists.ts proxy.ts supabase/migrations` vacío.
</verification>

<success_criteria>
Un cliente con el link del mail abre una página con el branding de su negocio, ve exactamente cuántos turnos se cancelan y hasta cuándo, confirma una vez y su turno fijo queda dado de baja con los turnos futuros liberados; el dueño recibe el aviso. El mail de alta ya lleva ese link. Los flujos de cancel de turno suelto siguen exactamente igual que antes.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-03-SUMMARY.md` when done
</output>
