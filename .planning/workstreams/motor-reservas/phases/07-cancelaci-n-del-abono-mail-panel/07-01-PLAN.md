---
phase: 07-cancelaci-n-del-abono-mail-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/abono-cancel.ts
  - lib/abono-cancel.test.ts
  - test/abono-cancel.test.ts
autonomous: true
requirements: [ABONO-04, ABONO-05]
must_haves:
  truths:
    - "Existe UNA sola función de baja de serie que las dos vías llaman (D-07): la baja del cliente por token y la del dueño por panel producen exactamente el mismo efecto sobre los turnos"
    - "Dar de baja pone abonos.status='cancelled' + cancelled_at, lo que frena la generación forward sin tocar el cron (que ya filtra status='active')"
    - "La baja cancela TODOS los turnos de la serie con date >= hoy en fecha AR inclusive, y ninguno con date anterior a hoy (D-01/D-02)"
    - "La cancelación en masa nunca toca turnos de otra serie ni de otro tenant (doble scoping abono_id + business_id, D-24)"
    - "Re-ejecutar la baja sobre una serie ya cancelada devuelve alreadyCancelled sin volver a tocar turnos (D-05)"
    - "El conteo y la última fecha del preview excluyen los turnos de la serie ya cancelados individualmente"
    - "Un abono en status 'completed' se puede dar de baja por el mismo camino (completed → cancelled, D-21)"
  prohibitions:
    - "Ninguna query de cancelación masiva sin el doble scoping abono_id + business_id (D-24)"
    - "Ninguna función, endpoint ni rama de código que devuelva un abono 'cancelled' a 'active' (D-04: cancelled es terminal)"
    - "El helper NO instancia clientes Supabase: recibe el cliente por parámetro (rol-agnóstico, patrón de lib/abono-generation.ts)"
    - "No se aplica la regla de las 24h del cancel de turno suelto en ningún punto de la baja de serie (D-06)"
    - "No se modifica app/cancelar/[token]/ ni app/api/cancel/[token]/route.ts (D-10)"
    - "El helper NO manda mails: cada vía dispara UN solo mail al cliente y UN solo aviso al dueño (D-14)"
    - "El corte 'hoy' no se deriva del reloj del proceso en UTC: sale de todayInAR() (D-02)"
  artifacts:
    - path: "lib/abono-cancel.ts"
      provides: "Motor compartido de baja de serie: cancelAbonoSeries + previewAbonoCancellation + helpers puros de ventana futura"
      exports: ["cancelAbonoSeries", "previewAbonoCancellation", "selectFutureOccurrences", "summarizeOccurrences", "todayISOInAR"]
      min_lines: 90
    - path: "lib/abono-cancel.test.ts"
      provides: "Tests puros (sin DB) de la ventana futura, la exclusión de cancelados y el corte AR"
    - path: "test/abono-cancel.test.ts"
      provides: "Tests contra la DB local: scoping por serie y por tenant, idempotencia, completed → cancelled"
  key_links:
    - from: "lib/abono-cancel.ts"
      to: "lib/booking-window.ts"
      via: "todayInAR() como única fuente de 'hoy' en fecha AR (D-02)"
      pattern: "todayInAR"
    - from: "lib/abono-cancel.ts"
      to: "appointments (tabla)"
      via: "UPDATE masivo acotado por business_id + abono_id + date >= hoy (D-24)"
      pattern: "\\.eq\\('abono_id'"
---

<objective>
Construir el **motor compartido de baja de serie** (`lib/abono-cancel.ts`): la ÚNICA implementación de "dar de baja un abono" que van a llamar las dos vías de esta fase — el link del mail (cliente, Plan 03) y el panel del dueño (Plan 04). Cumple D-07 por construcción: dos implementaciones que puedan divergir en el efecto sobre los turnos son una falla de la fase.

El motor hace tres cosas y nada más: (1) marca la fila del abono como `cancelled` de forma idempotente (D-05), (2) cancela en masa los turnos futuros de esa serie con doble scoping `abono_id` + `business_id` (D-01/D-24), y (3) informa cuántos turnos se cancelaron y cuál era el último (D-03). Es rol-agnóstico como `lib/abono-generation.ts` (recibe el cliente Supabase por parámetro) para que la vía pública lo llame con service role y la vía panel con anon+RLS, sin bifurcar la lógica.

Purpose: es el espinazo de la fase. Las dos vías de arriba son cáscaras de autorización + mail sobre esta función; si esta función está bien, la consistencia entre mail y panel (criterio 4 del ROADMAP §Phase 7) queda garantizada estructuralmente.
Output: `lib/abono-cancel.ts` + tests puros (`lib/abono-cancel.test.ts`) + tests contra la DB local (`test/abono-cancel.test.ts`).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-PATTERNS.md

# Analog directo del contrato rol-agnóstico + del scoping por tenant
@lib/abono-generation.ts
@lib/booking-window.ts
@lib/types.ts
</context>

## Artifacts this phase produces (Plan 01)

Símbolos y archivos que este plan crea y que los planes 03 / 04 consumen:

| Artefacto | Tipo | Firma / forma |
|---|---|---|
| `lib/abono-cancel.ts` | archivo nuevo | módulo de dominio, sin imports de React ni de factories de Supabase |
| `todayISOInAR()` | función exportada | `(): string` — 'yyyy-MM-dd' del día calendario AR |
| `selectFutureOccurrences<T>(rows, todayStr)` | función pura exportada | `<T extends { date: string \| null; status?: string \| null }>(rows: T[], todayStr: string) => T[]` |
| `summarizeOccurrences(rows)` | función pura exportada | `(rows: { date: string \| null }[]) => AbonoCancelSummary` |
| `AbonoCancelSummary` | tipo exportado | `{ count: number; lastDate: string \| null }` |
| `previewAbonoCancellation(input)` | función async exportada | `({ supabase, businessId, abonoId, todayStr? }) => Promise<AbonoCancelSummary>` |
| `cancelAbonoSeries(input)` | función async exportada | `({ supabase, businessId, abonoId, todayStr? }) => Promise<CancelAbonoSeriesResult>` |
| `CancelAbonoSeriesResult` | tipo exportado | unión: `{ ok: true; alreadyCancelled: true; cancelledCount: 0; lastDate: null }` \| `{ ok: true; alreadyCancelled: false; cancelledCount: number; lastDate: string \| null }` \| `{ ok: false; error: 'not_found' \| 'update_failed' }` |
| `lib/abono-cancel.test.ts` | archivo nuevo | suite pura (sin DB) |
| `test/abono-cancel.test.ts` | archivo nuevo | suite contra la DB local con fixtures |

<tasks>

<task type="auto">
  <name>Task 1: lib/abono-cancel.ts — motor compartido de baja (D-01/D-02/D-04/D-05/D-07/D-24)</name>
  <read_first>
    - lib/abono-generation.ts — el contrato que hay que espejar: cabecera doctrinal en español (qué hace / qué NO hace / decisiones no obvias), tipo de entrada con `supabase: SupabaseClient` recibido por parámetro, y el scoping por `business_id` + `abono_id` en toda lectura y todo UPDATE (líneas ~183-220).
    - lib/booking-window.ts — `todayInAR()`: única fuente de verdad del día calendario argentino, robusta con el proceso en UTC.
    - app/api/abonos/create/route.ts — el helper local `toISODate` (líneas 70-77) y `countAbonoAppointments` (líneas 303-315): el molde de serialización de fecha y de conteo acotado por tenant que hay que centralizar acá.
    - lib/types.ts — `interface Abono`: columnas `status` ('active' | 'cancelled' | 'completed'), `cancel_token`, `cancelled_at`.
    - supabase/migrations/054_abonos.sql — confirmar que `status`, `cancelled_at` y `appointments.abono_id` ya existen (esta fase NO agrega migración).
  </read_first>
  <files>lib/abono-cancel.ts</files>
  <action>
    Crear `lib/abono-cancel.ts` con cabecera doctrinal en español al estilo de `lib/abono-generation.ts`, enunciando: que es la ÚNICA implementación de la baja y que las dos vías (token público y panel autenticado) la llaman igual (D-07); que la regla de las 24 horas del cancel de turno suelto NO rige acá (D-06); que `cancelled` es terminal y este módulo no expone ningún camino de vuelta a `active` (D-04); que no manda mails (D-14, eso es del caller); y por qué el UPDATE masivo lleva dos filtros y no uno (D-24).

    Es rol-agnóstico: tipa `supabase` como `SupabaseClient` de `@supabase/supabase-js` recibido por parámetro y no importa ningún factory de cliente del repo — el mismo contrato que `GenerateAbonoInput`.

    Exportar, en este orden:

    (1) `type AbonoCancelSummary = { count: number; lastDate: string | null }`.

    (2) `todayISOInAR(): string` — envuelve `todayInAR()` de `@/lib/booking-window` y serializa con los componentes LOCALES del Date (getFullYear / getMonth+1 / getDate con padStart a 2), igual que el `toISODate` que ya viven duplicados en `app/api/abonos/create/route.ts` y en el cron. Comentar por qué la serialización sale de los componentes locales y no de la representación UTC del instante: el server corre en UTC en Vercel y ese camino corre el día una jornada entera cerca de medianoche AR (D-02).

    (3) `selectFutureOccurrences<T extends { date: string | null; status?: string | null }>(rows: T[], todayStr: string): T[]` — PURA. Conserva las filas con `date` no nulo, `date >= todayStr` (comparación lexicográfica de 'yyyy-MM-dd', inclusive: el turno de HOY entra, D-02) y `status !== 'cancelled'` (los ya cancelados individualmente no cuentan — Claude's Discretion del CONTEXT). Si `status` viene `undefined` (la query del caller ya filtró), la fila se conserva.

    (4) `summarizeOccurrences(rows: { date: string | null }[]): AbonoCancelSummary` — PURA. `count` = cantidad de filas; `lastDate` = el máximo `date` (comparación de strings) o `null` si no hay filas.

    (5) `previewAbonoCancellation({ supabase, businessId, abonoId, todayStr })` → `Promise<AbonoCancelSummary>`. Query a `appointments` seleccionando `date, status`, encadenando `.eq('business_id', businessId)`, `.eq('abono_id', abonoId)`, `.neq('status', 'cancelled')` y `.gte('date', todayStr ?? todayISOInAR())`; el resultado pasa igual por `selectFutureOccurrences` + `summarizeOccurrences` para que la regla viva en un solo lugar. Si la query devuelve error, devolver `{ count: 0, lastDate: null }` y loguear con `console.error('[abonos/cancel] preview error:', ...)` usando el ternario `e instanceof Error ? e.message : e` sobre el mensaje del error.

    (6) `cancelAbonoSeries({ supabase, businessId, abonoId, todayStr })` → `Promise<CancelAbonoSeriesResult>` con la unión declarada en la tabla de artefactos. Secuencia exacta:
      a. **Gate atómico de estado (D-05).** UPDATE sobre `abonos` con `{ status: 'cancelled', cancelled_at: new Date().toISOString() }`, encadenando `.eq('id', abonoId)`, `.eq('business_id', businessId)`, `.neq('status', 'cancelled')` y `.select('id')`. Comentar por qué el filtro de estado va DENTRO del UPDATE y no en un `if` previo: es lo que hace que dos requests simultáneas sobre el mismo token no disparen dos mails (solo la que voltea la fila sigue). Nótese que `.neq('status','cancelled')` deja pasar `completed` — es lo que habilita D-21.
      b. Si el UPDATE devuelve error → `console.error('[abonos/cancel] update abono ...')` y `{ ok: false, error: 'update_failed' }`.
      c. Si el UPDATE devolvió 0 filas, desambiguar con una lectura acotada: `.from('abonos').select('id, status').eq('id', abonoId).eq('business_id', businessId).maybeSingle()`. Sin fila → `{ ok: false, error: 'not_found' }` (no existe, o es de otro tenant: mismo resultado a propósito, no se distingue). Con fila → `{ ok: true, alreadyCancelled: true, cancelledCount: 0, lastDate: null }`, sin tocar un solo appointment.
      d. **Cancelación en masa (D-01/D-24).** UPDATE sobre `appointments` con `{ status: 'cancelled' }` encadenando `.eq('business_id', businessId)`, `.eq('abono_id', abonoId)`, `.gte('date', todayStr ?? todayISOInAR())`, `.neq('status', 'cancelled')` y `.select('date')`. Los dos `.eq` son la mitigación central de la fase: sin ambos, la baja podría alcanzar turnos de otra serie o de otro tenant. Si devuelve error → log + `{ ok: false, error: 'update_failed' }` (la fila del abono ya quedó cancelada: la generación forward ya está frenada, que es la garantía crítica; el caller decide si reintenta).
      e. Derivar el resultado de las filas realmente afectadas: `summarizeOccurrences(data ?? [])` → `{ ok: true, alreadyCancelled: false, cancelledCount: count, lastDate }`. Se usan las filas del UPDATE y no un conteo previo para que el número informado sea el efecto real, sin ventana de carrera entre el preview y la baja.

    No exportar ninguna función de reactivación ni ningún camino que escriba `status: 'active'` sobre un abono.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `lib/abono-cancel.ts` existe y exporta `cancelAbonoSeries`, `previewAbonoCancellation`, `selectFutureOccurrences`, `summarizeOccurrences` y `todayISOInAR`: `grep -cE "^export (async )?function (cancelAbonoSeries|previewAbonoCancellation|selectFutureOccurrences|summarizeOccurrences|todayISOInAR)" lib/abono-cancel.ts` devuelve 5.
    - El módulo no instancia clientes Supabase: `grep -cE "^import .*(lib/supabase|@/lib/supabase)" lib/abono-cancel.ts` devuelve 0.
    - El corte temporal sale del helper AR: `grep -cE "\btodayInAR\s*\(" lib/abono-cancel.ts` es ≥ 1, y `grep -cE "toISOString\(\)\s*\.\s*slice\(\s*0\s*,\s*10\s*\)" lib/abono-cancel.ts` devuelve 0.
    - Doble scoping presente: `grep -cE "\.eq\('business_id'" lib/abono-cancel.ts` es ≥ 3 y `grep -cE "\.eq\('abono_id'" lib/abono-cancel.ts` es ≥ 2.
    - No hay ningún camino de reactivación: `grep -cE "status:\s*'active'" lib/abono-cancel.ts` devuelve 0.
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>
  <done>El motor compartido existe, compila, es rol-agnóstico, acota toda escritura por business_id + abono_id, es idempotente por gate atómico y no ofrece reactivación.</done>
</task>

<task type="auto">
  <name>Task 2: lib/abono-cancel.test.ts — tests puros de la ventana futura y del corte AR</name>
  <read_first>
    - lib/booking-window.test.ts — el patrón exacto de test puro colocado junto al módulo: `vi.useFakeTimers()` + `vi.setSystemTime()` para fijar el borde de medianoche, `afterEach(() => vi.useRealTimers())`, y aserciones independientes del timezone del runner.
    - lib/abono-cancel.ts — las firmas recién creadas.
  </read_first>
  <files>lib/abono-cancel.test.ts</files>
  <action>
    Crear `lib/abono-cancel.test.ts` (suite pura, sin Supabase ni red) con cabecera en español explicando que acá se prueban las REGLAS de la baja (qué se considera futuro, qué se excluye, cómo se resuelve "hoy"), y que el efecto contra la DB se prueba en `test/abono-cancel.test.ts`.

    Casos mínimos:
    - `todayISOInAR` con el reloj fijado en `2026-07-20T02:30:00.000Z` (= 23:30 AR del 19) devuelve `'2026-07-19'`; con el reloj en `2026-07-19T15:00:00.000Z` devuelve `'2026-07-19'`.
    - `selectFutureOccurrences` conserva el turno cuyo `date` es exactamente `todayStr` (frontera inclusive, D-02).
    - `selectFutureOccurrences` descarta un `date` anterior a `todayStr`.
    - `selectFutureOccurrences` descarta las filas con `status: 'cancelled'` aunque sean futuras, y conserva las que tienen `status: 'confirmed'` o `status` ausente.
    - `selectFutureOccurrences` descarta las filas con `date: null`.
    - `summarizeOccurrences` sobre lista vacía devuelve `{ count: 0, lastDate: null }`.
    - `summarizeOccurrences` sobre fechas desordenadas devuelve el máximo como `lastDate` y el largo como `count`.
    - Composición: dado un set mixto (pasados, futuros, uno ya cancelado) el par `selectFutureOccurrences` + `summarizeOccurrences` devuelve el conteo y la última fecha esperados.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx vitest run lib/abono-cancel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/abono-cancel.test.ts` sale 0 con al menos 8 tests pasando.
    - La suite no importa `@supabase/supabase-js` ni ningún módulo de `lib/supabase`: `grep -cE "supabase" lib/abono-cancel.test.ts` devuelve 0.
    - Existe un test que asierta la frontera inclusive (turno de hoy incluido) y otro que asierta la exclusión de los ya cancelados.
  </acceptance_criteria>
  <done>Las reglas puras de la baja (frontera AR inclusive, exclusión de cancelados, conteo/última fecha) están cubiertas sin DB y pasan.</done>
</task>

<task type="auto">
  <name>Task 3: test/abono-cancel.test.ts — efecto real contra la DB (scoping, idempotencia, completed)</name>
  <read_first>
    - test/abono-generation.test.ts — el molde completo: `describe.skipIf(!hasSupabaseCreds)`, `seedOneTenant` / `teardownOneTenant` / `seedTimeBlock` de `./helpers/booking-fixtures`, seed de `clients` + `abonos` con service-role, fechas fijas en el futuro lejano para no chocar con lógica de turnos pasados, y limpieza por `business_id`.
    - test/helpers/booking-fixtures.ts — qué devuelve `SeededTenant` (admin, businessId, serviceId, professionalId, locationId).
    - lib/abono-cancel.ts — las firmas a ejercitar.
  </read_first>
  <files>test/abono-cancel.test.ts</files>
  <action>
    Crear `test/abono-cancel.test.ts` con el mismo encabezado y estructura que `test/abono-generation.test.ts` (misma guarda `describe.skipIf(!hasSupabaseCreds)`, mismo uso del `admin` del fixture como el `supabase` del motor: el módulo es rol-agnóstico y acá se asierta la lógica, no RLS).

    Sembrar: un tenant `t` y un tenant `other`; en `t`, un cliente, un abono A y un abono B (misma agenda, distinto horario) con turnos etiquetados por `abono_id`; en `other`, un abono con sus turnos. Materializar los turnos con inserts directos a `appointments` (acá no se está probando el anti-doble-booking, ya cubierto por Phase 6), con fechas fijas repartidas en pasado y futuro respecto de un `todayStr` que se le pasa EXPLÍCITAMENTE a `cancelAbonoSeries` para que el test no dependa del reloj.

    Casos mínimos:
    (1) **Efecto**: `cancelAbonoSeries` sobre el abono A devuelve `ok: true`, `alreadyCancelled: false`, `cancelledCount` igual a la cantidad de turnos futuros de A y `lastDate` igual a la fecha del último; en la DB el abono A queda `status='cancelled'` con `cancelled_at` no nulo.
    (2) **Frontera (D-02)**: el turno de A cuyo `date` es exactamente `todayStr` queda `cancelled`; el turno de A anterior a `todayStr` conserva su status original.
    (3) **Scoping por serie (D-24)**: ningún turno del abono B cambia de status.
    (4) **Scoping por tenant (D-24)**: ningún turno ni abono de `other` cambia de status, incluso invocando con el `abonoId` de `other` y el `businessId` de `t` (debe devolver `{ ok: false, error: 'not_found' }` y no tocar nada).
    (5) **Idempotencia (D-05)**: una segunda llamada sobre A devuelve `alreadyCancelled: true` con `cancelledCount: 0`, y no cambia `cancelled_at` ni el status de ningún turno adicional.
    (6) **Ya cancelados excluidos**: un turno futuro de A cancelado a mano ANTES de la baja no se cuenta en `cancelledCount`.
    (7) **completed → cancelled (D-21)**: un abono con `status='completed'` se da de baja igual, quedando `cancelled`.
    (8) **Preview**: `previewAbonoCancellation` sobre una serie viva devuelve el mismo `count`/`lastDate` que después informa `cancelAbonoSeries`.

    Teardown por `business_id` de ambos tenants.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx vitest run test/abono-cancel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/abono-cancel.test.ts` sale 0 con al menos 8 tests pasando (o skipeados en bloque si no hay creds de Supabase, igual que el resto de `test/`).
    - Existe un test que asierta explícitamente que los turnos de OTRO abono del mismo negocio no cambian, y otro que asierta que los del otro tenant no cambian.
    - Existe un test que asierta `alreadyCancelled: true` en la segunda invocación.
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>
  <done>El efecto real de la baja está probado contra la DB: cancela lo futuro de SU serie y de SU tenant, respeta la frontera de hoy, es idempotente y admite completed → cancelled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (route handler) → `cancelAbonoSeries` | El `businessId` que entra ya viene resuelto por el caller (token o sesión); el motor no lo deriva de ningún input crudo |
| motor → `appointments` / `abonos` | Con service role no hay RLS: el aislamiento lo dan exclusivamente los filtros por `business_id` |
| requests concurrentes → misma serie | Dos bajas simultáneas del mismo abono no pueden duplicar efectos ni mails |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | UPDATE masivo de `appointments` en `cancelAbonoSeries` | mitigate | el UPDATE encadena SIEMPRE `.eq('business_id', businessId)` y `.eq('abono_id', abonoId)` (D-24); acceptance criteria greguean ambos; test 3 y 4 de `test/abono-cancel.test.ts` lo verifican contra la DB |
| T-07-02 | Tampering | rol del cliente Supabase | mitigate | el motor no importa ningún factory de cliente: recibe `supabase` por parámetro (patrón `lib/abono-generation.ts`); la elección service-role vs anon+RLS la hace cada vía según su modelo de autorización |
| T-07-03 | Tampering | fila `abonos` bajo concurrencia | mitigate | el gate de estado vive DENTRO del UPDATE (`.neq('status','cancelled')` + `.select('id')`): solo la request que voltea la fila continúa, las demás obtienen `alreadyCancelled` (D-05) y no re-disparan mails |
| T-07-04 | Tampering | cálculo del corte "hoy" | mitigate | `todayISOInAR()` deriva de `todayInAR()` (UTC-3 sin DST); el proceso corre en UTC en Vercel y el camino de serialización UTC directa corre el día cerca de medianoche AR — está prohibido por acceptance criteria |
| T-07-05 | Information Disclosure | `previewAbonoCancellation` | mitigate | lee únicamente `date, status` del par (`business_id`, `abono_id`) recibido; no devuelve ids ni datos de cliente |
| T-07-06 | Elevation of Privilege | reactivación de una serie dada de baja | mitigate | D-04: el módulo no expone ninguna función de reactivación y no escribe `status: 'active'`; verificado por grep negativo |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | este plan NO instala paquetes; si apareciera uno, slopcheck + checkpoint humano bloqueante antes del install |
</threat_model>

<verification>
- `npx tsc --noEmit` pasa.
- `npx vitest run lib/abono-cancel.test.ts test/abono-cancel.test.ts` verde.
- `git status --porcelain supabase/migrations` vacío (esta fase NO agrega migración).
- `git status --porcelain app/cancelar app/api/cancel` vacío (D-10: las rutas vivas del cancel de turno suelto no se tocan).
</verification>

<success_criteria>
Existe una única función de baja de serie, rol-agnóstica, idempotente y con doble scoping por serie y por tenant, probada tanto en sus reglas puras como en su efecto real contra la DB. Los Planes 03 y 04 pueden construir sus dos vías encima sin re-implementar nada.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-01-SUMMARY.md` when done
</output>
