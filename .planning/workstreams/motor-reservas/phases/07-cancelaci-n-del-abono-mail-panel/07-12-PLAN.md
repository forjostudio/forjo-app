---
phase: 07-cancelaci-n-del-abono-mail-panel
plan: 12
type: execute
wave: 3
depends_on: [07-06, 07-07, 07-09, 07-10, 07-11]
gap_closure: true
closes_findings: [WR-08]
files_modified:
  - test/abono-cancel-routes.test.ts
autonomous: true
requirements: [ABONO-04, ABONO-05]
must_haves:
  truths:
    - "Dos POST concurrentes sobre el mismo cancel_token producen exactamente UN mail al cliente y UN aviso al dueño (WR-08, D-14, T-07-15)"
    - "La rama already_cancelled NO dispara ningún mail por ninguna de las dos vías (D-05/D-14)"
    - "La vía del panel sin sesión devuelve 401 y no toca ninguna fila (D-23)"
    - "La vía del panel con el abonoId de otro tenant devuelve 404 y no toca ninguna fila del otro negocio (D-22/D-23/D-24)"
    - "Un token con formato inválido y uno inexistente devuelven el mismo 404 genérico, sin revelar si la serie existe (D-22)"
    - "Los tests corren contra la DB local con fixtures y teardown, y skipean limpio si faltan las credenciales de Supabase"
  prohibitions:
    - "NO se modifica ningún archivo de producción desde este plan: es cobertura, no cambio de comportamiento"
    - "NO se relaja ninguna aserción existente de test/abono-cancel.test.ts ni de test/abono-cancel-email.test.ts"
    - "NO se agregan crons, migraciones ni dependencias"
    - "No se modifican 07-01..07-05-PLAN.md"
  artifacts:
    - path: "test/abono-cancel-routes.test.ts"
      provides: "Tests a nivel RUTA de las dos vías de baja: concurrencia, autorización y aislamiento por tenant"
  key_links:
    - from: "test/abono-cancel-routes.test.ts"
      to: "app/api/abonos/cancel/[token]/route.ts"
      via: "importa el handler POST y lo invoca dos veces en paralelo contra la DB local"
      pattern: "abonos/cancel/\\[token\\]"
    - from: "test/abono-cancel-routes.test.ts"
      to: "app/api/abonos/cancel/route.ts"
      via: "importa el handler POST y lo invoca con y sin sesión"
      pattern: "app/api/abonos/cancel/route"
---

<objective>
Cerrar **WR-08**: no hay ni un test a nivel de RUTA, así que el invariante estrella de la fase queda sin cubrir.

Los tests existentes cubren muy bien el motor (`test/abono-cancel.test.ts`, `lib/abono-cancel.test.ts`) y los templates (`test/abono-cancel-email.test.ts`), pero ninguno ejercita `app/api/abonos/cancel/route.ts` ni `app/api/abonos/cancel/[token]/route.ts`. Queda sin verificar justo lo que la fase declara crítico:

- **D-14 / T-07-15 bajo concurrencia:** que dos POST simultáneos sobre el mismo token produzcan UN solo mail. Lo que hoy se testea (Tests 12/13 de `abono-cancel-email.test.ts`) es que *la función de mail* hace un POST cuando se la llama una vez — eso no prueba nada sobre el anti-avalancha; el gate que lo garantiza es de la ruta más el motor.
- `401` sin sesión, `404` con `abonoId` de otro tenant, `400` con body inválido en la vía panel.
- Que la rama `already_cancelled` **no** llame a los templates de baja.

Este plan corre al final de la tanda (Wave 3) a propósito: testea los handlers **tal como quedan** después de los planes 07-06 (barrido de reparación), 07-07 (timeout y escapado), 07-09 (`after()` en la vía pública) y 07-11 (etiqueta compartida). Es también el gate de integración de todo el cierre del review.

**Nota técnica sobre `after()`.** Después del Plan 07-09 las dos vías mandan los mails dentro de `after()` de `next/server`, que necesita un request scope real y no existe cuando se invoca el handler directo desde Vitest. El test tiene que mockear parcialmente `next/server` conservando todos sus exports reales y reemplazando solo `after` por una implementación que ejecute el callback y guarde su promesa, para poder esperarla antes de asertar sobre los mails. Sin ese mock, el handler tira al llegar a `after()` y el test no prueba nada.

Purpose: el anti-avalancha y el aislamiento por tenant de las dos vías dejan de depender de la lectura del código (ABONO-04, ABONO-05, D-14/D-22/D-23/D-24).
Output: 1 archivo de tests nuevo, más el gate final de la tanda.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-REVIEW.md
@.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/deferred-items.md

# Handlers bajo test
@app/api/abonos/cancel/[token]/route.ts
@app/api/abonos/cancel/route.ts

# Moldes de test
@test/abono-cancel.test.ts
@test/mp-cobro-callback.test.ts
@test/helpers/booking-fixtures.ts
</context>

## Artifacts this phase produces (Plan 12)

| Artefacto | Tipo | Firma / forma |
|---|---|---|
| `test/abono-cancel-routes.test.ts` | archivo de test nuevo | `describe.skipIf(!hasSupabaseCreds)` con dos bloques: vía pública por token y vía panel autenticada |
| mock parcial de `next/server` | infraestructura de test | conserva los exports reales y reemplaza `after` por un ejecutor que guarda las promesas de los callbacks |
| helper de request | infraestructura de test | construye el `Request` con body JSON para la vía panel y el `params` como Promise para la vía por token |

<tasks>

<task type="auto">
  <name>Task 1: vía pública por token — concurrencia (D-14), 404 genérico y already_cancelled sin mails</name>
  <read_first>
    - `.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-REVIEW.md` sección `#### WR-08` (líneas 314-332) — qué falta cubrir y la forma sugerida del test.
    - `app/api/abonos/cancel/[token]/route.ts` tal como quedó tras el Plan 07-09 — la firma `POST(_req, { params })` con `params` como Promise, el gate de estado, la llamada al motor, la forma de las respuestas y el bloque de mails dentro de `after()`.
    - `test/abono-cancel.test.ts` completo — los fixtures (`seedOneTenant`, `teardownOneTenant`, `seedSeriesAppointments`), las fechas fijas de 2031, cómo se siembra una fila `abonos` con `t.admin` y el `describe.skipIf(!hasSupabaseCreds)`.
    - `test/mp-cobro-callback.test.ts` líneas 1-45 — el patrón de mocks del proyecto: `vi.hoisted` para las factories, `vi.mock('@/lib/supabase/admin', ...)`, `vi.mock` de módulos de dominio, y los `beforeEach`/`afterEach` que silencian consola y limpian mocks.
    - `test/concurrency.test.ts` — cómo el repo ejercita carreras reales con `Promise.all` contra la DB local.
    - `test/env.ts` — `hasSupabaseCreds` y el criterio de skip graceful.
  </read_first>
  <files>test/abono-cancel-routes.test.ts</files>
  <action>
    Crear `test/abono-cancel-routes.test.ts` con cabecera doctrinal en español que enuncie qué cubre este archivo y qué NO (las reglas puras están en `lib/abono-cancel.test.ts`, el efecto del motor en `test/abono-cancel.test.ts` y los templates en `test/abono-cancel-email.test.ts`; acá se prueba el CONTRATO DE LAS RUTAS).

    Infraestructura, con `vi.hoisted` para las factories:
    - `vi.mock('next/server', ...)` **parcial**: importar el módulo original y sobrescribir solo `after` con una función que invoque el callback y guarde la promesa resultante en un array accesible desde el test. Sin esto el handler tira al llegar a `after()`, porque no hay request scope.
    - `vi.mock('@/lib/supabase/admin', ...)`: `createAdminClient` devuelve un cliente Supabase REAL con la service-role key del entorno (misma construcción que usa `test/helpers/booking-fixtures.ts`). Se mockea la FACTORY, no el comportamiento: las queries golpean la DB local de verdad, que es lo único que puede probar una carrera.
    - `vi.mock('@/lib/email', ...)`: `sendAbonoCancelledEmail` y `sendAbonoCancelledAdminNotification` como spies que resuelven. Son el contador del anti-avalancha.
    - `vi.mock('@/lib/business-secrets', ...)`: `getBusinessSecrets` devuelve un objeto con la key y el from de prueba.

    Un helper local que construya el segundo argumento del handler: un objeto con `params` como `Promise.resolve({ token })`, tal como Next 16 se lo pasa.

    Casos, todos dentro de `describe.skipIf(!hasSupabaseCreds)` con `seedOneTenant` en `beforeAll` y `teardownOneTenant` en `afterAll`, sembrando una serie con turnos futuros y el `cancel_token` leído de la fila insertada:

    - **Carrera (D-14 / T-07-15).** `await Promise.all([POST(req, ctx), POST(req, ctx)])` sobre el MISMO token. Después de esperar las promesas registradas por el `after` mockeado: `sendAbonoCancelledEmail` fue llamado exactamente 1 vez y `sendAbonoCancelledAdminNotification` exactamente 1 vez. Exactamente una de las dos respuestas trae `ok: true` y la otra `reason: 'already_cancelled'`. Y en la base no queda ningún turno de la serie con `date >= cutoff` sin cancelar.
    - **Idempotencia sin mails.** Un tercer POST sobre el mismo token devuelve `reason: 'already_cancelled'` y los dos spies siguen en 1 llamada cada uno.
    - **Token inexistente.** Un uuid válido que no corresponde a ninguna serie devuelve status 404 y `reason: 'not_found'`, sin ninguna llamada a los spies de mail.
    - **Token con formato inválido.** Una cadena que no es un uuid devuelve exactamente el MISMO status 404 y el mismo `reason: 'not_found'` que el caso anterior — no se distingue, y no hay 500 (D-22, T-07-13).
    - **Token vacío.** Devuelve 400 con `reason: 'invalid'`.
    - **Efecto real de la baja.** En el caso de la carrera, el conteo devuelto por la respuesta `ok: true` coincide con la cantidad de turnos que efectivamente pasaron a `cancelled` en la base.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx vitest run test/abono-cancel-routes.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `test/abono-cancel-routes.test.ts` existe y `npx vitest run test/abono-cancel-routes.test.ts --no-file-parallelism` sale con código 0.
    - El bloque de la vía pública tiene al menos 6 casos.
    - La carrera es real: `grep -cE "Promise\.all\(\[" test/abono-cancel-routes.test.ts` es ≥ 1 y el caso asierta `toHaveBeenCalledTimes(1)` sobre los dos spies de mail.
    - El mock de `after` es parcial: `grep -cE "importOriginal|vi\.importActual" test/abono-cancel-routes.test.ts` es ≥ 1.
    - Skipea limpio sin credenciales: `grep -cE "describe\.skipIf\(!hasSupabaseCreds\)" test/abono-cancel-routes.test.ts` es ≥ 1.
    - Hay teardown: `grep -cE "teardownOneTenant" test/abono-cancel-routes.test.ts` es ≥ 1.
    - Prueba de que el test detecta la regresión: comentando temporalmente el gate `.neq('status', 'cancelled')` del UPDATE de `abonos` en `lib/abono-cancel.ts`, el caso de la carrera FALLA. Revertir después de comprobarlo y dejarlo anotado en el SUMMARY.
    - Ningún archivo de producción quedó modificado: `git status --porcelain app/ lib/ supabase/` no devuelve ninguna línea.
  </acceptance_criteria>
  <done>La vía pública tiene cobertura a nivel ruta del anti-avalancha bajo carrera real, del 404 indistinguible y de la idempotencia sin mails.</done>
</task>

<task type="auto">
  <name>Task 2: vía panel autenticada — 401, 400, 404 de tenant ajeno y cero escrituras cruzadas</name>
  <read_first>
    - `app/api/abonos/cancel/route.ts` tal como quedó tras el Plan 07-11 — el gate de sesión, la resolución del negocio por `owner_id`, el parseo defensivo del body, la re-lectura por `id + business_id` y las respuestas con clave `error` (no `reason`).
    - `test/isolation.test.ts` líneas 30-55 — cómo el repo construye un cliente anon autenticado: `createClient(url, anonKey, { auth: { persistSession: false } })` más `signInWithPassword` con el email y password del fixture, y el guard anti-falso-verde que verifica que la sesión tenga `access_token` (para no estar asertando con service-role por accidente).
    - `test/helpers/booking-fixtures.ts` — `SeededTenant` expone `email` y `password` del dueño creado con `email_confirm: true`, que es lo que hace que `signInWithPassword` funcione de inmediato.
    - `.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-CONTEXT.md` D-22/D-23/D-24 — el contrato de aislamiento que estos casos verifican.
  </read_first>
  <files>test/abono-cancel-routes.test.ts</files>
  <action>
    Agregar al mismo archivo un segundo bloque `describe` para la vía del panel. Mockear `@/lib/supabase/server` de forma que `createClient` devuelva el cliente que el caso necesite: para los casos autenticados, un cliente anon con la sesión del dueño del fixture (`signInWithPassword` con `email` y `password` de `SeededTenant`); para el caso sin sesión, un cliente anon sin login.

    Incluir el guard anti-falso-verde del proyecto: antes de correr las aserciones, verificar que la sesión del cliente autenticado tiene `access_token`. Un cliente service-role no produce sesión por `signIn`, así que si por error se configurara con la key equivocada el test falla ruidosamente en vez de dar un verde falso.

    Sembrar DOS tenants (el propio y uno ajeno), cada uno con su serie y sus turnos futuros.

    Casos:
    - **Sin sesión → 401.** `POST` con un body válido devuelve status 401 y `error: 'unauthorized'`. Ninguna fila cambia: la serie del tenant sigue en `active` y sus turnos futuros siguen sin cancelar.
    - **Body que no es JSON → 400.** `error: 'bad_request'`.
    - **Body sin `abonoId` → 400.** `error: 'missing_fields'`.
    - **`abonoId` de OTRO tenant → 404.** `error: 'not_found'`, y — la aserción que importa — la serie del OTRO negocio sigue en `active` y **ninguno** de sus turnos futuros quedó cancelado (D-24). Contar las filas antes y después.
    - **`abonoId` inexistente → 404.** Mismo status y mismo cuerpo que el caso anterior: no se distingue "no existe" de "es de otro negocio" (D-22).
    - **Baja válida → 200 y UN solo mail al cliente.** `ok: true`, `alreadyCancelled: false`, `cancelledCount` igual a la cantidad de turnos que pasaron a `cancelled` en la base, y `sendAbonoCancelledEmail` llamado exactamente 1 vez tras esperar las promesas del `after` mockeado.
    - **La vía panel NO avisa al dueño (D-13).** `sendAbonoCancelledAdminNotification` no fue llamado en ninguno de los casos de esta vía.
    - **Reintento → 200 con `alreadyCancelled: true` y sin mail nuevo.** El spy sigue en 1 llamada.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx vitest run test/abono-cancel-routes.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/abono-cancel-routes.test.ts --no-file-parallelism` sale 0 con al menos 14 casos en total (los dos bloques).
    - El guard anti-falso-verde existe: `grep -cE "access_token" test/abono-cancel-routes.test.ts` es ≥ 1.
    - Hay dos tenants sembrados: `grep -cE "seedOneTenant\(" test/abono-cancel-routes.test.ts` es ≥ 2.
    - El caso de tenant ajeno asierta cero escrituras: existe un caso cuyo nombre menciona el otro negocio y que compara el conteo de turnos no cancelados del otro tenant antes y después de la llamada.
    - La vía panel no avisa al dueño: hay una aserción `not.toHaveBeenCalled()` sobre el spy del aviso al dueño.
    - `npx tsc --noEmit` sale 0.
    - Ningún archivo de producción quedó modificado: `git status --porcelain app/ lib/ supabase/` no devuelve ninguna línea.
  </acceptance_criteria>
  <done>La vía del panel tiene cobertura a nivel ruta de sus cuatro caminos de rechazo y de que una baja no puede alcanzar la serie de otro negocio.</done>
</task>

<task type="auto">
  <name>Task 3: gate final de la tanda — suite completa, build y auditoría de cobertura de los 15 hallazgos</name>
  <read_first>
    - `.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-REVIEW.md` — la lista completa de los 15 hallazgos con sus IDs.
    - Los SUMMARY de los planes 07-06 a 07-11 — qué cerró cada uno y con qué evidencia.
    - `.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/deferred-items.md` — los fallos pre-existentes de la suite que NO son de esta tanda.
  </read_first>
  <files>test/abono-cancel-routes.test.ts</files>
  <action>
    Gate de integración de todo el cierre del review:

    (1) `npx tsc --noEmit` → 0 errores.
    (2) `npx vitest run --no-file-parallelism` → 0 fallos. Comparar el conteo contra el baseline registrado por el Plan 07-06 y contra el 683 documentado en `deferred-items.md`.
    (3) `npm run build` → 0 errores. Es lo que valida la metadata `robots` de la página pública y que el endpoint nuevo del link no colisione con las rutas existentes bajo `app/api/abonos/`.
    (4) `npx eslint` sobre todos los archivos tocados por la tanda → 0 errores.
    (5) Verificar a mano que la ruta `/api/abonos/cancel` sigue resolviendo al handler estático y no al dinámico nuevo: en la salida de `npm run build`, la tabla de rutas tiene que listar `/api/abonos/cancel`, `/api/abonos/cancel/[token]`, `/api/abonos/create` y `/api/abonos/cancel-link/[id]` como entradas separadas.

    (6) Armar en el SUMMARY una **tabla de cobertura** con las 15 filas del review (CR-01, WR-01..WR-09, IN-01..IN-05), y para cada una: el plan que la cerró, el archivo donde vive el arreglo y la evidencia concreta (test, criterio de aceptación o comando). Cualquier hallazgo sin evidencia se reporta como abierto — no se marca cerrado por confianza.

    (7) Registrar también, si quedó alguna, la deuda anotada por los planes previos: el `noindex` faltante en la ruta hermana `/cancelar/[token]` (Plan 07-09), la evolución a vista agregada del panel (Plan 07-10) y los `console.log` de los templates viejos (Plan 07-07).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npx vitest run --no-file-parallelism && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sale 0.
    - `npx vitest run --no-file-parallelism` sale 0 y el conteo de tests verdes es ≥ 700 (683 de baseline más los casos agregados por los planes 07-06, 07-07, 07-09 y 07-12).
    - `npm run build` sale 0 y su tabla de rutas lista las cuatro rutas de `app/api/abonos/` como entradas separadas.
    - `npx eslint` sobre los archivos tocados por la tanda sale 0.
    - El SUMMARY contiene una tabla con exactamente 15 filas, una por hallazgo del review, cada una con plan, archivo y evidencia.
    - Ningún hallazgo queda con evidencia vacía; si alguno quedara abierto, está declarado explícitamente como abierto.
  </acceptance_criteria>
  <done>La tanda completa compila, corre verde y tiene una auditoría fila por fila que muestra dónde y cómo se cerró cada uno de los 15 hallazgos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → Supabase LOCAL con service-role | los fixtures crean y borran usuarios y negocios reales; las credenciales salen del entorno local |
| handler bajo test → DB local | la carrera se ejercita de verdad contra Postgres, no contra un doble |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-55 | Repudiation | el anti-avalancha (D-14 / T-07-15) verificado solo por lectura de código | mitigate | test de carrera real con `Promise.all` de dos POST sobre el mismo token contra la DB local, asertando exactamente 1 llamada a cada template de mail. Verificado además con la prueba de mutación del criterio de aceptación: quitando el gate atómico, el test falla |
| T-07-56 | Elevation of Privilege | la baja por panel alcanzando la serie de otro tenant | mitigate | caso dedicado con dos tenants sembrados que asierta 404 Y cero cambios en las filas del negocio ajeno, comparando conteos antes y después (D-24) |
| T-07-57 | Information Disclosure | el 404 revelando si una serie existe | mitigate | casos que exigen status y cuerpo IDÉNTICOS para "no existe", "formato de token inválido" y "es de otro negocio" (D-22) |
| T-07-58 | Spoofing | test que da verde usando service-role donde debería usar la sesión del dueño | mitigate | guard anti-falso-verde del proyecto: se verifica que el cliente de aserción tenga `access_token` de sesión antes de correr las aserciones (patrón de `test/isolation.test.ts`) |
| T-07-59 | Information Disclosure | credenciales de Supabase en el entorno de test | accept | los tests skipean limpio sin credenciales (`describe.skipIf(!hasSupabaseCreds)`), corren contra la base LOCAL y los fixtures borran lo que crean en `teardownOneTenant`. Mismo criterio que el resto de la suite |
| T-07-60 | Tampering | el mock parcial de `next/server` ocultando un bug real de `after()` | accept | el mock reemplaza SOLO `after` y conserva el resto de los exports reales. Riesgo residual: no se verifica el comportamiento de `after()` en el runtime de Vercel — eso lo cubre el checkpoint humano end-to-end (07-05) y la verificación en staging |
| T-07-SC | Tampering | npm/pip/cargo installs | mitigate | este plan NO agrega ni actualiza ninguna dependencia: usa Vitest y los helpers de test que ya están en el repo. `package.json` y `package-lock.json` quedan intactos |
</threat_model>

<verification>
1. `npx vitest run test/abono-cancel-routes.test.ts --no-file-parallelism` → 0 fallos, ≥ 14 casos.
2. `npx vitest run --no-file-parallelism` → 0 fallos, conteo ≥ 700.
3. `npx tsc --noEmit` y `npm run build` → 0 errores.
4. `git status --porcelain app/ lib/ supabase/` → vacío (este plan no toca producción).
5. Prueba de mutación: quitando el gate atómico del motor, el caso de la carrera falla; revertida después de comprobarlo.
</verification>

<success_criteria>
- WR-08 cerrado: existe `test/abono-cancel-routes.test.ts` con cobertura a nivel ruta de las dos vías.
- El anti-avalancha bajo carrera real está verificado, no supuesto.
- El aislamiento por tenant de la baja por panel está verificado con dos tenants y conteos antes/después.
- La tanda completa compila, corre verde y tiene la auditoría de cobertura de los 15 hallazgos del review.
</success_criteria>

<output>
Crear `.planning/workstreams/motor-reservas/phases/07-cancelaci-n-del-abono-mail-panel/07-12-SUMMARY.md` al terminar, con la tabla de cobertura de los 15 hallazgos (plan / archivo / evidencia) y la lista de deuda anotada por los planes previos.
</output>
