---
phase: 04-ventana-de-reserva-p-blica
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/(dashboard)/settings/settings-client.tsx
autonomous: true
requirements: [BOOK-WINDOW-01]
must_haves:
  truths:
    - "El dueño ve en Ajustes → Cobros un control de ventana de reserva con 3 modos mutuamente excluyentes (días de anticipación / sin límite / fecha exacta) — D-01/D-04"
    - "Guardar persiste en businesses escribiendo EXACTAMENTE la columna del modo activo y nuleando la otra (Pitfall 4: nunca dejar las dos seteadas)"
    - "El estado inicial del control refleja el valor guardado del negocio (max_advance_days / max_advance_date), con default 30 días visible"
  artifacts:
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "Control de ventana de reserva en la tab Cobros + handler de save (patrón saveDeposit)"
      contains: "max_advance"
  key_links:
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "businesses (tabla)"
      via: "supabase.from('businesses').update({ max_advance_days, max_advance_date }).eq('id', business.id) + toast"
      pattern: "from\\('businesses'\\).update"
---

<objective>
Control en el dashboard para que el dueño configure la ventana de reserva pública (BOOK-WINDOW-01). Va en Ajustes → tab **Cobros** (hub Negocio), junto a la config de Seña, reusando el patrón `saveDeposit` ya establecido (setState + `supabase.from('businesses').update(...)` + toast).

Expone los 3 modos mutuamente excluyentes (D-01): (a) días de anticipación (input numérico), (b) sin límite (toggle), (c) fecha exacta de corte (date input). El save escribe una sola columna y nulea la otra (Pitfall 4).

Purpose: es la superficie con la que el dueño maneja la ventana; sin esto el valor solo se puede cambiar por SQL. Depende de Plan 01 (columnas + tipos ya en `businesses`/`Business`).
Output: control funcional en Cobros que persiste `max_advance_days`/`max_advance_date`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/phases/04-ventana-de-reserva-p-blica/04-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/04-ventana-de-reserva-p-blica/04-CONTEXT.md

# Patrón de update + tab Cobros (leer antes de tocar)
@app/(dashboard)/settings/settings-client.tsx
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estado + handler de save del control de ventana</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - settings-client.tsx L711-716 (patrón depositForm/setDepositForm + savingDeposit)
    - settings-client.tsx L753-759 (saveDeposit: update de businesses + toast — el patrón exacto a imitar)
    - lib/types.ts L38-54 (los campos max_advance_days/max_advance_date que llegan en la prop business)
    - 04-RESEARCH.md §Pitfall 4 (los dos modos NO pueden convivir en la DB: el save nulea el inactivo)
  </read_first>
  <action>
    En `settings-client.tsx`, junto a los otros `useState` de forms (~L711-740), agregar el estado del control de ventana. Modelar un modo activo derivado del business que llega por props:
      - modo inicial: si `business.max_advance_date` presente → 'fecha'; si `business.max_advance_days && > 0` → 'dias'; si no → 'sin_limite'.
      - `windowForm` con: `mode` ('dias' | 'sin_limite' | 'fecha'), `days` (number, inicial `business.max_advance_days ?? 30`), `date` (string ISO o '' , inicial `business.max_advance_date ?? ''`).
      - `savingWindow` (boolean) para el estado de carga del botón.

    Agregar el handler `saveWindow()` siguiendo EXACTAMENTE el patrón `saveDeposit` (L753-759): setSavingWindow(true), calcular el payload según el modo (Pitfall 4 — una columna con valor, la otra null):
      - mode 'dias' → `{ max_advance_days: days (>=1), max_advance_date: null }`
      - mode 'sin_limite' → `{ max_advance_days: null, max_advance_date: null }`
      - mode 'fecha' → `{ max_advance_days: null, max_advance_date: date }` (date en 'yyyy-mm-dd')
    Luego `supabase.from('businesses').update(payload).eq('id', business.id)`, setSavingWindow(false), toast.success('Ventana de reserva guardada') / toast.error('Error al guardar'). NO tocar depositForm ni saveDeposit.

    Validación mínima en cliente antes del update: modo 'dias' exige days entero ≥ 1; modo 'fecha' exige date no vacío. Si no cumple, toast.error y no persistir.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "saveWindow" "app/(dashboard)/settings/settings-client.tsx" && grep -q "max_advance_date: null" "app/(dashboard)/settings/settings-client.tsx" && echo OK-SAVE</automated>
  </verify>
  <acceptance_criteria>
    - Existe `windowForm` (mode/days/date) inicializado desde `business.max_advance_days`/`business.max_advance_date`.
    - `saveWindow()` persiste una sola columna según el modo y nulea la otra (los 3 modos cubiertos).
    - El estado de carga (`savingWindow`) deshabilita el botón; toast de éxito/error como saveDeposit.
    - depositForm/saveDeposit no se alteran.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: UI del control (Card en la tab Cobros)</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - settings-client.tsx L1538-1577 (TabsContent value="cobros": la Card de Seña y la de limpieza — imitar el layout Card/Label/Input/Button y el spacing)
    - settings-client.tsx L1543-1564 (checkbox + inputs condicionales + botón Guardar del bloque Seña)
  </read_first>
  <action>
    Dentro de `<TabsContent value="cobros" ...>` (L1539), agregar una nueva `<Card className="p-6 space-y-4">` DESPUÉS de la Card de Seña (L1565) y antes o después de la de limpieza — mantener consistencia visual con las Cards hermanas (mismo padding, tipografía de título `font-semibold text-sm`, Label/Input del design system, un solo botón primario "Guardar").

    Título: "Ventana de reserva". Copy de ayuda (text-xs text-muted-foreground): explicar que limita hasta con cuánta anticipación un cliente puede reservar desde la página pública (no afecta la carga manual de turnos).

    Selector de los 3 modos (D-04, forma exacta = discreción siguiendo el design system; recomendado radios o un grupo de 3 opciones mutuamente excluyentes que setean `windowForm.mode`):
      - "Hasta X días de anticipación" → input numérico (min 1) ligado a `windowForm.days`, visible solo en modo 'dias'.
      - "Sin límite" → modo 'sin_limite'.
      - "Hasta una fecha exacta" → date input (`<input type="date">` o el date-picker del design system) ligado a `windowForm.date`, visible solo en modo 'fecha'.
    Los inputs de días y fecha se muestran condicionalmente según el modo activo (patrón del bloque Seña, que muestra monto/horas solo si require_deposit).

    Botón: `<Button onClick={saveWindow} disabled={savingWindow}>{savingWindow ? 'Guardando...' : 'Guardar'}</Button>`.

    Accesibilidad (regla UI del proyecto): cada control con su `<Label htmlFor>`; touch targets adecuados; estados focus visibles (usar los componentes del design system que ya los traen). Mobile-first: inputs a ancho completo en mobile.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsc --noEmit 2>&1 | grep -v "node_modules" | (grep "settings-client.tsx" && exit 1 || echo OK-TSC)</automated>
    <automated>grep -q "Ventana de reserva" "app/(dashboard)/settings/settings-client.tsx" && echo OK-UI</automated>
  </verify>
  <acceptance_criteria>
    - En Ajustes → Cobros aparece la Card "Ventana de reserva" con los 3 modos mutuamente excluyentes.
    - Los inputs de días y de fecha se muestran solo en su modo; el botón Guardar invoca saveWindow.
    - Consistencia visual con las Cards hermanas de la tab (padding, tipografía, Label/Input/Button del design system); labels visibles y focus states presentes.
    - `tsc --noEmit` sin errores nuevos en settings-client.tsx.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño (autenticado, browser) → businesses (RLS owner-only) | El control escribe la fila del propio negocio con anon key + RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering / Elevation of Privilege | `saveWindow` update de businesses | mitigate | `.eq('id', business.id)` + RLS owner-only de businesses (with check ya vigente) impiden escribir la fila de otro tenant; el update solo toca max_advance_* (no plan_status ni columnas sensibles) |
| T-04-06 | (integridad de config) | dos modos conviviendo en DB | mitigate | El save escribe una columna y nulea la otra según el modo (Pitfall 4); la precedencia fecha>días del helper es solo red de seguridad, no comportamiento esperado |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Sin instalación de paquetes en este plan (usa componentes ya presentes del design system) |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores nuevos.
- Manual (checkpoint humano de la fase, no de este plan): abrir Ajustes → Cobros, cambiar entre los 3 modos, guardar, refrescar y confirmar que el modo/valor persiste; confirmar que "sin límite" nulea ambas columnas.
</verification>

<success_criteria>
- Control de ventana de reserva funcional en Ajustes → Cobros con los 3 modos.
- Persistencia correcta: una columna por modo, la otra en null.
- Sin regresión visual ni de estado en el resto de la tab Cobros.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 02)
- `app/(dashboard)/settings/settings-client.tsx` — Card "Ventana de reserva" en la tab Cobros + `windowForm`/`saveWindow` (persiste max_advance_days/max_advance_date)
</artifacts>

<output>
Create `.planning/workstreams/motor-reservas/phases/04-ventana-de-reserva-p-blica/04-02-SUMMARY.md` when done
</output>
