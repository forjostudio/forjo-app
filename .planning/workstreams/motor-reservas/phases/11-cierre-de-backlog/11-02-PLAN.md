---
phase: 11-cierre-de-backlog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/settings/settings-client.tsx
  - components/dashboard/canchas-manager.tsx
autonomous: true
requirements: [EXTRA-A]
must_haves:
  truths:
    - "Intentar borrar un servicio, una sede o una cancha con turnos asociados muestra un copy que aclara que 'tiene turnos asociados' incluye pasados y cancelados (cancelar no borra la fila) y ofrece las dos vías: desactivar para conservar historial, o borrar los turnos primero (D-04)."
    - "deleteProfessional captura el error del DELETE, ramifica por 23503 y NO saca al profesional de la lista ni muestra 'Profesional eliminado' cuando el FK bloquea (fin del toast mentiroso)."
    - "El comportamiento del borrado NO cambia: el FK 23503 sigue protegiendo el historial de Finanzas; el soft-disable (toggleService/toggleLocation) sigue siendo la vía correcta."
  artifacts:
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "Copy nuevo en los toasts de servicio (:403) y sede (:738) + deleteProfessional (:573) alineado a deleteService (captura 23503)"
      contains: "23503"
    - path: "components/dashboard/canchas-manager.tsx"
      provides: "Copy nuevo en el toast de borrado de cancha bloqueado por FK (:181)"
      contains: "turnos asociados"
  key_links:
    - from: "app/(dashboard)/settings/settings-client.tsx (deleteProfessional)"
      to: "deleteService (:397-409)"
      via: "mismo patrón: capturar { error }, ramificar error.code === '23503', return sin mutar estado si falló"
      pattern: "error.code === '23503'"
---

<objective>
**EXTRA-A (D-04, folded todo):** aclarar el copy de los toasts de borrado bloqueado por FK (23503) para servicio/sede/cancha, y alinear `deleteProfessional` al patrón de `deleteService` (hoy es fire-and-forget con borrado optimista y toast "eliminado" mentiroso). Es **solo copy + el fix del handler**; NO cambia el comportamiento del borrado (el FK protege el historial de Finanzas; el soft-disable ya existe).

Purpose: hoy el mensaje es engañoso (no aclara que "turnos asociados" incluye pasados y cancelados, ni ofrece la salida de desactivar) y `deleteProfessional` puede mentir "eliminado" cuando el FK bloqueó el DELETE.
Output (artifacts de este plan): los copys de toast (servicio/sede/cancha) + el fix de `deleteProfessional`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy nuevo en los 3 toasts de borrado bloqueado (servicio, sede, cancha) — D-04</name>
  <files>app/(dashboard)/settings/settings-client.tsx, components/dashboard/canchas-manager.tsx</files>
  <read_first>
    - app/(dashboard)/settings/settings-client.tsx — `deleteService` (:397-409) como patrón correcto (captura `{ error }`, ramifica `error.code === '23503'`, no muta estado si falló); el string de servicio (:403) y el de sede (:738, usa `${locWord}`).
    - components/dashboard/canchas-manager.tsx — el toast de cancha (:181), que ramifica por `res.error === 'has_appointments'` (:180).
    - 11-RESEARCH.md §EXTRA-A — las 3 strings actuales (tabla) + el copy de referencia D-04.
    - 11-PATTERNS.md §settings-client + canchas-manager — analog `deleteService` y qué es "solo copy".
  </read_first>
  <action>
    Reemplazar el texto de los 3 toasts de borrado bloqueado por FK, adaptando el sustantivo y el género del verbo ("el servicio" / "el ${locWord}" / "la cancha" → "Desactivalo"/"Desactivala"). El copy debe: (a) aclarar que "tiene turnos asociados" incluye **pasados y cancelados** (cancelar no borra la fila); (b) ofrecer las **dos vías**: desactivar para dejar de ofrecerlo y conservar el historial, o borrar esos turnos primero. Base de referencia (ajustable en género/sustantivo): "No se puede eliminar: tiene turnos asociados, incluidos pasados y cancelados (cancelar no los borra). Desactivalo para dejar de ofrecerlo y conservar el historial, o borrá esos turnos primero." Aplicarlo en `settings-client.tsx:403` (servicio) y `:738` (sede) y en `canchas-manager.tsx:181` (cancha). SOLO copy: NO tocar la lógica de borrado, ni las condiciones de ramificación (`error.code === '23503'` / `res.error === 'has_appointments'`), ni el soft-disable. El FK sigue protegiendo el historial de Finanzas.
  </action>
  <verify>
    <automated>grep -q 'pasados y cancelados' "app/(dashboard)/settings/settings-client.tsx" && grep -q 'pasados y cancelados' "components/dashboard/canchas-manager.tsx" && npx tsc --noEmit</automated>
    <human-check>Intentar borrar un servicio/sede/cancha que tiene turnos: aparece el toast nuevo que aclara pasados+cancelados y ofrece desactivar o borrar los turnos.</human-check>
  </verify>
  <acceptance_criteria>
    - Los 3 toasts (servicio/sede/cancha) muestran el copy nuevo con la aclaración de pasados+cancelados y las dos vías.
    - Ninguna condición de ramificación ni el comportamiento de borrado cambió.
    - `npx tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>Los 3 toasts (servicio/sede/cancha) muestran el copy nuevo (pasados+cancelados + las dos vías, D-04); ninguna condición de ramificación ni el borrado cambió; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Fix de deleteProfessional — alinear al patrón de deleteService (captura 23503)</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - app/(dashboard)/settings/settings-client.tsx — `deleteProfessional` (:573): hoy `await supabase.from('professionals').delete()...` sin capturar el error, borrado optimista de la lista (`setProfessionals`/`setAgendaSpaces`) y `toast.success('Profesional eliminado')` aunque el FK bloquee; el analog correcto `deleteService` (:397-409).
    - 11-RESEARCH.md §EXTRA-A (⚠ deleteProfessional NO maneja el 23503) + Assumptions Log A3 (el FK que bloquea es el de `appointments`; `professional_services`/`agenda_spaces` caen por CASCADE → el mensaje genérico "tiene turnos asociados" es correcto).
    - 11-PATTERNS.md §settings-client (Fix real: reescribir igual que deleteService).
  </read_first>
  <action>
    Reescribir `deleteProfessional` (:573) espejando `deleteService`: capturar `const { error }` del DELETE; si `error.code === '23503'`, mostrar el copy nuevo (sustantivo "el profesional", "Desactivalo") y `return` SIN mutar `setProfessionals`/`setAgendaSpaces` ni mostrar success; si hay otro error, toast genérico de fallo y return; solo en el camino sin error, sacar al profesional de la lista y mostrar "Profesional eliminado". Usar el mensaje genérico "tiene turnos asociados" (el FK que bloquea es el de `appointments`; los otros caen por CASCADE). NO cambiar la firma ni los callers del handler; NO cambiar el comportamiento del soft-disable de profesionales si existiera. El objetivo es que el toast deje de mentir y el estado optimista no se aplique cuando el FK bloquea.
  </action>
  <verify>
    <automated>grep -q "error.code === '23503'" "app/(dashboard)/settings/settings-client.tsx" && npx tsc --noEmit && npm test</automated>
    <human-check>Intentar borrar un profesional con turnos asociados: aparece el toast nuevo y el profesional NO desaparece de la lista (no más "eliminado" mentiroso). Borrar un profesional sin turnos: se elimina normal.</human-check>
  </verify>
  <acceptance_criteria>
    - `deleteProfessional` captura el error, ramifica `error.code === '23503'` con el copy nuevo y NO muta el estado ni muestra success cuando el FK bloquea.
    - Un profesional sin turnos se sigue borrando igual que hoy.
    - `npx tsc --noEmit` limpio y `npm test` verde.
  </acceptance_criteria>
  <done>`deleteProfessional` captura el error, ramifica 23503 con copy nuevo y NO muta el estado cuando el FK bloquea (fin del toast mentiroso); un profesional sin turnos se borra igual; tsc + npm test verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (sesión) → tabla `professionals`/`services`/`locations` (DELETE) | acción autenticada por RLS + `.eq('business_id', ...)`; el FK 23503 es la guarda de integridad del historial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Repudiation / Integrity | deleteProfessional (borrado optimista + toast success mentiroso) | mitigate | capturar `{ error }`, ramificar 23503 y NO mutar el estado cuando el FK bloquea; el toast refleja la realidad |
| T-11-09 | Tampering | comportamiento de borrado (FK 23503) | accept | plan solo-copy + fix de UI; el FK sigue protegiendo el historial de Finanzas; el soft-disable es la vía correcta y no se toca |
</threat_model>

<verification>
- `npx tsc --noEmit` limpio.
- `npm test` verde (ningún comportamiento de borrado cambia).
- Verificación manual del copy en los 4 borrados (servicio/sede/cancha/profesional) y de que `deleteProfessional` no muta la lista cuando el FK bloquea.
</verification>

<success_criteria>
- Los toasts de borrado bloqueado aclaran pasados+cancelados y ofrecen desactivar o borrar los turnos (D-04).
- `deleteProfessional` deja de mentir "eliminado" cuando el FK bloquea, sin cambiar el comportamiento del borrado.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-02-SUMMARY.md` when done
</output>
