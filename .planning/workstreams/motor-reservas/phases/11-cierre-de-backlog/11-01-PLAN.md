---
phase: 11-cierre-de-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/abonos/abonos-client.tsx
  - app/(dashboard)/clients/clients-client.tsx
  - app/cancelar/[token]/cancel-client.tsx
  - app/abono/cancelar/[token]/abono-cancel-client.tsx
  - .impeccable/hook.cache.json
autonomous: true
requirements: [POLISH-01, POLISH-02, POLISH-03]
must_haves:
  truths:
    - "En el tab Archivados de Abonos, una serie cancelada y una completada se distinguen a simple vista sin abrir el detalle (chip semántico por estado) — SC1 (D-01)."
    - "Clientes ya no dispara el error react-hooks/set-state-in-effect (clients-client.tsx:497); npm run lint queda limpio y la pantalla se comporta igual: búsqueda, filtros, alta y edición sin renders en cascada — SC2."
    - "Las 2 pantallas de cancelación siguen con el mismo borde lateral acentuado (border-l-4 + borderLeftColor: accent), consistentes entre sí, y NO se afloja el endurecimiento público (404 genérico, token, número informado por el servidor, noindex, contraste por luminancia) — SC3 (D-03)."
  artifacts:
    - path: "app/(dashboard)/abonos/abonos-client.tsx"
      provides: "Chip Cancelado/Completado por status en el bloque de badges de Archivados"
      contains: "Badge"
    - path: "app/(dashboard)/clients/clients-client.tsx"
      provides: "Subcomponente ClientDetail montado con key={selected.id}; sin el useEffect de reset"
      contains: "key="
    - path: "app/cancelar/[token]/cancel-client.tsx"
      provides: "Pantalla de cancelación de turno — borde verificado, diseño y endurecimiento intactos"
    - path: "app/abono/cancelar/[token]/abono-cancel-client.tsx"
      provides: "Pantalla de cancelación de abono — borde verificado, gemela consistente"
  key_links:
    - from: "app/(dashboard)/abonos/abonos-client.tsx"
      to: "AbonoRow.status"
      via: "el chip se deriva de status ('cancelled'|'completed'), sin re-interpretar isAbonoActivo"
      pattern: "status"
    - from: "app/(dashboard)/clients/clients-client.tsx"
      to: "ClientDetail"
      via: "remount por key al cambiar de cliente reproduce el reset del sub-form sin setState-en-effect"
      pattern: "key=\\{selected"
---

<objective>
Cerrar los tres pulidos de la ROADMAP (SC1/SC2/SC3), todos de presentación o higiene de render, sin tocar el motor de reservas ni el aislamiento por tenant:
- **POLISH-01 (D-01):** chip semántico por estado en el tab Archivados de Abonos para distinguir cancelada de completada de un vistazo.
- **POLISH-02:** eliminar el error `react-hooks/set-state-in-effect` de `clients-client.tsx:497` con el patrón `key` (remount), sin cambiar el comportamiento.
- **POLISH-03 (D-03):** confirmar que las 2 pantallas de cancelación coinciden en el borde acentuado (ya lo hacen) y aceptar el finding `side-tab` como intencional, SIN cambiar el diseño ni aflojar el endurecimiento público.

Purpose: son los tres pendientes chicos e independientes que quedaron del ciclo; cierran el milestone v0.25.
Output (artifacts de este plan): el chip de estado en Archivados, el fix `key` de Clientes (subcomponente `ClientDetail`), y la aceptación reviewable del finding `side-tab` en las 2 pantallas de cancelación.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: POLISH-01 — Chip Cancelado/Completado en Archivados (D-01)</name>
  <files>app/(dashboard)/abonos/abonos-client.tsx</files>
  <read_first>
    - app/(dashboard)/abonos/abonos-client.tsx — `status: 'active'|'cancelled'|'completed'` (:35), `Badge` ya importado (:22), tab `'archivados'` (:280), el `.map` de cada serie (:298-339) y el contenedor de badges `flex flex-col items-end` (:322-334) donde ya viven el Badge de conteo y el de salteadas.
    - components/ui/badge.tsx — variants: `destructive` = `bg-destructive/10 text-destructive` (muted/destructive suave), `secondary` = `bg-secondary text-secondary-foreground` (neutral). NO existe variant `success`.
    - 11-PATTERNS.md §abonos-client.tsx (Analog A/B) — punto de inserción exacto y qué NO copiar.
  </read_first>
  <action>
    Agregar un chip de estado en el contenedor de badges `flex flex-col items-end` (~:322-334), junto al Badge de conteo, dentro del `.map` de series. Derivar el chip del `status` de la serie: `'cancelled'` → Badge variant `destructive` con texto "Cancelado"; `'completed'` → Badge variant `secondary` con texto "Completado". Reusar el componente `Badge` ya importado (`:22`) y los tokens semánticos de sus variants — CERO hex sueltos, per D-01. El chip es aditivo: NO tocar `isAbonoActivo` (:82-96) ni re-interpretar la semántica de `status` (recordá: `'completed'` es flag del motor de generación, no un estado de negocio — el chip se muestra igual pero no cambia ninguna lógica). Mostrarlo en el tab Archivados (donde viven cancelled+completed). NO copiar `StatusBadge` de `appointments-client.tsx:56-62` (usa hex hardcodeados `bg-red-500/20`, viola D-01). Cambio puramente presentacional: no toca queries, ni el payload, ni el comportamiento.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q '"destructive"' "app/(dashboard)/abonos/abonos-client.tsx" && grep -q 'Cancelado' "app/(dashboard)/abonos/abonos-client.tsx" && grep -q 'Completado' "app/(dashboard)/abonos/abonos-client.tsx" && npm test</automated>
    <human-check>Abrir /abonos → tab Archivados: una serie cancelada muestra chip "Cancelado" (tono destructive suave) y una completada muestra "Completado" (neutral); se distinguen sin abrir el detalle.</human-check>
  </verify>
  <acceptance_criteria>
    - En Archivados, cada serie muestra un chip por estado con tokens semánticos (destructive/secondary), sin hex.
    - Una serie cancelada y una completada se distinguen a simple vista (SC1).
    - `isAbonoActivo` y la semántica de `status` quedan intactas; las suites de abonos siguen verdes.
    - `npx tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>En Archivados se distingue cancelada de completada con un chip semántico (destructive/secondary, sin hex); `isAbonoActivo`/`status` intactos; tsc y suites verdes (SC1).</done>
</task>

<task type="auto">
  <name>Task 2: POLISH-02 — Eliminar set-state-in-effect en Clientes con patrón key (remount)</name>
  <files>app/(dashboard)/clients/clients-client.tsx</files>
  <read_first>
    - app/(dashboard)/clients/clients-client.tsx — el derivado en render `const selected = clients.find(...)` (:467); el `useEffect` ofensor (:494-508) que en `:497` dispara el ERROR `react-hooks/set-state-in-effect`; el `// eslint-disable-line` de `:508` que silencia OTRA regla (`exhaustive-deps`), no ésta; los estados a mover: `editMode` (:190), `editForm` (:191), `notes` (:192), `historyExpanded` (:198); los callers que editan después: autosave `handleNotesChange` (:511-519), edición (:521-540), `setEditForm(f=>...)` (:861-873); el warning `TrendingUp` sin usar (:24).
    - 11-RESEARCH.md §POLISH-02 — el error real (verificado con npx eslint), por qué "derivar en render" NO sirve (notes/editForm son estado editable), y el patrón `key` como recomendación HIGH.
    - 11-PATTERNS.md §clients-client.tsx — código a eliminar + pattern a aplicar + estados a mover.
  </read_first>
  <action>
    Extraer el panel de detalle/edición del cliente a un subcomponente co-ubicado `ClientDetail` (mismo archivo o adyacente, siguiendo la convención `*-client.tsx` del repo) y montarlo con `key={selected.id}` cuando hay `selected`. Dentro de `ClientDetail`, inicializar los `useState` desde la prop `client` (notes de `client.notes || ''`, `editMode`/`historyExpanded` en `false`, `editForm` desde los campos de `client`), de modo que al cambiar de cliente el remount por `key` reproduzca EXACTO el reset actual — sin `useEffect` y sin renders en cascada. Borrar el `useEffect` de reset (:494-508) COMPLETO junto con su `// eslint-disable-line`. Mover al subcomponente los estados `editMode/editForm/notes/historyExpanded` y los callers que los editan (autosave de notes con debounce, edición, `setEditForm(f=>...)`), preservando el comportamiento tal cual (búsqueda, filtros y alta siguen en el componente padre y NO cambian). Además, sacar el import `TrendingUp` sin usar (:24) para dejar el archivo 100% limpio de lint. NO usar "derivar en render" para notes/editForm (son estado editable). NO tocar deps ni el disable de exhaustive-deps como atajo — no apagan el error real.
  </action>
  <verify>
    <automated>npx eslint "app/(dashboard)/clients/clients-client.tsx" && npm run lint && npx tsc --noEmit</automated>
    <human-check>Abrir /clientes: buscar, filtrar, dar de alta un cliente, seleccionar otro y editar notas/campos — comportamiento idéntico a hoy; al cambiar de cliente el form de detalle se resetea; sin parpadeos/renders en cascada.</human-check>
  </verify>
  <acceptance_criteria>
    - `npx eslint "app/(dashboard)/clients/clients-client.tsx"` sin errores (el `set-state-in-effect` de :497 desaparece) y `npm run lint` limpio (incluye el warning de `TrendingUp`).
    - El `useEffect` de reset (:494-508) y su `eslint-disable-line` ya no existen.
    - Búsqueda, filtros, alta y edición se comportan idénticos a hoy; el reset del sub-form al cambiar de cliente ocurre por remount (`key`).
    - `npx tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>`eslint` de clients-client.tsx sin errores (se fue el set-state-in-effect de :497) y `npm run lint` limpio; el useEffect de reset ya no existe; comportamiento idéntico vía remount por `key` (SC2).</done>
</task>

<task type="auto">
  <name>Task 3: POLISH-03 — Verificar consistencia del borde y aceptar side-tab como intencional (D-03)</name>
  <files>app/cancelar/[token]/cancel-client.tsx, app/abono/cancelar/[token]/abono-cancel-client.tsx, .impeccable/hook.cache.json</files>
  <read_first>
    - app/cancelar/[token]/cancel-client.tsx — el borde acentuado (:85): `rounded-md border border-border border-l-4 p-4 space-y-1 text-sm mb-5` + `style={{ borderLeftColor: accent }}`.
    - app/abono/cancelar/[token]/abono-cancel-client.tsx — el borde gemelo (:136), byte-idéntico al anterior.
    - 11-RESEARCH.md §POLISH-03 — las 2 pantallas YA coinciden en el borde; la única divergencia (logo-fallback `text-white` vs `accentText`) está FUERA de scope (contraste, no borde); mecánica de aceptación del finding `side-tab` por la vía reviewable de impeccable; y la lista de endurecimiento que NO se toca.
    - 11-PATTERNS.md §cancel-client + abono-cancel-client — acción verify-only.
  </read_first>
  <action>
    Verificación + aceptación reviewable, SIN cambiar el diseño ni el endurecimiento:
    1) Confirmar que el `className` del borde acentuado es byte-idéntico en las 2 pantallas (`rounded-md border border-border border-l-4 p-4 space-y-1 text-sm mb-5` + `borderLeftColor: accent`). Ya coinciden — dejarlas tal cual; NO editar el borde (D-03: es el patrón de marca app-wide).
    2) Registrar el finding `side-tab` del hook de diseño como INTENCIONAL usando la skill `impeccable` en modo reviewable, acotada EXCLUSIVAMENTE a los 2 paths de cancelación, con la justificación de marca (border-l-4 + accent es el patrón app-wide). Invocar la skill `impeccable` para que persista la aceptación en su config (`.impeccable/`; el path/mecanismo exacto lo determina la skill). PROHIBIDO: `eslint-disable` a lo bruto, borrar el borde, o tocar reglas globales.
    3) NO tocar el endurecimiento público de esas pantallas: 404 genérico, token no adivinable, número informado por el servidor, `noindex`, contraste por luminancia (`accentText`, IN-05). La divergencia del logo-fallback (`text-white` vs `accentText`) se documenta como observación fuera de scope — NO se arregla acá.
    Si al ejecutar la skill hace falta un marcador de intención en código, limitarse a un comentario (no cambia comportamiento); nunca modificar clases del borde ni el flujo de validación del token.
  </action>
  <verify>
    <automated>test "$(grep -c 'border-l-4 p-4 space-y-1 text-sm mb-5' "app/cancelar/[token]/cancel-client.tsx")" = "$(grep -c 'border-l-4 p-4 space-y-1 text-sm mb-5' "app/abono/cancelar/[token]/abono-cancel-client.tsx")" && grep -q 'borderLeftColor' "app/cancelar/[token]/cancel-client.tsx" && grep -q 'borderLeftColor' "app/abono/cancelar/[token]/abono-cancel-client.tsx" && npx tsc --noEmit</automated>
    <human-check>Abrir /cancelar/[token] y /abono/cancelar/[token] con un token válido de prueba: el borde lateral acentuado se ve igual en ambas; nada del contenido cambia; el hook de diseño ya no marca el side-tab como pendiente.</human-check>
  </verify>
  <acceptance_criteria>
    - Las 2 pantallas conservan el mismo borde acentuado (verificado por diff/grep del className); no se editó el diseño.
    - El finding `side-tab` queda aceptado como intencional por la vía reviewable de impeccable acotada a esos 2 archivos (sin eslint-disable global, sin borrar el borde).
    - Ninguna propiedad de endurecimiento (404/token/número del servidor/noindex/contraste) fue tocada.
    - `npx tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>Las 2 pantallas conservan el mismo borde acentuado (verificado por diff/grep); el finding `side-tab` queda aceptado por la vía reviewable de impeccable; el endurecimiento público intacto (SC3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon → pantallas públicas de cancelación (`/cancelar/[token]`, `/abono/cancelar/[token]`) | superficie pública y anónima, ya endurecida; POLISH-03 no debe aflojarla |
| owner (sesión) → dashboard (Abonos/Clientes) | render autenticado por RLS; POLISH-01/02 son solo presentación/higiene de render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | pantallas públicas de cancelación (POLISH-03) | mitigate | plan verify-only: NO se tocan 404 genérico/token/número del servidor/noindex/contraste; se verifica por diff que solo cambia la config de impeccable |
| T-11-02 | Tampering | chip de estado en Archivados (POLISH-01) | accept | presentacional puro sobre `status` que ya llega en el payload; no re-interpreta `isAbonoActivo`; sin superficie de seguridad |
| T-11-08 | Tampering | refactor de Clientes (POLISH-02) | accept | remount por `key` preserva comportamiento; sin cambios de red/DB; suites + eslint como guarda |
</threat_model>

<verification>
- `npm run lint` limpio (POLISH-02: sin `set-state-in-effect`, sin warning de `TrendingUp`).
- `npx tsc --noEmit` limpio.
- `npm test` verde (guarda de regresión de abonos/clientes; ningún comportamiento cambia).
- Verificación visual de los 3 ítems (chips en Archivados, Clientes sin renders en cascada, borde gemelo consistente).
</verification>

<success_criteria>
- SC1: en Archivados se distingue cancelada de completada sin abrir el detalle (chip semántico).
- SC2: Clientes deja de disparar el error de eslint y se comporta igual (búsqueda/filtros/alta/edición, sin cascada).
- SC3: las 2 pantallas de cancelación quedan consistentes en el borde acentuado, sin aflojar el endurecimiento.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-01-SUMMARY.md` when done
</output>
