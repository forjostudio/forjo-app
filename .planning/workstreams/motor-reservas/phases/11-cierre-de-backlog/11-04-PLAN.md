---
phase: 11-cierre-de-backlog
plan: 04
type: execute
wave: 2
depends_on: ["11-02", "11-03"]
files_modified:
  - lib/booking-selector.ts
  - test/booking-selector.test.ts
  - app/[slug]/booking-client.tsx
  - app/(dashboard)/settings/settings-client.tsx
  - app/(dashboard)/settings/page.tsx
autonomous: true
requirements: [EXTRA-B]
must_haves:
  truths:
    - "Con public_selector_default = 'any' (default) el paso 2 del booking es byte-idéntico a hoy: la tarjeta 'Cualquiera' va arriba de la lista de profesionales (D-06, cero regresión)."
    - "Con 'choose', la tarjeta 'Cualquiera' se muestra DEBAJO de la lista de profesionales (profesionales primero); sigue mostrándose (ASIGN-01) y el gate ≥2 capaces sigue intacto (D-07)."
    - "El dueño cambia el setting desde Ajustes (control de 2 opciones) y persiste vía supabase.from('businesses').update({ public_selector_default }).eq('id', business.id), owner-only por RLS (D-05)."
    - "EXTRA-B es solo orden de UI: NO toca book_slot_atomic, availability, create ni el contrato; el servidor sigue siendo la autoridad de la asignación (D-08)."
    - "El helper anyCardPlacement es una función pura, testeada sin DOM: 'choose'→'last', cualquier otro valor (incl. 'any'/null/undefined)→'first'."
  artifacts:
    - path: "lib/booking-selector.ts"
      provides: "Helper puro anyCardPlacement(setting) -> 'first'|'last' (orden de la tarjeta Cualquiera)"
      exports: ["anyCardPlacement"]
    - path: "test/booking-selector.test.ts"
      provides: "Unit test del helper sin DOM (any/choose/null/undefined/garbage)"
    - path: "app/[slug]/booking-client.tsx"
      provides: "Orden de la tarjeta 'Cualquiera' en el paso 2 según business.public_selector_default"
      contains: "anyCardPlacement"
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "Toggle (2 opciones) que persiste public_selector_default owner-only"
      contains: "public_selector_default"
    - path: "app/(dashboard)/settings/page.tsx"
      provides: "public_selector_default en el read-path del dashboard que alimenta el toggle"
      contains: "public_selector_default"
  key_links:
    - from: "app/[slug]/booking-client.tsx (paso 2, showAny)"
      to: "lib/booking-selector.ts (anyCardPlacement)"
      via: "el orden de la tarjeta 'Cualquiera' vs capaces.map se decide con el helper sobre business.public_selector_default"
      pattern: "anyCardPlacement"
    - from: "app/(dashboard)/settings/settings-client.tsx (toggle)"
      to: "businesses.public_selector_default"
      via: "supabase.from('businesses').update({ public_selector_default }).eq('id', business.id) — RLS owner-only"
      pattern: "public_selector_default"
---

<objective>
**EXTRA-B — wiring + toggle (D-05/D-06/D-07/D-08, Opción A confirmada):** manifestar el setting `public_selector_default` como el **orden/prominencia de la tarjeta "Cualquiera"** en el paso 2 de la reserva pública (única palanca observable — el paso 2 siempre exige un tap, así que sólo cambiar el `useState` inicial sería inerte), y dar al dueño el **toggle en Ajustes** para elegirlo.

- `'any'` (default) → "Cualquiera" arriba (byte-idéntico a hoy, D-06).
- `'choose'` → "Cualquiera" debajo de la lista de profesionales; sigue visible con ≥2 capaces (D-07, gate intacto).
- Es **solo orden de UI**: NO toca el motor (D-08). El servidor sigue siendo la autoridad de la asignación.

Depende del Plan 11-03 (columna 061 aplicada en local + tipo + read-path público) y va después del Plan 11-02 (ambos tocan `settings-client.tsx`).

Purpose: cerrar la decisión abierta de Phase 10 (default del selector) como un setting real por negocio, sin regresión.
Output (artifacts de este plan): la **función pura de orden del selector** (`anyCardPlacement`) + su test, el orden condicional en el paso 2, y el toggle en Ajustes con su read-path.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper puro anyCardPlacement + unit test (sin DOM)</name>
  <files>lib/booking-selector.ts, test/booking-selector.test.ts</files>
  <read_first>
    - 11-RESEARCH.md §EXTRA-B (⚠ la migración es mecánica, la semántica NO; Opción A) + §Validation Architecture / Wave 0 Gaps (extraer función pura para unit test sin DOM).
    - lib/staff-services.ts — estilo de helper puro del repo (named export, sin dependencias de framework) y su test asociado (test/*), como molde de forma y de test.
  </read_first>
  <behavior>
    - anyCardPlacement('choose') → 'last'
    - anyCardPlacement('any') → 'first'
    - anyCardPlacement(null) → 'first' (default/legacy safety)
    - anyCardPlacement(undefined) → 'first'
    - anyCardPlacement('cualquier_otro') → 'first' (fail-safe: solo 'choose' cambia el orden)
  </behavior>
  <action>
    Crear `lib/booking-selector.ts` con un tipo exportado para el enum (`'any' | 'choose'`) y una función pura exportada `anyCardPlacement(setting: string | null | undefined): 'first' | 'last'` que devuelve `'last'` únicamente cuando `setting === 'choose'`, y `'first'` en cualquier otro caso (incluye `'any'`, `null`, `undefined` y valores inesperados — fail-safe hacia el comportamiento actual, D-06). Encabezar con un comentario que explique la semántica: `'first'` = tarjeta "Cualquiera" arriba (Phase 10 D-01, byte-idéntico); `'last'` = debajo de la lista de profesionales (D-07). Escribir `test/booking-selector.test.ts` (vitest) cubriendo los 5 casos del bloque behavior. Función pura, cero dependencias de React/DOM, para poder testearla barata. NO poner acá ninguna lógica del gate `showAny` (ese sigue viviendo en booking-client).
  </action>
  <verify>
    <automated>npx vitest run test/booking-selector.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `anyCardPlacement` existe como named export puro y pasa los 5 casos del test.
    - `'choose'`→`'last'`; `'any'`/`null`/`undefined`/valor inesperado→`'first'`.
    - `npx vitest run test/booking-selector.test.ts` verde; `npx tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>`anyCardPlacement` es un named export puro: 'choose'→'last', todo lo demás→'first'; los 5 casos del test pasan; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Orden de la tarjeta "Cualquiera" en el paso 2 según el setting (Opción A)</name>
  <files>app/[slug]/booking-client.tsx</files>
  <read_first>
    - app/[slug]/booking-client.tsx — `useState('none')` de `selectedPro` (:50); el gate `capaces` + `showAny = capaces.length >= 2` + `isAny` (:116-130, D-02/D-07); el paso 2 (`step === 2`, :502-555): tarjeta "Cualquiera" (:507-519), fallback sentinel `capaces.length === 0` (:520-536), y `capaces.map` de profesionales (:537-553), cada card llama `setStep(3)`.
    - lib/booking-selector.ts — el helper `anyCardPlacement` (Task 1).
    - 11-RESEARCH.md §EXTRA-B (Opción A: el setting controla el orden/prominencia, NO auto-salteo ni highlight) y §Pitfall 2 (cambiar solo el useState es inerte).
    - 11-PATTERNS.md §page.tsx + booking-client (Manifestación en el paso 2).
  </read_first>
  <action>
    En el paso 2, cuando `showAny` es true, decidir el ORDEN de la tarjeta "Cualquiera" (bloque :507-519) respecto de `capaces.map` (:537-553) con `anyCardPlacement(business.public_selector_default)`: `'first'` → tarjeta "Cualquiera" ANTES de la lista (orden actual, byte-idéntico para `'any'`, D-06); `'last'` → tarjeta "Cualquiera" DESPUÉS de la lista de profesionales (profesionales primero, D-07). En ambos casos la tarjeta "Cualquiera" se sigue mostrando (ASIGN-01) y el gate `showAny` (≥2 capaces) queda intacto. NO cambiar el `useState('none')` de la línea 50 (es inerte — el efecto visible lo da el orden, no el valor inicial). NO tocar el bloque sentinel `capaces.length === 0` (:520-536) ni `isAny`/`setStep(3)`/la señal `anyProfessional` al reservar. NO tocar canchas (`canchas-booking-client.tsx` no se toca — no tiene "Cualquiera", D-09 de Phase 10). El cambio es puramente de orden de render; el servidor sigue siendo la autoridad de la asignación (D-08).
  </action>
  <verify>
    <automated>grep -q "anyCardPlacement" "app/[slug]/booking-client.tsx" && npx vitest run test/booking-cualquiera-public.test.ts && npx tsc --noEmit</automated>
    <human-check>En un negocio con ≥2 profesionales capaces: con setting 'any', el paso 2 muestra "Cualquiera" arriba (igual que hoy); con 'choose', "Cualquiera" aparece debajo de la lista de profesionales y sigue reservable. En un negocio de canchas, el flujo no cambia.</human-check>
  </verify>
  <acceptance_criteria>
    - Con `'any'` el paso 2 es byte-idéntico a hoy (tarjeta "Cualquiera" arriba, D-06).
    - Con `'choose'` la tarjeta "Cualquiera" va debajo de la lista, sigue visible con ≥2 capaces (D-07).
    - `useState('none')` (:50), el sentinel, `isAny` y la señal `anyProfessional` no cambiaron; canchas intacto.
    - La suite `booking-cualquiera-public.test.ts` sigue verde (gate ≥2 + contrato intactos); `npx tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>El paso 2 ordena la tarjeta "Cualquiera" según `anyCardPlacement(business.public_selector_default)`: 'any' byte-idéntico (D-06), 'choose' abajo con gate ≥2 intacto (D-07); useState/sentinel/isAny/canchas sin tocar (D-08); suite verde.</done>
</task>

<task type="auto">
  <name>Task 3: Toggle en Ajustes + read-path del dashboard (persistencia owner-only)</name>
  <files>app/(dashboard)/settings/settings-client.tsx, app/(dashboard)/settings/page.tsx</files>
  <read_first>
    - app/(dashboard)/settings/settings-client.tsx — el bloque `require_deposit` (checkbox ~:1750-1756) y su persistencia `supabase.from('businesses').update({ ... }).eq('id', business.id)` (~:865) como molde de un toggle de booking + patrón de escritura owner-only.
    - app/(dashboard)/settings/page.tsx — el server component que arma `business` y se lo pasa a `SettingsClient`: verificar si el `.select()` sobre `businesses` es `*` o una lista explícita; si es explícita, agregar `public_selector_default` (mirror de `require_deposit`).
    - lib/types.ts — `public_selector_default` ya está en el tipo público (Plan 11-03); confirmar que el tipo del `business` que consume `settings-client` también lo incluye (agregarlo si es un tipo distinto).
    - 11-RESEARCH.md §Toggle en el panel + §Security Domain (V4 access control) + 11-PATTERNS.md §Persistencia de un setting de business.
  </read_first>
  <action>
    Agregar en `settings-client.tsx`, en la sección de reservas/seña (junto al bloque `require_deposit`), un control de 2 opciones (radio o segmented, porque es enum `'any'|'choose'`) para `public_selector_default`. Copy sugerido: "Cuando reservan, ¿preseleccionar 'Cualquiera'?" — "Sí (recomendado)" = `any` / "No, que elijan un profesional" = `choose`. Persistir con `supabase.from('businesses').update({ public_selector_default: value }).eq('id', business.id)` (cliente browser anon + RLS owner-only, patrón `require_deposit`) + `toast` de éxito/fallo. Leer el valor actual desde `business.public_selector_default` (default a `'any'` si viniera nullish). En `app/(dashboard)/settings/page.tsx`, si el `.select()` sobre `businesses` es una lista explícita de columnas, agregar `public_selector_default` para que el toggle refleje el valor guardado; si ya usa `*`, no hace falta. NO usar service-role ni endpoint nuevo (D: patrón existente). NO tocar el motor ni el flujo de reserva.
  </action>
  <verify>
    <automated>grep -q "public_selector_default" "app/(dashboard)/settings/settings-client.tsx" && grep -q "eq('id', business.id)" "app/(dashboard)/settings/settings-client.tsx" && npx tsc --noEmit && npm test</automated>
    <human-check>En Ajustes: el control muestra el valor guardado; cambiarlo persiste (recargar y sigue). Un negocio no puede escribir el setting de otro (RLS owner-only). El paso 2 público refleja el valor elegido.</human-check>
  </verify>
  <acceptance_criteria>
    - El toggle de 2 opciones existe en la sección de reservas/seña y refleja el valor guardado.
    - Persiste vía `supabase.from('businesses').update({ public_selector_default }).eq('id', business.id)` (owner-only, sin service-role ni endpoint nuevo).
    - El read-path del dashboard trae la columna (o usa `*`); `npx tsc --noEmit` limpio y `npm test` verde.
  </acceptance_criteria>
  <done>El toggle de 2 opciones existe en Ajustes, refleja el valor guardado y persiste owner-only vía `businesses.update().eq('id', business.id)` (sin service-role/endpoint nuevo, D-05); tsc + npm test verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (browser + RLS) → businesses.public_selector_default (UPDATE) | escritura autenticada del setting; RLS owner-only + `.eq('id', business.id)` |
| anon → paso 2 del booking público | el orden de la tarjeta "Cualquiera" es presentación; la asignación la resuelve el servidor (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Elevation / Tampering | escritura cross-tenant del setting | mitigate | browser client + RLS owner-only + `.eq('id', business.id)` (patrón `require_deposit`, ASVS V4); sin service-role |
| T-11-07 | Tampering | EXTRA-B tocando el motor / asignación forzada por el front | accept | solo orden de UI; NO toca `book_slot_atomic`/availability/create/contrato; el server sigue siendo autoridad y el gate ≥2 queda intacto (D-08) |
| T-11-11 | Tampering | valor de enum inválido escrito por el toggle | mitigate | el control emite solo `'any'|'choose'`; el CHECK de la 061 (Plan 11-03) es el fail-closed en DB |
</threat_model>

<verification>
- `npx vitest run test/booking-selector.test.ts` verde (helper puro, los 5 casos).
- `npx vitest run test/booking-cualquiera-public.test.ts` verde (gate ≥2 capaces + contrato de disponibilidad intactos tras el reorder).
- `npm test` completo verde + `npx tsc --noEmit` limpio.
- Verificación visual: 'any' preserva el paso 2 de hoy; 'choose' pone "Cualquiera" abajo; el toggle persiste owner-only; canchas intacto.
</verification>

<success_criteria>
- El setting `public_selector_default` es configurable por el dueño (toggle en Ajustes) y persiste owner-only.
- `'any'` (default) preserva el paso 2 byte-idéntico (D-06); `'choose'` reordena la tarjeta "Cualquiera" abajo con el gate ≥2 intacto (D-07).
- EXTRA-B no tocó el motor de reservas (D-08); el helper de orden es puro y testeado.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-04-SUMMARY.md` when done
</output>
