---
phase: 11-cierre-de-backlog
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/061_public_selector_default.sql
  - supabase/schema.sql
  - lib/types.ts
  - app/[slug]/page.tsx
autonomous: true
requirements: [EXTRA-B]
user_setup:
  - service: supabase-prod
    why: "La migración 061 se aplica A MANO al Supabase de prod, coordinada con el deploy (paso de deploy, NO parte de esta fase); en local se valida con supabase db reset."
must_haves:
  truths:
    - "La migración 061 agrega businesses.public_selector_default text NOT NULL DEFAULT 'any' con CHECK IN ('any','choose'), idempotente (ADD COLUMN IF NOT EXISTS + constraint vía pg_constraint) — molde de abono_window_weeks."
    - "supabase db reset local replaya 001→061 limpio y toda fila existente lee public_selector_default = 'any' (default = comportamiento de Phase 10, cero regresión, D-06)."
    - "public_selector_default viaja server-side por el .select() de app/[slug]/page.tsx y por el tipo PublicBusiness, sin abrir tablas puente a anon ni tocar el contrato de disponibilidad {ok,busy,full} (D-08)."
  artifacts:
    - path: "supabase/migrations/061_public_selector_default.sql"
      provides: "Columna public_selector_default + CHECK enum, idempotente"
      contains: "public_selector_default"
    - path: "supabase/schema.sql"
      provides: "Columna + constraint reflejados quirúrgicamente en el bloque businesses"
      contains: "public_selector_default"
    - path: "lib/types.ts"
      provides: "Campo public_selector_default en el tipo PublicBusiness (enum 'any'|'choose')"
      contains: "public_selector_default"
    - path: "app/[slug]/page.tsx"
      provides: "public_selector_default agregado al .select() de businesses (read-path público)"
      contains: "public_selector_default"
  key_links:
    - from: "app/[slug]/page.tsx (.select businesses)"
      to: "BookingClient (prop business)"
      via: "la columna llega gratis como parte de business una vez en el select + el tipo PublicBusiness"
      pattern: "public_selector_default"
---

<objective>
**EXTRA-B — plomería (D-05/D-06/D-08):** crear el setting `public_selector_default` como **columna de primera clase** en `businesses` (migración **061**, idempotente, molde `abono_window_weeks`), aplicarlo local con `supabase db reset`, y cablear el **read-path público** para que la columna viaje server-side hasta `BookingClient` — sin abrir superficie a `anon` ni tocar el motor de reservas.

Este plan deja el terreno listo; el **wiring del paso 2 + el toggle en Ajustes** van en el Plan 11-04 (Wave 2, depende de éste).

Purpose: el default del selector público ("Cualquiera" vs "elegí") tiene que ser configurable por negocio, con `'any'` como default para cero regresión (D-06).
Output (artifacts de este plan): migración 061 + columna `public_selector_default`, `schema.sql` actualizado, el campo en `PublicBusiness`, y el read-path en `app/[slug]/page.tsx`.

**Nota de deploy (NO es una tarea de esta fase):** la 061 se aplica **A MANO** al Supabase de prod coordinada con el deploy (+ `NOTIFY pgrst, 'reload schema'`). La última migración en prod = **060**. NUNCA por el flujo GSD.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-PATTERNS.md
@supabase/migrations/055_abono_window_bounds.sql
@supabase/migrations/060_public_professionals_exclude_canchas.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 061 + schema.sql quirúrgico + read-path (tipo + select)</name>
  <files>supabase/migrations/061_public_selector_default.sql, supabase/schema.sql, lib/types.ts, app/[slug]/page.tsx</files>
  <read_first>
    - supabase/schema.sql — el bloque de la tabla `businesses` y sus columnas de settings con DEFAULT (`require_deposit` bool DEFAULT false, `default_slot_duration` int DEFAULT 60, `max_advance_days` int DEFAULT 30, `abono_window_weeks` int DEFAULT 8 + CHECK vía pg_constraint). Ese CHECK es el molde de idempotencia.
    - supabase/migrations/055_abono_window_bounds.sql y 060_public_professionals_exclude_canchas.sql — estilo de migración numerada idempotente del repo.
    - app/[slug]/page.tsx — el `.select('id, owner_id, ..., max_advance_days, max_advance_date')` sobre `businesses` (:63) y el `<BookingClient business={business as unknown as PublicBusiness} ... />` (:153-161): el business completo ya viaja como prop.
    - lib/types.ts — el tipo `PublicBusiness` (dónde agregar el campo, mirando cómo están tipadas `require_deposit`/`max_advance_days`).
    - 11-RESEARCH.md §EXTRA-B (Migración 061 + "Superficie pública: no exponer de más") y 11-PATTERNS.md §migración 061 + §page.tsx/booking-client.
  </read_first>
  <action>
    1) Crear `supabase/migrations/061_public_selector_default.sql`: `ALTER TABLE public.businesses ADD COLUMN IF NOT EXISTS public_selector_default text NOT NULL DEFAULT 'any';` seguido de un bloque `DO $$ ... END$$` que agrega el CONSTRAINT `businesses_public_selector_default_chk CHECK (public_selector_default IN ('any','choose'))` solo si no existe (consultando `pg_constraint` por `conname`), igual que el molde de `abono_window_weeks`. Cerrar con `NOTIFY pgrst, 'reload schema';`. Encabezar con un comentario explicando: `'any'` = default (comportamiento de Phase 10 D-01, cero regresión); `'choose'` = sin preselección, "Cualquiera" sigue disponible con ≥2 capaces (D-07). `NOT NULL DEFAULT 'any'` ⇒ todas las filas existentes y nuevas nacen en `'any'` (D-06).
    2) Editar `supabase/schema.sql` **quirúrgicamente** (NO por dump — el CLI reordena el archivo): agregar la columna `public_selector_default` y su CONSTRAINT dentro del bloque de la tabla `businesses`, junto a las otras columnas de settings.
    3) Agregar `public_selector_default: 'any' | 'choose'` al tipo `PublicBusiness` en `lib/types.ts` (mismo estilo que `require_deposit`/`max_advance_days`).
    4) Agregar `public_selector_default` al string del `.select(...)` sobre `businesses` en `app/[slug]/page.tsx:63`. NO tocar `public_professionals`/`public_professional_services` ni el contrato de disponibilidad `{ ok, busy, full }` (D-08). El valor llega gratis como parte de `business` a `BookingClient`; este plan NO consume la columna en el render todavía (eso es el Plan 11-04).
  </action>
  <verify>
    <automated>test -f supabase/migrations/061_public_selector_default.sql && grep -q "IN ('any', 'choose')" supabase/migrations/061_public_selector_default.sql && grep -q "public_selector_default" supabase/schema.sql && grep -q "public_selector_default" lib/types.ts && grep -q "public_selector_default" "app/[slug]/page.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - La 061 existe, es idempotente (`ADD COLUMN IF NOT EXISTS` + CHECK vía `pg_constraint`) y termina con `NOTIFY pgrst, 'reload schema'`.
    - `schema.sql` refleja la columna + constraint en el bloque `businesses` (edición quirúrgica, no dump).
    - `PublicBusiness` incluye el campo y `app/[slug]/page.tsx` lo trae en el `.select()`.
    - `npx tsc --noEmit` limpio. No se tocó ninguna vista pública ni el contrato de disponibilidad.
  </acceptance_criteria>
  <done>La 061 existe (idempotente, CHECK enum, NOTIFY pgrst); schema.sql refleja columna+constraint; `PublicBusiness` y el `.select()` público traen la columna; tsc limpio; sin superficie nueva a anon (D-08).</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar la 061 en local — supabase db reset + verificación</name>
  <files>supabase/migrations/061_public_selector_default.sql, supabase/schema.sql</files>
  <read_first>
    - MEMORY [Infra de testing roadmap] — baseline local replayable con `supabase db reset` (PG17 local; analytics/studio/storage off en Windows).
    - 11-RESEARCH.md §Migración 061 (Validación local) + §Runtime State Inventory (Stored data: filas existentes ← DEFAULT 'any' automático, sin backfill).
  </read_first>
  <action>
    **[BLOCKING] — correr DESPUÉS de escribir la 061 (Task 1) y ANTES del wiring/tests de EXTRA-B (Plan 11-04).** Ejecutar `npx supabase db reset` en local para replayar 001→061 en PG17. Verificar que: (a) el reset corre limpio (la 061 aparece aplicada); (b) la columna existe con `NOT NULL DEFAULT 'any'` y el CHECK `IN ('any','choose')` está presente (inspeccionar `\d businesses` o un `SELECT` sobre `information_schema`/`pg_constraint`); (c) el negocio seedeado (seed.sql) lee `public_selector_default = 'any'` (default, D-06). Si el reset falla, corregir la 061 y volver a correr — NO avanzar al Plan 11-04 hasta que el reset esté limpio. Este es el gate que garantiza que la columna existe antes de que el Plan 11-04 la consuma y la testee. NO aplicar a prod (eso es paso manual de deploy).
  </action>
  <verify>
    <automated>npx supabase db reset && npx supabase migration list --local</automated>
    <human-check>En la salida del reset, la 061 figura aplicada; `SELECT public_selector_default FROM businesses;` devuelve 'any' para las filas seedeadas; el CHECK rechaza un valor fuera de ('any','choose').</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` replaya 001→061 sin error; la 061 aparece en `migration list --local`.
    - La columna existe con `NOT NULL DEFAULT 'any'` y el CHECK `IN ('any','choose')`.
    - Las filas existentes/seedeadas leen `'any'` (cero regresión, D-06).
    - Gate cumplido: la 061 está viva en local antes de que el Plan 11-04 consuma/teste la columna.
  </acceptance_criteria>
  <done>[BLOCKING] `supabase db reset` replaya 001→061 limpio; columna NOT NULL DEFAULT 'any' + CHECK presentes; filas seedeadas leen 'any' (D-06); gate cumplido para el Plan 11-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon → read-path público del slug (`app/[slug]/page.tsx` .select businesses) | server-side, tenant resuelto por slug; la columna nueva es un flag de presentación del propio negocio |
| DDL (migración 061) → tabla `businesses` | cambio de esquema idempotente, aplicado a mano a prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | valor de `public_selector_default` fuera del enum | mitigate | CHECK `IN ('any','choose')` en la 061 (fail-closed a nivel DB, ASVS V5) |
| T-11-05 | Information Disclosure | la columna nueva llegando al público | mitigate | viaja solo por el `.select()` server-side del slug ya existente; NO abre `public_professionals`/tabla puente a `anon`; NO toca el contrato `{ok,busy,full}` (D-08, ASVS V1) |
| T-11-10 | Tampering | migración no idempotente / regresión al re-aplicar | accept | `ADD COLUMN IF NOT EXISTS` + CHECK vía `pg_constraint`; `DEFAULT 'any'` preserva Phase 10 en todas las filas (D-06) |
</threat_model>

<verification>
- `npx supabase db reset` limpio con la 061 aplicada (Task 2, [BLOCKING]).
- Columna `NOT NULL DEFAULT 'any'` + CHECK `IN ('any','choose')` presentes en local.
- `npx tsc --noEmit` limpio con el tipo + read-path.
- No se tocó ninguna vista pública ni el contrato de disponibilidad (D-08).
</verification>

<success_criteria>
- La columna `public_selector_default` existe (migr. 061), con default `'any'` (cero regresión) y CHECK del enum.
- El read-path público la transporta hasta `BookingClient` sin exponer superficie nueva a `anon`.
- La 061 está aplicada en local y lista para que el Plan 11-04 la consuma.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/11-cierre-de-backlog/11-03-SUMMARY.md` when done
</output>
