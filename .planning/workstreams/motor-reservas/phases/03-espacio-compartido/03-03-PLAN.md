---
phase: 03-espacio-compartido
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - lib/verticals.ts
  - lib/use-terminology.tsx
  - app/(dashboard)/settings/page.tsx
  - app/(dashboard)/settings/settings-client.tsx
autonomous: false
requirements: [ESPACIO-01]
user_setup: []

must_haves:
  truths:
    - "El dueño puede crear espacios físicos (nombre) y mapear cada agenda (professional) a uno o varios espacios desde el editor de settings existente (D-04)"
    - "El alta de espacios y el mapeo escriben `spaces` / `agenda_spaces` por el browser client con RLS (sin pasar business_id falsificable)"
    - "`settings/page.tsx` carga `spaces` y `agenda_spaces` por `.eq('business_id', business.id)` y los pasa como props iniciales"
    - "`VerticalTerminology` gana `resource`/`resources` y el rubro 'Cancha de fútbol' resuelve a 'Cancha'/'Canchas' sin romper los otros verticales"
  prohibitions:
    - "NO se crea una pantalla/sección nueva dedicada 'Espacios/Canchas': la UI vive dentro del editor de settings existente (D-04)"
    - "NO se crea un `VerticalKey` nuevo ni menú/types propios: la terminología es label-only (D-05)"
    - "NO se agregan dependencias npm: la UI reusa vaul/shadcn/@/components/ui ya presentes"
    - "El override de terminología por type NO altera la terminología de salud/belleza ni del resto de general"
  artifacts:
    - path: "lib/verticals.ts"
      provides: "resource/resources en VerticalTerminology + override por type 'Cancha de fútbol' → Cancha"
      contains: "resource"
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "CRUD de spaces + mapeo agenda→espacios reusando el patrón de professionals"
      contains: "agenda_spaces"
  key_links:
    - from: "app/(dashboard)/settings/page.tsx"
      to: "spaces / agenda_spaces"
      via: "Promise.all carga spaces y agenda_spaces por business_id y los pasa a SettingsClient"
      pattern: "from\\('spaces'\\)|from\\('agenda_spaces'\\)"
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "spaces / agenda_spaces"
      via: "supabase.from('spaces').insert(...) y supabase.from('agenda_spaces').insert/delete(...)"
      pattern: "from\\('spaces'\\)|from\\('agenda_spaces'\\)"
---

<objective>
Cerrar ESPACIO-01 del lado de la configuración: la UI para que el dueño cree espacios físicos y mapee cada agenda (professional) a sus espacios, DENTRO del editor de settings existente (D-04, reusando el patrón de CRUD de professionals), y el término genérico mínimo del eje de agenda ("Recurso/Cancha", D-05, label-only) para que el rubro canchas no vea "Profesional/Equipo".

Purpose: Sin esta UI el dueño no puede modelar la F11 = {A,B,C}; el modelo de datos del Plan 01 quedaría inerte. Es la pieza de menor riesgo de la fase (no toca el core de integridad) y paraleliza con Plan 02. La terminología es label-only: cero impacto en datos.
Output: settings con alta de espacios + mapeo agenda→espacios; `resource`/`resources` en la terminología con override por type para "Cancha de fútbol".
</objective>

<artifacts_this_phase_produces>
> Inventario acumulado (este plan aporta los marcados [P03]).

- [P01] tablas spaces + agenda_spaces, book_slot_atomic extendido, tipos Space/AgendaSpace, error path slot_taken, migración 042
- [P02] disponibilidad acoplada + re-check de espacio (UX)
- [P03] `resource`/`resources` en `VerticalTerminology` (default 'Profesional'/'Equipo' por vertical)
- [P03] `TYPE_TERMINOLOGY_OVERRIDE` por type con `'Cancha de fútbol': { resource:'Cancha', resources:'Canchas' }`, aplicado en getVertical/resolveVertical tras el spread
- [P03] carga de spaces + agenda_spaces en `settings/page.tsx` → props de SettingsClient
- [P03] UI de alta de espacios + mapeo agenda→espacios en settings-client (patrón professionals)
- [P04] appointment_spaces + EXCLUDE backstop + trigger
- [P05] fixtures seedSpace/seedAgendaSpace + CONC-03
</artifacts_this_phase_produces>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-01-SUMMARY.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Terminología "Recurso/Cancha" — resource/resources + override por type (label-only)</name>
  <read_first>
    - lib/verticals.ts (archivo completo, 1-144) — `VerticalTerminology` (7-17, hoy SIN término de eje de agenda), los 3 verticales (35-93), "Cancha de fútbol" como type de general (línea 79), `getVertical` (118-121), `resolveVertical` (125-132). El archivo es framework-agnostic — mantenerlo sin React/iconos.
    - lib/use-terminology.tsx (1-29) — `useTerminology()` devuelve `terminology` tal cual; si VerticalTerminology gana campos, el provider los expone sin cambios
    - 03-RESEARCH.md §"Terminología Recurso/Cancha" (líneas 416-431) — las dos opciones (override por type recomendado, A1); §Assumptions A1
    - 03-PATTERNS.md §"lib/verticals.ts" (líneas 154-163) — override por type tras el spread
  </read_first>
  <files>lib/verticals.ts, lib/use-terminology.tsx</files>
  <action>
En `lib/verticals.ts`, agregar a `VerticalTerminology` dos campos: `resource: string` y `resources: string` (eje de la agenda: por defecto "Profesional"/"Equipo"). Agregar `resource`/`resources` a la terminología de los TRES verticales (salud, belleza, general) con default `'Profesional'`/`'Equipo'` (consistente con cómo el dashboard hoy nombra el eje). NO crear un VerticalKey nuevo ni tocar menu/types/features.

Como "Cancha de fútbol" es un `type` DENTRO del vertical `general` (línea 79) y la terminología hoy se resuelve por VerticalKey (no por type), agregar un override por type: `const TYPE_TERMINOLOGY_OVERRIDE: Record<string, Partial<VerticalTerminology>> = { 'Cancha de fútbol': { resource: 'Cancha', resources: 'Canchas' } }`. Aplicarlo en `getVertical` (y por transitividad en `resolveVertical`, que llama a getVertical) tras el spread del vertical: mergear `terminology: { ...VERTICALS[key].terminology, ...(TYPE_TERMINOLOGY_OVERRIDE[businessType] ?? {}) }`. El override solo aplica al type exacto "Cancha de fútbol"; salud/belleza y el resto de general conservan "Profesional"/"Equipo" byte-idéntico.

Atención: `resolveVertical` prioriza el `vertical` almacenado sobre el `type` (líneas 125-132). El override por type solo cae cuando se resuelve por type (getVertical). Documentar en comentario que el término "Cancha" depende del `type`, no solo del VerticalKey. `lib/use-terminology.tsx` NO requiere cambios de lógica — solo se beneficia del tipo extendido (verificar que `useTerminology()` siga compilando).
  </action>
  <verify>
    <automated>grep -cE "resource:|TYPE_TERMINOLOGY_OVERRIDE" lib/verticals.ts</automated>
  </verify>
  <acceptance_criteria>
    - `VerticalTerminology` tiene `resource` y `resources` (string); los 3 verticales los definen con default 'Profesional'/'Equipo'.
    - `TYPE_TERMINOLOGY_OVERRIDE['Cancha de fútbol']` = `{ resource: 'Cancha', resources: 'Canchas' }`, aplicado en getVertical tras el spread.
    - `getVertical('Cancha de fútbol').terminology.resource === 'Cancha'` y `getVertical('Médico').terminology.resource === 'Profesional'` (override solo afecta el type exacto).
    - `npx tsc --noEmit` pasa (use-terminology compila con el tipo extendido).
  </acceptance_criteria>
  <done>El eje de agenda se nombra "Cancha" para el rubro canchas y "Profesional" para el resto, label-only, sin VerticalKey nuevo ni impacto en datos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Carga de spaces + agenda_spaces en settings/page.tsx</name>
  <read_first>
    - app/(dashboard)/settings/page.tsx (1-41) — el `Promise.all` que carga services/professionals/locations por `.eq('business_id', business.id).order('created_at')` (25-29) y el render de `<SettingsClient initial.../>` (31-39)
    - lib/types.ts — interfaces Space/AgendaSpace (creadas en Plan 01)
    - 03-PATTERNS.md §"settings-client.tsx" (líneas 175-188) — "Carga inicial: viene como prop desde settings/page.tsx (Server Component, .eq('business_id', business.id))"
  </read_first>
  <files>app/(dashboard)/settings/page.tsx</files>
  <action>
En `app/(dashboard)/settings/page.tsx`, extender el `Promise.all` existente para cargar también `spaces` y `agenda_spaces` por tenant: `supabase.from('spaces').select('*').eq('business_id', business.id).order('created_at')` y `supabase.from('agenda_spaces').select('*').eq('business_id', business.id)`. Pasar `initialSpaces={spaces || []}` e `initialAgendaSpaces={agendaSpaces || []}` como props a `<SettingsClient>`. Usar el cliente `supabase` server (anon+RLS) ya creado — RLS garantiza el aislamiento por tenant. NO tocar la carga de services/professionals/locations ni la firma de los demás props.
  </action>
  <verify>
    <automated>grep -cE "from\('spaces'\)|from\('agenda_spaces'\)|initialSpaces|initialAgendaSpaces" "app/(dashboard)/settings/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` carga `spaces` y `agenda_spaces` dentro del `Promise.all`, ambos con `.eq('business_id', business.id)`.
    - Pasa `initialSpaces` e `initialAgendaSpaces` a `<SettingsClient>`.
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>El Server Component carga spaces + agenda_spaces por tenant y los inyecta como props iniciales del client.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: UI de alta de espacios + mapeo agenda→espacios en settings-client (patrón professionals)</name>
  <read_first>
    - app/(dashboard)/settings/settings-client.tsx:417-499 — el CRUD de professionals: estado (`professionals`/`newPro`/`savingPro`/`editingPro`, 418-424), insert `supabase.from('professionals').insert({...}).select().single()` (486-490), actualización optimista `setProfessionals(prev => ...)` (492-498), `toast.error`/`toast.success`. Es el patrón a copiar TAL CUAL.
    - lib/types.ts — Space/AgendaSpace (Plan 01)
    - 03-PATTERNS.md §"settings-client.tsx" (líneas 175-188) — estado + escritura + aislamiento por RLS + reuso de vaul/shadcn sin deps nuevas
    - 03-CONTEXT.md D-04 (línea 31) — UI dentro del editor existente, NO pantalla nueva
  </read_first>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <action>
En `app/(dashboard)/settings/settings-client.tsx`, agregar — reusando el patrón del CRUD de professionals (418-499) — la UI de espacios + mapeo, dentro del editor existente (D-04; ubicarla en/junto a la tab de Professionals/Equipo, sin crear una pantalla nueva):
- Aceptar las nuevas props `initialSpaces: Space[]` e `initialAgendaSpaces: AgendaSpace[]` (importar los tipos de `@/lib/types`).
- Estado local: `const [spaces, setSpaces] = useState<Space[]>(initialSpaces)`, `const [agendaSpaces, setAgendaSpaces] = useState<AgendaSpace[]>(initialAgendaSpaces)`, más estado de form (`newSpaceName`, `savingSpace`) análogo a `newPro`/`savingPro`.
- Alta de espacio: `supabase.from('spaces').insert({ name }).select().single()` (RLS WITH CHECK pone el business_id; NO pasar business_id falsificable — pero el insert necesita business_id porque NOT NULL, así que pasar `business_id: business.id` como en professionals línea 488, y RLS lo valida) → actualización optimista `setSpaces` + toast. Permitir borrar un espacio (`.delete().eq('id', ...)`) con la misma actualización optimista.
- Mapeo agenda→espacios: por cada professional, mostrar qué espacios ocupa (checkbox/multi-select reusando `@/components/ui`/shadcn/vaul ya presentes). Marcar/desmarcar escribe `agenda_spaces`: `supabase.from('agenda_spaces').insert({ business_id: business.id, professional_id, space_id })` al marcar y `.delete().eq('professional_id', ...).eq('space_id', ...)` al desmarcar, con actualización optimista de `agendaSpaces`. Usar el término del eje (`useTerminology().resource`) para el label si es trivial — NO bloquea si se deja "Agenda/Profesional".
- NO agregar dependencias npm. Reusar el lenguaje visual existente. Toda escritura va por el browser client con RLS (aislamiento por tenant garantizado por las policies del Plan 01).
  </action>
  <verify>
    <automated>grep -cE "from\('spaces'\)|from\('agenda_spaces'\)" "app/(dashboard)/settings/settings-client.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `settings-client.tsx` acepta `initialSpaces`/`initialAgendaSpaces` y mantiene estado local con `useState`.
    - Existe alta de espacio vía `supabase.from('spaces').insert(...)` con actualización optimista + toast, siguiendo el patrón de professionals.
    - El mapeo escribe `agenda_spaces` con insert/delete por (professional_id, space_id) y actualización optimista.
    - No hay imports de dependencias npm nuevas.
    - `npx tsc --noEmit` pasa y `npm run lint` no introduce errores nuevos.
  </acceptance_criteria>
  <done>El dueño crea espacios y mapea cada agenda a sus espacios desde settings, reusando el patrón de professionals y vaul/shadcn, escribiendo por RLS sin superficie de UI nueva.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificación visual del editor de espacios + mapeo</name>
  <what-built>
La UI de alta de espacios físicos + mapeo agenda→espacios dentro del editor de settings (D-04), reusando el patrón de professionals y vaul/shadcn, más el término "Cancha" para el rubro canchas.
  </what-built>
  <how-to-verify>
1. `npm run dev` y entrar al dashboard de un negocio de prueba (idealmente con type "Cancha de fútbol" para ver el término "Cancha"; o cualquier negocio para verificar el flujo).
2. En Settings, en la sección de agendas/equipo: crear 3 espacios (A, B, C). Confirmar que aparecen en la lista (actualización optimista) y que un refresh los conserva (persistieron en `spaces`).
3. Crear 4 agendas/professionals (cruzada A, cruzada B, cruzada C, F11) si no existen. Mapear: cruzada A→{A}, cruzada B→{B}, cruzada C→{C}, F11→{A,B,C}. Confirmar que los checkboxes reflejan el mapeo y que un refresh lo conserva (persistió en `agenda_spaces`).
4. Para un negocio "Cancha de fútbol": confirmar que el eje de la agenda se nombra "Cancha"/"Canchas" donde se usa el término; para un negocio de salud/belleza, confirmar que sigue diciendo "Profesional".
5. Confirmar que no se rompió el resto de Settings (services/professionals/locations siguen funcionando).
  </how-to-verify>
  <action>Checkpoint humano: con `npm run dev`, verificar visualmente que el alta de espacios + el mapeo agenda→espacios persiste tras refresh (escribió en spaces/agenda_spaces) y que el término del eje resuelve "Cancha" para el rubro "Cancha de fútbol" y "Profesional" para salud/belleza. NO es trabajo automatizable: requiere ojo humano sobre la UI.</action>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <verify><human-check>Alta de espacios + mapeo persisten tras refresh; término por rubro correcto; resto de Settings intacto.</human-check></verify>
  <done>El editor de espacios + mapeo funciona end-to-end y el término del eje se nombra por rubro, sin romper el resto de Settings.</done>
  <resume-signal>Escribí "approved" si el alta de espacios + mapeo persiste y el término resuelve por rubro; o describí los problemas visuales/funcionales.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño (browser client anon+RLS) → spaces / agenda_spaces | el dueño edita su propio negocio; RLS WITH CHECK impide escribir/leer datos de otro tenant |
| settings/page.tsx (server anon+RLS) → spaces / agenda_spaces | carga por business_id; RLS es la red de seguridad |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | escritura de spaces/agenda_spaces desde el client | mitigate | El browser client escribe con RLS WITH CHECK por op (Plan 01); aunque se falsifique un business_id en el payload, la policy lo rechaza. El mapeo a un professional_id/space_id de otro negocio falla por FK + RLS (Task 3). |
| T-03-10 | Information Disclosure | carga de espacios de otro tenant en page.tsx | mitigate | `.eq('business_id', business.id)` + RLS del cliente server (anon+RLS) (Task 2). |
| T-03-11 | Tampering | terminología filtrando/alterando otros verticales | accept | El override por type es label-only, no toca datos; solo afecta el string del type exacto "Cancha de fútbol" (Task 1). Riesgo nulo de seguridad. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes; la UI reusa vaul/shadcn ya presentes (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `npx tsc --noEmit` y `npm run lint` pasan sin errores nuevos.
- grep confirma carga y escritura de spaces/agenda_spaces filtradas por business_id / RLS.
- `getVertical('Cancha de fútbol').terminology.resource === 'Cancha'`; `getVertical('Médico').terminology.resource === 'Profesional'`.
- Checkpoint visual: alta de espacios + mapeo persisten; término por rubro correcto.
</verification>

<success_criteria>
- ESPACIO-01 config cubierto: el dueño modela la F11 = {A,B,C} desde settings, reusando el patrón existente sin pantalla nueva ni deps (D-04).
- El eje de agenda se nombra "Cancha" para canchas y "Profesional" para el resto (D-05, label-only).
- Aislamiento por tenant garantizado por RLS en lectura y escritura.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-03-SUMMARY.md` when done
</output>
