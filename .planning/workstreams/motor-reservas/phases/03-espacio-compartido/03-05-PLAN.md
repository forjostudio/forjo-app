---
phase: 03-espacio-compartido
plan: 05
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-04]
files_modified:
  - test/helpers/booking-fixtures.ts
  - test/concurrency.test.ts
autonomous: true
requirements: [CONC-03]
user_setup: []

must_haves:
  truths:
    - "`test/helpers/booking-fixtures.ts` exporta `seedSpace` y `seedAgendaSpace` (service-role insert, throw en error) y `SeededTenant` puede aportar una 2ª agenda hermana para CONC-03"
    - "`test/concurrency.test.ts` contiene un test CONC-03 donde dos altas EN PARALELO sobre agendas DISTINTAS que comparten un espacio resuelven exactamente 1 ok + 1 slot_taken"
    - "CONC-03 verifica el resultado contra el estado REAL de la DB (no contra los retornos del core): exactamente 1 fila ocupa el slot solapado por espacio"
    - "La suite completa (`npm run test`) queda verde con CONC-03 incluido, contra Supabase local con 042 aplicada"
  prohibitions:
    - "El test NO confía solo en los retornos del core: asierta el estado de la DB con el cliente service-role (molde occupantsAt de CONC-01)"
    - "CONC-03 NO debe ser flaky: el advisory lock por espacio del Plan 01 lo hace determinista (siempre 1 ok + 1 rechazo)"
    - "El test NO mezcla professional_id null y la sentinela entre las dos agendas (Pitfall 1); usa professional_id reales distintos"
    - "NO se rompe ningún test existente (CONC-01/02, CUPOS-02/03, booking-core)"
  artifacts:
    - path: "test/helpers/booking-fixtures.ts"
      provides: "seedSpace + seedAgendaSpace + 2ª agenda hermana en SeededTenant"
      contains: "seedAgendaSpace"
    - path: "test/concurrency.test.ts"
      provides: "test CONC-03 anti-conflicto-de-espacio concurrente"
      contains: "CONC-03"
  key_links:
    - from: "test/concurrency.test.ts"
      to: "lib/booking-core.ts"
      via: "dos createAppointmentCore en Promise.all sobre agendas que comparten espacio"
      pattern: "Promise.all"
    - from: "test/concurrency.test.ts"
      to: "test/helpers/booking-fixtures.ts"
      via: "seedSpace + seedAgendaSpace para mapear las dos agendas al mismo espacio"
      pattern: "seedAgendaSpace"
---

<objective>
Cerrar CONC-03 — la ingeniería real de la fase: el test de anti-conflicto-de-espacio concurrente. Dos reservas simultáneas sobre agendas DISTINTAS que comparten un espacio físico en el horario solapado no pueden ambas confirmar; exactamente una confirma y la otra recibe slot_taken. Más los fixtures `seedSpace`/`seedAgendaSpace` que mapean las agendas a un espacio compartido.

Purpose: La garantía atómica de exclusión por espacio vive en la DB (Plan 01 advisory lock + Plan 04 EXCLUDE backstop); este test es la ÚNICA prueba de que funciona bajo concurrencia real. Es el criterio de éxito duro de la fase, el equivalente de CONC-01 para el espacio compartido. El advisory lock lo hace determinista (no flaky).
Output: fixtures de espacio + test CONC-03, suite verde contra Supabase local con 042 aplicada.
</objective>

<artifacts_this_phase_produces>
> Inventario acumulado (este plan aporta los marcados [P05]).

- [P01] tablas spaces + agenda_spaces, book_slot_atomic extendido, tipos, error path slot_taken, migración 042
- [P02] disponibilidad acoplada + re-check de espacio (UX)
- [P03] terminología + UI de config
- [P04] appointment_spaces + EXCLUDE backstop + triggers
- [P05] helper `seedSpace(seeded, { name })` → inserta en spaces, devuelve space_id
- [P05] helper `seedAgendaSpace(seeded, { professionalId, spaceId })` → inserta en agenda_spaces
- [P05] 2ª agenda hermana disponible en el fixture (segundo professional_id) para CONC-03
- [P05] test `CONC-03` en `test/concurrency.test.ts` (anti-conflicto-de-espacio concurrente)
</artifacts_this_phase_produces>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-01-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fixtures seedSpace + seedAgendaSpace + 2ª agenda hermana</name>
  <read_first>
    - test/helpers/booking-fixtures.ts (archivo completo, 1-137) — `SeededTenant` (20-33), `seedOneTenant` (39-88, inserta business/location/service/professional con service-role + throw en error), `seedTimeBlock` (100-123, el molde EXACTO para seedSpace/seedAgendaSpace: `seeded.admin.from(...).insert(...).select('id').single()` con throw), teardown por CASCADE de business (129-136)
    - lib/types.ts — Space/AgendaSpace (Plan 01)
    - 03-PATTERNS.md §"test/helpers/booking-fixtures.ts" (líneas 203-208) — seedSpace/seedAgendaSpace molde de seedTimeBlock; 2ª professional si CONC-03 lo necesita; teardown por CASCADE ya cubre las tablas nuevas
  </read_first>
  <files>test/helpers/booking-fixtures.ts</files>
  <action>
En `test/helpers/booking-fixtures.ts`, agregar dos helpers nuevos (molde de `seedTimeBlock`, service-role `seeded.admin.from(...).insert(...).select('id').single()` con throw en error):
- `export async function seedSpace(seeded: SeededTenant, opts?: { name?: string }): Promise<string>` → `insert({ business_id: seeded.businessId, name: opts?.name ?? '__test_space' }).select('id').single()`, devuelve el space_id.
- `export async function seedAgendaSpace(seeded: SeededTenant, args: { professionalId: string; spaceId: string }): Promise<void>` → `insert({ business_id: seeded.businessId, professional_id: args.professionalId, space_id: args.spaceId })` (PK (professional_id, space_id)).

Para CONC-03 hace falta una 2ª agenda (hermana) que comparta un espacio con la 1ª. Agregar un helper `export async function seedProfessional(seeded: SeededTenant, opts?: { name?: string }): Promise<string>` (molde del insert de professional de seedOneTenant líneas 79-85) que devuelva un professional_id adicional — así el test crea la cancha-A y la F11 como dos agendas y las mapea al espacio A. (Alternativa: extender `SeededTenant` con un `professionalIdB` sembrado en seedOneTenant; elegir lo que sea menos invasivo — el helper aparte es preferible para no cambiar la firma de seedOneTenant que usan CONC-01/02.)

El teardown por CASCADE de `business` ya borra spaces/agenda_spaces/appointment_spaces (FK ON DELETE CASCADE a businesses/professionals/spaces) — NO cambiar teardownOneTenant. NO romper la firma de `seedOneTenant`/`seedTimeBlock` (los usan CONC-01/02/CUPOS).
  </action>
  <verify>
    <automated>grep -cE "export async function seedSpace|export async function seedAgendaSpace" test/helpers/booking-fixtures.ts</automated>
  </verify>
  <acceptance_criteria>
    - `seedSpace` inserta en `spaces` por service-role y devuelve el space_id (throw en error, molde seedTimeBlock).
    - `seedAgendaSpace` inserta en `agenda_spaces` (business_id, professional_id, space_id).
    - Existe forma de obtener una 2ª agenda hermana (helper `seedProfessional` o `professionalIdB` en SeededTenant) sin romper la firma de seedOneTenant.
    - `npx tsc --noEmit` pasa; los tests existentes que importan de booking-fixtures siguen compilando.
  </acceptance_criteria>
  <done>Los fixtures permiten sembrar espacios y mapear dos agendas distintas al mismo espacio para CONC-03, sin romper los fixtures existentes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Test CONC-03 — anti-conflicto-de-espacio concurrente</name>
  <read_first>
    - test/concurrency.test.ts (archivo completo, 1-227) — CONC-01 (89-110) es el molde EXACTO: `seedTimeBlock` + `baseInput()` (52-67) + dos `createAppointmentCore` en `Promise.all` (97-100) + assert `oks.length === 1` (104) + `occupantsAt` (71-80) como verificación independiente de la DB; `describe.skipIf(!hasSupabaseCreds)` (25); `DATE = '2031-03-03'` lunes (23); `afterEach` limpia appointments + time_blocks (41-46)
    - 03-RESEARCH.md §Phase Requirements / CONC-03 (línea 47) — "dos altas en paralelo sobre agendas que comparten espacio → exactamente 1 ok + 1 rechazo; verificación independiente del estado de la DB"; §Pitfall 1 (no mezclar null/sentinela — usar professional_id reales distintos)
    - 03-PATTERNS.md §"test/concurrency.test.ts" (líneas 191-199)
  </read_first>
  <files>test/concurrency.test.ts</files>
  <action>
En `test/concurrency.test.ts`, agregar un `it('CONC-03 — anti-conflicto-de-espacio: dos reservas concurrentes sobre agendas que comparten espacio, solo una confirma', ...)` dentro del mismo `describe.skipIf(!hasSupabaseCreds)`. Importar `seedSpace`/`seedAgendaSpace` (y el helper de 2ª agenda) de `./helpers/booking-fixtures`.

Setup del test (reusar el molde de CONC-01):
- Sembrar un `time_block` con `seedTimeBlock(t, { capacity: 1 })` (las canchas son capacity 1; el conflicto es por solape de espacio, no por cupo — D-03).
- Crear un espacio: `const spaceA = await seedSpace(t, { name: 'A' })`.
- Obtener una 2ª agenda (hermana): el `t.professionalId` es la agenda 1 (ej. cruzada A); crear/obtener una 2ª (ej. la F11) con el helper de 2ª agenda.
- Mapear AMBAS agendas al mismo espacio A: `seedAgendaSpace(t, { professionalId: t.professionalId, spaceId: spaceA })` y `seedAgendaSpace(t, { professionalId: <agenda2>, spaceId: spaceA })`.
- `afterEach` ya limpia appointments + time_blocks; agregar limpieza de `agenda_spaces` y `spaces` del business en `afterEach` (o sembrar/limpiar dentro del test) para no contaminar otros tests del mismo business.

Acción concurrente: dos `createAppointmentCore` en `Promise.all`, una con `professionalId: t.professionalId` y otra con `professionalId: <agenda2>`, AMBAS al MISMO `time: '09:00'` y misma duración (solapan en tiempo y comparten el espacio A). Usar `baseInput()` pero overrideando `professionalId` distinto en cada una (Pitfall 1: professional_id reales distintos, nunca null/sentinela).

Aserciones:
- `const oks = [a, b].filter(r => r.ok)` y `const rejected = [a, b].filter(r => !r.ok && r.error === 'slot_taken')` → `expect(oks.length).toBe(1)` y `expect(rejected.length).toBe(1)`.
- Verificación independiente de la DB (molde occupantsAt): contar las filas confirmed/pending_payment en el slot solapado a través de AMBAS agendas hermanas — debe ser exactamente 1 (no 2). Como `occupantsAt` filtra por slot exacto sin bucket, sirve: contar appointments del business en `09:00` con status que ocupa → exactamente 1.
- Comentar que es DETERMINISTA: el advisory lock por espacio del RPC serializa la carrera dentro de la DB (la 1ª toma el lock de A, inserta; la 2ª espera el lock, ve el solape en la agenda hermana que comparte A → slot_taken).
  </action>
  <verify>
    <automated>npm run test -- test/concurrency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test/concurrency.test.ts` contiene un test cuyo nombre incluye `CONC-03` con dos `createAppointmentCore` en `Promise.all` sobre dos professional_id distintos mapeados al mismo espacio.
    - Asierta exactamente `oks.length === 1` y `rejected.length === 1` con `error === 'slot_taken'`.
    - Verifica contra la DB que exactamente 1 fila ocupa el slot solapado (no 2) — verificación independiente del retorno del core.
    - El test corre verde contra Supabase local con 042 aplicada (o se skipea limpio sin creds vía `describe.skipIf`).
    - `npm run test` (suite completa) queda verde: CONC-01/02, CUPOS-02/03, booking-core y el nuevo CONC-03.
  </acceptance_criteria>
  <done>CONC-03 prueba determinísticamente que dos reservas concurrentes sobre agendas que comparten espacio no pueden ambas confirmar, verificado contra el estado real de la DB; la suite completa queda verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test (service-role) → DB local | el test usa el cliente service-role del fixture como el `supabase` del core; asierta la lógica de exclusión de espacio, no RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Tampering (test gap) | regresión silenciosa de la exclusión de espacio | mitigate | CONC-03 es la guarda: si la exclusión de espacio se rompiera (2 ok), el assert `occupantsAt === 1` la detecta. La suite queda roja si entra una sobre-reserva de espacio (Task 2). |
| T-03-17 | Tampering (flaky test) | test no determinista enmascarando una carrera | mitigate | El advisory lock por espacio del Plan 01 serializa en la DB → CONC-03 es determinista (siempre 1 ok + 1 rechazo), igual que CONC-01; no flaky (Task 2). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes; Vitest ^4.1.9 ya presente (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `npm run test` corre la suite completa verde, incluido CONC-03 (con creds de Supabase local) o skipeado limpio sin creds.
- grep confirma seedSpace/seedAgendaSpace y el test CONC-03 con Promise.all + assert de DB.
- Cero regresión: CONC-01/02, CUPOS-02/03, booking-core siguen verdes.
- `npx tsc --noEmit` pasa.
</verification>

<success_criteria>
- CONC-03 cubierto: dos reservas concurrentes sobre agendas que comparten espacio físico no pueden ambas confirmar en el horario solapado, probado determinísticamente contra la DB real.
- La suite Vitest extiende el molde CONC-01 con la exclusión de espacio; queda como guarda de no-regresión del milestone.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-05-SUMMARY.md` when done
</output>
