---
phase: 03-espacio-compartido
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/api/booking/availability/route.ts
  - lib/booking-core.ts
autonomous: true
requirements: [ESPACIO-02]
user_setup: []

must_haves:
  truths:
    - "`/api/booking/availability` marca como ocupado (busy) un slot de la agenda consultada si una agenda hermana (comparte ≥1 espacio) tiene un turno que solapa en tiempo"
    - "El bloqueo es bidireccional: consultar la F11 refleja turnos de las cruzadas, y consultar una cruzada refleja turnos de la F11 (cae solo de la simetría del set de espacios)"
    - "El re-check de espacio en booking-core rechaza temprano (UX) con slot_taken cuando hay solape en una agenda hermana, antes de entrar al RPC"
    - "Una agenda sin espacios mapeados no cambia su disponibilidad ni su re-check (skip total)"
  prohibitions:
    - "La respuesta de availability NO cambia de forma: sigue siendo `{ ok, busy, full }`; NO expone qué agenda hermana bloqueó ni cuántos espacios hay (D-06)"
    - "El bloqueo por espacio NO va a `full` (eso es cupo lleno); va a `busy` (solape 1-a-la-vez) — Pitfall 5"
    - "El re-check JS NO es la autoridad: la garantía vive en el RPC (Plan 01); este re-check es solo rechazo temprano de UX"
    - "Una agenda hermana grupal parcial (capacity>1, 1/N ocupado) NO desaparece el slot por el camino de cupos — el bloqueo de espacio es independiente de capacity"
  artifacts:
    - path: "app/api/booking/availability/route.ts"
      provides: "disponibilidad acoplada bidireccional por espacio compartido (siblingBusy → busy)"
      contains: "agenda_spaces"
    - path: "lib/booking-core.ts"
      provides: "re-check de espacio (UX) antes del RPC"
      contains: "agenda_spaces"
  key_links:
    - from: "app/api/booking/availability/route.ts"
      to: "agenda_spaces"
      via: "resuelve mySpaces por bucket y siblingBuckets por space compartido (.in('space_id', ...).neq('professional_id', bucket))"
      pattern: "agenda_spaces"
    - from: "lib/booking-core.ts"
      to: "agenda_spaces"
      via: "resuelve mySpaces + siblings y marca spaceClash contra los clashes ya traídos"
      pattern: "agenda_spaces"
---

<objective>
Cerrar el READ PATH de la exclusión acoplada (ESPACIO-02): que `/api/booking/availability` refleje el bloqueo cruzado bidireccional (la cancha aparece ocupada si la F11 — o cualquier agenda que comparta su espacio — tiene un turno solapado, y viceversa), manteniendo D-06 (público ve libre/ocupado, nunca el detalle). Más el re-check de espacio en `booking-core.ts` como rechazo temprano de UX (la autoridad sigue siendo el RPC del Plan 01).

Purpose: El Plan 01 garantiza que el insert no sobre-reserva un espacio (write). Este plan hace que el público NO VEA un slot como libre cuando un espacio compartido ya está tomado (read) — sino el usuario intentaría reservar y recibiría un rechazo confuso. Cierra la experiencia bidireccional de ESPACIO-02.
Output: availability con bloqueo acoplado (siblingBusy → busy) + re-check de espacio (UX) en el core.
</objective>

<artifacts_this_phase_produces>
> Inventario acumulado (este plan aporta los marcados [P02]).

- [P01] tablas spaces + agenda_spaces, book_slot_atomic extendido, tipos Space/AgendaSpace, error path slot_taken de espacio, migración 042
- [P02] disponibilidad acoplada bidireccional en `/api/booking/availability`: resolución de siblingBuckets por espacio compartido → siblingBusy agregado a `busy`
- [P02] re-check de espacio (UX) en `lib/booking-core.ts`: mySpaces + siblings + spaceClash contra los clashes ya traídos
- [P03] término resource/resources + override por type (terminología)
- [P03] UI alta de espacios + mapeo en settings-client
- [P04] appointment_spaces + EXCLUDE backstop + trigger
- [P05] fixtures seedSpace/seedAgendaSpace + CONC-03
</artifacts_this_phase_produces>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Disponibilidad acoplada bidireccional por espacio compartido (read path)</name>
  <read_first>
    - app/api/booking/availability/route.ts (archivo completo, 1-113) — el analog self: `SENTINEL` (línea 11), resolución de `bucket` (línea 80), `appts` traídos del negocio sin filtrar por bucket (37-42), filtro de holds vencidos (84), `busy` por capacity<=1 (93-95), `full` por count>=capacity (102-110), respuesta `{ ok, busy, full }` (112)
    - 03-RESEARCH.md §Pattern 4 (líneas 277-305) — el esqueleto exacto de siblingBuckets + siblingBusy + el comentario D-06 no-leak; §Pitfall 5 (no confundir bloqueo de espacio con cupo lleno → busy, no full)
    - 03-PATTERNS.md §"app/api/booking/availability/route.ts" (líneas 115-127) — punto de inserción tras resolver bucket
    - test/concurrency.test.ts:168-226 (CUPOS-02) — el test de no-leak que DEBE seguir verde: `Object.keys(body).sort() === ['busy','full','ok']` y leakKeys
  </read_first>
  <files>app/api/booking/availability/route.ts</files>
  <behavior>
    - Test 1 (bidireccional F11→cruzadas): con F11→{A,B,C}, cruzada B→{B}, y un turno en la cruzada B a las 20:00, consultar availability de la F11 incluye 20:00 en `busy`.
    - Test 2 (bidireccional cruzada→F11): con un turno en la F11 a las 20:00, consultar availability de la cruzada B incluye 20:00 en `busy`.
    - Test 3 (no-leak D-06): la respuesta sigue siendo exactamente `{ ok, busy, full }`; ninguna entrada de busy expone count/remaining/seat/capacity.
    - Test 4 (sin espacios = sin cambio): una agenda sin filas en agenda_spaces devuelve la misma disponibilidad que antes (skip total).
    - Test 5 (CUPOS-02 intacto): el test existente de no-leak grupal sigue verde.
  </behavior>
  <action>
En `app/api/booking/availability/route.ts`, tras resolver `bucket` (línea 80) y antes de construir `busy`/`full`, agregar la resolución de agendas hermanas por espacio compartido con el cliente service-role ya creado (`supabase`):
- `const { data: mySpaces } = await supabase.from('agenda_spaces').select('space_id').eq('business_id', business.id).eq('professional_id', bucket)`.
- Si `mySpaces?.length`: `const { data: sib } = await supabase.from('agenda_spaces').select('professional_id').eq('business_id', business.id).in('space_id', mySpaces.map(s => s.space_id)).neq('professional_id', bucket)` → `siblingBuckets = [...new Set((sib||[]).map(s => s.professional_id as string))]`. Si no hay espacios, `siblingBuckets = []` (skip total).
- Construir `siblingBusy`: de los `appts` ya traídos del negocio (37-42), filtrar los que caen en una agenda hermana (`siblingBuckets.includes(a.professional_id ?? SENTINEL)`), aplicar el MISMO filtro de holds vencidos que `live` (status confirmed o expires_at null o futuro), y mapear a `{ time, status, expires_at, duration_minutes }`.
- Mergear `siblingBusy` en `busy` (concatenar): el bloqueo por espacio es un solape 1-a-la-vez, va a `busy`, NUNCA a `full` (Pitfall 5 — `full` queda reservado para count>=capacity del propio bucket). Independiente de la capacity de la agenda hermana.

D-06 (LOCKED): la respuesta NO cambia de forma — sigue `return Response.json({ ok: true, busy, full }, ...)` con las mismas claves. El público no puede inferir qué agenda hermana bloqueó ni cuántos espacios hay. Resolver `agenda_spaces` por service-role (NO hay read anon — Plan 01). Comentar en español por qué siblingBusy va a busy y no a full, y por qué D-06 se preserva.
  </action>
  <verify>
    <automated>npm run test -- test/concurrency.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `route.ts` resuelve `mySpaces` y `siblingBuckets` consultando `agenda_spaces` filtrado por `.eq('business_id', business.id)` con `.in('space_id', ...).neq('professional_id', bucket)`.
    - `siblingBusy` se construye de los `appts` del negocio filtrados por `siblingBuckets` con el mismo filtro de holds vencidos, y se mergea en `busy` (no en `full`).
    - La respuesta sigue siendo `{ ok, busy, full }` (mismas 3 claves); el test CUPOS-02 (`Object.keys(body).sort() === ['busy','full','ok']`) sigue verde tras correr la suite.
    - Una agenda sin filas en agenda_spaces no agrega siblingBusy (skip).
  </acceptance_criteria>
  <done>availability refleja el bloqueo cruzado bidireccional por espacio compartido en `busy`, manteniendo el contrato `{ ok, busy, full }` de D-06; la suite de concurrencia sigue verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-check de espacio (UX) en booking-core antes del RPC</name>
  <read_first>
    - lib/booking-core.ts:106-156 — el re-check de bucket existente: `SENTINEL` (15), `bucket = proId ?? SENTINEL` (108), `clashes` traídos (113-118), `overlaps(a)` (136-140), `sameBucket` (141), predicado "ocupado de verdad" (144-146), `nowMs` (109), retorno slot_taken (155)
    - 03-RESEARCH.md §Pattern 2 (líneas 214-247) — el esqueleto exacto del re-check de espacio en JS; recalca que es solo UX, NO autoridad
    - 03-PATTERNS.md §"lib/booking-core.ts" (líneas 94-111) — punto de inserción tras sameBucket (141)
  </read_first>
  <files>lib/booking-core.ts</files>
  <action>
En `lib/booking-core.ts`, tras el `sameBucket` (línea 141) y junto al chequeo `taken`, agregar el re-check de espacio (rechazo temprano de UX; la autoridad es el RPC del Plan 01). Reusar `bucket`, `clashes`, `overlaps(a)`, `nowMs`, `SENTINEL` existentes:
- `const { data: mySpaces } = await supabase.from('agenda_spaces').select('space_id').eq('business_id', business.id).eq('professional_id', proId ?? SENTINEL)` (mismo estilo que las queries 113-118; bucketización byte-idéntica — Pitfall 1).
- Si `mySpaces && mySpaces.length > 0`: resolver `siblings` con `.in('space_id', spaceIds).neq('professional_id', proId ?? SENTINEL)`, construir `siblingBuckets = new Set(...)`, y `spaceClash = (clashes || []).some(a => siblingBuckets.has(a.professional_id ?? SENTINEL) && overlaps(a) && (a.status === 'confirmed' || a.expires_at == null || new Date(a.expires_at as string).getTime() > nowMs))`.
- Si `spaceClash`: `return { ok: false, error: 'slot_taken', status: 409 }` (reusar el código existente, NO agregar space_taken).
- Si la agenda no tiene espacios mapeados → skip total (no se hace ninguna query extra de siblings; comportamiento cupos/individual byte-idéntico).

Insertar este bloque SIN alterar la lógica capacity-aware del `taken && slotCapacity <= 1` existente (líneas 154-156): el re-check de espacio es independiente del cupo (un espacio físico no se comparte como un cupo — Pitfall 5). Comentar en español que es solo UX y que la autoridad es el RPC.
  </action>
  <verify>
    <automated>npm run test -- lib/booking-core.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `booking-core.ts` consulta `agenda_spaces` por `.eq('professional_id', proId ?? SENTINEL)` con la bucketización byte-idéntica al SENTINEL existente.
    - Resuelve siblings con `.in('space_id', ...).neq('professional_id', proId ?? SENTINEL)` y rechaza con `slot_taken` 409 cuando hay un clash solapado en una agenda hermana.
    - Una agenda sin filas en agenda_spaces no hace queries extra de siblings (skip).
    - El test existente `lib/booking-core.test.ts` sigue verde (cero regresión cupos/individual).
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>El core rechaza temprano (UX) con slot_taken cuando un espacio compartido está tomado, sin alterar el camino cupos/individual; la suite del core sigue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente público (anon) → /api/booking/availability (service-role) | el slug+date+professionalId llegan sin confianza; tenant por slug; la respuesta no debe filtrar detalle interno |
| caller → booking-core re-check | el re-check lee agenda_spaces con el cliente recibido; es UX, la autoridad es el RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | respuesta de /api/booking/availability | mitigate | La respuesta se mantiene `{ ok, busy, full }` sin conteo ni detalle de qué agenda/espacio bloqueó (D-06); el test CUPOS-02 de no-leak sigue verde (Task 1). |
| T-03-07 | Tampering | acoplamiento cross-tenant en availability | mitigate | `agenda_spaces` se consulta con `.eq('business_id', business.id)`; las agendas hermanas son del mismo negocio. Un space de otro tenant no entra al set (Task 1). |
| T-03-08 | Information Disclosure | filtración de qué agenda bloqueó vía el error | mitigate | El re-check devuelve el genérico `slot_taken` 409 sin payload de detalle; el público solo sabe "ocupado" (Task 2). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (RESEARCH §Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `npm run test` corre la suite completa verde, incluido CUPOS-02 (no-leak) y booking-core.test.ts (cero regresión).
- `npx tsc --noEmit` pasa.
- grep confirma que availability y booking-core consultan agenda_spaces filtrado por business_id y que siblingBusy/spaceClash van por solape (busy / slot_taken), no por cupo.
- La respuesta de availability conserva exactamente las claves `{ ok, busy, full }`.
</verification>

<success_criteria>
- ESPACIO-02 read-path cubierto: la disponibilidad refleja el bloqueo cruzado bidireccional por espacio compartido sin exponer detalle (D-06).
- El re-check de espacio (UX) rechaza temprano sin ser la autoridad (que sigue en el RPC).
- Cero regresión: agendas sin espacios y el camino cupos/individual no cambian; CUPOS-02 y booking-core.test.ts verdes.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/03-espacio-compartido/03-02-SUMMARY.md` when done
</output>
