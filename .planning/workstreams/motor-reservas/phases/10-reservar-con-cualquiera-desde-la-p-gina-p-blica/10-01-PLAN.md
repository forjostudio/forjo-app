---
phase: 10-reservar-con-cualquiera-desde-la-p-gina-p-blica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/059_public_professional_services.sql
  - supabase/schema.sql
  - app/api/booking/availability/route.ts
  - app/api/booking/create/route.ts
autonomous: true
requirements: [ASIGN-01, DISP-01, DISP-02, DISP-03]
must_haves:
  truths:
    - "GET /api/booking/availability?any=1&serviceId=… devuelve { ok, busy:[], full } donde full contiene solo los start-times bloqueados para TODOS los profesionales capaces (DISP-01/DISP-03)"
    - "Sin any=1, la respuesta de availability para un professionalId concreto es byte-idéntica a la de antes de la fase (DISP-02/D-08)"
    - "POST /api/booking/create con anyProfessional:true pasa autoAssign:true a createAppointmentCore; un professionalId del body nunca se usa como asignación (D-05)"
    - "La vista public_professional_services expone solo business_id/professional_id/service_id a anon; professional_services sigue sin policy anon (D-07)"
    - "Los guards plan_status (SEC-04), ventana de reserva (v0.22) y reCAPTCHA corren antes del core, sin cambios en el camino nuevo (D-10)"
  artifacts:
    - path: "supabase/migrations/059_public_professional_services.sql"
      provides: "Vista acotada anon de la tabla puente staff↔servicios"
      contains: "public_professional_services"
    - path: "app/api/booking/availability/route.ts"
      provides: "Rama de agregación across-staff gateada por any=1"
      contains: "busy: []"
    - path: "app/api/booking/create/route.ts"
      provides: "Wiring del boolean anyProfessional → autoAssign"
      contains: "anyProfessional"
  key_links:
    - from: "app/api/booking/create/route.ts"
      to: "lib/booking-core.ts"
      via: "autoAssign:true cuando anyProfessional===true"
      pattern: "autoAssign"
    - from: "app/api/booking/availability/route.ts"
      to: "lib/staff-services.ts"
      via: "professionalsForService para la regla del comodín"
      pattern: "professionalsForService"
---

<objective>
Construir la superficie de servidor de "Cualquiera": la vista acotada nueva que expone quién es capaz de qué servicio a `anon` (D-07), la rama de agregación de disponibilidad across-staff en `/api/booking/availability` (DISP-01/03) y el wiring del boolean `anyProfessional` → `autoAssign` en `/api/booking/create` (ASIGN-01/D-05). El camino específico/omitido y el gemelo canchas quedan intactos (DISP-02/D-08/D-09).

Purpose: sin esta capa "Cualquiera" no puede ni mostrar disponibilidad real (el bucket SENTINEL no agrega) ni asignar a nadie (hoy `create` no dispara `autoAssign`). El motor de asignación ya existe (Phase 9, migr. 058) — esta fase lo **expone** sin tocarlo.
Output: migr. 059 (`public_professional_services`) + edición quirúrgica de `supabase/schema.sql` + rama `any=1&serviceId` en availability + wiring `autoAssign` en create.

## Artifacts this phase produces (Plan 01)
- `supabase/migrations/059_public_professional_services.sql` — vista acotada `public_professional_services` (molde `044_public_canchas.sql`).
- Rama `any=1&serviceId` en `app/api/booking/availability/route.ts` (contrato `{ ok, busy:[], full }`, D-06).
- Flag `anyProfessional` (front→create) leído en `app/api/booking/create/route.ts` → `autoAssign`.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-PATTERNS.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 059 — vista acotada public_professional_services</name>
  <files>supabase/migrations/059_public_professional_services.sql, supabase/schema.sql</files>
  <read_first>
    - @supabase/migrations/044_public_canchas.sql — MOLDE EXACTO a copiar (owner postgres, sin security_invoker, GRANT ALL a los 3 roles, header de comentarios denso en español, bloque de "qué NO hace": apply a mano + NOTIFY pgrst + regenerar schema.sql).
    - @supabase/migrations/057_professional_services.sql — la tabla base puente (business_id/professional_id/service_id NOT NULL, RLS por op, SIN policy anon). La vista lee de acá.
    - @lib/types.ts (interface ProfessionalService, líneas ~158-162) — confirma que la forma de la vista = { business_id, professional_id, service_id }.
  </read_first>
  <action>
    Crear `supabase/migrations/059_public_professional_services.sql` copiando el molde de 044: `CREATE OR REPLACE VIEW public.public_professional_services AS SELECT business_id, professional_id, service_id FROM public.professional_services;` (sin JOIN — la puente ya tiene las 3 columnas). Owner `postgres` (definer). GRANT ALL a `anon`, `authenticated`, `service_role`. NO usar `security_invoker=true` (heredaría la RLS de `professional_services`, que no tiene policy anon → 0 filas para anon → el gating de "Cualquiera" quedaría siempre oculto; molde correcto = definer, igual que public_services/044). Escribir el header en español al estilo 044: contexto (para qué sirve — listar capaces y gatear "Cualquiera" con ≥2, D-02/D-07), regla dura (solo 3 columnas no sensibles; la tabla puente NUNCA se abre a anon), y el bloque "qué NO hace" (apply a prod a mano coordinado con el deploy + `NOTIFY pgrst, 'reload schema';` + regenerar schema.sql; NO push remoto; la única validación local es `supabase db reset`, que corre en el Plan 04). Documentar que la regla del comodín se preserva sola: un profesional sin filas simplemente no aparece en la vista y `professionalsForService` lo interpreta como capaz de todo (cero backfill). Luego editar `supabase/schema.sql` QUIRÚRGICAMENTE (no dump): insertar el bloque de la vista + los GRANT en la sección de vistas públicas, junto a `public_canchas`, sin reordenar el resto del archivo (patrón del repo, STATE Phase 06). La baseline en prod es 058; 059 es la próxima (D-09 histórico: apply manual, out of scope de este plan).
  </action>
  <acceptance_criteria>
    - `059_public_professional_services.sql` define la vista con exactamente 3 columnas (business_id, professional_id, service_id), owner postgres, GRANT ALL a los 3 roles, y NO contiene `security_invoker`.
    - El archivo es idempotente (`CREATE OR REPLACE VIEW`).
    - `supabase/schema.sql` contiene el bloque de la vista `public_professional_services` (edición quirúrgica, sin reordenar el resto).
    - La validación por `supabase db reset` está diferida al Plan 04 (BLOCKING) — este plan solo escribe la migración y el snapshot.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "public_professional_services" supabase/migrations/059_public_professional_services.sql supabase/schema.sql && ! grep -qi "security_invoker" supabase/migrations/059_public_professional_services.sql && echo OK</automated>
  </verify>
  <done>La vista acotada existe en la migración 059 y en schema.sql, con el molde definer de 044; la validación de replay queda para el Plan 04.</done>
</task>

<task type="auto">
  <name>Task 2: Rama de agregación across-staff en availability (any=1&serviceId)</name>
  <files>app/api/booking/availability/route.ts</files>
  <read_first>
    - @app/api/booking/availability/route.ts — camino de hoy COMPLETO: `SENTINEL` (línea 11), resolución de negocio por slug (28-34), query de `appts` con professional_id/duration_minutes (37-42), `dow`/`capBlocks`/`capacityFor` (53-75), bucket por professionalId (80), `siblingBusy` de espacio compartido (86-120), `busy`/`full` y el retorno `{ ok, busy, full }` con `Cache-Control: no-store` (131-156).
    - @supabase/migrations/058_professional_auto_assignment.sql — el criterio EXACTO de candidatos del RPC (candidatos = professionals `active=true` AND `service_id IS NULL` AND capacidad por comodín). Espejar ese criterio para no divergir de a quién asigna el RPC.
    - @lib/staff-services.ts — `professionalsForService(serviceId, activeProfessionals, bridge)` (helper PURO): la regla del comodín (0 filas = capaz de todo). Reusarla; NO reimplementar la regla en el endpoint.
  </read_first>
  <action>
    Insertar una RAMA NUEVA al comienzo del handler, tras validar slug/date y resolver el negocio, gateada por `const any = searchParams.get('any') === '1'` y `const serviceIdParam = searchParams.get('serviceId')`. La rama retorna ANTES de tocar el bucket específico existente, dejando el camino de hoy byte-idéntico (DISP-02/D-08). Dentro de la rama: (1) resolver la duración del servicio leyendo `services` filtrado por `business_id` (anti-tampering aunque sea read — nunca confiar en un serviceId ajeno). (2) Traer los `professionals` del negocio con `active=true` AND `service_id IS NULL` (excluir canchas, espejando el criterio del RPC 058) y la tabla puente `professional_services` del negocio; derivar los capaces con `professionalsForService(serviceIdParam, candidatos, puente)` (comodín reusado, Don't Hand-Roll). NO filtrar por sede: `public_professionals` no expone location_id y el front no manda location — divergencia aceptada y documentada (RESEARCH A3), backstopeada por el RPC que sí filtra por sede (un slot puntual raro puede fallar con slot_taken → toast normal). (3) Reusar los `appts` del negocio+fecha (ya se traen) bucketeados por `professional_id` real (NO colapsar a SENTINEL — Pitfall 1). (4) Enumerar la grilla semanal del `dow` a paso = duración (misma fórmula que el client: `for (t=openMin; t+dur<=closeMin; t+=dur)` sobre los `time_blocks` del `dow`); para cada start-time, un capaz está LIBRE si ningún turno vivo suyo solapa `[t, t+dur)` con buffer, AND su count < capacityFor(t), AND no está bloqueado por su `siblingBusy` (espacio compartido, computado por-pro). El slot es agregado-disponible si AL MENOS UN capaz está libre; si NINGUNO, el start-time va a `full` (en 'HH:MM'). (5) Devolver `Response.json({ ok: true, busy: [], full }, { headers: { 'Cache-Control': 'no-store' } })`. Regla dura D-06: `busy` SIEMPRE vacío en esta rama, `full` colapsa a booleano-por-slot; NUNCA counts, NUNCA per-pro, NUNCA qué agenda bloqueó. Concatenar los `busy` de todos los pros daría la INTERSECCIÓN (lo opuesto a la unión DISP-01) — no hacerlo. Agregar un comentario en español explicando por qué la unión se computa a nivel start-time y se devuelve en `full`.
  </action>
  <acceptance_criteria>
    - Con `any=1&serviceId=…`: la respuesta es `{ ok:true, busy:[], full:[...] }`; un start-time entra en `full` si y solo si NINGÚN profesional capaz lo tiene libre (2 pros, 1 ocupado a las 10:00 → 10:00 NO está en full; ambos ocupados → 10:00 sí está en full).
    - La rama retorna antes del bucketing existente; el camino sin `any` no cambia (DISP-02).
    - La respuesta nunca incluye conteos, professional_id, ni entradas por-pro (D-06).
    - El criterio de capaces reusa `professionalsForService` (comodín) y excluye `service_id IS NOT NULL` (canchas).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/booking-public-regression.test.ts</automated>
  </verify>
  <done>La rama `any` devuelve la unión de disponibilidad en `full` con `busy:[]`, y el path específico queda byte-idéntico (regresión verde).</done>
</task>

<task type="auto">
  <name>Task 3: Wiring del boolean anyProfessional → autoAssign en create</name>
  <files>app/api/booking/create/route.ts</files>
  <read_first>
    - @app/api/booking/create/route.ts — parseo defensivo del body (28-41), gate plan_status (73-75), backstop ventana `isDateOutOfWindow` (85-87), reCAPTCHA (96-101), derivación de service de cancha por business_id (128-146), llamada a `createAppointmentCore` (151-166).
    - @lib/booking-core.ts — la firma de `createAppointmentCore`: `autoAssign?: boolean` ya existe (Phase 9); con `autoAssign=true` traduce al UUID mágico ANY_PROFESSIONAL y saltea los re-checks JS (la autoridad es el RPC). NO editar este archivo.
  </read_first>
  <action>
    Junto al parseo defensivo del body, leer `const anyProfessional = body.anyProfessional === true` (boolean estricto — nunca interpretar un id como asignación, D-05). Pasar `autoAssign: anyProfessional` en el objeto que se le da a `createAppointmentCore`. NO tocar el orden de los guards: plan_status, ventana y reCAPTCHA siguen corriendo ANTES del core (D-10). NO tocar la derivación de service de cancha (path canchas, D-09) — `anyProfessional` no la afecta: un cliente de canchas manda `professionalId=cancha.id` y NO manda `anyProfessional`, así que recorre el camino de hoy. Documentar en un comentario en español que si un cliente forja `anyProfessional` en un negocio sin capaces, el RPC 058 hace `RAISE 'slot_taken'` → 409 (fail-safe); el server nunca acepta un `professionalId` del front como la asignación.
  </action>
  <acceptance_criteria>
    - `create/route.ts` lee `body.anyProfessional === true` y pasa `autoAssign` a `createAppointmentCore`.
    - Con `anyProfessional:true`, el turno queda con `professional_id` NO nulo (lo asigna el RPC); con el flag ausente, el comportamiento es el de hoy (professionalId:null cae al bucket SENTINEL — cero regresión single-pro, D-08).
    - El orden gate plan_status → ventana → reCAPTCHA → core no cambia (D-10).
    - Ningún `professionalId` del body se usa como asignación de "Cualquiera" (D-05).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/booking-core.test.ts && grep -n "anyProfessional" app/api/booking/create/route.ts</automated>
  </verify>
  <done>El create traduce el boolean `anyProfessional` a `autoAssign` sin reordenar guards ni tocar el path canchas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente anónimo → /api/booking/availability | input no confiable (slug/date/serviceId/any) cruza al service-role |
| cliente anónimo → /api/booking/create | input no confiable (boolean anyProfessional, professionalId) cruza al service-role |
| DB (vista definer) → RSC anon | `public_professional_services` sirve capacidad por servicio a anon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | rama `any` de availability | mitigate | Contrato `{ ok, busy:[], full }` — `full` colapsa a booleano-por-slot; nunca counts, per-pro ni la agenda interna (D-06). La unión se computa server-side y jamás se serializa por profesional. |
| T-10-02 | Information Disclosure | vista `public_professional_services` (059) | mitigate | Vista definer (owner postgres, sin security_invoker), solo 3 columnas no sensibles; `professional_services` sigue sin policy anon (057). El RSC filtra por `.eq('business_id')` al leerla (D-07). |
| T-10-03 | Tampering / Elevation of Privilege | create → asignación de "Cualquiera" | mitigate | El server lee un boolean (`anyProfessional`), nunca un id como asignación; el RPC 058 selecciona el pro bajo advisory lock y hace `RAISE 'slot_taken'` si no hay candidato. Anti-tampering por `business_id` intacto (D-05). |
| T-10-04 | Business logic bypass | guards del create en el camino nuevo | mitigate | plan_status (SEC-04), ventana de reserva (v0.22, `isDateOutOfWindow`) y reCAPTCHA corren ANTES del core, independientes de `autoAssign` (D-10). No se reordenan. |

No se instalan paquetes en esta fase (RESEARCH: sin cambios de package.json) → sin amenaza de cadena de suministro `T-10-SC`.
</threat_model>

<verification>
- `npx vitest run test/booking-public-regression.test.ts` verde (DISP-02/D-08 byte-idéntico).
- `npx vitest run test/booking-core.test.ts` verde (autoAssign en el core, sin regresión de los 4 caminos).
- `grep` confirma la vista sin `security_invoker`, `busy: []` en la rama `any`, y `anyProfessional` en el create.
- Validación de replay de la migración 059 (`supabase db reset`) diferida al Plan 04 (BLOCKING).
</verification>

<success_criteria>
- La vista acotada 059 existe (definer, 3 columnas) y `professional_services` sigue cerrada a anon.
- La rama `any` devuelve la unión de disponibilidad en `full` con `busy:[]` (DISP-01/03) y el path específico es byte-idéntico (DISP-02).
- El create dispara `autoAssign` desde el boolean `anyProfessional` sin regresionar guards ni canchas.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-01-SUMMARY.md` when done
</output>
