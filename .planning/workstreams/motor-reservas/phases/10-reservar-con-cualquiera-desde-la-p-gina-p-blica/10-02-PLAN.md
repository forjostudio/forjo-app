---
phase: 10-reservar-con-cualquiera-desde-la-p-gina-p-blica
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - app/[slug]/page.tsx
  - app/[slug]/booking-client.tsx
autonomous: true
requirements: [ASIGN-01, DISP-01, DISP-02, DISP-03]
must_haves:
  truths:
    - "Elegido el servicio, el paso 'Profesional' muestra una tarjeta 'Cualquiera' con sub-texto 'El primero disponible' arriba de la lista, SOLO cuando hay 2+ profesionales capaces del servicio (D-02/D-03)"
    - "Con 'Cualquiera' elegido, la grilla de horarios se pide con any=1&serviceId y refleja al equipo entero (libre si algún capaz libre) (DISP-01/DISP-03)"
    - "Elegido un profesional específico, la grilla es la de esa agenda — idéntica a hoy (DISP-02/D-08)"
    - "El front manda al create un boolean anyProfessional (con professionalId:null), nunca un professionalId como asignación (D-05)"
    - "canchas-booking-client.tsx no se toca; el gemelo canchas sigue igual (D-09/SC5)"
  artifacts:
    - path: "app/[slug]/page.tsx"
      provides: "Read-path de public_professional_services + prop a BookingClient (no a CanchasBookingClient)"
      contains: "public_professional_services"
    - path: "app/[slug]/booking-client.tsx"
      provides: "Tarjeta 'Cualquiera' gateada por capaces>=2 + señal any=1/anyProfessional"
      contains: "anyProfessional"
  key_links:
    - from: "app/[slug]/page.tsx"
      to: "public_professional_services (vista 059)"
      via: "supabase.from('public_professional_services').select"
      pattern: "public_professional_services"
    - from: "app/[slug]/booking-client.tsx"
      to: "lib/staff-services.ts"
      via: "professionalsForService para contar capaces (D-02)"
      pattern: "professionalsForService"
    - from: "app/[slug]/booking-client.tsx"
      to: "app/api/booking/availability/route.ts"
      via: "params any=1&serviceId cuando isAny"
      pattern: "'any'"
---

<objective>
Exponer "Cualquiera" en la reserva pública: leer la vista `public_professional_services` en `page.tsx` y pasarla a `BookingClient`; en el wizard, mostrar la tarjeta "Cualquiera" arriba de la lista de profesionales solo con 2+ capaces (D-02/D-03), filtrar la lista al set de capaces, y mandar la señal correcta (any=1 a availability, boolean `anyProfessional` a create — nunca un id, D-05). El gemelo canchas NO se toca (D-09/SC5).

Purpose: es la superficie visible de la feature. El scaffolding ya existe (`selectedPro` arranca en `'none'`); "Cualquiera" es re-significar `'none'` + gatear su visibilidad + wire-ear la señal. Depende del Plan 01 (la vista 059 y el contrato any=1/anyProfessional).
Output: read-path de la vista en `page.tsx` + tarjeta "Cualquiera" + gating + señal en `booking-client.tsx`. (La verificación humana end-to-end de la UX pública vive en el Plan 05, tras el `supabase db reset` del Plan 04 que deja la vista 059 viva.)

## Artifacts this phase produces (Plan 02)
- Read-path de `public_professional_services` en `app/[slug]/page.tsx` + prop `professionalServices` a `BookingClient`.
- Tarjeta "Cualquiera" ("El primero disponible") en el paso Profesional de `app/[slug]/booking-client.tsx`, gateada por `capaces.length >= 2`.
- Señal `any=1&serviceId` (availability) + boolean `anyProfessional` (create) desde el front.
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-PATTERNS.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read-path de public_professional_services en page.tsx</name>
  <files>app/[slug]/page.tsx</files>
  <read_first>
    - @app/[slug]/page.tsx — el `Promise.all` de vistas acotadas (75-90, incluye `public_services`, `public_professionals`, `public_canchas` con `.eq('business_id', business.id)`), la construcción de `bookingNode` (130-147: BookingClient para no-canchas, CanchasBookingClient para canchas), y la rama del `LandingRenderer` (174-187) que reusa `bookingNode` como `bookingSlot`.
    - @lib/types.ts (interface ProfessionalService) — la forma de la vista.
  </read_first>
  <action>
    Sumar al `Promise.all` un select más, con el MISMO patrón acotado: `supabase.from('public_professional_services').select('*').eq('business_id', business.id)` y destructurarlo como `professionalServices`. Pasar `professionalServices={professionalServices || []}` como prop SOLO a `<BookingClient>` (dentro de la construcción de `bookingNode`), NUNCA a `<CanchasBookingClient>` (D-09). Como `bookingNode` ya está armado antes de las ramas legacy y LandingRenderer, basta con agregar el prop en la construcción de `bookingNode` (no hay que tocar el LandingRenderer, que reusa `bookingNode` como `bookingSlot`). Fail-safe: si la vista todavía no está aplicada en la DB, el select devuelve error/[] y el `|| []` lo neutraliza — el booking sigue funcionando (RESEARCH: sin la vista, "Cualquiera" simplemente no se gatea con precisión).
  </action>
  <acceptance_criteria>
    - `page.tsx` lee `public_professional_services` filtrado por `business_id` en el `Promise.all`.
    - `professionalServices` se pasa como prop a `BookingClient` y NO a `CanchasBookingClient`.
    - El caso vista-ausente degrada a `[]` sin romper el render (fail-safe).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "public_professional_services" "app/[slug]/page.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>El RSC provee el mapeo staff↔servicios a `BookingClient` por la vista acotada, sin exponerlo a canchas.</done>
</task>

<task type="auto">
  <name>Task 2: Tarjeta "Cualquiera" + gating + señal any/anyProfessional en booking-client.tsx</name>
  <files>app/[slug]/booking-client.tsx</files>
  <read_first>
    - @app/[slug]/booking-client.tsx — `Props`/destructuring (19-29, 42), el estado `selectedPro` con default `'none'` (45), el step 2 "Profesional" (472-506: la tarjeta "Sin preferencia" 476-487 + la lista `professionals.map` 488-504), la derivación de `proId` en `handleDateSelect` (220) y `handleConfirm` (289), la construcción de params de availability (232-234) y el body del create (318-330), y el resumen del paso 3 (línea 544, muestra 'Sin preferencia').
    - @lib/staff-services.ts — `professionalsForService(serviceId, activeProfessionals, bridge)` (helper puro, comodín).
    - @lib/types.ts (interface ProfessionalService) — el tipo del prop nuevo.
  </read_first>
  <action>
    (1) Props/estado: agregar `professionalServices: ProfessionalService[]` a `Props` y al destructuring; importar `professionalsForService` de `@/lib/staff-services` y el tipo `ProfessionalService` de `@/lib/types`. Mantener el estado `selectedPro` con default `'none'` (D-01: `'none'` pasa a significar "Cualquiera" cuando hay 2+ capaces).
    (2) Cómputo de capaces + tarjeta: derivar `const capaces = selectedService ? professionalsForService(selectedService.id, professionals, professionalServices) : professionals` y `const showAny = capaces.length >= 2` (D-02). Re-etiquetar la tarjeta existente "Sin preferencia" (476-487) a copy **"Cualquiera"** + sub-texto **"El primero disponible"** (D-03), manteniendo su `onClick={() => { setSelectedPro('none'); setStep(3) }}` y la MISMA UI de tarjeta (espejar el patrón de tarjeta de profesional existente; no inventar diseño nuevo). Mostrar esa tarjeta SOLO si `showAny`. Filtrar la lista renderizada de profesionales a `capaces` (no `professionals` crudo). Alinear el copy del resumen del paso 3 (544): con "Cualquiera" mostrar "Cualquiera" en vez de "Sin preferencia".
    (3) Señal (D-05, sin id de asignación): derivar `const isAny = selectedPro === 'none' && capaces.length >= 2`. En availability (232-234): en la rama donde hoy hace `if (proId) params.set('professionalId', proId)`, agregar `else if (isAny) { params.set('any', '1'); params.set('serviceId', selectedService.id) }`. En el body del create (318-330): mandar `professionalId: isAny ? null : proId` y `anyProfessional: isAny`. NUNCA mandar un id como asignación de "Cualquiera". No tocar el bucle de generación de slots (255-278): la rama `any` devuelve `busy:[]` + `full` poblado, byte-compatible con el consumo actual (`full.includes(time)`).
    Con ≤1 capaz (o negocio sin profesionales nombrados): la tarjeta "Cualquiera" no se muestra y el flujo se comporta como hoy (D-02/D-08). NO tocar `canchas-booking-client.tsx` (D-09).
  </action>
  <acceptance_criteria>
    - La tarjeta "Cualquiera" ("El primero disponible") aparece arriba de la lista solo con `capaces.length >= 2`; con ≤1 capaz no se muestra.
    - La lista de profesionales del paso 2 se filtra a los capaces del servicio elegido.
    - Con "Cualquiera": la request de availability lleva `any=1&serviceId=…` (sin professionalId) y el body del create lleva `anyProfessional:true` + `professionalId:null`.
    - Con un profesional específico: la request lleva `professionalId` y NO `any` (DISP-02); el body del create lleva `professionalId` y `anyProfessional:false`.
    - `canchas-booking-client.tsx` no aparece en el diff (D-09/SC5).
  </acceptance_criteria>
  <verify>
    <automated>grep -n "anyProfessional" "app/[slug]/booking-client.tsx" && grep -n "El primero disponible" "app/[slug]/booking-client.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>El paso Profesional ofrece "Cualquiera" gateado por capaces>=2 y manda la señal correcta (boolean, no id); el gemelo canchas queda intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| booking-client (front) → /api/booking/create | el front declara la intención "Cualquiera" con un boolean, no con un id de asignación |
| booking-client (front) → /api/booking/availability | el front pide la grilla agregada con any=1&serviceId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | señal del front hacia create | mitigate | El front manda `anyProfessional` (boolean) con `professionalId:null`, gateado en `capaces.length >= 2`; nunca un id como asignación. La autoridad de la asignación es el server/RPC (D-05). Un boolean forjado sin capaces cae en `slot_taken` (fail-safe, cubierto por el Plan 01). |
| T-10-08 | Information Disclosure | grilla agregada en el client | mitigate | El client consume solo `{ busy:[], full }` (D-06); no recibe ni infiere counts ni qué profesional está ocupado. La agregación la hizo el server (Plan 01). |
| T-10-09 | Regression (gemelo canchas) | superficie compartida del booking público | mitigate | `professionalServices` no se pasa a `CanchasBookingClient`; `canchas-booking-client.tsx` no se toca. Todo lo nuevo está detrás de `showAny`/`isAny`, que canchas nunca dispara (D-09/SC5). |

No se instalan paquetes en esta fase → sin amenaza de cadena de suministro `T-10-SC`.
</threat_model>

<verification>
- `grep` confirma `public_professional_services` en page.tsx, `anyProfessional` + "El primero disponible" en booking-client.tsx.
- `npx tsc --noEmit` compila (nota: en este repo `npx tsc` puede salir 0 aún con errores — el chequeo fuerte es `npm run build`).
- La verificación humana end-to-end (tarjeta, gating por cantidad, grilla agregada, confirmación con nombre, cero-regresión de específico y de canchas) corre en el Plan 05, tras el reset del Plan 04.
</verification>

<success_criteria>
- El cliente puede elegir profesional o "Cualquiera" (ASIGN-01); "Cualquiera" solo con 2+ capaces (D-02).
- La grilla con "Cualquiera" refleja al equipo entero (DISP-01/03) y la de específico es idéntica a hoy (DISP-02).
- El front manda el boolean, nunca un id (D-05); canchas intacto (D-09/SC5).
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-02-SUMMARY.md` when done
</output>
