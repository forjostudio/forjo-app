---
phase: 10-reservar-con-cualquiera-desde-la-p-gina-p-blica
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - test/booking-cualquiera-public.test.ts
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03, ASIGN-05]
must_haves:
  truths:
    - "`supabase db reset` replaya 001→059 en la DB local sin error y public_professional_services queda viva (BLOCKING antes de los tests)"
    - "DISP-01: 2 pros, 1 ocupado a las 10:00 → la rama any ofrece las 10:00 (el otro libre)"
    - "DISP-03: 2 pros, ambos ocupados a las 10:00 → 10:00 va a full (no se ofrece)"
    - "DISP-02: para un professionalId específico, la respuesta de availability es byte-idéntica a la de antes de la fase (regresión verde)"
    - "ASIGN-05: un turno por 'Cualquiera' queda con professional_id NO nulo y sendConfirmationEmail recibe el nombre; sentinel → nada"
    - "Cero regresión de canchas: professionalId=cancha.id no toca la rama any (canchas-booking.test verde)"
  artifacts:
    - path: "test/booking-cualquiera-public.test.ts"
      provides: "Cobertura DISP-01/02/03 + ASIGN-05 + wiring anyProfessional→autoAssign"
      contains: "DISP-01"
  key_links:
    - from: "test/booking-cualquiera-public.test.ts"
      to: "app/api/booking/availability/route.ts"
      via: "GET con any=1&serviceId contra la DB local"
      pattern: "any=1|any', '1'|serviceId"
---

<objective>
Validar la fase end-to-end sin intervención humana: aplicar la migración 059 localmente (`supabase db reset`, BLOCKING, ANTES de los tests) y cubrir con Vitest los criterios de la ROADMAP — DISP-01/02/03 (grilla agregada vs. por-agenda), ASIGN-05 (nombre en el turno/mail) y la cero-regresión de canchas y del booking público.

Purpose: la rama de agregación y el wiring tocan superficies de alto riesgo de regresión (los 4 caminos que comparten el endpoint/core). La verificación prioriza tests contra la DB local (patrón STATE 07-12) por sobre dobles de Supabase.
Output: DB local con 059 viva + `test/booking-cualquiera-public.test.ts` + suite de regresión verde.

## Artifacts this phase produces (Plan 04)
- Aplicación local de la migr. 059 (`public_professional_services`) vía `supabase db reset` (replay 001→059).
- `test/booking-cualquiera-public.test.ts` (DISP-01/02/03 + ASIGN-05 + wiring anyProfessional→autoAssign).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply local de la migración 059 (supabase db reset)</name>
  <files>(ninguno — aplicación local de la DB, no toca el repo salvo tsbuildinfo)</files>
  <read_first>
    - @supabase/migrations/059_public_professional_services.sql — la vista escrita en el Plan 01 (Task 1).
    - @.planning/workstreams/motor-reservas/STATE.md — infra de testing (Supabase CLI local, baseline 040+, replay 001→059).
  </read_first>
  <action>
    [BLOCKING] Correr `npx supabase db reset` para replayar el baseline numerado 001→059 en el Postgres LOCAL (PG17), incluyendo la vista nueva 059. Confirmar que el reset termina sin error y que `public_professional_services` existe y es legible por el rol `anon` local (un `select` de la vista como anon devuelve filas, no un error de permiso — Pitfall 5: con `security_invoker` daría []). Recordar en el SUMMARY que la aplicación a PROD es un paso MANUAL de deploy (fuera de scope, D-09 histórico): aplicar 059 a mano + `NOTIFY pgrst, 'reload schema';` + regenerar `supabase/schema.sql`. Este task DEBE completarse antes de los tests (las tareas 2 y 3 dependen de la vista viva y del replay limpio).
  </action>
  <acceptance_criteria>
    - `npx supabase db reset` completa sin error (replay 001→059).
    - La vista `public_professional_services` existe en la DB local y es SELECT-able por `anon` (devuelve filas, no error de permiso).
    - El SUMMARY documenta el apply manual a prod pendiente (059 + NOTIFY pgrst + schema.sql).
  </acceptance_criteria>
  <verify>
    <automated>npx supabase db reset</automated>
  </verify>
  <done>La migración 059 replaya limpio y la vista queda viva en local; recién ahí corren los tests.</done>
</task>

<task type="auto">
  <name>Task 2: test/booking-cualquiera-public.test.ts — DISP-01/02/03 + ASIGN-05 + wiring</name>
  <files>test/booking-cualquiera-public.test.ts</files>
  <read_first>
    - @test/booking-public-regression.test.ts — patrón del harness del booking público (seed de negocio/servicio/profesional, llamada al route handler, aserciones sobre `{ ok, busy, full }`).
    - @test/staff-assignment.test.ts — fixture `seedProfessionalService` (Phase 9) y el patrón de carrera real contra la DB local; reusarlos.
    - @app/api/booking/availability/route.ts — la rama `any` (Plan 01) que se ejercita.
    - @app/api/booking/create/route.ts — el wiring `anyProfessional`→`autoAssign` (Plan 01).
  </read_first>
  <action>
    Crear `test/booking-cualquiera-public.test.ts` reusando los fixtures existentes (seed de negocio + 2 profesionales + servicio + `seedProfessionalService` para mapearlos al servicio, o dejarlos comodín). Cubrir: (1) **DISP-01** — 2 pros capaces, uno con turno vivo a las 10:00; la rama `GET availability?any=1&serviceId=…` NO incluye 10:00 en `full` (el otro está libre) y devuelve `busy:[]`. (2) **DISP-03** — ambos pros ocupados a las 10:00 → 10:00 SÍ está en `full`. (3) **DISP-02** — para un `professionalId` específico (sin `any`), la respuesta es idéntica a la esperada de hoy (byte-idéntica: mismas claves y valores que el path pre-fase; apoyarse en la regresión existente). (4) **ASIGN-05 / wiring** — un `POST create` con `anyProfessional:true` deja el turno con `professional_id` NO nulo (lo asignó el RPC), y el nombre resoluble por el join `professionals(name)`; un negocio sin profesionales nombrados (sentinel) deja `professional_id` NULL → sin nombre. Verificar además que el create pasa `autoAssign` (el turno de "Cualquiera" no cae al bucket SENTINEL). Priorizar aserciones contra la DB local por sobre dobles de Supabase. Documentar el caveat conocido (horario especial que EXTIENDE el día vía `schedule_exceptions`: el `full` server se computa sobre la grilla semanal; ese caso raro lo backstopea el RPC con `slot_taken` — no se testea como bug).
  </action>
  <acceptance_criteria>
    - DISP-01, DISP-03 y DISP-02 quedan cubiertos con aserciones sobre `{ ok, busy, full }`.
    - ASIGN-05: turno de "Cualquiera" → `professional_id` NO nulo + nombre resoluble; sentinel → NULL/sin nombre.
    - El test verifica que el create con `anyProfessional:true` no cae al bucket SENTINEL (Pitfall 2).
    - `npx vitest run test/booking-cualquiera-public.test.ts` verde.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/booking-cualquiera-public.test.ts</automated>
  </verify>
  <done>Los criterios DISP-01/02/03 y ASIGN-05 están cubiertos por tests que corren contra la DB local con 059 viva.</done>
</task>

<task type="auto">
  <name>Task 3: Regresión de los caminos compartidos (canchas + booking público + core)</name>
  <files>(ninguno — corre las suites existentes)</files>
  <read_first>
    - @.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-RESEARCH.md (sección "Estrategia de tests") — las suites a mantener verdes.
  </read_first>
  <action>
    Correr la suite completa de regresión de los 4 caminos que comparten endpoint/core: `test/canchas-booking.test.ts` (canchas sin "Cualquiera", D-09/SC5), `test/booking-public-regression.test.ts` (path específico/omitido byte-idéntico, DISP-02/D-08), `test/staff-assignment.test.ts` (asignación "cualquiera" del RPC, Phase 9), `test/booking-core.test.ts` (core + autoAssign). Todas deben quedar verdes: la rama `any` y el flag `anyProfessional` están gateados detrás de params que canchas y el negocio single-pro nunca mandan → el diff de availability para un `professionalId` concreto debe ser nulo. Si alguna falla, es señal de regresión (Pitfall 4) — reportar y no cerrar. Cerrar corriendo toda la suite (`npx vitest run`) para confirmar que el total sube desde el baseline (302 al cierre de v0.12; el workstream venía verde) sin romper nada.
  </action>
  <acceptance_criteria>
    - `test/canchas-booking.test.ts`, `test/booking-public-regression.test.ts`, `test/staff-assignment.test.ts`, `test/booking-core.test.ts` verdes.
    - `npx vitest run` (suite completa) verde, con los casos nuevos sumados.
    - Cero diff observable en la respuesta de availability para un `professionalId` concreto (DISP-02/D-08).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>Los 4 caminos compartidos siguen verdes con la fase aplicada; cero regresión de canchas ni del booking de 1 profesional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → DB local | los tests seedan y consultan la DB local con 059 aplicada; verifican el contrato acotado y la asignación |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10 | Information Disclosure (verificación) | contrato acotado de la rama any | mitigate | El test asierta que la rama `any` devuelve `busy:[]` y solo `full` (sin counts ni per-pro) — verifica la mitigación de T-10-01/D-06. |
| T-10-11 | Tampering (verificación) | wiring anyProfessional→autoAssign | mitigate | El test asierta que "Cualquiera" queda con `professional_id` NO nulo (no bucket SENTINEL) y que el server ignora ids como asignación — verifica T-10-03/T-10-07. |
| T-10-12 | Regression (verificación) | gemelo canchas + single-pro | mitigate | Las suites `canchas-booking`/`booking-public-regression` verdes verifican la cero-regresión de D-08/D-09/SC5. |

No se instalan paquetes en esta fase → sin amenaza de cadena de suministro `T-10-SC`.
</threat_model>

<verification>
- `npx supabase db reset` limpio (059 viva) ANTES de los tests (BLOCKING).
- `npx vitest run test/booking-cualquiera-public.test.ts` verde (DISP-01/02/03 + ASIGN-05).
- `npx vitest run` (suite completa) verde: canchas + booking público + core sin regresión.
</verification>

<success_criteria>
- La migración 059 replaya limpio en local y la vista queda viva.
- DISP-01/02/03 y ASIGN-05 cubiertos por tests contra la DB local.
- Cero regresión verificada en canchas, booking público específico y core (D-08/D-09/SC5).
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-04-SUMMARY.md` when done
</output>
