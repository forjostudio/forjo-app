---
phase: 10-reservar-con-cualquiera-desde-la-p-gina-p-blica
plan: 05
type: execute
wave: 4
depends_on: [10-04]
files_modified: []
autonomous: false
requirements: [ASIGN-01, ASIGN-05, DISP-01, DISP-02, DISP-03]
must_haves:
  truths:
    - "Un humano confirma end-to-end sobre la DB local (con la vista 059 viva) que 'Cualquiera' aparece con 2+ capaces, la grilla agregada respeta libre/lleno, la confirmación muestra el profesional, y ni el específico ni canchas regresionan"
  artifacts: []
  key_links: []
---

<objective>
Verificación humana end-to-end de la reserva pública con "Cualquiera", con la vista 059 ya viva en la DB local (aplicada en el Plan 04). Cierra visualmente los 5 success criteria de la fase: la tarjeta "Cualquiera" (D-03), el gating por cantidad (D-02), la grilla agregada (DISP-01/03), la confirmación con nombre (ASIGN-05/D-04), y la cero-regresión de específico (DISP-02) y del gemelo canchas (D-09/SC5).

Purpose: la fase se planificó SIN UI-SPEC (diseño lockeado en CONTEXT). Los criterios visuales (copy/placement de la tarjeta, nombre en pantalla) y el comportamiento de la grilla no se cubren con Vitest — un humano los confirma sobre datos y config reales. Matchea el patrón de checkpoint del workstream (06-05, 07-05).
Output: aprobación humana o lista de desvíos para cerrar en un plan de gap.

## Artifacts this phase produces (Plan 05)
- Registro de la verificación humana end-to-end (aprobación o desvíos).
</objective>

<execution_context>
@.claude/gsd-core/workflows/execute-plan.md
@.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Verificación end-to-end de la reserva pública con "Cualquiera"</name>
  <what-built>Reserva pública con "Cualquiera": tarjeta en el paso Profesional gateada por 2+ capaces (D-02/D-03), grilla de horarios agregada across-staff (DISP-01/03), señal boolean al server (D-05), y el nombre del profesional asignado en la pantalla de confirmación (ASIGN-05). Backend (vista 059 + rama any + wiring autoAssign) y mail ya construidos y testeados en los Planes 01/03/04; la vista 059 está viva en local tras el `supabase db reset` del Plan 04.</what-built>
  <how-to-verify>
    1. Levantar la app local (`npm run dev`) contra el Supabase LOCAL. Usar el negocio de prueba (`supabase/seed.sql`, login `test@forjo.local`). Asegurar 2+ profesionales activos que hagan el MISMO servicio (mapearlos en `/equipo` si hace falta; recordá que sin mapeo, por el comodín, todos son capaces).
    2. Abrir `/[slug]` del negocio. Paso 1: elegir un servicio que hagan 2+ personas. Paso 2: confirmar que aparece la tarjeta **"Cualquiera"** con sub-texto **"El primero disponible"** ARRIBA de la lista, con la misma UI que las tarjetas de profesional, y que la lista muestra solo los capaces del servicio.
    3. Elegir "Cualquiera" → elegir un día. Verificar que un horario se ofrece si AL MENOS un profesional lo tiene libre (DISP-01). Desde el panel, ocupar ese horario en AMBAS agendas y recargar: el horario debe desaparecer (DISP-03). Verificar que la grilla NO expone cuántos lugares quedan ni qué profesional está ocupado (D-06).
    4. Confirmar el turno (sin seña) y verificar en `turno/[token]` que muestra "Te atiende: {Nombre}" con el profesional que asignó el server (ASIGN-05). El mail real no se valida en local (Resend vacío) — queda para el post-deploy.
    5. Volver y elegir un profesional ESPECÍFICO: la grilla debe verse idéntica a la de hoy (DISP-02).
    6. Elegir un servicio con ≤1 capaz: la tarjeta "Cualquiera" NO debe aparecer (D-02). Abrir un negocio del vertical **canchas**: el flujo (`canchas-booking-client`) sigue sin opción "Cualquiera" (D-09/SC5).
  </how-to-verify>
  <action>Checkpoint humano: no hay pasos automatizables adicionales (los tests automatizados corrieron en el Plan 04). El operador ejercita el flujo público en vivo contra el entorno local con la vista 059 aplicada, siguiendo los pasos de `how-to-verify`. Si algo falla, describe el desvío concreto (qué paso, qué esperaba y qué observó) para cerrarlo en un plan de gap propio, sin re-abrir los planes cerrados.</action>
  <acceptance_criteria>
    - behavior: con 2+ capaces aparece la tarjeta "Cualquiera" ("El primero disponible") arriba de la lista; con ≤1 capaz no aparece (D-02/D-03)
    - behavior: con "Cualquiera", un horario se ofrece si algún capaz lo tiene libre y desaparece cuando ninguno tiene lugar (DISP-01/03), sin exponer counts ni per-pro (D-06)
    - behavior: la confirmación muestra "Te atiende: {Nombre}" con el profesional asignado por el server (ASIGN-05/D-04)
    - behavior: con un profesional específico la grilla es idéntica a la de hoy (DISP-02); el flujo canchas sigue sin "Cualquiera" (D-09/SC5)
  </acceptance_criteria>
  <verify>
    <human-check>El operador ejercita el flujo público en vivo siguiendo los pasos de `how-to-verify` y responde con la señal de reanudación.</human-check>
  </verify>
  <done>El operador aprobó el flujo end-to-end, o los desvíos quedaron descritos (paso, esperado, observado) para un plan de gap.</done>
  <resume-signal>Escribí "aprobado", o describí los desvíos (copy/placement de la tarjeta, gating por cantidad, comportamiento de la grilla agregada, nombre en la confirmación, regresión de específico o canchas) para cerrarlos en un plan de gap.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verificación humana → superficie pública anónima | el humano confirma que la grilla agregada no filtra la agenda interna y que el nombre sale del turno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Information Disclosure (verificación visual) | grilla agregada + confirmación | mitigate | El humano confirma que la grilla solo muestra libre/lleno (sin counts ni per-pro, D-06) y que el nombre en la confirmación es el del turno asignado por el server (D-04) — verifica T-10-01/T-10-05 en el flujo real. |

No se instalan paquetes en esta fase → sin amenaza de cadena de suministro `T-10-SC`.
</threat_model>

<verification>
- Checkpoint humano bloqueante: la fase no se da por cerrada hasta la aprobación o el registro de desvíos.
- secure-phase recomendado (superficie pública + linaje del núcleo anti-doble-booking), consistente con la política del workstream para Phases 6/9.
</verification>

<success_criteria>
- Un humano confirma los 5 success criteria de la ROADMAP sobre la app real con la vista 059 viva, o registra los desvíos para un plan de gap.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/10-reservar-con-cualquiera-desde-la-p-gina-p-blica/10-05-SUMMARY.md` when done
</output>
