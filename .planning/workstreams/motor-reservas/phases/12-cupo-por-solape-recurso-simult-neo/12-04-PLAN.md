---
phase: 12-cupo-por-solape-recurso-simult-neo
plan: 04
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - test/helpers/booking-fixtures.ts
  - test/concurrency.test.ts
autonomous: false
requirements: [CUPO-02, CUPO-04, CUPO-05]
must_haves:
  truths:
    - "N+1 reservas escalonadas concurrentes que comparten un instante común sobre un servicio simultáneo de cupo N resuelven EXACTAMENTE N ok + 1 slot_full, con un assert DURO sobre el estado real de la DB (nunca N+1 filas) (CUPO-04)."
    - "El test CUPO-04 FALLA contra el lock viejo (biz+date+time: los 3 starts distintos se cuelan) y PASA con el lock service-day (Plan 12-01) — prueba la re-granularización (D-06)."
    - "Una 2ª reserva solapada sobre un simultáneo cupo 1 se rechaza (409, sin sobre-reserva) (CUPO-02)."
    - "supabase db reset replaya baseline + 040..062 en orden limpio y `npm run test` queda VERDE incluyendo CONC-01/02/03, CUPOS-02/03, ALQUILER-02, canchas y staff-assignment (CUPO-05, cero regresión)."
  artifacts:
    - path: "test/helpers/booking-fixtures.ts"
      provides: "Helper seedSimultaneousService (setea capacity_mode + capacity en el service sembrado)"
      contains: "simultaneous_resource"
    - path: "test/concurrency.test.ts"
      provides: "Casos CUPO-04 (carrera N+1 escalonada) + CUPO-02 (solape rechazado) + simultáneo cap 1"
      contains: "CUPO-04"
  key_links:
    - from: "Promise.all de createAppointmentCore (starts escalonados)"
      to: "book_slot_atomic (lock service-day)"
      via: "cada .rpc es su propia transacción/conexión; el lock serializa la carrera DENTRO de la DB (determinista)"
      pattern: "Promise.all"
    - from: "occupantsAt (assert duro)"
      to: "appointments (estado real)"
      via: "cuenta filas que ocupan el intervalo por la DB, no por los retornos del core"
      pattern: "occupantsAt"
---

<objective>
**Verificación DURA del anti-sobrecupo por solape (CUPO-04/CUPO-02) + gate de cero regresión (CUPO-05).** El criterio de éxito de la fase no es lectura de código: es un **test de carrera real contra la DB local** (D-08). Este plan:
1. Agrega el helper de fixture `seedSimultaneousService` (setea `capacity_mode='simultaneous_resource'` + `capacity` en el service sembrado).
2. Agrega a `test/concurrency.test.ts` los casos **CUPO-04** (N+1 reservas escalonadas concurrentes que comparten un instante común → exactamente N ok + 1 `slot_full`, con assert duro sobre la DB), **CUPO-02** (2ª solapada rechazada) y **simultáneo cupo 1** (no-regresión).
3. Corre el gate `[BLOCKING]`: `supabase db reset` (baseline + 040..062) + `npm run test` completo VERDE, incluyendo la regresión de las 4 vías del RPC (CUPO-05).

Purpose: probar que la re-granularización del lock (Plan 12-01/D-06) realmente serializa las reservas escalonadas — el test FALLA con el lock viejo y PASA con el nuevo — y que ningún consumidor del RPC regresiona.
Output (artifacts de este plan): fixture `seedSimultaneousService`, casos CUPO-04/CUPO-02/simultáneo-cap-1 en `concurrency.test.ts`, y la suite completa verde sobre el baseline con la 062.

**Nota:** esta fase es **security-sensitive → secure-phase obligatorio** aguas abajo; el test CUPO-04 es la evidencia de atomicidad que ese gate exige (no se acepta lectura de código).
</objective>

<artifacts_produced>
- `test/helpers/booking-fixtures.ts`: `seedSimultaneousService(seeded, { capacity })` (o helper equivalente) que hace `update({ capacity_mode: 'simultaneous_resource', capacity }).eq('id', serviceId)`.
- `test/concurrency.test.ts`: `it('CUPO-04 ...')` (carrera N+1 escalonada), `it('CUPO-02 ...')` (2ª solapada → 409), `it('simultáneo cap 1 ...')` (no-regresión), en el mismo `describe.skipIf(!hasSupabaseCreds)`.
- Gate: `supabase db reset` + `npm run test` verde (fixtures + suite completa, CUPO-05).
</artifacts_produced>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/12-cupo-por-solape-recurso-simult-neo/12-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/12-cupo-por-solape-recurso-simult-neo/12-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/12-cupo-por-solape-recurso-simult-neo/12-PATTERNS.md
@test/concurrency.test.ts
@test/helpers/booking-fixtures.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture seedSimultaneousService + casos CUPO-04 / CUPO-02 / simultáneo cap 1</name>
  <files>test/helpers/booking-fixtures.ts, test/concurrency.test.ts</files>
  <read_first>
    - test/helpers/booking-fixtures.ts:100-177 — `seedTimeBlock`, `seedProfessional`, `seedSpace`, `seedAgendaSpace`, `seedProfessionalService`: el MOLDE de helper (service-role insert/update + throw en error). El nuevo helper hace un `update` sobre el service ya sembrado por `seedOneTenant` (serviceId en `SeededTenant`).
    - test/concurrency.test.ts:1-114 — imports, `describe.skipIf(!hasSupabaseCreds)`, `beforeAll(seedOneTenant {bufferMinutes:0, serviceDurationMinutes:30})`, `afterEach` (limpia appointments/time_blocks/agenda_spaces/spaces), `baseInput()` (professionalId FIJO — Pitfall 1), `occupantsAt(time)` (assert duro por la DB), y CONC-01 (86-114) como molde de carrera `Promise.all`.
    - test/concurrency.test.ts:232-311 — CONC-03/ALQUILER-02 (molde de escenarios con espacio; NO tocar, deben seguir verdes).
    - 12-RESEARCH.md §Q5 (forma concreta del test CUPO-04: N=2, dur 30, starts 16:00/16:10/16:20 que comparten [16:20,16:30); Promise.all; assert 2 ok + 1 slot_full + assert duro; casos adicionales CUPO-05) y §Common Pitfalls 1.
    - 12-PATTERNS.md §"test/concurrency.test.ts (MODIFY) + booking-fixtures.ts" (CUPO-04 shape).
  </read_first>
  <action>
    1) En `booking-fixtures.ts` agregar `seedSimultaneousService(seeded, opts: { capacity: number })`: `await seeded.admin.from('services').update({ capacity_mode: 'simultaneous_resource', capacity: opts.capacity }).eq('id', seeded.serviceId)` (service-role, throw en error). Molde de `seedProfessionalService`. NO cambiar la firma de `seedOneTenant`.
    2) En `concurrency.test.ts` (mismo `describe`), agregar un `occupantsOverlapping(instant)` helper si hace falta un assert por intervalo (o reusar `occupantsAt` sobre el instante común), y estos casos:
       - **CUPO-04 (carrera N+1 escalonada):** sembrar un `time_block` que cubra la ventana (capacity irrelevante para simultáneo — define el día) + `seedSimultaneousService(t, { capacity: 2 })`. Lanzar `Promise.all` de 3 `createAppointmentCore` escalonados que comparten el instante `[16:20,16:30)`: `time:'16:00'`, `'16:10'`, `'16:20'` (dur 30). Asertar `oks.length === 2` y `fulls.length === 1` (`error === 'slot_full'`). **Assert DURO por la DB:** exactamente 2 filas del `service_id` cuyo intervalo contiene las 16:20 (NUNCA 3). Comentario: con el lock viejo (biz+date+time) los 3 starts distintos tomarían locks distintos y pasarían → 3 aceptados (el bug); con el lock service-day serializan y el 3º ve overlap 2>=2 → slot_full. Es el test que falla ANTES del fix y pasa DESPUÉS (D-06/D-08).
       - **CUPO-02 (solape rechazado, secuencial):** cupo 2, reservar 16:00, 16:10 (ok), y una 3ª 16:20 que solapa a ambas → `slot_full` (409). Assert duro: 2 filas en el intervalo común.
       - **Simultáneo cap 1 (no-regresión):** `seedSimultaneousService(t, { capacity: 1 })`; 2ª reserva solapada escalonada → rechazada con 409 (`slot_full` o `slot_taken` por EXCLUDE 013 según `is_group` de cap 1 — asertar el 409 y cero sobre-reserva; fijar el error exacto observado). Assert duro: 1 fila.
    Usar `professionalId` FIJO en todos (Pitfall 1). Mantener el patrón `afterEach` de limpieza (agregar la limpieza del modo si hace falta re-seedear el service por test, o resetear `capacity_mode`/`capacity` a los defaults en el afterEach para no contaminar CONC-01/02/CUPOS-03 que comparten el mismo tenant/service).
  </action>
  <verify>
    <automated>grep -q "seedSimultaneousService" test/helpers/booking-fixtures.ts && grep -q "CUPO-04" test/concurrency.test.ts && grep -q "slot_full" test/concurrency.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `seedSimultaneousService` existe y setea `capacity_mode='simultaneous_resource'` + `capacity` en el service del tenant sembrado.
    - Existe el caso CUPO-04 con `Promise.all` de 3 starts escalonados que comparten un instante, asertando 2 ok + 1 `slot_full` y el assert DURO sobre la DB (exactamente 2 filas en el intervalo, nunca 3).
    - Existen CUPO-02 (2ª solapada → 409) y simultáneo cap 1 (no-regresión, 409 + 1 fila).
    - El `afterEach` deja el tenant/service en estado limpio para los casos grupales existentes (sin contaminar CONC-01/02/CUPOS-03). `npx tsc --noEmit` limpio.
  </acceptance_criteria>
  <done>Fixture `seedSimultaneousService` + casos CUPO-04 (carrera escalonada con assert duro), CUPO-02 y simultáneo cap 1 en concurrency.test.ts; professionalId fijo; afterEach no contamina; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] supabase db reset + npm run test verde (CUPO-04 + regresión CUPO-05)</name>
  <files>test/concurrency.test.ts, test/helpers/booking-fixtures.ts</files>
  <read_first>
    - MEMORY [Infra de testing roadmap] — `supabase db reset` local (PG17) replaya baseline + 040..062; `npm run test` = `vitest run`; los tests contra la DB corren con `describe.skipIf(!hasSupabaseCreds)` (requieren las 3 creds apuntando al Supabase LOCAL).
    - 12-RESEARCH.md §Validation Architecture (Test → Req map, Sampling Rate, Phase gate: suite completa verde + reset limpio + secure-phase) + §Q5 (casos adicionales CUPO-05: canchas + staff-assignment verdes).
    - test/canchas-booking.test.ts y test/staff-assignment.test.ts (existencia) — deben seguir verdes (D-14/CUPO-05); NO se modifican.
  </read_first>
  <action>
    **[BLOCKING] — gate de fase mandado por el schema-gate. Correr DESPUÉS de que 12-01/12-02 estén integrados y el Task 1 de este plan escrito.** Con el Supabase LOCAL corriendo y las 3 creds seteadas al proyecto local:
    1) `npx supabase db reset` — replaya baseline + 040..062 EN ORDEN; probar que la 062 aplica limpio (DDL + función). NO `supabase db push`, NO tocar prod.
    2) `npm run test` — la suite COMPLETA debe quedar VERDE, incluyendo: los casos nuevos (CUPO-04, CUPO-02, simultáneo cap 1) y, como regresión CUPO-05, CONC-01 (anti-sobrecupo grupal), CONC-02 (cupo 1), CUPOS-03/CUPOS-02 (grupal + no-leak), CONC-03 + ALQUILER-02 (espacio compartido), `test/canchas-booking.test.ts` (canchas, D-14) y `test/staff-assignment.test.ts` (multi-staff). El total actual (~283+ tests) debe quedar en verde.
    3) Auditar explícitamente CUPO-05: confirmar que ningún test grupal/cupo 1/canchas/abono/multi-staff cambió de resultado (los `slot_full`/`slot_taken` no se degradaron). Si algo regresiona, corregir el código de 12-01/12-02 (no relajar el test) y re-correr.
    Si `supabase db reset` o `npm run test` fallan, NO marcar el plan completo: es el gate de la fase. Reportar el estado exacto (qué test, qué error) en el SUMMARY.
    **Aplicación a prod (NO es tarea de esta fase):** aplicar la 062 a mano al Supabase de prod coordinada con el deploy (`NOTIFY pgrst, 'reload schema'` + regenerar `supabase/schema.sql`) es un paso de deploy que hace el dueño.
  </action>
  <verify>
    <automated>npx supabase db reset && npm run test</automated>
    <human-check>`supabase db reset` aplica 040..062 sin error; `npm run test` termina exit 0 con CUPO-04/CUPO-02/simultáneo-cap-1 en verde y CONC-01/02/03, CUPOS-02/03, ALQUILER-02, canchas y staff-assignment sin regresión.</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` replaya baseline + 040..062 sin error (la 062 aplica limpio y en orden).
    - `npm run test` sale exit 0 con la suite completa verde; CUPO-04 pasa (y se documenta que fallaría con el lock viejo), CUPO-02 y simultáneo cap 1 pasan.
    - Regresión CUPO-05 verificada: CONC-01/02/03, CUPOS-02/03, ALQUILER-02, canchas y staff-assignment verdes; `slot_full`/`slot_taken` no degradados.
    - NO se corrió `supabase db push` ni se tocó prod.
  </acceptance_criteria>
  <done>[BLOCKING] reset local aplica 040..062 limpio + `npm run test` exit 0 con CUPO-04/CUPO-02/simultáneo-cap-1 verdes y cero regresión de las 4 vías (CUPO-05); prod NO tocado; estado reportado en el SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → Supabase LOCAL (service-role) | los fixtures usan service-role solo para sembrar/limpiar; el test asierta la lógica del RPC (lock + conteo), no RLS |
| carrera concurrente → book_slot_atomic | N+1 `.rpc` en paralelo = N+1 transacciones reales; el lock service-day serializa dentro de la DB (determinista, no flaky) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Tampering / DoS de integridad | sobrecupo por solape no detectado (falso verde) | mitigate | el assert DURO cuenta filas reales en la DB (`occupantsAt`/overlap), no los retornos del core; el caso falla con el lock viejo y pasa con el nuevo (D-08) |
| T-12-17 | Tampering de integridad | regresión silenciosa de un consumidor del RPC | mitigate | el gate corre la suite COMPLETA (CONC-01/02/03, CUPOS, ALQUILER-02, canchas, staff-assignment) sobre el baseline con la 062 (CUPO-05) |
| T-12-18 | Tampering | validación falsa por DB sin migrar (tsc/build verde, DB vieja) | mitigate | el gate es `supabase db reset` (replaya 040..062) ANTES de `npm run test`; nunca se valida contra una DB sin la 062 |
</threat_model>

<verification>
- `npx supabase db reset` replaya baseline + 040..062 limpio.
- `npm run test` exit 0: CUPO-04 (2 ok + 1 slot_full + assert duro), CUPO-02, simultáneo cap 1, y regresión CUPO-05 verde.
- El caso CUPO-04 es determinista (el lock serializa la carrera); documentado que fallaría con el lock viejo.
- `npx tsc --noEmit` limpio con el fixture + los casos nuevos.
</verification>

<success_criteria>
- N+1 reservas escalonadas concurrentes sobre un recurso de cupo N nunca superan el cupo, probado contra la DB real (CUPO-04).
- Una 2ª reserva solapada en simultáneo se rechaza sin sobre-reserva (CUPO-02).
- Cero regresión de cupo 1, canchas, abonos, multi-staff y espacio compartido; suite completa verde sobre el baseline con la 062 (CUPO-05).
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/12-cupo-por-solape-recurso-simult-neo/12-04-SUMMARY.md` when done
</output>
