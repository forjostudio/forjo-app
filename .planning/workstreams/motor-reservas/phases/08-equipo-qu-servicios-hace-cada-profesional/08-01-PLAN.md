---
phase: 08-equipo-qu-servicios-hace-cada-profesional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/057_professional_services.sql
  - supabase/schema.sql
  - lib/types.ts
  - lib/staff-services.ts
  - test/staff-services.test.ts
autonomous: false
requirements: [STAFF-01, STAFF-02, STAFF-03]

must_haves:
  truths:
    - "La tabla puente professional_services existe con RLS habilitada + 4 policies por operación por tenant, sin acceso anon (STAFF-01)."
    - "El helper puro resuelve la regla del comodín (D-01): un profesional con 0 filas es capaz de TODOS los servicios (STAFF-03)."
    - "El helper resuelve 'quiénes ofrecen el servicio X' contando solo profesionales activos (STAFF-02, D-16)."
    - "Un servicio queda sin cobertura solo cuando todos los profesionales activos tienen mapeo explícito y ninguno lo marcó; si hay algún comodín, todos están cubiertos (STAFF-02)."
    - "La 057 no toca professionals.service_id (canchas, migr. 043) ni el motor de reservas (book_slot_atomic, constraints 011/013)."
  artifacts:
    - path: "supabase/migrations/057_professional_services.sql"
      provides: "Tabla puente professional_services (business_id/professional_id/service_id NOT NULL, PK compuesta, ON DELETE CASCADE) + RLS + 4 policies + índice inverso"
      contains: "create table if not exists"
    - path: "supabase/schema.sql"
      provides: "Snapshot regenerado con professional_services + sus policies"
      contains: "professional_services"
    - path: "lib/types.ts"
      provides: "Interface ProfessionalService (columnas snake_case espejo de la fila DB)"
      contains: "interface ProfessionalService"
    - path: "lib/staff-services.ts"
      provides: "Helper puro D-12: servicesForProfessional, professionalsForService, isServiceCovered"
      exports: ["servicesForProfessional", "professionalsForService", "isServiceCovered"]
    - path: "test/staff-services.test.ts"
      provides: "Tests vitest de la regla del comodín (0 filas = todos; último desmarcado = sin cobertura; mixto; inactivo excluido)"
      contains: "staff-services"
  key_links:
    - from: "lib/staff-services.ts"
      to: "lib/types.ts"
      via: "importa los tipos Professional, Service, ProfessionalService"
      pattern: "ProfessionalService"
    - from: "supabase/migrations/057_professional_services.sql"
      to: "supabase/schema.sql"
      via: "supabase db reset local replaya 001→057; schema.sql se actualiza a mano (quirúrgico)"
      pattern: "professional_services"
---

<objective>
Crear el espinazo de datos y de reglas de la Phase 8: la migración 057 con la tabla puente
`professional_services` (mapeo muchos-a-muchos profesional↔servicio, hermana de `agenda_spaces` de
la migr. 042) con RLS por operación por tenant, la interface TypeScript espejo, y el **helper puro
con tests** que encierra la regla del comodín (D-01/D-12) para que la UI de esta fase y las Phases
9/10 la interpreten idénticamente.

Purpose: sin este modelo + regla no hay dónde guardar "quién hace qué" ni una fuente única de verdad
para "quién puede hacer el servicio X". Es la fase de menor riesgo del milestone: agrega datos de
config, NO toca el motor de reservas (`book_slot_atomic`, availability, constraints 011/013) ni
`professionals.service_id` (canchas, migr. 043).

Output: `supabase/migrations/057_professional_services.sql`, `supabase/schema.sql` regenerado,
`ProfessionalService` en `lib/types.ts`, `lib/staff-services.ts` + `test/staff-services.test.ts`,
validados con `supabase db reset` local (PG17).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/08-equipo-qu-servicios-hace-cada-profesional/08-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/08-equipo-qu-servicios-hace-cada-profesional/08-PATTERNS.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
@supabase/migrations/042_spaces_and_coupled_exclusion.sql
@lib/booking-window.ts
@lib/types.ts
</context>

## Artifacts this phase produces (Plan 01)

| Símbolo | Tipo | Archivo |
|---------|------|---------|
| `professional_services` (tabla puente) | tabla DB | `supabase/migrations/057_professional_services.sql` |
| columnas `business_id`, `professional_id`, `service_id` (NOT NULL, PK `(professional_id, service_id)`) | columnas DB | idem |
| 4 policies `professional_services tenant {select,insert,update,delete}` | RLS policies | idem |
| índice `professional_services_by_service (service_id, professional_id)` | índice DB | idem |
| migración `057_professional_services.sql` | archivo SQL numerado | `supabase/migrations/` |
| snapshot regenerado con `professional_services` | schema | `supabase/schema.sql` |
| `interface ProfessionalService` | tipo TS | `lib/types.ts` |
| `servicesForProfessional`, `professionalsForService`, `isServiceCovered` | funciones puras | `lib/staff-services.ts` |
| suite de la regla del comodín | test vitest | `test/staff-services.test.ts` |

<tasks>

<task type="auto">
  <name>Task 1: Migración 057 (tabla puente professional_services + RLS + índice inverso) + tipo TS + schema.sql</name>
  <files>supabase/migrations/057_professional_services.sql, lib/types.ts, supabase/schema.sql</files>
  <read_first>
    - supabase/migrations/042_spaces_and_coupled_exclusion.sql (líneas 1-106: la PLANTILLA textual — header en español, tabla `agenda_spaces`, ENABLE RLS, las 4 policies por operación; copiar renombrando `space`→`service` y `spaces`→`services`). NO copiar §3-5 (el RPC book_slot_atomic + backstop) — la Phase 8 no toca el motor.
    - lib/types.ts (líneas 138-152: interfaces `Space` y `AgendaSpace` — el patrón exacto del tipo puente, columnas snake_case, comentario denso en español)
    - supabase/schema.sql (buscar la sección de `agenda_spaces` para insertar `professional_services` de forma quirúrgica, con su ENABLE RLS + policies + índice)
    - .claude/skills/supabase-multitenant-rls/SKILL.md (reglas: RLS habilitada en la MISMA migración; una policy por operación con la cláusula correcta — select/delete USING, insert WITH CHECK, update ambas; predicado de tenant por owner_id)
  </read_first>
  <action>
    Crear `supabase/migrations/057_professional_services.sql`, **idempotente** y numerada (última en prod = 056):
    (1) `create table if not exists "public"."professional_services"` con tres columnas `uuid NOT NULL`:
    `business_id` REFERENCES `businesses(id)` ON DELETE CASCADE, `professional_id` REFERENCES
    `professionals(id)` ON DELETE CASCADE, `service_id` REFERENCES `services(id)` ON DELETE CASCADE;
    `PRIMARY KEY ("professional_id", "service_id")`. Sin columna `created_at` (espejo exacto de
    `agenda_spaces`). El `ON DELETE CASCADE` en las tres FK es lo que hace gratis a D-17 (borrar un
    servicio o profesional limpia la puente).
    (2) `ALTER TABLE ... ENABLE ROW LEVEL SECURITY`.
    (3) Las **4 policies por operación** con el predicado de tenant idéntico al de 042
    (`business_id IN (SELECT id FROM businesses WHERE owner_id = (SELECT auth.uid()))`): `select` y
    `delete` con `USING`, `insert` con `WITH CHECK`, `update` con `USING` + `WITH CHECK`. Nombrarlas
    `"professional_services tenant select/insert/update/delete"`. Precedidas por `drop policy if exists`
    de cada una (idempotencia). **Sin policy anon** — el público nunca lee la puente (D-11; la Phase 10
    resolverá capacidad server-side por vista acotada en una migración posterior si hace falta).
    (4) `create index if not exists "professional_services_by_service" ON ... ("service_id", "professional_id")`
    — el índice inverso que `agenda_spaces` no necesitó, para la query de cobertura (STAFF-02) y la
    Phase 9 (buscar profesionales por servicio).
    (5) Header comment denso en español (estructura `-- Contexto / Qué hace / Qué NO hace` de 042):
    declarar que 057 crea SOLO la puente + RLS, que NO toca `professionals.service_id` (canchas, migr.
    043), que NO modifica el motor ni ninguna función/RPC, que hay CERO backfill (regla del comodín
    D-01 = sin filas = todos capaces), y que se aplica A MANO a prod coordinada con el deploy (+
    recarga del schema cache de PostgREST) — NO por el flujo GSD; validación local con el reset del
    baseline. Redactar el header sin incluir la sentencia DDL de redefinición de función; referirse al
    motor solo como "el RPC / la función atómica".
    En `lib/types.ts`, agregar la interface `ProfessionalService` junto a `AgendaSpace` (línea ~152):
    tres campos `string` snake_case (`business_id`, `professional_id`, `service_id`) con comentario que
    explique la regla del comodín (0 filas = capaz de todo, ver `lib/staff-services`). NO renombrar a
    camelCase (convención del repo).
    En `supabase/schema.sql`, agregar de forma **quirúrgica** (NO dump completo — el CLI reordena el
    archivo entero) la sección de `professional_services`: CREATE TABLE + ENABLE RLS + 4 policies +
    índice, junto a la de `agenda_spaces`.
  </action>
  <acceptance_criteria>
    - `supabase/migrations/057_professional_services.sql` existe y `grep -ci 'create table if not exists' supabase/migrations/057_professional_services.sql` ≥ 1.
    - `grep -ci 'create policy' supabase/migrations/057_professional_services.sql` == 4, y `grep -ci 'with check' supabase/migrations/057_professional_services.sql` ≥ 2 (insert + update).
    - `grep -ci 'enable row level security' supabase/migrations/057_professional_services.sql` == 1.
    - `grep -c 'professional_services_by_service' supabase/migrations/057_professional_services.sql` ≥ 1 (índice inverso).
    - `grep -ci 'create or replace function' supabase/migrations/057_professional_services.sql` == 0 (no toca el motor/RPC) y `grep -c 'book_slot_atomic' supabase/migrations/057_professional_services.sql` == 0.
    - `grep -c 'interface ProfessionalService' lib/types.ts` == 1 con tres campos snake_case.
    - `grep -c 'professional_services' supabase/schema.sql` ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>grep -ci 'create policy' supabase/migrations/057_professional_services.sql | grep -qx 4 && grep -qc 'professional_services_by_service' supabase/migrations/057_professional_services.sql && grep -qc 'interface ProfessionalService' lib/types.ts && grep -qc 'professional_services' supabase/schema.sql && echo OK</automated>
  </verify>
  <done>La migración 057 crea la tabla puente con RLS + 4 policies por tenant + índice inverso, es idempotente, no toca el motor ni professionals.service_id; el tipo TS y el schema.sql reflejan la tabla.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Helper puro lib/staff-services.ts (regla del comodín D-01/D-12) + test vitest</name>
  <files>lib/staff-services.ts, test/staff-services.test.ts</files>
  <read_first>
    - lib/booking-window.ts (el módulo canónico "regla pura compartida entre capas, testeable sin DB": doc-comment de fuente única de verdad, funciones PURAS sin React/Supabase sobre datos planos)
    - test/verticals.test.ts (líneas 1-40: idioma exacto del test puro — `import { describe, it, expect } from 'vitest'`, import desde `@/lib/...`, sin Supabase ni creds)
    - lib/types.ts (interfaces `Professional`, `Service` y la nueva `ProfessionalService` de Task 1 — inputs planos del helper)
    - .planning/workstreams/motor-reservas/phases/08-equipo-qu-servicios-hace-cada-profesional/08-CONTEXT.md (líneas 76-79 "Nota de implementación" + D-01 + D-16: la regla del comodín y el cálculo de cobertura contando solo activos)
    - .planning/workstreams/motor-reservas/phases/08-equipo-qu-servicios-hace-cada-profesional/08-UI-SPEC.md (líneas 297-299: "cuándo un servicio está sin cobertura")
  </read_first>
  <action>
    Crear `lib/staff-services.ts` como módulo PURO (sin React, sin Supabase; inputs = filas planas
    `Professional[]`, `Service[]`, `ProfessionalService[]`), con doc-comment en español que declare que
    es la fuente ÚNICA de verdad de la regla del comodín, consumida por la UI de esta fase (D-08
    cobertura) y las Phases 9 (RPC) y 10 (grilla pública), para que las tres capas no interpreten
    distinto. Exportar tres funciones:
    (1) `servicesForProfessional(professionalId, services, bridge)` → los servicios que hace ese
    profesional: si tiene 0 filas en `bridge` (comodín, D-01) devuelve TODOS los `services`; si tiene ≥1,
    devuelve exactamente los mapeados.
    (2) `professionalsForService(serviceId, activeProfessionals, bridge)` → los profesionales activos que
    ofrecen el servicio: los que tienen 0 filas (comodín) MÁS los que tienen una fila para ese
    `serviceId`. Recibe la lista ya filtrada a activos (D-16).
    (3) `isServiceCovered(serviceId, activeProfessionals, bridge)` → `true` salvo que TODOS los
    profesionales activos tengan mapeo explícito (≥1 fila) y NINGUNO haya marcado ese servicio; si queda
    al menos un comodín (0 filas) entre los activos, todo servicio está cubierto. Debe ser consistente
    con `professionalsForService(...).length > 0`.
    Crear `test/staff-services.test.ts` (vitest, idioma de test/verticals.test.ts) congelando los casos
    de la regla: (a) profesional con 0 filas aparece en TODOS los servicios y hace todos; (b) profesional
    con mapeo explícito hace solo los marcados; (c) desmarcar el último profesional que ofrecía un
    servicio → ese servicio queda sin cobertura (todos los activos con mapeo explícito, ninguno lo marcó);
    (d) si al menos un activo es comodín, ningún servicio queda sin cobertura; (e) un profesional
    INACTIVO no cuenta para la cobertura aunque tenga la fila (D-16) — el caller pasa solo activos, así
    que el test verifica que la función respeta la lista que recibe.
    NO acceder a Supabase ni mockearlo; NO reimplementar la regla en ningún otro lado (la UI la consume).
  </action>
  <acceptance_criteria>
    - `lib/staff-services.ts` exporta `servicesForProfessional`, `professionalsForService`, `isServiceCovered` (`grep -c 'export function' lib/staff-services.ts` ≥ 3) e importa solo tipos (`grep -c "from '@/lib/types'" lib/staff-services.ts` ≥ 1); es puro (sin cliente de datos), verificado porque `npm test` corre sin credenciales ni DB.
    - `test/staff-services.test.ts` cubre: 0-filas=comodín, mapeo explícito, último desmarcado=sin cobertura, comodín presente=todo cubierto, inactivo excluido.
    - `npm test` (vitest run) exits 0 con la suite staff-services pasando.
    - Comportamiento: `professionalsForService('X', activos, [])` (bridge vacío) devuelve TODOS los activos; con todos los activos mapeados a otros servicios devuelve `[]` y `isServiceCovered('X', ...)` == false.
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | grep -Eiq 'staff-services' && node ./node_modules/typescript/bin/tsc --noEmit && echo OK</automated>
  </verify>
  <done>El helper puro encierra la regla del comodín en una sola implementación testeada; los casos borde (comodín, último desmarcado, inactivo) están congelados; vitest verde y typecheck limpio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Validar 057 con supabase db reset local + confirmar schema.sql (aplicación a prod = manual/out-of-band)</name>
  <files>supabase/migrations/057_professional_services.sql, supabase/schema.sql</files>
  <read_first>
    - supabase/migrations/057_professional_services.sql (la migración creada en Task 1 — objeto de la validación)
    - supabase/schema.sql (confirmar que la sección professional_services quedó agregada quirúrgicamente)
  </read_first>
  <action>
    Validar la integridad de la migración 057 corriendo el reset del baseline local (`npx supabase db reset`),
    que replaya todas las migraciones numeradas 001→057 sobre PG17 local: prueba que la 057 es idempotente,
    ordena bien y no choca con constraints previos. Confirmar que `supabase/schema.sql` refleja la tabla
    puente. NO aplicar nada a la Supabase remota (prod se aplica a mano, out-of-band, coordinada por el
    dueño). Este es un checkpoint humano bloqueante porque el reset local depende del entorno (Windows,
    analytics/studio off) y coordina con el deploy manual de prod.
  </action>
  <acceptance_criteria>
    - `npx supabase db reset` termina sin error replayando 001→057 en PG17 local.
    - `supabase/schema.sql` contiene la sección `professional_services` (tabla + ENABLE RLS + 4 policies + índice).
    - Ningún comando del plan escribe en la Supabase remota.
  </acceptance_criteria>
  <verify>
    <human-check>El operador confirma que `npx supabase db reset` pasó limpio y schema.sql refleja professional_services</human-check>
  </verify>
  <done>La 057 replaya limpio sobre el baseline local (001→057), es idempotente y ordena bien; schema.sql actualizado; aplicación a prod queda como paso manual documentado.</done>
  <what-built>
    Migración 057 (tabla puente `professional_services` + RLS + 4 policies + índice inverso), la
    interface `ProfessionalService` y el helper puro con tests. Falta la validación de integridad de la
    migración: que replaye limpio 001→057 sobre el baseline local (PG17), que sea idempotente y ordene
    bien. Es el sustituto del proyecto para un push real — build/tsc pasan aun con una DB desactualizada
    porque los tipos salen del código, no de la DB viva; solo `supabase db reset` prueba el SQL.
  </what-built>
  <how-to-verify>
    1. En la terminal del repo, correr el reset del baseline local: `npx supabase db reset`.
       Debe terminar SIN error, replayando todas las migraciones numeradas hasta 057 inclusive sobre
       PG17 local (analytics/studio/storage off en Windows es esperado, no es fallo).
    2. Confirmar que 057 no rompió el orden ni chocó con constraints previos (el reset falla ruidoso si
       la 057 tiene un error de SQL, referencia una tabla inexistente, o duplica un nombre de policy).
    3. Confirmar que `supabase/schema.sql` contiene la sección `professional_services` (tabla + ENABLE
       RLS + 4 policies + índice) agregada quirúrgicamente y coherente con la migración.
    4. NO aplicar nada a la Supabase de PRODUCCIÓN: la 057 se aplica **a mano** al deploy, coordinada
       por el dueño (+ recarga del schema cache de PostgREST). Este plan NO incluye ningún comando que
       escriba en la Supabase remota.
    Responder "aprobado" si el reset local pasó limpio y el schema.sql refleja la tabla, o describir el
    error del reset.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" o pegá el error del reset</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño (sesión) → Supabase | El dueño autenticado escribe/lee `professional_services` con anon key + RLS; nunca service-role. |
| público (anon) → Supabase | La tabla puente NO tiene policy anon: el público jamás la lee en esta fase (constraint para Phase 10). |
| migración → prod | La 057 se aplica a mano, out-of-band; el flujo GSD nunca escribe en la Supabase remota. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | RLS SELECT de `professional_services` | mitigate | Policy `select USING (business_id IN businesses del owner)` — un negocio no puede leer el mapeo de otro; predicado idéntico a 042. |
| T-08-02 | Tampering / Elevation | RLS INSERT/UPDATE de `professional_services` | mitigate | `with check` por `business_id` (owner_id = auth.uid()) en insert y update: no se puede escribir ni mover una fila a otro tenant. FKs NOT NULL + PK compuesta. |
| T-08-03 | Elevation of Privilege | Superficie anon | mitigate | Sin policy anon (D-11): la puente nunca se abre a `anon`. Si la Phase 10 la necesita, será una vista acotada en migración posterior, nunca esta tabla. |
| T-08-04 | Tampering | Motor de reservas / canchas | mitigate | La 057 solo CREATE TABLE + policies + índice; no `create or replace function`, no ALTER sobre `professionals`/`appointments`, no toca `professionals.service_id` (043) ni constraints 011/013. Verificado por grep en acceptance. |
| T-08-05 | Tampering | Aplicación de la migración a prod | mitigate | Migración idempotente (`create table if not exists`, `drop policy if exists`), validada con `supabase db reset` local; aplicación a prod a mano y coordinada, nunca por el flujo GSD (D-11b). |
| T-08-SC | Tampering | Instalación de paquetes | accept | Esta fase NO instala paquetes (npm/pip/cargo) ni agrega dependencias (UI-SPEC "Registry Safety"): sin superficie de supply-chain nueva. |
</threat_model>

<verification>
- `npx supabase db reset` replaya 001→057 limpio en PG17 local (Task 3, blocking).
- `npm test` (vitest run) verde con la suite `staff-services`.
- `node ./node_modules/typescript/bin/tsc --noEmit` sin errores.
- Greps de acceptance de Task 1 (4 policies, índice inverso, sin `create or replace function`, tipo TS, schema.sql).
</verification>

<success_criteria>
- Existe la tabla puente `professional_services` con RLS + 4 policies por tenant + índice inverso, idempotente y validada con reset local (STAFF-01).
- El helper puro resuelve la regla del comodín y la cobertura en una sola implementación testeada (STAFF-02, STAFF-03).
- La 057 no toca el motor ni `professionals.service_id` — canchas intacto.
- `supabase/schema.sql` refleja la tabla; aplicación a prod queda como paso manual documentado.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/08-equipo-qu-servicios-hace-cada-profesional/08-01-SUMMARY.md` when done
</output>
