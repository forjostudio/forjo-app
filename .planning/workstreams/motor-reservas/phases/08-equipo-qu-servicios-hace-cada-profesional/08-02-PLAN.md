---
phase: 08-equipo-qu-servicios-hace-cada-profesional
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - app/(dashboard)/settings/settings-client.tsx
  - app/(dashboard)/equipo/page.tsx
  - app/(dashboard)/servicios/page.tsx
autonomous: true
requirements: [STAFF-01, STAFF-02, STAFF-03]

must_haves:
  truths:
    - "En /equipo el dueño marca servicios por profesional (chips), guarda al instante y al recargar el mapeo persiste; muchos-a-muchos (STAFF-01)."
    - "En /servicios el dueño ve, por servicio, quiénes lo ofrecen, y un servicio sin cobertura muestra un aviso claro de advertencia (STAFF-02)."
    - "El bloque de mapeo y la cobertura se ocultan con menos de 2 profesionales activos, y con 0 filas todos los profesionales se consideran capaces (STAFF-03)."
    - "En el vertical canchas ni el editor ni la cobertura se renderizan (D-18); el resto usa la terminología de term.resource/term.services."
    - "Desmarcar el último servicio de un profesional se guarda igual, con aviso; dejar un servicio sin nadie dispara un toast de advertencia en el momento (D-02/D-10)."
    - "D-03: un servicio nuevo NO se auto-asigna a nadie; una vez que el negocio tiene mapeo, el servicio nuevo arranca sin cobertura y la vista de cobertura (STAFF-02) lo señala — consecuencia del modelo + la regla del comodín all-or-nothing por negocio."
  prohibitions:
    - "D-04: esta fase NO modifica el alta manual de turno ni el alta de abonos; el mapeo NO restringe el panel del dueño (alimenta solo el booking público de las Phases 9/10). Verificación: ningún archivo de formularios del motor / alta manual / abonos aparece en files_modified (solo settings-client.tsx + los dos page.tsx)."
    - "D-15: cambiar el mapeo NO reescribe histórico ni toca abonos en curso (siguen con su profesional fijo); gobierna únicamente reservas públicas nuevas (consistente con D-04). Verificación: no hay writes a appointments/abonos/histórico en el diff de la fase."
  artifacts:
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "Bloque A (editor de chips optimista, view=equipo) + Bloque B (cobertura por servicio, view=servicios) + toggleProfessionalService/isServiceMapped + estado professionalServices"
      contains: "toggleProfessionalService"
    - path: "app/(dashboard)/equipo/page.tsx"
      provides: "Read-path: carga services + professional_services por tenant y los pasa como props"
      contains: "professional_services"
    - path: "app/(dashboard)/servicios/page.tsx"
      provides: "Read-path: carga professional_services por tenant y lo pasa como prop"
      contains: "professional_services"
  key_links:
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "professional_services (tabla)"
      via: "toggleProfessionalService escribe con browser client + RLS + .eq('business_id', ...)"
      pattern: "from\\('professional_services'\\)"
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "lib/staff-services.ts"
      via: "Bloque B (cobertura) y los toasts D-02/D-10 consumen el helper; no reimplementan la regla"
      pattern: "professionalsForService|isServiceCovered"
    - from: "app/(dashboard)/equipo/page.tsx"
      to: "professional_services (tabla)"
      via: "Promise.all carga services + professional_services scoped por business_id"
      pattern: "professional_services"
---

<objective>
Cablear las DOS superficies de la Phase 8 dentro del componente que ya sirve las dos vistas
(`SettingsClient` vía prop `view`): el **editor del mapeo** en `/equipo` (Bloque A, chips inline
optimistas, D-05/D-06) y la **cobertura por servicio** en `/servicios` (Bloque B, solo lectura,
D-08), más ampliar los dos read-paths para que carguen el mapeo (y los servicios en `/equipo`, que
hoy pasa `initialServices={[]}`). Cero componentes nuevos, cero dependencias nuevas — es el patrón
ya probado de `toggleAgendaSpace` con `services` en lugar de `spaces`.

Purpose: darle al dueño la config real ("qué hace cada persona") y la lectura inversa ("quién ofrece
cada servicio", con el hueco de cobertura visible). La escritura es una acción autenticada del dueño
(anon key + RLS + `.eq('business_id')`), nunca service-role. Sin tocar el motor de reservas ni las
superficies públicas.

Output: `settings-client.tsx` (Bloque A + Bloque B), `equipo/page.tsx` y `servicios/page.tsx`
read-paths ampliados.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/08-equipo-qu-servicios-hace-cada-profesional/08-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/08-equipo-qu-servicios-hace-cada-profesional/08-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/08-equipo-qu-servicios-hace-cada-profesional/08-UI-SPEC.md
@.claude/skills/convenciones-forjo/SKILL.md
@app/(dashboard)/settings/settings-client.tsx
@app/(dashboard)/equipo/page.tsx
@app/(dashboard)/servicios/page.tsx
@lib/staff-services.ts
</context>

## Artifacts this phase produces (Plan 02)

| Símbolo | Tipo | Archivo |
|---------|------|---------|
| prop `initialProfessionalServices?: ProfessionalService[]` + estado `professionalServices` | prop/state React | `app/(dashboard)/settings/settings-client.tsx` |
| `isServiceMapped(professionalId, serviceId)` | helper local | idem |
| `toggleProfessionalService(professionalId, serviceId)` | acción optimista (write) | idem |
| Bloque A: Card "Qué {servicios} hace cada {profesional}" (chips) | UI editor (view=equipo) | idem |
| Bloque B: línea "Lo hacen: …" + badge/línea "Sin cobertura" | UI cobertura (view=servicios) | idem |
| read-path con `services` + `professional_services` + prop `initialProfessionalServices` | server page | `app/(dashboard)/equipo/page.tsx` |
| read-path con `professional_services` + prop `initialProfessionalServices` | server page | `app/(dashboard)/servicios/page.tsx` |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: settings-client — prop/estado + toggleProfessionalService + Bloque A (editor de chips, view=equipo)</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - app/(dashboard)/settings/settings-client.tsx (líneas 101-156: type `SettingsView`, `Props`, destructuring del componente — dónde declarar `initialProfessionalServices` junto a `initialAgendaSpaces`; 195-199: `term`/`isCanchas`; 580-648: `agendaSpaces` state + `isMapped` + `toggleAgendaSpace` — EL patrón a espejar; 1343-1416: la tab `professionals` y la Card de CRUD; 1418-1520: la Card de espacios compartidos con gates y chips — molde de la Card nueva)
    - lib/staff-services.ts (helper del Plan 01: `isServiceCovered` para el toast D-10)
    - .planning/.../08-UI-SPEC.md (líneas 116-221: Bloque A completo — ubicación como 3ra Card, gates 1/2/3, anatomía, contrato exacto del chip con `focus-visible`, estados, avisos D-02/D-10 y su regla de precedencia; 303-333: Copywriting Contract y regla de género de `term.services`)
    - lib/types.ts (interface `ProfessionalService` del Plan 01)
  </read_first>
  <action>
    En `settings-client.tsx`:
    (1) Agregar a `Props` la prop opcional `initialProfessionalServices?: ProfessionalService[]` junto a
    `initialAgendaSpaces` (importar `ProfessionalService` desde `@/lib/types`), con default `[]` en la
    desestructuración del componente (línea ~156). Agregar un `useState` `professionalServices` /
    `setProfessionalServices` junto a `agendaSpaces` (~línea 584).
    (2) Crear `isServiceMapped(professionalId, serviceId)` (espejo de `isMapped`) y
    `toggleProfessionalService(professionalId, serviceId)` (espejo EXACTO de `toggleAgendaSpace`, líneas
    617-648): escritura optimista con rollback contra `supabase.from('professional_services')`, con
    `.eq('business_id', business.id).eq('professional_id', ...).eq('service_id', ...)` en el delete
    (defensa en profundidad) y `insert({ business_id, professional_id, service_id })` en el alta; en
    error, rollback del estado exacto + `toast.error` con el copy del UI-SPEC. Escritura por el **browser
    client con RLS**, NUNCA service-role.
    (3) Avisos al desmarcar (después de aplicar la escritura optimista), UN solo toast por toggle con la
    precedencia del UI-SPEC (D-10 gana sobre D-02; si falla, solo `toast.error`): si al desmarcar algún
    servicio quedó sin ningún profesional capaz → `toast.warning` (D-10, copy: nadie ofrece "{servicio}");
    si no, si el profesional quedó con 0 servicios marcados → `toast.info` (D-02, copy: vuelve a ofrecerse
    para todo). Calcular la condición D-10 consumiendo `isServiceCovered` de `@/lib/staff-services` sobre
    el estado post-write (profesionales ACTIVOS) — NO reimplementar la regla. Marcar (no desmarcar) nunca
    dispara toast.
    (4) Bloque A: nueva **tercera Card** dentro de `<TabsContent value="professionals">`, entre la Card
    "Profesionales del equipo" y la Card "Espacios físicos compartidos", `<Card className="p-6 space-y-4 mt-4">`.
    Gates en orden (todos): (1) NO renderizar si `isCanchas` (D-18); (2) NO renderizar si
    `professionals.filter(p => p.active).length < 2` (D-07, sin empty state); (3) si `services.length === 0`
    renderizar header + línea guía (molde de la guía de `professionals.length === 0` en 1479-1483). Listar
    solo profesionales ACTIVOS; los chips = todos los `services` en orden `created_at` (incluidos
    inactivos). El chip = copia textual del chip de espacios (1495-1510) con el ÚNICO agregado
    `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`; envolver la fila de chips en
    `<div role="group" aria-label={...}>`; icono `Check` con `aria-hidden`. Estado comodín "Hace todo" en
    `text-[11px]` cuando el profesional tiene 0 filas. Copy y terminología DESDE `term` (usar
    `resourceWord`/`resourcesWord` = `term.resource/resources` y `term.services`), evitando el artículo
    antes de `term.services` (regla de género del UI-SPEC). Sin spinner/disabled (optimista instantáneo).
    Reusar exactamente los tokens/escala del UI-SPEC (no valores nuevos: `h-8`, `gap-2`, `rounded-full`,
    tokens de color por clase, cero hex hardcodeado).
  </action>
  <acceptance_criteria>
    - `grep -c 'toggleProfessionalService' app/(dashboard)/settings/settings-client.tsx` ≥ 2 (definición + uso en el chip) y `grep -c "from('professional_services')" app/(dashboard)/settings/settings-client.tsx` ≥ 2 (insert + delete).
    - El delete de `professional_services` está scoped por tenant (incluye business_id + professional_id + service_id en el encadenado de filtros, espejo de `toggleAgendaSpace`) y NO usa service-role (`grep -ci 'createAdminClient\|service_role' app/(dashboard)/settings/settings-client.tsx` == 0).
    - El toast D-10 se deriva de `isServiceCovered` importado de `@/lib/staff-services` (`grep -c 'staff-services' app/(dashboard)/settings/settings-client.tsx` ≥ 1).
    - El chip nuevo incluye `focus-visible:ring-2 focus-visible:ring-ring` y `role="group"` para la fila.
    - Comportamiento: en un negocio con ≥2 profesionales activos, marcar un chip lo pinta al instante y persiste tras recargar `/equipo`; con vertical canchas la Card no aparece; con 1 profesional activo la Card no aparece; con 0 servicios aparece solo el header + la línea guía.
    - `node ./node_modules/typescript/bin/tsc --noEmit` sin errores.
  </acceptance_criteria>
  <verify>
    <automated>grep -qc 'toggleProfessionalService' 'app/(dashboard)/settings/settings-client.tsx' && grep -qc 'staff-services' 'app/(dashboard)/settings/settings-client.tsx' && node ./node_modules/typescript/bin/tsc --noEmit && echo OK</automated>
  </verify>
  <done>El editor de chips (Bloque A) guarda al instante, optimista con rollback, gateado por canchas/<2 activos/sin servicios, con toasts D-02/D-10 derivados del helper; escritura autenticada por RLS, no service-role.</done>
</task>

<task type="auto">
  <name>Task 2: settings-client — Bloque B (cobertura por servicio, view=servicios, solo lectura)</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - app/(dashboard)/settings/settings-client.tsx (líneas 1226-1261: el `map` de servicios de la rama no-canchas donde se agregan el badge y la línea; 195-199: `isCanchas`/`term`; 1664-1666 según UI-SPEC: los tokens del aviso de MercadoPago `border-warning/30 bg-warning/10 text-warning` a reusar)
    - lib/staff-services.ts (`professionalsForService`, `isServiceCovered` — la fuente de la lista y del booleano; NO reimplementar)
    - .planning/.../08-UI-SPEC.md (líneas 225-299: Bloque B completo — dos agregados quirúrgicos a la fila existente, gates 1+2, anatomía con/ sin cobertura, badge y línea de acción con `role="status"`, `' · '` como joiner, `[p.name, p.last_name].filter(Boolean).join(' ')`, texto plano sin pills; 316-329: copy y prohibición de afirmar que "no se puede reservar")
  </read_first>
  <action>
    En el `map` de servicios de `settings-client.tsx` (rama `isCanchas === false`, ~1230-1261), agregar de
    forma QUIRÚRGICA (sin reescribir la fila) los dos elementos del UI-SPEC, ambos gateados por: (1) NO
    `isCanchas` (D-18 — `/servicios` no redirige por vertical, así que el gate acá es la única defensa) y
    (2) `professionals.filter(p => p.active).length >= 2` (derivado de D-07):
    (a) un **badge** "Sin cobertura" al lado del nombre del servicio (envolver nombre+badge en
    `flex items-center gap-2 min-w-0`, nombre conserva `truncate`, badge `flex-shrink-0`) con icono
    `TriangleAlert` (`aria-hidden`) y tokens `border-warning/30 bg-warning/10 text-warning`, SOLO cuando el
    servicio está sin cobertura;
    (b) una **línea nueva** (la última de la fila): si el servicio tiene cobertura, `Lo hacen: {nombres}`
    en `text-[11px]` con joiner `' · '`; si no, la línea de acción con `role="status"`, icono
    `TriangleAlert` y texto "Nadie lo ofrece — asignalo en Equipo" con "Equipo" como `<Link href="/equipo">`.
    La lista de nombres y el booleano de cobertura salen de `professionalsForService(s.id, activos, professionalServices)`
    e `isServiceCovered(...)` de `@/lib/staff-services` (activos = `professionals.filter(p => p.active)`);
    PROHIBIDO reimplementar la regla del comodín en el componente. Nombres con
    `[p.name, p.last_name].filter(Boolean).join(' ')`. Texto plano, sin pills (la cobertura es solo
    lectura). Copy DESDE el Copywriting Contract; el copy NO puede afirmar que un servicio sin cobertura
    "no se puede reservar" (en esta fase el mapeo todavía no afecta la reserva pública). Cero hex
    hardcodeado; solo tokens por clase.
  </action>
  <acceptance_criteria>
    - `grep -c 'professionalsForService\|isServiceCovered' app/(dashboard)/settings/settings-client.tsx` ≥ 1 en la lógica de cobertura (regla consumida, no reimplementada).
    - `grep -c 'Sin cobertura' app/(dashboard)/settings/settings-client.tsx` ≥ 1 (badge) y `grep -c 'role="status"' app/(dashboard)/settings/settings-client.tsx` ≥ 1 (línea de acción).
    - El badge y la línea usan tokens `text-warning`/`bg-warning/10`/`border-warning/30` (sin hex).
    - Comportamiento: con ≥2 profesionales activos, un servicio que hace al menos uno muestra "Lo hacen: {nombres}"; un servicio que no cubre nadie muestra el badge "Sin cobertura" + la línea con link a Equipo; con un comodín (profesional de 0 filas) presente, ningún servicio muestra "Sin cobertura"; en vertical canchas no aparece ni badge ni línea; con <2 activos no aparece nada.
    - `node ./node_modules/typescript/bin/tsc --noEmit` sin errores.
  </acceptance_criteria>
  <verify>
    <automated>grep -qc 'professionalsForService\|isServiceCovered' 'app/(dashboard)/settings/settings-client.tsx' && grep -qc 'role="status"' 'app/(dashboard)/settings/settings-client.tsx' && node ./node_modules/typescript/bin/tsc --noEmit && echo OK</automated>
  </verify>
  <done>La cobertura (Bloque B) lista quiénes ofrecen cada servicio y marca el hueco con badge + línea de advertencia, consumiendo el helper; gateada por canchas/<2 activos; solo lectura, sin afirmar impacto en la reserva pública.</done>
</task>

<task type="auto">
  <name>Task 3: Read-paths — cargar services + professional_services por tenant y pasarlos como props</name>
  <files>app/(dashboard)/equipo/page.tsx, app/(dashboard)/servicios/page.tsx</files>
  <read_first>
    - app/(dashboard)/equipo/page.tsx (todo: hoy pasa `initialServices={[]}` en línea 31 y no carga `professional_services`; el `Promise.all` de líneas 22-26 y el redirect por canchas en 18 que NO se toca)
    - app/(dashboard)/servicios/page.tsx (todo: `Promise.all` de líneas 19-25; NO redirige por vertical — comentario 14-18)
    - .planning/.../08-PATTERNS.md (líneas 121-141: contrato exacto de los dos read-paths — qué agregar al Promise.all y qué props pasar)
    - .planning/.../08-UI-SPEC.md (líneas 365-374: "Datos que la UI necesita" — qué falta en cada vista)
  </read_first>
  <action>
    En `app/(dashboard)/equipo/page.tsx`: agregar al `Promise.all` (líneas 22-26) dos cargas nuevas,
    ambas `.eq('business_id', business.id)` (defensa en profundidad + RLS): `services` (con `.order('created_at')`)
    y `professional_services`. Cambiar `initialServices={[]}` por `initialServices={services || []}` y
    agregar la prop `initialProfessionalServices={professionalServices || []}`. NO tocar el redirect por
    canchas de la línea 18 (gate primario de D-18). Usar `select('*')` en `services` (igual que las otras
    cargas; no sobre-seleccionar).
    En `app/(dashboard)/servicios/page.tsx`: agregar al `Promise.all` (líneas 19-25) la carga de
    `professional_services` con `.eq('business_id', business.id)` y pasar
    `initialProfessionalServices={professionalServices || []}`. Mantener el comentario/behavior de "no
    redirige por vertical acá" (14-18) — el gate de canchas de la cobertura vive dentro del componente.
    Ambas páginas ya resuelven `business` por `owner_id`; reusar ese `business.id`.
  </action>
  <acceptance_criteria>
    - `grep -c "professional_services" app/(dashboard)/equipo/page.tsx` ≥ 1 y `grep -c 'initialServices={services' app/(dashboard)/equipo/page.tsx` == 1 (ya no es `{[]}`).
    - `grep -c "professional_services" app/(dashboard)/servicios/page.tsx` ≥ 1 y ambas páginas pasan `initialProfessionalServices=`.
    - Las dos cargas nuevas incluyen `.eq('business_id', business.id)` (grep del patrón presente en ambas líneas nuevas).
    - El redirect por canchas de `equipo/page.tsx` (línea ~18) sigue presente sin cambios.
    - `node ./node_modules/typescript/bin/tsc --noEmit` sin errores.
  </acceptance_criteria>
  <verify>
    <automated>grep -qc 'professional_services' 'app/(dashboard)/equipo/page.tsx' && grep -qc 'professional_services' 'app/(dashboard)/servicios/page.tsx' && grep -qc 'initialProfessionalServices' 'app/(dashboard)/equipo/page.tsx' && node ./node_modules/typescript/bin/tsc --noEmit && echo OK</automated>
  </verify>
  <done>Los dos read-paths cargan el mapeo (y /equipo también los servicios) por tenant y los pasan como props; el editor y la cobertura rinden sin fetch en cliente; el redirect por canchas de /equipo intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño (sesión) → Supabase | El editor escribe `professional_services` con el browser client (anon key + RLS + `.eq('business_id')`), nunca service-role. |
| read-path (server) → Supabase | Los dos `page.tsx` cargan con la sesión del dueño (RLS) + filtro explícito por `business_id`. |
| público (anon) | Fuera de alcance: esta fase no toca ninguna superficie pública; la puente no se expone a `anon`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering / Elevation (IDOR) | `toggleProfessionalService` (write) | mitigate | Los `professional_id`/`service_id` provienen SOLO de `professionals` y `services` cargados scoped por `business_id` en el read-path — el cliente no puede introducir un ID de otro tenant desde la UI. Además RLS `with check` por `business_id` (Plan 01) y `.eq('business_id', business.id)` en el delete. |
| T-08-07 | Spoofing / Elevation | Plano de escritura | mitigate | La escritura usa el browser client autenticado (anon key + RLS), NUNCA `createAdminClient`/service-role — verificado por grep en acceptance. |
| T-08-08 | Information Disclosure | Read-paths de `/equipo` y `/servicios` | mitigate | Ambas cargas `.eq('business_id', business.id)` + RLS; se amplía `initialServices` en `/equipo` con `select('*')` como las demás (sin sobre-selección de otro tenant). |
| T-08-09 | Tampering | Superficies públicas / motor | mitigate | El plan solo toca `settings-client.tsx` + los dos `page.tsx` autenticados; no toca booking público, `book_slot_atomic`, availability ni `nuevo-turno-form` (D-04). Cero regresión del motor. |
| T-08-10 | Elevation of Privilege | Gate por vertical canchas | mitigate | Editor y cobertura gateados por `isCanchas` (D-18); `/equipo` además redirige antes de las queries (gate primario). Evita mostrar canchas como "equipo" o cobertura donde no aplica. |
| T-08-SC | Tampering | Instalación de paquetes | accept | Sin instalaciones ni dependencias nuevas (UI-SPEC "Registry Safety"): cero superficie de supply-chain. |
</threat_model>

<verification>
- `node ./node_modules/typescript/bin/tsc --noEmit` sin errores tras los 3 tasks.
- Greps de acceptance: `toggleProfessionalService`, `from('professional_services')` (insert+delete), `staff-services`, `role="status"`, `professional_services` en los dos page.tsx, `initialProfessionalServices`, ausencia de service-role.
- Verificación manual (post-ejecución, UAT del checkpoint de fase): marcar servicios en `/equipo` y ver persistencia al recargar; ver "Lo hacen: …" y el badge "Sin cobertura" en `/servicios`; confirmar que en canchas y con <2 profesionales activos nada aparece.
</verification>

<success_criteria>
- El dueño configura qué hace cada profesional con chips que guardan al instante y persisten (STAFF-01).
- La cobertura por servicio muestra quiénes lo ofrecen y señala el servicio sin cobertura (STAFF-02).
- Con <2 profesionales activos o sin mapeo, todo funciona como hoy; canchas intacto (STAFF-03, D-18).
- La regla del comodín se consume del helper (Plan 01), no se reimplementa; escritura por RLS, no service-role.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/08-equipo-qu-servicios-hace-cada-profesional/08-02-SUMMARY.md` when done
</output>
