---
phase: 09-asignaci-n-autom-tica-at-mica-de-profesional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/058_professional_auto_assignment.sql
  - supabase/schema.sql
  - lib/booking-core.ts
autonomous: true
requirements: [ASIGN-02, ASIGN-03, ASIGN-04]
user_setup:
  - service: supabase-prod
    why: "La migración 058 (CREATE OR REPLACE book_slot_atomic) se aplica A MANO al Supabase de PROD coordinada con el deploy (D-09). Fuera del alcance automatizado de esta fase — NO se scriptea ningún push."
    dashboard_config:
      - task: "Ejecutar 058_professional_auto_assignment.sql en prod (psql / SQL editor) + luego NOTIFY pgrst, 'reload schema'; La última migración aplicada en prod es la 057."
        location: "Supabase prod, en el deploy que shippee la Phase 9"

must_haves:
  truths:
    - "book_slot_atomic conserva su firma de 14 parámetros y RETURNS TABLE (id, cancel_token) byte-idéntica → CREATE OR REPLACE puro, sin DROP FUNCTION (cero regresión de los 4 callers)"
    - "Con el UUID mágico ANY_PROFESSIONAL el RPC selecciona, BAJO el advisory lock, un profesional capaz (paridad-comodín con staff-services.ts) + de la sede + activo + libre, e inserta el professional_id REAL elegido — nunca el UUID mágico"
    - "Sin candidato capaz libre el RPC hace RAISE 'slot_taken' (D-10, el error de disponibilidad de siempre)"
    - "El advisory lock del slot se amplía a hash(business_id + date + time) — se quita v_bucket del hash (D-04); no degrada slot_full ni slot_taken (lock más grueso = más seguro)"
    - "Toda subquery nueva del RPC filtra por business_id = p_business_id explícito (D-08, SECURITY DEFINER: RLS no protege adentro)"
    - "lib/booking-core.ts expone autoAssign?: boolean y la constante ANY_PROFESSIONAL; los 4 callers actuales (que no setean el flag) se comportan byte-idéntico"
    - "supabase db reset replaya 001→058 limpio y las suites existentes de concurrencia + core pasan sin cambios (smoke de cero regresión)"
  artifacts:
    - path: "supabase/migrations/058_professional_auto_assignment.sql"
      provides: "book_slot_atomic modificado: lock ampliado + selección de candidato 'cualquiera' + UUID mágico"
      contains: "book_slot_atomic"
    - path: "supabase/schema.sql"
      provides: "snapshot regenerado del RPC book_slot_atomic (058)"
      contains: "book_slot_atomic"
    - path: "lib/booking-core.ts"
      provides: "flag autoAssign + constante ANY_PROFESSIONAL; re-checks JS envueltos en if (!autoAssign)"
      contains: "autoAssign"
  key_links:
    - from: "lib/booking-core.ts"
      to: "supabase/migrations/058_professional_auto_assignment.sql"
      via: "rpc('book_slot_atomic') con p_professional_id = ANY_PROFESSIONAL cuando autoAssign"
      pattern: "book_slot_atomic"
    - from: "supabase/migrations/058_professional_auto_assignment.sql"
      to: "lib/staff-services.ts"
      via: "query de candidatos con paridad-comodín exacta (NOT EXISTS / EXISTS sobre professional_services)"
      pattern: "professional_services"
---

<objective>
Modificar el núcleo anti-doble-booking `book_slot_atomic` (migración **058**, `CREATE OR REPLACE FUNCTION`) para que, cuando una reserva NO elige profesional ("cualquiera"), el RPC elija **DENTRO de la misma transacción `SECURITY DEFINER`** un profesional libre y capaz — el de menos turnos ese día — sin carreras (D-05). Y ampliar `lib/booking-core.ts` con el flag aditivo `autoAssign` que señala esa intención al RPC.

Purpose: es el corazón de integridad del producto (v0.9/v0.12). Este plan escribe el motor y lo aplica al Supabase LOCAL; la verificación bajo concurrencia real y la cero-regresión de los 4 caminos las hace el Plan 09-02.
Output: `058_professional_auto_assignment.sql`, `supabase/schema.sql` regenerado, `lib/booking-core.ts` con `autoAssign` + `ANY_PROFESSIONAL`, y la 058 aplicada en la DB local (`supabase db reset`).

Implementa ASIGN-02 (asigna un capaz libre), ASIGN-03 (atómico bajo el advisory lock) y ASIGN-04 (menos turnos ese día). Honra D-01..D-12 (ver 09-CONTEXT.md).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/09-asignaci-n-autom-tica-at-mica-de-profesional/09-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/09-asignaci-n-autom-tica-at-mica-de-profesional/09-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/09-asignaci-n-autom-tica-at-mica-de-profesional/09-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Símbolos que la Phase 9 crea (los produce este plan salvo donde se indica 09-02):
- `supabase/migrations/058_professional_auto_assignment.sql` — `CREATE OR REPLACE FUNCTION book_slot_atomic` con lock ampliado + selección de candidato **(este plan)**.
- Comportamiento nuevo de `book_slot_atomic`: rama "cualquiera" que elige profesional bajo el lock **(este plan)**.
- El parámetro/flag `autoAssign` en `CreateAppointmentInput` (`lib/booking-core.ts`) **(este plan)**.
- La constante UUID centinela mágico `ANY_PROFESSIONAL = '00000000-0000-0000-0000-000000000001'` **(este plan)**.
- El fixture de test `seedProfessionalService` en `test/helpers/booking-fixtures.ts` **(Plan 09-02)**.
- El archivo de tests de concurrencia/asignación `test/staff-assignment.test.ts` **(Plan 09-02)**.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migración 058 — book_slot_atomic con lock ampliado + selección de candidato "cualquiera"</name>
  <files>supabase/migrations/058_professional_auto_assignment.sql, supabase/schema.sql</files>
  <read_first>
    - supabase/migrations/042_spaces_and_coupled_exclusion.sql (el cuerpo VIGENTE de book_slot_atomic — copiarlo como base y modificarlo, NO reescribir de cero; firma en :126-141, lock slot+bucket en :154-155, bloque de espacio :162-196, count/seat/insert :201-236, OWNER/GRANT :240-244)
    - supabase/migrations/041_time_blocks_capacity_and_seat.sql (definición base: índice 011, EXCLUDE 013, count vs capacity, sentinel de bucket)
    - lib/staff-services.ts (:43-53 professionalsForService — la regla del comodín canónica a espejar 1:1 en SQL)
    - supabase/migrations/057_professional_services.sql (la tabla puente + índice professional_services_by_service que sirve el EXISTS)
    - 09-RESEARCH.md §"Gray Area 1" (lock ampliado), §"Gray Area 2" (query de candidatos completa, :179-222), §"Gray Area 3" / §"Secuencia final del RPC" (:271-288), Pitfalls 1-6
    - 09-PATTERNS.md §"supabase/migrations/058_*.sql" (orden de bloques + cambios que introduce 058)
  </read_first>
  <action>
    Crear `058_professional_auto_assignment.sql` con `CREATE OR REPLACE FUNCTION "public"."book_slot_atomic"(...)`. Copiar la firma de 14 parámetros y el `RETURNS TABLE ("id" uuid, "cancel_token" uuid) LANGUAGE plpgsql SECURITY DEFINER SET search_path = public` **byte-idénticos** a los de 042:126-142 (mantener `CREATE OR REPLACE` puro; recrear la función NO está permitido — cambiar params o RETURNS obliga a recrearla y rompe los 4 callers, Pitfall 5). Re-emitir el `ALTER FUNCTION ... OWNER TO "postgres"` y el `GRANT EXECUTE ... TO "anon","authenticated","service_role"` con la firma completa de 14 tipos (042:240-244). Encabezar con comentario denso en español (molde 042:1-40): "Contexto (Phase 9, ASIGN-02/03/04)", "Qué hace", "Qué NO hace (invariantes)", citando D-04/D-05/D-06/D-07/D-08/D-10.

    Cambios sobre el cuerpo de 042:
    1. **DECLARE nuevos:** `v_effective_pro uuid := p_professional_id;` `v_is_any boolean := (p_professional_id = '00000000-0000-0000-0000-000000000001'::uuid);` (el UUID mágico "cualquiera", DISTINTO del sentinel cero de "sin profesional").
    2. **Lock ampliado (§GA1, D-04):** el `pg_advisory_xact_lock` del slot pasa a hashear `p_business_id::text || p_date::text || p_time::text` — SIN `v_bucket::text`. Todo lo demás del lock (forma de un argumento, seed 0, locks de espacio posteriores) queda igual. Justificar en comentario por qué un lock más grueso no degrada slot_full/slot_taken.
    3. **Selección de candidato (§GA2), DESPUÉS del lock y ANTES del bloque de espacio:** `IF v_is_any THEN SELECT p.id INTO v_effective_pro FROM professionals p WHERE ... ORDER BY (<count carga>) ASC, p.created_at ASC, p.id ASC LIMIT 1;` con — usar la query completa de 09-RESEARCH.md §GA2:179-222 —: filtro `p.business_id = p_business_id` (D-08), `p.active = true` (D-07), `p.service_id IS NULL` (excluir canchas, Pitfall 6), filtro de sede `(p.location_id = p_location_id OR p.location_id IS NULL)` (D-07/D-13), paridad-comodín exacta (`NOT EXISTS ninguna fila en professional_services del pro` **OR** `EXISTS una fila de ese service_id` — 1:1 con staff-services.ts:48-52, ambas subqueries con `ps.business_id = p_business_id`), y "libre" = `NOT EXISTS` de turno solapado del pro ese día con guarda de holds vigentes `(a.status='confirmed' OR a.expires_at IS NULL OR a.expires_at > now())` (Pitfall 4). El `ORDER BY` cuenta la carga = turnos NO cancelados del pro ese día completo, TODAS las sedes/servicios, misma guarda de holds (D-02/D-03). Tras el SELECT: `IF v_effective_pro IS NULL THEN RAISE EXCEPTION 'slot_taken' USING ERRCODE = 'P0001'; END IF;` (D-10). `END IF;`
    4. **Recompute y uso de v_effective_pro:** recomputar `v_bucket := COALESCE(v_effective_pro, '00000000-0000-0000-0000-000000000000'::uuid)` (literal byte-idéntico al índice 011, Pitfall 1). A partir de la selección, TODO uso de `p_professional_id` en el cuerpo usa `v_effective_pro`: la resolución/exclusión de espacios del bloque 042 (el `array_agg` keya por `v_effective_pro`, el `<>` de self-exclusión también), y la columna `professional_id` del INSERT. `p_professional_id` solo se usa antes de la selección (para detectar el mágico) y **nunca** se inserta. Para los 4 callers actuales `v_effective_pro = p_professional_id` → comportamiento byte-idéntico.

    Regenerar `supabase/schema.sql` quirúrgicamente: reemplazar SOLO el bloque `CREATE OR REPLACE FUNCTION book_slot_atomic ...` vigente por el nuevo (NO dump completo — el CLI reordena el archivo entero; patrón 042/057). No tocar nada más del schema.
  </action>
  <acceptance_criteria>
    - La firma se mantiene byte-idéntica: `grep -c 'RETURNS TABLE ("id" uuid, "cancel_token" uuid)' supabase/migrations/058_professional_auto_assignment.sql` == 1, y el `ALTER FUNCTION ... (uuid, uuid, uuid, uuid, date, time without time zone, integer, uuid, text, text, text, text, text, timestamp with time zone)` de 14 tipos aparece (OWNER + GRANT).
    - No se recrea la función: `grep -ciE '^[[:space:]]*DROP[[:space:]]+FUNCTION' supabase/migrations/058_professional_auto_assignment.sql` == 0.
    - Lock ampliado: la línea del `pg_advisory_xact_lock` del slot hashea `p_business_id::text || p_date::text || p_time::text` y NO contiene `v_bucket` en esa expresión (verificable leyendo la sentencia del lock del slot).
    - El UUID mágico está presente y es distinto del sentinel: `grep -c '00000000-0000-0000-0000-000000000001' supabase/migrations/058_professional_auto_assignment.sql` >= 1.
    - Paridad-comodín: la query de candidatos usa `professional_services` con `ps.business_id = p_business_id` en ambas subqueries (D-08) y refleja `NOT EXISTS ... OR EXISTS ...` de staff-services.ts:48-52.
    - `p.service_id IS NULL` (excluye canchas) y `(p.location_id = p_location_id OR p.location_id IS NULL)` (sede) presentes en la selección.
    - `RAISE EXCEPTION 'slot_taken'` cuando `v_effective_pro IS NULL` (D-10).
    - `supabase/schema.sql` contiene el cuerpo nuevo del RPC (mismo lock ampliado + UUID mágico) y sigue teniendo una sola definición de `book_slot_atomic`.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'RETURNS TABLE ("id" uuid, "cancel_token" uuid)' supabase/migrations/058_professional_auto_assignment.sql && grep -q '00000000-0000-0000-0000-000000000001' supabase/migrations/058_professional_auto_assignment.sql && ! grep -qiE '^[[:space:]]*DROP[[:space:]]+FUNCTION' supabase/migrations/058_professional_auto_assignment.sql</automated>
  </verify>
  <done>058 escrita con firma/RETURNS byte-idénticos, lock ampliado (sin v_bucket), selección de candidato con paridad-comodín + sede + activos + libre + orden por carga, RAISE slot_taken sin candidato, v_effective_pro usado en espacio/count/insert (nunca el UUID mágico), y schema.sql regenerado quirúrgicamente.</done>
</task>

<task type="auto">
  <name>Task 2: lib/booking-core.ts — flag aditivo autoAssign + constante ANY_PROFESSIONAL</name>
  <files>lib/booking-core.ts</files>
  <read_first>
    - lib/booking-core.ts (el core completo; SENTINEL :15, CreateAppointmentInput :26-47, resolución de proId :93-104, re-checks JS :106-189, liberación de holds :196-208, llamada al RPC :244-261, traducción de errores :263-281)
    - 09-RESEARCH.md §"Gray Area 3" → "Cómo lo pasa el core" (:252-269) y §"Code Examples" (:401-412)
    - 09-PATTERNS.md §"lib/booking-core.ts" (puntos de inserción exactos)
  </read_first>
  <action>
    Extensión **aditiva** — cero cambio para los 4 callers actuales (ninguno setea el flag):
    - Agregar la constante de módulo `const ANY_PROFESSIONAL = '00000000-0000-0000-0000-000000000001'` junto al `SENTINEL` (:15), con comentario de que es DISTINTA del sentinel "sin profesional".
    - Agregar `autoAssign?: boolean` a `CreateAppointmentInput` (comentario: Phase 9 — "cualquiera", el RPC elige el profesional).
    - En `createAppointmentCore`: cuando `autoAssign === true`, **saltear** la resolución/validación anti-tampering de `professionalId` (:93-104) y en su lugar fijar el proId que va al RPC en `ANY_PROFESSIONAL`; y **envolver en `if (!autoAssign) { … }`** los re-checks JS que hoy son solo UX (re-check de bucket/solape, re-check de espacio compartido, re-check capacity-aware, y la liberación de holds per-bucket :106-208) — no son computables sin un bucket concreto y la autoridad es el RPC. Con `autoAssign` falsy, el flujo queda byte-idéntico al de hoy.
    - La llamada `.rpc('book_slot_atomic', { ... })` (:244-261) NO cambia de forma: los mismos 14 `p_*`; solo el valor de `p_professional_id` es `proId` real (flujo actual) o `ANY_PROFESSIONAL` (autoAssign). El bloque de traducción de errores `rpcErr` (:263-281) NO cambia — el `RAISE 'slot_taken'` de "sin candidato" (D-10) cae en la rama `slot_taken`→409 ya existente.
    - El anti-tampering de tenant sobre `service`/`location` (que sale por `business_id`) se mantiene intacto para AMBOS caminos: `autoAssign` NO acepta ninguna lista de profesionales del cliente (D-06); lo único que el caller manda para "cualquiera" es el boolean.
  </action>
  <acceptance_criteria>
    - `grep -c "autoAssign" lib/booking-core.ts` >= 2 (tipo + uso).
    - `grep -c "ANY_PROFESSIONAL = '00000000-0000-0000-0000-000000000001'" lib/booking-core.ts` == 1.
    - `grep -c "if (!autoAssign)" lib/booking-core.ts` >= 1 (los re-checks JS quedan gateados por el flag).
    - La llamada `supabase.rpc('book_slot_atomic'` conserva las mismas 14 claves `p_*` (ninguna clave nueva; solo cambia el valor de `p_professional_id`).
    - Type-check limpio: `npx tsc --noEmit` sin errores nuevos (nota: `npx tsc` puede salir 0 espuriamente — la prueba real de no-regresión es el smoke de la Task 3 y la suite del Plan 09-02).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "autoAssign" lib/booking-core.ts && grep -c "ANY_PROFESSIONAL = '00000000-0000-0000-0000-000000000001'" lib/booking-core.ts && grep -c "if (!autoAssign)" lib/booking-core.ts</automated>
  </verify>
  <done>booking-core.ts expone autoAssign + ANY_PROFESSIONAL; con autoAssign se saltean la validación de professionalId y los re-checks JS y se pasa el UUID mágico al RPC; con el flag falsy el core es byte-idéntico a hoy; la firma de la llamada rpc y la traducción de errores intactas.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar 058 a la DB LOCAL (supabase db reset) + smoke de cero regresión</name>
  <files>supabase/migrations/058_professional_auto_assignment.sql</files>
  <read_first>
    - test/concurrency.test.ts (la suite existente que NO debe cambiar de comportamiento tras 058)
    - test/env.ts / package.json (comando de tests; `hasSupabaseCreds` gatea las suites contra la DB local)
    - 09-RESEARCH.md §"Runtime State Inventory" / "Canon operativo (D-09)"
  </read_first>
  <action>
    **[BLOCKING] — este paso DEBE correr después de escribir la 058 (Task 1) y ANTES de cualquier tarea de tests (Plan 09-02).** Motivo: build/tsc pasan sin aplicar la SQL (los tipos son escritos a mano, no derivados de la DB), así que testear contra un RPC local viejo sería un falso positivo.
    - Correr `supabase db reset` (o `npx supabase db reset`) contra el Supabase LOCAL: replaya el baseline 001→058 en orden y aplica la 058. Verificar que el replay termina limpio (sin error de SQL) y que la 058 aparece aplicada (`supabase migration list --local` la lista).
    - Smoke de cero regresión (primera prueba de que 058 no rompió el núcleo, antes de escribir tests nuevos): correr las suites EXISTENTES `test/concurrency.test.ts` y `test/booking-core.test.ts` con `npm test` filtrando esos archivos. Deben pasar **sin cambios** (CONC-01 anti-sobrecupo, CONC-02 cupo 1, CUPOS-02/03, CONC-03 y ALQUILER-02 espacio compartido siguen determinísticos — el lock más grueso no los degrada).
    - NO scriptear ningún push a prod: el apply a prod de la 058 es un paso manual de deploy (ver `user_setup`). Documentarlo en el SUMMARY, no ejecutarlo.
  </action>
  <acceptance_criteria>
    - `supabase db reset` local termina sin error y `supabase migration list --local` incluye la 058.
    - `npm test` sobre `test/concurrency.test.ts` y `test/booking-core.test.ts` pasa (0 fallos) — smoke de cero regresión.
    - No existe ningún comando que aplique la 058 a prod (`supabase db push` / `--linked`) en el plan ni en scripts nuevos.
  </acceptance_criteria>
  <verify>
    <automated>npx supabase db reset && npm test -- test/concurrency.test.ts test/booking-core.test.ts</automated>
  </verify>
  <done>La 058 está aplicada en la DB local (replay 001→058 limpio) y las suites de concurrencia + core existentes pasan sin cambios; el apply a prod queda documentado como paso manual de deploy, no scripteado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente/público → route handler → createAppointmentCore | Entra input no confiable; el core resuelve tenant y re-valida service/location por business_id. Para "cualquiera" el cliente SOLO manda un boolean (`autoAssign`), nunca una lista de profesionales. |
| createAppointmentCore → RPC book_slot_atomic (SECURITY DEFINER) | Cruce a una función que bypassa RLS: dentro, el aislamiento por tenant depende de filtros `business_id = p_business_id` explícitos. |
| concurrencia entre transacciones | Dos reservas simultáneas del mismo instante compiten por el mismo advisory lock; la selección de candidato debe ver estado consistente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering / DoS (integridad) | selección de "cualquiera" bajo concurrencia (book_slot_atomic) | mitigate | La selección corre BAJO el `pg_advisory_xact_lock` ampliado a hash(business_id+date+time) (§GA1); el INSERT del pro elegido pasa por el índice 011 / EXCLUDE 013 / RPC 042 como backstop atómico. Ampliar el lock es estrictamente más seguro (más grueso serializa más). Verificación de carrera real = Plan 09-02 (ASIGN-03). |
| T-09-02 | Tampering (integridad) | firma/semántica del RPC compartida por los 4 callers | mitigate | Firma de 14 params + `RETURNS TABLE (id, cancel_token)` byte-idéntica → `CREATE OR REPLACE` puro (sin DROP FUNCTION); flag `autoAssign` aditivo (los 4 callers no lo setean → `v_effective_pro = p_professional_id` → byte-idéntico). Smoke en Task 3 + regresión completa en 09-02. |
| T-09-03 | Spoofing / Tampering | conjunto de candidatos de la vía "cualquiera" | mitigate | Candidatos derivados 100% server-side de `business_id` + `professional_services` (Phase 8); el cliente solo manda `autoAssign` boolean (D-06). Un `professionalId` ajeno no puede colarse por autoAssign (se saltea toda resolución de professionalId). |
| T-09-04 | Elevation of Privilege | queries nuevas dentro de SECURITY DEFINER | mitigate | RLS no protege adentro de la función: TODA subquery nueva (candidatos, carga, capaz-comodín) filtra por `business_id = p_business_id` explícito (D-08). |
| T-09-05 | DoS / Integridad | selección que "asigna a alguien ocupado" o se cae | mitigate | Guarda de holds vigentes `(status='confirmed' OR expires_at IS NULL OR expires_at > now())` en "libre" y en el conteo de carga (Pitfall 4); `RAISE 'slot_taken'` limpio si no hay candidato (D-10) — nunca sobre-reserva ni excepción no controlada. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala ningún paquete (09-RESEARCH: sin Package Legitimacy Audit; solo `supabase db reset` + `vitest`, ya presentes) → sin superficie de supply chain. |
</threat_model>

<verification>
- `supabase db reset` local replaya 001→058 limpio; la 058 queda aplicada.
- Grep de estructura: firma byte-idéntica, sin DROP FUNCTION, lock ampliado, UUID mágico presente, `autoAssign` + `ANY_PROFESSIONAL` en el core.
- Smoke de cero regresión: `test/concurrency.test.ts` + `test/booking-core.test.ts` pasan sin cambios tras 058.
- La verificación de atomicidad bajo concurrencia real (ASIGN-03), la selección del menos-cargado (ASIGN-04) y la regresión completa de los 4 caminos (D-11) las cubre el Plan 09-02 (secure-phase obligatorio, D-12).
</verification>

<success_criteria>
- 058 escrita con firma/RETURNS byte-idénticos (CREATE OR REPLACE puro), lock ampliado a (business_id+date+time), selección de candidato con paridad-comodín exacta + sede + activos + libre + orden por carga (D-01/D-02/D-03/D-07), RAISE slot_taken sin candidato (D-10), v_effective_pro insertado (nunca el UUID mágico), filtro business_id explícito en toda subquery (D-08).
- `lib/booking-core.ts` con `autoAssign` + `ANY_PROFESSIONAL`; los 4 callers byte-idénticos con el flag falsy.
- 058 aplicada en la DB local; smoke de concurrencia + core sin regresión.
- `supabase/schema.sql` regenerado quirúrgicamente con el RPC nuevo.
</success_criteria>

<output>
Crear `.planning/workstreams/motor-reservas/phases/09-asignaci-n-autom-tica-at-mica-de-profesional/09-01-SUMMARY.md` al terminar.
</output>
