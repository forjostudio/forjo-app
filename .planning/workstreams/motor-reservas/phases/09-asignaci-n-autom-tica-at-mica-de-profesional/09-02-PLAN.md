---
phase: 09-asignaci-n-autom-tica-at-mica-de-profesional
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - test/helpers/booking-fixtures.ts
  - test/staff-assignment.test.ts
autonomous: true
requirements: [ASIGN-02, ASIGN-03, ASIGN-04]

must_haves:
  truths:
    - "Una reserva autoAssign queda confirmada con un professional_id concreto, capaz del servicio y libre en ese horario — verificado leyendo la fila de appointments en la DB (ASIGN-02)"
    - "Dos autoAssign concurrentes al mismo horario terminan con dos professional_id DISTINTOS; si solo uno estaba libre → 1 ok + 1 slot_taken; occupantsAt exacto, nunca sobrecupo — verificado con Promise.all contra la DB (ASIGN-03)"
    - "Sin capaz libre, la reserva se rechaza con slot_taken (D-10)"
    - "Entre varios libres y capaces el turno cae en el de menos turnos ese día; empate → created_at asc (ASIGN-04)"
    - "El conjunto de candidatos respeta el mapeo professional_services (paridad-comodín) y la sede; un professionalId ajeno no entra por la vía autoAssign (aislamiento por tenant)"
    - "Cero regresión: las 4 suites del motor (concurrency, booking-core, manual-booking, booking-public-regression) + canchas + abono pasan SIN cambios (D-11)"
  artifacts:
    - path: "test/helpers/booking-fixtures.ts"
      provides: "fixture seedProfessionalService (inserta en professional_services con service-role)"
      contains: "seedProfessionalService"
    - path: "test/staff-assignment.test.ts"
      provides: "tests de asignación + carrera real (ASIGN-02/03/03b/04 + paridad-comodín + sede)"
      contains: "autoAssign"
  key_links:
    - from: "test/staff-assignment.test.ts"
      to: "lib/booking-core.ts"
      via: "createAppointmentCore({ autoAssign: true, ... }) → RPC book_slot_atomic"
      pattern: "autoAssign"
    - from: "test/helpers/booking-fixtures.ts"
      to: "professional_services"
      via: "seedProfessionalService inserta el mapeo staff↔servicio para probar mapeo explícito vs. comodín"
      pattern: "professional_services"
---

<objective>
Probar, **contra la DB local con la 058 aplicada**, que la asignación automática de "cualquiera" funciona y es atómica, y que los 4 caminos que comparten el motor no regresan (D-11, D-12). Es la fase de mayor riesgo del milestone: la garantía real vive en la DB, así que la verificación es por **carrera real** (`Promise.all`) y aserción **independiente** del estado de la DB — no lectura de código.

Purpose: cerrar ASIGN-02/03/04 con evidencia ejecutable y dejar listo el secure-phase obligatorio (D-12).
Output: `seedProfessionalService` en `test/helpers/booking-fixtures.ts` y `test/staff-assignment.test.ts` con los 6 tests de asignación + la corrida de regresión completa verde.

Depende de 09-01 (la 058 debe estar aplicada en la DB local y `autoAssign` debe existir en el core).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/phases/09-asignaci-n-autom-tica-at-mica-de-profesional/09-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/09-asignaci-n-autom-tica-at-mica-de-profesional/09-RESEARCH.md
@.planning/workstreams/motor-reservas/phases/09-asignaci-n-autom-tica-at-mica-de-profesional/09-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/09-asignaci-n-autom-tica-at-mica-de-profesional/09-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Símbolos que la Phase 9 crea (este plan produce los dos últimos; los tres primeros los produjo 09-01):
- `supabase/migrations/058_professional_auto_assignment.sql` — `book_slot_atomic` con lock ampliado + selección "cualquiera" (09-01).
- El flag `autoAssign` en `CreateAppointmentInput` (`lib/booking-core.ts`) (09-01).
- La constante UUID centinela mágico `ANY_PROFESSIONAL = '00000000-0000-0000-0000-000000000001'` (09-01).
- El fixture `seedProfessionalService` en `test/helpers/booking-fixtures.ts` **(este plan)**.
- El archivo de tests de concurrencia/asignación `test/staff-assignment.test.ts` **(este plan)**.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fixture seedProfessionalService en test/helpers/booking-fixtures.ts</name>
  <files>test/helpers/booking-fixtures.ts</files>
  <read_first>
    - test/helpers/booking-fixtures.ts (moldes existentes: seedProfessional :130-138, seedSpace :144-152, seedAgendaSpace :158-163 — puente sin id sintético, service-role insert, throw en error)
    - supabase/migrations/057_professional_services.sql (columnas de professional_services: business_id/professional_id/service_id NOT NULL, PK compuesta)
    - 09-PATTERNS.md §"test/helpers/booking-fixtures.ts"
  </read_first>
  <action>
    Agregar `export async function seedProfessionalService(seeded: SeededTenant, args: { professionalId: string; serviceId: string }): Promise<void>` con el molde EXACTO de `seedAgendaSpace` (:158-163): insertar en `professional_services` con `{ business_id: seeded.businessId, professional_id: args.professionalId, service_id: args.serviceId }` usando `seeded.admin` (service-role), sin retorno (PK compuesta), `throw new Error(...)` si `ins.error`. Comentario de una línea explicando que sirve para probar el mapeo EXPLÍCITO vs. la regla del comodín (0 filas). No tocar la firma de `seedOneTenant` ni de los otros helpers.
  </action>
  <acceptance_criteria>
    - `grep -c "export async function seedProfessionalService" test/helpers/booking-fixtures.ts` == 1.
    - Inserta en `professional_services` con `business_id` + `professional_id` + `service_id` (las 3 columnas NOT NULL de la 057) vía `seeded.admin`.
    - Lanza error si el insert falla (mismo patrón que seedAgendaSpace).
    - `npx tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "export async function seedProfessionalService" test/helpers/booking-fixtures.ts</automated>
  </verify>
  <done>seedProfessionalService disponible, molde de seedAgendaSpace, service-role, sin retorno, throw en error; firmas existentes intactas.</done>
</task>

<task type="auto">
  <name>Task 2: test/staff-assignment.test.ts — asignación + carrera real (ASIGN-02/03/03b/04 + paridad-comodín + sede)</name>
  <files>test/staff-assignment.test.ts</files>
  <read_first>
    - test/concurrency.test.ts (molde EXACTO: describe.skipIf(!hasSupabaseCreds) :25, seedOneTenant/beforeAll :29-34, teardown :36-38, afterEach limpieza :41-50, baseInput :56-71, occupantsAt :75-84, carrera Promise.all :101-104 y :259-262 con aserción dura + occupantsAt exacto)
    - test/helpers/booking-fixtures.ts (seedOneTenant, seedTimeBlock, seedProfessional, y el nuevo seedProfessionalService)
    - lib/booking-core.ts (CreateAppointmentInput con autoAssign; el core saltea professionalId bajo autoAssign)
    - 09-RESEARCH.md §"Gray Area 3" → matriz de tests (:300-307) y "Leer el pro asignado" (09-PATTERNS.md :129)
  </read_first>
  <action>
    Crear `test/staff-assignment.test.ts` copiando el esqueleto de `concurrency.test.ts`: `describe.skipIf(!hasSupabaseCreds)('asignación automática: cualquiera', ...)`, `beforeAll` con `seedOneTenant({ bufferMinutes: 0, serviceDurationMinutes: 30 })` (`supabase = t.admin`), `afterAll` con `teardownOneTenant`, y `afterEach` que limpia `appointments`, `time_blocks`, `agenda_spaces`, `spaces` **y además `professional_services`** por `business_id`. Fecha fija lunes (ej. `'2031-03-03'`, EXTRACT(dow)=1) alineada con `seedTimeBlock` default.
    - `baseInput()` estilo concurrency pero con `professionalId: null` y `autoAssign: true` (el core ignora professionalId bajo autoAssign).
    - Helper `assignedProAt(appointmentId)` (estilo `occupantsAt`): `SELECT professional_id FROM appointments WHERE id = <id>` con `t.admin`. El Phase 9 no cambia el RETURNS, así que el pro asignado se lee de la fila.
    Agregar los 6 tests (setup y aserción dura exactos en 09-RESEARCH.md §GA3:300-307):
    1. **ASIGN-02** (asigna un capaz libre): 2 pros activos comodín (0 filas en la puente), `seedTimeBlock` cap 1; un `autoAssign` en '09:00' → `result.ok`, `assignedProAt(id) ∈ {proA, proB}`, `occupantsAt('09:00') === 1`.
    2. **ASIGN-03** (dos concurrentes → distintos): 2 pros libres, cap 1; `Promise.all` de dos `autoAssign` en '09:00' → 2 ok, los dos `professional_id` DISTINTOS, `occupantsAt('09:00') === 2` (no 3).
    3. **ASIGN-03b** (queda uno → 1 ok + 1 slot_taken): 2 pros pero uno pre-ocupado en '09:00' (insert/alta previa a ese pro); `Promise.all` de dos `autoAssign` → 1 ok + 1 `slot_taken`, `occupantsAt('09:00') === 2` (el pre + 1).
    4. **ASIGN-04** (menos turnos ese día): 2 pros; proA con 2 turnos ese día en OTRAS horas (seedear appointments a proA vía admin), proB con 0; un `autoAssign` en '09:00' → cae en proB. Variante empate 0-0 → gana `created_at` más viejo (el pro sembrado primero).
    5. **paridad-comodín** (respeta el mapeo): 2º servicio sembrado; proA mapeado SOLO a ese otro servicio (`seedProfessionalService`), proB comodín; `autoAssign` del servicio del fixture → cae en proB, proA NUNCA elegido.
    6. **sede (D-07)**: 2ª location sembrada; proA con `location_id = L1` (update vía admin), proB sin location; reserva con `locationId = L2` → solo proB candidato (sin-sede vale para todas), proA excluido.
    Nota anti-tampering: en todos, la vía "cualquiera" solo pasa `autoAssign: true` — nunca una lista de professionalId; el pro sale de la selección server-side.
  </action>
  <acceptance_criteria>
    - `test/staff-assignment.test.ts` existe con `describe.skipIf(!hasSupabaseCreds)` y usa `createAppointmentCore({ autoAssign: true })`.
    - ASIGN-02: el appointment insertado tiene `professional_id` ∈ {proA, proB} y `occupantsAt === 1`.
    - ASIGN-03: `Promise.all` de dos autoAssign → dos `professional_id` distintos + `occupantsAt('09:00') === 2` (aserción contra la DB, no contra los retornos del core).
    - ASIGN-03b: 1 ok + 1 `slot_taken` (409); `occupantsAt === 2`.
    - ASIGN-04: el turno cae en el pro con menos turnos ese día; empate resuelto por `created_at asc` (determinístico).
    - paridad-comodín: el pro mapeado a otro servicio nunca se elige para el servicio pedido.
    - sede: el pro de otra location no se elige; el sin-location sí.
    - `npm test -- test/staff-assignment.test.ts` pasa (0 fallos) contra la DB local con 058 aplicada.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- test/staff-assignment.test.ts</automated>
  </verify>
  <done>Los 6 tests de asignación pasan contra la DB local: ASIGN-02/03/03b/04 verificados por estado de la DB (occupantsAt + assignedProAt), paridad-comodín y sede respetadas, carrera real por Promise.all — evidencia ejecutable de atomicidad y anti-tampering.</done>
</task>

<task type="auto">
  <name>Task 3: Regresión completa de los 4 caminos (D-11) — la suite entera verde</name>
  <files>test/staff-assignment.test.ts</files>
  <read_first>
    - 09-RESEARCH.md §"Regresión de los 4 caminos (D-11)" (:309-316) — qué suite cubre cada camino
    - test/concurrency.test.ts, test/manual-booking.test.ts, test/booking-public-regression.test.ts, test/booking-core.test.ts, test/canchas-booking.test.ts, test/abono-generation.test.ts (las suites que deben pasar SIN cambios)
  </read_first>
  <action>
    Correr la suite COMPLETA (`npm test`) contra la DB local con la 058 aplicada y confirmar **cero regresión** de los 4 caminos que comparten el motor (ninguno setea `autoAssign` → `v_effective_pro = p_professional_id` → byte-idéntico):
    - Profesional específico + cupos grupales → `test/concurrency.test.ts` (CONC-01/02, CUPOS-02/03), `test/booking-core.test.ts`.
    - Cancha / espacio compartido → `test/concurrency.test.ts` (CONC-03, ALQUILER-02) + `test/canchas-booking.test.ts`.
    - Alta manual autenticada → `test/manual-booking.test.ts`.
    - Ocurrencia de abono (generación forward) → `test/abono-generation.test.ts` (+ abono-create / abono-cron si aplican).
    - Core general / público → `test/booking-public-regression.test.ts`.
    Las suites existentes NO se editan (si alguna requiere un ajuste por la 058, es una regresión a investigar, no a "arreglar" el test). Registrar en el SUMMARY el total de tests verde (N/N) como evidencia para el secure-phase (D-12).
  </action>
  <acceptance_criteria>
    - `npm test` (suite completa) pasa: 0 fallos. Las 4 suites del motor + canchas + abono verdes SIN edición.
    - El nuevo `test/staff-assignment.test.ts` queda incluido en la corrida y verde.
    - El SUMMARY registra el conteo total de tests (N/N) y confirma que ninguna suite existente fue modificada para pasar.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>La suite completa pasa contra la DB local con 058: cero regresión de los 4 caminos (D-11) + los 6 tests de asignación nuevos verdes; evidencia ejecutable lista para secure-phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → createAppointmentCore → RPC book_slot_atomic (DB local con 058) | Los tests ejercitan la vía "cualquiera" contra la DB real; la aserción dura lee el estado de la DB (no los retornos del core). |
| concurrencia (Promise.all) | Dos reservas autoAssign simultáneas verifican que el advisory lock ampliado serializa sin doble-asignación ni sobrecupo. |
| aislamiento por tenant | Los candidatos salen de `business_id` + `professional_services`; los tests confirman que el mapeo/sede acotan el conjunto y que un pro ajeno no entra por autoAssign. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering / DoS (integridad) | atomicidad de la asignación bajo concurrencia | mitigate | ASIGN-03 con `Promise.all` real + aserción `occupantsAt` contra la DB: dos autoAssign concurrentes → dos pros distintos o 1 ok + 1 slot_taken, nunca sobrecupo. Verificación de carrera real (no lectura de código), exigida por D-12. |
| T-09-02 | Tampering (integridad) | regresión de los 4 caminos del RPC | mitigate | Task 3: la suite COMPLETA (concurrency, booking-core, manual-booking, booking-public-regression, canchas, abono) pasa SIN editar los tests → cero regresión (D-11). |
| T-09-03 | Spoofing / Tampering | conjunto de candidatos derivado del cliente | mitigate | Tests de paridad-comodín y de sede: el candidato sale de `professional_services` + business_id + location; los tests solo mandan `autoAssign: true`, nunca una lista de professionalId (D-06). |
| T-09-04 | Elevation of Privilege | cross-tenant dentro de SECURITY DEFINER | mitigate | El fixture crea un tenant aislado (prefijo único); la selección filtra por business_id → los tests confirman que no se elige un pro fuera del tenant/servicio/sede. |
| T-09-05 | DoS / Integridad | sin candidato libre → caída o asignación a ocupado | mitigate | ASIGN-03b: cuando solo queda uno libre, una confirma y la otra recibe `slot_taken` (D-10) — nunca excepción no controlada ni asignación a alguien ocupado. |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala ningún paquete (solo `vitest` + `supabase db reset`, ya presentes) → sin superficie de supply chain. |
</threat_model>

<verification>
- `npm test -- test/staff-assignment.test.ts` pasa: ASIGN-02/03/03b/04 + paridad-comodín + sede verdes contra la DB local con 058.
- `npm test` (suite completa) pasa: cero regresión de los 4 caminos (D-11).
- Todas las aserciones de carrera/atomicidad leen el estado de la DB (`occupantsAt`, `assignedProAt`), no los retornos del core — evidencia válida para secure-phase (D-12).
</verification>

<success_criteria>
- `seedProfessionalService` disponible (molde seedAgendaSpace, service-role).
- `test/staff-assignment.test.ts` con los 6 tests verdes: asignación de capaz libre (ASIGN-02), dos concurrentes → distintos / 1 ok + 1 slot_taken (ASIGN-03/03b), menos turnos ese día con desempate por created_at (ASIGN-04), paridad-comodín y sede respetadas (aislamiento por tenant).
- Suite completa verde sin editar suites existentes → cero regresión (D-11); conteo N/N registrado en el SUMMARY para el secure-phase obligatorio (D-12).
</success_criteria>

<output>
Crear `.planning/workstreams/motor-reservas/phases/09-asignaci-n-autom-tica-at-mica-de-profesional/09-02-SUMMARY.md` al terminar.
</output>
