---
phase: 16-correcciones-del-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/070_service_gates_direction_and_time_precision.sql
  - supabase/schema.sql
  - lib/appointment-time.ts
  - lib/types.ts
  - .planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-BASELINE-070.md
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]

must_haves:
  truths:
    - "Pasar un servicio de individual a grupal o simultaneo con turnos futuros vivos ya NO se rechaza (GATE-01, D-02)"
    - "Cambiar hacia individual, y grupal <-> simultaneo, con turnos futuros vivos SIGUE rechazando con service_mode_has_future_appointments (D-02)"
    - "Marcar completed un turno futuro ya no abre el gate de cambio de modo (GATE-02, D-03)"
    - "Un turno de hoy a hora ya pasada deja de bloquear el cambio de modo Y el borrado del servicio (GATE-03, D-04)"
    - "Un turno completed sigue sin bloquear el borrado del servicio: los dos predicados divergen a proposito y esta escrito (D-03)"
    - "El comportamiento viejo quedo MEDIDO antes del cambio y el nuevo medido despues con el mismo script (control negativo)"
    - "Los dos comentarios del codigo que la 070 vuelve falsos (lib/appointment-time.ts y lib/types.ts) dicen la verdad, sin tocar una linea de codigo"
  artifacts:
    - path: "supabase/migrations/070_service_gates_direction_and_time_precision.sql"
      provides: "Las tres correcciones del predicado en UNA sola redefinicion de los dos gates (D-01), con transaccion explicita (D-05)"
      contains: "services_block_mode_change"
      min_lines: 120
    - path: "supabase/schema.sql"
      provides: "Espejo quirurgico del cuerpo de las dos funciones corregidas"
      contains: "v_now_time"
    - path: "lib/appointment-time.ts"
      provides: "La cabecera deja de declarar una divergencia con el gate de borrado que la 070 elimina"
      contains: "070"
    - path: "lib/types.ts"
      provides: "El comentario de capacity_mode declara la salvedad de direccion del gate"
      contains: "070"
    - path: ".planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-BASELINE-070.md"
      provides: "Control negativo A/B: los ocho casos medidos ANTES y DESPUES contra el Postgres local"
      contains: "DESPUES"
  key_links:
    - from: "supabase/migrations/070_service_gates_direction_and_time_precision.sql"
      to: "supabase/schema.sql"
      via: "espejado quirurgico del cuerpo de las dos funciones (nunca db dump)"
      pattern: "v_now_time"
    - from: "supabase/migrations/070_service_gates_direction_and_time_precision.sql"
      to: "lib/appointment-time.ts"
      via: "el predicado de fecha+hora replica isPastAppointment y compara el INICIO del turno"
      pattern: "v_now_time"
    - from: ".planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-BASELINE-070.md"
      to: "supabase/migrations/070_service_gates_direction_and_time_precision.sql"
      via: "cada caso del A/B mide una de las tres correcciones del archivo"
      pattern: "Caso"
---

<objective>
Corregir en **una sola migración** (D-01) el `IF EXISTS` que comparten los dos gates de servicio, y
**medirlo antes de cambiarlo**.

Purpose: las tres correcciones —GATE-01 (recorte por dirección), GATE-02 (`completed` futuro deja de
abrir el gate de modo, residual **R-15-A**) y GATE-03 (fecha **+ hora** en los **dos** gates)— viven
en el mismo predicado. Tocarlo tres veces multiplica el riesgo sobre funciones que ya están en
producción y triplica el trabajo de `secure-phase`.

Y el orden importa tanto como el cambio: **tres veces entre v0.26 y v0.27 el fix propuesto estaba mal
y se salvó porque alguien lo midió antes** — el `BEFORE UPDATE` de la 067 (habría roto todas las bajas
de abono en producción) y el `capacity >= 2` del review de la 069 (habría reabierto lo que cerró la
064). Por eso la Task 1 **no toca nada**: reproduce el comportamiento actual y lo deja escrito. Si un
caso del baseline no da lo que este plan predice, la premisa está mal — parar y reportar, no seguir.

Por eso también **D-06**: `secure-phase` es obligatorio y el register de este plan no puede limitarse a
decir que las direcciones peligrosas siguen cerradas — tiene que **mostrarlo**, y su evidencia es la
tabla A/B que produce la Task 1 más los casos por dirección de 16-02.

Output: `supabase/migrations/070_service_gates_direction_and_time_precision.sql`, el espejo quirúrgico
en `supabase/schema.sql`, y `16-BASELINE-070.md` con la tabla A/B de los ocho casos.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-CONTEXT.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<constraints>
- **Ejecución sobre `main`, SIN worktree.** Ejecutores **secuenciales**.
- **La 070 NO se aplica a producción en esta fase (D-08).** Se deja el archivo y, en 16-02, el runbook.
- Toda la validación va contra el Postgres **LOCAL**:
  `docker exec -i supabase_db_forjo-app psql -U postgres -d postgres -v ON_ERROR_STOP=1`.
- **NO correr `supabase db reset`** sin avisarlo antes: borra el negocio de prueba local. Tampoco hace
  falta — `CREATE OR REPLACE FUNCTION` es idempotente y la re-corribilidad se prueba aplicando el
  archivo **dos veces**.
- **NUNCA `supabase db push`**: prod no tiene `supabase_migrations.schema_migrations` y el proyecto sí
  está linkeado, así que intentaría replayar desde el baseline.
- El espejado de `supabase/schema.sql` es **quirúrgico**. **Prohibido `supabase db dump`.**
- **NUNCA `npx tsc`** (baja `tsc@2.0.4`, que no es el compilador y siempre sale 0) →
  `./node_modules/.bin/tsc --noEmit`.
- **`npx vitest run` completo NO es un gate útil** acá (timeouts contra el Supabase local): suites
  puntuales con `./node_modules/.bin/vitest run <archivo> --no-file-parallelism --testTimeout=30000`.
- Un `UPDATE`/`DELETE` que no matchea ninguna fila sale **"Success" sin que el trigger corra**: todo
  caso de repro **fuerza su fila en la misma transacción**.
- Este plan **no toca** `app/`, `lib/` ni `test/`. Los tests se arreglan en **16-02**, y este plan
  predice exactamente cuáles van a quedar en rojo. Si te encontrás editando un `.tsx`, te fuiste de
  alcance.
- Comentarios densos **en español**, explicando el *por qué*, no el *qué*.
</constraints>

<!-- planner-discipline-allow: duration_minutes -->
<!-- planner-discipline-allow: a."date" >= v_today -->
<!-- planner-discipline-allow: RETURN NULL -->

<tasks>

<task type="auto">
  <name>Task 1: Medir el comportamiento ACTUAL de los dos gates (control negativo, ANTES de tocar nada)</name>
  <files>.planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-BASELINE-070.md</files>
  <read_first>
    - `supabase/migrations/068_service_capacity_unified_and_mode_gate.sql` — buscar
      `services_block_mode_change`: el guard de no-cambio (`IS NOT DISTINCT FROM`) y, debajo, el
      `IF EXISTS` que se corrige.
    - `supabase/migrations/065_service_snapshot_and_delete_gate.sql` — buscar `services_block_delete`:
      el mismo `IF EXISTS`, carácter por carácter, con otro código de dominio en el `RAISE`.
    - `16-CONTEXT.md` §D-02/D-03/D-04 — fijan qué tiene que dar cada caso **después** del cambio.
  </read_first>
  <action>
    Escribir y correr **un solo script SQL de repro** contra el Postgres local y dejar su salida
    literal en `16-BASELINE-070.md`, bajo el encabezado `## ANTES (predicado vigente: 065 + 068)`.
    En esta task **no se modifica ningún archivo de `supabase/`**.

    Forma del script (verificada contra el local al planificar):

    - Se ejecuta con `docker exec -i supabase_db_forjo-app psql -U postgres -d postgres -v ON_ERROR_STOP=1`
      leyendo por stdin; abre con `BEGIN;` y cierra con `ROLLBACK;` — no deja una sola fila.
    - Primero un **guard de medianoche**: un bloque `DO $$ ... END $$;` que calcula
      `(now() AT TIME ZONE 'America/Argentina/Buenos_Aires')::time` y hace `RAISE EXCEPTION` si cae
      fuera de `[01:00, 23:30]`. Los casos de "hoy a hora ya pasada" usan horas fijas de madrugada y
      el de "hoy a hora futura" usa `23:59`; fuera de esa ventana el script mediría lo contrario de lo
      que dice medir, y un repro no determinista es peor que ninguno.
    - Una tabla temporal `_repro(caso text, esperado_hoy text, medido text)` y, al cierre,
      `SELECT * FROM _repro ORDER BY caso;`: **la evidencia vuelve como FILAS**. `RAISE NOTICE` no
      sirve como evidencia (en el SQL Editor de Supabase ni siquiera se ve).
    - Un negocio desechable con `INSERT INTO businesses (slug, name) VALUES (...)`: `owner_id` es
      NULLABLE, así que **no hace falta** usuario de `auth`.
    - Un servicio **por caso**, con
      `INSERT INTO services (business_id, name, duration_minutes, price, active, capacity_mode, capacity)`
      ya nacido en el modo que el caso necesita. El `INSERT` no pasa por el gate (es un trigger de
      `UPDATE`), así que sembrar el modo en el `INSERT` evita que el propio fixture rebote contra el
      gate que se está midiendo.
    - Un turno por caso con
      `INSERT INTO appointments (business_id, service_id, client_name, date, time, duration_minutes, status)`,
      cada uno en **hora distinta**: el EXCLUDE gist `appointments_no_overlap` mira `business_id` +
      profesional coalescido + solape de intervalos, y acá el profesional queda NULL para todos, así
      que dos turnos vivos solapados del mismo negocio morirían con `23P01` y el script fallaría por
      el motivo equivocado.
    - Cada intento adentro de un bloque
      `DO $$ BEGIN <UPDATE o DELETE>; INSERT INTO _repro ... 'PASA'; EXCEPTION WHEN others THEN INSERT INTO _repro ... format('RECHAZO %s / %s', SQLSTATE, SQLERRM); END $$;`
      — el `EXCEPTION` abre una subtransacción, así que un rechazo no aborta el resto del script.

    Los ocho casos, con lo que **tienen que dar hoy**. Si alguno no da esto, **parar y reportar**:

    | Caso | Fixture | Intento | Esperado HOY |
    |---|---|---|---|
    | 1 — GATE-01, dirección segura | `individual`/1 + turno FUTURO `confirmed` 09:00 | `capacity_mode='group_class', capacity=2` | **RECHAZO** `P0001 / service_mode_has_future_appointments` — es justo lo que GATE-01 viene a abrir |
    | 2 — GATE-01, dirección peligrosa hacia individual | `group_class`/2 + turno FUTURO `confirmed` 10:00 | `capacity_mode='individual', capacity=1` | **RECHAZO** — acá vive R-1, tiene que seguir igual después |
    | 3 — GATE-01, cambio de eje de conteo | `group_class`/2 + turno FUTURO `confirmed` 11:00 | `capacity_mode='simultaneous_resource', capacity=2` | **RECHAZO** — tiene que seguir igual |
    | 4 — GATE-02 (R-15-A) | `group_class`/2 + ÚNICO turno FUTURO `status='completed'` 12:00 | `capacity_mode='individual', capacity=1` | **PASA** — éste es el agujero |
    | 5 — GATE-03 en el gate de modo | `group_class`/2 + ÚNICO turno de HOY `confirmed` a las `00:00` | `capacity_mode='individual', capacity=1` | **RECHAZO** — éste es el bug |
    | 6 — GATE-03 en el gate de borrado | `individual`/1 + ÚNICO turno de HOY `confirmed` a las `00:30` | `DELETE FROM services WHERE id = ...` | **RECHAZO** `P0001 / service_has_future_appointments` — mismo bug, otro gate |
    | 7 — frontera conservada | `individual`/1 + turno de HOY `confirmed` a las `23:59` | `DELETE` | **RECHAZO** — un turno de hoy que todavía no llegó tiene que seguir bloqueando |
    | 8 — divergencia de estados (D-03) | `individual`/1 + ÚNICO turno FUTURO `completed` 13:00 | `DELETE` | **PASA** — para el borrado, un turno completado es historia (HIST-01..03), y eso NO cambia |

    El archivo lleva: encabezado con fecha, hora AR de la corrida y `git rev-parse --short HEAD`; el
    script completo; la salida literal pegada; y la tabla de arriba con la columna `Medido` llena.
    Dejar el archivo listo para que la Task 2 le agregue la sección `## DESPUES`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; B=.planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-BASELINE-070.md &amp;&amp; test -f "$B" &amp;&amp; grep -cE 'PASA|RECHAZO' "$B" &amp;&amp; git status --porcelain supabase/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'PASA|RECHAZO' <baseline>` &gt;= **8** — los ocho casos tienen un resultado medido, no
      una descripción. (La tabla lleva una fila por caso, numeradas 1..8, con columnas
      Caso · Esperado HOY · Medido.)
    - `grep -c "P0001" <baseline>` &gt;= **1** y `grep -c "service_mode_has_future_appointments" <baseline>` &gt;= **1**:
      la evidencia trae el **SQLSTATE y el código de dominio literales** que devolvió la base, no una
      paráfrasis. Un baseline que dice "falló" no sirve de control negativo.
    - `git status --porcelain supabase/ | wc -l` == **0** — la Task 1 **no toca** `supabase/`. Si toca
      algo, se está midiendo el mundo ya modificado y el control negativo se perdió.
    - `grep -c "ROLLBACK" <baseline>` &gt;= **1** — el script no deja datos en la base local.
    - `grep -cE "01:00|23:30" <baseline>` &gt;= **1** — el guard de medianoche está en el script.
    - Los ocho resultados **coinciden** con la columna "Esperado HOY" de la tabla de esta task.
      Si alguno no coincide: **parar**, no escribir la migración, y reportarlo en el SUMMARY como
      premisa rota.
  </acceptance_criteria>
  <done>El comportamiento actual de los dos gates está medido, con SQLSTATE y código de dominio
  literales, y escrito en un archivo del repo. Cualquier afirmación posterior sobre "qué cambió" se
  puede contrastar contra esta medición.</done>
</task>

<task type="auto">
  <name>Task 2: La migración 070 — las tres correcciones en una sola redefinición de los dos gates</name>
  <files>supabase/migrations/070_service_gates_direction_and_time_precision.sql, .planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-BASELINE-070.md</files>
  <read_first>
    - `supabase/migrations/069_shared_capacity_agenda_and_space_gates.sql` — **el molde**: `BEGIN;` en
      la línea 115, `COMMIT;` en la 491 y `NOTIFY pgrst, 'reload schema';` **después** del COMMIT. Y su
      header, que razona por qué el recorte del gate espejo es **nominal y no numérico** — el mismo
      tipo de error que GATE-01 tiene que evitar en el sentido contrario.
    - `supabase/migrations/068_...sql` y `065_...sql` — los cuerpos vigentes que se redefinen. Se parte
      **de ellos**, no de cero: todo lo que no está en la lista de cambios queda byte-idéntico.
    - `lib/appointment-time.ts` — `isPastAppointment` compara `hhmmss(appt.time) < hhmmss(now.time)`:
      el **INICIO** del turno, con límite estricto.
  </read_first>
  <action>
    Crear `supabase/migrations/070_service_gates_direction_and_time_precision.sql` con **dos**
    `CREATE OR REPLACE FUNCTION` —`services_block_delete` y `services_block_mode_change`— dentro de un
    `BEGIN;` / `COMMIT;` explícito (D-05, lección de la 068: su atomicidad dependía de cómo la aplicara
    el cliente), y el `NOTIFY pgrst, 'reload schema';` después del `COMMIT`.

    **NO se dropean ni se recrean los triggers.** `CREATE OR REPLACE FUNCTION` conserva el binding de
    `services_block_delete_trg` y `services_block_mode_change_trg`; recrearlos es riesgo gratis. Y NO
    se extrae una función auxiliar compartida para el predicado: es la decisión que D-03 exige tomar
    explícitamente, y la respuesta es **no**, porque esta migración es exactamente la que los hace
    **divergir** (ver abajo). Compartir el predicado justo cuando se decide separarlo es el peor
    momento posible.

    **Los cambios, y nada más:**

    (1) **GATE-01 — recorte por dirección de ORIGEN**, solo en `services_block_mode_change`. Debajo del
        guard de no-cambio existente (`IF NEW."capacity_mode" IS NOT DISTINCT FROM OLD."capacity_mode" THEN RETURN NEW; END IF;`,
        que queda **intacto**) va un segundo guard, escrito literal así:
        `IF OLD."capacity_mode" = 'individual' THEN` + `RETURN NEW;` + `END IF;`.
        `services.capacity_mode` es `NOT NULL` con default `'individual'`, así que la igualdad simple
        alcanza y no hace falta `IS DISTINCT FROM`.
        El comentario tiene que decir **por qué el criterio es nominal y no numérico**: un turno nace
        `is_group = true` solo si el servicio no era `individual` al crearse, y post-068 vale
        `is_group ⟺ capacity_mode <> 'individual'` (el auditor de la Phase 15 lo **probó**, no lo
        dedujo: el CHECK de coherencia impide cualquier cambio de solo `capacity` que cruce la frontera
        1/2). Escribirlo como `OLD."capacity" <= 1` sería el mismo error que el review de la 069
        propuso en el otro sentido: un criterio numérico que se despega del significado en cuanto el
        CHECK cambie. Y la tabla de direcciones, con lo que decide cada una:
        `individual` → grupal/simultáneo **pasa** (las filas existentes siguen dentro del EXCLUDE gist
        013 **y** además se cuentan contra el cupo nuevo); grupal/simultáneo → `individual` **rechaza**
        (esas filas están fuera del EXCLUDE y el gate espejo deja de cubrirlas: ahí vive **R-1**);
        grupal ⇄ simultáneo **rechaza** (cambia el eje de conteo, y un conjunto hoy legal puede volverse
        ilegal). **Un solo turno futuro alcanza** para abrir el agujero en las dos peligrosas: no es
        cuestión de cantidad.

    (2) **GATE-02 — los dos predicados DIVERGEN, a propósito y escrito.** En
        `services_block_mode_change` la rama de estado pasa a
        `AND (a."status" IS NULL OR a."status" <> 'cancelled')`. En `services_block_delete` la rama
        **no se toca**: sigue siendo `AND (a."status" IS NULL OR a."status" NOT IN ('cancelled', 'completed'))`.
        El comentario del header tiene que sostener la divergencia con el criterio de cada gate, no con
        su forma: el gate de **borrado** pregunta *"¿queda algo por prestar?"* — un turno completado ya
        se prestó, es historia, y su nombre sobrevive por el snapshot de la 065, así que no puede
        trabar el borrado (fue el gap UAT #2 que la Phase 13 arregló; volver a contarlo sería
        re-romperlo). El gate de **modo** pregunta *"¿queda alguna fila cuyo `is_group` quedaría
        desalineado?"*, y ahí `completed` **no** es una salida legítima: es un botón de un solo click
        del panel sobre un turno **futuro** (`appointments-client.tsx`), o sea un bypass accesible del
        gate — que es exactamente el residual **R-15-A** de `15-SECURITY.md`, cuya condición de cierre
        registrada es esta. `cancelled` sí sigue afuera en los dos: cancelar destruye el compromiso y
        es la salida documentada que el propio copy del panel le ofrece al dueño.
        Dejar además escrito, en el header, que **los dos predicados ya no son intercambiables**, para
        que nadie los "unifique" más adelante por simetría.

    (3) **GATE-03 — fecha + hora, en los DOS gates.** En cada función se agrega al `DECLARE`
        `v_now_time time := (now() AT TIME ZONE 'America/Argentina/Buenos_Aires')::time;` (al lado del
        `v_today` que ya existe; `now()` es estable dentro de la transacción, así que las dos lecturas
        son del mismo instante y no hay skew), y la rama de fecha del `IF EXISTS` pasa a ser, literal y
        en las dos funciones:
        `AND (a."date" > v_today OR (a."date" = v_today AND a."time" >= v_now_time))`.
        El comentario tiene que dejar tres cosas por escrito:
        · Se compara contra el **INICIO** del turno, nunca contra su fin, y **no es estilo**:
          `lib/appointment-time.ts::isPastAppointment` —el fix del gap **G4** en la UI— compara el
          inicio, así que sumarle la duración al inicio reintroduciría la misma inconsistencia UI↔base
          que GATE-03 viene a cerrar, solo que al revés (un turno de 30 min a las 14:00 sería "pasado"
          en la UI a las 14:01 y "futuro" en la base hasta las 14:30). Por eso el predicado **no
          menciona la duración del turno**.
        · El `>=` es **inclusive** en la hora exacta, igual que el `<` estricto de `isPastAppointment`:
          el turno de la hora exacta todavía no pasó.
        · **No hay rama defensiva ante NULL** para estas dos columnas y eso está verificado, no
          asumido: `appointments.date` y `appointments.time` son **NOT NULL** (`schema.sql`, tabla
          `appointments`). La rama `status IS NULL` **sí** se conserva —`status` sigue siendo
          NULLABLE y `NOT IN`/`<>` sobre NULL evalúa NULL, ni true ni false, así que esas filas
          quedarían fuera del `EXISTS` y **abrirían** el gate: el repo ya pagó esa trampa dos veces.
        · La hora se toma en **AR** (UTC-3 sin DST) por el mismo motivo que `v_today`: a las 22:00 de
          Buenos Aires el `now()` en UTC ya es el día siguiente.

    **Inventario explícito de lo que NO cambia** (patrón de cada redefinición de este repo, va en el
    header): la firma y el `LANGUAGE plpgsql SECURITY DEFINER SET search_path = public` de las dos
    funciones; el filtro por tenant `AND (OLD."business_id" IS NULL OR a."business_id" = OLD."business_id")`
    en las dos (obligatorio: dentro de `SECURITY DEFINER` la RLS **no** aplica); el guard de cascada de
    la 065 y la ausencia deliberada de ese guard en la 068; el bloque de abono activo de la 065 con su
    `service_has_active_abono`; los **dos códigos de dominio** (`service_has_future_appointments` y
    `service_mode_has_future_appointments`), que **no** se renombran ni se agregan nuevos —el panel los
    mapea con `code === 'P0001' && message.includes(...)` y el copy actual sigue siendo cierto después
    del recorte, así que **esta fase no toca una sola línea de `.tsx`**—; y los `RETURN OLD` / `RETURN NEW`
    de salida, que son obligatorios (una salida nula desde un trigger BEFORE cancela la escritura **sin
    error**: PostgREST responde 204 y el panel diría "guardado" sin haber guardado — T-14-16).

    Header del archivo: molde 065/068/069 — qué corrige, de dónde salió cada corrección (UAT de la
    Phase 15, residual R-15-A de `15-SECURITY.md`, gap G4 de la Phase 13), la decisión de divergencia,
    la re-corribilidad (`CREATE OR REPLACE` puro ⇒ segunda pasada no-op) y la nota de que **NO se
    aplica a producción dentro de la fase** (D-08).

    **Después de escribir el archivo:**
    1. Aplicarlo al Postgres local por `docker exec -i ... psql ... -v ON_ERROR_STOP=1 -f -` (o `<`).
    2. Aplicarlo **una segunda vez**: tiene que terminar sin error (idempotencia).
    3. Re-correr el **mismo** script de la Task 1 y agregar a `16-BASELINE-070.md` la sección
       `## DESPUES (070 aplicada al local)` con la salida literal y la tabla A/B final (caso, ANTES,
       DESPUÉS, corrección que lo explica). Los flips esperados: **1 y 5 y 6** pasan de RECHAZO a PASA;
       **4** pasa de PASA a RECHAZO; **2, 3, 7 y 8 no se mueven**. Cualquier otro movimiento es un
       efecto no previsto: parar y reportar.
    4. Correr las dos suites de gate **sabiendo que la de modo va a quedar en rojo**, y registrar el
       detalle en el SUMMARY. Predicción exacta, que es en sí misma una verificación:
       `test/capacity-mode-change-gate.test.ts` → **4 pasan, 3 fallan**, y los que fallan son
       **exactamente los casos 1, 3 y 4** (los tres asertan un rechazo desde `individual` →
       `group_class`, que es la dirección que GATE-01 abre a propósito). Los tres caen por el **guard
       de dirección**, que devuelve antes de llegar al `EXISTS`: el 4 también, **a cualquier hora** —
       GATE-03 no interviene en su caída.
       `test/service-delete-gate.test.ts` → **sin cambios: 12 passed | 1 expected fail**. Si falla otro
       caso, o si la suite de borrado se mueve, el cambio hizo algo no previsto: **parar**. Arreglar los
       tests es trabajo de **16-02**, no de esta task.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; F=supabase/migrations/070_service_gates_direction_and_time_precision.sql &amp;&amp; test -f "$F" &amp;&amp; docker exec -i supabase_db_forjo-app psql -U postgres -d postgres -v ON_ERROR_STOP=1 -f - &lt; "$F" &amp;&amp; docker exec -i supabase_db_forjo-app psql -U postgres -d postgres -v ON_ERROR_STOP=1 -f - &lt; "$F"</automated>
  </verify>
  <acceptance_criteria>
    Con `F=supabase/migrations/070_service_gates_direction_and_time_precision.sql` (los conteos que
    filtran comentarios usan `sed 's/--.*//' "$F"` porque el header **sí** va a nombrar el predicado
    viejo al explicarlo — un conteo crudo se auto-invalidaría):

    - `grep -cE '^(BEGIN|COMMIT);' "$F"` == **2** (D-05; mismo conteo que la 069).
    - `sed 's/--.*//' "$F" | grep -cF 'a."date" > v_today OR (a."date" = v_today AND a."time" >= v_now_time)'` == **2** — la rama de fecha+hora, en las **dos** funciones.
    - `sed 's/--.*//' "$F" | grep -cF 'v_now_time'` &gt;= **4** (dos `DECLARE` + dos usos).
    - `sed 's/--.*//' "$F" | grep -cF 'a."date" >= v_today'` == **0** — el predicado viejo no sobrevive
      en código (en comentarios sí puede aparecer, y por eso el conteo va sobre el archivo sin
      comentarios).
    - `sed 's/--.*//' "$F" | grep -c 'duration_minutes'` == **0** — se compara contra el inicio del
      turno, no contra su fin.
    - `sed 's/--.*//' "$F" | grep -cF "OLD.\"capacity_mode\" = 'individual'"` == **1** — el guard de
      dirección de GATE-01, solo en el gate de modo.
    - `sed 's/--.*//' "$F" | grep -cF 'IS NOT DISTINCT FROM'` == **1** — el guard de no-cambio de la
      068 sigue vivo (y sigue yendo **primero**).
    - `sed 's/--.*//' "$F" | grep -cF "a.\"status\" <> 'cancelled'"` == **1** y
      `sed 's/--.*//' "$F" | grep -cF "NOT IN ('cancelled', 'completed')"` == **1** — la divergencia de
      D-03, una por gate.
    - `sed 's/--.*//' "$F" | grep -cF 'OLD."business_id" IS NULL OR a."business_id" = OLD."business_id"'` == **2** — el filtro por tenant sobrevivió la reescritura en las dos funciones.
    - `grep -c 'SECURITY DEFINER' "$F"` == **2** y `grep -c 'search_path' "$F"` &gt;= **2**.
    - `sed 's/--.*//' "$F" | grep -ciE 'return[[:space:]]+null'` == **0** (usar `-ciE`, **no** `-ciF`:
      esa combinación aborta en el Git Bash de esta máquina) y
      `sed 's/--.*//' "$F" | grep -cF 'RETURN NEW;'` &gt;= **3** (los dos guards + el cierre) y
      `sed 's/--.*//' "$F" | grep -cF 'RETURN OLD;'` &gt;= **2**.
    - `grep -c 'DROP TRIGGER' "$F"` == **0** y `grep -c 'CREATE TRIGGER' "$F"` == **0** — los triggers
      no se tocan.
    - `grep -c "NOTIFY pgrst" "$F"` == **1**, y su línea es **posterior** a la del `COMMIT;`
      (`grep -n` para comprobarlo).
    - El archivo aplica **dos veces seguidas** contra el local con exit 0 (idempotencia).
    - Verificación **por instalación**, no por archivo:
      `docker exec -i supabase_db_forjo-app psql -U postgres -d postgres -tAc "select proname, position('v_now_time' in prosrc) > 0, position('a.\"date\" >= v_today' in prosrc) = 0 from pg_proc where proname in ('services_block_delete','services_block_mode_change');"`
      devuelve `t|t` para las **dos** funciones.
    - `16-BASELINE-070.md` tiene la sección `## DESPUES` y la tabla A/B con los ocho casos; los flips
      son exactamente 1, 4, 5 y 6.
    - `./node_modules/.bin/vitest run test/service-delete-gate.test.ts --no-file-parallelism --testTimeout=30000`
      sigue en **12 passed | 1 expected fail**, y
      `./node_modules/.bin/vitest run test/capacity-mode-change-gate.test.ts --no-file-parallelism --testTimeout=30000`
      falla en **exactamente** los casos 1, 3 y 4. Declarar los dos resultados en el SUMMARY.
  </acceptance_criteria>
  <done>Las tres correcciones están en un solo archivo re-corrible, aplicadas al Postgres local y
  medidas con el mismo script que midió el estado anterior. Lo que cambió está demostrado caso por
  caso, y lo que no cambió también.</done>
</task>

<task type="auto">
  <name>Task 3: Espejo quirurgico en schema.sql + los dos comentarios de lib/ que dejan de ser ciertos</name>
  <files>supabase/schema.sql, lib/appointment-time.ts, lib/types.ts</files>
  <read_first>
    - `supabase/schema.sql` — los dos bloques a reemplazar: el que arranca en
      `CREATE OR REPLACE FUNCTION "public"."services_block_delete"() RETURNS "trigger"` y el que
      arranca en `CREATE OR REPLACE FUNCTION "public"."services_block_mode_change"() RETURNS "trigger"`,
      cada uno hasta su `$$;`. Localizarlos por **patrón**, no por número de línea.
    - Ojo con el vecino: `abonos_block_delete`, más arriba en el mismo archivo, **también** usa
      `a."date" >= v_today`. Está **fuera de alcance** — GATE-03 toca los dos gates de *servicio*, no
      el de abonos. Si se lo lleva puesto, la migración y el espejo dejan de coincidir.
    - `lib/appointment-time.ts`, el bloque de cabecera: hoy declara una **"DIVERGENCIA CONOCIDA"** con
      el gate de borrado y cierra con que corregirla *"pide una migración nueva"*. Esa migración es la
      **070**.
    - `lib/types.ts`, el comentario de `capacity_mode`: hoy dice que cambiar el modo con turnos futuros
      vivos **lo rechaza la base**, sin salvedad de dirección.
  </read_first>
  <action>
    Reemplazar en `supabase/schema.sql` **solo** los cuerpos de esas dos funciones, con la misma lógica
    que quedó en la 070 y respetando el estilo del archivo (identificadores citados,
    `LANGUAGE "plpgsql" SECURITY DEFINER SET "search_path" TO 'public'`, y comentarios **condensados** —
    `schema.sql` no lleva el header largo de la migración, lleva dos o tres líneas por decisión).

    Los comentarios condensados tienen que conservar las tres razones que un lector necesita para no
    "arreglar" esto después: por qué el guard de dirección mira el **modo de ORIGEN** y no el cupo; por
    qué los dos predicados **divergen** en el conjunto de estados; y por qué la comparación es contra el
    **inicio** del turno.

    `schema.sql` es un **espejo**, no una fuente: se edita a mano y solo en esos dos bloques.
    **Prohibido `supabase db dump`** — reescribiría el archivo entero y sepultaría el cambio real.

    **Y los dos comentarios de `lib/` que esta migración vuelve falsos.** No es alcance de más: son las
    únicas dos afirmaciones del código que la 070 desmiente, ningún otro plan de la fase puede tocarlas
    (16-02 es `test/` y la Phase 17 es superficie `.tsx`), y un comentario que miente es peor que
    ninguno — la misma lógica con la que la Phase 15 reescribió la justificación del gate espejo en vez
    de dejarla vencida. **Se tocan comentarios y nada más: cero líneas de código, cero cambios de
    comportamiento.**

    - `lib/appointment-time.ts` (bloque de cabecera): la "DIVERGENCIA CONOCIDA" con el gate de borrado
      **deja de existir** — desde la 070 los dos gates comparan fecha **y** hora, igual que
      `isPastAppointment`. Reescribir ese párrafo para que diga lo que pasa ahora: que la función es la
      **fuente de verdad del criterio** y que los gates de la base lo replican en SQL, nombrando la
      migración **070** y el gap **G4** que lo originó. Conservar la nota de hora AR, que sigue vigente.
    - `lib/types.ts` (comentario de `capacity_mode`): agregar la salvedad de dirección — el rechazo
      aplica cuando el modo de **origen** no es `individual`; desde `individual` hacia grupal o
      simultáneo el cambio **pasa**, porque esos turnos nacieron `is_group = false` y siguen dentro del
      EXCLUDE gist 013. Nombrar la **070** al lado de la 068 que ya cita.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; sed 's/--.*//' supabase/schema.sql | grep -cF 'date" >= v_today' &amp;&amp; sed 's/--.*//' supabase/schema.sql | grep -cF 'v_now_time' &amp;&amp; git diff --numstat -- supabase/schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - `sed 's/--.*//' supabase/schema.sql | grep -cF 'date" >= v_today'` == **1**. Hoy son **3**
      (`abonos_block_delete`, `services_block_delete`, `services_block_mode_change`): el que queda es
      el de abonos, que está fuera de alcance. Este único número prueba las dos cosas a la vez —que
      los gates de servicio cambiaron y que el de abonos **no** se tocó de rebote.
    - `sed 's/--.*//' supabase/schema.sql | grep -cF 'v_now_time'` == **4** (hoy **0**): dos `DECLARE`
      + dos usos.
    - `sed 's/--.*//' supabase/schema.sql | grep -cF "a.\"status\" <> 'cancelled'"` == **1** (hoy **0**)
      y `sed 's/--.*//' supabase/schema.sql | grep -cF "NOT IN ('cancelled', 'completed')"` == **2**
      — hoy son **3** (gate de abonos + los dos de servicio); el que se va es el del gate de **modo**.
      La divergencia queda espejada, y el número prueba que se fue **uno solo**.
    - `sed 's/--.*//' supabase/schema.sql | grep -cF "OLD.\"capacity_mode\" = 'individual'"` == **1**
      (hoy **0**).
    - `git diff --numstat -- supabase/schema.sql` → añadidas &lt;= **60** y borradas &lt;= **40**. Un
      `db dump` movería miles de líneas: este número es el detector, no una meta de brevedad.
    - `git diff -- supabase/schema.sql | grep -c '^@@'` &lt;= **6**, y **todos** los hunks caen dentro de
      los dos bloques de función (verificable a ojo con `git diff -U2`).
    - `grep -c "070" lib/appointment-time.ts` &gt;= **1** (hoy **0**) y
      `grep -c "070" lib/types.ts` &gt;= **1** (hoy **0**) — los dos comentarios nombran la migración que
      los volvió ciertos otra vez.
    - `grep -ci "DIVERGENCIA CONOCIDA" lib/appointment-time.ts` == **0** (hoy **1**) — la divergencia
      que el comentario declaraba **ya no existe**. (Usar `-ci`, **no** `-ciF`.)
    - `grep -c "individual" lib/types.ts` &gt;= **5** — hoy son **4**; la que se suma es la salvedad de
      dirección en el comentario de `capacity_mode`.
    - `./node_modules/.bin/tsc --noEmit` exit **0** — se tocaron comentarios, así que tiene que seguir
      compilando sin mover una coma de tipos.
    - `git diff --name-only` no incluye ningún archivo de `app/` ni de `test/`. De `lib/` solo los
      **dos** declarados (`lib/appointment-time.ts`, `lib/types.ts`), y **solo comentarios**:
      `git diff --numstat -- lib/` → añadidas &lt;= **20** y borradas &lt;= **20**, y
      `git diff -U0 -- lib/ | grep -E '^[+-][^+-]' | grep -vcE '^[+-]\s*(//|\*|/\*)'` == **0**
      (cero líneas de código tocadas: todo lo que cambia es comentario).
  </acceptance_criteria>
  <done>El espejo dice lo mismo que la base local, el gate de abonos quedó intacto, el diff es chico y
  localizado, y los dos comentarios del código que la 070 desmiente dejaron de mentir — sin tocar una
  sola línea de código.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño autenticado → `services` de su propio negocio | el `UPDATE`/`DELETE` entra por PostgREST y el gate corre **dentro** de la transacción, sin ventana TOCTOU |
| sesión anon de OTRO dueño → `services` ajeno | dentro de una función `SECURITY DEFINER` **no hay RLS**: el filtro por tenant explícito es la única defensa que queda adentro |
| PATCH forjado por PostgREST (sin UI) → los dos gates | la pantalla no es parte de la defensa; el gate tiene que sostenerse solo |
| alta manual del panel (**exenta** de la ventana de reserva) → `appointments` con fecha/hora en el pasado | es la superficie que puede crear filas *detrás* del corte nuevo de GATE-03 |
| operador → producción | la 070 se aplica **a mano**; dentro de esta fase **no se aplica** (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | GATE-01 abre `individual` → grupal/simultáneo con turnos futuros vivos | mitigate | El criterio es **nominal** (`OLD."capacity_mode" = 'individual'`), no numérico: esas filas nacieron `is_group = false`, siguen **dentro** del EXCLUDE gist 013 y además se cuentan contra el cupo nuevo. Se **demuestra**, no se argumenta: caso 1 del A/B de la Task 2 + aserción en 16-02 de que las filas preexistentes siguen con `is_group = false` **después** del cambio de modo |
| T-16-02 | Tampering | las direcciones peligrosas (hacia `individual`, grupal ⇄ simultáneo) — es donde vive R-1 | mitigate | El recorte se escribe por **negación explícita** de una sola dirección de origen; los casos 2 y 3 del A/B miden que **no se mueven** (RECHAZO antes y después), y 16-02 los fija por dirección con control negativo contra el predicado viejo |
| T-16-03 | Tampering | GATE-02: `completed` en un turno **futuro** era un bypass de un click del gate de modo (R-15-A) | mitigate | El gate de modo pasa a excluir **solo** `'cancelled'`; el caso 4 del A/B lo mide flipeando de PASA a RECHAZO. Cierra el residual con la condición de cierre que `15-SECURITY.md §4` dejó registrada |
| T-16-04 | Tampering de integridad | la divergencia deliberada entre los dos predicados invita a "unificarlos" después | mitigate | La divergencia queda **escrita** en el header de la 070 y en `schema.sql` con el criterio de cada gate, y hay un **testigo por lado**: caso 8 del A/B (borrado: `completed` futuro sigue sin bloquear) y caso 4 (modo: ahora bloquea). Los conteos de grep de la Task 2 fallan si alguien vuelve a igualarlos |
| T-16-05 | Tampering | GATE-03 es **permisivo**, y el alta manual está **exenta** de la ventana de reserva ⇒ se pueden crear turnos en el pasado | **accept** | Evaluado, no asumido. El vector completo es: el dueño baja el modo a `individual` (permitido porque su único turno ya pasó) y después crea **a mano** un turno pasado que se solape con la fila vieja `is_group = true`, que el gist no ve. Es **autolesión** del propio dueño, en su propio tenant, sobre un horario **que ya pasó**: no afecta la disponibilidad futura, no toca otro negocio y ninguna superficie **anónima** puede crear turnos en el pasado (el booking público sí respeta la ventana). **Reabrir si** alguna superficie no autenticada pudiera crear turnos con fecha/hora pasada, o si el alta manual empezara a generar turnos que el motor cuente hacia adelante |
| T-16-06 | Elevation of Privilege | dentro de `SECURITY DEFINER` la RLS no aplica: reescribir el `IF EXISTS` puede perder el filtro por tenant | mitigate | El filtro explícito se conserva **en las dos** funciones y hay criterio de grep con conteo exacto (== 2), más `SECURITY DEFINER` == 2 y `search_path` fijo. El aislamiento se prueba por comportamiento con **sesión anon real de otro dueño** en 16-02 |
| T-16-07 | Information Disclosure | el texto del `RAISE` viaja hasta el navegador | mitigate | Los **dos códigos de dominio no se tocan** (literales fijos, sin `%`, sin nombres, sin fechas, sin conteos) y **no se agrega ninguno nuevo**, así que el mapeo del panel por `code === 'P0001' && message.includes(...)` sigue válido y no hay riesgo de colisión por substring. Verificado además que el copy actual del panel sigue siendo **cierto** después del recorte ⇒ esta fase **no toca `.tsx`** |
| T-16-08 | Denial of Service (integridad) | una salida nula del trigger cancela la escritura **sin error**: PostgREST responde 204 y el panel diría "guardado" (T-14-16) | mitigate | Criterio de grep sobre el archivo sin comentarios (`return[[:space:]]+null` == 0, `RETURN NEW;` >= 3, `RETURN OLD;` >= 2) y, por comportamiento, el caso de 16-02 que **sí** pasa y **relee la fila**: error nulo + fila sin cambiar sería el verde falso |
| T-16-09 | Tampering | aplicar la 070 a medias (lección directa del incidente de la 068) | mitigate | `BEGIN;` / `COMMIT;` explícito en el archivo (D-05, molde 069), `NOTIFY pgrst` **después** del COMMIT, y re-corribilidad probada aplicando el archivo **dos veces** contra el local |
| T-16-10 | Tampering | **condición de reapertura de T-15-06** (`15-SECURITY.md §4`): *"reabrir si el gate pasa a mirar **transiciones** en vez de estado final"* — y GATE-01 hace exactamente eso | mitigate | Se cita y se **descarta demostrando** (D-06). El guard nuevo mira la transición, sí, pero **solo el origen**, y no hay encadenamiento posible: para llegar a una dirección peligrosa desde `individual` hay que **estar** en grupal o simultáneo, y ese paso vuelve a entrar al `EXISTS` con el estado final de la tabla. El `EXISTS` sigue mirando **qué queda colgando**, no cómo llegó la fila a su estado, así que dos PATCH encadenados (`individual` → grupal → `individual`) rebotan en el segundo. Evidencia: casos **1, 2 y 3** del A/B — el 1 flipea a PASA y los otros dos **no se mueven**, medidos en la misma corrida. **Reabrir si** el guard llegara a decidir por `NEW.capacity_mode` o por una historia de cambios en vez de por `OLD` |
| T-16-11 | Tampering | dos comentarios del código (`lib/appointment-time.ts`, `lib/types.ts`) quedan **falsos** el día que se aplica la 070 | mitigate | Se corrigen en la misma unidad revisable que el cambio que los desmiente (Task 3), **solo comentarios** y con criterio de grep que lo prueba (cero líneas de código tocadas). Un comentario vencido sobre el criterio "pasado/futuro" es justo el que hace que el próximo lector reintroduzca la divergencia UI↔base que GATE-03 vino a cerrar |
| T-16-SC | Tampering (supply chain) | dependencias | mitigate | **Cero** paquetes nuevos: el plan escribe SQL, comentarios y un `.md`. No hay `npm install`, así que no aplica el gate de legitimidad de paquetes |
</threat_model>

<verification>
1. El baseline de la Task 1 existe, con los ocho casos medidos **antes** de tocar `supabase/`, y sus
   ocho resultados coinciden con la predicción del plan.
2. La 070 aplica **dos veces seguidas** contra el Postgres local con exit 0.
3. El A/B de `16-BASELINE-070.md` muestra **exactamente** cuatro flips (casos 1, 4, 5 y 6) y cuatro
   casos inmóviles (2, 3, 7 y 8).
4. Verificación **por instalación** sobre `pg_proc.prosrc`: las dos funciones tienen `v_now_time` y
   **no** tienen el predicado viejo.
5. `sed 's/--.*//' supabase/schema.sql | grep -cF 'date" >= v_today'` == **1** (solo el gate de abonos,
   fuera de alcance) y `git diff --numstat -- supabase/schema.sql` con **<= 60 líneas añadidas y <= 40 borradas** (el mismo umbral que fija el criterio de la Task 3: 60 es el detector de un `db dump`, no una meta de brevedad).
6. `./node_modules/.bin/vitest run test/service-delete-gate.test.ts --no-file-parallelism --testTimeout=30000`
   sigue en **12 passed | 1 expected fail**.
7. `./node_modules/.bin/vitest run test/capacity-mode-change-gate.test.ts --no-file-parallelism --testTimeout=30000`
   falla en **exactamente** los casos 1, 3 y 4 — rojo **esperado y declarado**, que 16-02 cierra.
   Cualquier otro caso en rojo es un efecto no previsto: parar y reportar.
8. `git diff --name-only` no toca `app/` ni `test/`; de `lib/` solo los dos archivos declarados, y
   `git diff -U0 -- lib/ | grep -E '^[+-][^+-]' | grep -vcE '^[+-]\s*(//|\*|/\*)'` == **0** (todo lo
   que cambió en `lib/` es comentario).
9. `./node_modules/.bin/tsc --noEmit` exit **0**.
</verification>

<success_criteria>
- Las tres correcciones viven en **una sola** migración re-corrible, con transacción explícita, y el
  archivo **no** se aplicó a producción.
- El comportamiento viejo quedó **medido** antes del cambio y el nuevo después, con el mismo script y
  con SQLSTATE y códigos de dominio literales.
- Las dos direcciones peligrosas siguen rechazando: está medido, no argumentado.
- La divergencia entre los dos predicados es una **decisión escrita**, con un testigo por lado.
- `schema.sql` espeja la base local con un diff chico y localizado, y el gate de abonos quedó intacto.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-01-SUMMARY.md` when done
</output>
