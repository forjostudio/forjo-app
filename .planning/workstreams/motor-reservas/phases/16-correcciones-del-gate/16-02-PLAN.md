---
phase: 16-correcciones-del-gate
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - test/helpers/booking-fixtures.ts
  - test/capacity-mode-change-gate.test.ts
  - test/service-delete-gate.test.ts
  - .planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-RUNBOOK-070.md
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]

must_haves:
  truths:
    - "Existe un test POR DIRECCION: las dos seguras pasan y las dos peligrosas siguen rechazando con su codigo de dominio"
    - "Un turno futuro marcado completed ya no abre el gate de modo, y sigue sin trabar el borrado del servicio: un testigo por lado"
    - "Un turno de hoy a hora ya pasada no bloquea ninguno de los dos gates, y uno de hoy a hora futura sigue bloqueando los dos"
    - "Cada caso nuevo discriminante fue VISTO FALLAR contra el predicado viejo instalado a mano"
    - "El runbook deja pre-flight con criterio de aborto, verificacion por instalacion y rollback por objeto"
  artifacts:
    - path: "test/capacity-mode-change-gate.test.ts"
      provides: "Matriz de direcciones del gate de modo + GATE-02 + GATE-03 contra Postgres real"
      contains: "service_mode_has_future_appointments"
    - path: "test/service-delete-gate.test.ts"
      provides: "GATE-03 en el gate de borrado y el testigo de la divergencia de estados"
      contains: "service_has_future_appointments"
    - path: "test/helpers/booking-fixtures.ts"
      provides: "seedSimultaneousService acepta un serviceId, espejo de seedGroupClassService"
      contains: "seedSimultaneousService"
    - path: ".planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-RUNBOOK-070.md"
      provides: "Procedimiento de aplicacion manual de la 070 con criterio de aborto y rollback por objeto"
      contains: "pg_proc"
  key_links:
    - from: "test/capacity-mode-change-gate.test.ts"
      to: "supabase/migrations/070_service_gates_direction_and_time_precision.sql"
      via: "los casos golpean el trigger por PostgREST y asiertan el codigo de dominio del RAISE"
      pattern: "service_mode_has_future_appointments"
    - from: "test/capacity-mode-change-gate.test.ts"
      to: "lib/appointment-time.ts"
      via: "la frontera de hoy se calcula con nowInAR, la misma fuente que usa la UI"
      pattern: "nowInAR"
    - from: ".planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-RUNBOOK-070.md"
      to: "supabase/migrations/070_service_gates_direction_and_time_precision.sql"
      via: "el runbook es el procedimiento de aplicacion manual de ese archivo"
      pattern: "070"
---

<objective>
Probar el gate corregido **por dirección** y dejar escrito cómo se aplica a producción.

Purpose: 16-01 **estrecha** un gate que cierra el riesgo residual R-1 de v0.26 y que acaba de pasar
auditoría con `SECURED 32/32`. Un cambio así no se sostiene con un argumento: se sostiene con un test
por dirección que **se vio fallar** contra el predicado viejo (**D-07**), y con un register que
**demuestra** en vez de argumentar (**D-06**). Un test que nunca falló no prueba nada, y en este
workstream ya hubo tres fixes propuestos que estaban mal y se salvaron porque alguien los midió.

Hay un detalle que este plan tiene que decir en voz alta para que nadie lo lea como regresión: **varios
de los siete casos actuales de `capacity-mode-change-gate` cambian de escenario esperado, no se
rompen**. Hoy asiertan que `individual` → `group_class` con turnos futuros **rechaza**, y GATE-01 abre
justamente esa dirección a propósito. Se re-anclan a una dirección peligrosa, donde el rechazo sigue
siendo la conducta correcta.

Output: la matriz de direcciones en los dos archivos de test, el fixture que la habilita, y
`16-RUNBOOK-070.md`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-01-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-BASELINE-070.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
</context>

<constraints>
- **Ejecución sobre `main`, SIN worktree.** Ejecutores **secuenciales**: este plan depende de que la
  070 de 16-01 ya esté **aplicada al Postgres local**.
- Suites puntuales:
  `./node_modules/.bin/vitest run <archivo> --no-file-parallelism --testTimeout=30000`.
  **`npx vitest run` completo NO es un gate útil** en esta máquina (timeouts contra el Supabase local).
- **NUNCA `npx tsc`** → `./node_modules/.bin/tsc --noEmit`.
- **NO correr `supabase db reset`** sin avisarlo: borra el negocio de prueba local. El A/B se hace
  instalando y desinstalando los cuerpos de las funciones a mano.
- Los tests corren contra el **Supabase local**; sin creds la suite entera se skipea, y **un skip no
  cuenta como verde** — declararlo así en el SUMMARY.
- Un `UPDATE`/`DELETE` que no matchea ninguna fila sale **"Success" sin que el trigger corra**: cada
  caso siembra su propia fila y **relee el estado real de la base**, nunca solo el error que volvió.
- Este plan **no toca** `supabase/`, `app/` ni `lib/`. Si un test revela un defecto del predicado, el
  arreglo va en la 070 (plan 16-01) y se reporta; no se ajusta el test para que pase.
- **La 070 NO se aplica a producción en esta fase (D-08).** El runbook se escribe, no se ejecuta.
- Comentarios densos **en español**, explicando el *por qué*.
</constraints>

<!-- planner-discipline-allow: a."date" >= v_today -->

<tasks>

<task type="auto">
  <name>Task 1: La matriz de direcciones del gate de modo (GATE-01/02/03) + el fixture que la habilita</name>
  <files>test/helpers/booking-fixtures.ts, test/capacity-mode-change-gate.test.ts</files>
  <read_first>
    - `test/capacity-mode-change-gate.test.ts` — **el molde**, entero: asierta `code` **y** `message`,
      relee la fila con service-role, trae los dos guards anti-falso-verde del `beforeAll` y el
      contrapeso del caso de aislamiento. Todo eso se conserva.
    - `test/helpers/booking-fixtures.ts` — `seedService`, `seedGroupClassService` (ya acepta
      `serviceId`) y `seedSimultaneousService` (todavía no).
    - `lib/appointment-time.ts` — `nowInAR` devuelve `{ date, time }` en hora AR con `time` en
      `'HH:mm:ss'`.
    - `.planning/.../16-BASELINE-070.md` — la tabla A/B de 16-01 dice qué hace hoy la base en cada
      dirección; los casos de esta task son su versión permanente.
  </read_first>
  <action>
    **(a) Fixture.** En `test/helpers/booking-fixtures.ts`, `seedSimultaneousService` pasa a aceptar
    `opts: { capacity: number; serviceId?: string }` y a filtrar por `opts.serviceId ?? seeded.serviceId`
    — **espejo literal** de `seedGroupClassService`, que ya lo hace. Es cambio compatible hacia atrás:
    los llamadores actuales pasan solo `{ capacity }` y siguen apuntando al service del tenant. Hace
    falta para declarar simultáneo un **segundo** servicio del mismo negocio, que es lo que necesita
    la matriz de direcciones (cada caso siembra su propio service con `seedService`). Documentar el
    porqué arriba de la firma, con el molde de comentario que ya tiene el helper hermano.

    ⚠ **Trampa de orden, y va escrita en el archivo de test:** los dos helpers hacen un `UPDATE` sobre
    `services`, así que **pasan por el gate de modo**. Primero se declara el modo del service, después
    se siembra el turno. Al revés, el propio fixture rebota contra el gate que el caso quiere medir.

    **(b) Constantes y guard de la suite.** Importar `nowInAR` desde `@/lib/appointment-time` (el alias
    `@/` ya resuelve en los tests) y usarlo para la frontera de hoy: es **la misma fuente que usa la
    UI**, y que las dos coincidan es literalmente lo que GATE-03 vino a arreglar. Agregar
    `PAST_TIME_TODAY = '00:00:00'` y `FUTURE_TIME_TODAY = '23:59:00'`, y en el `beforeAll` un guard que
    **tire** (no que skipee) si la hora AR cae fuera de `['01:00:00', '23:30:00']`, con un mensaje que
    explique que en la ventana de medianoche esas dos constantes dejan de ser deterministas. Un skip
    silencioso escondería el agujero de cobertura; un throw con motivo, no.

    **(c) Los casos que CAMBIAN de escenario — no son regresiones.** Dejar en cada uno un comentario
    que diga qué cambió y por qué:

    | Caso | Hoy | Queda |
    |---|---|---|
    | 1 — el rechazo | `individual` → `group_class` con turno futuro vivo | **misma aserción, otra dirección**: `group_class`/2 → `individual`/1. La dirección vieja ahora **pasa** por GATE-01; el rechazo se prueba donde vive R-1 |
    | 2 — detector de la salida nula del trigger | `individual` → `group_class` sin futuros vivos | **dirección peligrosa** (`group_class`/2 → `individual`/1, con un futuro `cancelled` + un pasado vivo). Si no, el caso pasaría por el guard de dirección **sin llegar al `EXISTS`** y dejaría de detectar lo que dice detectar |
    | 3 — rama `status IS NULL` | `individual` → `group_class` con futuro sin estado | **dirección peligrosa**, misma aserción. Es la rama que el repo ya pagó dos veces |
    | 4 — frontera "hoy" | un solo caso a las `10:30`, que asierta bloqueo | **absorbido por los dos casos GATE-03 de (d)** — desaparece como caso propio, no se duplica: (d) ya trae hoy-a-hora-**pasada** (no bloquea) y hoy-a-hora-**futura** (bloquea). Duplicarlo sería además **imposible de ejecutar**: dos turnos del mismo tenant en `TODAY_AR` a la misma hora chocan con el índice único 011 (`23505`) y, separados de minutos, con el EXCLUDE gist 013 (`23P01`). Hoy este caso además es una moneda al aire: a las 11:00 AR mediría lo contrario que a las 10:00 |
    | 5 — escrituras legítimas | desactivar + renombrar | **sin cambios**. El guard de no-cambio corre **antes** que el de dirección y que el `EXISTS` |
    | 6 — aislamiento por tenant | cross-tenant + contrapeso | **sin cambios**, pero agregar una línea: desde GATE-01 esa dirección ya no rebota por el gate, así que el caso ahora prueba **solo** la RLS — que es exactamente lo que tiene que probar |
    | 7 — CHECK de coherencia | los dos sentidos, `23514` | **sin cambios**. El CHECK corre igual con o sin el recorte |

    **(d) Los casos NUEVOS.** Cada uno siembra su propio service con `seedService` y usa una hora libre
    (las ocupadas hoy son `09:00`, `09:30`, `10:00`, `10:30`, `11:00` sobre la fecha futura fija; el
    EXCLUDE gist mira solape sobre el mismo profesional del tenant, así que alcanza con separar 30 min):

    - **GATE-01 — dirección segura A:** `individual` → `group_class`/2 **con un turno futuro
      `confirmed`** ⇒ **pasa**, y la fila queda escrita (releerla). Y además —ésta es la evidencia de
      que estrechar no reabre R-1— asertar que el turno preexistente **sigue con `is_group = false`**
      después del cambio: por eso sigue dentro del EXCLUDE gist 013 y por eso la dirección es segura.
    - **GATE-01 — dirección segura B:** `individual` → `simultaneous_resource`/2 con turno futuro vivo
      ⇒ **pasa**.
    - **GATE-01 — dirección peligrosa C:** `group_class`/2 → `simultaneous_resource`/2 con turno futuro
      vivo ⇒ **rechaza** (`P0001` + `service_mode_has_future_appointments`) y la fila queda intacta.
      Cambia el **eje de conteo**: un conjunto hoy legal puede volverse ilegal.
    - **GATE-01 — dirección peligrosa D:** `simultaneous_resource`/2 → `group_class`/2 con turno futuro
      vivo ⇒ **rechaza**.
    - **GATE-02:** `group_class`/2 cuyo **único** turno futuro tiene `status = 'completed'` →
      `individual`/1 ⇒ **rechaza**. Es el residual **R-15-A**: marcar completado un turno futuro era un
      bypass de un click. **En este caso va el testigo de la divergencia (D-03)**: un comentario con
      referencia cruzada al gate de **borrado**, donde ese mismo estado sigue **sin** bloquear. Es el
      par del comentario de la Task 2, y sin los dos nadie frena al próximo que quiera "unificar" los
      predicados por simetría — que es exactamente la amenaza T-16-04.
    - **GATE-03 — el fix:** `group_class`/2 con **único** turno de HOY a `PAST_TIME_TODAY`, vivo →
      `individual`/1 ⇒ **pasa**, y queda escrito.
    - **GATE-03 — la frontera:** `group_class`/2 con turno de HOY a `FUTURE_TIME_TODAY`, vivo →
      `individual`/1 ⇒ **rechaza**. Sin este caso, GATE-03 podría haberse pasado de laxo y nadie se
      enteraría.

    Todos con timeout explícito por caso (`}, 20000)`), como el resto del archivo.

    **(e) Control negativo A/B — obligatorio, y se declara en el SUMMARY.** Con la suite en verde:
    instalar a mano en el local los cuerpos **viejos** de las dos funciones (copiarlos de la 068 y la
    065 a un `.sql` desechable **fuera de `supabase/migrations/`**, aplicarlo), correr las dos suites,
    anotar qué cae, y **restaurar** aplicando de nuevo la 070. Los casos **discriminantes** —los que
    tienen que caer con el predicado viejo— son: las dos direcciones seguras, el de GATE-02 y el de
    GATE-03 en esta suite, más el de GATE-03 de la Task 2. Los demás son **invariantes**: pasan con los
    dos predicados a propósito, y eso también hay que declararlo (un invariante no es un control).
    Si un caso discriminante **pasa** con el predicado viejo, ese caso no prueba nada: arreglarlo.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; ./node_modules/.bin/vitest run test/capacity-mode-change-gate.test.ts --no-file-parallelism --testTimeout=30000</automated>
  </verify>
  <acceptance_criteria>
    - La suite corre con **0 fallos** y **0 skips**, con **&gt;= 13** casos. La aritmética, explícita
      para que nadie la reconstruya mal: de los **7** actuales sobreviven **6** (1, 2, 3, 5, 6 y 7) y
      el **4** queda **absorbido** por los dos casos GATE-03 de (d) — no se suma aparte —, más los
      **7** nuevos de (d): **6 + 7 = 13**. Si aparecen skips faltan las creds de Supabase y **no
      cuenta como verde**.
    - `grep -c "capacity_mode: 'simultaneous_resource'" test/capacity-mode-change-gate.test.ts` &gt;= **2**
      — las dos direcciones que involucran el modo simultáneo.
    - `grep -c "'completed'" test/capacity-mode-change-gate.test.ts` &gt;= **1** (GATE-02).
    - `grep -ci "diverg" test/capacity-mode-change-gate.test.ts` &gt;= **1** (hoy **0**) — el testigo de
      la divergencia está escrito **también de este lado**, no solo en el gate de borrado. Sin este
      criterio, omitir el comentario no rompe nada y T-16-04 se queda sin defensa. (Usar `-ci`, **no**
      `-ciF`: esa combinación aborta en el Git Bash de esta máquina.)
    - `grep -c "is_group" test/capacity-mode-change-gate.test.ts` &gt;= **1** — la aserción que demuestra
      por qué la dirección segura es segura.
    - `grep -c "nowInAR" test/capacity-mode-change-gate.test.ts` &gt;= **1** (hoy **0**) y
      `grep -c "PAST_TIME_TODAY\|FUTURE_TIME_TODAY" test/capacity-mode-change-gate.test.ts` &gt;= **2**
      (hoy **0**).
    - `grep -c "GUARD" test/capacity-mode-change-gate.test.ts` &gt;= **5** — hoy son **4** (los dos
      guards anti-falso-verde del molde, que se conservan), más el de la ventana de medianoche.
    - `grep -c "23:30" test/capacity-mode-change-gate.test.ts` &gt;= **1** (hoy **0**) — el borde
      superior de la ventana está escrito en el guard, no implícito.
    - `grep -cE "\}, [0-9]{4,}\)" test/capacity-mode-change-gate.test.ts` &gt;= **13** — hoy son **7**:
      timeout explícito por caso, porque los 5000 ms por defecto no alcanzan contra el Supabase local.
      (El regex acepta cualquier timeout de 4+ dígitos, no solo `20000`.)
    - `grep -cF 'opts: { capacity: number; serviceId?: string }' test/helpers/booking-fixtures.ts` == **2**
      — hoy es **1** (`seedGroupClassService`); la segunda es `seedSimultaneousService` con la **misma**
      firma, que es el punto: son helpers hermanos y ahora lo parecen.
    - `./node_modules/.bin/vitest run test/concurrency.test.ts test/booking-cualquiera-public.test.ts --no-file-parallelism --testTimeout=30000`
      sigue en verde: son los llamadores de `seedSimultaneousService` y el cambio de firma tiene que
      ser compatible hacia atrás.
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - El A/B está corrido y **declarado en el SUMMARY**, caso por caso, separando discriminantes de
      invariantes. Los cuatro discriminantes de esta suite **cayeron** con el predicado viejo.
  </acceptance_criteria>
  <done>El gate está probado por dirección contra Postgres real: las dos seguras pasan, las dos
  peligrosas rechazan con su código de dominio, `completed` futuro ya no abre nada y la frontera de hoy
  distingue la hora. Y cada caso que prueba algo nuevo se vio fallar sin el fix.</done>
</task>

<task type="auto">
  <name>Task 2: GATE-03 en el gate de BORRADO y el testigo de la divergencia de estados</name>
  <files>test/service-delete-gate.test.ts</files>
  <read_first>
    - `test/service-delete-gate.test.ts` — el molde de este archivo: `deleteService`, `serviceExists`,
      `seedAppointment`, y en particular el caso **7** (turno de hoy `completed` no bloquea) y el **8**
      (turno futuro `completed` tampoco). Hoy la suite corre **12 passed | 1 expected fail**.
    - `supabase/migrations/065_service_snapshot_and_delete_gate.sql` — el criterio de estados de este
      gate, que **no cambia**, y su porqué (HIST-01..03: el snapshot conserva el historial).
  </read_first>
  <action>
    Agregar **dos** casos y **un** comentario. Nada más: el gate de borrado no cambia de dirección ni
    de estados, así que los 13 casos actuales tienen que seguir exactamente como están.

    - Copiar del archivo hermano el guard de la ventana de medianoche y las constantes
      `PAST_TIME_TODAY` / `FUTURE_TIME_TODAY` derivadas de `nowInAR` (mismo criterio, mismo motivo).
      Los turnos de hoy que ya existen en este archivo usan `15:00` con estado `completed`, que está
      fuera del EXCLUDE gist, así que no chocan con los nuevos.
    - **GATE-03 — el fix:** un servicio cuyo **único** turno es de HOY a `PAST_TIME_TODAY`, `confirmed`
      ⇒ el borrado **pasa** y el servicio **desaparece de verdad** (releer con `serviceExists`, que es
      lo único que distingue "se borró" de "el trigger canceló el borrado en silencio"). Comentario:
      es el mismo bug que la Phase 13 arregló en la UI (gap **G4**) y que nunca había cruzado al SQL —
      la UI mostraba el turno en "Pasados" mientras la base lo contaba como futuro.
    - **GATE-03 — la frontera:** un servicio cuyo único turno es de HOY a `FUTURE_TIME_TODAY`,
      `confirmed` ⇒ el borrado **sigue rechazando** con `P0001` + `service_has_future_appointments`, y
      el servicio sigue existiendo. Un turno de hoy que todavía no llegó es un compromiso por delante.
    - **Testigo de la divergencia (D-03):** ampliar el comentario del caso 8 (turno **futuro**
      `completed` no bloquea el borrado) para marcarlo como el testigo de que los dos predicados
      **divergen a propósito** desde la 070, con referencia cruzada al caso de GATE-02 del archivo del
      gate de modo, donde ese mismo estado **sí** bloquea. Sin este par de comentarios, el próximo que
      lea los dos archivos va a "arreglar" uno de los dos.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; ./node_modules/.bin/vitest run test/service-delete-gate.test.ts --no-file-parallelism --testTimeout=30000</automated>
  </verify>
  <acceptance_criteria>
    - La suite corre con **0 fallos** y **0 skips**, y pasa de **13** casos a **&gt;= 15**
      (hoy: `12 passed | 1 expected fail`; el `expected fail` sigue siendo uno solo).
    - `grep -c "PAST_TIME_TODAY\|FUTURE_TIME_TODAY" test/service-delete-gate.test.ts` &gt;= **2**
      (hoy **0**).
    - `grep -c "nowInAR" test/service-delete-gate.test.ts` &gt;= **1** (hoy **0**).
    - `grep -c "23:30" test/service-delete-gate.test.ts` &gt;= **1** (hoy **0**) — el guard de medianoche
      con su borde escrito.
    - `grep -c "service_has_future_appointments" test/service-delete-gate.test.ts` &gt;= **5** — hoy son
      **4**; el quinto es el rechazo de la frontera nueva.
    - `grep -cE "\}, [0-9]{4,}\)" test/service-delete-gate.test.ts` &gt;= **12** — hoy son **10**:
      timeout explícito también en los dos casos nuevos.
    - `grep -ci "diverg" test/service-delete-gate.test.ts` &gt;= **1** — el testigo está anotado como tal
      (usar `-ci`, **no** `-ciF`: esa combinación aborta en el Git Bash de esta máquina).
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - El caso nuevo de GATE-03 **cayó** contra el predicado viejo en la pasada A/B de la Task 1(e). El
      de la frontera es **invariante** (pasa con los dos): declararlo así, no venderlo como control.
  </acceptance_criteria>
  <done>El gate de borrado distingue la hora, su frontera está fijada por un caso propio, y la
  divergencia de estados entre los dos gates tiene un testigo escrito de cada lado.</done>
</task>

<task type="auto">
  <name>Task 3: Runbook de aplicación de la 070 a producción (D-08 — se escribe, no se ejecuta)</name>
  <files>.planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-RUNBOOK-070.md</files>
  <read_first>
    - `.planning/workstreams/motor-reservas/phases/15-modelo-de-cupo-unificado/15-RUNBOOK-068.md` — **el
      molde**, y también la lección: cada paso trae la query **literal**, el resultado esperado al lado
      y qué hacer si no coincide; **todos** los controles devuelven **filas** (los `RAISE NOTICE` no se
      ven en el SQL Editor de Supabase); y hay una fila de tabla para el caso "la query no midió lo que
      creías". Leer también su advertencia inicial: la 068 se aplicó **fuera del orden** que su propio
      runbook fijaba.
    - `supabase/migrations/070_service_gates_direction_and_time_precision.sql` (de 16-01) — el archivo
      que este runbook gobierna.
  </read_first>
  <action>
    Escribir `16-RUNBOOK-070.md` con el molde del 068. Encabezado: migración, fase, **última aplicada en
    prod = 069 (2026-08-16)**, próxima = **070**, y estado **NO APLICADA — aplicarla es decisión del
    dueño (D-08)**.

    **1. Pre-flight, con criterio de ABORTO.** Queries literales contra producción, todas devolviendo
    filas, cada una con su tabla SEGUIR / ABORTAR / REGISTRAR:

    - *(i) ¿Prod está en el estado que la 070 espera?* Verificación **por instalación** sobre
      `pg_proc`: para `services_block_delete` y `services_block_mode_change`, devolver `proname` junto
      con `position('v_now_time' in prosrc)` y `position('a."date" >= v_today' in prosrc)`.
      Interpretación: las dos funciones con el predicado viejo y sin `v_now_time` ⇒ **SEGUIR**; ya con
      `v_now_time` ⇒ **la 070 ya está aplicada**, no re-aplicar (aunque sea idempotente, re-aplicar sin
      saber por qué es cómo se pierde el rastro); menos de dos filas ⇒ 🛑 **ABORTAR**, prod no está en
      el estado que este runbook asume.
    - *(ii) ¿A cuántos servicios les cambia algo hoy?* `select capacity_mode, capacity, count(*) from services group by 1,2 order by 3 desc;`.
      Con **cero** servicios distintos de `individual`, GATE-01 y GATE-02 no cambian el comportamiento
      de nadie el día de la aplicación: **SEGUIR y registrarlo**. Si aparece algún `group_class` o
      `simultaneous_resource`: **SEGUIR, pero registrar id y nombre** — son los únicos servicios sobre
      los que el gate de modo puede llegar a dispararse, así que son los que hay que mirar si algo se
      comporta raro después de aplicar.
    - *(iii) ¿Cuántos turnos deja de contar GATE-03 en el instante de aplicar?* Un `count(*)` de
      `appointments` de **hoy AR** con `time` menor a la hora AR actual y estado distinto de
      `'cancelled'`. No hay criterio de aborto: es el efecto buscado. Se **registra** el número, porque
      es la única forma de explicar después por qué un servicio que ayer no se podía borrar hoy sí.
    - *(iv)* La regla que la Phase 14 dejó escrita con sangre: **un cero solo vale si otra query ya
      devolvió números**. Dejar la fila "la query devolvió 0 filas / no midió nada ⇒ 🛑 ABORTAR,
      verificar que estás en el proyecto de producción".

    **2. Orden de aplicación.** Escribir explícitamente que la regla del repo es **código primero,
    migración después**, y por qué **acá la 070 puede ir sola**: el código que mapea los rechazos de
    estos gates a copy propia ya está en producción desde 15-02, y el recorte **no agrega ningún código
    de dominio nuevo** — el mapeo del panel (`code === 'P0001'` + `message.includes(...)`) sigue siendo
    válido y su copy sigue siendo cierta.

    **3. Aplicación.** Pegar el archivo **entero, de una sola vez**, en el SQL Editor. Trae su propio
    `BEGIN;` / `COMMIT;` (D-05): **no partirlo en pedazos** — es exactamente la lección de la 068, cuya
    atomicidad dependía de cómo lo aplicara el cliente. **Nunca `supabase db push`**: prod no tiene
    `supabase_migrations.schema_migrations` y el proyecto sí está linkeado, así que intentaría replayar
    desde el baseline.

    **4. Verificación post-aplicación, por INSTALACIÓN.** Repetir la query (i) —ahora tiene que dar
    `v_now_time` presente y el predicado viejo ausente en las dos funciones— y sumar una sobre
    `pg_trigger` que confirme que `services_block_delete_trg` y `services_block_mode_change_trg` siguen
    enganchados a sus funciones. El `CREATE OR REPLACE` no toca los triggers, pero comprobarlo es la
    diferencia entre creer y saber. Dejar escrito **por qué la verificación es por instalación y no por
    comportamiento** (mismo criterio D-09 que la 068): con cero servicios no-`individual` en prod, el
    rechazo del gate de modo no se puede provocar desde la UI; el **comportamiento** está probado
    contra el Postgres local en 16-01 y 16-02.

    **5. Rollback, por objeto.** Re-aplicar los cuerpos de la 065 y la 068 (las dos funciones juntas,
    en una transacción) devuelve el predicado viejo sin tocar ninguna otra cosa: no hay DDL de tablas,
    ni datos migrados, ni constraints nuevos. Dejar escrito **qué se pierde al revertir**: vuelve el
    bloqueo del cambio más frecuente (`individual` → grupal, y `individual` es el default), se reabre
    **R-15-A** (marcar completado un turno futuro vuelve a abrir el gate de modo) y vuelve el turno de
    hoy ya pasado trabando el borrado hasta mañana.

    **6. Registro.** Una sección final para anotar, el día que se aplique: fecha, quién, salida de cada
    control y cualquier desvío. El runbook del 068 terminó siendo el **registro** de cómo se aplicó y
    de qué salió mal; que éste nazca preparado para eso.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; R=.planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-RUNBOOK-070.md &amp;&amp; test -f "$R" &amp;&amp; grep -c "pg_proc" "$R" &amp;&amp; grep -c "pg_trigger" "$R" &amp;&amp; grep -c "ABORTAR" "$R"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pg_proc" <runbook>` &gt;= **2** (pre-flight y verificación post-aplicación).
    - `grep -c "pg_trigger" <runbook>` &gt;= **1**.
    - `grep -c "ABORTAR" <runbook>` &gt;= **3** — hay criterio de aborto de verdad, no una lista de pasos.
    - `grep -c "db push" <runbook>` &gt;= **1** — la prohibición está escrita.
    - `grep -ci "rollback" <runbook>` &gt;= **1** y el rollback nombra **las dos** funciones.
    - `grep -c "069" <runbook>` &gt;= **1** y `grep -c "070" <runbook>` &gt;= **3** — el runbook dice de
      dónde viene y qué aplica.
    - `grep -ci "R-15-A" <runbook>` &gt;= **1** — qué se pierde al revertir está escrito.
    - El documento **no** dice que la 070 esté aplicada: `grep -ci "NO APLICADA\|no aplicada" <runbook>` &gt;= **1**.
    - Cada control del pre-flight devuelve **filas** y trae su tabla de decisión.
  </acceptance_criteria>
  <done>Queda un procedimiento con criterios de decisión, no una lista de pasos: cuándo abortar, en qué
  orden, cómo verificar sin poder provocar el rechazo, y qué se pierde si se revierte.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sesión anon del dueño (RLS) → `services` de **otro** negocio | el aislamiento se asierta con la **anon key**, nunca con la service-role |
| fixture con service-role → base local | los helpers bypassan RLS a propósito para sembrar; si se usaran para asertar, el test no probaría nada |
| suite de tests → Postgres local | el sujeto es el trigger: la única forma de probarlo es escribiendo de verdad y mirando el error que vuelve |
| operador → producción | el runbook es el **único** artefacto que gobierna la aplicación manual de la 070 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Tampering | un test de gate que pasa **con y sin** el fix | mitigate | A/B obligatorio contra el predicado viejo instalado a mano, con la lista de casos **discriminantes** separada de los **invariantes**. Es el estándar del workstream (15-05 lo corrió contra un mutante) y es lo que salvó tres fixes equivocados entre v0.26 y v0.27 |
| T-16-13 | Tampering | falso verde por escritura que no matchea ninguna fila (sale "Success" sin que el trigger corra) | mitigate | Cada caso **fuerza su fila** sembrando su propio service, y asierta el **estado real** de la base después (`modeOf` / `serviceExists`), no solo el error que volvió |
| T-16-14 | Elevation of Privilege | asertar aislamiento con el cliente service-role (bypassa RLS ⇒ el caso pasaría por el motivo equivocado) | mitigate | Se conservan los **dos guards anti-falso-verde** del `beforeAll`: abortar si el cliente de aserción perdió la sesión anon, y abortar si la anon key coincide con la service-role |
| T-16-15 | Tampering | la cobertura de GATE-01 se prueba solo en la dirección cómoda | mitigate | La matriz es **completa por dirección**: dos seguras que pasan y dos peligrosas que rechazan, cada una con su propio caso y con relectura de la fila. Más la aserción de `is_group = false` sobre el turno preexistente, que es la razón por la que la dirección segura es segura |
| T-16-16 | Denial of Service (integridad) | los casos de GATE-03 dependen del reloj: en la ventana de medianoche AR medirían lo contrario de lo que dicen medir | mitigate | Constantes fijas de madrugada y de fin de día + guard en el `beforeAll` que **tira** (no skipea) fuera de `[01:00, 23:30]` AR, con el motivo escrito. Un skip silencioso escondería el agujero |
| T-16-17 | Repudiation | la 070 se aplica a prod sin evidencia de en qué estado estaba ni de qué quedó | mitigate | El runbook fija pre-flight con **criterio de aborto**, verificación **por instalación** sobre `pg_proc` / `pg_trigger`, rollback por objeto y una sección de registro para el día de la aplicación |
| T-16-18 | Information Disclosure | datos de prueba en la base local | **accept** | Los fixtures usan nombres con prefijo `__test_` y `teardownOneTenant` borra el negocio entero por cascada; corren contra el Supabase **local**, nunca contra prod |
| T-16-SC | Tampering (supply chain) | dependencias | mitigate | **Cero** paquetes nuevos: los tests usan `vitest` y `@supabase/supabase-js`, ya presentes. No hay `npm install`, así que no aplica el gate de legitimidad de paquetes |
</threat_model>

<verification>
1. `./node_modules/.bin/vitest run test/capacity-mode-change-gate.test.ts test/service-delete-gate.test.ts --no-file-parallelism --testTimeout=30000`
   con **0 fallos** y **0 skips**. Declarar el conteo por archivo: hoy son 7 y 13; tienen que quedar
   **&gt;= 13** (el 4 se absorbe, no se suma) y **&gt;= 15**.
2. Regresión de los llamadores del fixture tocado:
   `./node_modules/.bin/vitest run test/concurrency.test.ts test/booking-cualquiera-public.test.ts --no-file-parallelism --testTimeout=30000`
   en verde.
3. `./node_modules/.bin/tsc --noEmit` exit **0**.
4. **A/B ejecutado y declarado caso por caso**: con los cuerpos viejos instalados, los cinco casos
   discriminantes caen; con la 070 restaurada, todo vuelve a verde. Un test de garantía que pasa con y
   sin la garantía no cuenta como verificación.
5. El runbook existe y cumple sus criterios de grep.
6. `git diff --name-only` no toca `supabase/`, `app/` ni `lib/`.
7. La 070 sigue **sin aplicar** en producción al cerrar la fase (D-08).
</verification>

<success_criteria>
- Hay un test **por dirección**: las dos seguras pasan, las dos peligrosas rechazan con su código de
  dominio, y la fila se relee en los cuatro casos.
- `completed` en un turno futuro ya no abre el gate de modo, y sigue sin trabar el borrado: un testigo
  escrito de cada lado de la divergencia.
- La frontera de "hoy" distingue la **hora** en los dos gates, y hay un caso que fija cada lado del
  corte.
- Cada caso discriminante **se vio fallar** contra el predicado viejo, y está declarado cuál es
  discriminante y cuál invariante.
- El runbook deja pre-flight con criterio de aborto, verificación por instalación y rollback por
  objeto, con lo que se pierde al revertir escrito.
</success_criteria>

<output>
Create `.planning/workstreams/motor-reservas/phases/16-correcciones-del-gate/16-02-SUMMARY.md` when done
</output>
