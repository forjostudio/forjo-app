---
phase: 18-el-modelo-y-la-disponibilidad
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - test/helpers/booking-fixtures.ts
  - test/availability-service-window.test.ts
  - app/api/booking/availability/route.ts
autonomous: true
requirements: [AGENDA-03, AGENDA-04]

must_haves:
  truths:
    - "Pedir disponibilidad para un servicio deja de ofrecer los horarios de las franjas que no lo dan (AGENDA-03)"
    - "Las franjas comodin (0 filas) siguen ofreciendo todo, igual que hoy (D-01/D-02)"
    - "Un horario que tambien produce una franja que SI da el servicio no se oculta, aunque otra franja solapada no lo de"
    - "Un cliente que no manda serviceId (canchas, clientes viejos) recibe una respuesta byte-identica a la de hoy (AGENDA-04)"
    - "La respuesta no cambia de forma: sigue siendo ok/busy/full y no dice POR QUE un horario esta oculto"
    - "Las tres ramas del endpoint respetan la regla, no solo el camino por defecto"
  artifacts:
    - path: "app/api/booking/availability/route.ts"
      provides: "los horarios de las franjas que no dan el servicio pedido se suman a `full` en las tres ramas"
      contains: "startTimesNotOffered"
    - path: "test/availability-service-window.test.ts"
      provides: "verificacion contra la DB local: casos CON filas mapeadas, no solo el comodin"
      contains: "availabilityGET"
    - path: "test/helpers/booking-fixtures.ts"
      provides: "seedTimeBlockService — siembra una fila de la puente sobre un tenant ya sembrado"
      contains: "seedTimeBlockService"
  key_links:
    - from: "app/api/booking/availability/route.ts"
      to: "lib/time-block-services.ts"
      via: "import del helper puro — la regla del comodin NO se reimplementa acá (AGENDA-02)"
      pattern: "time-block-services"
    - from: "app/api/booking/availability/route.ts"
      to: "time_block_services"
      via: "lectura con service role filtrada por business_id (aislamiento por tenant)"
      pattern: "time_block_services"
---

<objective>
Que **la superficie que OFRECE** respete la regla: pedir disponibilidad para un servicio deja de
ofrecer los horarios de las franjas que no lo dan, y sigue ofreciendo todas las de comodín
(AGENDA-03).

Purpose: es la mitad visible del milestone. Hoy `/api/booking/availability` filtra las franjas por
negocio y por día de la semana, y **no** por servicio — aunque ya recibe `serviceId` desde 15-04. El
resultado es que un negocio de clases ofrece cerámica en el horario de yoga.

Output: el endpoint suma los horarios no ofrecidos a `full` en sus **tres** ramas, más una suite de
integración contra la DB local que ejercita los casos **con filas mapeadas** (los únicos que prueban
algo, D-02).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/18-el-modelo-y-la-disponibilidad/18-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/18-el-modelo-y-la-disponibilidad/18-PATTERNS.md
@.planning/workstreams/motor-reservas/phases/18-el-modelo-y-la-disponibilidad/18-02-SUMMARY.md
@lib/time-block-services.ts
@.claude/skills/supabase-multitenant-rls/SKILL.md
</context>

<constraints>
- **Ejecución sobre `main`, sin worktree.** Este plan toca **tres** archivos:
  `test/helpers/booking-fixtures.ts`, `test/availability-service-window.test.ts` (nuevo) y
  `app/api/booking/availability/route.ts`. Si te encontrás editando `lib/booking-core.ts`,
  `app/api/booking/create/route.ts` o cualquier `.tsx`, te fuiste de alcance.
- ⚠ **CERO archivos `.tsx`.** El cliente público (`app/[slug]/booking-client.tsx`) **ya** oculta los
  horarios que vienen en `full` y **ya** manda siempre `serviceId`. No hay nada que tocar ahí, y
  tocarlo es Phase 20.
- **La regla del comodín NO se reimplementa acá.** Se importa del helper del Plan 02. Si aparece un
  `filter` sobre la puente adentro del route handler que decida comodín vs. mapeo explícito, se
  rompió AGENDA-02.
- **Anti-tampering de tenant:** la lectura de la puente va con `.eq('business_id', business.id)`. El
  `serviceId` que llega del cliente **ya** está re-validado por la resolución izada del endpoint
  (líneas ~46-58: si es de otro negocio, corta con `invalid_service` 400 antes de todo).
- **El contrato de respuesta no cambia:** sigue siendo `{ ok, busy, full }` con las mismas claves.
  Los horarios no ofrecidos se mezclan en `full` con los llenos y quedan **indistinguibles** — el
  público no recibe el motivo (misma regla de no-fuga que ya rige `full`).
- **NUNCA `npx tsc`** → `./node_modules/.bin/tsc --noEmit`. Para cambios bajo `app/`, `npm run build`
  es más fuerte.
- **`npx vitest run` completo NO es un gate útil** (suites de abono flaky en paralelo). Correr suites
  nombradas.
- La suite de integración corre contra el **Postgres LOCAL** (`.env.test.local` pisa `.env.local`).
  **Nunca** contra producción. La 071 tiene que estar aplicada localmente (Plan 01).
- `grep -ciF` aborta en este Git Bash (exit 134): usar `-cF` **o** `-ciE`, nunca los tres flags.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: El fixture de la puente y la suite de integración (queda ROJA — la cierra el Task 2)</name>
  <files>test/helpers/booking-fixtures.ts, test/availability-service-window.test.ts</files>
  <read_first>
    - `test/helpers/booking-fixtures.ts:235-241` (`seedProfessionalService`) — el molde exacto del
      fixture nuevo: service-role, sin retorno, `throw` en error.
    - `test/helpers/booking-fixtures.ts:103-126` (`seedTimeBlock`) — la firma y los defaults
      (`dayOfWeek: 1`, `08:00`-`20:00`), y que devuelve el id del bloque.
    - `test/booking-cualquiera-public.test.ts:1-60` — cómo se invoca el route handler real de
      disponibilidad (`GET as availabilityGET`), la constante de fecha `'2031-03-03'` (lunes, dow=1,
      alineada con el default de `seedTimeBlock`) y el `describe.skipIf(!hasSupabaseCreds)`.
    - `test/service-coverage-public.test.ts:1-45` — el molde de cabecera de una suite de integración
      de esta fase: qué bug cierra, qué casos y por qué.
    - `18-CONTEXT.md` D-02 — por qué un caso con la puente vacía **no prueba nada**.
  </read_first>
  <action>
    **(a) El fixture.** Agregar a `test/helpers/booking-fixtures.ts` una función `seedTimeBlockService`
    que reciba el tenant sembrado y el par `{ timeBlockId, serviceId }` e inserte la fila en
    `time_block_services` con el `business_id` del tenant. Molde literal de `seedProfessionalService`:
    service-role, sin retorno, `throw` con mensaje contextualizado si el insert falla. Comentario
    arriba explicando que el teardown por CASCADE del negocio ya limpia estas filas, y que sembrar
    **una sola** fila cambia el modo de esa franja de comodín a mapeo explícito — que es exactamente
    lo que estos tests necesitan medir.

    **(b) La suite.** Crear `test/availability-service-window.test.ts` con
    `describe.skipIf(!hasSupabaseCreds)` y una cabecera densa en español que diga qué congela: la
    regla del comodín aplicada al **read-path**, ejercitada con **filas presentes**, porque el estado
    vacío es el camino de hoy.

    Escenario compartido (`beforeAll`): `seedOneTenant` con duración de servicio 30 y buffer 0; un
    segundo servicio con `seedService`; y **tres** franjas del mismo día de la semana (dow 1, la
    fecha `'2031-03-03'`): **A** 09:00-12:00, **B** 12:00-15:00 y **C** 09:00-12:00 (C **solapa** a A
    a propósito). Guardar los tres ids.

    Los casos, todos golpeando el route handler real con service role:

    1. **Con filas, servicio NO mapeado** — mapear A→servicio1 y C→servicio1. Pedir disponibilidad
       para el **servicio2**: los horarios que genera A/C (09:00…11:30, paso 30) tienen que venir en
       `full`, y los de B (12:00…14:30, comodín) **no**.
    2. **Con filas, servicio SÍ mapeado** — mismo estado, pedir para el **servicio1**: ningún horario
       de bloque en `full` (sólo ocupación real, que acá es cero). — *control: distingue "la regla
       aplica" de "el endpoint oculta todo"*
    3. **Comodín (0 filas)** — borrar las filas de la puente y volver a pedir para el servicio2:
       `full` no contiene ningún horario de bloque. Es la cero regresión de AGENDA-04… **y por sí
       solo no prueba nada**: vale únicamente emparejado con el caso 1. Decirlo en el texto del test.
    4. **Solape** — mapear A→servicio1 y **C→servicio2**, pedir para el servicio2: las 09:00 **no**
       están en `full` (C las produce y C sí da el servicio2), aunque A no lo dé. — *control negativo
       de la resta de conjuntos*
    5. **Rama "Cualquiera"** — con un segundo profesional comodín y `any=1&serviceId=servicio2`
       (cupo 1), con A/C mapeadas al servicio1: los horarios de A/C siguen ocultos.
    6. **Rama recurso simultáneo** — llevar el servicio2 a `simultaneous_resource` con cupo 2
       (`seedSimultaneousService`), pedir para él: los horarios de A/C siguen ocultos. ⚠ Devolver el
       servicio al modo por defecto en el `afterEach`/`afterAll`, como advierte el propio fixture: el
       tenant se comparte entre casos.
    7. **Sin `serviceId`** — con las filas puestas, pedir sin `serviceId` (el caso de canchas y de
       los clientes viejos): `full` no contiene ningún horario de bloque. La regla **no** se aplica
       cuando no se sabe de qué servicio se habla.

    Al terminar el task, la suite tiene que estar **ROJA** en los casos 1, 4, 5 y 6 (el endpoint
    todavía no filtra) y **VERDE** en 2, 3 y 7. **Ese rojo es esperado y lo cierra el Task 2 de este
    mismo plan** — dejarlo declarado en el SUMMARY con los nombres de los casos.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && grep -cF "seedTimeBlockService" test/helpers/booking-fixtures.ts && grep -cE "^  it\(" test/availability-service-window.test.ts</automated>
  </verify>
  <done>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF "seedTimeBlockService" test/helpers/booking-fixtures.ts` ≥ **1** y el fixture inserta
      en `time_block_services` con el `business_id` del tenant.
    - `grep -cE "^  it\(" test/availability-service-window.test.ts` ≥ **7**.
    - `npx vitest run test/availability-service-window.test.ts` corre **sin skipear** (las tres creds
      están; si skipea, la medición no existe y el task no está hecho) y falla en **exactamente** los
      casos 1, 4, 5 y 6.
    - El SUMMARY declara el rojo esperado y que lo cierra el Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: El endpoint deja de ofrecer los horarios de las franjas que no dan el servicio</name>
  <files>app/api/booking/availability/route.ts</files>
  <read_first>
    - `app/api/booking/availability/route.ts:94-110` — la lectura de `time_blocks` del día
      (`.select('start_time, end_time')` filtrado por negocio + `day_of_week`) y la definición de
      `toMin` justo debajo. **Ahí va el código nuevo.**
    - `app/api/booking/availability/route.ts:46-58` — la resolución izada del servicio, que es la que
      ya hizo el anti-tampering por `business_id`.
    - `app/api/booking/availability/route.ts:130-145` — cómo el propio archivo **ya** lee
      `professional_services` con service role y la interpreta con el helper puro
      `professionalsForService`. Es el molde de import y de uso.
    - Las **tres** líneas donde el archivo devuelve `{ ok: true, ... }`: la rama "Cualquiera"
      (~272), la del recurso simultáneo (~378) y la final (~530).
    - `18-PATTERNS.md` §4 y `lib/time-block-services.ts` (cabecera de `startTimesNotOffered`).
  </read_first>
  <action>
    ⚠ **Leé esto antes de escribir una línea, porque la intuición apunta al lado equivocado.** Este
    endpoint **no devuelve la grilla de horarios**: devuelve `busy` (ocupados) y `full` (la lista de
    horarios a **ocultar**). La grilla la arma el cliente con sus propias franjas. Sus tres usos de
    los bloques del día son para **enumerar candidatos a ocultar**. Por eso *filtrar* la lista de
    bloques —quedarse con los que dan el servicio— produce **menos** horarios ocultos, o sea
    **ofrece de más**: es la regla al revés. Lo correcto es **sumar** a `full` los horarios que sólo
    producen franjas que no dan el servicio.

    Tres cambios, en este orden.

    **(a)** En la lectura de `time_blocks` del día, agregar la columna de id al `select` (hoy trae
    sólo apertura y cierre). Sin el id no hay con qué cruzar contra la puente. Comentar por qué se
    suma la columna.

    **(b)** Inmediatamente después de la definición de `toMin` —o sea **antes** de la rama
    "Cualquiera", que retorna temprano—, declarar `notOffered` como arreglo de horarios `'HH:MM'`.
    Cuando hay servicio resuelto (`svc` y el parámetro), leer la puente con el service role ya
    creado: `time_block_services` filtrada por `.eq('business_id', business.id)` (aislamiento por
    tenant, aunque sea un read; mismo criterio que la lectura de `professional_services` que el
    archivo ya hace unas líneas abajo), y llenar `notOffered` con `startTimesNotOffered(...)` pasando
    el `serviceId` ya re-validado, los bloques del día, las filas de la puente y la duración del
    servicio.

    Comentar el bloque con densidad: qué requisito implementa (AGENDA-03), por qué la regla vive en
    el helper y no acá (AGENDA-02 — tres capas la tienen que interpretar igual), por qué se calcula
    **una vez arriba** en vez de dentro de cada rama (las tres necesitan lo mismo, y una de ellas
    retorna temprano), y el fail-safe: **sin `serviceId` no se aplica nada** (canchas y clientes
    viejos quedan byte-idénticos, AGENDA-04) y **con la puente vacía el helper devuelve vacío**
    (día de la migración: todos los negocios en cero, D-02).

    **(c)** Sumar `notOffered` a `full` en los **tres** `return Response.json({ ok: true, ... })` del
    archivo: el de la rama "Cualquiera", el de la rama de recurso simultáneo y el final. Concatenar
    sin mutar el arreglo original de cada rama. En el `return` final, `full` ya viene mutado por el
    bloque de espacio compartido: concatenar ahí también.

    Dejar escrito en un comentario, arriba del cálculo, el **caveat conocido y aceptado** (ya
    documentado en este archivo para el eje del cupo): los horarios especiales que **extienden** la
    jornada viven en `schedule_exceptions`, no en `time_blocks`, así que no generan candidatos y no
    se pueden ocultar por esta vía. Quién respalda ese caso es el backstop del `create` (Plan 04) —
    y ahí, deliberadamente, un horario que no cae en ninguna franja **se acepta**.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && npx vitest run test/availability-service-window.test.ts test/booking-cualquiera-public.test.ts test/booking-public-regression.test.ts test/canchas-booking.test.ts</automated>
  </verify>
  <done>
    - `./node_modules/.bin/tsc --noEmit` exit **0** y `npm run build` exit **0**.
    - `npx vitest run test/availability-service-window.test.ts` exit **0**, **0 failed**, **0
      skipped**: los 7 casos en verde, incluidos el 1, 4, 5 y 6 que estaban rojos.
    - No-regresión: `npx vitest run test/booking-cualquiera-public.test.ts
      test/booking-public-regression.test.ts test/canchas-booking.test.ts test/concurrency.test.ts`
      exit **0**, 0 failed.
    - `grep -cE "^import .*time-block-services" app/api/booking/availability/route.ts` = **1**.
    - `grep -cE "startTimesNotOffered\(" app/api/booking/availability/route.ts` ≥ **1**.
    - `grep -cE "\.from\('time_block_services'\)" app/api/booking/availability/route.ts` = **1** — la
      puente se lee **una sola vez**, arriba, no una por rama (la rama "Cualquiera" retorna temprano).
    - `grep -cE "\.concat\(notOffered\)" app/api/booking/availability/route.ts` ≥ **3** — las tres
      ramas.
    - `grep -cE "return Response\.json\(\{ ok: true" app/api/booking/availability/route.ts` = **3**
      (calibrado contra el archivo actual: sigue habiendo exactamente tres salidas exitosas, no se
      agregó ni se borró ninguna).
    - `git diff --name-only` toca **exactamente** `app/api/booking/availability/route.ts`.
    - `git diff -- app/\[slug\]/booking-client.tsx app/\[slug\]/canchas-booking-client.tsx` **vacío**:
      cero `.tsx`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente anónimo → `GET /api/booking/availability` | endpoint público, alcanzable directo; decide **qué se le ofrece** a un cliente no autenticado |
| route handler (service role) → `time_block_services` | corre con service role: **bypassa RLS**, el aislamiento lo tiene que poner la query |
| respuesta `{ ok, busy, full }` → cliente anónimo | todo lo que entra en `full` es observable por cualquiera |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-11 | Information Disclosure | mezclar los horarios "no ofrecidos" con los "llenos" podría filtrar la configuración de agenda del negocio | mitigate | Van al MISMO arreglo `full`, que es un booleano por horario y nunca dice el motivo: lleno, agenda ocupada, espacio tomado y ahora "el servicio no se da acá" son indistinguibles. El contrato `{ ok, busy, full }` no cambia de forma |
| T-18-12 | Tampering | un `serviceId` de otro negocio usado para leer el mapeo ajeno | mitigate | Doble control ya existente + nuevo: la resolución izada corta con `invalid_service` (400) si el servicio no es del negocio del slug, **y** la lectura de la puente lleva `.eq('business_id', business.id)` explícito aunque corra con service role |
| T-18-13 | Denial of Service (auto-infligido, severidad alta) | implementar la regla como un **filtro** sobre los bloques en vez de una suma a `full` invierte el efecto: se ofrecen MÁS horarios, no menos, y el bug es silencioso | mitigate | El helper del Plan 02 expresa la operación como resta de conjuntos, y el caso de test 4 (franjas solapadas con el mismo inicio) falla con la implementación ingenua. Documentado en el `<action>` como la trampa central del plan |
| T-18-14 | Denial of Service (regresión) | ocultarle horarios a canchas o a los clientes viejos que no mandan `serviceId` | mitigate | El cálculo está gateado por servicio resuelto: sin `serviceId` no se lee la puente ni se oculta nada. Caso de test 7 + suite de canchas en verde |
| T-18-15 | Spoofing / bypass | un cliente forjado ignora `full` y pide reservar un horario no ofrecido | accept (lo cierra el Plan 04) | El read-path **no es el control**: es UX. La autoridad del ACEPTA es el backstop de `createAppointmentCore` (Plan 04, D-04). Aceptado acá **porque el Plan 04 del mismo wave lo cierra** — no queda abierto al final de la fase |
| T-18-16 | Denial of Service (cobertura incompleta) | los horarios especiales que EXTIENDEN la jornada no están en `time_blocks` ⇒ no se pueden ocultar por esta vía | accept | Caveat ya documentado en este mismo archivo para el eje del cupo. Coherente con la decisión del backstop: un horario que no cae en ninguna franja se acepta (es el comportamiento de hoy y no se toca en esta fase) |
| T-18-SC | Tampering (supply chain) | dependencias | accept | **Cero** paquetes nuevos. `git diff -- package.json package-lock.json` vacío |
</threat_model>

<uat_script>
Rápido, en el navegador, contra el Supabase LOCAL (`npm run dev`). **Sólo tiene sentido con filas
mapeadas**: el estado vacío es el comportamiento de hoy y no prueba nada.

1. En local, sembrar a mano una fila de la puente para el negocio de prueba: elegir una franja de la
   mañana y mapearla a **un solo** servicio (`insert into public.time_block_services ...` por
   `docker exec ... psql`). No hay UI para esto todavía — la trae la Phase 19.
2. Abrir la página pública del negocio (`/[slug]`), elegir el servicio **que NO fue mapeado** y una
   fecha del día de la semana de esa franja.
3. **Esperado:** los horarios de la franja de la mañana **no aparecen**; los de las otras franjas
   (que siguen en comodín) **sí**.
4. Volver atrás, elegir el servicio **que SÍ fue mapeado**, misma fecha.
5. **Esperado:** aparecen todos los horarios, incluidos los de la mañana.
6. Borrar la fila sembrada y recargar: **todo vuelve a aparecer** para los dos servicios (comodín).
</uat_script>

<verification>
1. `./node_modules/.bin/tsc --noEmit` exit **0**; `npm run build` exit **0**.
2. `npx vitest run test/availability-service-window.test.ts` exit 0, 0 failed, **0 skipped**.
3. `npx vitest run test/booking-cualquiera-public.test.ts test/booking-public-regression.test.ts
   test/canchas-booking.test.ts test/concurrency.test.ts` exit 0, 0 failed.
4. Los conteos de grep del Task 2 dan los valores exactos declarados.
5. `git diff --name-only` toca exactamente los tres archivos del plan; cero `.tsx`.
6. `git diff -- package.json package-lock.json` vacío.
</verification>

<success_criteria>
- Pedir horarios para un servicio devuelve **sólo** las franjas donde ese servicio se da, más todas
  las de comodín (AGENDA-03).
- Las tres ramas del endpoint respetan la regla, no sólo el camino por defecto.
- Canchas, clientes viejos y los negocios sin mapeo reciben lo mismo que hoy (AGENDA-04).
</success_criteria>

## Artifacts this phase produces

| Artefacto | Plan | Estado al cerrar este plan |
|---|---|---|
| `supabase/migrations/071_time_block_services.sql` | 18-01 | validado en LOCAL; pendiente de aplicación MANUAL a prod |
| `lib/types.ts` → `TimeBlockService` · `lib/time-block-services.ts` · su suite | 18-02 | creados, verdes |
| `test/helpers/booking-fixtures.ts` → `seedTimeBlockService` | **18-03** | creado |
| `test/availability-service-window.test.ts` | **18-03** | creado, verde (7 casos) |
| `app/api/booking/availability/route.ts` (AGENDA-03) | **18-03** | modificado |
| `lib/booking-core.ts` + `app/api/booking/create/route.ts` (backstop D-04) | 18-04 | — |

<output>
Create `.planning/workstreams/motor-reservas/phases/18-el-modelo-y-la-disponibilidad/18-03-SUMMARY.md` when done
</output>
