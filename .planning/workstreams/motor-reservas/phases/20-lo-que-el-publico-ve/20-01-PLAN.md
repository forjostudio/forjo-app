---
phase: 20-lo-que-el-publico-ve
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/[slug]/page.tsx
  - lib/staff-services.ts
  - test/service-coverage-public.test.ts
  - test/schedule-coverage-public.test.ts
autonomous: true
requirements: [AGENDA-07]

must_haves:
  truths:
    - "La vista publica public_time_block_services llega al RSC de app/[slug]/page.tsx y viaja como prop timeBlockServices a BookingClient, molde exacto de professionalServices (migr. 059) — sin filtrar servicios server-side por franja (D-02/D-04 los resuelve el cliente en el Plan 20-02)"
    - "page.tsx deja de OCULTAR con bookableServices el servicio sin cobertura de staff: pasa el catalogo activo completo a BookingClient (D-05) — la regla del comodin de staff se computa DESPUES, por servicio, no como filtro de array"
    - "isServiceStaffed(...) devuelve true en modo sentinel (0 profesionales nombrados) y en modo comodin (0 filas mapeadas), byte-identico al guard que hoy vive inline en bookableServices — bookableServices sigue pasando sus 3 casos existentes sin tocar sus aserciones"
    - "public_time_block_services es legible con la anon key SIN sesion y responde solo las filas del negocio pedido, verificado contra la DB local (no solo por lectura de codigo): un servicio con mapeo explicito da isServiceScheduled=true, un servicio huerfano da false, y con la puente vacia dan true los dos (comodin, D-02 del milestone)"
  artifacts:
    - path: "app/[slug]/page.tsx"
      provides: "8va query del Promise.all (public_time_block_services) + prop timeBlockServices a BookingClient + services sin pre-filtrar por bookableServices"
      contains: "public_time_block_services"
    - path: "lib/staff-services.ts"
      provides: "funcion isServiceStaffed con el guard sentinel ya existente en bookableServices, reusada por bookableServices (comportamiento byte-identico, refactor sin regresion)"
      exports: ["isServiceStaffed"]
    - path: "test/schedule-coverage-public.test.ts"
      provides: "verificacion contra DB local (anon key) de que la vista es legible y que isServiceScheduled distingue servicio mapeado de huerfano leyendo la ruta real"
      contains: "public_time_block_services"
  key_links:
    - from: "app/[slug]/page.tsx"
      to: "app/[slug]/booking-client.tsx"
      via: "prop timeBlockServices, molde exacto de professionalServices"
      pattern: "timeBlockServices"
    - from: "app/[slug]/page.tsx"
      to: "lib/staff-services.ts"
      via: "page.tsx DEJA de llamar bookableServices para filtrar el array (D-05); la funcion sigue existiendo pero el consumidor por-servicio pasa a ser booking-client.tsx en el Plan 20-02"
      pattern: "bookableServices"
---

<objective>
Preparar los datos que el Plan 20-02 necesita para resolver AGENDA-07 en el cliente, sin tocar todavia
ni un pixel de `booking-client.tsx`: (1) sumar la vista `public_time_block_services` (migr. 071 §3, ya
en producción) al `Promise.all` de `app/[slug]/page.tsx` y pasarla como prop nueva, molde exacto de
`professionalServices` (migr. 059); (2) dejar de pre-filtrar el catálogo de servicios con
`bookableServices` antes de que llegue a `BookingClient` — D-05 del CONTEXT unifica el tratamiento del
vacío: el eje staff pasa de OCULTAR a DESHABILITAR-CON-MOTIVO, así que el filtro por array deja de tener
sentido y se reemplaza por un chequeo por-servicio (`isServiceStaffed`, nueva) que el Plan 20-02 va a
consumir igual que ya consume `isServiceScheduled` para el eje franja.

Purpose: la Phase 18 dejó la vista creada **sin consumidor**, explícitamente para esta fase — no hace
falta migración ni endpoint nuevo (fence 3). D-03 del CONTEXT sigue intacto: este plan no toca
`lib/landing/derive.ts`, ni tiene motivo técnico para hacerlo (la landing no consume la puente).

Output: `app/[slug]/page.tsx` con la 8va query + prop nueva + catálogo sin pre-filtrar;
`lib/staff-services.ts` con `isServiceStaffed` exportada; `test/schedule-coverage-public.test.ts` nuevo,
mirror de `test/service-coverage-public.test.ts` para el eje franja.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-PATTERNS.md
@app/[slug]/page.tsx
@lib/staff-services.ts
@lib/time-block-services.ts
@test/service-coverage-public.test.ts
@.claude/skills/convenciones-forjo/SKILL.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
</context>

<constraints>
- **D-03 (locked, fence 1) — `lib/landing/derive.ts` NO se toca.** Ningún task de este plan escribe en
  ese archivo. Si durante la implementación aparece una razón para hacerlo, es señal de que hay que
  rediscutir D-03, no de que se puede tocar de paso.
- **Fence 3 — sin migración nueva.** La vista `public_time_block_services` y su `GRANT SELECT` ya están
  en producción (migr. 071/072). La próxima migración libre sigue siendo la 077; este plan no crea
  ninguna. Si algo parece requerir DDL, revisar de nuevo el enfoque.
- **AGENDA-02 — la regla del comodín no se reimplementa.** `isServiceScheduled`/`blocksForService`
  (franja) e `isServiceStaffed`/`isServiceCovered` (staff) son las ÚNICAS fuentes. Cero
  `.filter(r => r.service_id === ...)` inline en este plan.
- **Service role solo en route handlers.** El RSC de `page.tsx` sigue usando `createPublicServerClient()`
  (anon, sin cookies) para la query nueva — mismo cliente que las otras 7 queries del `Promise.all`.
- **`bookableServices` no cambia de comportamiento en este plan** (solo se refactoriza por dentro): el
  test `test/service-coverage-public.test.ts` tiene que seguir en verde SIN tocar sus 3 `it(...)`.
- **NUNCA `npx tsc`** (baja un paquete que no es el compilador y siempre sale 0) →
  `./node_modules/.bin/tsc --noEmit`.
- Comentarios densos **en español**, explicando el *por qué*, no el *qué* — convención del repo.
- Cero dependencias nuevas: `git diff -- package.json package-lock.json` tiene que quedar vacío.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: RSC — sumar la vista al Promise.all y dejar de pre-filtrar el catálogo (D-05)</name>
  <files>app/[slug]/page.tsx</files>
  <read_first>
    - `app/[slug]/page.tsx` **íntegro (205 líneas)**. Puntos exactos: el `Promise.all` de las líneas
      76-98 (7 queries; el molde a espejar es el bloque `public_professional_services`, líneas 91-97,
      con su comentario de fail-safe `|| []`); el `import { bookableServices } from '@/lib/staff-services'`
      en la línea 9; el comentario "Gap UAT Phase 10" y la constante `staffBookableServices`
      (líneas 136-141, a reemplazar); la construcción de `bookingNode` y el `<BookingClient>`
      (líneas 144-162, donde hoy se pasa `services={staffBookableServices}`).
    - `supabase/migrations/071_time_block_services.sql` líneas 144-151 — la vista
      `public_time_block_services` (columnas `business_id, time_block_id, service_id`), `OWNER TO
      postgres`, sin `security_invoker`.
    - `supabase/migrations/072_public_views_read_only.sql` líneas 102-106 — el `REVOKE ALL` +
      `GRANT SELECT` a `anon` sobre esa vista.
    - `lib/types.ts` líneas 180-184 — `TimeBlockService { business_id, time_block_id, service_id }`, el
      tipo de fila que va a viajar en la prop nueva.
    - `.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-CONTEXT.md` — D-02, D-04
      (por qué la query viaja cruda, sin filtrar server-side) y D-05 completo (por qué se acepta el
      costo de cambiar el comportamiento de `bookableServices` en `page.tsx`).
  </read_first>
  <action>
    En el `Promise.all` (líneas 76-98), agregar como **8vo elemento** del array la query
    `supabase.from('public_time_block_services').select('*').eq('business_id', business.id)`, con un
    comentario (molde del bloque de `public_professional_services`, líneas 91-97) que documente: qué
    vista es (migr. 071 §3), que NO se abre la tabla base `time_block_services` a `anon` (mismo criterio
    que D-07 de la migr. 059 para el staff), que el consumidor es `BookingClient` vía la regla del
    comodín de `lib/time-block-services` (D-02/D-04/AGENDA-07), y que el fail-safe `|| []` degrada al
    comodín (todo agendado) — nunca al revés (Pitfall 5 del research). Agregar la variable
    correspondiente (nombrarla `timeBlockServices`) en la posición 8 del array de destructuring de la
    línea 76.

    Pasar `timeBlockServices={timeBlockServices || []}` como prop nueva al `<BookingClient>` de las
    líneas 153-161 (mismo patrón `|| []` que `professionalServices` en la línea 160). El
    `<CanchasBookingClient>` (líneas 145-151) y el `<LandingRenderer>` (líneas 193-202) NO reciben esta
    prop — la landing no la necesita (D-03) y canchas no mapea servicio↔franja en esta fase.

    **D-05 — dejar de pre-filtrar el catálogo con `bookableServices`:**
    - Quitar el `import { bookableServices } from '@/lib/staff-services'` de la línea 9.
    - Quitar la constante `staffBookableServices` (línea 141) y su comentario "Gap UAT Phase 10"
      (líneas 136-140).
    - Pasar `services={services || []}` directo al `<BookingClient>` — el mismo array crudo que ya
      recibe `<LandingRenderer>` en la línea 196 (las dos ramas quedan consistentes: la landing siempre
      mostró el catálogo completo, y ahora `BookingClient` también lo recibe completo).
    - En el lugar donde estaba el comentario borrado, dejar uno nuevo y corto explicando que el filtro
      por cobertura de staff se movió: ya NO oculta servicios en el array que llega al selector público,
      ahora `booking-client.tsx` (Plan 20-02) deshabilita-con-motivo cada tarjeta usando
      `isServiceStaffed` por servicio — cita D-05 y deja explícito que esto **modifica intencionalmente**
      un comportamiento que ya pasó UAT en producción (el gap de la Phase 10), decisión del dueño
      aceptando el costo.

    No tocar ninguna otra rama del archivo: el bloque canchas (líneas 144-151), el gateo por vertical
    (línea 135), `force-dynamic` (línea 37), ni las 7 queries existentes del `Promise.all`.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && grep -cF "public_time_block_services" "app/[slug]/page.tsx" && grep -cF "timeBlockServices" "app/[slug]/page.tsx" && grep -cF "staffBookableServices" "app/[slug]/page.tsx"; grep -cE "from '@/lib/staff-services'" "app/[slug]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF "public_time_block_services" "app/[slug]/page.tsx"` >= **1**.
    - `grep -cF "timeBlockServices" "app/[slug]/page.tsx"` >= **2** (la variable desestructurada + la
      prop en el JSX).
    - `grep -cF "staffBookableServices" "app/[slug]/page.tsx"` = **0** — la constante vieja ya no existe.
    - `grep -cE "from '@/lib/staff-services'" "app/[slug]/page.tsx"` = **0** — el import se quitó
      entero (el consumo por-servicio se mudó al Plan 20-02).
    - `grep -cF "services={staffBookableServices}" "app/[slug]/page.tsx"` = **0** y
      `grep -cF "services={services || []}" "app/[slug]/page.tsx"` >= **1** (BookingClient recibe el
      catálogo sin pre-filtrar).
  </acceptance_criteria>
  <done>
    `page.tsx` suma la 8va query + prop `timeBlockServices`, y `BookingClient` recibe el catálogo de
    servicios SIN el filtro `bookableServices` — el eje staff queda listo para que el Plan 20-02 lo
    resuelva por-servicio en el cliente.
  </done>
</task>

<task type="auto">
  <name>Task 2: lib/staff-services.ts — isServiceStaffed (el guard sentinel, exportado y reusado)</name>
  <files>lib/staff-services.ts, test/service-coverage-public.test.ts</files>
  <read_first>
    - `lib/staff-services.ts` **íntegro (88 líneas)**. Puntos exactos: `professionalsForService`
      (líneas 43-53), `isServiceCovered` (líneas 62-68) y `bookableServices` (líneas 80-87, el guard
      sentinel vive inline en la línea 85: `if (activeProfessionals.length === 0) return [...services]`).
    - `lib/time-block-services.ts` líneas 111-117 — `isServiceScheduled`, el molde de nombre y JSDoc a
      espejar: una función booleana que DELEGA en la primitiva ya existente (nunca reimplementa), con
      un JSDoc que explica el POR QUÉ y quién la va a consumir.
    - `test/service-coverage-public.test.ts` **íntegro (110 líneas)**. El docstring de cabecera
      (líneas 13-20) dice hoy "el fix filtra la lista que `app/[slug]/page.tsx` pasa a `BookingClient`
      con `bookableServices`" — eso deja de ser cierto tras el Task 1 de este mismo plan y hay que
      corregirlo. Los 3 `it(...)` (líneas 74-109) NO se tocan.
    - `.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-CONTEXT.md` — D-05
      completo.
  </read_first>
  <action>
    Agregar una función nueva exportada `isServiceStaffed(serviceId, activeProfessionals, bridge):
    boolean`, ubicada inmediatamente ANTES de `bookableServices` (línea 80). Cuerpo: el MISMO guard
    sentinel que hoy vive inline en `bookableServices` (`activeProfessionals.length === 0` ⇒ `true`)
    seguido de `isServiceCovered(serviceId, activeProfessionals, bridge)`. JSDoc en español que explique:
    es la versión POR SERVICIO del guard que hasta ahora solo existía embebido en el filtro de
    `bookableServices`; la necesita `booking-client.tsx` (Plan 20-02) para decidir, servicio por
    servicio, si deshabilitar la tarjeta del paso 1 con el motivo "Sin profesional disponible" (D-05) en
    vez de ocultarlo del array — el comportamiento viejo, el gap UAT de la Phase 10.

    Reescribir el CUERPO de `bookableServices` para que delegue en `isServiceStaffed`:
    `services.filter((s) => isServiceStaffed(s.id, activeProfessionals, bridge))`. La firma no cambia.
    Este es un refactor **preservando comportamiento** — `test/service-coverage-public.test.ts` tiene
    que seguir en verde SIN modificar sus 3 `it(...)`. Actualizar el comentario de cabecera de
    `bookableServices` (líneas 70-79) para anotar que sigue existiendo como filtro puro reutilizable,
    pero que `app/[slug]/page.tsx` YA NO la llama para armar el array del selector público (Plan 20-01
    Task 1, D-05): el consumidor por-servicio ahora es `isServiceStaffed`, usado directo dentro de
    `booking-client.tsx` (Plan 20-02).

    En `test/service-coverage-public.test.ts`, actualizar el docstring de cabecera (líneas 13-20) para
    dejar de decir que "el fix filtra la lista que `app/[slug]/page.tsx` pasa a `BookingClient`" — en su
    lugar, explicar que este archivo verifica la función pura `bookableServices` en sí misma (que sigue
    existiendo y sigue siendo correcta), y que el comportamiento visible al público para el eje staff
    (deshabilitar-con-motivo en vez de ocultar, D-05) lo verifica ahora `booking-client.tsx` con
    `isServiceStaffed` (ver Plan 20-02). NO tocar ninguno de los 3 `it(...)` ni sus aserciones — el
    contrato de la función no cambió.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && npx vitest run test/service-coverage-public.test.ts && grep -cE "^export function " lib/staff-services.ts && grep -cF "isServiceStaffed" lib/staff-services.ts</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `npx vitest run test/service-coverage-public.test.ts` exit **0**, 0 failed, 0 skipped — los 3 casos
      (a)/(b)/(c) siguen pasando SIN que sus aserciones hayan cambiado
      (`git diff -- test/service-coverage-public.test.ts` no toca ninguna línea con `expect(`).
    - `grep -cE "^export function " lib/staff-services.ts` = **5** (las 4 existentes + `isServiceStaffed`).
    - `grep -cF "isServiceStaffed" lib/staff-services.ts` >= **2** (la definición + el uso dentro de
      `bookableServices`).
    - `grep -cF "activeProfessionals.length === 0" lib/staff-services.ts` >= **1** dentro de
      `isServiceStaffed` (el guard sentinel se movió, no se duplicó de una forma distinta).
  </acceptance_criteria>
  <done>
    `isServiceStaffed` existe, exportada, con el mismo guard sentinel que ya validaban los 3 casos de
    `test/service-coverage-public.test.ts`; `bookableServices` delega en ella y sigue pasando sin tocar
    sus aserciones.
  </done>
</task>

<task type="auto">
  <name>Task 3: test/schedule-coverage-public.test.ts — la vista leída por anon, eje franja</name>
  <files>test/schedule-coverage-public.test.ts</files>
  <read_first>
    - `test/service-coverage-public.test.ts` **íntegro** — el molde exacto de estructura: fixture
      `seedOneTenant`/`teardownOneTenant`, `describe.skipIf(!hasSupabaseCreds)`, un `afterEach` que
      resetea al estado comodín entre casos.
    - `test/isolation.test.ts` líneas 366-457 — el `describe('agenda por servicio: time_block_services +
      su vista pública')`. El caso `'el público SÍ puede LEER por la vista acotada'` (líneas 440-457) YA
      prueba el permiso `anon` sobre `public_time_block_services` con el patrón `anonPublic()` (línea
      390: `createClient(url, anonKey, { auth: { persistSession: false } })`). Este archivo nuevo NO
      repite esa aserción de permisos — la da por hecha y agrega la capa de encima: aplicar
      `isServiceScheduled` sobre lo leído por esa misma ruta.
    - `test/helpers/booking-fixtures.ts` líneas 40-89 (`seedOneTenant`/`SeededTenant`), 104-127
      (`seedTimeBlock`), 144-167 (`seedService`) y 243-261 (`seedTimeBlockService`) — las firmas exactas
      de los 4 fixtures a usar.
    - `lib/time-block-services.ts` líneas 38-42 (`BlockWindow`) y 111-117 (`isServiceScheduled`) — la
      función a ejercitar; `TimeBlock` (que devuelve `time_blocks`) ya satisface `{ id: string }`, sin
      cast.
    - `test/env.ts` — `hasSupabaseCreds`, el mismo skip-graceful que usa toda la suite.
  </read_first>
  <action>
    Crear `test/schedule-coverage-public.test.ts`, molde carácter por carácter de
    `test/service-coverage-public.test.ts` (mismo estilo de cabecera explicando el POR QUÉ, mismo
    `describe.skipIf(!hasSupabaseCreds)`), pero para el eje FRANJA:

    1. `beforeAll`: `seedOneTenant()` (deja 1 servicio `t.serviceId` + 1 profesional comodín, sin usarse
       acá), un 2º servicio "huérfano" con `seedService(t)`, y UN `time_block` con `seedTimeBlock(t)`
       (los defaults de día/horario alcanzan).
    2. Una función local `readPublicSchedule()` que lee, con un cliente `anon` **SIN sesión** (mismo
       patrón `anonPublic()` que `test/isolation.test.ts:390` — NO usar `t.admin`, porque el punto de
       este archivo es ejercitar la ruta real que usa `app/[slug]/page.tsx`), dos cosas filtradas por
       `.eq('business_id', t.businessId)`: `time_blocks` (tabla base, tiene policy pública) y
       `public_time_block_services` (la vista).
    3. Caso (a): tras `seedTimeBlockService({ timeBlockId, serviceId: t.serviceId })` (mapeo explícito),
       leer con `readPublicSchedule()` y verificar `isServiceScheduled(t.serviceId, blocks, bridge)` ===
       `true` y `isServiceScheduled(svc2, blocks, bridge)` === `false` (el huérfano).
    4. Caso (b), control de no-regresión: SIN ningún `seedTimeBlockService` (la puente queda vacía,
       comodín), `isServiceScheduled` da `true` para los DOS servicios leyendo por la misma ruta.
    5. `afterEach`: `t.admin.from('time_block_services').delete().eq('business_id', t.businessId)` para
       volver al estado comodín entre casos (mismo patrón que el `afterEach` de
       `service-coverage-public.test.ts` sobre `professional_services`).

    El caso (a) es el que de verdad muerde (research, Pitfall 2): si la vista quedara con permisos mal
    aplicados, o si por error se leyera `time_block_services` (tabla base) en vez de
    `public_time_block_services` (la vista) con el cliente anon, este test se pone rojo con datos
    reales — no depende de una lectura de código para detectarlo.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && npx vitest run test/schedule-coverage-public.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `npx vitest run test/schedule-coverage-public.test.ts` exit **0** — con las 3 credenciales de
      Supabase local presentes corre y pasa (0 failed); sin ellas, skipea limpio (0 failed, N skipped),
      igual que el resto de la suite.
    - `grep -cF "public_time_block_services" test/schedule-coverage-public.test.ts` >= **1** y
      `grep -cF "persistSession: false" test/schedule-coverage-public.test.ts` >= **1** (se lee con
      cliente anon sin sesión, no con `t.admin`).
    - `grep -cF "isServiceScheduled" test/schedule-coverage-public.test.ts` >= **2** (los casos a y b).
  </acceptance_criteria>
  <done>
    El eje franja tiene su test espejo del eje staff: la vista pública se lee con anon key por la ruta
    real y `isServiceScheduled` distingue mapeado de huérfano contra datos de la DB local.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cliente anónimo (browser) → RSC `/[slug]` | El único input no confiable que entra a este plan es el `slug` de la URL, ya resuelto contra `public_businesses` antes del `Promise.all`. |
| RSC → vista `public_time_block_services` | Lectura con `anon` key, sin cookies (mismo cliente que las otras 7 queries del `Promise.all`); el aislamiento por tenant lo pone `.eq('business_id', business.id)`, la vista NO filtra por sí sola (contrato D-16 de `lib/time-block-services.ts`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | La query nueva en `app/[slug]/page.tsx` podría filtrar el mapeo de OTRO negocio si se omite el filtro por tenant | mitigate | `.eq('business_id', business.id)` en la query nueva, idéntico al patrón de las otras 6 vistas `public_*` ya consumidas en el mismo archivo; verificado contra datos reales en Task 3 (no solo lectura de código) |
| T-20-02 | Tampering | La vista `public_time_block_services` podría quedar escribible por `anon` (repetición de CR-01) | accept | Ya mitigado en la Phase 18 (migr. 072/073: `REVOKE ALL` + `GRANT SELECT`, sin `security_invoker`); este plan NO crea la vista, solo la consume. Cubierto por `test/isolation.test.ts:392-438`, que este plan no modifica |
| T-20-03 | Denial of Service (fallo mudo) | Si la lectura de la vista fallara (permiso mal aplicado, `NOTIFY pgrst` faltante), el fail-safe podría interpretarse mal y apagar servicios en vez de degradar | mitigate | El `|| []` deja la puente vacía ⇒ regla del comodín ⇒ TODO servicio queda agendado — degrada al comportamiento actual, nunca al revés (Pitfall 5 del research). El guard sentinel de `isServiceStaffed` sigue el mismo principio en el eje staff |
| T-20-04 | Elevation of Privilege | Quitar el filtro `bookableServices` de `page.tsx` (D-05) podría leerse como "aflojar un control" | accept | El filtro NUNCA fue un control de seguridad, era UX: `app/api/booking/create/route.ts` no re-valida hoy que el `professionalId` elegido cubra el `serviceId` (grep confirmado, sin `professional_services` en ese archivo). Ocultar vs. deshabilitar no cambia la superficie atacable — es deuda preexistente, no introducida por este plan. Queda anotado para un `todos/pending` futuro, fuera de alcance de AGENDA-07 |
| T-20-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Cero paquetes nuevos: `git diff -- package.json package-lock.json` vacío |
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` exit **0**.
2. `npx vitest run test/service-coverage-public.test.ts test/schedule-coverage-public.test.ts` exit
   **0**, 0 failed.
3. `grep -cF "staffBookableServices" "app/[slug]/page.tsx"` = **0**;
   `grep -cE "from '@/lib/staff-services'" "app/[slug]/page.tsx"` = **0**.
4. `grep -cF "public_time_block_services" "app/[slug]/page.tsx"` >= **1**;
   `grep -cF "timeBlockServices" "app/[slug]/page.tsx"` >= **2**.
5. `grep -cE "^export function " lib/staff-services.ts` = **5**.
6. `git diff -- lib/landing/derive.ts` vacío (D-03 intacto).
7. `ls supabase/migrations | grep -c "^077"` = **0** (sin migración nueva, fence 3).
8. `git diff --name-only` toca **exactamente** los 4 archivos del frontmatter.
9. `git diff -- package.json package-lock.json` vacío.
</verification>

<success_criteria>
- `app/[slug]/page.tsx` le pasa a `BookingClient` el catálogo de servicios completo (sin
  `bookableServices`) más la prop `timeBlockServices`, dejando al Plan 20-02 todo lo que necesita para
  resolver AGENDA-07 en el cliente.
- `isServiceStaffed` existe, es la única fuente del guard sentinel de staff, y `bookableServices` sigue
  pasando sus 3 casos existentes sin haber tocado una sola aserción.
- El eje franja tiene cobertura de test contra datos reales (anon key, DB local), no solo por lectura de
  código.
</success_criteria>

## Artifacts this phase produces

| Artefacto | Plan | Estado al cerrar este plan |
|---|---|---|
| `app/[slug]/page.tsx` → 8va query `public_time_block_services` + prop `timeBlockServices` a `BookingClient` | **20-01** | creado |
| `app/[slug]/page.tsx` → `services={services \|\| []}` sin pre-filtrar (D-05) | **20-01** | creado |
| `lib/staff-services.ts` → `isServiceStaffed` | **20-01** | creado |
| `test/schedule-coverage-public.test.ts` | **20-01** | creado, verde |
| `app/[slug]/booking-client.tsx` → prop `timeBlockServices`, derivación `serviceBlocks` (D-04), tarjeta del paso 1 con `disabled`+motivo por dos ejes (D-02/D-05) | 20-02 | — |

<output>
Create `.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-01-SUMMARY.md` when done
</output>