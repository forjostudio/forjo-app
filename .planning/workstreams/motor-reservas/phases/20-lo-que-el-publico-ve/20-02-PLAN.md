---
phase: 20-lo-que-el-publico-ve
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - app/[slug]/booking-client.tsx
autonomous: true
requirements: [AGENDA-07]

must_haves:
  truths:
    - "En /[slug], un servicio sin ninguna franja que lo cubra aparece en el paso 1 con la tarjeta deshabilitada y el texto 'Sin horarios disponibles' debajo, y tocarla NO avanza al paso 2 (D-02, criterio 2 de AGENDA-07) — verdad observable en el navegador"
    - "Un servicio que solo se da ciertos dias (ej. ceramica los martes) muestra en el calendario del paso 3 SOLO esos dias como seleccionables; los demas dias aparecen cerrados (D-04, criterio 1 de AGENDA-07 a nivel dia) — verdad observable en el navegador"
    - "Un servicio con franja pero sin ningun profesional que lo haga aparece deshabilitado con un motivo DISTINTO ('Sin profesional disponible'), distinguible del motivo de franja (D-05) — verdad observable en el navegador"
    - "Un servicio con cobertura completa (franja + staff) se reserva de punta a punta exactamente igual que antes de esta fase — cero regresion (AGENDA-04) — verdad observable en el navegador"
  artifacts:
    - path: "app/[slug]/booking-client.tsx"
      provides: "prop timeBlockServices, derivacion serviceBlocks (D-04), tarjeta del paso 1 con disabled+motivo por los dos ejes (D-02/D-05)"
      contains: "isServiceScheduled"
  key_links:
    - from: "app/[slug]/booking-client.tsx"
      to: "lib/time-block-services.ts"
      via: "isServiceScheduled/blocksForService, nunca un filtro inline (AGENDA-02)"
      pattern: "isServiceScheduled|blocksForService"
    - from: "app/[slug]/booking-client.tsx"
      to: "lib/staff-services.ts"
      via: "isServiceStaffed, nunca un filtro inline (mismo principio que AGENDA-02 aplicado al eje staff)"
      pattern: "isServiceStaffed"
---

<objective>
Cerrar AGENDA-07 en el cliente público: el paso 1 del selector de servicios deshabilita con motivo
visible el servicio sin franja que lo cubra (D-02) o sin profesional que lo haga (D-05, con un motivo
DISTINTO y distinguible), y el calendario del paso 3 solo ofrece los días donde el servicio elegido
realmente se da (D-04, criterio 1 de AGENDA-07 a nivel día — hallazgo del research que el CONTEXT no
había cubierto explícitamente pero que usa el MISMO helper y la MISMA prop de D-02, sin superficie
nueva).

Purpose: la Phase 18 ya cerró el criterio a nivel horario (`startTimesNotOffered` en el endpoint de
disponibilidad); lo que falta es que el cliente no le muestre al usuario una pared después de dos pasos
— ni a nivel tarjeta (D-02/D-05) ni a nivel día (D-04). D-01 justifica por qué esto vive en la Phase 20
(booking público, verbo "consume") y no en la 21 (onboarding, verbo "declara").

Output: `app/[slug]/booking-client.tsx` con la prop `timeBlockServices`, la derivación `serviceBlocks`
(D-04), el paso 1 con `disabled`+motivo por los dos ejes, y un registro de UAT humana ejecutada contra
el negocio de prueba local.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-PATTERNS.md
@app/[slug]/booking-client.tsx
@lib/time-block-services.ts
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<constraints>
- **D-02 (locked) — copiar el molde, no inventar uno.** El picker de consultorios del paso 3
  (`booking-client.tsx:654-681`) ya resuelve "deshabilitado + motivo visible" exactamente así. El paso 1
  reusa esas mismas piezas (clases, `disabled`, `type="button"`, párrafo de motivo).
- **D-03 (locked, fence 1) — `lib/landing/derive.ts` NO se toca.** Este plan no escribe en ese archivo;
  no hay ninguna razón para hacerlo (la landing no consume la puente).
- **D-04 — no-op sobre los slots.** El fix de días entra SOLO en `openDaysSet`. NO tocar `locHasBlocks`
  (pregunta de SEDE, no de servicio — Pitfall 6 del research) ni `handleDateSelect`/`weekly`
  (Pitfall 3: filtrar ahí es redundante, el server ya oculta los mismos horarios vía `full`).
- **D-05 — motivo del eje staff sin filtrar información interna a un anónimo.** El texto no puede
  nombrar profesionales ni instruir acciones del panel (ej. "asignalo en Equipo" es copy del dueño,
  `settings-client.tsx:2622`, y no corresponde acá).
- **AGENDA-02 — cero regla del comodín reimplementada.** `isServiceScheduled`/`blocksForService` para
  franja, `isServiceStaffed` para staff (creada en el Plan 20-01). Ningún `.filter(r => r.service_id ===
  ...)` inline en el componente.
- **Pitfall 4 — el `disabled` nativo ya cancela el `onClick`.** No agregar un guard adicional en el
  handler del `<button>`.
- **NUNCA `npx tsc`** → `./node_modules/.bin/tsc --noEmit`.
- No existe `@testing-library/react` en este repo — no se agrega una dependencia de testing nueva para
  este plan. La verificación de este archivo es `tsc` + greps estructurales + la UAT humana de la Task 3.
- Comentarios densos **en español**, explicando el *por qué*.
- Cero dependencias nuevas: `git diff -- package.json package-lock.json` vacío.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Paso 1 — deshabilitar con motivo por los dos ejes (D-02 + D-05)</name>
  <files>app/[slug]/booking-client.tsx</files>
  <read_first>
    - `app/[slug]/booking-client.tsx` líneas 1-19 (imports), 21-35 (`Props`, dónde entra la prop nueva),
      60 (firma de `BookingClient`, dónde se desestructura), 134-167 (el bloque D-01/D-02/D-03 de
      multi-staff — `capaces`/`showAny`/`isAny` — mismo idioma de import y de uso de helper puro a
      seguir), 538-575 (el paso 1 a modificar), 654-681 (el molde EXACTO del picker de consultorios,
      paso 3, con `disabled`+motivo — D-02 manda copiarlo literal).
    - `lib/time-block-services.ts` líneas 87-117 (`blocksForService`, `isServiceScheduled`) — firmas y
      contrato del comodín (genéricas sobre `T extends { id: string }`, `TimeBlock[]` entra sin cast).
    - `lib/staff-services.ts` (tras el Plan 20-01) — `isServiceStaffed(serviceId, activeProfessionals,
      bridge)`, mismo guard sentinel que ya usaba `bookableServices`.
    - `.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-CONTEXT.md` — D-02 completo
      (por qué no ocultar, por qué no dejar avanzar) y D-05 completo (por qué el motivo tiene que
      distinguirse por eje, y el costo aceptado de tocar comportamiento ya UAT-eado).
    - `.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-RESEARCH.md` — secciones
      "Pitfall 4" y "Pitfall 5" (fail-safe de la puente vacía: NUNCA agregar un guard que invierta la
      regla del comodín).
  </read_first>
  <action>
    En el `import type` de la línea 8, agregar `TimeBlockService` a la lista (junto a
    `ProfessionalService`). En la línea 10, ampliar el import existente de `@/lib/staff-services` para
    traer también `isServiceStaffed` (junto a `professionalsForService`). Agregar una línea de import
    nueva, `import { isServiceScheduled } from '@/lib/time-block-services'`, inmediatamente después.

    En la interfaz `Props` (líneas 21-35), agregar el campo `timeBlockServices: TimeBlockService[]`,
    después de `professionalServices`, con un comentario que espeje el de esa prop (líneas 31-34):
    qué vista lo alimenta (`public_time_block_services`, migr. 071 §3), que se interpreta con la regla
    del comodín de `lib/time-block-services` (0 filas para una franja = sirve para cualquier servicio), y
    para qué sirve acá — deshabilitar-con-motivo el servicio sin cobertura (D-02) y filtrar los días del
    calendario al servicio elegido (D-04, Task 2 de este plan).

    Desestructurar `timeBlockServices` en la firma de `BookingClient` (línea 60), junto a
    `professionalServices`.

    En el paso 1 (líneas 538-575): convertir el callback `services.map(service => (...))` a cuerpo de
    bloque (`services.map(service => { ...; return (...) })`). Antes del `return`, calcular:
    - `scheduled = isServiceScheduled(service.id, timeBlocks, timeBlockServices)`
    - `staffed = isServiceStaffed(service.id, professionals, professionalServices)`
    - `enabled = scheduled && staffed`

    Agregar al `<button>`: `type="button"` y `disabled={!enabled}` (molde exacto del paso 3, línea 665).
    Cambiar el `cn(...)` de dos ramas a TRES, en este orden de precedencia — `!enabled` primero,
    `selectedService?.id === service.id` segundo, el default al final —, reusando para la primera rama
    las clases de deshabilitado del molde del paso 3
    (`border-border/50 bg-secondary/30 opacity-60 cursor-not-allowed`). Documentar en un comentario por
    qué el orden es ese: con `disabled`, la rama "seleccionada" es inalcanzable para una tarjeta
    deshabilitada (el `onClick` nunca corrió), así que evaluar `!enabled` primero es lo único consistente
    (mismo razonamiento que el research dejó escrito para este mismo `cn()`).

    Debajo del `<div className="flex items-center justify-between...">` que ya pinta título/descripción/
    precio/duración (líneas 557-570), agregar un párrafo condicional que solo se renderiza cuando
    `!enabled`, con el mismo tag y clases que el molde del paso 3
    (`<p className="text-xs text-muted-foreground mt-1">`), con el texto `!scheduled ? 'Sin horarios
    disponibles' : 'Sin profesional disponible'`. Documentar en un comentario, en español, la precedencia
    deliberada: el motivo de franja (el foco de AGENDA-07 en esta fase) se muestra primero si los dos
    ejes fallan a la vez; el motivo de staff (D-05, aceptado como regresión candidata a verificar aparte
    de AGENDA-07) solo aparece cuando la franja SÍ cubre el servicio.

    No cambiar la estructura interna de la tarjeta (título, descripción, precio, duración) ni tocar los
    pasos 2, 3 o 4.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && grep -cF 'isServiceScheduled(' "app/[slug]/booking-client.tsx" && grep -cF 'isServiceStaffed(' "app/[slug]/booking-client.tsx" && grep -cF 'Sin horarios disponibles' "app/[slug]/booking-client.tsx"; grep -cF 'Sin profesional disponible' "app/[slug]/booking-client.tsx"; grep -cF 'disabled={!enabled}' "app/[slug]/booking-client.tsx"; grep -cF 'type="button"' "app/[slug]/booking-client.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF 'isServiceScheduled(' "app/[slug]/booking-client.tsx"` >= **1**.
    - `grep -cF 'isServiceStaffed(' "app/[slug]/booking-client.tsx"` >= **1**.
    - `grep -cF 'Sin horarios disponibles' "app/[slug]/booking-client.tsx"` = **2** (el molde del paso 3
      + el nuevo del paso 1).
    - `grep -cF 'Sin profesional disponible' "app/[slug]/booking-client.tsx"` = **1** (único, nuevo).
    - `grep -cF 'disabled={!enabled}' "app/[slug]/booking-client.tsx"` = **2** (paso 3 existente + paso 1
      nuevo).
    - `grep -cF 'type="button"' "app/[slug]/booking-client.tsx"` = **6** (5 existentes + 1 nuevo en el
      paso 1).
    - `grep -cE "\.filter\(r => r\.service_id" "app/[slug]/booking-client.tsx"` = **0** — cero
      reimplementación inline de la regla del comodín (AGENDA-02).
  </acceptance_criteria>
  <done>
    Un servicio sin franja que lo cubra y uno sin profesional que lo haga se ven deshabilitados con
    motivos distintos y distinguibles; ninguno de los dos deja avanzar al paso 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Calendario — filtrar los días clickeables al servicio elegido (D-04)</name>
  <files>app/[slug]/booking-client.tsx</files>
  <read_first>
    - `app/[slug]/booking-client.tsx` líneas 183-211 (`openDaysSet`, `isDayOpen`, las excepciones) — el
      bloque exacto a tocar; el ÚNICO cambio es la línea de `openDaysSet`.
    - `app/[slug]/booking-client.tsx` líneas 224-271 (`handleDateSelect`, la variable local `weekly` en
      la línea 236) — leer para CONFIRMAR que no se toca (es un no-op algebraico sobre los slots
      visibles: el server ya oculta los mismos horarios vía `full`/`startTimesNotOffered`).
    - `app/[slug]/booking-client.tsx` línea 124 (`locHasBlocks`) — leer para confirmar que no se toca
      (pregunta de SEDE, no de servicio).
    - `.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-RESEARCH.md` sección
      "HALLAZGO-01" completa — el diagnóstico, la prueba algebraica
      (`starts(todas) − full = starts(ofrecen)`), y las dos advertencias (no tocar `locHasBlocks`, no
      filtrar `locSpecialDates`/`globalExcByDate`).
  </read_first>
  <action>
    Agregar, inmediatamente antes de la línea 184 (`openDaysSet`), una derivación nueva `serviceBlocks`
    envuelta en `useMemo` (mismo estilo que el resto del archivo): con `selectedService` presente,
    `blocksForService(selectedService.id, timeBlocks, timeBlockServices)`; sin servicio elegido (paso 1,
    todavía no hay selección), `timeBlocks` tal cual. Dependencias del `useMemo`:
    `[selectedService, timeBlocks, timeBlockServices]`. Ampliar el import de `@/lib/time-block-services`
    agregado en la Task 1 para incluir también `blocksForService`.

    Cambiar `openDaysSet` (línea 184) para que derive de `serviceBlocks` en vez de `timeBlocks` directo —
    es el ÚNICO cambio a esa línea. El resto de `isDayOpen` (líneas 206-211) queda intacto:
    `globalExcByDate`/`locSpecialDates` siguen leyendo de `exceptions`, sin tocar.

    NO tocar: `locHasBlocks` (línea 124), `handleDateSelect` completo (líneas 224-350, incluida la
    variable local `weekly` de la línea 236), ni ninguna rama de excepción de horario especial.

    Actualizar el comentario de la línea 183 ("Días de la semana abiertos...") para explicar que ahora es
    POR SERVICIO cuando hay uno elegido (D-04), citando la prueba de no-regresión sobre los slots
    (Pitfall 3 del research): filtrar los bloques del servicio en el cliente no cambia ni un horario
    visible —el server ya oculta exactamente los mismos horarios vía `full`—; lo único que cambia es qué
    días aparecen clickeables.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && ./node_modules/.bin/tsc --noEmit && grep -cF 'blocksForService(' "app/[slug]/booking-client.tsx" && grep -cF 'serviceBlocks' "app/[slug]/booking-client.tsx"; grep -cF "timeBlocks.filter(b => b.day_of_week === date.getDay())" "app/[slug]/booking-client.tsx"; grep -cF "timeBlocks.some(b => b.location_id === id)" "app/[slug]/booking-client.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `./node_modules/.bin/tsc --noEmit` exit **0**.
    - `grep -cF 'blocksForService(' "app/[slug]/booking-client.tsx"` >= **1**.
    - `grep -cF 'serviceBlocks' "app/[slug]/booking-client.tsx"` >= **2** (la derivación + su uso en
      `openDaysSet`).
    - `grep -cF "timeBlocks.filter(b => b.day_of_week === date.getDay())" "app/[slug]/booking-client.tsx"`
      = **1**: la línea de `weekly` dentro de `handleDateSelect` sigue byte-idéntica (Pitfall 3, no se
      toca).
    - `grep -cF "timeBlocks.some(b => b.location_id === id)" "app/[slug]/booking-client.tsx"` = **1**:
      `locHasBlocks` sigue byte-idéntica (Pitfall 6, no se toca).
    - `grep -cF "new Set(timeBlocks.map(b => b.day_of_week))" "app/[slug]/booking-client.tsx"` = **0**:
      la línea vieja de `openDaysSet` ya no existe tal cual (ahora deriva de `serviceBlocks`).
  </acceptance_criteria>
  <done>
    Un servicio que solo se da ciertos días (ej. cerámica los martes) muestra en el calendario SOLO esos
    días como seleccionables, sin haber tocado el server ni la generación de slots dentro de un día.
  </done>
</task>

<task type="auto">
  <name>Task 3: UAT humana — los tres motivos distinguibles en el navegador (D-02/D-04/D-05)</name>
  <files>—</files>
  <read_first>
    - `supabase/seed.sql` íntegro — el negocio de prueba (`negocio-prueba`, login `test@forjo.local` /
      `Forjo1234!`), el único servicio "Corte" y la única profesional "Ana", horarios L-D 08:00-20:00.
    - `.planning/workstreams/motor-reservas/phases/19-el-panel/19-UI-SPEC.md` sección "Bloque A" — dónde
      vive el editor de chips franja↔servicio en `/agenda` que se usa para configurar el escenario.
    - `.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-RESEARCH.md` — el escenario
      canónico "cerámica los martes 15-16" citado como el caso real que motivó el milestone.
  </read_first>
  <action>
    Prerrequisito: `npm run dev` contra el Supabase LOCAL con el seed aplicado (`supabase db reset`).
    Loguearse en `/login` con `test@forjo.local` / `Forjo1234!`.

    Configurar el escenario en el panel (una sola vez, se reutiliza para las tres decisiones):
    1. En `/servicios`, crear dos servicios activos nuevos: "Cerámica" (30 min) y "Yoga" (30 min). Queda
       el catálogo: Corte, Cerámica, Yoga.
    2. En `/equipo`, crear una 2ª profesional activa "Bruno". Mapear a Ana y a Bruno EXCLUSIVAMENTE con
       "Corte" (tildar solo Corte en cada una — el estado inicial es comodín/todo, así que hay que dejar
       el mapeo explícito y excluyente para las dos).
    3. En `/agenda`, en el bloque de horario del MARTES, tildar únicamente el chip "Cerámica" (destildar
       "Corte" si aparece tildado). En los otros 6 días (lunes, miércoles a domingo), tildar únicamente
       "Corte" en su bloque de horario. Guardar. (No hace falta tocar nada para "Yoga": al quedar los 7
       bloques con mapeo explícito y ninguno lo incluye, "Yoga" queda automáticamente sin ninguna franja
       que lo cubra — la regla del comodín deja de aplicar donde ya hay mapeo.)

    Verificación en `/negocio-prueba` (pestaña nueva, SIN sesión — simula al cliente anónimo):

    1. **D-02 (servicio sin ninguna franja).** En el paso 1, la tarjeta de "Yoga" tiene que verse
       atenuada/deshabilitada, con el texto "Sin horarios disponibles" debajo, y tocarla NO tiene que
       avanzar al paso 2.
    2. **D-04 (filtro de días).** Elegir "Cerámica" (tarjeta habilitada — SÍ tiene franja, el martes) y
       avanzar hasta el calendario del paso 3. Solo el/los martes del mes visible tienen que verse
       seleccionables; el resto de los días de la semana tiene que verse cerrado/no clickeable.
    3. **D-05 (sin profesional, motivo distinto).** Volver al paso 1 y confirmar que "Cerámica" ahora
       muestra "Sin profesional disponible" — NO "Sin horarios disponibles" (tiene franja el martes; lo
       que le falta es staff) — y que la tarjeta también está deshabilitada.
    4. **Cero regresión.** "Corte" tiene que verse habilitado y reservable de punta a punta (elegir
       profesional, un día que no sea martes, un horario, completar el form) sin ningún cambio de
       comportamiento respecto de antes de esta fase.

    Transcribir en el SUMMARY las 4 observaciones, una por punto, con lo que se vio literalmente en
    pantalla (no "ok"). Si algo no coincide con lo esperado, es un gap y se reporta como tal — no se
    aproxima ni se da por bueno.
  </action>
  <verify>
    <human-check>Las 4 observaciones transcritas en `<action>`, con descripción literal de lo visto en
    cada paso (el motivo exacto leído bajo cada tarjeta, qué días aparecen clickeables en el calendario
    de Cerámica, y el resultado de reservar Corte de punta a punta). No hay verificación automática
    posible para esta task: `human_verify_mode` del proyecto es `end-of-phase`, y el contexto de
    planificación de esta fase pidió explícitamente un paso de UAT humana con pasos reproducibles usando
    el negocio de prueba local, porque `tsc` + tests verdes no son evidencia de que la fase funciona con
    datos reales en el navegador.</human-check>
  </verify>
  <acceptance_criteria>
    - El SUMMARY contiene las 4 observaciones numeradas, cada una describiendo lo visto (motivo exacto
      leído bajo la tarjeta, comportamiento del calendario, resultado de intentar reservar Corte).
    - El motivo de "Yoga" y el de "Cerámica" (tras el paso 2 de configuración) son literalmente distintos
      ("Sin horarios disponibles" vs. "Sin profesional disponible").
    - Ninguna observación reporta que se pudo avanzar de paso con una tarjeta deshabilitada.
  </acceptance_criteria>
  <done>
    Las tres decisiones (D-02, D-04, D-05) quedan verificadas con datos reales en el navegador, no solo
    por lectura de código, usando el negocio de prueba local.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cliente anónimo (browser) → estado de React de `BookingClient` | El estado (`selectedService`, `enabled`, `disabled`) es enteramente client-side y manipulable por devtools o una request forjada; NUNCA es el control de seguridad, solo UX. |
| `BookingClient` → `POST /api/booking/create` | El create ya re-valida server-side el eje franja (backstop `isServiceAllowedAt`, Phase 18, `service_not_scheduled`); este plan no toca ese backstop ni lo debilita. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | Un cliente podría forjar una request directa a `/api/booking/create` con un `serviceId` cuya tarjeta está deshabilitada en el paso 1 | accept | El `disabled` es UX, no control. La autoridad real es el backstop server-side `isServiceAllowedAt` (`lib/booking-core.ts`, Phase 18, ya en producción); este plan no lo toca ni lo debilita — sigue rechazando con 400 `service_not_scheduled` |
| T-20-06 | Information Disclosure | El motivo "Sin profesional disponible" mostrado a un anónimo podría filtrar detalle de configuración interna del negocio | mitigate | Copy genérica sin nombrar profesionales ni instruir acciones del panel (nunca la frase "asignalo en Equipo", que es dueño-facing). El anónimo solo ve el estado, no el detalle de quién cubre qué |
| T-20-07 | Elevation of Privilege | Un booking con un profesional específico que NO cubre el servicio elegido (coincidencia staff↔servicio) no se re-valida hoy server-side | accept | Pre-existente y fuera de esta fase (ver Plan 20-01, T-20-04: `app/api/booking/create/route.ts` no valida `professional_services` para el camino con profesional específico). D-05 no lo empeora ni lo mejora — antes tampoco había control ahí. Deuda anotada para un `todos/pending` futuro |
| T-20-08 | Denial of Service | El filtro de días (D-04, `serviceBlocks`) podría ocultar por error un día que hoy SÍ está disponible | mitigate | `serviceBlocks` es un SUBCONJUNTO de `timeBlocks` (nunca lo amplía): con `selectedService` nulo se usa `timeBlocks` completo (byte-idéntico a hoy), y con servicio elegido la prueba algebraica del research (`starts(todas) − full = starts(ofrecen)`) demuestra que no cambia ni un slot visible — solo apaga días sin franja |
| T-20-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Cero paquetes nuevos: `git diff -- package.json package-lock.json` vacío |
</threat_model>

<verification>
1. `./node_modules/.bin/tsc --noEmit` exit **0**.
2. `grep -cF 'isServiceScheduled(' "app/[slug]/booking-client.tsx"` >= **2** (paso 1 + `serviceBlocks`
   vía `blocksForService`, contado junto con `blocksForService(` >= **1**).
3. `grep -cF 'isServiceStaffed(' "app/[slug]/booking-client.tsx"` >= **1**.
4. `grep -cF 'Sin horarios disponibles' "app/[slug]/booking-client.tsx"` = **2**;
   `grep -cF 'Sin profesional disponible' "app/[slug]/booking-client.tsx"` = **1**.
5. `grep -cF "timeBlocks.filter(b => b.day_of_week === date.getDay())" "app/[slug]/booking-client.tsx"` =
   **1** (Pitfall 3, `weekly` intacto); `grep -cF "timeBlocks.some(b => b.location_id === id)"
   "app/[slug]/booking-client.tsx"` = **1** (Pitfall 6, `locHasBlocks` intacto).
6. `git diff -- lib/landing/derive.ts` vacío (D-03 intacto).
7. El SUMMARY de la Task 3 registra las 4 observaciones de la UAT humana, con motivos literales.
8. `git diff --name-only` toca **exactamente** `app/[slug]/booking-client.tsx`.
9. `git diff -- package.json package-lock.json` vacío.
</verification>

<success_criteria>
- Un cliente anónimo que elige un servicio sin franja ve el motivo "Sin horarios disponibles" en el
  selector y no puede avanzar (D-02, criterio 2 de AGENDA-07).
- Un cliente que elige un servicio con franja parcial (ej. solo martes) ve el calendario restringido a
  esos días (D-04, criterio 1 de AGENDA-07 a nivel día).
- Un cliente que elige un servicio sin staff ve un motivo DISTINTO y distinguible ("Sin profesional
  disponible", D-05), verificado como regresión candidata aparte del eje franja.
- Un servicio con cobertura completa se comporta exactamente igual que antes de esta fase.
- Las tres verdades de arriba están confirmadas con datos reales en el navegador (Task 3), no solo por
  lectura de código.
</success_criteria>

## Artifacts this phase produces

| Artefacto | Plan | Estado al cerrar este plan |
|---|---|---|
| `app/[slug]/page.tsx` → 8va query `public_time_block_services` + prop `timeBlockServices` | 20-01 | ya creado |
| `app/[slug]/page.tsx` → `services={services \|\| []}` sin pre-filtrar (D-05) | 20-01 | ya creado |
| `lib/staff-services.ts` → `isServiceStaffed` | 20-01 | ya creado |
| `test/schedule-coverage-public.test.ts` | 20-01 | ya creado |
| `app/[slug]/booking-client.tsx` → prop `timeBlockServices` | **20-02** | creado |
| `app/[slug]/booking-client.tsx` → derivación `serviceBlocks` (D-04) | **20-02** | creado |
| `app/[slug]/booking-client.tsx` → paso 1 con `disabled`+motivo por dos ejes (D-02/D-05) | **20-02** | creado |
| UAT humana registrada en `20-02-SUMMARY.md` (D-02/D-04/D-05 en navegador) | **20-02** | creado |

<output>
Create `.planning/workstreams/motor-reservas/phases/20-lo-que-el-publico-ve/20-02-SUMMARY.md` when done
</output>