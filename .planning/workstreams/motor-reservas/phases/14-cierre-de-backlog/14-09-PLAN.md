---
phase: 14-cierre-de-backlog
plan: 09
type: execute
wave: 5
depends_on: [14-08]
files_modified:
  - app/(dashboard)/settings/settings-client.tsx
  - .planning/workstreams/motor-reservas/REQUIREMENTS.md
  - .planning/workstreams/motor-reservas/ROADMAP.md
autonomous: false
gap_closure: true
requirements: [POLISH-04, POLISH-05]

must_haves:
  truths:
    - "En la vista Equipo, el recuadro de preselección de profesional ocupa el ancho de su contenido, no el de la tarjeta (D-01/D-02/D-03)"
    - "Los controles de alta de la vista Equipo quedan auditados uno por uno con veredicto registrado: ninguno queda fuera del inventario, que es exactamente el error que produjo este gap"
    - "El dueño confirmó MIRANDO la aplicación que 'Alto' y 'Medio' se distinguen dentro de los modales del CRM y que el panel no cambió de aspecto"
    - "Los 9 self-start de 14-01 y la paridad Servicios/Canchas de 14-05 siguen intactos"
    - "REQUIREMENTS.md, ROADMAP.md y la verificación de la fase dicen lo mismo sobre POLISH-04 y POLISH-05"
  artifacts:
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "radiogroup de preselección desestirado + auditoría de los controles de alta de Equipo"
      contains: "self-start"
    - path: ".planning/workstreams/motor-reservas/REQUIREMENTS.md"
      provides: "POLISH-04 y POLISH-05 marcados Complete solo tras la UAT aprobada"
      contains: "POLISH-04"
    - path: ".planning/workstreams/motor-reservas/ROADMAP.md"
      provides: "Wave 4 y Wave 5 con 14-08 y 14-09, y el estado real de la fase"
      contains: "14-08-PLAN.md"
  key_links:
    - from: "app/(dashboard)/equipo/page.tsx"
      to: "app/(dashboard)/settings/settings-client.tsx"
      via: "la vista Equipo se renderiza con <SettingsClient view=\"equipo\"> — el gap vive en ese archivo aunque la pantalla sea otra"
      pattern: "view=\\\"equipo\\\""
    - from: ".planning/workstreams/motor-reservas/REQUIREMENTS.md"
      to: ".planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-VERIFICATION.md"
      via: "el estado de POLISH-04/05 refleja la evidencia de la verificación y de la UAT, no el 'plan ejecutado'"
      pattern: "POLISH-05"
---

<objective>
Cerrar los **gaps 2 y 3 (WARNING)** de `14-VERIFICATION.md` — los dos de cobertura de POLISH-04 en la
vista **Equipo** —, **verificar con el dueño** que el fix del portal del plan 14-08 cerró de verdad el
gap 1, y dejar la trazabilidad de la fase consistente.

Purpose: los dos gaps de POLISH-04 viven en `app/(dashboard)/settings/settings-client.tsx`, el mismo
archivo que 14-01 sí corrigió, pero en el bloque de multi-staff (EXTRA-B / D-19) que quedó fuera del
inventario auditado en `14-PATTERNS.md` §1.b. El error de origen fue de **inventario**, no de técnica:
por eso este plan no aplica un parche puntual sino que **audita y registra** cada control de la vista,
incluidos los que resulten estar bien.

El gap 1 (POLISH-05) es una **falla visual**: no hay forma honesta de cerrarlo con una aserción de
código. El plan 14-08 dejó el mecanismo probado por tests; acá lo cierra un **checkpoint humano
bloqueante** redactado para que sea imposible aprobarlo sin haber abierto el navegador — hay evidencia
registrada en este proyecto de que `auto_advance` auto-aprueba los `human-verify`, y la UAT de 14-07
demostró que exigir observaciones transcritas encuentra lo que el pipeline verde no ve.

Output: la vista Equipo alineada al criterio único de D-01/D-02/D-03 con su auditoría registrada, las
6 observaciones humanas transcritas, y `REQUIREMENTS.md` + `ROADMAP.md` diciendo lo mismo que
`14-VERIFICATION.md`.

**Orden no negociable:** la trazabilidad (Task 3) se toca **después** de que el checkpoint (Task 2)
quede aprobado. Marcar `Complete` algo que la UAT no vio es exactamente la inconsistencia que este
plan viene a cerrar.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-VERIFICATION.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-PATTERNS.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<artifacts_this_phase_produces>
Este plan **no crea símbolos nuevos**: no hay componentes, funciones, clases CSS ni custom properties
nuevas. Modifica el valor de un `className` existente (y, si la auditoría lo prueba, el de hasta dos
más), y edita dos documentos de planificación.

| Símbolo / artefacto | Archivo | Estado |
|---|---|---|
| `<div role="radiogroup" aria-label="Preselección del profesional">` | `app/(dashboard)/settings/settings-client.tsx` (~`:1914`) | `className` modificado (sin cambio de estructura ni de props) |
| controles de alta de la vista Equipo | `app/(dashboard)/settings/settings-client.tsx` (~`:1836`, ~`:1989`) | auditados; modificados **solo** si la auditoría prueba la causa |
| checklist §"Cierre de backlog (polish)" | `REQUIREMENTS.md` | 2 líneas de estado |
| tabla de Traceability | `REQUIREMENTS.md` | 2 celdas de estado |
| §"Phase 14" | `ROADMAP.md` | línea de `**Plans**`, Wave 4, Wave 5 y línea de `**Waves**` |

**Archivos que NO se tocan y deben quedar fuera del diff:** `components/ui/card.tsx`,
`components/ui/button.tsx`, `components/ui/input.tsx`, `components/ui/label.tsx`,
`components/dashboard/active-tabs.tsx`, `components/crm/risk-badge.tsx`, `app/globals.css`,
`app/themes.css`, y todo lo que tocó el plan 14-08.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: POLISH-04 en la vista Equipo — radiogroup de preselección + auditoría de los controles de alta</name>

  <files>app/(dashboard)/settings/settings-client.tsx</files>

  <read_first>
    - `app/(dashboard)/settings/settings-client.tsx` — leer, en este orden:
      (a) `:1560-1570` y `:2140-2160`, el molde ya aplicado por 14-01 (`<Button className="self-start">`
      hijo directo de `<Card>`) con su comentario de `:2146` explicando por qué el fix vive en el
      call-site; (b) **`:1760-2000` completo**, que es el bloque de la vista Equipo: alta de
      profesional (`:1810-1838`), enlace de datos de contacto (`:1830`), Bloque A de chips
      (`:1843-1905`), la tarjeta de preselección (`:1907-1942`) y la tarjeta de espacios
      (`:1948-2045`).
    - `app/(dashboard)/equipo/page.tsx` — confirma que la pantalla `/equipo` renderiza
      `<SettingsClient view="equipo">`: el gap se ve en `/equipo` pero se arregla en `settings-client.tsx`.
    - `components/ui/card.tsx` — **solo lectura, prohibido modificar**: `:15` declara `flex flex-col`,
      la causa 2 de POLISH-04. Tocarlo cambiaría el layout de toda la app.
    - `components/ui/button.tsx` — **solo lectura**: `:7` (el botón es `inline-flex items-center
      justify-center`) y `:25` (el tamaño default es `h-8`).
    - `components/ui/input.tsx` — **solo lectura**: `:12`, el campo es `h-8`.
    - `components/ui/label.tsx` — **solo lectura**: `:12`, la etiqueta es `flex items-center text-sm
      leading-none`.
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-PATTERNS.md` §1.b — el
      inventario que 14-01 auditó (10 confirmados) y los 20 candidatos descartados. **Este bloque de
      Equipo no está en ninguna de las dos listas: ese es el gap.**
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-CONTEXT.md` §POLISH-04 —
      D-01 (criterio único), D-02 (las dos causas) y D-03 (la alineación en desktop se decide caso por
      caso: `self-start` en tarjetas de configuración).
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-07-SUMMARY.md` — Task 2,
      puntos **6 y 7**: la redacción literal de lo que el dueño vio.
  </read_first>

  <action>
    **(a) Gap 2 — el recuadro de preselección (D-01 / D-02 / D-03).**
    En la vista Equipo, el `<div role="radiogroup" aria-label="Preselección del profesional">`
    (hoy `:1914`) es **hijo directo** de `<Card className="p-6 space-y-4 mt-4">` (hoy `:1909`). Como
    `Card` es `flex flex-col`, su `align-items: stretch` lo estira a todo el ancho de la tarjeta pese
    a que el propio div declara `inline-flex`: un ítem flex se blockifica y el `stretch` gana. Es la
    causa 2 exacta que 14-01 corrigió en 9 botones de este mismo archivo.

    Fix: agregar `self-start` como **primera** clase de ese `className`, dejando el resto de las
    clases tal cual están (`inline-flex`, `flex-wrap`, el gap, el radio, el borde y el padding). No
    cambiar la estructura, ni `role`, ni `aria-label`, ni los dos `<button role="radio">` de adentro,
    ni sus `aria-checked` — la semántica de accesibilidad ya está bien y no es parte del gap.

    Agregar arriba un comentario en español con el mismo criterio que el de `:2146`: por qué el fix
    vive en el call-site (tocar el componente `Card` cambiaría el layout de toda la app) y que este
    control quedó fuera del inventario auditado por 14-01. **No** escribir el atributo literal dentro
    del comentario: describirlo en prosa.

    ⚠ **Hay un SEGUNDO radiogroup en este archivo con el `className` idéntico y NO se toca.** Es el
    de `CapacityModeFields` (hoy `:177`, etiquetado "Cómo se ocupa el cupo", consumido en `:1693` y
    `:1738`): su padre es un `<div className="space-y-1.5">`, que es un contenedor de **bloque**, así
    que no lo estira nada. Un `grep` del `role` devuelve **2** líneas y solo una tiene defecto —
    justamente el tipo de confusión que produjo este gap. Verificar el padre antes de tocar, y dejar
    el de `:177` exactamente como está.

    **(b) Gap 3 — auditoría de los controles de alta de la vista Equipo.**
    La vista Equipo tiene exactamente **dos** controles de alta más un enlace, y hay que auditar los
    tres:

    | # | Ubicación (hoy) | Control | Contenedor padre (hoy) |
    |---|---|---|---|
    | 1 | `:1835-1837` | botón de alta de profesional | `<div className="border-t border-border pt-4 space-y-3">` (bloque) |
    | 2 | `:1988-1990` | botón de alta de espacio | `<div className="flex items-end gap-2">`, junto al campo de nombre |
    | 3 | `:1830` | enlace "+ Datos de contacto (opcional)" | mismo bloque que el #1 |

    Para **cada uno**, registrar en el SUMMARY una fila con: el `display` y el `align-items` efectivos
    del contenedor padre tal como salen de sus clases, la altura declarada del control, y el veredicto
    (defecto probado / correcto). Datos ya verificados que **no** hay que re-descubrir: el botón es
    `inline-flex items-center justify-center` con `h-8` en su tamaño default, el campo es `h-8`, y la
    etiqueta es `text-sm leading-none` — o sea que en la fila #2 el botón y el campo declaran la misma
    altura y el `items-end` los deja a ras.

    Aplicar el fix **solo donde la causa quede probada**, siempre en el call-site y nunca en
    `components/ui/{card,button,input,label}.tsx`, según esta tabla de decisión:

    - el control se estira por un padre flex-column ⇒ `self-start` (molde de los 9 de 14-01);
    - el control es inline-level suelto dentro de un contenedor de bloque y arrastra el hueco de la
      caja de línea ⇒ pasar ese contenedor a `flex` + `flex-col` + `items-start` **conservando el
      espaciado declarado** (el `space-y-3` se traduce a `gap-3`), sin tocar los hermanos;
    - el control convive en una fila `items-end` con un campo de otra altura ⇒ igualar contra el campo
      con el molde de la fila de alta de Servicios (`:1687-1691`).

    Si la auditoría prueba que un control **ya está correcto**, se deja intacto y se registra la
    evidencia. **El árbitro final es el checkpoint humano de la Task 2:** si el dueño reporta que
    alguno se sigue viendo mal, se aplica el fix que describa su transcripción **antes** de cerrar
    este plan, y se actualiza la fila correspondiente de la tabla de auditoría.

    **(c) Nada fuera de la vista Equipo.** En particular quedan intactos los 9
    `<Button className="self-start">` que dejó 14-01 en este archivo y los dos consumos del módulo
    compartido de tabs de 14-05 (`:1592` y `:1594`).
  </action>

  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE '<Button[^>]*className="self-start"' "app/(dashboard)/settings/settings-client.tsx"`
      devuelve exactamente **9** — ni uno más ni uno menos. Es el anti-regresión de 14-01: el fix del
      radiogroup **no** es un `<Button>`, así que este número no se mueve. (Se cuenta la construcción
      `<Button …>`, no el token suelto, para que un comentario no pueda alterar el conteo.)
    - `grep -cE 'role="radiogroup"' "app/(dashboard)/settings/settings-client.tsx"` sigue devolviendo
      exactamente **2** (el de Equipo y el de `CapacityModeFields`): no se agregó ni se borró ninguno.
    - `grep -cE 'className="self-start inline-flex' "app/(dashboard)/settings/settings-client.tsx"`
      devuelve exactamente **1** — solo el radiogroup de Equipo lleva la clase nueva, y va primera.
      El de `CapacityModeFields` (`:177`) sigue empezando su `className` con la clase de display, sin
      tocar.
    - `grep -cE '<ActiveTabs' "app/(dashboard)/settings/settings-client.tsx"` devuelve exactamente
      **2** (las píldoras y el empty state de Servicios). Anti-regresión de 14-05 / EXTRA-A.
    - `grep -c 'aria-label="Preselección del profesional"' "app/(dashboard)/settings/settings-client.tsx"`
      devuelve `1` (la semántica del control no se rompió al editar su `className`).
    - `npx tsc --noEmit` sale 0 y `npm run build` sale 0.
    - `git diff --name-only` de esta task lista **exactamente 1 archivo**
      (`app/(dashboard)/settings/settings-client.tsx`) y **no** incluye `components/ui/card.tsx`,
      `components/ui/button.tsx`, `components/ui/input.tsx`, `components/ui/label.tsx` ni
      `components/dashboard/active-tabs.tsx`.
    - El SUMMARY trae la **tabla de auditoría** de (b) con una fila por cada uno de los 3 controles,
      cada una con contenedor, display/align-items, altura declarada y veredicto — incluidas las filas
      cuyo veredicto sea "correcto, no se toca".
  </acceptance_criteria>

  <done>El recuadro de preselección ocupa el ancho de su contenido, los 3 controles de alta de Equipo están auditados con veredicto registrado, y los 9 botones de 14-01 y los tabs de 14-05 siguen intactos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT visual bloqueante de los 3 gaps (POLISH-05 dentro del CRM + POLISH-04 en Equipo)</name>

  <files>—</files>

  <read_first>
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-08-SUMMARY.md` — §"A mirar
      en el checkpoint humano (14-09)": los 3 ítems que el plan 14-08 dejó explícitamente para esta
      observación, y el inventario de los 33 `<DialogContent>`.
    - El SUMMARY de la Task 1 de este plan — la tabla de auditoría de los 3 controles de alta de
      Equipo, para saber cuáles se tocaron y cuáles se declararon correctos.
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-07-SUMMARY.md` — Task 2,
      puntos 3, 6 y 7: la redacción literal de las 3 observaciones que abrieron estos gaps. Esta UAT
      las tiene que responder de nuevo, no dar por hecho que cambiaron.
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-CONTEXT.md` §D-05, §D-06,
      §D-07 (el aspecto objetivo del badge y la aceptación explícita de que el CRM cambie) y §D-01,
      §D-02, §D-03 (el criterio de ancho).
  </read_first>

  <what-built>
    - **Plan 14-08:** el popup portaleado del `Dialog` ahora recibe el scope del shell activo, así que
      dentro de los modales del CRM la superficie de peligro y el primario vuelven a ser los del
      super-admin. Nadie tocó `components/crm/risk-badge.tsx`.
    - **Task 1 de este plan:** el recuadro de preselección de profesional de `/equipo` dejó de
      estirarse a todo el ancho de su tarjeta, y los 3 controles de alta de esa vista quedaron
      auditados (con o sin cambio, según la evidencia).
    - Lo que **no** se puede verificar con código: si los dos chips se ven de colores distintos, si el
      panel del dueño quedó igual que antes, y si los controles de alta de Equipo se ven bien. Eso lo
      decide esta observación.
  </what-built>

  <action>
    **Hay que abrir el navegador. No se aprueba de memoria, ni por lectura del diff, ni porque los
    tests del plan 14-08 estén verdes** — esos tests prueban el cableado, no lo que se ve. Cada punto
    pide una observación concreta que hay que **transcribir en la respuesta**; una aprobación sin las
    observaciones se rechaza.

    **Prerrequisitos (el ejecutor los deja listos ANTES de pedir la observación):**
    1. `npm run dev` levantado contra el Supabase **LOCAL**, con el negocio de prueba sembrado.
    2. El usuario de prueba tiene que poder entrar al CRM: en el Supabase **local**, su
       `raw_app_meta_data.is_admin` debe ser `true` (14-07 tuvo que setearlo para poder abrir
       `/admin/auditoria`; sin eso el punto 1 de abajo es inobservable). **Producción no se toca.**
    3. Tiene que existir al menos un negocio en `/admin/negocios` para abrir su ficha.

    **Observaciones a transcribir — son 6, todas numeradas, ninguna se puede saltear:**

    **1. Los dos chips dentro de los modales del CRM (el gap que falló).** En `/admin/negocios/<id>`,
    abrí el modal donde aparece un chip **Medio** (el de cambio de plan) y, aparte, cualquier
    `ConfirmDialog` destructivo del CRM donde aparezca un chip **Alto**. Transcribí, para cada uno:
    **de qué color se ve el chip** y **si los dos se distinguen a simple vista**. Esperado: el "Alto"
    en rojo y el "Medio" en amarillo — que es como se ven hoy en `/admin/auditoria` fuera de un modal.
    Si los dos siguen viéndose del mismo color, el checkpoint **NO** está aprobado.

    **2. El resto del modal del CRM.** En el mismo modal: ¿el fondo y los textos se leen como parte del
    chrome del super-admin (el mismo aspecto que la pantalla que está detrás), o el modal se ve con el
    aspecto del panel del dueño? Transcribí lo que ves. Anotá también si algo quedó ilegible por falta
    de contraste.

    **3. Los 4 ConfirmDialog del panel del dueño (que NO son del CRM) — anti-regresión.** Abrí los
    cuatro y describí, para cada uno, **de qué color se ve el chip "Alto"** y si el modal conserva el
    aspecto del panel (claro u oscuro según tu tema, **no** el del super-admin):
      - `/abonos` → detalle de una serie **activa** → "Dar de baja";
      - `/abonos` → detalle de una serie **archivada** → "Eliminar";
      - `/servicios` → eliminar un servicio;
      - `/servicios` (o la pantalla de canchas si el negocio es de ese rubro) → eliminar una cancha.
    Si alguno de estos cuatro cambió de aspecto, es una fuga del scope del CRM al panel y el checkpoint
    **NO** está aprobado.

    **4. Diálogos que no son ConfirmDialog + drawers en mobile — anti-regresión.** Abrí "Nuevo turno"
    desde `/agenda`, el modal de "Copiar a N días" de Días especiales, y el modal de planes de
    Ajustes. Después, a **375px**, abrí cualquier drawer con un selector adentro (por ejemplo el alta
    de turno) y desplegá el selector. Transcribí si algo se ve distinto a como se veía antes y si el
    selector sigue siendo clickeable dentro del drawer.

    **5. `/equipo` — el recuadro de preselección (gap 2).** En `/equipo`, buscá el bloque
    **"Al reservar, ¿preseleccionar «Cualquiera»?"**. Transcribí: ¿el recuadro con las dos opciones
    ocupa **todo el ancho de la tarjeta** o queda del **ancho de su contenido**? Mirálo a **375px** y a
    **≥1280px**. Esperado en desktop: ancho de su contenido. Confirmá además que las dos opciones
    siguen funcionando (al tocar una, queda marcada).

    **6. `/equipo` — los controles de alta (gap 3).** En `/equipo`, mirá el botón de **agregar
    profesional** y el de **agregar espacio**, a **375px** y a **≥1280px**. Si alguno se ve mal,
    describí **cuál** y **cómo**: ¿queda pegado al fondo de su bloque?, ¿el símbolo "+" se ve más
    arriba o más abajo que el texto?, ¿el botón queda más alto o más bajo que el campo que tiene al
    lado?, ¿sobra o falta espacio arriba o abajo? Si se ven bien, decilo con esas palabras. **Tu
    descripción es la que manda:** si reportás un defecto, se arregla en este mismo plan y volvés a
    mirar.

    **7. Cero regresión de 14-01 (rápido).** En `Negocio → Cobros`, confirmá que "Guardar" y "Liberar
    horarios vencidos" siguen a **ancho de contenido** en desktop, y en `/servicios` que las píldoras
    Activos/Desactivados siguen como estaban.
  </action>

  <verify>
    <human-check>Las 7 observaciones transcritas por el dueño, con colores y anchos concretos. No hay verificación automática posible para este checkpoint: los tests de 14-08 prueban el cableado del scope, no que dos chips se vean de colores distintos.</human-check>
  </verify>

  <acceptance_criteria>
    - El SUMMARY contiene **7 observaciones numeradas**, una por punto, cada una describiendo lo que
      se vio en pantalla — no "ok", no "aprobado" a secas, no un resumen del ejecutor.
    - El punto 1 reporta un **color concreto** para el chip "Alto" y otro para el "Medio", y un
      veredicto explícito de si se distinguen.
    - El punto 3 reporta un veredicto por cada uno de los 4 ConfirmDialog del panel, incluyendo si
      alguno cambió de aspecto (esperado: ninguno).
    - El punto 4 reporta si algún diálogo o drawer del panel se ve distinto a antes (esperado:
      ninguno) y si el selector dentro del drawer sigue siendo clickeable en mobile.
    - El punto 5 reporta, para el recuadro de preselección, **"ancho completo" o "ancho de contenido"**
      a 375px y a ≥1280px (esperado en desktop: ancho de contenido).
    - El punto 6 reporta, para cada uno de los dos controles de alta de Equipo, o bien "se ve bien" o
      bien una **descripción concreta del defecto**. Si describe un defecto, el ejecutor lo corrige en
      la Task 1 y este punto se vuelve a observar antes de cerrar el checkpoint.
    - El punto 7 reporta un veredicto explícito de cero regresión sobre 14-01 y 14-05.
  </acceptance_criteria>

  <resume-signal>
    Pegá las **7 observaciones numeradas** con lo que viste en pantalla, en tus palabras (colores
    concretos, ancho completo o ancho de contenido, y en el punto 6 la descripción del defecto si lo
    hay). **La palabra "aprobado" sola no aprueba este checkpoint**: sin las 7 observaciones
    transcritas, el plan no avanza a la Task 3 — es la lección de 14-07 y de la evidencia registrada
    de que los checkpoints se auto-aprueban sin que nadie abra el navegador. Si alguna observación
    reporta un defecto, el ejecutor lo corrige y este checkpoint se vuelve a correr sobre el punto
    afectado.
  </resume-signal>

  <done>Los 3 gaps de la fase están verificados mirando la aplicación, en los dos shells y en los dos anchos, con las 7 observaciones transcritas y sin defectos abiertos.</done>
</task>

<task type="auto">
  <name>Task 3: Cerrar la trazabilidad de POLISH-04 y POLISH-05 (REQUIREMENTS.md + ROADMAP.md)</name>

  <files>.planning/workstreams/motor-reservas/REQUIREMENTS.md, .planning/workstreams/motor-reservas/ROADMAP.md</files>

  <read_first>
    - `.planning/workstreams/motor-reservas/REQUIREMENTS.md` — §"Cierre de backlog (polish)"
      (hoy `:61-66`) y la tabla de Traceability (hoy `:95-111`). Ojo: el estado de POLISH-04 y
      POLISH-05 **ya fue corregido** por el commit de verificación (`0fdd851`) al estado provisorio que
      apunta a este plan; esta task hace el **cierre**, no la corrección inicial que describe
      `14-VERIFICATION.md`. Registrar ese matiz en el SUMMARY.
    - `.planning/workstreams/motor-reservas/ROADMAP.md` — §"Phase 14: Cierre de backlog" completa
      (hoy `:519-560`): la línea de `**Plans**`, las listas de Wave 1 / Wave 2 / Wave 3 y la línea
      final de `**Waves**`.
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-VERIFICATION.md` —
      §"Requirements Coverage" y la nota de inconsistencia documental, que es lo que esta task cierra.
    - Los SUMMARY de `14-08` y de este plan (Tasks 1 y 2), que son la evidencia a citar.
  </read_first>

  <action>
    **Precondición dura: esta task NO se ejecuta si el checkpoint de la Task 2 no quedó aprobado con
    las 7 observaciones transcritas.** Si quedó algún defecto abierto, se corrige y se re-observa
    primero.

    **En `REQUIREMENTS.md`:**
    - En el checklist de §"Cierre de backlog (polish)", las entradas de **POLISH-04** y **POLISH-05**
      pasan de sin marcar a marcadas.
    - En la tabla de Traceability, la celda de estado de **POLISH-04** y la de **POLISH-05** pasan a
      `Complete`, quedando las cuatro filas de la fase (POLISH-04, 05, 06, 07) con el mismo valor.

    **En `ROADMAP.md`, §"Phase 14"** — con `Edit` de reemplazo acotado, **nunca** con `Write`: una
    reescritura completa del archivo destruye las fases que no están en la ventana del diff.
    - Los encabezados `**Wave 4**` / `**Wave 5**`, las dos entradas de plan y la línea de `**Waves**`
      **ya existen** (los escribió el planner al crear estos planes). Esta task **no** los duplica:
      confirma que las dos entradas quedaron marcadas como hechas por el flujo de ejecución y, si no
      lo están, las marca.
    - La línea `**Plans**: 9 plans (7 ejecutados + 2 de cierre de gaps) — …` se reescribe al estado
      final: qué cerró 14-08 (la causa raíz del scope del portal, gap 1) y qué cerró 14-09 (los 2 gaps
      de cobertura de POLISH-04 en Equipo + la UAT bloqueante), y se retira la afirmación de que la
      fase no está cerrada, citando la aprobación del checkpoint de la Task 2 como evidencia.

    **No** tocar ninguna otra fase del ROADMAP, ni la sección de `Future Requirements`, ni
    `Out of Scope`, ni las entradas de 14-01 a 14-07.
  </action>

  <verify>
    <automated>grep -cE '^- \[x\] 14-0[1-7]-PLAN\.md' .planning/workstreams/motor-reservas/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -nE '^- \[x\] \*\*POLISH-0[45]\*\*' .planning/workstreams/motor-reservas/REQUIREMENTS.md`
      devuelve exactamente **2** líneas.
    - `grep -nE '^\| POLISH-0[4567] \| Phase 14 \| Complete' .planning/workstreams/motor-reservas/REQUIREMENTS.md`
      devuelve exactamente **4** líneas (las cuatro de la fase, con el mismo estado).
    - `grep -cE '^- \[x\] 14-0[89]-PLAN\.md' .planning/workstreams/motor-reservas/ROADMAP.md`
      devuelve exactamente **2** (las dos entradas nuevas, con el mismo formato que las 7 previas).
      ⚠ No usar un `grep` de `**Wave N**` como criterio: el ROADMAP tiene 42 encabezados de wave de
      todas las fases y el conteo no distingue la sección de Phase 14.
    - `grep -cE '^- \[x\] 14-0[1-7]-PLAN\.md' .planning/workstreams/motor-reservas/ROADMAP.md`
      devuelve exactamente **7** — prueba anti-clobber de que las entradas previas de la fase siguen
      ahí y siguen marcadas.
    - `git diff --stat -- .planning/workstreams/motor-reservas/ROADMAP.md` muestra **≤ 20 líneas
      cambiadas**: prueba de que se usó `Edit` acotado y no una reescritura del archivo.
    - `git diff --name-only` de esta task lista exactamente los 2 archivos de planificación y ningún
      archivo de `app/`, `lib/`, `components/` ni `supabase/`.
  </acceptance_criteria>

  <done>REQUIREMENTS.md, ROADMAP.md y 14-VERIFICATION.md dicen lo mismo sobre POLISH-04 y POLISH-05, y las entradas previas de la fase quedaron intactas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ninguna nueva. El plan cambia un `className` en una vista **autenticada** del dueño (`/equipo`, ya
gateada por `app/(dashboard)/equipo/page.tsx:8-18`: sesión + negocio propio + redirección del rubro
canchas) y edita dos documentos de planificación. No toca datos, endpoints, queries, policies, RLS,
el motor de reservas ni el flujo de pagos.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-36 | Tampering | `app/(dashboard)/settings/settings-client.tsx` (vista Equipo) | mitigate | El archivo es compartido por 5 vistas (`config`, `negocio`, `servicios`, `equipo`, `consultorios`): un cambio mal ubicado rompe pantallas que la UAT de 14-07 dio por buenas. Mitigación: los criterios de aceptación fijan el conteo exacto de los 9 botones de 14-01 y de los 2 consumos del módulo de tabs de 14-05, y el punto 7 del checkpoint humano lo confirma a ojo. |
| T-14-37 | Tampering | `components/ui/card.tsx` y demás componentes transversales | mitigate | Corregir la causa 2 tocando el componente `Card` cambiaría el layout de toda la app. Mitigación: los 4 componentes de `components/ui/` quedan fuera del diff por criterio de aceptación (`git diff --name-only` = 1 archivo). |
| T-14-38 | Spoofing | checkpoint `human-verify` de la Task 2 | mitigate | Hay evidencia registrada en este proyecto de que `auto_advance` auto-aprueba los checkpoints sin que nadie abra el navegador — así fue como el gap 1 llegó hasta la verificación. Mitigación: el `resume-signal` exige las 7 observaciones transcritas y declara explícitamente que "aprobado" solo no alcanza; el gap 1 (POLISH-05) **no puede cerrarse por ninguna otra vía**. |
| T-14-39 | Repudiation | `REQUIREMENTS.md`, `ROADMAP.md` | mitigate | Marcar un requisito como `Complete` sin evidencia deja una fuente de verdad falsa que la próxima fase hereda (es literalmente la inconsistencia que registró `14-VERIFICATION.md`). Mitigación: la Task 3 es la última, tiene precondición dura sobre la aprobación del checkpoint, y sus criterios exigen que las 4 filas de la fase queden con el mismo estado y que las 7 entradas previas sigan intactas. |
| T-14-40 | Information Disclosure | vista `/equipo` | accept | El cambio es de ancho de un contenedor: no agrega, quita ni reordena ningún dato en el DOM. Los nombres de profesionales y espacios que ya se mostraban siguen bajo el mismo guard de sesión + tenant. |
| T-14-SC | Tampering | supply chain (npm) | mitigate | El plan **no instala nada**. Criterio de aceptación: `git diff --name-only` no incluye `package.json` ni `package-lock.json`. Sin instalaciones no aplica el gate de legitimidad de paquetes. |
</threat_model>

<verification>
- `npx tsc --noEmit` sale 0 y `npm run build` completa sin error.
- `npx vitest run` sin fallas fuera de las 3 suites de abonos con flakiness pre-existente
  (`abono-create`, `abono-cron`, `abono-generation`), baseline documentado de forma cruzada por 6
  SUMMARYs de esta fase. **No se exige 887/887** y no se re-investiga esa flakiness.
- Los conteos anti-regresión dan exacto: 9 botones con `self-start` en `settings-client.tsx`, 2
  consumos de `<ActiveTabs`, 7 entradas `14-0N-PLAN.md` marcadas en el ROADMAP.
- Las 7 observaciones humanas están transcritas en el SUMMARY, no resumidas.
- `git diff --name-only` del plan completo lista exactamente los 3 archivos de `files_modified`.
</verification>

<success_criteria>
- El recuadro de preselección de profesional de `/equipo` ocupa el ancho de su contenido en desktop
  (D-01/D-02/D-03), y su semántica de radiogroup quedó intacta.
- Los 3 controles de alta de la vista Equipo están auditados con veredicto y evidencia registrados —
  incluidos los que resultaron correctos. Nadie queda fuera del inventario.
- El dueño confirmó por escrito, mirando la aplicación, que dentro de los modales del CRM "Alto" y
  "Medio" se distinguen, que los 4 ConfirmDialog del panel no cambiaron de aspecto, y que los demás
  diálogos y drawers se ven como antes.
- `REQUIREMENTS.md`, `ROADMAP.md` y `14-VERIFICATION.md` dicen lo mismo sobre POLISH-04 y POLISH-05.
- Ni `components/ui/card.tsx`, ni los demás componentes de `components/ui/`, ni
  `components/dashboard/active-tabs.tsx`, ni los archivos del plan 14-08 aparecen en el diff.
</success_criteria>

<output>
Crear `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-09-SUMMARY.md` al terminar.

Incluir obligatoriamente:
- La **tabla de auditoría** de los 3 controles de alta de Equipo (contenedor, display/align-items,
  altura declarada, veredicto), con las filas "correcto, no se toca" incluidas.
- Las **7 observaciones humanas transcritas** del checkpoint, numeradas, con las palabras del dueño —
  no un resumen. Si alguna disparó una corrección, registrar la corrección y la re-observación.
- Los **conteos anti-regresión** con su output literal (los 9 `self-start`, los 2 `<ActiveTabs`, las 7
  entradas del ROADMAP).
- El **estado final de POLISH-04 y POLISH-05** con el enlace a la evidencia que lo respalda.
- Una sección **"Estado de la fase 14"** declarando sin ambigüedad si la fase queda cerrada, y
  cualquier ítem que se mande al backlog en vez de arreglarse acá (con el motivo).
</output>
