---
phase: 14-cierre-de-backlog
plan: 07
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03, 14-04, 14-05, 14-06]
files_modified: []
autonomous: false
requirements: [POLISH-04, POLISH-05, POLISH-06, POLISH-07, EXTRA-A, EXTRA-B]

user_setup:
  - service: supabase-produccion
    why: "La migración 066 (gate de borrado de abono) se aplica A MANO al Supabase de producción, coordinada con el deploy. `supabase db push` está PROHIBIDO y el flujo GSD nunca la aplica."
    dashboard_config:
      - task: "Verificar que la última migración aplicada en producción es la 065 y ejecutar el contenido de supabase/migrations/066_abono_delete_gate.sql"
        location: "Supabase Dashboard → SQL Editor (proyecto de producción)"
      - task: "Ejecutar NOTIFY pgrst, 'reload schema'; en la misma sesión"
        location: "Supabase Dashboard → SQL Editor (proyecto de producción)"

must_haves:
  truths:
    - "D-06: el cambio del RiskBadge se verificó en LOS DOS shells, con el CRM cambiando de aspecto a propósito"
    - "D-01/D-02: los botones de acción se ven deliberados y no estirados, en mobile y en desktop"
    - "D-12: los labels de clasificación de clientes se leen en masculino en la pantalla real"
    - "D-14: Servicios se ve exactamente igual que antes del refactor y Canchas alcanzó la paridad"
    - "El operador sabe que la 066 se aplica a mano a producción y con qué runbook"
  artifacts:
    - path: ".planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-07-SUMMARY.md"
      provides: "resultado de la UAT visual con las observaciones reportadas y el estado del apply de la 066"
      min_lines: 40
  key_links:
    - from: "14-07-SUMMARY.md"
      to: "14-04-SUMMARY.md"
      via: "el runbook del apply manual de la 066 sale del SUMMARY del plan que la escribió"
      pattern: "066"
---

<objective>
Cerrar la fase por donde se abre: mirándola. POLISH-04, POLISH-05, D-12 y EXTRA-A son cambios
**visuales** — ningún `tsc`, `vitest` ni `build` puede decir si se ven bien. Y la migración 066
necesita su handoff operativo explícito.

Purpose: el aprendizaje registrado de este workstream es que **`auto_advance` auto-aprueba los
checkpoints de verificación humana** — en Phase 12 los 5 pasos manuales quedaron pendientes con el
plan marcado como aprobado. Por eso cada checkpoint de acá pide **observaciones reportadas**, no un
"aprobado": si nadie abrió el navegador, no hay con qué llenarlas.

Output: la UAT visual de los 6 ítems de la fase en los dos shells y en los dos anchos, más el
runbook del apply manual de la 066 en producción, ejecutado o explícitamente agendado.

⚠ Este plan **no modifica código**. Si un checkpoint encuentra un problema, se abre un plan de cierre
de gaps; no se parchea desde acá.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-01-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-02-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-03-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-04-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-05-SUMMARY.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-06-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Ningún símbolo ni archivo de código. Este plan produce **evidencia**: el `14-07-SUMMARY.md` con las
observaciones de la UAT y el estado del apply de la 066 en producción.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Preparar el entorno de UAT y dejar los datos del caso a la vista</name>

  <files>—</files>

  <read_first>
    - Los seis SUMMARY de la fase (`14-01` a `14-06`), en particular los ítems marcados como "A mirar
      en la UAT visual" que dejaron los planes 14-01 y 14-02, y la prueba manual del 14-06.
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-04-SUMMARY.md` §Runbook.
    - `supabase/seed.sql` — el negocio de prueba persistente que recrea `npx supabase db reset`, y las
      credenciales de login local.
  </read_first>

  <action>
    Dejar el entorno listo para que la verificación humana sea de un tirón, sin que el revisor tenga
    que fabricar datos:

    1. Levantar el stack local y correr `npx supabase db reset` (replaya el baseline hasta la 066 y
       recrea el negocio de prueba). Levantar `npm run dev`.
    2. Sembrar por SQL contra el Supabase local, en el negocio de prueba, los casos que la UAT
       necesita y que el seed no trae:
       - un cliente **sin ningún turno**, creado hoy (caso de D-10);
       - un cliente con turnos cuyo último sea de hace ~50 días (caso de D-11, el rango que cambia de
         tab);
       - una serie de abono **dada de baja** y una serie **con todas las sesiones asignadas y sin
         turnos futuros** (los dos casos del tab de archivados);
       - una serie de abono **activa** (el caso que no debe ofrecer borrado);
       - en un negocio de vertical canchas, al menos **una cancha activa y una desactivada**.
    3. Anotar en el SUMMARY los identificadores y nombres de cada caso sembrado, para que el revisor
       sepa exactamente a qué fila mirar.
  </action>

  <verify>
    <automated>npx supabase migration list --local</automated>
  </verify>

  <acceptance_criteria>
    - `npx supabase migration list --local` muestra la `066` aplicada localmente.
    - `npm run dev` levanta y `/clientes`, `/abonos`, `/servicios` y `/admin/auditoria` responden 200.
    - El SUMMARY lista los 6 casos sembrados con su nombre visible en pantalla.
    - `npx vitest run` verde y `npm run build` completa (última corrida de la fase entera antes de la UAT).
  </acceptance_criteria>

  <done>El entorno local corre la fase completa y los datos de cada caso están identificados por nombre.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT visual de POLISH-04, POLISH-05 y EXTRA-A en los dos shells</name>

  <files>—</files>

  <read_first>
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-01-SUMMARY.md` y
      `14-02-SUMMARY.md` — los dos ítems que esos planes marcaron explícitamente como "A mirar en la
      UAT visual" (los paneles laterales angostos de Agenda y de Clientes bajo D-01).
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-05-SUMMARY.md` — la API del
      módulo compartido y qué se espera que **no** haya cambiado en Servicios.
    - `14-CONTEXT.md` §D-05, §D-06 — el aspecto objetivo del badge y la aceptación explícita de que
      el CRM cambie.
  </read_first>

  <what-built>
    POLISH-04 (ancho de botones app-wide, 22 call-sites), POLISH-05 (RiskBadge "Alto" con relleno
    semántico de peligro en los dos shells) y EXTRA-A (tabs Activos/Desactivados en Canchas, con
    Servicios refactorizado sin cambiar de aspecto).
  </what-built>

  <action>
    **Hay que abrir el navegador. No se aprueba de memoria ni por lectura del diff.**
    Cada punto pide una observación concreta que hay que **transcribir en la respuesta**; una
    aprobación sin las observaciones se rechaza.

    **A — POLISH-05, los dos shells (D-06).** El badge cambia de aspecto también dentro del CRM y eso
    está aceptado.
    1. Abrir `/admin/auditoria`. **Reportar de qué color se ve el chip "Alto"** y si tiene o no un
       puntito a la izquierda.
    2. En el panel, disparar los 4 diálogos de confirmación que muestran el chip: eliminar un abono
       (`/abonos`), eliminar un servicio y eliminar una sede (`/servicios`), y eliminar una cancha
       (negocio de vertical canchas). **Reportar si el chip "Alto" se ve igual en los cuatro** y si
       en alguno quedó gris.
    3. Comparar contra un chip "Medio" (por ejemplo el de la tarjeta de precio de plan). **Reportar
       cuál de los dos pesa más visualmente.** El resultado esperado es que "Alto" pese más que
       "Medio"; si sigue pesando más "Medio", el objetivo de POLISH-05 no se cumplió.
    4. Repetir el punto 1 en los temas claro y oscuro. **Reportar si el texto del chip se lee bien en
       los dos.**

    **B — POLISH-04, ancho de botones.** Mirar en **375px** y en **≥1280px**.
    5. `Negocio → Cobros`: los botones "Guardar" y "Liberar horarios vencidos" (los dos casos que
       motivaron el pedido). **Reportar si en desktop ocupan todo el ancho de la tarjeta o solo su
       contenido**, y si en mobile siguen siendo cómodos de tocar.
    6. Recorrer el resto de Ajustes y reportar **cuántos botones quedaron ocupando todo el ancho de
       su tarjeta en desktop** (esperado: ninguno).
    7. **Los dos casos marcados como riesgo por los planes 14-01 y 14-02:** el panel lateral angosto
       de `Agenda → Días especiales` (3 botones) y el panel lateral de `/clientes` (Exportar CSV +
       Importar CSV en un grid de 2 columnas, y "Nuevo cliente" en su propia fila). **Reportar si en
       desktop se ven deliberados o si quedaron chicos y desalineados dentro de su columna.** Es el
       único punto de esta UAT donde una respuesta negativa puede llevar a revisar una decisión.
    8. `Turnos` y `Abonos`: el formulario de alta con el aviso de cliente duplicado, y el botón
       "Guardar" de la ventana de reserva. **Reportar si los dos "Guardar" de la ventana de reserva
       (el de Agenda y el de Abonos) se ven idénticos.**

    **C — EXTRA-A, tabs en Canchas y cero regresión en Servicios.**
    9. `/servicios` con un negocio normal: **reportar si las píldoras Activos/Desactivados, sus
       contadores y el estado vacío de cada tab se ven exactamente como antes de esta fase.**
       Cualquier diferencia perceptible es una regresión y se reporta como tal.
    10. Negocio de vertical canchas: **reportar si aparecen las dos píldoras con contadores correctos**,
        si al desactivar una cancha se mueve sola al otro tab, si el tab vacío muestra el panel de
        borde punteado con su texto, y **si alguna cancha desactivada sigue apareciendo tachada**
        (esperado: ninguna, D-15).
  </action>

  <acceptance_criteria>
    - El SUMMARY contiene **10 observaciones numeradas**, una por punto, cada una describiendo lo que se vio en pantalla (no "ok" ni "aprobado" a secas).
    - El punto 1 reporta un color concreto para el chip "Alto" y la presencia o ausencia del puntito.
    - El punto 3 reporta cuál de los dos chips ("Alto" o "Medio") pesa más visualmente.
    - El punto 6 reporta un número de botones estirados en desktop (esperado `0`).
    - El punto 7 reporta un veredicto explícito sobre los dos paneles laterales angostos.
    - El punto 9 reporta si hubo o no diferencia perceptible en Servicios respecto de antes de la fase.
    - El punto 10 reporta si quedó alguna cancha desactivada con tachado (esperado: ninguna).
  </acceptance_criteria>

  <resume-signal>
    Responder con las observaciones de los 10 puntos (una línea cada uno). Si todo pasa, cerrar con
    "aprobado". Si algo falla, describir qué pantalla, qué ancho y qué se vio.
  </resume-signal>

  <done>Los tres ítems visuales están verificados en los dos shells, los dos temas y los dos anchos, con observaciones transcritas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT funcional de POLISH-06, POLISH-07 y EXTRA-B</name>

  <files>—</files>

  <read_first>
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-03-SUMMARY.md` — qué casos
      cubre ya la suite de integración del endpoint y cuál queda para la mano.
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-06-SUMMARY.md` — los 4
      pasos de la prueba manual que ya corrió el ejecutor y el invariante de visibilidad del botón.
    - El SUMMARY de esta misma fase (Task 1) — los nombres e ids de los 6 casos sembrados.
    - `14-CONTEXT.md` §D-08, §D-09, §D-10, §D-11, §D-12, §D-19.
  </read_first>

  <what-built>
    POLISH-06 (una serie dada de baja deja de ofrecer "Copiar link de baja", y el endpoint deja de
    entregarlo), POLISH-07 (un cliente sin turnos cae en "Nuevos" y el umbral de pausa se unificó en
    60 días, con los labels en masculino) y EXTRA-B (borrado definitivo de una serie archivada).
  </what-built>

  <action>
    **Hay que abrir el navegador y usar los datos sembrados en la Task 1.**

    **D — POLISH-07.**
    1. `/clientes`: buscar el cliente **sin ningún turno** creado hoy. **Reportar en qué tab aparece
       y qué dice su etiqueta de estado.** Esperado: tab "Nuevos", etiqueta en masculino.
    2. **Reportar el texto exacto de las 5 píldoras de filtro** de la pantalla. Esperado: todas en
       masculino salvo "Frecuentes" y "Pausa".
    3. Abrir la ficha de ese mismo cliente y **reportar qué sugerencia le muestra**. Esperado:
       la de cliente reciente, **no** la de cliente pausado.
    4. Buscar el cliente con último turno de hace ~50 días y **reportar en qué tab cayó**. Esperado:
       fuera de "Pausa" (efecto aceptado de D-11).
    5. En el filtro de obras sociales (si el negocio es de vertical salud), **reportar si el texto
       "Todas las obras sociales" sigue en femenino**. Esperado: sí, no se toca.

    **E — POLISH-06.**
    6. `/abonos` → tab Archivados → abrir una serie **dada de baja**. **Reportar si aparece el botón
       "Copiar link de baja" y si aparece el párrafo que explica mandarlo por WhatsApp.** Esperado:
       ninguno de los dos, y sin texto huérfano ni hueco raro donde estaban.
    7. Abrir una serie **activa** y **reportar si el botón y su párrafo siguen ahí**. Esperado: sí —
       la acción solo desaparece para las series dadas de baja.
    8. Copiar el id de la serie dada de baja y pedir su link directamente por la API
       (`/api/abonos/cancel-link/<id>` en el navegador, con la sesión abierta). **Reportar el status
       y el cuerpo de la respuesta.** Esperado: 404, y el mismo cuerpo que devuelve un id inventado
       (probar los dos y comparar).

    **F — EXTRA-B.**
    9. `/abonos` → tab Activos → abrir una serie. **Reportar si ofrece "Eliminar".** Esperado: no.
    10. Tab Archivados → abrir la serie dada de baja. **Reportar si ofrece "Eliminar"**, y confirmar.
        **Reportar si la serie desapareció de la lista y si los contadores de los dos tabs se
        actualizaron.**
    11. Ir a `Finanzas` y a la ficha del cliente de esa serie. **Reportar si los turnos que la serie
        había generado siguen apareciendo, con su nombre de servicio y su precio.** Es la promesa de
        D-16: borrar la serie no destruye historia.
    12. Repetir con la serie que tenía **todas las sesiones asignadas**: también se borra.
    13. **Camino de rechazo:** abrir el diálogo de eliminar sobre una serie archivada y, **antes** de
        confirmar, cambiar por SQL el estado de esa serie a activo; después confirmar. **Reportar si
        el diálogo quedó abierto y qué mensaje apareció.** Esperado: el modal NO cierra y se explica
        que hay que darla de baja primero. Es el backstop del gate de la 066.
  </action>

  <acceptance_criteria>
    - El SUMMARY contiene **13 observaciones numeradas**, una por punto, con lo observado en pantalla.
    - El punto 1 reporta el tab y la etiqueta exacta del cliente sin turnos (esperado: tab de nuevos, etiqueta en masculino).
    - El punto 2 transcribe el texto de las 5 píldoras de filtro.
    - El punto 8 transcribe status y cuerpo de las **dos** respuestas (serie dada de baja e id inventado) y declara si son idénticos.
    - El punto 11 reporta si los turnos de la serie borrada siguen visibles con nombre y precio.
    - El punto 13 reporta si el modal quedó abierto y cuál fue el mensaje.
  </acceptance_criteria>

  <resume-signal>
    Responder con las observaciones de los 13 puntos (una línea cada uno). Si todo pasa, cerrar con
    "aprobado". Si algo falla, describir el paso y lo observado.
  </resume-signal>

  <done>Los tres ítems funcionales están verificados end-to-end, incluidos el camino de rechazo del gate y la supervivencia del historial.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Apply manual de la migración 066 en producción</name>

  <files>supabase/migrations/066_abono_delete_gate.sql</files>

  <read_first>
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-04-SUMMARY.md` §Runbook —
      el runbook completo, con el pre-check y la señal de éxito.
    - `supabase/migrations/066_abono_delete_gate.sql` — el contenido íntegro a ejecutar.
    - `.planning/workstreams/motor-reservas/REQUIREMENTS.md` §"Decisiones LOCKED" — la regla del
      workstream sobre migraciones aplicadas a mano.
  </read_first>

  <what-built>
    La migración **066** (`supabase/migrations/066_abono_delete_gate.sql`) está escrita, validada
    contra el Supabase local con `npx supabase db reset`, espejada en `supabase/schema.sql` y
    cubierta por `test/abono-delete-gate.test.ts`.
  </what-built>

  <action>
    **Acción humana obligatoria: el apply en producción NO lo hace el flujo GSD.**
    `supabase db push` está prohibido en este proyecto y ningún agente debe intentarlo.

    Runbook (el detalle completo está en `14-04-SUMMARY.md` §Runbook):
    1. Confirmar en el Supabase de **producción** que la última migración aplicada es la **065**.
    2. Ejecutar el contenido íntegro de `supabase/migrations/066_abono_delete_gate.sql` en el SQL
       Editor de producción.
    3. Ejecutar el `NOTIFY` de recarga del schema cache de PostgREST en la misma sesión.
    4. Verificar en producción que borrar una serie de abono activa devuelve el error del gate y que
       borrar una archivada funciona.
    5. Coordinar el orden con el deploy: **la 066 va antes** de que el código del plan 14-06 esté
       sirviendo, o el botón de eliminar aparecerá sin gate en la base.

    Si el apply se posterga al momento del deploy del milestone, eso también es una respuesta válida:
    hay que **decirlo explícitamente** para que quede registrado como pendiente operativo bloqueante,
    igual que se hizo con las migraciones 054 a 065.
  </action>

  <acceptance_criteria>
    - El SUMMARY declara uno de estos dos estados, sin ambigüedad: "066 aplicada en producción el `<fecha>`" o "066 pendiente — se aplica en el deploy del milestone".
    - Si el estado es "aplicada": el SUMMARY registra el resultado del paso 4 (borrar una serie activa en producción devuelve el error del gate; borrar una archivada funciona).
    - Si el estado es "pendiente": el ítem queda registrado en el SUMMARY como **bloqueante operativo** con la nota de que la base va **antes** que el deploy del código del plan 14-06.
    - Ningún comando ejecutado durante este checkpoint contiene `db push`.
  </acceptance_criteria>

  <resume-signal>
    Responder "066 aplicada en prod" con la fecha, o "066 pendiente — se aplica en el deploy del
    milestone". Cualquiera de las dos cierra el checkpoint; el silencio no.
  </resume-signal>

  <done>El estado del apply de la 066 en producción está declarado explícitamente y registrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositorio → base de producción | La 066 cruza este límite **solo por mano humana**. Ningún agente ni comando del flujo GSD lo hace. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-28 | Tampering | apply de la 066 en producción | mitigate | Checkpoint humano bloqueante con runbook y verificación posterior en prod; `supabase db push` prohibido. El orden respecto del deploy está explicitado (la base primero). |
| T-14-29 | Repudiation | checkpoints auto-aprobados | mitigate | Cada checkpoint exige **observaciones transcritas**, no un "aprobado" suelto: una auto-aprobación deja el SUMMARY sin las 23 observaciones y el hueco es visible. Lección registrada del workstream (Phase 12). |
| T-14-30 | Information Disclosure | datos sembrados para la UAT | accept | El sembrado corre contra el Supabase **local** sobre el negocio de prueba del `seed.sql`; ningún dato real de cliente entra en juego. |
</threat_model>

<verification>
- Los tres checkpoints respondidos con contenido, no con una aprobación vacía.
- Las 23 observaciones (10 + 13) transcritas en el SUMMARY.
- El estado del apply de la 066 en producción declarado explícitamente (aplicada con fecha, o pendiente para el deploy).
- `npx vitest run`, `npx tsc --noEmit` y `npm run build` verdes en la corrida final de la Task 1.
</verification>

<success_criteria>
- Los 4 Success Criteria del ROADMAP para Phase 14 verificados **mirando la aplicación**, no leyendo el diff.
- El RiskBadge verificado en los dos shells y en los dos temas (D-06).
- Servicios confirmado sin regresión visual y Canchas con paridad (D-14, D-15).
- El borrado de una serie archivada verificado end-to-end, incluido el camino de rechazo de la base.
- El apply de la 066 en producción ejecutado o registrado como pendiente operativo bloqueante.
</success_criteria>

<output>
Crear `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-07-SUMMARY.md` al terminar.
Debe contener, sí o sí:
1. Las 10 observaciones del primer checkpoint y las 13 del segundo, transcritas.
2. El veredicto del punto 7 (los dos paneles laterales angostos bajo D-01): si el resultado es
   negativo, abrir un ítem de cierre de gaps en vez de aprobar.
3. El estado del apply de la 066 en producción, con fecha o con la declaración de pendiente.
4. La lista de todo hallazgo que no se cerró en esta fase, para que el cierre del milestone lo vea.
</output>
