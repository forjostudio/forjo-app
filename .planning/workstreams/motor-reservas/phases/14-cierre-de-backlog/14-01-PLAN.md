---
phase: 14-cierre-de-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/settings/settings-client.tsx
  - app/(dashboard)/agenda/agenda-client.tsx
  - components/dashboard/nuevo-turno-form.tsx
  - components/dashboard/nuevo-abono-form.tsx
  - components/crm/risk-badge.tsx
autonomous: true
requirements: [POLISH-04, POLISH-05]

must_haves:
  truths:
    - "D-01: un botón de acción del dashboard es full-width en mobile (<640px) y ancho-por-contenido en desktop"
    - "D-02: los botones que se estiraban SIN clase de ancho (hijos directos de <Card>) dejan de ocupar todo el ancho en desktop"
    - "D-05: el RiskBadge 'Alto' se ve como pill relleno de peligro, sin dot"
    - "D-06: el cambio del badge aplica en los DOS shells desde UN solo componente"
  artifacts:
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "8 botones hijos directos de <Card> desestirados con self-start (D-02, D-03)"
      contains: "self-start"
    - path: "app/(dashboard)/agenda/agenda-client.tsx"
      provides: "4 botones w-full sm:w-auto (D-01 causa 1) + 1 self-start (D-02 causa 2)"
      contains: "sm:w-auto"
    - path: "components/dashboard/nuevo-turno-form.tsx"
      provides: "2 botones del aviso de dedupe alineados a D-01"
      contains: "sm:w-auto"
    - path: "components/dashboard/nuevo-abono-form.tsx"
      provides: "2 botones del aviso de dedupe alineados a D-01"
      contains: "sm:w-auto"
    - path: "components/crm/risk-badge.tsx"
      provides: "variante alto con relleno semántico de peligro, sin dot (D-05, D-07)"
      contains: "danger-foreground"
  key_links:
    - from: "components/crm/risk-badge.tsx"
      to: "app/globals.css"
      via: "consume la indirección --danger / --danger-foreground, nunca el token de un shell puntual (D-04, D-07)"
      pattern: "var\\(--danger\\)"
---

<objective>
Cerrar POLISH-04 en Ajustes, Agenda y los dos formularios de alta, y POLISH-05 en el
`RiskBadge`. Son los tres archivos de pulido puro que **ningún otro plan de esta fase toca**,
así que este plan corre en Wave 1 sin conflicto.

Purpose: los botones "Guardar" y "Liberar horarios vencidos" de Negocio → Cobros se leen
estirados por accidente (D-02), y el badge "Alto" pesa visualmente menos que "Medio" (D-05).
Los dos son bugs de presentación reportados por el dueño.

Output: 19 call-sites de botón alineados al criterio único de D-01/D-02 + la variante `alto`
del badge convertida a relleno semántico de peligro.

**NO se toca `components/ui/card.tsx`.** Su `flex flex-col` es la causa raíz de D-02 pero es
transversal a toda la app: el fix vive en los call-sites. Tampoco se tocan los tokens de
`app/globals.css` / `app/themes.css` (D-04: la indirección ya se arregló en 13-05 #1).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-PATTERNS.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<artifacts_this_phase_produces>
Este plan **no crea símbolos nuevos**. Modifica valores de `className` en call-sites existentes
y la definición de una variante `cva` ya exportada.

| Símbolo | Archivo | Estado |
|---|---|---|
| `riskBadgeVariants` (variante `alto`) | `components/crm/risk-badge.tsx` | modificado (ya exportado) |
| `RiskBadge` | `components/crm/risk-badge.tsx` | firma intacta, render cambiado |

Convención de clase adoptada para toda la fase (D-01, discreción del planner sobre el orden que
el repo no normalizó): **`sm:w-auto` se agrega al FINAL de la lista de clases existente**, molde
literal de `abonos-client.tsx:480` (`w-full gap-1.5 sm:w-auto`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Desestirar los 8 botones hijos directos de Card en Ajustes (POLISH-04 causa 2 / D-02)</name>

  <files>app/(dashboard)/settings/settings-client.tsx</files>

  <read_first>
    - `app/(dashboard)/settings/settings-client.tsx` — leer la línea **1568** primero: es el
      precedente literal ya validado en producción (`<Button className="self-start" …>` hijo
      directo del `<Card>` de `:1452`). Después leer los 8 targets con su `<Card>` padre para
      confirmar que ninguno tiene todavía una clase de ancho: `:1446` (Guardar panel, `<Card>` de
      `:1429`), `:2165` (Guardar / Seña, `<Card>` de `:2142`), `:2174` (Liberar horarios vencidos,
      `<Card>` de `:2169`), `:2222` (Conectar con MercadoPago, `<Card>` de `:2183`, dentro de una
      expresión condicional), `:2282` (Conectar Google Calendar, `<Card>` de `:2256`, rama del
      ternario), `:2325` (Guardar / Notificaciones, `<Card>` de `:2293`), `:2359` (Guardar /
      reCAPTCHA, `<Card>` de `:2332`), `:2392` (Ver planes, `<Card>` de `:2389`).
    - `components/ui/card.tsx` — **solo lectura**: confirmar en `:15` el `flex flex-col` que causa
      el `align-items: stretch`. **Prohibido modificarlo.**
    - `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-PATTERNS.md` §1.b — el
      inventario auditado (10 confirmados) y los 20 candidatos descartados, para no "arreglar"
      botones cuyo padre es un `div` de bloque o un `div.flex` en fila.
  </read_first>

  <action>
    Agregar a cada uno de los 8 `<Button>` listados el atributo `className="self-start"`, con el
    mismo texto exacto que ya usa `:1568`. Los 8 hoy no declaran ningún `className`, así que es un
    atributo nuevo, no un merge — verificarlo antes de escribir.

    Resolución explícita de **D-03** (alineación en desktop, delegada al planner): **los 8 van a la
    izquierda con `self-start`**, sin excepción y sin `justify-end`. Motivo: los 8 viven dentro de
    una `<Card>` de configuración cuyo contenido arranca alineado a la izquierda (título, ayuda,
    campos); mandar el botón a la derecha crearía un eje visual nuevo por card en una pantalla que
    ya tiene 10+ cards apiladas. `justify-end` queda descartado para esta fase — el precedente
    `:1568` ya eligió izquierda y esta fase lo replica, no lo revisa.

    Los 2 casos que motivaron el pedido del usuario son `:2165` y `:2174` (Negocio → Cobros): dejar
    un comentario denso en español sobre el primero de los dos explicando el porqué no obvio — que
    el botón se estiraba sin declarar ancho porque su contenedor es flex-column, y citando D-02.
    No repetir el comentario en los otros 7.

    **No tocar** ninguno de los descartados del §1.b de PATTERNS (`:1485`, `:1488`, `:1494`,
    `:1498`, `:1656`, `:1659`, `:1662`, `:1709`, `:1854`, `:2091`, `:2116`, `:2203`, `:2216`,
    `:2248`, `:2272`, `:2276`, `:2382`, `:2383`): su padre no los estira y agregarles la clase
    cambiaría layouts que hoy están bien.

    **No tocar** las píldoras Activos/Desactivados ni el bloque de Servicios (`:1592-1660`): son
    territorio del plan 14-05, que depende de este.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE 'className="[^"]*self-start' "app/(dashboard)/settings/settings-client.tsx"` devuelve exactamente `9` (el precedente de `:1568` + los 8 nuevos).
    - `grep -cE 'className="self-start"' "app/(dashboard)/settings/settings-client.tsx"` devuelve exactamente `9` (los 8 nuevos usan el texto literal idéntico al precedente, sin `cn()` ni clases extra).
    - `git diff --stat components/ui/card.tsx` no devuelve ninguna línea (el archivo transversal quedó intacto).
    - `git diff --numstat "app/(dashboard)/settings/settings-client.tsx"` muestra a lo sumo `9` líneas agregadas y `8` borradas (8 líneas de botón reescritas + 1 línea de comentario nueva): ningún cambio estructural colado.
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>

  <done>Los 8 botones de Ajustes declaran `self-start`, `card.tsx` está intacto y el proyecto tipa.</done>
</task>

<task type="auto">
  <name>Task 2: Alinear a D-01 los 9 botones de Agenda y de los dos formularios de alta (POLISH-04)</name>

  <files>app/(dashboard)/agenda/agenda-client.tsx, components/dashboard/nuevo-turno-form.tsx, components/dashboard/nuevo-abono-form.tsx</files>

  <read_first>
    - `app/(dashboard)/agenda/agenda-client.tsx` — los 4 casos de causa 1: `:1035` (Marcar como
      cerrado, `variant="destructive"`), `:1037` (Quitar excepción), `:1047` (Aplicar horario
      especial) — los tres viven en el **panel lateral angosto** de Días especiales — y `:1156`
      (Copiar a N días, dentro de un `DialogContent`). Y el caso de causa 2: `:946` (Guardar de la
      ventana de reserva), hijo directo del `<Card>` que cierra en `:947`.
    - `components/dashboard/nuevo-turno-form.tsx` `:455` y `:460` — los dos botones del aviso ámbar
      de dedupe de cliente. Leer también las otras líneas del archivo que declaran ancho completo
      en un `input`/`div`: **no son botones y no se tocan**.
    - `components/dashboard/nuevo-abono-form.tsx` `:417` y `:422` — gemelos literales de los
      anteriores; el resultado debe quedar idéntico en los dos archivos.
    - `app/(dashboard)/abonos/abonos-client.tsx` `:480` y `:492` — el molde de destino ya vigente en
      el repo (`w-full gap-1.5 sm:w-auto`). **Solo lectura**: ese archivo lo escriben los planes
      14-03 y 14-06.
    - `app/(dashboard)/appointments/appointments-client.tsx` `:275` — la tercera ocurrencia del
      patrón, con el orden de clases invertido; se deja como está (la fase no normaliza el orden de
      lo existente, solo fija el orden de lo nuevo).
  </read_first>

  <action>
    En los 8 botones de causa 1 (`agenda-client.tsx` `:1035`, `:1037`, `:1047`, `:1156`;
    `nuevo-turno-form.tsx` `:455`, `:460`; `nuevo-abono-form.tsx` `:417`, `:422`) agregar
    `sm:w-auto` **al final** de la lista de clases que ya declaran, conservando el resto tal cual
    (incluido el `gap-1.5` de `nuevo-turno-form.tsx:460` y `nuevo-abono-form.tsx:422`). Resultado
    esperado: `w-full sm:w-auto` en los que solo tenían ancho, y `w-full gap-1.5 sm:w-auto` en los
    dos que además tenían gap.

    En `agenda-client.tsx:946` (causa 2, hijo directo de `<Card>`) agregar `className="self-start"`
    con el mismo criterio de D-03 resuelto en la Task 1 (izquierda, no `justify-end`): es el gemelo
    exacto del "Guardar" de la ventana de reserva de Abonos que resuelve el plan 14-03 — los dos
    tienen que quedar idénticos.

    **Riesgo conocido a dejar asentado en el SUMMARY, no a resolver acá:** los 3 botones de
    `:1035/:1037/:1047` viven en el panel lateral angosto de Días especiales (columna dentro de un
    `lg:flex-row`). D-01 es LOCKED y explícitamente "sin excepciones por contenedor", así que se
    aplica igual; pero a partir de 640px esos botones pasan a ancho-por-contenido dentro de un panel
    que sigue siendo angosto. Anotarlo en el SUMMARY como ítem a mirar en la UAT visual del plan
    14-07 — **no** inventar una excepción ni revertirlo por cuenta propia.

    Comentario denso solo en el primero de los 3 del panel angosto, explicando ese trade-off y
    citando D-01. Los gemelos de los dos formularios no necesitan comentario nuevo.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE 'className="[^"]*sm:w-auto"' "app/(dashboard)/agenda/agenda-client.tsx"` devuelve `4`.
    - `grep -cE 'className="w-full"' "app/(dashboard)/agenda/agenda-client.tsx"` devuelve `0` (los 4 eran todos botones).
    - `grep -cE 'className="[^"]*self-start' "app/(dashboard)/agenda/agenda-client.tsx"` devuelve `1`.
    - `grep -cE 'className="[^"]*sm:w-auto"' components/dashboard/nuevo-turno-form.tsx` devuelve `2`; el mismo comando sobre `components/dashboard/nuevo-abono-form.tsx` devuelve `2`.
    - `grep -cE 'className="w-full gap-1\.5"' components/dashboard/nuevo-turno-form.tsx` devuelve `0`; ídem sobre `components/dashboard/nuevo-abono-form.tsx`.
    - `grep -cE 'className="w-full"' components/dashboard/nuevo-turno-form.tsx` devuelve `3` y sobre `components/dashboard/nuevo-abono-form.tsx` devuelve `4` (los `input`/`div` de ancho completo NO se tocaron: bajó exactamente 1 en cada archivo).
    - `git diff --stat "app/(dashboard)/abonos/abonos-client.tsx" "app/(dashboard)/appointments/appointments-client.tsx"` no devuelve ninguna línea.
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>

  <done>Los 9 botones de Agenda y de los dos formularios siguen el criterio único de D-01/D-02, sin colateral sobre inputs ni sobre archivos de otros planes.</done>
</task>

<task type="auto">
  <name>Task 3: RiskBadge "Alto" con relleno semántico de peligro (POLISH-05 / D-05, D-06, D-07)</name>

  <files>components/crm/risk-badge.tsx</files>

  <read_first>
    - `components/crm/risk-badge.tsx` — entero (69 líneas): el `cva` `riskBadgeVariants` (`:22-36`),
      el bloque del dot de `alto` (`:54-60`), el dot de `bajo` (`:61-63`) y el JSDoc de cabecera
      (`:4-18`), que hoy describe el comportamiento viejo.
    - `components/crm/confirm-dialog.tsx` `:180-200` — `confirmButtonClass()`, el molde exacto de la
      clase de relleno de peligro y la razón documentada de por qué se referencia la indirección y
      nunca el token específico de un shell.
    - `app/globals.css` `:88-110` y `:246-258` — la indirección resuelta en 13-05 #1 (`:root` y
      `.crm-shell`). **Solo lectura, no redeclarar nada (D-04, D-07).**
    - `app/themes.css` `:34-42`, `:105-110`, `:173-178` — los pares de peligro por theme. **Solo
      lectura**: el contraste ya está resuelto, no se recalcula.
  </read_first>

  <action>
    Cambiar la variante `alto` del `cva` para que use borde transparente y el par de tokens
    semánticos de peligro: el fondo pasa a `bg-[var(--danger)]` y el texto a
    `text-[var(--danger-foreground)]`, reemplazando el `border-border bg-secondary text-foreground`
    actual. Queda simétrica con `medio`, que ya es un pill relleno.

    Eliminar por completo el bloque condicional que renderiza el dot cuando `risk === 'alto'`
    (`:54-60`): con el relleno el dot es redundante (D-05). El dot de `bajo` (`:61-63`) **se
    conserva sin cambios**, igual que la variante `medio` y `bajo` del `cva` y el `RISK_LABEL`.

    Actualizar el JSDoc de cabecera y el comentario del dot: hoy describen "pill oscuro + dot rojo"
    para `alto`, que deja de ser cierto. El texto nuevo tiene que dejar asentado el invariante D-07
    — un componente compartido resuelve el peligro por la indirección semántica y nunca por el token
    propio de un shell puntual — sin escribir ese token en una expresión `var(...)`, porque el gate
    de esta tarea cuenta usos reales.

    **No** agregar `hover` (el badge no es interactivo, así que el token de hover no aplica). **No**
    crear variantes por shell ni bifurcar por scope: D-06 es explícito en que sigue habiendo UN solo
    componente y que el CRM cambia de aspecto a propósito.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE 'alto: .border-transparent' components/crm/risk-badge.tsx` devuelve `1`.
    - `grep -cE 'bg-\[var\(--danger\)\]' components/crm/risk-badge.tsx` devuelve `1` y `grep -cE 'text-\[var\(--danger-foreground\)\]' components/crm/risk-badge.tsx` devuelve `1`.
    - `grep -cE "risk === 'alto'" components/crm/risk-badge.tsx` devuelve `0` (el bloque del dot de alto desapareció).
    - `grep -cE "risk === 'bajo'" components/crm/risk-badge.tsx` devuelve `1` (el dot de bajo sobrevivió).
    - `grep -cE 'var\(--crm-danger\)' components/crm/risk-badge.tsx` devuelve `0`.
    - `grep -cE 'medio: .border-transparent bg-primary text-primary-foreground' components/crm/risk-badge.tsx` devuelve `1` (la variante medio quedó byte-idéntica).
    - `git diff --stat app/globals.css app/themes.css components/crm/confirm-dialog.tsx` no devuelve ninguna línea.
    - `npx tsc --noEmit` sale 0 y `npx vitest run` termina con el mismo número de tests que antes del cambio, sin fallos.
  </acceptance_criteria>

  <done>La variante `alto` es un pill relleno de peligro sin dot, el resto del componente y los tokens quedaron intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Ninguna nueva. Este plan no toca datos, ni endpoints, ni queries, ni permisos: cambia valores de
`className` y una variante `cva`. No cruza ningún límite de confianza.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | `components/crm/risk-badge.tsx` | accept | El badge renderiza un literal de un enum de 3 valores (`RISK_LABEL`) que ya se mostraba; el cambio es de color. No se agrega ningún dato del negocio al DOM. |
| T-14-02 | Tampering | `components/ui/card.tsx` | mitigate | El componente transversal queda fuera del diff: criterio de aceptación `git diff --stat` vacío en las tres tareas. Modificarlo cambiaría el layout de toda la app en un plan de pulido. |
| T-14-03 | Denial of Service | superficie visual | accept | Un `className` incorrecto degrada la lectura, no la disponibilidad. Backstop = la UAT visual bloqueante del plan 14-07 sobre los dos shells (D-06). |
</threat_model>

<verification>
- `npx tsc --noEmit` sale 0.
- `npx vitest run` verde, sin cambio en el conteo de tests (este plan no agrega ni toca suites).
- `npm run build` completa sin error.
- `git diff --name-only` lista exactamente los 5 archivos de `files_modified` y ninguno más.
</verification>

<success_criteria>
- Los 19 call-sites de botón (8 en Ajustes + 4 en Agenda causa 1 + 1 en Agenda causa 2 + 4 en los dos formularios, más el precedente conservado) responden al criterio único de D-01/D-02.
- `RiskBadge` con `risk="alto"` renderiza un pill relleno de peligro sin dot, desde un único componente compartido por los dos shells (D-06).
- Ni `components/ui/card.tsx`, ni `app/globals.css`, ni `app/themes.css` aparecen en el diff.
</success_criteria>

<output>
Crear `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-01-SUMMARY.md` al terminar.
Incluir en el SUMMARY, bajo un encabezado propio, el ítem "**A mirar en la UAT visual (14-07):** los
3 botones del panel lateral angosto de Agenda → Días especiales (`:1035`, `:1037`, `:1047`) bajo
D-01, que a partir de 640px pasan a ancho-por-contenido dentro de un panel angosto".
</output>
