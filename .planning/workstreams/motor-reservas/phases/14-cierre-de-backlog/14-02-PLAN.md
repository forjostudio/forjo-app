---
phase: 14-cierre-de-backlog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/client-status.ts
  - test/client-status.test.ts
  - app/(dashboard)/clients/clients-client.tsx
autonomous: true
requirements: [POLISH-07, POLISH-04]

must_haves:
  truths:
    - "D-10: un cliente recién creado sin ningún turno aparece en el tab 'Nuevos', nunca en 'Pausa'"
    - "D-11: 'Pausa' significa lo mismo en el filtro y en la sugerencia — un único umbral de 60 días"
    - "D-12: los 8 labels de clasificación de clientes están en masculino"
    - "D-01: los botones del panel de Clientes son full-width en mobile y ancho-por-contenido en desktop"
  artifacts:
    - path: "lib/client-status.ts"
      provides: "clasificación pura de cliente (visits + daysSinceLast) y el umbral único de pausa"
      exports: ["PAUSED_AFTER_DAYS", "classifyClient", "ClientStatusKey"]
      min_lines: 25
    - path: "test/client-status.test.ts"
      provides: "cobertura unitaria sin DB de D-10 y D-11, incluidos los bordes"
      min_lines: 30
    - path: "app/(dashboard)/clients/clients-client.tsx"
      provides: "labels masculinos, consumo del helper puro en los dos sitios y los 3 botones bajo D-01"
      contains: "@/lib/client-status"
  key_links:
    - from: "app/(dashboard)/clients/clients-client.tsx"
      to: "lib/client-status.ts"
      via: "clientStats y getSuggestion llaman al MISMO helper puro — no pueden divergir"
      pattern: "classifyClient\\("
---

<objective>
Cerrar POLISH-07 (clasificación de clientes) y la parte de POLISH-04 que vive en el panel de
Clientes. Este plan es el **dueño exclusivo** de `clients-client.tsx` en toda la fase, por eso
POLISH-04 en ese archivo viaja acá y no en 14-01.

Purpose: hoy un cliente con 0 turnos hereda un `daysSinceLast` sentinela de 999 y cae en "Pausa"
— el dueño ve como "clientes que dejaron de venir" a gente que nunca vino (D-10). Además el umbral
de pausa discrepa entre el filtro (45 días) y el copy de la sugerencia (60 días / "más de 2 meses"),
así que la misma persona puede estar en el tab "Pausa" y recibir el consejo de un cliente activo.

Output: un helper puro testeable sin DB (`lib/client-status.ts`) como fuente única de la regla,
consumido por los dos sitios que hoy la reimplementan, más los 8 labels en masculino.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/motor-reservas/STATE.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-CONTEXT.md
@.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-PATTERNS.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<artifacts_this_phase_produces>
| Símbolo | Archivo | Tipo |
|---|---|---|
| `ClientStatusKey` | `lib/client-status.ts` | type exportado (`'new' \| 'active' \| 'frequent' \| 'paused'`) |
| `PAUSED_AFTER_DAYS` | `lib/client-status.ts` | const exportada = `60` |
| `classifyClient` | `lib/client-status.ts` | función pura exportada `({ visits, daysSinceLast }) => ClientStatusKey` |
| `test/client-status.test.ts` | ruta nueva | suite unitaria (sin DB, sin creds) |

Molde de extracción: `lib/staff-services.ts` (`isServiceCovered`, `professionalsForService`),
consumido desde `settings-client.tsx:1634-1639`. Mismo criterio: la regla vive en `lib/`, el
componente no la reimplementa.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper puro lib/client-status.ts + su suite unitaria (D-10, D-11)</name>

  <files>lib/client-status.ts, test/client-status.test.ts</files>

  <read_first>
    - `app/(dashboard)/clients/clients-client.tsx` `:394-413` — el `clientStats` actual: de dónde
      salen `visits` (turnos `confirmed` + `completed`) y `daysSinceLast` (`:402`, con el sentinela
      `999` cuando no hay ninguna fecha), y el orden actual de la cascada (`:406`).
    - `app/(dashboard)/clients/clients-client.tsx` `:149-182` — `getSuggestion(visits, daysSinceLast)`:
      su umbral `> 60` en `:150` y sus cortes por visitas (`<= 1`, `<= 4`, resto). Confirmar que los
      cortes por visitas coinciden exactamente con los de `clientStats` antes de unificar.
    - `lib/staff-services.ts` — el molde de helper puro del repo: named exports, sin imports de
      React ni de Supabase, comentarios densos en español explicando el porqué.
    - `test/staff-services.test.ts` — el molde de suite unitaria pura (sin `hasSupabaseCreds`, sin
      fixtures): así se escribe una suite que corre en cualquier entorno.
  </read_first>

  <behavior>
    - `PAUSED_AFTER_DAYS` vale `60` y es el único número de la regla.
    - `classifyClient({ visits: 0, daysSinceLast: 999 })` → `'new'` (D-10, el caso exacto del bug:
      el sentinela ya no gana).
    - `classifyClient({ visits: 0, daysSinceLast: 0 })` → `'new'`.
    - `classifyClient({ visits: 1, daysSinceLast: 10 })` → `'new'`.
    - `classifyClient({ visits: 2, daysSinceLast: 10 })` → `'active'`.
    - `classifyClient({ visits: 4, daysSinceLast: 10 })` → `'active'`.
    - `classifyClient({ visits: 5, daysSinceLast: 10 })` → `'frequent'`.
    - `classifyClient({ visits: 3, daysSinceLast: 50 })` → `'active'` (D-11: los 46-60 días salen de
      Pausa; con el umbral viejo de 45 esto daba `'paused'`).
    - `classifyClient({ visits: 3, daysSinceLast: 60 })` → `'active'` (borde: 60 exactos NO es pausa,
      el corte es estrictamente mayor).
    - `classifyClient({ visits: 3, daysSinceLast: 61 })` → `'paused'`.
    - `classifyClient({ visits: 7, daysSinceLast: 120 })` → `'paused'` (pausa sigue ganándole a
      frecuente, igual que hoy: no es un cambio de comportamiento).
  </behavior>

  <action>
    Crear `lib/client-status.ts` con el tipo `ClientStatusKey`, la constante de módulo
    `PAUSED_AFTER_DAYS` y la función pura `classifyClient`, que recibe un objeto con `visits` y
    `daysSinceLast` (patrón del proyecto: >2 parámetros relacionados viajan en un objeto
    desestructurado) y devuelve la key.

    Orden de evaluación **obligatorio** (D-10 es explícito): el guard de `visits` sin ninguna visita
    va **primero**, antes del chequeo de días. Recién después la cascada actual: pausa por umbral,
    frecuente, activo, nuevo. El helper **no lee `created_at`** ni ningún otro dato del cliente — la
    firma es exactamente esos dos números y nada más (D-10 lo pide para no threadear nada nuevo a
    `clientStats`).

    Comentario denso en español arriba de la función explicando el porqué no obvio: que
    `daysSinceLast` llega con un sentinela cuando la persona no tiene ninguna fecha, que ese
    sentinela hacía que un alta reciente se leyera como abandono, y que "Pausa" pasa a significar
    literalmente *vino y dejó de venir*. Citar D-10 y D-11 en el comentario.

    Crear `test/client-status.test.ts` cubriendo los 10 casos del bloque `<behavior>`, uno por `it`
    o con `it.each`, más una aserción sobre el valor de la constante. Suite **pura**: sin
    `hasSupabaseCreds`, sin `describe.skipIf`, sin fixtures — tiene que correr aunque no haya
    Supabase local levantado.
  </action>

  <verify>
    <automated>npx vitest run test/client-status.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run test/client-status.test.ts` sale 0 con al menos `10` tests pasando y `0` skipped.
    - `grep -cE 'export const PAUSED_AFTER_DAYS = 60' lib/client-status.ts` devuelve `1`.
    - `grep -cE "from '@supabase|from 'react|from 'next" lib/client-status.ts` devuelve `0` (helper puro, sin dependencias de framework).
    - `grep -cE 'created_at' lib/client-status.ts` devuelve `0`.
    - `grep -cE 'skipIf|hasSupabaseCreds' test/client-status.test.ts` devuelve `0` (la suite corre siempre).
    - `npx tsc --noEmit` sale 0.
  </acceptance_criteria>

  <done>`classifyClient` implementa D-10 y D-11 y los 10 casos del contrato pasan sin DB.</done>
</task>

<task type="auto">
  <name>Task 2: Consumir el helper en los dos sitios y pasar los 8 labels a masculino (D-10, D-11, D-12)</name>

  <files>app/(dashboard)/clients/clients-client.tsx</files>

  <read_first>
    - `lib/client-status.ts` — recién creado en la Task 1: la firma exacta a consumir.
    - `app/(dashboard)/clients/clients-client.tsx` `:37-59` — el bloque de constantes completo:
      `STATUS_DOT` (`:41-46`), `STATUS_LABEL` (`:47-52`), `FILTER_TABS` (`:53-59`).
    - `app/(dashboard)/clients/clients-client.tsx` `:142-182` — `fmtLastVisit` y `getSuggestion`
      enteros, para reescribir la selección de rama sin tocar los textos ni los colores de cada
      sugerencia.
    - `app/(dashboard)/clients/clients-client.tsx` `:394-413` — `clientStats`.
    - `app/(dashboard)/clients/clients-client.tsx` `:649` — `"Todas las obras sociales"`. **Contexto
      de lo que NO se toca:** ahí el femenino concuerda con "obras sociales" y es correcto.
    - `lib/verticals.ts` `:45-46`, `:69-70`, `:90-91`, `:108-109` — la verificación de que todos los
      verticales usan sustantivo masculino (`Cliente` / `Paciente`), que es lo que habilita D-12.
  </read_first>

  <action>
    (a) Importar `classifyClient` y `PAUSED_AFTER_DAYS` desde `@/lib/client-status` (alias `@/*`, no
    ruta relativa) y reemplazar la cascada inline de `clientStats` por una llamada al helper. El
    resto del `useMemo` (cálculo de `visits`, `lastDate`, `daysSinceLast`, `totalSpend`, la forma del
    objeto devuelto y sus deps) queda **exactamente igual**: solo se sustituye el bloque que decide
    el `status`. El sentinela `999` de `:402` se conserva tal cual — el helper es quien lo neutraliza
    ahora, y sacarlo obligaría a cambiar el tipo de `daysSinceLast` que consumen otros lugares del
    archivo.

    (b) Reescribir `getSuggestion` para que la **rama la elija `classifyClient`** con los mismos dos
    argumentos que ya recibe, y el cuerpo solo mapee la key devuelta a su objeto de copy/colores.
    Los 4 objetos (`CLIENTE PAUSADO`, `CLIENTE RECIENTE`, `CLIENTE EN DESARROLLO`, `CLIENTE
    FRECUENTE`) mantienen sus `label`, `text`, `status`, `color`, `border` y `bg` **byte-idénticos**;
    lo único que cambia es cómo se elige cuál. Con esto desaparece la discrepancia de D-11 sin tocar
    el copy visible, y el guard de D-10 aplica también a la sugerencia (hoy un cliente con 0 turnos
    recibe el consejo de "hace más de 2 meses que no viene").

    (c) Aplicar D-12 sobre los 8 labels, sin tocar nada más de esos objetos: en `STATUS_LABEL`, la
    key `new` pasa a `NUEVO` y la key `active` pasa a `ACTIVO` (`frequent` y `paused` quedan igual);
    en `FILTER_TABS`, la etiqueta de `all` pasa a `Todos`, la de `active` a `Activos` y la de `new` a
    `Nuevos` (`frequent` y `paused` quedan igual). `STATUS_DOT` **no se toca** (son clases de color,
    no copy). Barrer el resto del archivo por otros textos de UI que nombren la clasificación en
    femenino y alinearlos, **excepto** `:649`, donde el femenino concuerda con el sustantivo y es
    correcto.

    Comentario denso citando D-11 sobre la unificación del umbral, explicando el efecto aceptado: los
    clientes de 46 a 60 días sin venir salen del tab de pausa y pasan a activos / en desarrollo.
  </action>

  <verify>
    <automated>npx vitest run test/client-status.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE "from '@/lib/client-status'" "app/(dashboard)/clients/clients-client.tsx"` devuelve `1`.
    - `grep -cE 'classifyClient\(' "app/(dashboard)/clients/clients-client.tsx"` devuelve `2` (una llamada en `clientStats`, otra en `getSuggestion`).
    - `grep -cE 'daysSinceLast > 45|daysSinceLast > 60' "app/(dashboard)/clients/clients-client.tsx"` devuelve `0` (ningún umbral literal sobrevive en el componente).
    - `grep -cE "new: 'NUEVO'" "app/(dashboard)/clients/clients-client.tsx"` devuelve `1` y `grep -cE "active: 'ACTIVO'" "app/(dashboard)/clients/clients-client.tsx"` devuelve `1`.
    - `grep -cE "label: 'Todos'" "app/(dashboard)/clients/clients-client.tsx"` devuelve `1`, `grep -cE "label: 'Activos'"` devuelve `1` y `grep -cE "label: 'Nuevos'"` devuelve `1`.
    - `grep -cE "label: 'Todas'|label: 'Activas'|label: 'Nuevas'" "app/(dashboard)/clients/clients-client.tsx"` devuelve `0`.
    - `grep -c 'Todas las obras sociales' "app/(dashboard)/clients/clients-client.tsx"` devuelve `1` (intacto).
    - `grep -cE "new: 'bg-red-400'" "app/(dashboard)/clients/clients-client.tsx"` devuelve `1` (`STATUS_DOT` sin tocar).
    - `grep -cE "'CLIENTE PAUSADO'|'CLIENTE RECIENTE'|'CLIENTE EN DESARROLLO'|'CLIENTE FRECUENTE'" "app/(dashboard)/clients/clients-client.tsx"` devuelve `4` (los 4 copys de sugerencia sobrevivieron).
    - `npx tsc --noEmit` sale 0 y `npx vitest run` verde.
  </acceptance_criteria>

  <done>La regla de clasificación vive en un solo lugar, los dos consumidores la comparten y los 8 labels están en masculino.</done>
</task>

<task type="auto">
  <name>Task 3: POLISH-04 en el panel de Clientes, incluido el enlace gemelo (D-01)</name>

  <files>app/(dashboard)/clients/clients-client.tsx</files>

  <read_first>
    - `app/(dashboard)/clients/clients-client.tsx` `:588-609` — el bloque entero de las 3 acciones:
      el `<a>` de Exportar CSV (`:592-599`, estilizado con `cn(buttonVariants(...), …)`), el
      `<Button>` de Importar CSV (`:601`) y el `<Button>` primario Nuevo cliente (`:607`), con los
      comentarios que explican el grid de 2 columnas y por qué el CTA va en su propia fila.
    - `app/(dashboard)/abonos/abonos-client.tsx` `:480` — el molde de destino del repo
      (`w-full gap-1.5 sm:w-auto`), para respetar el orden de clases que fija esta fase. **Solo
      lectura**: ese archivo pertenece a los planes 14-03 y 14-06.
  </read_first>

  <action>
    Agregar `sm:w-auto` **al final** de la lista de clases de los tres call-sites: el `<Button>` de
    Importar CSV (`:601`), el `<Button>` de Nuevo cliente (`:607`) y — decisión explícita de este
    plan — también el `<a>` de Exportar CSV (`:595`), cuya lista de clases vive dentro del `cn()`
    junto a `buttonVariants`.

    **Por qué el `<a>` entra en el lote (hallazgo del pattern-mapper, no está en el CONTEXT):** los
    dos son hermanos directos dentro del mismo `grid grid-cols-2` y se leen como un par. Cambiar
    solo el `<Button>` dejaría dos gemelos de distinto ancho en la misma fila — un bug visual nuevo
    peor que el que POLISH-04 viene a cerrar. D-01 habla de "todo botón de acción del dashboard";
    un `<a>` que usa `buttonVariants` es un botón de acción a todos los efectos.

    **Riesgo conocido a dejar asentado en el SUMMARY, no a resolver acá:** las 3 acciones viven en
    un panel lateral angosto (`lg:w-80`) y las 2 primeras en un grid de 2 columnas. A partir de
    640px pasan a ancho-por-contenido dentro de celdas de grid, así que quedan alineadas a la
    izquierda de su celda. D-01 es LOCKED y sin excepciones por contenedor, así que se aplica igual;
    anotarlo como ítem a mirar en la UAT visual del plan 14-07.

    No tocar el layout: el `grid grid-cols-2 gap-2` y la fila propia del CTA se conservan, igual que
    los comentarios que los explican.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cE 'className="[^"]*sm:w-auto"' "app/(dashboard)/clients/clients-client.tsx"` devuelve `2`.
    - `grep -cE "'w-full gap-1\.5 sm:w-auto'" "app/(dashboard)/clients/clients-client.tsx"` devuelve `1` (el `<a>` dentro del `cn()`).
    - `grep -cE 'className="w-full"' "app/(dashboard)/clients/clients-client.tsx"` devuelve `0` y `grep -cE 'className="w-full gap-1\.5"' "app/(dashboard)/clients/clients-client.tsx"` devuelve `0`.
    - `grep -c 'grid grid-cols-2 gap-2' "app/(dashboard)/clients/clients-client.tsx"` devuelve `1` (el layout del panel quedó intacto).
    - `npx tsc --noEmit` sale 0 y `npm run build` completa sin error.
  </acceptance_criteria>

  <done>Las 3 acciones del panel de Clientes siguen el criterio único de D-01 y ninguna quedó desalineada respecto de su hermana.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client (payload RSC de `/clientes`) | `clients` y `appointments` ya viajan al componente; este plan solo reordena cómo se derivan de esos datos. No agrega ninguna columna nueva al payload. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Information Disclosure | `lib/client-status.ts` | mitigate | La firma del helper es dos números (`visits`, `daysSinceLast`): no recibe ni retiene identidad, contacto ni `business_id`. Criterio de aceptación: sin imports de Supabase/React/Next y sin `created_at`. |
| T-14-05 | Tampering | clasificación en el cliente | accept | La clasificación es presentación derivada de datos que el dueño ya puede ver de su propio negocio; no gatea ninguna acción ni permiso. Alterarla en el navegador no expone nada que la RLS no haya autorizado ya. |
| T-14-06 | Repudiation | umbral unificado en 60 días (D-11) | accept | Cambio de criterio de negocio explícitamente aceptado en el CONTEXT: los clientes de 46-60 días salen del tab de pausa. Se registra en el SUMMARY para que la UAT lo contraste contra datos reales. |
</threat_model>

<verification>
- `npx vitest run test/client-status.test.ts` sale 0 con ≥10 tests.
- `npx vitest run` verde en toda la suite (el conteo total sube en al menos 10 respecto del baseline).
- `npx tsc --noEmit` sale 0.
- `npm run build` completa sin error.
- `git diff --name-only` lista exactamente los 3 archivos de `files_modified`.
</verification>

<success_criteria>
- Un cliente con 0 turnos aparece bajo "Nuevos" y **no** bajo "Pausa", venga de un alta de hoy o de hace un año (D-10).
- El tab de pausa y el copy de la sugerencia comparten el mismo umbral, definido una sola vez (D-11).
- Los 8 labels de la tabla de D-12 están en masculino y `"Todas las obras sociales"` sigue intacto.
- Las 3 acciones del panel de Clientes responden al criterio único de D-01.
</success_criteria>

<output>
Crear `.planning/workstreams/motor-reservas/phases/14-cierre-de-backlog/14-02-SUMMARY.md` al terminar.
Incluir el ítem "**A mirar en la UAT visual (14-07):** las 3 acciones del panel lateral angosto de
Clientes bajo D-01 (grid de 2 columnas + CTA en fila propia)" y, si el negocio de prueba tiene
clientes con 46-60 días sin venir, cuántos se movieron de tab por D-11.
</output>
