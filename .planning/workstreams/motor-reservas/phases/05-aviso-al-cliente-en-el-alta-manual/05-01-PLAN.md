---
phase: 05-aviso-al-cliente-en-el-alta-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/email.ts
  - test/manual-notify-email.test.ts
autonomous: true
requirements: [BOOK-NOTIFY-01]

must_haves:
  truths:
    - "Existe una función que arma un mail de turno confirmado LIMPIO: servicio, fecha, hora, negocio y link de cancelar."
    - "El mail NO muestra precio, ni seña, ni saldo (a diferencia del mail del booking público)."
    - "El mail mantiene el mismo estilo/marca (accent color, logo/nombre, footer Forjo) que el resto de los mails del cliente."
    - "Si no hay cancelToken, el mail se manda igual pero sin el botón de cancelar (degradación elegante, D-04)."
  artifacts:
    - path: "lib/email.ts"
      provides: "Nuevo export sendManualBookingConfirmation (variante sin precio/seña, D-03)"
      contains: "export async function sendManualBookingConfirmation"
    - path: "test/manual-notify-email.test.ts"
      provides: "Test puro (fetch stubbeado) del contenido del mail: presencia de servicio/fecha/hora/negocio/cancel-link + ausencia de precio/seña/saldo"
      contains: "sendManualBookingConfirmation"
  key_links:
    - from: "lib/email.ts (sendManualBookingConfirmation)"
      to: "lib/email.ts (resolveSender, fmtDate, renderEmailHeader, resendSend)"
      via: "reusa los helpers existentes; NO duplica el resolver de sender ni el POST a Resend"
      pattern: "resolveSender|fmtDate|renderEmailHeader"
    - from: "test/manual-notify-email.test.ts"
      to: "global.fetch (Resend)"
      via: "vi.stubGlobal('fetch', ...) captura el payload y asierta sobre html/text/subject sin red"
      pattern: "stubGlobal"
---

<objective>
Crear la variante de mail para el alta manual: una confirmación de turno LIMPIA (servicio, fecha,
hora, negocio + link de cancelar) SIN el bloque de precio/seña/saldo del mail del booking público.

Purpose: BOOK-NOTIFY-01 (D-03) exige avisarle al cliente que su turno quedó confirmado, pero el alta
manual NO maneja seña — reusar `sendConfirmationEmail` verbatim mostraría "$0 de seña" y un total
confuso. Esta función es la base que el endpoint (Plan 02) dispara en `after()`.

Output: nuevo export `sendManualBookingConfirmation` en `lib/email.ts` + su test puro
(`test/manual-notify-email.test.ts`). Sin tocar el endpoint ni el form (eso es Plan 02).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/motor-reservas/ROADMAP.md
@.planning/workstreams/motor-reservas/phases/05-aviso-al-cliente-en-el-alta-manual/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sendManualBookingConfirmation en lib/email.ts + test puro</name>
  <files>lib/email.ts, test/manual-notify-email.test.ts</files>
  <read_first>
    - lib/email.ts — leer `sendConfirmationEmail` (L74, es la BASE a adaptar; NO reusar verbatim) y
      `sendClientCancelEmail` (L445, el más parecido en forma: mismo header/footer, cancel-link, sin precio).
      Reusar los helpers ya existentes del módulo: `resolveSender`, `fmtDate`, `renderEmailHeader`,
      `fmtPrice` (este NO se usa acá), `resendSend`, y `normalizeArWhatsApp` (import ya presente).
    - test/webhook-deposit.test.ts — patrón `vi.stubGlobal('fetch', vi.fn().mockResolvedValue(...))`
      para interceptar la red (Resend usa `fetch` crudo a api.resend.com) y capturar el payload.
    - test/auth-email-templates.test.ts — molde de test PURO de contenido de mail (sin red, sin Supabase).
  </read_first>
  <behavior>
    Test 1 (envío): con `resendApiKey` propia + `resendFrom`, `sendManualBookingConfirmation` hace UN
      POST a https://api.resend.com/emails; el fetch stubbeado se llama exactamente 1 vez.
    Test 2 (contenido presente): el html capturado incluye el nombre del servicio, la fecha formateada
      por `fmtDate`, la hora (HH:mm), y el nombre del negocio.
    Test 3 (cancel-link con token): pasando `cancelToken`, el html incluye la URL
      `${baseUrl}/cancelar/${cancelToken}` (mismo mecanismo que el mail público).
    Test 4 (degradación D-04): SIN `cancelToken`, el fetch igual se llama 1 vez (el mail se manda) y el
      html NO incluye `/cancelar/` (no hay botón de cancelar).
    Test 5 (SIN precio/seña — el corazón de D-03): sobre el `html` Y el `text` capturados, el runtime
      assertion verifica que NO aparecen los conceptos de importe: ni un signo de importe, ni las
      palabras de seña/saldo/total. (Assertions en memoria sobre el string renderizado, NO greps de archivo.)
  </behavior>
  <action>
    Agregar a lib/email.ts un nuevo export `sendManualBookingConfirmation` modelado sobre
    `sendConfirmationEmail` PERO sin nada de precio/seña (D-03). Firma por objeto desestructurado
    (convención del módulo): `to`, `clientName`, `service`, `date`, `time`, `businessName`,
    `businessSlug`, `primaryColor?`, `logoUrl?`, `whatsapp?`, `cancelToken?`, `resendApiKey?`,
    `resendFrom?`. NO recibir `price` ni `deposit` — no existen en el alta manual.

    Reusar tal cual los helpers del módulo: `resolveSender(businessName, resendApiKey, resendFrom)` para
    key+from (esto ya sanitiza el display name → cubre header-injection, ver threat model), `fmtDate` para
    la fecha, `renderEmailHeader(businessName, logoUrl)` para el header logo+nombre, `normalizeArWhatsApp`
    para el link de WhatsApp, y `resendSend` para el POST. Construir el html reusando la MISMA estructura
    de tabla/branding que `sendConfirmationEmail` (accent = `primaryColor` con fallback '#d94a2b';
    baseUrl = `process.env.NEXT_PUBLIC_APP_URL || 'https://gestion.forjo.studio'`; footer
    "Enviado por Forjo Gestión · forjo.studio/${businessSlug}"), PERO con el bloque "Detalle del turno"
    conteniendo SOLO Servicio / Fecha / Hora — eliminar por completo las filas de Total, Seña abonada y
    Saldo en el local. El botón "Cancelar turno" se renderiza SOLO si hay `cancelToken` (link
    `${baseUrl}/cancelar/${cancelToken}`); sin token, se omite el botón (degradación D-04). El link de
    WhatsApp se renderiza solo si `normalizeArWhatsApp(whatsapp)` da un número usable. Subject y `text`
    (fallback plano) también SIN importes: p. ej. subject tipo "Turno confirmado — {negocio} · {fecha}
    {hora} hs" y text con servicio/fecha/hora y, si hay token, el link de cancelar. Cerrar con un
    `console.log` de confirmación al estilo de las otras funciones del módulo.

    Crear test/manual-notify-email.test.ts como test PURO (environment node, sin Supabase): en cada caso,
    stubbear `global.fetch` con `vi.stubGlobal` devolviendo `{ ok: true, json: async () => ({ id: 'x' }) }`,
    llamar `sendManualBookingConfirmation` con `resendApiKey` y `resendFrom` de prueba (para que
    `resolveSender` tome el path de key propia y NO dependa de `process.env.RESEND_API_KEY`), capturar el
    body del fetch (`JSON.parse(fetchMock.mock.calls[0][1].body)`) y asertar `payload.html` / `payload.text`
    / `payload.subject` según los 5 casos del bloque behavior. Restaurar el fetch en `afterEach`
    (`vi.unstubAllGlobals`). Las verificaciones de "sin precio/seña" son assertions en memoria sobre el
    string renderizado (`expect(payload.html).not.toContain(...)`), NUNCA greps sobre lib/email.ts (ese
    archivo contiene esas palabras legítimamente en OTRAS funciones).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx vitest run test/manual-notify-email.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/email.ts` exporta `sendManualBookingConfirmation` (grep del identificador de la firma, no del contenido del mail).
    - `npx vitest run test/manual-notify-email.test.ts` pasa con los 5 casos verdes.
    - El html renderizado en el test NO contiene precio/seña/saldo (assertion en memoria sobre el payload capturado).
    - El html renderizado incluye servicio, fecha (fmtDate), hora y nombre del negocio; con token incluye `/cancelar/${token}`, sin token no.
    - `npx tsc --noEmit` sin errores nuevos.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| params de la función → header/HTML del mail | `businessName` viaja al header `From` y al cuerpo; `to` es el destinatario |
| función → Resend (api.resend.com) | POST server-only con la key del tenant (resuelta por el caller en Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering (header injection) | `From` display name = `businessName` | mitigate | Se reusa `resolveSender` → `fromDisplayName` ya sanitiza `\r\n"<>` y trunca a 64 chars; no se arma el header a mano |
| T-05-05 | Information Disclosure | cuerpo del mail | mitigate | La función solo renderiza los params que recibe (datos del propio turno/negocio); no lee DB ni otros tenants. El caller (Plan 02) acota la fuente |
| T-05-SC | Tampering (supply chain) | npm installs | accept | Esta fase NO agrega dependencias: `sendManualBookingConfirmation` reusa `fetch` crudo y helpers ya en el repo (resend por POST directo). Sin superficie de install |
</threat_model>

<verification>
- `npx vitest run test/manual-notify-email.test.ts` verde (5 casos).
- `npx tsc --noEmit` sin errores.
- Inspección: el bloque "Detalle del turno" del nuevo template NO tiene filas de Total/Seña/Saldo.
</verification>

<success_criteria>
- Existe `sendManualBookingConfirmation` con confirmación limpia (servicio/fecha/hora/negocio + cancel-link opcional), sin precio/seña.
- Test puro cubre presencia de datos, cancel-link con/sin token, y ausencia de importes.
- Cero cambios en el endpoint o el form (eso es Plan 02).
</success_criteria>

## Artifacts this phase produces (Plan 01)

- `lib/email.ts` → `sendManualBookingConfirmation` (nuevo export, variante sin precio/seña).
- `test/manual-notify-email.test.ts` → test puro del contenido del mail.

<output>
Create `.planning/workstreams/motor-reservas/phases/05-aviso-al-cliente-en-el-alta-manual/05-01-SUMMARY.md` when done
</output>
