---
phase: 03-import-de-clientes-csv
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - app/(dashboard)/clients/clients-client.tsx
autonomous: true
requirements: [DATA-03]
user_setup: []

must_haves:
  truths:
    - "El header de /clients muestra 'Importar CSV' (outline) junto a 'Exportar CSV' en una fila 2-col, y 'Nuevo cliente' (primary) pasa a fila propia full-width"
    - "El botón abre un Dialog ancho (sm:max-w-2xl) que avanza por 4 etapas: upload → preview → confirmando → resumen"
    - "La preview muestra filas ok/error/duplicada distinguidas por fill+border+icono+texto (nunca solo por color) + contadores (válidas/con error/duplicadas)"
    - "Confirmar re-postea el MISMO File a /confirm con anti-doble-submit (botón disabled + 'Importando...'); durante la escritura no se puede cerrar el Dialog"
    - "El resumen muestra 3 conteos (importados/omitidos/fallidos); al cerrar, los importados aparecen en la lista con badge 'Importado'"
  artifacts:
    - path: "app/(dashboard)/clients/clients-client.tsx"
      provides: "Botón 'Importar CSV' + Dialog de import de 4 etapas (upload/preview/confirming/resumen)"
      contains: "Importar CSV"
  key_links:
    - from: "app/(dashboard)/clients/clients-client.tsx"
      to: "app/api/import/clients/preview/route.ts"
      via: "fetch POST con FormData al endpoint de preview (etapa upload → preview)"
      pattern: "api/import/clients/preview"
    - from: "app/(dashboard)/clients/clients-client.tsx"
      to: "app/api/import/clients/confirm/route.ts"
      via: "fetch POST re-posteando el mismo File al endpoint de confirm (etapa confirmando)"
      pattern: "api/import/clients/confirm"
---

<objective>
Agregar a la pantalla `/clients` el botón "Importar CSV" y el Dialog de import de 4 etapas (upload → preview → confirmar → resumen) según el UI-SPEC aprobado (Opción A: un solo Dialog ancho `sm:max-w-2xl` que swapea contenido). Reuso puro del design system ya shipeado (Bauhaus + shadcn base-nova): cero componentes nuevos, cero deps de UI, cero rutas nuevas. Cierra el round-trip visible del milestone (exportás → reimportás) con SC-1 (preview no escribe), anti-doble-submit en la escritura, y SC-4 (resumen + badge "Importado" en la lista).

Purpose: Es la cara del flujo — donde el dueño ve qué se va a importar ANTES de escribir (SC-1) y el resumen DESPUÉS (SC-4). La fidelidad visual y la consistencia con los Dialogs hermanos (alta/merge) es load-bearing. Todo lo delicado (aislamiento, re-parseo) ya vive en el server (Plan 02); esta UI solo orquesta el flujo y re-postea el File.
Output: `app/(dashboard)/clients/clients-client.tsx` modificado (botón + Dialog).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gestion-rebrand/phases/03-import-de-clientes-csv/03-UI-SPEC.md
@.planning/workstreams/gestion-rebrand/phases/03-import-de-clientes-csv/03-PATTERNS.md
@.planning/workstreams/gestion-rebrand/phases/03-import-de-clientes-csv/03-02-SUMMARY.md

# Analog a modificar (leer antes de escribir — es su propio analog)
@app/(dashboard)/clients/clients-client.tsx
@components/ui/table.tsx
</context>

<artifacts_this_phase_produces>
**Este plan (03)** entrega la UI del import: el botón "Importar CSV" y el Dialog de 4 etapas en `clients-client.tsx`. Consume los endpoints `preview`/`confirm` del Plan 02 (shapes en su `<interface_context>`) y el badge "Importado" (`Badge variant="secondary"`) ya definido en la Fase 2. Cierra DATA-03 end-to-end.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Header de /clients — botón "Importar CSV" (grid 2-col) + "Nuevo cliente" full-width</name>
  <files>app/(dashboard)/clients/clients-client.tsx</files>
  <read_first>
    - app/(dashboard)/clients/clients-client.tsx L500-528 — el header actual (fila 1 título+merge; fila 2 grid 2-col Exportar+Nuevo). L14 (buttonVariants), L18 (Badge), L22-26 (imports de lucide).
    - 03-UI-SPEC.md §"Header placement" (layout RESUELTO: fila 2-col Exportar+Importar ambos outline; Nuevo cliente en fila propia full-width) + §"Button spec for 'Importar CSV'" (variant outline, icono Upload, sin gap manual) + §"Behavior-Frozen Guardrails".
    - 03-PATTERNS.md §"app/(dashboard)/clients/clients-client.tsx (MODIFY)" §Header.
  </read_first>
  <action>
    Reestructurar la fila 2 del header (L513-528) según el UI-SPEC (layout resuelto con el usuario):
    - Fila secundaria `grid grid-cols-2 gap-2`: "Exportar CSV" (el `<a>` outline existente L517-524, se mantiene) + "Importar CSV" NUEVO (`Button variant="outline"` con icono lucide `Upload` `w-4 h-4` + label "Importar CSV", `w-full`, onClick → `setImportOpen(true)`). Los dos actions de round-trip CSV emparejados, ambos peso secundario.
    - Fila primaria full-width: "Nuevo cliente" (el Button primary existente L525-527) pasa a su propia fila `w-full`, FUERA del grid 2-col, para que el CTA primario quede dominante y sin apretar. El orden exacto de las filas (primaria arriba o abajo del par CSV) es discreción, pero el primary NUNCA en el mismo grid 2-col que los dos secundarios (evita 3 celdas apretadas a 320px).

    Guardrails duros (UI-SPEC): NO poner `gap-*` manual en el `Button` de "Importar CSV" (el componente ya trae su gap icono-label); NO copiar el `gap-1.5` legacy del `<a>` de export al botón nuevo (solo múltiplos de 4). Importar `Upload` de lucide-react (agregar al import block L22-26). Agregar el estado `const [importOpen, setImportOpen] = useState(false)` junto al resto de estados del componente. Área de toque ≥44px en mobile (Button default + w-full lo cubren). NO tocar la fila 1 (título + merge) ni el layout de dos paneles.
  </action>
  <verify>
    <automated>npx eslint app/(dashboard)/clients/clients-client.tsx --max-warnings=9999 2>&1 | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{const nuevos=(d.match(/Importar CSV|setImportOpen/g)||[]);if(/Parsing error|is not defined.*Upload|'Upload' is not defined/.test(d))throw new Error('Upload sin importar o parse error');console.log('eslint corrió (comparar conteo baseline vs post en el SUMMARY)')})"</automated>
  </verify>
  <done>El header tiene "Importar CSV" (outline, icono Upload, sin gap manual) en el grid 2-col junto a Exportar; "Nuevo cliente" quedó en fila propia full-width; existe el estado importOpen; Upload importado; tsc verde; conteo eslint sin errores nuevos (baseline documentado).</done>
</task>

<task type="auto">
  <name>Task 2: Dialog de import — 4 etapas (upload → preview → confirmando → resumen)</name>
  <files>app/(dashboard)/clients/clients-client.tsx</files>
  <read_first>
    - app/(dashboard)/clients/clients-client.tsx L939-983 (Dialog del alta — molde de open/onOpenChange + reset al cerrar + DialogContent), L192-224 (onCreateClient — patrón fetch/setCreating/res.json().catch/toast/finally + anti-doble-submit), L260-266 (dedup, NO se toca), L19 (imports Dialog family), L18 (Badge).
    - components/ui/table.tsx — Table, TableHeader, TableBody, TableRow, TableHead, TableCell (reuso para la preview desktop).
    - 03-UI-SPEC.md §"Interaction Contracts" completo (shell del Dialog + las 4 etapas), §"Preview error-row treatment" (fill+border+icono+texto, nunca hue solo), §"Counters"/"Resumen tiles" color mapping, §"Copywriting Contract" (todos los textos en español), §"Behavior-Frozen Guardrails".
    - 03-02-SUMMARY.md — los shapes reales de /preview y /confirm (interface_context del Plan 02) por si el executor ajustó nombres.
  </read_first>
  <action>
    Agregar el Dialog de import en `clients-client.tsx`, reusando patrones ya presentes en el archivo (es su propio analog). Cero componentes/deps nuevos.

    ESTADO local del flujo: `stage: 'upload' | 'preview' | 'confirming' | 'resumen'` (default 'upload'), el `File` seleccionado en React state (para re-postear a confirm sin re-seleccionar — stateless re-upload), el objeto `preview` devuelto por el endpoint, y el `resumen` devuelto por confirm. Reset completo al cerrar (stage→'upload', File→null, preview/resumen→null) — molde del reset del alta modal.

    SHELL del Dialog: `Dialog open={importOpen} onOpenChange={...}` con `DialogContent className="sm:max-w-2xl"` (más ancho que el `sm:max-w-md` del alta, para la tabla). `DialogTitle` = `Importar ${term.clients.toLowerCase()}` (respeta terminología vertical — "Importar pacientes" en salud). Deshabilitar close-on-outside/Escape SOLO durante `confirming` (write en vuelo); re-habilitar cuando la respuesta llega.

    ETAPA 1 — Upload: dropzone `border-2 border-dashed border-border rounded-lg` con padding generoso (`py-6`/`px-4`, múltiplos de 4), icono `Upload` (`w-8 h-8 text-muted-foreground`), instrucción de una línea, y un `Button variant="outline"` "Elegir archivo" que dispara un `<input type="file" accept=".csv,text/csv">` visualmente oculto (native, sin componente nuevo). Al elegir archivo: validación client-side de extensión `.csv` + tamaño ≤2MB para feedback instantáneo (el server re-valida — D-03/D-04); si inválido → mensaje inline `text-sm text-destructive` "Subí un archivo .csv de hasta 2 MB." y NO avanzar. Estado "archivo elegido": mostrar nombre+tamaño + un `X` icon-only para quitarlo con `aria-label="Quitar archivo"` + `focus-visible` ring. Acción "Continuar" (deshabilitada hasta que haya un `.csv` válido) → POST `multipart/form-data` (`const fd = new FormData(); fd.append('file', file)`; `fetch('/api/import/clients/preview', { method:'POST', body: fd })` SIN header Content-Type — el browser lo setea con boundary); loading = `Loader2` spinner + disabled. En `ok` → guardar el preview y `setStage('preview')`. Errores: header inválido → "El CSV no tiene el formato esperado. Exportá tus clientes desde Forjo y usá ese archivo como base."; network → `toast.error('No se pudo procesar el archivo. Intentá de nuevo.')` y quedarse en etapa 1.

    ETAPA 2 — Preview (SC-1, NADA se escribe): línea intro "Revisá antes de importar. Todavía no se guardó nada." + contadores (`Badge`): "N válidas" (outline/neutral), "N con error" (`variant="destructive"`), "N duplicadas" (`variant="secondary"`) desde el preview response. Desktop (`sm:` up): `Table` (reuso de components/ui/table.tsx) con columnas Nombre·Teléfono·Email·Estado; body scrollable `max-h-[50vh] overflow-y-auto` (para un CSV de 2000 filas). Tratamiento de filas: válida = default; error = `bg-destructive/10` + `border-l-2 border-destructive` + icono `AlertCircle` + mensaje `text-xs text-destructive` en la celda Estado; duplicada = `text-muted-foreground` + `Badge variant="secondary"` "Duplicado". NUNCA distinguir por color solo — siempre fill+border+icono+texto. Mobile (`< sm:`): filas stacked `divide-y divide-border` (NO tabla), mismo tratamiento con `border-l-2`. Mensajes de error por fila en español según el copywriting (nombre faltante → "Falta el nombre."; sin contacto → "Falta un contacto (teléfono o email)."; teléfono inválido → "El teléfono no es válido."). Empty state (importables===0): bloque `AlertCircle` + "No hay filas para importar." + cuerpo explicativo + "Elegir otro archivo" (vuelve a etapa 1), y "Confirmar import" deshabilitado. Acción "Cambiar archivo" (ghost/outline) → etapa 1. CTA "Confirmar import" (`Button` default = `bg-primary`, el ÚNICO accent del flujo) habilitado solo con importables>0.

    ETAPA 3 — Confirmando (write en vuelo): al click "Confirmar import" → `setStage('confirming')`, botón disabled + label "Importando..." + `Loader2 animate-spin w-4 h-4`. Anti-doble-submit obligatorio (molde del `creating`/`saving` del alta). Re-POST del MISMO `File` (retenido en state) por multipart a `/api/import/clients/confirm`. Dialog lock: close-on-outside/Escape deshabilitados durante confirming. En `ok` con `resumen` → `setStage('resumen')`. Error network/server → `toast.error('No se pudo completar la importación. Intentá de nuevo.')`, volver a etapa 2 con el botón re-habilitado, re-habilitar el cierre, SIN perder el file/preview.

    ETAPA 4 — Resumen (SC-4): 3 tiles side-by-side (`grid grid-cols-3 gap-2` en sm, stacked en mobile), cada uno número grande `text-2xl font-bold` + label `text-xs`: "Importados" (`text-foreground`), "Omitidos" (`text-muted-foreground`), "Fallidos" (`text-destructive` solo si >0, si no `text-muted-foreground`). Headline "Importación completada." Helper opcional si hubo omitidos/fallidos: "Las filas con error no se importaron; las duplicadas se omitieron." Acción "Cerrar" (preferir `outline`/neutral — el flujo terminó, mantener el accent reservado). Al cerrar: reset del flujo + REFLEJAR los importados en la lista — preferir re-fetch de clientes (via el `createClient()` browser ya usado en el archivo) sobre state surgery, dado el tamaño del batch; los importados aparecen con badge "Importado" (`Badge variant="secondary"`, ya mapeado en la Fase 2 — NO re-mapear).

    Guardrails (UI-SPEC "Behavior-Frozen"): reuso de Dialog/Button/Table/Badge/Card existentes; sin variantes nuevas; `--primary` SOLO en "Confirmar import"; sin `gap-*` manual en Buttons; solo múltiplos de 4 (nada de gap-1.5/p-1.5). Importar los iconos lucide que falten (`Upload`, `FileText`, `AlertCircle`, `CheckCircle2`, `Loader2` — `X` ya está L25) y la Table family + DialogFooter si no están importados. La dedup L260-275 NO se toca.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>El Dialog avanza por las 4 etapas con el estado stage+File+preview+resumen; upload valida client-side y POSTea a /preview; preview marca filas ok/error/duplicada por fill+border+icono+texto con contadores; confirmando re-postea el mismo File con anti-doble-submit y bloqueo de cierre; resumen muestra 3 conteos y al cerrar re-fetchea la lista (importados con badge "Importado"); copy en español; sin componentes/deps nuevos; tsc verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuario (dueño autenticado) → UI del Dialog | El dueño elige el archivo; la UI valida client-side solo por UX. |
| UI → route handlers (preview/confirm) | La UI postea el File; el server es la fuente de verdad (re-valida). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | Preview client-side como fuente de verdad | mitigate | La preview es pura UX (SC-1); el confirm re-postea el File CRUDO y el server re-parsea+re-valida (D-03, Plan 02). La UI nunca manda "las filas que vio". La validación client-side de extensión/tamaño es solo feedback rápido; el server re-valida autoritativamente. |
| T-03-12 | Denial of Service | Doble submit del confirm (insert duplicado) | mitigate | Anti-doble-submit: botón disabled + label "Importando..." + Dialog lock (sin close-on-outside/Escape) durante `confirming`. Molde del `creating`/`saving` del alta. El write no es idempotente → el disable es obligatorio. |
| T-03-13 | Information Disclosure | Render de valores importados en el panel | mitigate | React escapa el render por default; los valores (incl. nombres con `=`/fórmula des-escapados) se muestran como texto, NO como fórmula. La defensa anti-fórmula se re-aplica en el export (esc de la Fase 2). El badge de origen usa el mapeo existente ("Importado" = secondary). |

**Residuales:** ninguno high. La UI no introduce superficie de escritura propia — todo write pasa por el confirm handler (Plan 02) con su aislamiento. El trade-off del re-upload es imperceptible (el File se retiene en state, el usuario no re-selecciona).
</threat_model>

<verification>
- `npx tsc --noEmit` verde.
- `npx eslint` sobre clients-client.tsx: conteo de problemas SIN aumento vs baseline (el archivo ya tiene errores pre-existentes de set-state-in-effect documentados; NO introducir nuevos) → registrar baseline vs post en el SUMMARY.
- Grep: existe `Importar CSV`, `setImportOpen`, `api/import/clients/preview`, `api/import/clients/confirm`, `aria-label="Quitar archivo"`; el `<input type="file">` tiene `accept=".csv`; el badge de importados usa `variant="secondary"`.
- Guardrails: `--primary`/`bg-primary` solo en "Confirmar import"; sin `gap-1.5`/`p-1.5`/`gap-*` manual en Buttons nuevos; la dedup L260-275 intacta.
- Human-check (visual, `human_verify_mode=end-of-phase`): correr el flujo real en `/clients` — exportar clientes, reimportar el mismo archivo, ver preview con filas marcadas, confirmar, ver resumen y los importados con badge "Importado".
</verification>

<success_criteria>
- El botón "Importar CSV" abre el Dialog de 4 etapas; el header respeta el layout resuelto (par CSV 2-col + Nuevo cliente full-width).
- La preview no escribe y marca ok/error/duplicada de forma accesible (WCAG AA, no hue-solo) con contadores.
- Confirmar re-postea el File con anti-doble-submit y bloqueo de cierre; el resumen muestra los 3 conteos.
- Los importados aparecen en la lista con badge "Importado"; cero componentes/deps de UI nuevos; sin regresión eslint.
</success_criteria>

<output>
Create `.planning/workstreams/gestion-rebrand/phases/03-import-de-clientes-csv/03-03-SUMMARY.md` when done
</output>
