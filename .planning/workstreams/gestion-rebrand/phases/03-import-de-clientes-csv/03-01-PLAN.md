---
phase: 03-import-de-clientes-csv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/clients-create.ts
  - lib/clients-import.ts
  - test/clients-import.test.ts
autonomous: true
requirements: [DATA-03]
user_setup: []

must_haves:
  truths:
    - "papaparse@5.5.4 + @types/papaparse@5.5.2 quedan instaladas y pineadas (package.json + lockfile commiteados)"
    - "Un cliente exportado con nombre '=X' (que el export prefija a \"'=X\") vuelve a ser '=X' al importarlo — round-trip lossless (unescapeFormulaGuard)"
    - "Una fila sin nombre o sin ningún contacto se clasifica como error (missing_fields); un teléfono con letras como invalid_phone"
    - "Filas duplicadas (email minúsc / teléfono solo-dígitos) contra los existentes del negocio Y dentro del propio CSV se marcan como duplicadas, no como importables"
    - "buildClientInsert acepta origin='importado' sin romper el alta manual (default sigue 'manual')"
  artifacts:
    - path: "lib/clients-import.ts"
      provides: "Lógica pura: parseCsv, unescapeFormulaGuard, classifyRows (parse+validate+dedup), sin imports de React/Next"
      contains: "unescapeFormulaGuard"
      min_lines: 60
    - path: "lib/clients-create.ts"
      provides: "buildClientInsert con parámetro origin retro-compatible"
      contains: "origin: 'manual' | 'importado'"
    - path: "test/clients-import.test.ts"
      provides: "Tests de round-trip lossless, validación por fila, dedup, origin='importado'"
      contains: "unescapeFormulaGuard"
    - path: "package.json"
      provides: "papaparse pineada como dependency"
      contains: "papaparse"
  key_links:
    - from: "lib/clients-import.ts"
      to: "lib/clients-create.ts"
      via: "importa validateClientBody / isValidPhone / buildClientInsert de @/lib/clients-create"
      pattern: "from '@/lib/clients-create'"
    - from: "lib/clients-import.ts"
      to: "papaparse"
      via: "import Papa from 'papaparse' para el parseo RFC4180"
      pattern: "import Papa from 'papaparse'"
    - from: "test/clients-import.test.ts"
      to: "lib/clients-import.ts"
      via: "importa la lógica pura para aserciones sin server vivo"
      pattern: "from '@/lib/clients-import'"
---

<objective>
Instalar `papaparse` (D-05, primera dep del milestone) y construir la LÓGICA PURA del import CSV — parseo RFC4180, des-escapado del prefijo anti-fórmula (round-trip lossless), validación por fila reusando el alta manual, y deduplicación (vs DB + intra-CSV). Parametrizar `buildClientInsert` para aceptar `origin='importado'` sin romper el alta manual de la Fase 2. Todo respaldado por tests que corren sin server (misma fuente de verdad que los route handlers de la Wave 2).

Purpose: Esta capa pura es la fundación testeable que los dos route handlers (preview/confirm) importan. Aislar el parseo+clasificación acá evita el bug más peligroso de la fase (que preview diga "válida" y confirm rechace) y permite el test estrella de round-trip sin levantar el server (per D-03).
Output: papaparse instalada+pineada, `lib/clients-import.ts` (nuevo), `lib/clients-create.ts` (parametrizado), `test/clients-import.test.ts` (nuevo).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gestion-rebrand/phases/03-import-de-clientes-csv/03-RESEARCH.md
@.planning/workstreams/gestion-rebrand/phases/03-import-de-clientes-csv/03-PATTERNS.md
@.planning/workstreams/gestion-rebrand/phases/03-import-de-clientes-csv/03-CONTEXT.md

# Analogs a imitar (leer antes de escribir)
@lib/clients-create.ts
@app/api/export/clients/route.ts
@test/manual-client.test.ts
</context>

<artifacts_this_phase_produces>
Esta fase (a lo largo de sus 3 planes) produce: la dep `papaparse`, `lib/clients-import.ts` (lógica pura), los 2 route handlers (`app/api/import/clients/{preview,confirm}/route.ts`), el Dialog de import en `clients-client.tsx`, el helper `unescapeFormulaGuard`, y el parámetro `origin` en `buildClientInsert`. **Este plan (01)** entrega: la dep + `lib/clients-import.ts` + el parámetro `origin` + el test de la lógica pura.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Instalar papaparse (D-05) — primera dependencia del milestone</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - 03-RESEARCH.md §Standard Stack / §Installation / §Package Legitimacy Audit — versiones pineadas y rationale.
    - 03-CONTEXT.md D-05 — dep aprobada por el usuario en discuss-phase (NO requiere checkpoint humano nuevo).
  </read_first>
  <action>
    Instalar la dependencia de parseo CSV aprobada en D-05, pineada exactamente a las versiones verificadas contra el registro npm (papaparse 5.5.4 como dependency, los tipos 5.5.2 como devDependency). Correr, en PowerShell, `npm install papaparse@5.5.4` seguido de `npm install -D @types/papaparse@5.5.2`. NO instalar ninguna otra dep (nada de xlsx ni parsers alternativos — D-05 lockeó solo papaparse). Verificar que `package.json` liste `papaparse` con la versión exacta (sin `^` que permita saltos de major) y que `package-lock.json` haya cambiado. Commitear AMBOS lockfiles (a diferencia de las Fases 1/2 que fueron cero-deps). NO agregar checkpoint humano: el approval de D-05 + la legitimidad confirmada (mholt/PapaParse, ~11.5M dl/sem, sin postinstall) ya satisfacen el gate — el flag `SUS` del seam es un falso positivo por `too-new` documentado en el RESEARCH.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies?.papaparse!=='5.5.4') throw new Error('papaparse no pineada a 5.5.4: '+p.dependencies?.papaparse); if(p.devDependencies?.['@types/papaparse']!=='5.5.2') throw new Error('@types/papaparse no pineada a 5.5.2'); console.log('OK papaparse pineada')"</automated>
  </verify>
  <done>package.json lista papaparse@5.5.4 (dependency, sin caret) y @types/papaparse@5.5.2 (devDependency); package-lock.json actualizado; ninguna otra dep agregada.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parametrizar origin en buildClientInsert (retro-compatible)</name>
  <files>lib/clients-create.ts</files>
  <read_first>
    - lib/clients-create.ts L42-58 — hoy hardcodea origin:'manual' en el return (L56).
    - 03-RESEARCH.md §Pattern 5 "Ajuste requerido a buildClientInsert" + §Pitfall 5.
    - 03-PATTERNS.md §"lib/clients-create.ts (MODIFY)" — el cambio quirúrgico exacto.
  </read_first>
  <behavior>
    - buildClientInsert(business, input) sin tercer argumento → el objeto de insert lleva origin='manual' (comportamiento actual, alta manual intacta).
    - buildClientInsert(business, input, 'importado') → el objeto de insert lleva origin='importado'.
    - El CHECK de migr.049 (reserva|manual|importado) acepta ambos → sin migración nueva.
    - El gate de insurance_* por vertical (salud) sigue igual — el nuevo parámetro no lo toca.
  </behavior>
  <action>
    Cambio quirúrgico y retro-compatible: agregar a `buildClientInsert` un tercer parámetro `origin: 'manual' | 'importado' = 'manual'` con default `'manual'`, y en el objeto de retorno reemplazar la clave hardcodeada `origin: 'manual'` por `origin` (shorthand que usa el parámetro). NO tocar nada más de la función (el gate de `insurance_*` por vertical, el business_id de sesión, etc. quedan idénticos). El default preserva `app/api/clients/create/route.ts` (que llama sin el tercer arg) — cero regresión en el alta manual. Actualizar el comentario de cabecera de la función si menciona "origin='manual' fijo" para reflejar que ahora es parametrizable con default 'manual'. NO cambiar la firma de `validateClientBody` ni `isValidPhone`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>buildClientInsert acepta un tercer parámetro origin con default 'manual'; llamado sin él produce origin='manual'; con 'importado' produce origin='importado'; tsc verde; clients/create/route.ts no se modifica.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lib/clients-import.ts (lógica pura) + test/clients-import.test.ts</name>
  <files>lib/clients-import.ts, test/clients-import.test.ts</files>
  <read_first>
    - 03-RESEARCH.md §Pattern 2 (parseo papaparse: header:true, skipEmptyLines:'greedy', transformHeader trim+lowercase, validar meta.fields contra el header rígido), §Pattern 3 (unescapeFormulaGuard — el caso estrella), §Pattern 6 (loop completo de validación + dedup), §Pitfall 1 (BOM), §Pitfall 3 (acumulación del prefijo).
    - 03-PATTERNS.md §"lib/clients-import.ts" (patrón de módulo, unescapeFormulaGuard, dedup) + §"test/clients-import.test.ts" (molde 1:1 de manual-client.test.ts + los 5 casos propios).
    - app/api/export/clients/route.ts L58-64,85 — el esc() y el header que este módulo revierte (contrato de round-trip).
    - lib/clients-create.ts — validateClientBody / isValidPhone / buildClientInsert a reusar.
    - test/manual-client.test.ts — molde EXACTO del test (skipIf, seedOneTenant, ownerAnon anon-key autenticado, guard anti-falso-verde, afterEach).
  </read_first>
  <behavior>
    - unescapeFormulaGuard("'=X") → "=X"; unescapeFormulaGuard("=X") → "=X"; unescapeFormulaGuard("O'Brien") → "O'Brien" (el ' no precede a un char de fórmula, NO se quita); unescapeFormulaGuard("'hola") → "'hola" (el char siguiente al ' no es =+-@\t\r).
    - Round-trip estrella: valor original "=Ana" → esc() del export lo emite como "'=Ana" → unescapeFormulaGuard lo devuelve a "=Ana" (lossless). El valor almacenado es "=Ana", NO "'=Ana".
    - classifyRows sobre un parseo con: una fila válida (nombre+contacto), una sin nombre (→ errores, missing_fields), una sin contacto (→ errores, missing_fields), un teléfono con letras (→ errores, invalid_phone), una duplicada intra-CSV (email repetido en minúsc → duplicadas), una duplicada vs existentes del negocio (→ duplicadas) → devuelve conteos correctos { importables, errores, duplicadas, total } y los índices de fila en errores usan row = índice + 2 (header = fila 1).
    - parseCsv de un CSV con BOM inicial (U+FEFF) y header rígido → la primera columna es "nombre" (NO "nombre" con el BOM pegado adelante); un CSV con header equivocado → señalado como header inválido (no se parsean filas).
    - Notas > 1000 chars → truncadas a 1000 (consistente con el alta manual).
    - Insert con buildClientInsert(business, input, 'importado') → origin='importado' en la fila persistida (via ownerAnon, RLS real).
  </behavior>
  <action>
    Crear `lib/clients-import.ts` como lógica PURA framework-agnostic (cero imports de React/Next; sí `import Papa from 'papaparse'` y los helpers de `@/lib/clients-create`), imitando el patrón de módulo de `lib/clients-create.ts`: cabecera en español con las invariantes de seguridad (business_id nunca del CSV, origin='importado' forzado, el des-escapado es seguro porque la defensa anti-fórmula vive en el export/render), `interface` PascalCase para la fila cruda, named exports de funciones puras.

    Exponer:
    - `unescapeFormulaGuard(v)` — quita UN `'` líder si y solo si el siguiente carácter matchea el patrón que el export prefija. El conjunto de caracteres de fórmula a detectar es EXACTAMENTE el mismo que usa el `esc()` del export en `export/clients/route.ts` L59 (los caracteres de inicio de fórmula de Excel/Sheets más tab y CR) — reusar ese mismo regex, invertido para detectar el prefijo en vez de agregarlo. NO reescribir el conjunto de caracteres a ojo; copiarlo del `esc()` para que import y export queden acoplados.
    - `parseCsv(text)` — strip defensivo del BOM al inicio (secuencia de escape U+FEFF, NUNCA el glifo pegado — misma convención que el export), luego `Papa.parse` con `header:true`, `skipEmptyLines:'greedy'`, `transformHeader` que hace trim+lowercase. Validar `meta.fields` contra el header rígido canónico `nombre,telefono,email,origen,notas,obra_social,nro_obra_social`; si faltan columnas obligatorias del header, señalar header inválido (columnas extra se ignoran — D-01). Devolver las filas crudas + un flag/estructura que indique header válido o no.
    - `classifyRows({ rows, existing, business, origin })` — por cada fila: des-escapar (unescapeFormulaGuard) nombre/teléfono/email/notas, trim, `validateClientBody` (reusado); si inválida → a `errores` con `{ row: índice+2, error }`; si no, dedup por email-minúsculas y teléfono-solo-dígitos (`replace(/\D/g,'')`) contra el índice de existentes Y contra los Sets intra-CSV (`seen*`) — la MISMA lógica de dedup que `clients-client.tsx` L260-266; si duplicada → contar en `duplicadas` (omitir); si única → construir la fila importable (notas truncadas a 1000 como el alta manual). Devolver `{ importables, errores, duplicadas, total }`. NO hacer el insert acá (lo hace el confirm handler en la Wave 2); esta función devuelve las filas importables ya normalizadas.

    Crear `test/clients-import.test.ts` copiando el molde de `test/manual-client.test.ts` 1:1: `describe.skipIf(!hasSupabaseCreds)`, `seedOneTenant`/`teardownOneTenant`, `ownerAnon` = cliente anon-key AUTENTICADO como el dueño (NUNCA service-role para las aserciones de insert), el GUARD anti-falso-verde (sesión anon + anonKey !== SERVICE_ROLE_KEY), `afterEach` que limpia `clients` del tenant y restaura el vertical. Los tests de lógica PURA (unescapeFormulaGuard, classifyRows, parseCsv, round-trip) NO necesitan Supabase — deben poder correr sin las creds; solo el test de `origin='importado'` en el insert real usa `ownerAnon`. Cubrir los casos del bloque behavior, con el test de round-trip lossless (`=X` → export esc → import unescape → `=X`) como caso estrella explícito.

    IMPORTANTE (comment-text discipline): el test negativo-greps por literales de error como `missing_fields`/`invalid_phone` en aserciones (`.toBe('missing_fields')`), NO en comentarios que un gate pudiera invalidar. Mantené los literales solo dentro de `expect(...)`.
  </action>
  <verify>
    <automated>npx vitest run test/clients-import.test.ts</automated>
  </verify>
  <done>lib/clients-import.ts exporta parseCsv, unescapeFormulaGuard y classifyRows como lógica pura; test/clients-import.test.ts pasa (los casos de lógica pura corren sin Supabase, el de insert usa ownerAnon anon+RLS); el round-trip lossless `=X`→`=X` está verificado; tsc verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV (dato de origen no confiable) → lógica pura | Los valores del CSV (nombres/notas cargados en reservas públicas) cruzan hacia la clasificación. |
| lógica pura → test con Supabase anon+RLS | El único punto que escribe (test de origin) usa la sesión del dueño, no service-role. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | unescapeFormulaGuard (des-escapado del prefijo `'`) | mitigate | Quita UN solo `'` líder y SOLO si el siguiente char es un caracter de inicio de fórmula (el mismo conjunto que el `esc()` del export detecta, más tab y CR), exactamente el patrón que prefija el export. Un `'` legítimo (O'Brien) no se toca. El valor des-escapado es seguro: la defensa anti-fórmula se re-aplica en el export (esc) y React escapa el render. Almacenar el `'` corrompería el dato sin ganar seguridad. Test estrella lo verifica. |
| T-03-02 | Tampering | Header rígido del CSV | mitigate | parseCsv valida meta.fields contra el header canónico; header equivocado → señalado como inválido, NO se parsean filas (D-01). La columna `origen` del CSV se ignora (nunca llega al insert). |
| T-03-03 | Spoofing/EoP | Test de insert (falso-verde) | mitigate | El test usa ownerAnon (anon-key autenticado como el dueño) con el GUARD anti-falso-verde (anonKey !== SERVICE_ROLE_KEY); jamás service-role para aserciones de escritura. Molde manual-client.test.ts. |
| T-03-SC | Tampering | npm install papaparse | accept | D-05 aprobado por el usuario; legitimidad confirmada (mholt/PapaParse, ~11.5M dl/sem, sin postinstall). El flag `SUS` del seam es falso positivo por `too-new`. Pin exacto @5.5.4 evita saltos. Sin checkpoint bloqueante (approval previo). |

**Residuales:** ninguno high. El des-escapado (T-03-01) es intencional y seguro por diseño (defensa en la capa de salida). La dep (T-03-SC) es aceptada por approval previo + legitimidad.
</threat_model>

<verification>
- `npx tsc --noEmit` verde tras el cambio de buildClientInsert y el nuevo módulo.
- `npx vitest run test/clients-import.test.ts` pasa; el caso de round-trip lossless (`=X`) está presente y verde.
- `node -e` confirma papaparse@5.5.4 y @types/papaparse@5.5.2 pineadas.
- El alta manual NO se rompe: `app/api/clients/create/route.ts` sigue llamando `buildClientInsert` sin tercer arg y produce origin='manual' (cubierto por el default + tsc).
</verification>

<success_criteria>
- papaparse instalada, pineada y commiteada (lockfiles).
- lib/clients-import.ts entrega parseo RFC4180 (round-trip con el export), des-escapado lossless, validación reusada y dedup idéntica a la del panel.
- buildClientInsert parametrizado sin regresión en el alta manual.
- Tests verdes, con el round-trip estrella cubierto.
</success_criteria>

<output>
Create `.planning/workstreams/gestion-rebrand/phases/03-import-de-clientes-csv/03-01-SUMMARY.md` when done
</output>
