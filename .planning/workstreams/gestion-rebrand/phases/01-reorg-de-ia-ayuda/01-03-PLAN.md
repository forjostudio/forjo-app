---
phase: 01-reorg-de-ia-ayuda
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - app/(dashboard)/ayuda/page.tsx
  - components/dashboard/sidebar.tsx
  - app/(dashboard)/settings/settings-client.tsx
autonomous: true
requirements: [HELP-01]
must_haves:
  truths:
    - "Existe una ruta /ayuda que renderiza una FAQ estática (cómo usar Forjo Gestión), sin relación con el agente."
    - "La FAQ se arma desde un array TS local FAQ: { pregunta, respuesta }[] versionado en el repo — sin Supabase, sin MDX, sin dependencia nueva."
    - "Cada pregunta es un disclosure nativo <details>/<summary> (zero-install); múltiples pueden estar abiertos a la vez; el chevron rota al abrir y nunca usa color de acento."
    - "El sidebar tiene un link Ayuda (icono HelpCircle) en el footer que lleva a /ayuda."
    - "Configuración (/settings) tiene un segundo acceso a /ayuda (¿Necesitás ayuda? Ver la guía)."
    - "La página de ayuda no agrega ninguna dependencia npm (no shadcn Accordion, no @radix-ui/react-accordion)."
  artifacts:
    - path: "app/(dashboard)/ayuda/page.tsx"
      provides: "server component con array FAQ estático + disclosures <details>/<summary> + header PageEyebrow/h1"
      contains: "const FAQ"
      min_lines: 40
    - path: "components/dashboard/sidebar.tsx"
      provides: "link Ayuda (HelpCircle) → /ayuda en el footer"
      contains: "HelpCircle"
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "segundo acceso a /ayuda desde Configuración"
      contains: "/ayuda"
  key_links:
    - from: "components/dashboard/sidebar.tsx"
      to: "app/(dashboard)/ayuda/page.tsx"
      via: "Link href=/ayuda en el footer del sidebar"
      pattern: "href=\"/ayuda\""
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "app/(dashboard)/ayuda/page.tsx"
      via: "Link href=/ayuda desde una tab de Configuración"
      pattern: "/ayuda"
---

<objective>
Crear la FAQ/ayuda estática (HELP-01): ruta `/ayuda` como server component que renderiza un array
TS local `FAQ: { pregunta, respuesta }[]` con disclosures nativos `<details>`/`<summary>`
(zero-install, sin shadcn Accordion ni dependencia nueva). Dos accesos: link "Ayuda" (HelpCircle)
en el footer del sidebar y un link "¿Necesitás ayuda? Ver la guía" desde Configuración. Contenido
sin relación con el agente. Claude redacta el set de 7 preguntas de arranque (el usuario lo revisa;
el contenido no es crítico, la estructura sí).

Purpose: cerrar la ayuda estática del rebrand con un patrón zero-install que respeta la preferencia
"preferir zero-install, flaggear deps nuevas" (flag de dependencia RESUELTO en UI-SPEC §Registry).
Output: `app/(dashboard)/ayuda/page.tsx` (nuevo) + link en `sidebar.tsx` + link en `settings-client.tsx`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gestion-rebrand/phases/01-reorg-de-ia-ayuda/01-CONTEXT.md
@.planning/workstreams/gestion-rebrand/phases/01-reorg-de-ia-ayuda/01-PATTERNS.md
@.planning/workstreams/gestion-rebrand/phases/01-reorg-de-ia-ayuda/01-UI-SPEC.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear la página /ayuda con FAQ estática y disclosures nativos (HELP-01)</name>
  <files>app/(dashboard)/ayuda/page.tsx</files>
  <read_first>
    - components/dashboard/page-eyebrow.tsx (PageEyebrow — reusar para el eyebrow del header)
    - app/(dashboard)/settings/settings-client.tsx L793-805 (patrón de header: contenedor space-y-6 max-w-3xl + PageEyebrow + h1 con font-heading)
    - app/(dashboard)/settings/page.tsx L7-18 (patrón de server component del route group (dashboard); la ayuda no necesita fetch de negocio, solo queda protegida por el layout)
  </read_first>
  <action>
    Crear `app/(dashboard)/ayuda/page.tsx` como **server component** (`export default async function AyudaPage()`
    o función sync — no hace falta fetch). Declarar a nivel de módulo un array TS local
    `const FAQ: { pregunta: string; respuesta: string }[] = [...]` (D-08: versionado en git, sin Supabase, sin MDX)
    con las 7 preguntas de arranque (D-09: Claude redacta el draft inicial, el usuario lo revisa/ajusta; el
    contenido no es crítico para el plan, la estructura sí) de UI-SPEC §Copywriting L166-173, cada respuesta de 2-4 oraciones cortas,
    orientadas a la acción (verbo + próximo paso), apuntando a la sección relevante del panel. Redactar en español:
      1. ¿Cómo creo un turno?  2. ¿Cómo cargo mis servicios / prestaciones?  3. ¿Cómo agrego a mi equipo / profesionales?
      4. ¿Cómo configuro mis consultorios / locales?  5. ¿Cómo cobro una seña? / ¿Cómo conecto MercadoPago?
      6. ¿Cómo comparto mi página pública de reservas?  7. ¿Qué es el rubro y qué cambia según el mío?
    (En la #5 mencionar que MercadoPago se conecta desde Negocio → Integraciones, coherente con D-06.)

    Header modelado sobre settings-client L793-805: contenedor raíz `<div className="space-y-6 max-w-3xl">`;
    dentro un `<div>` con `<PageEyebrow label="Ayuda" />`, `<h1 className="text-2xl font-bold mt-2 font-[family-name:var(--font-heading)]">Ayuda</h1>`
    y un `<p className="text-sm text-muted-foreground mt-1">Cómo usar Forjo Gestión</p>`. Importar `PageEyebrow`
    de `@/components/dashboard/page-eyebrow`.

    Lista de disclosures (UI-SPEC §C — contrato exacto): contenedor `rounded-lg border border-border divide-y divide-border`.
    Por cada item de FAQ, un `<details className="group">` con:
      - `<summary className="flex items-center justify-between gap-3 px-4 py-3 cursor-pointer select-none list-none font-semibold text-sm hover:bg-secondary/50 focus-visible:ring-[3px] focus-visible:ring-ring/50 rounded-lg [&::-webkit-details-marker]:hidden">`
        con `{item.pregunta}` y un `<ChevronDown className="w-4 h-4 text-muted-foreground group-open:rotate-180 transition-transform flex-shrink-0" />`.
      - `<p className="px-4 pb-4 text-sm leading-relaxed text-muted-foreground">{item.respuesta}</p>`.
    Importar `ChevronDown` de `lucide-react`. El chevron NUNCA usa color de acento (solo `text-muted-foreground`).
    Múltiples disclosures pueden estar abiertos a la vez (son `<details>` independientes, sin `name` compartido).
    NO usar shadcn Accordion ni agregar `@radix-ui/react-accordion` ni ninguna dependencia (UI-SPEC §Registry:
    zero-install nativo). El teclado (Enter/Space en el summary) es nativo — no overridear.
    Comentar en español el *por qué* del disclosure nativo (zero-install, sin dep) y que el contenido vive en git.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "ayuda/page.tsx" | grep -qx 0 && echo "OK tipos ayuda" ; npx eslint "app/(dashboard)/ayuda/page.tsx" ; git diff --stat package.json | grep -q . && echo "REVISAR: package.json cambió (dep nueva prohibida)" || echo "OK sin deps nuevas"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sin errores en `app/(dashboard)/ayuda/page.tsx`; eslint del archivo pasa.
    - El archivo declara `const FAQ: { pregunta: string; respuesta: string }[]` con exactamente 7 entradas.
    - Cada entrada se renderiza como un `<details className="group">` con un `<summary>` que incluye `[&::-webkit-details-marker]:hidden`, `list-none` y `focus-visible:ring-[3px]`, y un `<ChevronDown>` con `group-open:rotate-180`.
    - El contenedor de la lista usa `rounded-lg border border-border divide-y divide-border`.
    - El header usa `PageEyebrow label="Ayuda"`, un `h1` con `font-[family-name:var(--font-heading)]` y texto "Ayuda", y el subtítulo "Cómo usar Forjo Gestión"; el contenedor raíz es `max-w-3xl`.
    - El archivo NO importa ningún Accordion ni `@radix-ui/react-accordion`; `git diff --stat package.json` está vacío (ninguna dependencia agregada).
    - El chevron usa `text-muted-foreground` (no `text-primary` ni color de acento).
  </acceptance_criteria>
  <done>
    /ayuda renderiza el header (eyebrow + h1 "Ayuda" + subtítulo) y 7 disclosures nativos con chevron que rota;
    el contenido sale de un array TS local; cero dependencias nuevas; tsc/eslint verdes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Agregar los dos accesos a /ayuda (footer del sidebar + Configuración)</name>
  <files>components/dashboard/sidebar.tsx, app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - components/dashboard/sidebar.tsx (ya agrupado por Plan 01-01; el bloque de imports de lucide-react y el footer con "Cerrar sesión")
    - app/(dashboard)/settings/settings-client.tsx (ya con 3 tabs de config por Plan 01-02; elegir placement del link, ej. footer de la tab Apariencia o Seguridad)
  </read_first>
  <action>
    Acceso 1 — sidebar (D-07): agregar una fila "Ayuda" en el footer del sidebar (región de "Cerrar sesión",
    encima del botón de logout o junto a los links del footer), como `<Link href="/ayuda">` con el MISMO styling
    de fila que las demás filas del sidebar (`flex items-center gap-3 px-3 py-2 rounded-lg text-sm ... text-muted-foreground hover:bg-secondary hover:text-foreground transition-colors`,
    + `focus-visible:ring-[3px] focus-visible:ring-ring/50` por a11y) y un icono `<HelpCircle className="w-4 h-4 flex-shrink-0" />`.
    Importar `HelpCircle` en el bloque de imports de `lucide-react` del sidebar. Conservar `onClick={() => setMobileOpen(false)}`
    para que cierre el drawer en mobile, igual que las filas de nav.

    Acceso 2 — Configuración (D-07): agregar en `settings-client.tsx`, dentro de la view `config` (ej. al pie de la
    tab Apariencia o Seguridad — el placement es discreción del executor), un link/botón "¿Necesitás ayuda? Ver la guía"
    que navegue a `/ayuda`. Usar `<Link href="/ayuda">` (importar `Link` de `next/link` si no está) con un estilo sobrio
    (texto pequeño `text-sm text-muted-foreground hover:text-foreground`), coherente con el resto de la config. No abrir en
    pestaña nueva (es navegación interna del dashboard).

    Ambos accesos deben resolver a `/ayuda`. NO cambiar destinos de otros items del sidebar (behavior-frozen).
    Comentar en español brevemente el *por qué* del segundo acceso (HELP-01 pide acceso desde Configuración o el footer).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v '^#' | grep -cE "sidebar.tsx|settings-client.tsx" | grep -qx 0 && echo "OK tipos" ; grep -c 'href="/ayuda"' components/dashboard/sidebar.tsx | grep -qx 1 && echo "OK link ayuda en sidebar" ; grep -q "/ayuda" "app/(dashboard)/settings/settings-client.tsx" && echo "OK link ayuda en config"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` sin errores en ambos archivos; eslint de ambos pasa.
    - `components/dashboard/sidebar.tsx` importa `HelpCircle` de `lucide-react` y contiene exactamente un `<Link href="/ayuda">` con icono `HelpCircle` en el footer, con clases de fila coherentes (`flex items-center gap-3 px-3 py-2 rounded-lg text-sm`) y `focus-visible:ring-[3px]`.
    - `app/(dashboard)/settings/settings-client.tsx` contiene un `Link`/navegación a `/ayuda` con el texto "¿Necesitás ayuda? Ver la guía" (o equivalente) dentro de la view `config`.
    - Ningún otro `href` del sidebar cambió (git diff de sidebar limitado al import de HelpCircle + la nueva fila Ayuda).
    - El link de config es navegación interna (no `target="_blank"`).
  </acceptance_criteria>
  <done>
    El footer del sidebar tiene un link "Ayuda" (HelpCircle) → /ayuda que cierra el drawer en mobile; Configuración
    tiene un segundo acceso "¿Necesitás ayuda? Ver la guía" → /ayuda; ningún otro destino cambió; tsc/eslint verdes.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `app/(dashboard)/ayuda/page.tsx` (nuevo): server component con array `FAQ: { pregunta, respuesta }[]` (7 entradas)
  + disclosures nativos `<details>`/`<summary>` con `ChevronDown` + header `PageEyebrow`/h1 "Ayuda". Zero-install.
- `components/dashboard/sidebar.tsx` (edit): import `HelpCircle` + `<Link href="/ayuda">` en el footer.
- `app/(dashboard)/settings/settings-client.tsx` (edit): link "¿Necesitás ayuda? Ver la guía" → `/ayuda` en la view config.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (ayuda/page.tsx) → cliente | Contenido 100% estático (array TS en el repo); no hay fetch, ni input del usuario, ni datos de tenant. Protegida por el layout del route group (dashboard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | Contenido de la FAQ | accept | La FAQ es contenido estático genérico (cómo usar la app), sin datos de ningún tenant ni secretos. No hay fuga posible. La página vive en (dashboard) → requiere sesión por el layout, pero no lee datos de negocio. |
| T-03-02 | Tampering | Disclosure nativo `<details>` | accept | HTML nativo sin JS custom ni ARIA manual; no hay estado de servidor ni acción. Superficie nula. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | HARD GATE del plan: cero dependencias nuevas. La FAQ usa `<details>`/`<summary>` nativo — NO shadcn Accordion, NO `@radix-ui/react-accordion`, NO base-ui accordion. Acceptance criteria fuerza `git diff --stat package.json` vacío. Sin superficie de supply-chain. |

Residual: ninguno relevante. Contenido estático, sin input no confiable, sin nueva dependencia.
</threat_model>

<verification>
- `npx tsc --noEmit` verde en los 3 archivos.
- `npx eslint` verde en ayuda/page.tsx, sidebar.tsx y settings-client.tsx.
- `git diff --stat package.json` vacío (ninguna dependencia agregada — HARD GATE zero-install).
- Verificación manual del ejecutor: entrar a /ayuda desde el footer del sidebar y desde el link de Configuración;
  abrir/cerrar varias preguntas (múltiples abiertas a la vez); el chevron rota; el foco por teclado es visible.
</verification>

<success_criteria>
Ruta /ayuda con FAQ estática de 7 preguntas en disclosures nativos; dos accesos (footer del sidebar + Configuración);
sin relación con el agente; cero dependencias nuevas. Corresponde a SC-5 del ROADMAP.
</success_criteria>

<output>
Create `.planning/workstreams/gestion-rebrand/phases/01-reorg-de-ia-ayuda/01-03-SUMMARY.md` when done
</output>
