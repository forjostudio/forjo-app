---
phase: 01-reorg-de-ia-ayuda
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/dashboard/sidebar.tsx
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "El sidebar muestra los items agrupados en 5 secciones (PANEL · AGENDA · GESTIÓN · REPORTES · AJUSTES) en vez de lista plana."
    - "Cada item del sidebar sigue linkeando exactamente al mismo href que antes (mismo destino, mismo comportamiento)."
    - "El gating por vertical se mantiene: en canchas el item Equipo no aparece bajo GESTIÓN (no está en su menu) y GESTIÓN muestra solo sus items sobrevivientes."
    - "Un grupo sin items sobrevivientes no renderiza nada (ni su header)."
    - "El estado activo de la fila sigue siendo bg-primary text-primary-foreground (tratamiento actual del dashboard, NO el del CRM)."
    - "El footer conserva Ver mi página, Cerrar sesión y la firma hecho con Forjo Studio idénticos."
  artifacts:
    - path: "components/dashboard/sidebar.tsx"
      provides: "Sidebar agrupado data-driven con array de grupos { section, keys[] } filtrado contra resolveVertical(business).menu"
      contains: "PANEL"
  key_links:
    - from: "components/dashboard/sidebar.tsx"
      to: "lib/verticals.ts"
      via: "resolveVertical(business).menu — filtro por grupo (NO se modifica verticals.ts)"
      pattern: "resolveVertical\\(business\\)"
---

<objective>
Reorganizar el sidebar del dashboard de lista plana a 5 grupos con headers (PANEL · AGENDA ·
GESTIÓN · REPORTES · AJUSTES) — NAV-01, behavior-frozen. El agrupado se hace **dentro de
`components/dashboard/sidebar.tsx`** con un array data-driven `{ section, keys[] }` filtrado
contra `resolveVertical(business).menu`, espejando el patrón visual de `crm-sidebar.tsx` sin
importarlo. Ningún item cambia de destino; el gating por vertical se preserva porque cada grupo
se filtra contra el mismo `menu` que ya usa `buildNav`.

Purpose: dejar el chasis de navegación agrupado (arquitectura de información del rebrand) sin
tocar comportamiento ni destinos — es la base sobre la que cuelgan las fases siguientes.
Output: `components/dashboard/sidebar.tsx` editado (agrupado + a11y focus-visible + aria-current).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gestion-rebrand/phases/01-reorg-de-ia-ayuda/01-CONTEXT.md
@.planning/workstreams/gestion-rebrand/phases/01-reorg-de-ia-ayuda/01-PATTERNS.md
@.planning/workstreams/gestion-rebrand/phases/01-reorg-de-ia-ayuda/01-UI-SPEC.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agrupar el sidebar en 5 secciones data-driven (NAV-01)</name>
  <files>components/dashboard/sidebar.tsx</files>
  <read_first>
    - components/dashboard/sidebar.tsx (archivo a editar — mantener buildNav L34-51, el header de marca L66-87, y TODO el footer L111-149 intactos salvo lo indicado)
    - components/crm/crm-sidebar.tsx (SOLO referencia visual del agrupado L173-184; NO importarlo — el CRM no es tenant-scoped y este sidebar sí)
    - lib/verticals.ts L58,L80,L101,L122 (arrays menu por vertical — NO modificar; canchas L122 no incluye 'equipo')
  </read_first>
  <action>
    Convertir el `<nav>` de lista plana (L89-120) a render agrupado, per D-01/D-02 y UI-SPEC §A.
    Mantener `buildNav(business)` tal cual (deriva ITEMS key→{href,label,icon} desde `resolveVertical`
    y filtra contra `v.menu`); en vez de devolver la lista plana, exponer también el `menu` resuelto
    para poder agrupar. Definir un array de grupos LOCKED a nivel de módulo o dentro del render:
    grupos en este orden con estas keys — PANEL: `dashboard`; AGENDA: `appointments`,`agenda`,`clients`,`patients`;
    GESTIÓN: `servicios`,`equipo`,`consultorios`,`negocio`; REPORTES: `finances`; AJUSTES: `settings`.
    El grupo MENSAJES del brief queda EXCLUIDO (no renderizar). Por cada grupo, mapear sus keys al
    ITEMS correspondiente y filtrar `.filter(Boolean)` contra `resolveVertical(business).menu` (mismo
    filtro que ya usa L50, aplicado por grupo) — un grupo sin items sobrevivientes NO renderiza nada,
    ni su header. Nota: `clients` y `patients` mapean ambos a `/clients`; en cada vertical el `menu`
    incluye solo uno de los dos (salud=`patients`, resto=`clients`), así que AGENDA nunca duplica la fila.
    Render de cada grupo espejando crm-sidebar L173-184: header = `<p className="px-2 pt-4 pb-1 font-[family-name:var(--font-geist-mono)] text-[11px] tracking-wider uppercase text-muted-foreground">`
    con el label del grupo, y los items dentro de `<div className="space-y-0.5">`. El primer grupo NO
    lleva `<hr>`; la separación es el `pt-4` de cada header.
    Cada fila de item conserva su render actual (Link con Icon + label), estado activo por comparación
    EXACTA `pathname === item.href`, y clases activas **`bg-primary text-primary-foreground`** (mantener
    el tratamiento actual del dashboard — NO copiar el `bg-secondary`+barra-de-acento del CRM). Inactivo
    = `text-muted-foreground hover:bg-secondary hover:text-foreground`. Agregar accesibilidad que hoy falta:
    `aria-current="page"` en la fila activa y `focus-visible:ring-[3px] focus-visible:ring-ring/50` en
    todas las filas interactivas (per UI-SPEC §A "accessibility non-negotiable"). Conservar `onClick={() => setMobileOpen(false)}`.
    Mantener EXACTOS (behavior-frozen, D-03): la fila "Ver mi página" (L111-119, link externo con su href
    `${process.env.NEXT_PUBLIC_APP_URL}/${business.slug}`), y todo el bloque footer L122-149 ("Cerrar sesión"
    + la firma "hecho con Forjo Studio" con su SVG). No cambiar `w-60`/`w-64`, el drawer mobile, ni los toggles Menu/X.
    Comentar en español el *por qué* del agrupado data-driven (el filtro por grupo preserva el gating por
    vertical sin tocar `verticals.ts`). NO tocar `lib/verticals.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v '^#' | grep -c "sidebar.tsx" | grep -qx 0 && echo "OK sin errores de tipos en sidebar" ; npx eslint components/dashboard/sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` no reporta errores en `components/dashboard/sidebar.tsx` y `npx eslint components/dashboard/sidebar.tsx` pasa sin errores.
    - El archivo contiene las 5 cadenas literales de grupo exactamente: `PANEL`, `AGENDA`, `GESTIÓN`, `REPORTES`, `AJUSTES` (grep confirma las 5).
    - El archivo NO contiene la cadena `MENSAJES` (grupo excluido).
    - Cada key de menú (`dashboard`,`appointments`,`agenda`,`clients`,`patients`,`servicios`,`equipo`,`consultorios`,`negocio`,`finances`,`settings`) aparece asignada a exactamente un grupo; los hrefs no cambian respecto a `buildNav` (mismo mapeo ITEMS).
    - El filtro por grupo usa `resolveVertical(business).menu` (o el `v.menu` derivado de él); `lib/verticals.ts` NO fue modificado (git diff sobre `lib/verticals.ts` vacío).
    - Las clases del estado activo son `bg-primary text-primary-foreground` (grep confirma la cadena en la fila de item); NO aparece `before:bg-primary` (patrón del CRM) en las filas del sidebar.
    - Las filas interactivas incluyen `focus-visible:ring-[3px]` y `focus-visible:ring-ring/50`; la fila activa incluye `aria-current`.
    - El footer conserva las cadenas `Ver mi página`, `Cerrar sesión` y `hecho con` (grep confirma las 3).
  </acceptance_criteria>
  <done>
    El sidebar renderiza 5 grupos con headers mono en el orden PANEL·AGENDA·GESTIÓN·REPORTES·AJUSTES;
    en un negocio canchas GESTIÓN omite Equipo; cada item lleva al mismo destino que antes; el estado
    activo usa bg-primary; el footer es idéntico; tsc y eslint verdes; verticals.ts intacto.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `components/dashboard/sidebar.tsx` (edit): array de grupos data-driven `{ section, keys[] }` filtrado
  contra `resolveVertical(business).menu`, con headers de sección mono, a11y (`aria-current` + `focus-visible:ring`)
  y estado activo `bg-primary`. (El link de Ayuda del footer lo agrega el Plan 03, que también toca este archivo en una wave posterior.)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server (page.tsx) → client (Sidebar) | El `business` ya autenticado (resuelto por `owner_id` en el layout) baja como prop al sidebar client. El sidebar no acepta input no confiable ni resuelve el tenant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | Gating por vertical en el sidebar agrupado | mitigate | El filtro por grupo usa `resolveVertical(business).menu` (misma fuente que `buildNav` hoy); un item que un vertical ocultaba (ej. `equipo` en canchas) sigue oculto porque su key no está en `menu`. Verificación: acceptance criteria fuerza que canchas omita Equipo y que `verticals.ts` no se modifique. |
| T-01-02 | Tampering | Destinos de navegación | accept | Los `href` salen de un mapa estático (`ITEMS`) local, no de input del cliente; ningún destino cambia (behavior-frozen). Riesgo de regresión, no de ataque; cubierto por el criterio "mismos hrefs que buildNav". |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | Cero dependencias nuevas en este plan (solo se importa `HelpCircle`… — no; ni siquiera eso: este plan no agrega imports de deps externas). No hay instalación de paquetes → sin superficie de supply-chain. |

Residual: regresión visual/gating. No hay superficie de ataque nueva (client component sin input no confiable, sin fetch, sin route nueva).
</threat_model>

<verification>
- `npx tsc --noEmit` verde (sin errores en sidebar.tsx).
- `npx eslint components/dashboard/sidebar.tsx` verde.
- `git diff --stat lib/verticals.ts` vacío (no se tocó).
- Verificación manual del ejecutor: cargar el dashboard como negocio general (ve los 5 grupos completos)
  y como negocio canchas (GESTIÓN sin Equipo). El item activo se pinta con `bg-primary`.
</verification>

<success_criteria>
Sidebar agrupado en PANEL·AGENDA·GESTIÓN·REPORTES·AJUSTES; ítems al mismo destino; gating por vertical
intacto (canchas sin Equipo); grupo vacío no renderiza header; footer behavior-frozen; a11y agregada;
`lib/verticals.ts` sin cambios. Corresponde a SC-1 y parte de SC-4 del ROADMAP.
</success_criteria>

<output>
Create `.planning/workstreams/gestion-rebrand/phases/01-reorg-de-ia-ayuda/01-01-SUMMARY.md` when done
</output>
