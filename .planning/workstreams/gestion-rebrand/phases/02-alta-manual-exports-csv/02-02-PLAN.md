---
phase: 02-alta-manual-exports-csv
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/api/clients/create/route.ts
  - app/(dashboard)/clients/clients-client.tsx
autonomous: true
requirements: [CLIENT-01]
user_setup: []

must_haves:
  truths:
    - "El dueño abre un Dialog 'Nuevo cliente' desde el header del panel de Clientes y da de alta un cliente que no vino por reserva"
    - "El alta valida nombre obligatorio + al menos un contacto (teléfono o email) antes de escribir"
    - "El cliente creado aparece en la lista al instante con badge 'Manual' (SC-1)"
    - "Cada fila de cliente muestra un badge de origen (Reserva outline · Manual default · Importado secondary) que coexiste con el status dot"
    - "El insert deriva business_id de la sesión (owner_id) y fija origin='manual' server-side, nunca del cliente"
  artifacts:
    - path: "app/api/clients/create/route.ts"
      provides: "route handler autenticado de alta manual (anon+RLS, tenant por owner_id, origin='manual')"
      exports: ["POST"]
    - path: "app/(dashboard)/clients/clients-client.tsx"
      provides: "Dialog de alta 'Nuevo cliente' + botón trigger en el header + badge de origen por fila"
      contains: "origin"
  key_links:
    - from: "app/(dashboard)/clients/clients-client.tsx"
      to: "app/api/clients/create/route.ts"
      via: "fetch POST /api/clients/create con el body del alta; prepend del cliente devuelto a setClients"
      pattern: "fetch\\(.*api/clients/create"
    - from: "app/api/clients/create/route.ts"
      to: "supabase clients"
      via: "insert en clients con business_id de la sesión + origin='manual', anon+RLS"
      pattern: "\\.from\\('clients'\\)\\.insert"
---

<objective>
Implementar el alta manual de cliente (CLIENT-01): un route handler autenticado que inserta en `clients` con `origin='manual'` (business_id derivado de la sesión), un Dialog "Nuevo cliente" en la pantalla de Clientes (tercer Dialog espejando delete/merge), y el badge de origen por fila (SC-2).

Purpose: el dueño puede cargar clientes que no vinieron por reserva y distinguir el origen de cada uno. Es la feature central de la fase.

Output: `app/api/clients/create/route.ts` (nuevo), edición de `app/(dashboard)/clients/clients-client.tsx` (Dialog alta + botón + badge).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/skills/supabase-multitenant-rls/SKILL.md
@.claude/skills/convenciones-forjo/SKILL.md
@.planning/workstreams/gestion-rebrand/phases/02-alta-manual-exports-csv/02-CONTEXT.md
@.planning/workstreams/gestion-rebrand/phases/02-alta-manual-exports-csv/02-UI-SPEC.md
@.planning/workstreams/gestion-rebrand/phases/02-alta-manual-exports-csv/02-PATTERNS.md

# Analog EXACTO del endpoint de alta (auth gate + tenant por owner_id + insert en clients, anon+RLS)
@app/api/appointments/create/route.ts

# Pantalla a editar (Dialogs delete/merge L786-829, header L417-427, fila L519-543, gating salud L601-608)
@app/(dashboard)/clients/clients-client.tsx
@components/ui/badge.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route handler POST /api/clients/create (alta autenticada, anon+RLS)</name>
  <files>app/api/clients/create/route.ts</files>
  <read_first>
    - app/api/appointments/create/route.ts L19-37 (auth gate + resolución de tenant por owner_id — analog EXACTO; copiar el patrón)
    - app/api/appointments/create/route.ts L39-60 (parseo defensivo del body + narrowing manual + missing_fields)
    - app/api/appointments/create/route.ts L179-184 (insert en clients con business_id de la sesión, anon+RLS)
    - .claude/skills/supabase-multitenant-rls/SKILL.md (regla 5: NUNCA service-role acá; anon+RLS alcanza porque el tenant se resuelve por owner_id)
  </read_first>
  <behavior>
    - Sin sesión (auth.getUser null) → 401 `{ ok: false, error: 'unauthorized' }`.
    - Sesión sin business (owner_id sin negocio) → 404 `{ ok: false, error: 'not_found' }`.
    - Body no-JSON → 400 `{ ok: false, error: 'bad_request' }`.
    - `name` vacío → 400 `{ ok: false, error: 'missing_fields' }`.
    - `name` presente pero sin teléfono NI email → 400 `{ ok: false, error: 'missing_fields' }`.
    - `name` + al menos un contacto → 201/200 `{ ok: true, client: <fila insertada con origin='manual'> }`.
    - El `business_id` de la fila insertada = el resuelto por owner_id (NUNCA un business_id del body).
    - `origin` de la fila insertada = `'manual'` (fijo server-side, el cliente no lo elige).
    - `insurance_name`/`insurance_number` solo se persisten si el vertical del negocio es salud; en otros verticales se ignoran aunque lleguen en el body.
  </behavior>
  <action>
    Crear `app/api/clients/create/route.ts` clonando la estructura de `app/api/appointments/create/route.ts` (es el patrón canónico de escritura autenticada del dueño). Usar el cliente **anon+RLS** de `@/lib/supabase/server` (`createClient()`), NUNCA `@/lib/supabase/admin`/service-role: el aislamiento lo garantizan RLS + el filtro por `business_id` resuelto por `owner_id` (defensa en profundidad, per contribution de security y skill supabase-multitenant-rls).

    Auth gate + tenant (espejo de appointments/create L19-37): `auth.getUser()` → sin user devolver 401 `unauthorized`; resolver `business` con `.from('businesses').select('id, type, vertical').eq('owner_id', user.id).single()` (traer las columnas que necesite `resolveVertical` para decidir salud) → sin business devolver 404 `not_found`. El tenant SIEMPRE es el actor de la sesión, nunca un id del body (per D-02 y contribution de security).

    Parseo defensivo del body (espejo L39-60): `try { raw = await request.json() } catch { return 400 'bad_request' }`, luego narrowing manual `typeof body.x === 'string' ? body.x.trim() : ...`. Campos: `name` (obligatorio), `phone`, `email`, `notes`, `insurance_name`, `insurance_number`. Validación (D-02): `name` no vacío + al menos uno de `phone`/`email` → si falla, 400 `missing_fields`.

    Gating de obra social (D-02): resolver el vertical del negocio con `resolveVertical(business)` (de `@/lib/verticals`) y solo incluir `insurance_name`/`insurance_number` en el insert si el vertical es salud. En otros verticales, ignorarlos aunque lleguen.

    Insert (espejo L179-184): `.from('clients').insert({ business_id: business.id, name, phone, email, notes, ...(isSalud ? { insurance_name, insurance_number } : {}), origin: 'manual' }).select('*').single()`. `business_id` = `business.id` (resuelto por sesión, NO del body). `origin: 'manual'` fijo. Respuesta de éxito: `Response.json({ ok: true, client: created })` (fila completa para el prepend optimista en el cliente). Error de insert → `Response.json({ ok: false, error: 'insert_failed' }, { status: 500 })`, con `console.error('[clients/create] insert error:', ...)` (prefijo de módulo, convención del proyecto).

    Cabecera de comentarios en español explicando el *por qué* (espejo del bloque L7-18 de appointments/create): endpoint autenticado, anon+RLS y NO service-role, tenant por owner_id, `origin='manual'` fijado server-side. NO reusar el dedupe de `resolveClientId` (D-02 no lo exige para el alta manual; el alta crea un cliente nuevo directo).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "origin: 'manual'" app/api/clients/create/route.ts && grep -q "createClient" app/api/clients/create/route.ts && grep -vq "supabase/admin" app/api/clients/create/route.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existe `app/api/clients/create/route.ts` con `export async function POST`.
    - Importa `createClient` de `@/lib/supabase/server` y NO importa `@/lib/supabase/admin` (grep `supabase/admin` NO matchea).
    - El insert fija `origin: 'manual'` y `business_id: business.id` (resuelto por `owner_id`, no del body).
    - Devuelve 401 `unauthorized` sin sesión, 404 `not_found` sin business, 400 `bad_request`/`missing_fields` en validación, 500 `insert_failed` en error de insert, y `{ ok: true, client }` en éxito.
    - `insurance_name`/`insurance_number` solo se persisten en vertical salud (gate por `resolveVertical`).
    - `npx tsc --noEmit` verde.
  </acceptance_criteria>
  <done>El endpoint de alta autenticado existe, usa anon+RLS, deriva el tenant de la sesión, fija origin='manual' y devuelve la fila creada; tsc verde.</done>
</task>

<task type="auto">
  <name>Task 2: Dialog "Nuevo cliente" + botón trigger + badge de origen en clients-client.tsx</name>
  <files>app/(dashboard)/clients/clients-client.tsx</files>
  <read_first>
    - app/(dashboard)/clients/clients-client.tsx L786-799 (Dialog delete — patrón EXACTO a espejar) y L802-829 (Dialog merge, sm:max-w-md)
    - app/(dashboard)/clients/clients-client.tsx L417-427 (header del panel izquierdo — donde va el botón "Nuevo cliente")
    - app/(dashboard)/clients/clients-client.tsx L519-543 (fila de cliente — badge va en el bloque info L537-540, NO tocar el status dot L542)
    - app/(dashboard)/clients/clients-client.tsx L119-121 (isSalud, term) y L601-608 (gating obra social del edit form inline — mismo gate para el alta)
    - components/ui/badge.tsx L11-22 (variantes default/secondary/outline ya existen, h-5 fijo)
    - .planning/workstreams/gestion-rebrand/phases/02-alta-manual-exports-csv/02-UI-SPEC.md §A (contrato del Dialog), §B + §Color (mapeo de variantes del badge), §Copywriting
  </read_first>
  <action>
    Editar `app/(dashboard)/clients/clients-client.tsx` para agregar tres cosas, TODO por reuse (sin componentes ni deps nuevos — solo import de `Badge` y de los iconos lucide `UserPlus`/`Download`):

    **(a) Badge de origen por fila** (SC-2): en el bloque info de la fila (L537-540), debajo del nombre / junto a la meta `{fichaNum(num)} · {cs?.visits} visitas` (L539), renderizar un `Badge` de `@/components/ui/badge` con la variante según `client.origin` (mapeo LOCKED del UI-SPEC §Color): `reserva`→`variant="outline"` label "Reserva", `manual`→`variant="default"` label "Manual", `importado`→`variant="secondary"` label "Importado". NO tocar ni restilar el status dot (L542) — es lifecycle status, coexiste con el origen (guardrail behavior-frozen). El `h-5` del badge entra en el `py-3` de la fila sin romper la altura. El badge es NO interactivo (label de estado, sin hover/click/focus).

    **(b) Botón trigger "Nuevo cliente"** (CLIENT-01 / UI-SPEC §A): en la fila del header del panel izquierdo (L418-427, el `flex items-center justify-between` que hoy tiene el `h1` + el ícono de merge), agregar un `Button` primary (default `bg-primary`, icono `UserPlus`) que abre el Dialog de alta. Copy: `Nuevo ${term.client}` (respeta terminología por vertical: "Nuevo paciente" en salud). Asegurar hit area ≥ 44px en mobile (UI-SPEC Spacing: usar `size` default en mobile o padding suficiente — NO icon-only, el label es obligatorio).

    **(c) Tercer Dialog de alta** (UI-SPEC §A): agregar un `Dialog` (`DialogContent className="sm:max-w-md"`, como el merge L803) al final del árbol junto a los otros dos (después de L829). `DialogTitle` = `Nuevo ${term.client}`. Campos en orden: 1. Nombre (obligatorio) · 2. Teléfono · 3. Email · 4. Notas (`Textarea`, opcional) · 5. Obra social (`insurance_name`) + N° de afiliado (`insurance_number`) **solo si `isSalud`** (mismo gate `{isSalud && (...)}` que el edit form L601). Cada campo con `Label` visible arriba (nunca placeholder-only); marcar opcionales con "(opcional)", NO asteriscar obligatorios. Validación con `zod` + `zodResolver` (ya en el stack): `name` no vacío, al menos uno de `phone`/`email`, `email` válido si viene lleno; errores inline `text-xs text-destructive` onBlur. Submit `Button` "Guardar"/"Guardando..." (`bg-primary`, `setSaving(true)` + disable para evitar doble submit); Cancel `Button variant="outline"` "Cancelar" + close por `onOpenChange`.

    **Write path del alta**: `fetch('/api/clients/create', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(...) })`, chequear `res.ok` y `body.ok`. En éxito (UI-SPEC §A Success): cerrar dialog, `setClients(prev => [body.client, ...prev])` (prepend → aparece al instante, SC-1), `toast.success('${term.client} creado/a')` (o "Cliente creado"). En error: mantener dialog abierto, re-habilitar submit, `toast.error('No se pudo crear el cliente. Intentá de nuevo.')`, sin perder los datos ingresados. NO hacer insert directo desde el cliente para el alta (a diferencia de los updates inline que sí usan `supabase.from('clients').update`) — el alta va SIEMPRE por el endpoint server-side (aislamiento por tenant).

    Copy exacto del UI-SPEC §Copywriting (español): validaciones "Ingresá un nombre." / "Ingresá al menos un teléfono o un email." / "El email no es válido."; labels "Nombre" · "Teléfono" · "Email" · "Notas (opcional)" · "Obra social" · "N° de afiliado". No signalizar estado con font-weight (guardrail UI-SPEC). No introducir `--primary` fuera de: botón "Nuevo cliente", submit del modal, y badge "Manual" (guardrail UI-SPEC).

    Guardrails behavior-frozen (UI-SPEC): NO modificar `lib/verticals.ts`; NO cambiar el layout de dos paneles; NO tocar el status dot ni los Dialogs delete/merge existentes.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/(dashboard)/clients/clients-client.tsx" && grep -q "api/clients/create" "app/(dashboard)/clients/clients-client.tsx" && grep -q "from '@/components/ui/badge'" "app/(dashboard)/clients/clients-client.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `clients-client.tsx` importa `Badge` de `@/components/ui/badge` y los iconos `UserPlus`/`Download` de lucide.
    - Cada fila renderiza un `Badge` mapeado por `client.origin`: reserva→outline, manual→default, importado→secondary, con labels "Reserva"/"Manual"/"Importado".
    - El status dot existente (L542) sigue presente sin cambios.
    - Hay un botón "Nuevo cliente" (o `Nuevo ${term.client}`) primary en el header del panel izquierdo que abre un Dialog.
    - El Dialog de alta tiene los campos nombre/teléfono/email/notas + obra social gateada por `isSalud`, con validación zod (nombre + al menos un contacto).
    - El submit hace `fetch` a `/api/clients/create` (grep matchea) y en éxito hace prepend a `setClients` + `toast.success`.
    - `npx tsc --noEmit` y `npx eslint` sobre el archivo pasan sin errores nuevos (baseline pre-existente sin regresión).
  </acceptance_criteria>
  <done>La pantalla de Clientes tiene el botón + Dialog de alta funcional (escribe vía endpoint server-side, prepend optimista) y el badge de origen por fila; tsc + eslint verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente (browser) → API | el body del alta llega del navegador del dueño (input semi-confiable: sesión válida, pero campos sin validar) |
| API → DB (clients) | el insert cruza a la tabla de tenant; el business_id debe venir de la sesión, no del body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | POST /api/clients/create sin sesión | mitigate | auth gate `auth.getUser()` → 401 `unauthorized` sin sesión (espejo appointments/create L28) |
| T-02-05 | Tampering | business_id ajeno en el body | mitigate | el `business_id` del insert se deriva SIEMPRE de `owner_id` de la sesión; el body nunca aporta business_id (D-02 + skill supabase-multitenant-rls); cliente anon+RLS, no service-role |
| T-02-06 | Tampering | origin arbitrario desde el cliente | mitigate | `origin: 'manual'` fijado server-side en el insert; el cliente no elige origin; el CHECK de la DB (049) rechaza valores fuera de los tres |
| T-02-07 | Elevation | uso de service-role en el alta | mitigate | prohibido: el endpoint usa `@/lib/supabase/server` (anon+RLS); grep de verificación confirma que NO importa `supabase/admin` |
| T-02-08 | Information Disclosure | obra social escrita en vertical no-salud | mitigate | `insurance_*` solo se persiste si `resolveVertical(business)` es salud; en otros verticales se ignora aunque llegue en el body |
| T-02-09 | Tampering | body malformado / campos faltantes | mitigate | parseo defensivo (`try/catch` → 400 `bad_request`) + validación `name` + al menos un contacto → 400 `missing_fields` |
</threat_model>

<verification>
- `npx tsc --noEmit` verde con el endpoint y la edición del cliente.
- `npx eslint` sobre `clients-client.tsx` sin errores nuevos (baseline pre-existente).
- grep confirma: `origin: 'manual'` en el endpoint, sin `supabase/admin`; `fetch` a `/api/clients/create` y import de `Badge` en el cliente.
- Manual (cubierto en el UAT de fase): alta de un cliente aparece al instante con badge "Manual"; clientes viejos muestran "Reserva".
</verification>

<success_criteria>
- Alta manual funcional: el dueño crea un cliente desde `/clients`, queda asociado a su negocio (business_id de sesión), con `origin='manual'`, y aparece en la lista al instante (SC-1).
- Badge de origen por fila con las tres variantes correctas, coexistiendo con el status dot (SC-2).
- El endpoint usa anon+RLS, deriva el tenant de la sesión, valida el body y fija `origin` server-side.
</success_criteria>

<output>
Create `.planning/workstreams/gestion-rebrand/phases/02-alta-manual-exports-csv/02-02-SUMMARY.md` when done
</output>
