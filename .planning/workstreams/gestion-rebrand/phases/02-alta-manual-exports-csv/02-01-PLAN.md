---
phase: 02-alta-manual-exports-csv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/049_clients_origin.sql
  - supabase/schema.sql
  - lib/types.ts
autonomous: false
requirements: [CLIENT-01]
user_setup:
  - service: supabase
    why: "Aplicar la migración 049 a staging/prod es acción externa GATEADA (D-01) — el ejecutor genera el SQL + valida local, el usuario la aplica a staging (forjo-staging) y prod a mano."

must_haves:
  truths:
    - "La tabla clients tiene una columna origin con default 'reserva' que backfillea las filas existentes"
    - "El type Client de TS incluye origin como union literal reserva|manual|importado"
    - "supabase db reset local corre limpio (baseline + 040..049 replayan sin error)"
  artifacts:
    - path: "supabase/migrations/049_clients_origin.sql"
      provides: "ADD COLUMN origin text NOT NULL DEFAULT 'reserva' CHECK (origin IN ('reserva','manual','importado'))"
      contains: "ADD COLUMN IF NOT EXISTS \"origin\""
    - path: "supabase/schema.sql"
      provides: "schema regenerado con la columna origin en public.clients"
      contains: "origin"
    - path: "lib/types.ts"
      provides: "campo origin en interface Client"
      contains: "origin: 'reserva' | 'manual' | 'importado'"
  key_links:
    - from: "supabase/migrations/049_clients_origin.sql"
      to: "lib/types.ts"
      via: "la columna origin de la DB se refleja como Client.origin en TS (snake_case = columna)"
      pattern: "origin"
---

<objective>
Introducir la columna `origin` en la tabla `clients` (migración 049) y reflejarla en el type `Client` de TypeScript. Es la fundación de la fase: el badge de origen (SC-2) la lee, el alta manual (CLIENT-01) escribe `origin='manual'`, el export de clientes (DATA-01) la incluye en el CSV, y la Fase 3 (import) va a escribir `origin='importado'` (valor ya reservado en el CHECK).

Purpose: sin la columna existiendo en la DB, ni el badge ni el insert de `origin` del alta manual funcionan. Este plan la crea y la tipa; los consumidores viven en los planes 02 y 03.

Output: `supabase/migrations/049_clients_origin.sql` (nueva), `supabase/schema.sql` regenerado, `lib/types.ts` con `Client.origin`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/skills/supabase-multitenant-rls/SKILL.md
@.claude/skills/convenciones-forjo/SKILL.md
@.planning/workstreams/gestion-rebrand/phases/02-alta-manual-exports-csv/02-CONTEXT.md
@.planning/workstreams/gestion-rebrand/phases/02-alta-manual-exports-csv/02-PATTERNS.md

# Analog exacto de la migración (ADD COLUMN aditivo sobre tabla que YA tiene RLS)
@supabase/migrations/043_professionals_service_id.sql

# Type a editar (interface Client)
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 049 — ADD COLUMN clients.origin (text + CHECK)</name>
  <files>supabase/migrations/049_clients_origin.sql</files>
  <read_first>
    - supabase/migrations/043_professionals_service_id.sql (analog EXACTO: cabecera densa en español + ADD COLUMN IF NOT EXISTS sobre tabla con RLS existente; copiar el tono de la cabecera L1-33 y la estructura del ALTER L36-37)
    - supabase/migrations/048_app_settings.sql (migración vecina, confirma numeración)
    - .claude/skills/supabase-multitenant-rls/SKILL.md (clients YA tiene RLS por business_id → la columna hereda el aislamiento, NO se agrega policy)
  </read_first>
  <action>
    Crear `supabase/migrations/049_clients_origin.sql` con el modelo LOCKED de D-01: `ALTER TABLE "public"."clients" ADD COLUMN IF NOT EXISTS "origin" text NOT NULL DEFAULT 'reserva' CHECK (origin IN ('reserva','manual','importado'))`. Modelo text+CHECK como el resto del esquema (`appointments.status`, `plan_status`), NO enum Postgres (per D-01: evita el dolor de ALTER TYPE, más extensible).

    Escribir una cabecera de comentarios densa en español espejando el tono de 043 (contexto + qué hace + racional + qué NO hace/invariantes). Documentar como invariantes: (1) el `DEFAULT 'reserva'` hace el backfill automático de filas existentes (SC-2) — no hace falta un UPDATE separado; (2) NO se agrega policy RLS nueva: `clients` ya es RLS por `business_id` y la columna hereda ese aislamiento (agregar policy sería un error, no cambia la superficie de acceso); (3) el valor `'importado'` queda reservado para la Fase 3 (el CHECK ya lo admite; nadie lo escribe todavía); (4) NO se aplica vía `supabase db push` — la única validación autónoma es `supabase db reset` local (PG17, replaya baseline + 040..049); prod se aplica A MANO coordinado con el deploy + `NOTIFY pgrst, 'reload schema';`.

    Numeración: 049 = primera libre (045 landing_cms, 046 drop_business_hours, 047 backfill_vertical, 048 app_settings ya tomadas — NO renumerar ninguna). No tocar migraciones existentes.

    Descripción del texto de comentarios: al describir el CHECK en la cabecera, referí los valores por concepto (los tres orígenes: reserva, manual, importado) — no escribas literales que un grep de aceptación pueda invalidar. El literal de la cláusula CHECK sí va en el DDL (es load-bearing).
  </action>
  <verify>
    <automated>grep -q 'ADD COLUMN IF NOT EXISTS "origin"' "supabase/migrations/049_clients_origin.sql" && grep -q "CHECK (origin IN ('reserva','manual','importado'))" "supabase/migrations/049_clients_origin.sql" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Existe `supabase/migrations/049_clients_origin.sql`.
    - Contiene exactamente `ADD COLUMN IF NOT EXISTS "origin" text NOT NULL DEFAULT 'reserva'`.
    - Contiene la cláusula `CHECK (origin IN ('reserva','manual','importado'))`.
    - El `ALTER TABLE` apunta a `"public"."clients"`.
    - NO contiene ninguna sentencia `CREATE POLICY` ni `ENABLE ROW LEVEL SECURITY` (la columna hereda el RLS existente).
    - NO renumera ni edita las migraciones 040-048.
  </acceptance_criteria>
  <done>La migración 049 existe con el ADD COLUMN + CHECK correcto, cabecera en español, sin policy nueva, sin tocar migraciones ajenas.</done>
</task>

<task type="auto">
  <name>Task 2: Agregar origin a interface Client en lib/types.ts</name>
  <files>lib/types.ts</files>
  <read_first>
    - lib/types.ts L168-183 (interface Client — campos snake_case = columna DB; agregar `origin` cerca de `status`/`client_number` L175-176)
    - lib/types.ts L216 (Appointment.status como union literal — mismo estilo a espejar)
  </read_first>
  <action>
    Agregar el campo `origin: 'reserva' | 'manual' | 'importado'` al `interface Client` (L168-183), cerca de `status`/`client_number` (L175-176). Union type literal (mismo estilo que `Appointment.status` L216). Va NO opcional (sin `?`, sin `| null`) porque la columna es `NOT NULL DEFAULT` — siempre viaja un valor. Agregar un comentario breve en español explicando el origen del campo (de dónde viene el cliente: reserva pública, alta manual, o import CSV de la Fase 3), en línea con el estilo de comentarios de los campos de `lib/types.ts`.

    NO tocar otros campos del interface ni otros interfaces del archivo.
  </action>
  <verify>
    <automated>grep -q "origin: 'reserva' | 'manual' | 'importado'" lib/types.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `interface Client` incluye `origin: 'reserva' | 'manual' | 'importado'` (no opcional, sin `| null`).
    - `npx tsc --noEmit` pasa sin errores nuevos.
    - Ningún otro interface ni campo fue modificado.
  </acceptance_criteria>
  <done>El type Client expone `origin` como union literal; tsc verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint — validar migración 049 en local + regenerar schema.sql (aplicación a staging/prod GATEADA)</name>
  <what-built>La migración 049 (`ADD COLUMN clients.origin`) y el type `Client.origin`. La aplicación de la migración a staging/prod es GATEADA (D-01): NO autónoma.</what-built>
  <how-to-verify>
    El ejecutor NO aplica la migración a staging ni prod. Solo valida en local. Pasos:
    1. Correr `supabase db reset` local (PG17) y confirmar que replaya el baseline + 040..049 SIN error (la migración 049 aplica limpio sobre `clients`).
    2. Regenerar `supabase/schema.sql` con el patrón del repo (igual que se hizo tras 037/039/042/043) y confirmar que el `schema.sql` regenerado incluye la columna `origin` en `public.clients` con su default y CHECK. Committear el `schema.sql` regenerado.
    3. NO correr `supabase db push`. NO aplicar a staging/prod — eso lo hace el usuario a mano, coordinado con el deploy (flujo local → staging forjo-staging → prod), seguido de `NOTIFY pgrst, 'reload schema';`.
    Confirmá que: `supabase db reset` local corrió limpio, y `supabase/schema.sql` ya tiene la columna `origin`.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" cuando el reset local corrió limpio y schema.sql está regenerado con la columna origin (o describí el problema).</resume-signal>
  <files>supabase/schema.sql</files>
  <action>El ejecutor valida la migración SOLO en local y regenera `supabase/schema.sql`, luego SE DETIENE para el gate humano (D-01) — NO aplica a staging/prod ni corre `supabase db push`. Pasos: (1) `supabase db reset` local (PG17) y confirmar que replaya el baseline + 040..049 sin error; (2) regenerar `supabase/schema.sql` con el patrón del repo (igual que tras 037/039/042/043) y confirmar que incluye la columna `origin` en `public.clients`; (3) committear el `schema.sql` regenerado. La aplicación a staging (forjo-staging) y prod la hace el usuario a mano, seguida de `NOTIFY pgrst, 'reload schema';`.</action>
  <verify>
    <automated>grep -q "origin" supabase/schema.sql && echo OK</automated>
  </verify>
  <done>`supabase db reset` local corrió limpio, `supabase/schema.sql` regenerado incluye la columna `origin` en `public.clients`, y la aplicación a staging/prod queda pendiente de aprobación del usuario (gate D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migración → DB | DDL numerado aplicado a mano; el gate de aplicación (staging/prod) es humano, no automático |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | migración 049 sobre `clients` | mitigate | ADD COLUMN aditivo, sin DROP ni ALTER destructivo; `clients` ya tiene RLS por `business_id` → la columna hereda el aislamiento (skill supabase-multitenant-rls); NO se agrega policy nueva ni se abre acceso |
| T-02-02 | Elevation | aplicación de la migración | mitigate | aplicación GATEADA `autonomous: false` (D-01): el ejecutor solo valida con `supabase db reset` local, NUNCA `supabase db push` contra prod; staging/prod los aplica el usuario a mano |
| T-02-03 | Tampering | valor de `origin` inválido | accept | el `CHECK (origin IN (...))` rechaza a nivel DB cualquier valor fuera de los tres permitidos; el default cubre inserts que no lo especifican |
</threat_model>

<verification>
- `grep` confirma el ADD COLUMN + CHECK en la migración 049.
- `npx tsc --noEmit` verde con `Client.origin` agregado.
- Checkpoint humano: `supabase db reset` local limpio + `schema.sql` regenerado con la columna (aplicación a staging/prod diferida al usuario).
</verification>

<success_criteria>
- La columna `origin` existe en `clients` (migración 049) con default `reserva` y CHECK a los tres valores.
- El type `Client` tipa `origin` como union literal; tsc verde.
- `schema.sql` regenerado incluye la columna (tras el reset local del checkpoint).
- Ninguna migración ajena renumerada; ninguna policy RLS agregada.
</success_criteria>

<output>
Create `.planning/workstreams/gestion-rebrand/phases/02-alta-manual-exports-csv/02-01-SUMMARY.md` when done
</output>
