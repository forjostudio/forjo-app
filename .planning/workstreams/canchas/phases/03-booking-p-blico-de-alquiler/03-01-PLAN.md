---
phase: 03-booking-p-blico-de-alquiler
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/044_public_canchas.sql
  - supabase/schema.sql
  - test/canchas-booking.test.ts
  - test/concurrency.test.ts
autonomous: false
requirements: [ALQUILER-01, ALQUILER-02, ALQUILER-04]
user_setup:
  - service: supabase (prod)
    why: "La migración 044 (vista public_canchas) se aplica a prod A MANO coordinada con el deploy — el repo NO usa push/CLI automático (protocolo migr. 043)."
    dashboard_config:
      - task: "Aplicar el DDL de supabase/migrations/044_public_canchas.sql en la DB de prod y correr NOTIFY pgrst, 'reload schema'; para que PostgREST exponga la vista al anon"
        location: "Supabase (prod) → SQL Editor — DESPUÉS de mergear/deployar, coordinado con el deploy"

must_haves:
  truths:
    - "supabase db reset local (PG17) replaya el baseline + 040..044 sin error"
    - "public_canchas devuelve, para un negocio con una cancha activa, una fila { id, business_id, name, price, duration_minutes } donde id = professional_id"
    - "public_canchas NO devuelve filas de canchas soft-deleted (professional.active=false o service.active=false)"
    - "el nuevo test/canchas-booking.test.ts corre (skipIf sin creds) y cubre ALQUILER-01/04 + anti-tampering serviceId + cross-tenant contra el route handler real de create"
    - "test/concurrency.test.ts sigue verde con el caso canchas de exclusión por espacio (ALQUILER-02)"
  artifacts:
    - path: "supabase/migrations/044_public_canchas.sql"
      provides: "Vista acotada public_canchas para el booking anon (molde public_services, SIN security_invoker)"
      contains: "CREATE OR REPLACE VIEW \"public\".\"public_canchas\""
    - path: "test/canchas-booking.test.ts"
      provides: "Suite de integración del booking de canchas (ALQUILER-01/04 + seguridad anti-tampering/cross-tenant)"
      min_lines: 90
  key_links:
    - from: "supabase/migrations/044_public_canchas.sql"
      to: "professionals.service_id + services"
      via: "JOIN professionals p JOIN services s ON s.id = p.service_id WHERE p.service_id IS NOT NULL AND p.active AND s.active"
      pattern: "JOIN \"public\"\\.\"services\""
    - from: "test/canchas-booking.test.ts"
      to: "app/api/booking/create/route.ts"
      via: "POST al route handler real con { professionalId } y sin serviceId (verifica la rama canchas del Plan 02)"
      pattern: "booking/create"
  prohibitions:
    - "public_canchas NO expone service_id (puntero interno — D-01): service_id vive SOLO en el JOIN y el WHERE, jamás en el SELECT"
    - "public_canchas NO usa security_invoker (molde public_services, owner postgres — con security_invoker el anon no leería nada, Pitfall 1)"
    - "public_canchas NO expone columnas sensibles del professional (matrícula/contacto) ni del service (description/location_id)"
    - "el caso canchas de concurrency NO debilita book_slot_atomic: usa el RPC vía createAppointmentCore, nunca un count/check suelto como autoridad"
---

<objective>
Crear la vista pública acotada `public_canchas` (migración 044, aditiva) que expone a `anon` SOLO
lo reservable/mostrable de una cancha (id = professional_id, name, price, duration_minutes) sin
filtrar el puntero interno `service_id` ni config interna, y sembrar el scaffold de tests
(`test/canchas-booking.test.ts`) que la fase completa usará para verificar ALQUILER-01/02/04 y la
seguridad anti-tampering. Es Wave 1: la data-layer y los tests van ANTES de la rama del create
(Plan 02) y de la UI (Plan 03) que los consumen.

Purpose: aislar la cancha para el anon por el mismo patrón de vista acotada que `public_services`/
`public_professionals` (skill supabase-multitenant-rls), y dejar la red de tests lista para que los
planes downstream la hagan pasar (Nyquist — el test existe antes que el código que verifica).

Output: `supabase/migrations/044_public_canchas.sql`, `supabase/schema.sql` regenerado,
`test/canchas-booking.test.ts` (scaffold de integración con seedOneTenant + provisión de cancha) y
un caso canchas de exclusión por espacio en `test/concurrency.test.ts`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/canchas/ROADMAP.md
@.planning/workstreams/canchas/STATE.md
@.planning/workstreams/canchas/phases/03-booking-p-blico-de-alquiler/03-CONTEXT.md
@.planning/workstreams/canchas/phases/03-booking-p-blico-de-alquiler/03-RESEARCH.md

# Fuentes de verdad (leer antes de tocar):
@supabase/migrations/043_professionals_service_id.sql
@supabase/migrations/042_spaces_and_coupled_exclusion.sql
@lib/canchas.ts
@test/helpers/booking-fixtures.ts
@test/canchas-provision.test.ts
@test/concurrency.test.ts
@test/env.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 044 — vista acotada public_canchas (D-01) + regenerar schema.sql</name>
  <files>supabase/migrations/044_public_canchas.sql, supabase/schema.sql</files>
  <read_first>
    - supabase/migrations/043_professionals_service_id.sql (protocolo de migración del repo: db reset local, prod a mano + NOTIFY pgrst, regenerar schema.sql; cabecera de racional en español)
    - supabase/migrations/00000000000000_baseline.sql — buscar `public_services` y `public_professionals` (molde de vista acotada: OWNER postgres, WHERE active = true, GRANT a anon/authenticated/service_role, SIN security_invoker) y sus GRANT
    - lib/canchas.ts (el soft-delete de una cancha desactiva service Y professional — canchasFromData empareja por service_id, NUNCA por nombre)
  </read_first>
  <action>
    Crear `supabase/migrations/044_public_canchas.sql` (aditiva) con una cabecera densa en español
    (patrón de 043) que explique: qué expone la vista y por qué, el molde correcto (public_services,
    definer/owner postgres SIN security_invoker) y el protocolo manual del repo. El DDL: `CREATE OR
    REPLACE VIEW "public"."public_canchas"` que selecciona `p.id`, `p.business_id`, `p.name`,
    `s.price`, `s.duration_minutes` FROM `professionals` p JOIN `services` s ON `s.id = p.service_id`
    WHERE `p.service_id IS NOT NULL AND p.active = true AND s.active = true`. Después
    `ALTER VIEW ... OWNER TO postgres` y `GRANT SELECT ON TABLE "public"."public_canchas"` a `anon`,
    `authenticated` y `service_role` (igualar el patrón de GRANT de public_services del baseline; si
    ese patrón usa GRANT ALL, igualarlo para no divergir del repo — decidir leyendo el baseline).
    Reglas duras: el `service_id` se usa SOLO en el JOIN y el WHERE, NUNCA aparece en la lista del
    SELECT (D-01, no filtrar el puntero interno); NO agregar `WITH (security_invoker=true)` (rompería
    la lectura anon — copiaría por error el molde de crm_timeline en vez del de public_services);
    NO exponer columnas sensibles del professional (specialty/license_number/phone/email) ni del
    service (description/location_id). Luego regenerar `supabase/schema.sql` con el mismo procedimiento
    que usaron 037/039/042/043 (dump del schema tras el reset local). La aplicación a prod es MANUAL
    (user_setup), NO se corre push/CLI automático.
  </action>
  <acceptance_criteria>
    - source: `044_public_canchas.sql` contiene `CREATE OR REPLACE VIEW "public"."public_canchas"`, el JOIN por `s.id = p.service_id`, el WHERE con `p.active = true AND s.active = true`, `OWNER TO "postgres"` y `GRANT ... "public"."public_canchas" TO "anon"`.
    - source (negativo): la lista del SELECT de la vista NO contiene `service_id`; el archivo NO contiene `security_invoker`.
    - CLI: `supabase db reset` (PG17 local) replaya baseline + 040..044 SIN error.
    - behavior: en la DB local reseteada, `SELECT * FROM public_canchas` para un negocio con una cancha activa provisionada (via provisionCancha) devuelve una fila con las columnas { id, business_id, name, price, duration_minutes } y ninguna columna service_id; al soft-deletear la cancha (active=false), esa fila desaparece.
    - source: `supabase/schema.sql` incluye la definición de `public_canchas` (regenerado).
  </acceptance_criteria>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && supabase db reset</automated>
  </verify>
  <done>La migración 044 crea public_canchas (sin service_id, sin security_invoker, GRANT anon), db reset local pasa, schema.sql regenerado. Aplicación a prod queda pendiente como user_setup manual.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold test/canchas-booking.test.ts (ALQUILER-01/04 + anti-tampering + cross-tenant) y caso canchas de espacio en concurrency</name>
  <files>test/canchas-booking.test.ts, test/concurrency.test.ts</files>
  <read_first>
    - test/helpers/booking-fixtures.ts (seedOneTenant, seedSpace, seedAgendaSpace, seedProfessional, teardownOneTenant, seedTimeBlock — patrón service-role para fixtures)
    - test/canchas-provision.test.ts (provisionCancha como forma de crear una cancha real en la DB de test)
    - test/concurrency.test.ts (CONC-03: patrón de test de exclusión por espacio con createAppointmentCore + seedSpace/seedAgendaSpace/seedProfessional; DATE fija lunes; occupantsAt)
    - app/api/booking/create/route.ts (el route handler que el test de anti-tampering invoca vía POST; hoy exige serviceId — el test define el comportamiento que el Plan 02 debe cumplir)
    - lib/canchas.ts (provisionCancha para sembrar la cancha: service + professional(service_id) + space + agenda_spaces)
    - test/env.ts (hasSupabaseCreds — skipIf)
  </read_first>
  <action>
    Crear `test/canchas-booking.test.ts` con `describe.skipIf(!hasSupabaseCreds)` (molde de
    concurrency.test.ts / booking-core.test.ts). Fixtures con `seedOneTenant` + provisión de una cancha
    real via `provisionCancha(t.admin, t.businessId, { name, price, duration })` y un `seedTimeBlock`
    que cubra el día/horario de la fecha fija (lunes, patrón DATE de concurrency). Sembrar cada caso y
    limpiar en afterEach (borrar appointments/time_blocks/agenda_spaces/spaces del business), teardown
    en afterAll. Tests (dejarlos escritos apuntando al comportamiento objetivo del Plan 02 — pueden
    fallar en RED hasta que el Plan 02 aterrice la rama canchas del create; marcarlo en un comentario):
    (1) ALQUILER-01/04 "reserva de cancha" — POST a `/api/booking/create` (route handler real,
    importado como en concurrency importa availabilityGET) con body `{ slug, professionalId: cancha.id,
    date, time, clientName, clientPhone, clientEmail }` y SIN `serviceId`; esperar `ok:true`, y verificar
    en la DB con t.admin que el appointment creado referencia el `service_id` de la cancha (precio propio
    → ALQUILER-04) y su `duration_minutes` (la fija de la cancha → ALQUILER-01), NO una duración del
    cliente. (2) anti-tampering "serviceId ignorado" — POST con `professionalId: cancha.id` MÁS un
    `serviceId` FALSO (de otra cancha con precio/duración distintos); esperar que el appointment use el
    service DERIVADO del professional (el de cancha.id), NUNCA el del body. (3) cross-tenant — POST con
    un `professionalId` de OTRO tenant (seedOneTenant #2) contra el slug del tenant #1; esperar
    `ok:false` con `invalid_service` (400). (4) ALQUILER-03 "dos turnos consecutivos" — reservar el slot
    de la cancha y luego el slot consecutivo (time + duration) sobre la MISMA cancha; ambos `ok:true`
    (sin lógica nueva — D-06). Además, agregar a `test/concurrency.test.ts` (o dejar cubierto por el ya
    existente CONC-03 si aplica — leerlo) un caso de exclusión por espacio para DOS canchas que comparten
    espacio (ALQUILER-02): reservar una cancha que comparte espacio con otra ocupada en horario solapado
    → `slot_taken`, verificando en la DB exactamente 1 fila ocupa el slot. NO debilitar book_slot_atomic:
    usar `createAppointmentCore`/el RPC como autoridad, nunca un check suelto.
    Si el import del `POST` del create fuerza a construir un `Request` con body JSON, usar el patrón de
    `new Request(url, { method:'POST', headers, body: JSON.stringify(...) })` (mirror de cómo concurrency
    invoca availabilityGET con `new Request(url)`).
  </action>
  <acceptance_criteria>
    - source: existe `test/canchas-booking.test.ts` con `describe.skipIf(!hasSupabaseCreds)` y usa `seedOneTenant` + `provisionCancha`.
    - source: el test de "reserva de cancha" hace `fetch`/POST al handler de `app/api/booking/create` con body que incluye `professionalId` y NO incluye `serviceId`.
    - source: existe un caso que manda un `serviceId` falso + `professionalId` real y asserta que gana el service derivado del professional.
    - source: existe un caso cross-tenant (professionalId de otro tenant → invalid_service).
    - test: `npx vitest run test/canchas-booking.test.ts test/concurrency.test.ts` corre (skipeando si no hay creds); con creds locales, el caso ALQUILER-02 de espacio compartido pasa (exclusión heredada del motor).
    - behavior: los tests que dependen de la rama canchas del create (Task 2 casos 1-4) quedan en RED documentado hasta el Plan 02; el caso de espacio (ALQUILER-02) pasa ya (reuso del motor).
  </acceptance_criteria>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx vitest run test/canchas-booking.test.ts test/concurrency.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>test/canchas-booking.test.ts existe y define ALQUILER-01/03/04 + anti-tampering (serviceId ignorado) + cross-tenant contra el route handler real; el caso de exclusión por espacio (ALQUILER-02) pasa reusando el motor; tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (browser) → DB | El cliente final no autenticado lee canchas de un negocio; la vista es la ÚNICA superficie de lectura anon del modelo de cancha |
| test service-role → DB | Fixtures crean/borran datos con service-role (bypassa RLS) — solo en test, patrón del repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | public_canchas expone el puntero interno service_id | mitigate | El SELECT de la vista lista SOLO id/business_id/name/price/duration_minutes; service_id vive únicamente en JOIN+WHERE (D-01). Acceptance criterion negativo lo verifica |
| T-03-02 | Elevation of Privilege | security_invoker mal aplicado rompe/abre la lectura anon | mitigate | Molde public_services (owner postgres, SIN security_invoker); acceptance criterion niega la presencia de security_invoker; db reset + smoke anon confirman lectura |
| T-03-03 | Information Disclosure | vista expone canchas soft-deleted (config vieja) | mitigate | WHERE p.active AND s.active (consistente con public_services); test verifica que la fila desaparece al desactivar |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | La fase NO instala paquetes (RESEARCH §Package Legitimacy Audit: no aplica). Sin superficie de supply-chain nueva |
</threat_model>

<verification>
- `supabase db reset` (PG17 local) aplica baseline + 040..044 sin error.
- `npx vitest run test/canchas-booking.test.ts test/concurrency.test.ts` corre; el caso ALQUILER-02 (espacio compartido) pasa; los casos que dependen del Plan 02 quedan RED documentado.
- `npx tsc --noEmit` limpio.
- Smoke anon: `SELECT * FROM public_canchas` sin `service_id` en el resultset.
</verification>

<success_criteria>
- public_canchas existe en la migración 044, expone { id, business_id, name, price, duration_minutes }, sin service_id, sin security_invoker, GRANT a anon, filtra por active.
- schema.sql regenerado incluye la vista.
- test/canchas-booking.test.ts define la red de tests de la fase (ALQUILER-01/03/04 + anti-tampering + cross-tenant).
- El caso de exclusión por espacio (ALQUILER-02) pasa reusando el motor v0.12.
- Aplicación a prod de la 044 queda declarada como user_setup manual (autonomous:false).
</success_criteria>

<output>
Create `.planning/workstreams/canchas/phases/03-booking-p-blico-de-alquiler/03-01-SUMMARY.md` when done
</output>
