---
phase: 03-booking-p-blico-de-alquiler
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - app/[slug]/canchas-booking-client.tsx
  - app/[slug]/page.tsx
  - components/landing/landing-renderer.tsx
  - lib/types.ts
requirements: [ALQUILER-01, ALQUILER-03, ALQUILER-04]
autonomous: false

must_haves:
  truths:
    - "En /[slug] de un negocio canchas, el cliente elige una cancha (nombre + precio + duración fija visible), luego un día y un horario disponible, completa sus datos y reserva — SIN picker de duración ni de profesional"
    - "El precio mostrado y el resumen de la reserva son el precio propio de la cancha (ALQUILER-04)"
    - "El flujo entra por AMBOS caminos de /[slug]: legacy (BookingClient directo) y sección booking del LandingRenderer, gateado por resolveVertical(business).key === 'canchas' (D-05)"
    - "Los verticales salud/belleza/general siguen renderizando BookingClient byte-idéntico — cero regresión"
    - "El cliente reserva un slot y puede reservar el consecutivo (dos turnos) sin lógica nueva (ALQUILER-03/D-06)"
  artifacts:
    - path: "app/[slug]/canchas-booking-client.tsx"
      provides: "Client component de booking de canchas (3 pasos: Cancha → Fecha y hora → Tus datos)"
      min_lines: 200
    - path: "app/[slug]/page.tsx"
      provides: "Lectura de public_canchas (anon) + gateo canchas vs BookingClient en ambas ramas"
      contains: "public_canchas"
  key_links:
    - from: "app/[slug]/page.tsx"
      to: "public_canchas (vista, Plan 01)"
      via: "supabase.from('public_canchas').select('*').eq('business_id', business.id) con createPublicServerClient (anon)"
      pattern: "public_canchas"
    - from: "app/[slug]/canchas-booking-client.tsx"
      to: "/api/booking/availability + /api/booking/create"
      via: "GET availability con professionalId=cancha.id; POST create con { professionalId: cancha.id } sin serviceId (D-03)"
      pattern: "booking/(availability|create)"
    - from: "app/[slug]/page.tsx + components/landing/landing-renderer.tsx"
      to: "resolveVertical"
      via: "resolveVertical(business).key === 'canchas' decide el componente de booking; el renderer recibe el booking como prop"
      pattern: "resolveVertical"
  prohibitions:
    - "El client de canchas NO manda serviceId ni precio al create (D-03): solo professionalId=cancha.id"
    - "El client NO ofrece picker de duración ni de profesional (la duración es la fija de la cancha — ALQUILER-01/D-06)"
    - "NO se cambia la forma de respuesta de /api/booking/availability {ok,busy,full} — se consume igual que BookingClient"
    - "NO envolver <section id='reservar'> en transform/overflow-hidden/position:fixed|sticky/filter en el LandingRenderer (rompe el popover del calendario y los toasts — caja negra)"
    - "El gateo es ADITIVO: salud/belleza/general renderizan BookingClient byte-idéntico"
    - "Cero hex/px hardcodeado: usar los tokens y clases Tailwind del BookingClient (UI-SPEC)"
---

<objective>
Construir el flujo público de reserva de canchas: un client component nuevo
`app/[slug]/canchas-booking-client.tsx` (3 pasos: Cancha → Fecha y hora → Tus datos, D-02) que
reusa `/api/booking/availability` y `/api/booking/create` (con la rama canchas del Plan 02), y
gatearlo en `app/[slug]/page.tsx` y `components/landing/landing-renderer.tsx` por
`resolveVertical(business).key === 'canchas'` (D-05), leyendo la cancha desde `public_canchas`
(Plan 01). Implementar SEGÚN el 03-UI-SPEC, anclando visualmente al BookingClient existente.

Purpose: cerrar ALQUILER-01/03/04 en la superficie pública — el cliente elige cancha + horario al
precio propio de la cancha, sin duración custom, respetando la exclusión por espacio que ya vive en
availability + book_slot_atomic. Reproducir el lenguaje visual del BookingClient tal cual (UI fidelity
es load-bearing).

Output: `canchas-booking-client.tsx` (nuevo), `page.tsx` y `landing-renderer.tsx` gateados por
vertical, y el tipo de la cancha pública en `lib/types.ts`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/canchas/ROADMAP.md
@.planning/workstreams/canchas/phases/03-booking-p-blico-de-alquiler/03-CONTEXT.md
@.planning/workstreams/canchas/phases/03-booking-p-blico-de-alquiler/03-RESEARCH.md
@.planning/workstreams/canchas/phases/03-booking-p-blico-de-alquiler/03-UI-SPEC.md

# Fuentes de verdad (leer antes de tocar):
@app/[slug]/booking-client.tsx
@app/[slug]/page.tsx
@components/landing/landing-renderer.tsx
@lib/supabase/public.ts
@lib/verticals.ts
@lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client component canchas-booking-client.tsx (3 pasos, reusa availability/create) + tipo PublicCancha</name>
  <files>app/[slug]/canchas-booking-client.tsx, lib/types.ts</files>
  <read_first>
    - app/[slug]/booking-client.tsx (REFERENCIA VISUAL OBLIGATORIA — hero SVG Bauhaus bg-primary, barra de progreso, calendario mensual, grilla de slots, resumen border-l-primary, formulario, reCAPTCHA, redirección post-reserva pago/confirmación, footer; clases Tailwind a replicar verbatim)
    - .planning/workstreams/canchas/phases/03-booking-p-blico-de-alquiler/03-UI-SPEC.md (contrato de 3 pasos, copy exacto, tokens de color/tipografía/spacing, empty states, copy de 2 turnos consecutivos)
    - lib/types.ts (agregar el tipo de la cancha pública; patrón de Service/Professional/PublicBusiness)
    - lib/verticals.ts (terminología canchas: Reserva/Cancha/Sede)
    - RESEARCH §Q5 (patrones exactos a replicar del BookingClient: carga de availability con professionalId=cancha.id, cómputo de slots con duration=cancha.duration_minutes, POST al create sin serviceId, manejo de errores, navegación)
  </read_first>
  <action>
    En `lib/types.ts`, agregar un tipo `PublicCancha` = `{ id: string; business_id: string; name: string;
    price: number; duration_minutes: number }` (la forma de public_canchas — Plan 01). Crear
    `app/[slug]/canchas-booking-client.tsx` (`'use client'`) que recibe por props `business:
    PublicBusiness`, `canchas: PublicCancha[]`, `timeBlocks`, `exceptions`, `locations` (mismos shapes
    acotados que BookingClient). Implementar el flujo de 3 pasos del UI-SPEC con barra de progreso
    `Paso {step} de 3` y labels `['Cancha', 'Fecha y hora', 'Tus datos']`:
    Paso 1 (Cancha): grilla `grid grid-cols-1 sm:grid-cols-2 gap-3`, cada cancha un `<button>` tarjeta
    `rounded-lg border p-4 text-left` con nombre a la izquierda (font-semibold, --font-heading) y a la
    derecha precio `$X` (`Number(price).toLocaleString('es-AR')`, text-lg font-bold) + duración con
    icono Clock (`{duration_minutes} min`, text-[11px]). Sin descripción (la vista no la expone).
    Selección → paso 2. Empty state (sin canchas) con el copy del UI-SPEC.
    Paso 2 (Fecha y hora): bloque resumen `border-l-primary` "Cancha: {nombre} · {duration} min ·
    ${price}"; calendario mensual VERBATIM del BookingClient (ChevronLeft/Right, grid-cols-7, aspect-
    square); grilla de slots VERBATIM (`grid grid-cols-3 sm:grid-cols-4 gap-2`); la duración del slot
    es la fija de la cancha (`cancha.duration_minutes`), SIN picker de duración; sin selector de sede.
    Disponibilidad desde `GET /api/booking/availability?slug&date&professionalId={cancha.id}` con
    `cache:'no-store'`, leyendo `{busy,full}` (respuesta vieja sin `full` → `[]` defensivo), y el mismo
    cómputo de slots que el BookingClient (descartar solapados con buffer y los `full`). Copy opcional de
    2 turnos consecutivos (`text-xs text-muted-foreground` bajo la grilla, D-06). CTA "Continuar"
    disabled hasta día+horario.
    Paso 3 (Tus datos): bloque resumen (Cancha, "{día} a las {hora}", "Total: ${price}" — ALQUILER-04;
    "Seña requerida: ${deposit_amount}" si require_deposit — la seña sigue siendo el deposit_amount
    business-level, D-04); formulario con Nombre*/Teléfono*/Email* +
    Notas(opcional) usando `Input`/`Label`/`Textarea` (labels siempre visibles); reCAPTCHA v3 invisible
    solo en el camino sin seña (script como BookingClient:106-115). CTA con el copy del UI-SPEC
    ("Reservar cancha" / "Pagar seña $X"), estados de carga.
    POST al create: `body { slug, professionalId: cancha.id, date, time, locationId: null, clientName,
    clientPhone, clientEmail, notes, recaptchaToken }` — SIN serviceId, SIN precio (D-03). Redirección
    post-reserva IDÉNTICA al BookingClient: con seña → POST /api/payment/create → window.location.href;
    sin seña → POST /api/notify/booking (fire-and-forget) → router.push(`/{slug}/turno/{cancelToken}`).
    Manejo de errores con los toasts exactos del UI-SPEC (slot_taken/recaptcha_failed/genérico/sin
    conexión). Botón "Volver" en pasos > 1, auto-scroll (smoothScrollTo con prefers-reduced-motion),
    footer "hecho con Forjo Studio". Cero hex/px hardcodeado — tokens y clases del BookingClient.
  </action>
  <acceptance_criteria>
    - source: existe `app/[slug]/canchas-booking-client.tsx` con `'use client'`, 3 pasos y barra `Paso {step} de 3`.
    - source: el POST a `/api/booking/create` incluye `professionalId` y NO incluye `serviceId` ni `price`.
    - source: el GET a `/api/booking/availability` usa `professionalId=` con el id de la cancha; el cómputo de slots usa `cancha.duration_minutes` y no hay ningún control de selección de duración.
    - source: el resumen del paso 3 muestra el precio de la cancha (`toLocaleString('es-AR')`) como total.
    - source: `lib/types.ts` exporta `PublicCancha` con { id, business_id, name, price, duration_minutes }.
    - CLI: `npx tsc --noEmit` y `npm run lint` limpios.
  </acceptance_criteria>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>canchas-booking-client.tsx implementa los 3 pasos del UI-SPEC, reusa availability/create sin mandar serviceId/precio, muestra el precio propio de la cancha, y no ofrece picker de duración/profesional. tsc + lint limpios.</done>
</task>

<task type="auto">
  <name>Task 2: Gateo por vertical en page.tsx (lee public_canchas) y en landing-renderer.tsx (booking como prop)</name>
  <files>app/[slug]/page.tsx, components/landing/landing-renderer.tsx</files>
  <read_first>
    - app/[slug]/page.tsx (líneas 44-70: lee public_businesses + Promise.all de public_services/professionals/time_blocks/exceptions/locations con createPublicServerClient anon; línea 76: resolveVertical(business); líneas 95-109: rama legacy landing===null con BookingClient; líneas 113-126: rama LandingRenderer)
    - components/landing/landing-renderer.tsx (líneas 47-55: Props; líneas 177-194: case 'booking' — <section id="reservar"> caja negra con BookingClient; REGLA DURA: no envolver en transform/overflow/position:fixed|sticky)
    - lib/verticals.ts (resolveVertical → key 'canchas')
    - lib/supabase/public.ts (createPublicServerClient anon)
    - RESEARCH §Q6 (recomendación: page.tsx decide el componente de booking y lo pasa al renderer como prop `bookingSlot: ReactNode` para no acoplar el renderer al modelo canchas)
  </read_first>
  <action>
    En `app/[slug]/page.tsx`: agregar la lectura de canchas con el cliente anon ya creado —
    `supabase.from('public_canchas').select('*').eq('business_id', business.id)` (agregar al Promise.all
    de líneas 59-70 o como query condicional; es una vista barata acotada). Calcular
    `const isCanchas = resolveVertical(business).key === 'canchas'` (ya se llama resolveVertical en :76 —
    reusar). En la rama legacy (`landing === null`, :95-109) y en la del renderer (:113-126), cuando
    `isCanchas` renderizar `<CanchasBookingClient business=… canchas=… timeBlocks=… exceptions=…
    locations=… />` en vez de `<BookingClient>`. NO tocar `force-dynamic`, ni el JSON-LD, ni agregar
    queries de más para los otros verticales (la query de canchas es aditiva y no rompe salud/belleza/
    general). Para el renderer (D-05): NO acoplar `LandingRenderer` al modelo canchas — pasar el
    componente de booking ya resuelto como prop. En `components/landing/landing-renderer.tsx` agregar un
    prop opcional `bookingSlot?: React.ReactNode` a `Props`; en el `case 'booking'` (:177-194), si
    `bookingSlot` está presente renderizar `<section id="reservar" key={i}>{bookingSlot}</section>` (misma
    caja negra, SIN envoltorios nuevos — sin transform/overflow-hidden/position:fixed|sticky/filter), y
    si no, el `<BookingClient>` de siempre (path legacy de los otros verticales byte-idéntico). Desde
    `page.tsx` pasar `bookingSlot={<CanchasBookingClient .../>}` solo cuando `isCanchas`. Gateo ADITIVO:
    un negocio no-canchas ve exactamente lo de hoy.
  </action>
  <acceptance_criteria>
    - source: `page.tsx` contiene `.from('public_canchas')` con `createPublicServerClient` (anon) filtrado por `business.id`.
    - source: `page.tsx` computa `resolveVertical(business).key === 'canchas'` y elige `CanchasBookingClient` vs `BookingClient` en AMBAS ramas (legacy y renderer).
    - source: `landing-renderer.tsx` acepta un `bookingSlot` (o equivalente) por props y lo renderiza dentro de `<section id="reservar">` SIN añadir transform/overflow-hidden/position:fixed|sticky/filter.
    - source (negativo): el `case 'booking'` del renderer NO añade wrappers nuevos con transform/overflow/position alrededor de la sección.
    - CLI: `npx tsc --noEmit` y `npm run lint` limpios.
    - test: `npx vitest run test/landing-derive.test.ts test/landing-schema.test.ts` verde (el renderer sigue componiendo secciones sin regresión).
  </acceptance_criteria>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npm run lint && npx vitest run test/landing-derive.test.ts test/landing-schema.test.ts</automated>
  </verify>
  <done>page.tsx lee public_canchas y gatea CanchasBookingClient vs BookingClient por vertical en ambas ramas; landing-renderer recibe el booking como prop y lo renderiza en la caja negra sin envoltorios. Otros verticales byte-idénticos. tsc/lint/tests de landing verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verificación humana del flujo de reserva de canchas</name>
  <action>Pausar para verificación humana del flujo público de canchas end-to-end (ver how-to-verify). No auto-avanzar: es un checkpoint bloqueante.</action>
  <what-built>
    El flujo público de reserva de canchas: canchas-booking-client.tsx (3 pasos) gateado por vertical
    en /[slug] (legacy y landing), leyendo public_canchas, reusando availability/create/pago. Requiere
    que la migración 044 esté aplicada en la DB local/prod (Plan 01) para que public_canchas exista.
  </what-built>
  <how-to-verify>
    1. Con la migración 044 aplicada localmente (o en un negocio canchas real con una cancha provisionada
       en Phase 2), abrir `/{slug}` de un negocio con vertical canchas.
    2. Verificar Paso 1: aparece la grilla de canchas con nombre + precio propio + duración fija; NO hay
       selección de duración ni de profesional.
    3. Elegir una cancha → Paso 2: se ve el calendario y, al elegir un día, la grilla de horarios (la
       duración del slot es la de la cancha). Verificar el copy de "dos horarios seguidos".
    4. Elegir un horario → Continuar → Paso 3: el resumen muestra el precio de la cancha como total; si
       el negocio tiene seña, el CTA dice "Pagar seña $X"; si no, "Reservar cancha".
    5. Completar datos y reservar: sin seña → redirige a /{slug}/turno/{token}; con seña → redirige a
       MercadoPago. Verificar que el turno quedó con el precio/duración de la cancha.
    6. Verificar exclusión por espacio: reservar una cancha que comparte espacio con otra ya ocupada en
       horario solapado → ese horario no aparece / la reserva se rechaza ("Ese horario se acaba de
       ocupar").
    7. Regresión: abrir el /{slug} de un negocio salud/belleza/general y confirmar que el booking de
       siempre (servicio + profesional + duración) se ve y funciona idéntico.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" o describí los problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (browser) → /[slug] | RSC lee public_canchas con anon; el client component solo tiene datos mostrables (sin service_id) |
| client → /api/booking/{availability,create} | El client manda professionalId (la cancha), nunca serviceId/precio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | El RSC/client expone config interna de la cancha al anon | mitigate | page.tsx lee public_canchas (vista acotada sin service_id, Plan 01); el client recibe solo { id, name, price, duration_minutes }. No lee tablas base con anon |
| T-03-09 | Tampering | El client manda serviceId/precio al create | mitigate | El POST solo lleva professionalId=cancha.id; el server deriva el service (Plan 02). Prohibición explícita en must_haves; acceptance criterion niega serviceId en el body |
| T-03-10 | Denial of Service | Envoltorio en <section id="reservar"> rompe el calendario/toasts | mitigate | El renderer NO añade transform/overflow-hidden/position:fixed|sticky/filter alrededor de la sección (caja negra, RESEARCH Pitfall 4); el booking va como prop sin wrappers |
| T-03-11 | Business Logic bypass | Gateo por vertical rompe salud/belleza/general | mitigate | Gateo aditivo: solo con key==='canchas' cambia el componente; el resto renderiza BookingClient byte-idéntico. Checkpoint humano paso 7 + tests de landing lo verifican |
| T-03-12 | Spoofing / Business Logic | El flujo canchas saltea reCAPTCHA / gate de plan | mitigate | Reusa /api/booking/create y /api/payment/create sin cambios → hereda reCAPTCHA fail-closed (sin seña) y el gate de plan_status. No se reimplementa ningún guard |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | La fase NO instala paquetes; solo componentes ya en @/components/ui (RESEARCH §Registry Safety) |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run lint` limpios.
- `npx vitest run test/landing-derive.test.ts test/landing-schema.test.ts` verde (renderer sin regresión).
- Checkpoint humano: flujo de 3 pasos, precio de la cancha, exclusión por espacio, y regresión de otros verticales.
</verification>

<success_criteria>
- El cliente reserva una cancha + horario en /[slug] sin elegir duración ni profesional (ALQUILER-01).
- El precio mostrado/asociado es el propio de la cancha (ALQUILER-04).
- El flujo entra por ambos caminos (legacy + landing) gateado por vertical (D-05).
- Dos turnos consecutivos posibles sin lógica nueva (ALQUILER-03).
- salud/belleza/general renderizan BookingClient byte-idéntico (cero regresión).
- La sección booking del renderer no se envuelve en containing blocks (caja negra respetada).
</success_criteria>

<output>
Create `.planning/workstreams/canchas/phases/03-booking-p-blico-de-alquiler/03-03-SUMMARY.md` when done
</output>
