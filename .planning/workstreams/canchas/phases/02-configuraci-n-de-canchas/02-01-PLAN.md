---
phase: 02-configuraci-n-de-canchas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/043_professionals_service_id.sql
  - supabase/schema.sql
  - lib/types.ts
  - lib/canchas.ts
  - test/canchas-provision.test.ts
autonomous: false
requirements: [CANCHA-01, CANCHA-02, CANCHA-03]
user_setup:
  - service: supabase-prod
    why: "La migración 043 (aditiva, professionals.service_id) se aplica a PROD a mano, coordinada con el deploy. Workflow del repo: NUNCA push automático. Validación local con `supabase db reset` (PG17). Tras aplicar a prod: NOTIFY pgrst para refrescar el schema cache de PostgREST."
    dashboard_config:
      - task: "Aplicar 043_professionals_service_id.sql en el SQL editor de Supabase prod (o vía supabase db) en el momento del deploy, y ejecutar: NOTIFY pgrst, 'reload schema';"
        location: "Supabase Dashboard -> SQL Editor (proyecto prod gestion.forjo.studio)"

must_haves:
  truths:
    - "supabase db reset replaya el baseline + 040 + 041 + 042 + 043 sin error en PG17 local"
    - "professionals tiene una columna nullable service_id (FK -> services.id) tras 043"
    - "provisionCancha inserta service + professional(service_id seteado) + space + agenda_space, todos con business_id, en secuencia con rollback manual"
    - "reconstruir la tupla de una cancha se hace por professionals.service_id (puntero estable), nunca por nombre"
    - "deleteCancha soft-deletea (active=false en service y professional) o hard-deletea según haya reservas"
  artifacts:
    - path: "supabase/migrations/043_professionals_service_id.sql"
      provides: "Migración aditiva: ALTER TABLE professionals ADD COLUMN service_id uuid nullable FK -> services(id) + índice"
      contains: "ADD COLUMN"
    - path: "lib/canchas.ts"
      provides: "Capa pura de provisión/borrado/reconstrucción de la tupla cancha (service+professional+space+agenda_space)"
      exports: ["provisionCancha", "deleteCancha", "canchasFromData"]
      min_lines: 80
    - path: "lib/types.ts"
      provides: "Professional.service_id en el tipo (espejo de la columna 043)"
      contains: "service_id"
    - path: "test/canchas-provision.test.ts"
      provides: "Tests de la lógica pura de provisión + reconstrucción + rollback"
  key_links:
    - from: "lib/canchas.ts"
      to: "supabase/migrations/043_professionals_service_id.sql"
      via: "provisionCancha escribe professionals.service_id (la columna que crea 043)"
      pattern: "service_id"
    - from: "lib/canchas.ts"
      to: "lib/types.ts"
      via: "usa Service, Professional, Space, AgendaSpace"
      pattern: "from '@/lib/types'"
  prohibitions:
    - "salud/belleza/general: las filas professionals de esos verticales NUNCA reciben service_id (queda null) — la columna es nullable y solo canchas la usa. Cero regresión."
    - "Ningún insert (service/professional/space/agenda_space) sin business_id seteado explícito."
    - "NO reconstruir la tupla cancha emparejando por nombre (frágil al renombrar). Usar professionals.service_id."
    - "NO agregar policy RLS nueva: la columna 043 vive sobre professionals (ya RLS por business_id). NO tocar la RLS de services/spaces/agenda_spaces."
    - "NO agregar columnas de precio/duración (siguen en services) ni tablas nuevas."
    - "NO usar supabase db push ni aplicar la migración a prod automáticamente — es manual, coordinada con el deploy."
---

<objective>
Crear la base de datos + la capa de datos pura que sostiene la cancha como entidad reservable unificada. Precio y duración fija de la cancha viven en `services` (cada cancha = 1 fila en services, **D-01**) — esta capa no agrega columnas de precio/duración; solo materializa el linkeo 1:1 cancha↔agenda con la **migración aditiva 043** (`professionals.service_id`, el puntero estable lockeado en **D-06**), el tipo TypeScript espejo, y `lib/canchas.ts` —una capa pura testeable que orquesta la auto-provisión (D-02), reconstruye la tupla de una cancha por el puntero estable, y borra (soft/hard, D-05)— con sus tests.

Purpose: la UI de Plan 02 consume esta capa. Aislar la lógica de provisión/reconstrucción/borrado en `lib/canchas.ts` (en vez de inline en el componente de 1808 líneas) la hace testeable sin DOM y evita el linkeo frágil por nombre (Pitfall 2 del research). La columna 043 resuelve la mayor incertidumbre técnica de la fase (cómo se materializa el 1:1 sin emparejar por nombre).

Output: `supabase/migrations/043_professionals_service_id.sql`, `supabase/schema.sql` regenerado, `lib/types.ts` (Professional.service_id), `lib/canchas.ts`, `test/canchas-provision.test.ts`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/canchas/ROADMAP.md
@.planning/workstreams/canchas/STATE.md
@.planning/workstreams/canchas/phases/02-configuraci-n-de-canchas/02-CONTEXT.md
@.planning/workstreams/canchas/phases/02-configuraci-n-de-canchas/02-RESEARCH.md

# Fuentes de verdad del esquema/tipos/patrones
@supabase/migrations/042_spaces_and_coupled_exclusion.sql
@supabase/migrations/README.md
@lib/types.ts
@test/isolation.test.ts
@test/helpers/supabase-fixtures.ts
</context>

<note_research_divergence>
IMPORTANTE: el RESEARCH.md recomienda "cero migración / reusar professionals.specialty como puntero". Esa recomendación quedó SUPERADA por la decisión final lockeada **D-06** (tomada DESPUÉS del research): el 1:1 se materializa con una **columna nueva `professionals.service_id` vía migración aditiva 043**, NO reusando specialty (descartado por hacky). Este plan implementa D-06. Donde el research dice "specialty = service.id", leé "service_id = service.id".
</note_research_divergence>

<tasks>

<task type="auto">
  <name>Task 1: Migración 043 (professionals.service_id) + tipo + schema.sql</name>
  <files>supabase/migrations/043_professionals_service_id.sql, lib/types.ts, supabase/schema.sql</files>
  <read_first>
    - supabase/migrations/042_spaces_and_coupled_exclusion.sql (formato de cabecera/comentarios + invariantes del workflow de migraciones: validación SOLO con `supabase db reset` local, prod a mano, regenerar schema.sql)
    - supabase/migrations/README.md (reglas de orden y aplicación manual)
    - supabase/migrations/00000000000000_baseline.sql líneas 488-501 (DDL de professionals) y 1221 (policy "business member access" de professionals, ya RLS por business_id)
    - lib/types.ts líneas 105-117 (interface Professional — el espejo del tipo)
  </read_first>
  <acceptance_criteria>
    - source: existe supabase/migrations/043_professionals_service_id.sql con ALTER TABLE "public"."professionals" ADD COLUMN "service_id" uuid (nullable) REFERENCES "public"."services"("id") ON DELETE SET NULL, más un índice sobre service_id.
    - behavior: la columna es NULLABLE (sin NOT NULL, sin default que la llene) → las filas professionals existentes de salud/belleza/general quedan con service_id NULL tras la migración (cero backfill, cero regresión).
    - source: NO hay CREATE POLICY ni ALTER ... ENABLE ROW LEVEL SECURITY en 043 (la columna hereda la RLS existente de professionals; agregar policy sería error).
    - test: `supabase db reset` corre el baseline + 040 + 041 + 042 + 043 en orden sin error en PG17 local (replay limpio).
    - source: lib/types.ts interface Professional incluye `service_id?: string | null` con comentario en español (puntero 1:1 a su service para canchas; null en otros verticales, D-06).
    - source: supabase/schema.sql regenerado/actualizado refleja la columna service_id en professionals (patrón del repo: igual que 037/039 regeneraron schema.sql).
  </acceptance_criteria>
  <action>
    Escribir la migración aditiva 043 que materializa el puntero estable 1:1 cancha↔agenda de D-06. Cabecera de comentarios en español siguiendo el formato de 042: qué hace (agrega professionals.service_id, FK nullable a services), qué NO hace (no toca RLS — la columna vive sobre professionals que ya es RLS por business_id; no agrega tablas/columnas de precio/duración; no se aplica por push, validación SOLO con `supabase db reset` local, prod a mano + NOTIFY pgrst, regenerar schema.sql). El ALTER usa ADD COLUMN service_id uuid nullable con REFERENCES services(id) ON DELETE SET NULL (si se borra el service, la agenda queda sin puntero en vez de romper el FK). Agregar un índice btree sobre professionals(service_id) WHERE service_id IS NOT NULL (parcial: solo las filas-cancha lo usan) para que canchasFromData reconstruya la tupla rápido. Documentar en la cabecera el racional de D-06: descartar reusar specialty (semánticamente ajeno, frágil); la columna nullable garantiza cero regresión en salud/belleza/general. Actualizar lib/types.ts: agregar `service_id?: string | null` a la interface Professional con comentario en español explicando que es el puntero 1:1 a su service en el vertical canchas (D-06) y queda null en los demás verticales. Regenerar/actualizar supabase/schema.sql para reflejar la columna nueva (mismo patrón del repo, ver README y memoria infra-testing). NO agregar ninguna policy ni habilitar RLS en 043. NO modificar el código de la app en este task (solo SQL + tipo + schema).
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && supabase db reset >/dev/null 2>&1 && echo "db reset OK" && grep -q "service_id" supabase/migrations/043_professionals_service_id.sql && grep -q "service_id" lib/types.ts && echo "columna+tipo OK"; grep -ci "create policy" supabase/migrations/043_professionals_service_id.sql | grep -q '^0$' && echo "no policy nueva OK"</automated>
  </verify>
  <done>043 existe y `supabase db reset` replaya sin error; professionals.service_id es nullable FK a services con índice parcial; lib/types.ts tiene service_id; schema.sql regenerado; cero policy nueva.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/canchas.ts — provisión, reconstrucción y borrado de la tupla</name>
  <files>lib/canchas.ts</files>
  <read_first>
    - app/(dashboard)/settings/settings-client.tsx líneas 356-393 (addService, deleteService FK 23503, toggleService, setServiceLocations — patrón de inserts/soft-delete de services con business_id + defensa en profundidad .eq('business_id'))
    - app/(dashboard)/settings/settings-client.tsx líneas 543-622 (deleteProfessional, addSpace, deleteSpace, toggleAgendaSpace — inserts/deletes de professionals/spaces/agenda_spaces con business_id y rollback optimista)
    - lib/types.ts (Service, Professional, Space, AgendaSpace)
    - 02-RESEARCH.md Pregunta 2 (snippet de provisionCancha con rollback manual) y Pitfall 1 (atomicidad) + Pitfall 2 (linkeo frágil por nombre)
    - test/booking-core.test.ts (patrón de test de lógica pura del repo, para el estilo de los tests nuevos)
  </read_first>
  <acceptance_criteria>
    - source: lib/canchas.ts exporta `provisionCancha`, `deleteCancha`, `canchasFromData` (named exports, patrón del repo).
    - behavior: provisionCancha, dado { name, price, duration, sharedSpaceIds? } y un client + businessId, inserta en secuencia: (1) service {name, price, duration_minutes, business_id}; (2) professional {name, service_id: svc.id, business_id} (el puntero 1:1 de D-06); (3) si sharedSpaceIds vacío → space dedicado {name, business_id}, si no usa los sharedSpaceIds existentes; (4) agenda_spaces filas {business_id, professional_id, space_id} por cada space.
    - behavior: ante falla parcial, provisionCancha revierte lo creado antes (borra agenda_spaces/space dedicado/professional/service ya insertados, filtrando por business_id) y devuelve un resultado de error discriminado ({ ok:false, error }), nunca deja "media cancha".
    - behavior: canchasFromData(services, professionals, agendaSpaces) reconstruye cada cancha emparejando professional.service_id === service.id (NUNCA por nombre), devolviendo la tupla { service, professional, spaceIds }.
    - behavior: deleteCancha hace soft-delete por defecto (active=false en el service Y en el professional, para que la agenda salga del booking) y solo hard-deletea si se pide y no hay reservas; el delete filtra por business_id (defensa en profundidad) y maneja el FK 23503 (turnos asociados → sugerir desactivar).
    - source: cada insert/update/delete en lib/canchas.ts pasa por un client inyectado (no importa createClient directo) para ser testeable con un mock, y SIEMPRE incluye business_id.
    - test: `npm test -- canchas-provision` pasa (ver Task 3).
  </acceptance_criteria>
  <behavior>
    - provisionCancha (happy path, espacio dedicado): inserta 1 service + 1 professional(service_id=svc.id) + 1 space + 1 agenda_space, todos con business_id → devuelve { ok:true, service, professional, spaceIds:[sp.id] }.
    - provisionCancha (compartir espacio, caso F11→{A,B,C}): con sharedSpaceIds=[A,B,C] NO crea space nuevo, inserta 3 filas agenda_spaces mapeando la agenda a A,B,C.
    - provisionCancha (falla en el paso space): revierte professional y service ya creados (deletes filtrando por business_id) → { ok:false }.
    - provisionCancha (falla en agenda_spaces): revierte space dedicado + professional + service → { ok:false }.
    - canchasFromData: dado services=[s1,s2], professionals=[p1(service_id=s1.id), p2(service_id=s2.id), p3(service_id=null)], empareja s1↔p1 y s2↔p2, ignora p3 (no es cancha) → 2 canchas; renombrar s1.name NO rompe el match (se empareja por id, no por nombre).
    - deleteCancha (soft, default): setea active=false en service y professional de la cancha.
    - deleteCancha (hard, sin reservas): borra agenda_spaces, space dedicado, professional, service en orden; si professional tiene turnos (FK 23503) devuelve error sugiriendo desactivar.
  </behavior>
  <action>
    Crear lib/canchas.ts como capa pura (sin React, sin DOM), kebab-case, named exports, comentarios en español — orquesta los mismos inserts que ya viven en SettingsClient pero aislados y testeables. Recibe el SupabaseClient por parámetro (inyección) para ser mockeable; NUNCA importa createClient directo. provisionCancha(client, businessId, input): implementa la secuencia service→professional(service_id)→space(s)→agenda_spaces del snippet de RESEARCH Pregunta 2, PERO usando la columna real service_id (D-06) en vez de specialty — cada insert con business_id explícito (la RLS lo valida; defensa en profundidad), rollback manual en cada paso (borrar lo anterior filtrando por business_id si el siguiente falla, Pitfall 1), resultado discriminado { ok, ... }. canchasFromData(services, professionals, agendaSpaces): empareja por professional.service_id === service.id (puntero estable de D-06, NUNCA por nombre — Pitfall 2), devuelve la lista de canchas reconstruidas con sus spaceIds derivados de agendaSpaces filtrados por professional_id. deleteCancha(client, businessId, cancha, { hard }): soft por defecto (active=false en service Y professional — replica toggleService 380-383 extendido al professional, para que la agenda salga del booking, D-05); hard solo si se pide, en orden agenda_spaces→space dedicado→professional→service, filtrando por business_id, mapeando FK 23503 a un error de dominio "tiene reservas, desactivala". Documentar en cabecera el caveat de atomicidad (sin RPC/transacción, mismo nivel de garantía que el resto del dashboard; rollback mitiga pero no garantiza) y el racional de por qué el linkeo es por service_id (D-06). NO escribir UI acá (eso es Plan 02). NO crear server actions ni RPC (rompe el patrón del repo + exigiría migración).
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npm test -- canchas-provision 2>&1 | tail -15</automated>
  </verify>
  <done>lib/canchas.ts exporta provisionCancha/deleteCancha/canchasFromData; provisión con rollback; reconstrucción por service_id (no por nombre); soft/hard delete con business_id y FK 23503; tsc y los tests verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de la capa de canchas (provisión, reconstrucción, rollback)</name>
  <files>test/canchas-provision.test.ts</files>
  <read_first>
    - test/booking-core.test.ts (estilo de test de lógica pura del repo, vitest)
    - test/isolation.test.ts líneas 1-60 (patrón de aislamiento con 2 clientes anon-key — referencia para el criterion de aislamiento; usar describe.skipIf(!hasSupabaseCreds) si se toca Supabase real)
    - test/helpers/supabase-fixtures.ts (seedTwoTenants/teardown — para el test de aislamiento opcional)
    - lib/canchas.ts (la unidad bajo test, de Task 2)
  </read_first>
  <acceptance_criteria>
    - source: existe test/canchas-provision.test.ts (vitest, describe/it/expect).
    - test: cubre los casos del bloque <behavior> de Task 2: happy path (dedicado + compartido), rollback en falla del paso space, rollback en falla de agenda_spaces, reconstrucción por service_id, reconstrucción robusta al renombrar (mismo id), soft-delete (active=false en ambos), hard-delete con FK 23503.
    - behavior: los tests de provisión/reconstrucción/rollback usan un client MOCK (objeto que simula from().insert().select().single() y delete().eq()) → corren sin Supabase real ni DOM, siempre en CI.
    - test: el criterion de aislamiento por tenant (si se incluye como integration) usa SOLO clientes anon-key autenticados como cada dueño (NUNCA service-role en las aserciones — patrón isolation.test.ts), envuelto en describe.skipIf(!hasSupabaseCreds) para no romper CI sin secrets.
    - test: `npm test -- canchas-provision` pasa; `npm test` (suite completa, 302+ tests) sigue verde (cero regresión).
  </acceptance_criteria>
  <behavior>
    - test "provisiona con espacio dedicado": mock client; assert que se llamaron inserts a services, professionals (con service_id = id del service mockeado), spaces, agenda_spaces, cada uno con business_id; resultado { ok:true }.
    - test "comparte espacio (F11)": con sharedSpaceIds=[A,B,C]; assert NO insert a spaces y 3 inserts a agenda_spaces (A,B,C).
    - test "rollback si falla space": mock que devuelve error en el insert de spaces; assert que se llamó delete de professionals y services (rollback), resultado { ok:false }.
    - test "rollback si falla agenda_spaces": assert delete de space dedicado + professional + service.
    - test "reconstruye por service_id, no por nombre": canchasFromData con professional.service_id apuntando al service; cambiar service.name no rompe el emparejamiento.
    - test "ignora professionals sin service_id": p con service_id null no produce cancha.
    - test "soft-delete pone active=false en service y professional".
    - test "hard-delete con turnos (FK 23503) devuelve error de dominio sugiriendo desactivar".
  </behavior>
  <action>
    Crear test/canchas-provision.test.ts cubriendo el bloque <behavior> de arriba. Para provisión/reconstrucción/rollback: construir un client MOCK (un objeto cuyo .from(tabla) devuelve un encadenable .insert/.select/.single/.update/.delete/.eq configurable para simular éxito o error por tabla), sin tocar Supabase real — así corre siempre en CI y es la unidad que valida la orquestación + rollback de lib/canchas.ts. Para reconstrucción: pasar arrays de Service/Professional/AgendaSpace en memoria y assertear el emparejamiento por service_id (incluido el caso "renombrar el service NO rompe el match"). OPCIONAL (si se decide hacer integration de aislamiento): un describe.skipIf(!hasSupabaseCreds) que use seedTwoTenants y 2 clientes anon-key (dueño A / dueño B) para verificar que A no puede crear/mapear una cancha con business_id de B (la RLS lo rechaza) — copiar el patrón anti-falso-verde de isolation.test.ts (NUNCA service-role en las aserciones). Mantener los nombres de tests en español como el resto de la suite. Verificar que la suite completa sigue verde (cero regresión sobre los 302+ tests del milestone v0.12).
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npm test -- canchas-provision 2>&1 | tail -12 && npm test 2>&1 | tail -8</automated>
  </verify>
  <done>test/canchas-provision.test.ts cubre provisión (dedicado/compartido), rollback, reconstrucción por service_id (robusta al renombrar), soft/hard delete; los tests pasan con client mock; la suite completa sigue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño autenticado (browser) → Supabase (RLS) | El dashboard escribe con browser client + anon key; la autoridad de tenant es la RLS por business_id. La capa lib/canchas.ts orquesta esos writes. |
| migración 043 → esquema prod | La columna aditiva se aplica a prod a mano; ventana de error humano (aplicar fuera de orden / olvidar NOTIFY pgrst). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation | provisionCancha inserts (service/professional/space/agenda_space) | mitigate | Cada insert setea business_id explícito y la RLS existente (policy "business member access" de services/professionals; 4-policies WITH CHECK de spaces/agenda_spaces) rechaza a nivel DB cualquier business_id ajeno. Defensa en profundidad: los deletes/updates filtran por .eq('business_id'). NO se agrega policy nueva (no hace falta — riesgo no alcanza high). |
| T-02-02 | Integrity | Auto-provisión sin transacción → "media cancha" (filas huérfanas) | mitigate | Rollback manual explícito en cada paso de provisionCancha (borrar lo anterior filtrando por business_id). Caveat documentado: mismo nivel de garantía que el resto del dashboard (sin RPC para no migrar más allá de 043). Riesgo residual aceptado (no es cross-tenant; es consistencia intra-tenant). |
| T-02-03 | Tampering | Migración 043 (columna nueva sobre professionals) | accept | Columna aditiva nullable, sin policy nueva, hereda la RLS de professionals (ya por business_id). No abre superficie cross-tenant. ON DELETE SET NULL evita FK roto si se borra el service. Riesgo bajo, no alcanza high. |
| T-02-04 | Information Disclosure | service_id como puntero interno | accept | service_id es un uuid interno del propio tenant; no se expone a anon en esta fase (Plan 02 es 100% dashboard autenticado; ninguna lectura anon nueva). La exposición pública es scope de Phase 3 (vista acotada). |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 NO instala paquetes (RESEARCH: "cero dependencias nuevas"; `npm install` no se ejecuta). Sin superficie supply-chain nueva. |

**Evaluación de umbral (blocking threshold: high):** Ninguna amenaza de este plan alcanza `high`. La columna 043 es aditiva sobre una tabla ya RLS por business_id (sin policy nueva); la auto-provisión setea business_id en cada fila y la RLS lo valida (cross-tenant write rechazado a nivel DB). El único riesgo material es de integridad intra-tenant (media cancha), mitigado por rollback manual. Se declara explícitamente: **no hay amenazas high en este plan.**
</threat_model>

<verification>
- `supabase db reset` replaya baseline + 040 + 041 + 042 + 043 sin error (PG17 local).
- `npx tsc --noEmit` limpio (tipo Professional.service_id correcto).
- `npm run lint` sin errores nuevos.
- `npm test -- canchas-provision` verde; `npm test` (suite completa) verde (cero regresión sobre 302+ tests).
- professionals.service_id es nullable; ninguna fila de salud/belleza/general recibe service_id (verificable: la migración no hace backfill).
- supabase/schema.sql regenerado refleja la columna.
</verification>

<success_criteria>
- Migración 043 aditiva validada local; lista para aplicar a prod a mano (user_setup declarado).
- lib/canchas.ts: provisión con rollback, reconstrucción por service_id (no por nombre), soft/hard delete, todo con business_id.
- Tests verdes con client mock; aislamiento opcional con anon-key (sin falso verde).
- Cero regresión en otros verticales (service_id null) y en la suite existente.
</success_criteria>

<output>
Create `.planning/workstreams/canchas/phases/02-configuraci-n-de-canchas/02-01-SUMMARY.md` when done
</output>
