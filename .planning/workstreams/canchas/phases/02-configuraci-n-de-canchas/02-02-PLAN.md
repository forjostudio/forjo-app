---
phase: 02-configuraci-n-de-canchas
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/(dashboard)/servicios/page.tsx
  - app/(dashboard)/settings/settings-client.tsx
  - components/dashboard/canchas-manager.tsx
autonomous: false
requirements: [CANCHA-01, CANCHA-02, CANCHA-03]

must_haves:
  truths:
    - "En un negocio canchas, /servicios muestra el manager de canchas: alta de cancha (nombre + precio propio + duración fija propia), lista, edición y borrado, y mapeo a espacios"
    - "Crear una cancha en la UI dispara provisionCancha → service + professional(service_id) + space + agenda_space, todos con business_id (D-02)"
    - "Dos canchas pueden tener duraciones distintas y cada una conserva la suya (D-01 + CANCHA-02)"
    - "El control 'compartir espacio' (D-04) permite mapear una cancha a espacios existentes (caso F11→{A,B,C}); por defecto cada cancha crea su espacio dedicado 1:1"
    - "Borrar una cancha hace soft-delete (active=false) y la saca del listado de alta (D-05)"
    - "view='equipo' (salud/belleza/general) sigue funcionando idéntico — cero regresión"
  artifacts:
    - path: "app/(dashboard)/servicios/page.tsx"
      provides: "Carga professionals/spaces/agenda_spaces (hoy vacíos) por tenant para alimentar el manager de canchas"
      contains: "agenda_spaces"
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "Render de canchas en view='servicios' cuando vertical=canchas: form de cancha + provisionCancha + mapeo de espacios + soft-delete; el bloque de espacios alcanzable desde /servicios para canchas"
      contains: "canchas"
  key_links:
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "lib/canchas.ts"
      via: "el form de alta de cancha llama provisionCancha y la lista usa canchasFromData / deleteCancha"
      pattern: "provisionCancha|canchasFromData|deleteCancha"
    - from: "app/(dashboard)/servicios/page.tsx"
      to: "app/(dashboard)/settings/settings-client.tsx"
      via: "pasa initialProfessionals/initialSpaces/initialAgendaSpaces al SettingsClient"
      pattern: "initialSpaces|initialAgendaSpaces"
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "lib/verticals.ts"
      via: "resolveVertical(business).key === 'canchas' gatea el render de canchas"
      pattern: "resolveVertical"
  prohibitions:
    - "NO remover ni romper el bloque de espacios del TabsContent value='professionals' (view='equipo'): salud/belleza/general lo siguen usando. La migración a /servicios es ADITIVA y condicionada por vertical=canchas (Pitfall 4)."
    - "NO listar las agendas-cancha como 'Equipo' ni exponer campos de staff (specialty/license/phone/email) en la UI de cancha (Pitfall 3 — leak). Presentarlas como 'Canchas' (term.resource)."
    - "NO mostrar el service_id en la UI (es puntero interno)."
    - "NO emparejar la cancha con su agenda por nombre — usar canchasFromData (que empareja por service_id)."
    - "NO crear server actions ni RPC (patrón del repo: browser client + RLS). NO crear ruta nueva ni tocar el menú de Phase 1."
    - "NO insertar ninguna fila sin business_id."
---

<objective>
Construir la UI del manager de canchas en `/servicios` (D-03), consumiendo la capa `lib/canchas.ts` del Plan 01: cargar los datos que hoy faltan en `/servicios/page.tsx` (professionals/spaces/agenda_spaces), y renderizar —cuando el vertical es canchas— el alta de cancha (nombre + precio propio + duración fija propia, D-01), la lista con edición y soft-delete (D-05/CANCHA-02), y el mapeo a espacios con el control "auto 1:1 por defecto + compartir opcional" (D-04). La config de espacios que hoy vive (inalcanzable para canchas) en `view='equipo'` queda alcanzable desde `/servicios` para canchas, SIN romper ese camino para los otros verticales.

Purpose: es la cara que ve el dueño. Cierra los 4 criterios del ROADMAP a nivel UX: crear con precio+duración propios y asociar espacios (C1/CANCHA-01), editar/eliminar con duración variable (C2/CANCHA-02), mapeo cancha→espacios que acopla la disponibilidad del motor v0.12 (C3/CANCHA-03), todo aislado por tenant (C4).

Output: `app/(dashboard)/servicios/page.tsx` (carga ampliada), `app/(dashboard)/settings/settings-client.tsx` (render de canchas condicionado por vertical).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/canchas/ROADMAP.md
@.planning/workstreams/canchas/phases/02-configuraci-n-de-canchas/02-CONTEXT.md
@.planning/workstreams/canchas/phases/02-configuraci-n-de-canchas/02-RESEARCH.md

# El plan previo aporta la capa de datos que esta UI consume
@.planning/workstreams/canchas/phases/02-configuraci-n-de-canchas/02-01-PLAN.md

# Fuentes de verdad a tocar / replicar
@app/(dashboard)/servicios/page.tsx
@app/(dashboard)/equipo/page.tsx
@lib/verticals.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: /servicios/page.tsx — cargar professionals/spaces/agenda_spaces por tenant</name>
  <files>app/(dashboard)/servicios/page.tsx</files>
  <read_first>
    - app/(dashboard)/servicios/page.tsx (29 líneas — hoy carga solo services+locations y pasa initialProfessionals=[], sin spaces/agendaSpaces)
    - app/(dashboard)/equipo/page.tsx líneas 22-36 (el Promise.all exacto a copiar: professionals/spaces/agenda_spaces con .eq('business_id', business.id), y el shape de props que espera SettingsClient)
    - app/(dashboard)/settings/settings-client.tsx líneas 119-139 (interface Props: initialSpaces/initialAgendaSpaces opcionales)
  </read_first>
  <acceptance_criteria>
    - source: app/(dashboard)/servicios/page.tsx carga professionals, spaces y agenda_spaces con un Promise.all, cada query filtrada por .eq('business_id', business.id) (aislamiento por tenant, defensa en profundidad sobre la RLS).
    - source: pasa initialProfessionals={professionals||[]}, initialSpaces={spaces||[]}, initialAgendaSpaces={agendaSpaces||[]} al SettingsClient (ya no initialProfessionals={[]}).
    - behavior: para salud/belleza/general, /servicios sigue mostrando el CRUD de servicios igual que antes (la carga extra no cambia su render; el manager de canchas se gatea por vertical en Task 2). Cero regresión.
    - test: `npx tsc --noEmit` limpio; `npm run lint` sin errores nuevos.
  </acceptance_criteria>
  <action>
    Adaptar app/(dashboard)/servicios/page.tsx con Edit parcial (NO reescribir el archivo). Ampliar el Promise.all existente para cargar también professionals, spaces y agenda_spaces, copiando el patrón exacto de equipo/page.tsx:22-26 (cada uno con .eq('business_id', business.id) y .order('created_at') donde corresponda). Pasar esos tres como initialProfessionals/initialSpaces/initialAgendaSpaces al SettingsClient (reemplazar el initialProfessionals={[]} hardcodeado). Mantener view="servicios" y el resto igual. NO redirigir por vertical acá (a diferencia de /equipo): /servicios sirve a TODOS los verticales; el manager de canchas se condiciona dentro del componente (Task 2). Esto habilita que la UI de cancha tenga los datos de espacios/agendas que necesita.
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npm run lint 2>&1 | tail -5 && grep -q "agenda_spaces" "app/(dashboard)/servicios/page.tsx" && grep -q "initialSpaces" "app/(dashboard)/servicios/page.tsx" && echo "carga ampliada OK"</automated>
  </verify>
  <done>/servicios/page.tsx carga professionals/spaces/agenda_spaces por tenant y los pasa al SettingsClient; tsc+lint limpios; otros verticales sin cambios de render.</done>
</task>

<task type="auto">
  <name>Task 2: Manager de canchas en settings-client.tsx (form + mapeo + soft-delete) condicionado por vertical</name>
  <files>app/(dashboard)/settings/settings-client.tsx, components/dashboard/canchas-manager.tsx</files>
  <read_first>
    - app/(dashboard)/settings/settings-client.tsx líneas 351-420 (state + addService/saveEditService/toggleService — el form de service a adaptar para cancha)
    - app/(dashboard)/settings/settings-client.tsx líneas 553-622 (spaces/agenda_spaces state + addSpace/deleteSpace/toggleAgendaSpace/isMapped — el mapeo a reusar)
    - app/(dashboard)/settings/settings-client.tsx líneas 1149-1262 (TabsContent value='services' — el render a adaptar para canchas) y 1339-1441 (bloque de espacios del tab professionals — NO remover; alcanzable desde /servicios para canchas)
    - app/(dashboard)/settings/settings-client.tsx líneas 159-162 (term = resolveVertical(business).terminology; term.resource='Cancha' para canchas)
    - lib/canchas.ts (provisionCancha, canchasFromData, deleteCancha del Plan 01 — la capa que el form consume)
    - lib/verticals.ts líneas 104-124 (vertical canchas: service→Cancha, menú sin equipo) y 163-174 (resolveVertical)
    - 02-RESEARCH.md Pregunta 3 (qué adaptar/migrar) + Pitfall 3 (leak Equipo) + Pitfall 4 (no romper view='equipo')
  </read_first>
  <acceptance_criteria>
    - behavior: cuando resolveVertical(business).key === 'canchas' y view='servicios', el TabsContent renderiza el manager de canchas (no el CRUD genérico de service): form de alta con Nombre + Precio (su propio precio) + Duración fija (sus minutos) + control opcional de espacios; lista de canchas reconstruidas con canchasFromData; edición; soft-delete.
    - behavior: el alta de cancha llama provisionCancha(supabase, business.id, { name, price, duration, sharedSpaceIds }) — NO addService suelto — y actualiza el estado local (services/professionals/spaces/agendaSpaces) con el resultado; toast de éxito/error.
    - behavior: control "compartir espacio" (D-04): por defecto vacío → provisionCancha crea un space dedicado 1:1; si el dueño selecciona espacios existentes (dropdown/chips de spaces del negocio), se pasan como sharedSpaceIds y NO se crea space nuevo (caso F11→{A,B,C}).
    - behavior: editar una cancha edita su service (name/price/duration_minutes) reusando el flujo de saveEditService, filtrando por business_id; dos canchas conservan duraciones distintas e independientes.
    - behavior: borrar una cancha llama deleteCancha (soft por defecto, active=false) y la cancha sale del listado de alta; ConfirmDialog antes de borrar (patrón del repo).
    - source: para vertical != canchas, view='servicios' renderiza EXACTAMENTE el CRUD de services de hoy (sin cambios) y view='equipo' renderiza el bloque de espacios intacto. El bloque de espacios (líneas 1339-1441) NO se remueve.
    - source: la UI de cancha NO muestra service_id, ni campos de staff (specialty/license/phone/email); presenta el eje como term.resource ('Cancha'). (Pitfall 3.)
    - test: `npx tsc --noEmit` + `npm run lint` limpios; `npm test` (suite completa) verde (cero regresión).
  </acceptance_criteria>
  <action>
    Adaptar app/(dashboard)/settings/settings-client.tsx con Edits parciales (NO reescribir el archivo de 1808 líneas). Detectar el vertical con resolveVertical(business).key === 'canchas' (ya importado, línea 24). Cuando es canchas y view='servicios', renderizar un manager de canchas en lugar del CRUD genérico de service. Discreción de organización (D-03 dice "adaptada"): se recomienda extraer el render de canchas a un sub-componente (ej. components/dashboard/canchas-manager.tsx) que reciba business + los estados y handlers, para no inflar más el componente; alternativamente, condicionar in-place dentro del TabsContent value='services'. El form de alta usa Nombre + Precio + Duración (reusar los Inputs de precio/min del service, validar precio>0 y duración>0 siguiendo el patrón de validación de services — guard if(!name) y min en los inputs) + el control opcional de espacios; al confirmar llama provisionCancha del Plan 01 y mergea el resultado al estado local (services/professionals/spaces/agendaSpaces). La lista usa canchasFromData(services, professionals, agendaSpaces) para reconstruir cada cancha por service_id (NUNCA por nombre) y muestra nombre + "{duration}min · ${price}" (como el render actual de service, línea 1161) con botones desactivar/editar/borrar. El control "compartir espacio" (D-04): por defecto crea space dedicado (sharedSpaceIds vacío); ofrecer un control avanzado (chips/checkboxes de los spaces existentes del negocio, reusando isMapped/toggleAgendaSpace para canchas ya creadas y un selector en el alta) para mapear a espacios compartidos. El borrado usa deleteCancha (soft default) detrás de un ConfirmDialog (patrón de delService, línea 1169/1785-1804). CRÍTICO (Pitfall 4): NO remover ni alterar el bloque de espacios del TabsContent value='professionals' (líneas 1339-1441) — salud/belleza/general lo siguen usando vía view='equipo'; la migración a /servicios es aditiva y SOLO para canchas. CRÍTICO (Pitfall 3): presentar las agendas-cancha como 'Canchas' (term.resource), NO como 'Equipo'; no exponer service_id ni campos de staff. Mantener comentarios en español. NO crear server actions/RPC ni ruta nueva.
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npm run lint 2>&1 | tail -5 && grep -Eq "provisionCancha|canchasFromData|deleteCancha" "app/(dashboard)/settings/settings-client.tsx" && grep -q "value=\"professionals\"" "app/(dashboard)/settings/settings-client.tsx" && echo "manager canchas + bloque equipo intacto OK" && npm test 2>&1 | tail -6</automated>
  </verify>
  <done>El manager de canchas vive en /servicios para vertical=canchas (form con precio/duración propios, mapeo de espacios D-04, soft-delete D-05) consumiendo lib/canchas.ts; el bloque de espacios de view='equipo' queda intacto para los otros verticales; tsc+lint+suite verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verificación humana del manager de canchas</name>
  <action>Pausar para verificación humana: el dueño confirma manualmente, sobre la app corriendo, que el manager de canchas funciona end-to-end (alta con precio/duración propios, duraciones independientes entre canchas, edición, mapeo de espacios 1:1 + compartir, soft-delete) y que no hay regresión en salud/belleza/general (/servicios y /equipo intactos). Seguir los pasos de <how-to-verify>.</action>
  <what-built>Manager de canchas en /servicios para un negocio del vertical canchas: alta de cancha (nombre + precio propio + duración fija propia), lista con edición y soft-delete, y mapeo a espacios (auto 1:1 por defecto + compartir opcional). La config de espacios que antes vivía en view='equipo' (inalcanzable para canchas) ahora es alcanzable desde /servicios. Los otros verticales (salud/belleza/general) no deberían ver cambios.</what-built>
  <how-to-verify>
    1. Iniciá la app: `npm run dev`. Logueate con un negocio del vertical canchas (o seteá su rubro a "Cancha de fútbol" en /negocio y recargá).
    2. Andá a /servicios (rotulado "Canchas" en el menú). Verificá que ves el manager de canchas, NO el CRUD genérico de servicios.
    3. Creá una cancha "Cruzada A" con precio y duración 60min. Confirmá que aparece en la lista mostrando "60min · $<precio>".
    4. Creá una segunda cancha "Cancha 11" con duración 90min. Confirmá que cada una conserva SU duración (60 vs 90) — son independientes.
    5. Editá "Cruzada A" (cambiá precio o nombre) → persiste y no afecta a "Cancha 11".
    6. Probá el control "compartir espacio": creá "Cancha 11" mapeada a los espacios de A/B/C (si ya existen) o verificá que cada cancha nueva crea su espacio dedicado por defecto.
    7. Borrá (soft-delete) una cancha → desaparece del listado de alta (queda inactiva).
    8. REGRESIÓN: logueate (o cambiá el rubro) a un negocio salud/belleza/general. Confirmá que /servicios muestra el CRUD de servicios de siempre y que /equipo sigue mostrando el bloque de espacios físicos intacto.
  </how-to-verify>
  <resume-signal>Escribí "approved" si todo funciona, o describí qué falló (ej. "la segunda cancha tomó la duración de la primera", "se rompió /equipo en salud").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dueño autenticado (browser) → Supabase (RLS) | El manager de canchas escribe con browser client + anon key; la autoridad de tenant es la RLS por business_id. Consume lib/canchas.ts (Plan 01). |
| RSC /servicios/page.tsx → Supabase (cookies del dueño) | La carga server-side filtra por business_id + RLS; alimenta el componente client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering / Elevation | Alta/edición/borrado de cancha desde el browser | mitigate | Toda escritura pasa por provisionCancha/deleteCancha (Plan 01) que setea business_id en cada fila; la RLS de services/professionals/spaces/agenda_spaces rechaza business_id ajeno. La carga en page.tsx filtra por .eq('business_id'). Sin policy nueva. |
| T-02-06 | Information Disclosure | Listar agendas-cancha como 'Equipo' / exponer campos de staff o service_id | mitigate | Presentar el eje como term.resource ('Cancha'); no renderizar specialty/license/phone/email ni service_id en la UI de cancha (Pitfall 3). El redirect de /equipo para canchas (Phase 1) sigue vigente. |
| T-02-07 | Tampering | Mapear cancha a un space de otro tenant (cross-tenant agenda_spaces) | mitigate | Los spaces ofrecidos en el control "compartir espacio" salen de los initialSpaces cargados por .eq('business_id'); el insert de agenda_spaces lo valida la 4-policy WITH CHECK de la tabla (rechaza space_id/business_id ajeno). No se debilita el acople. |
| T-02-08 | Information Disclosure | Exposición a anon | accept | Plan 02 es 100% dashboard autenticado; NO agrega ninguna lectura/vista anon. La exposición pública de la cancha es scope de Phase 3 (vista acotada). No alcanza high. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 no instala paquetes (RESEARCH: cero dependencias nuevas). Sin superficie supply-chain. |

**Evaluación de umbral (blocking threshold: high):** Ninguna amenaza de este plan alcanza `high`. Toda escritura cross-tenant la rechaza la RLS existente a nivel DB (las opciones de espacio salen de datos ya filtrados por business_id); el leak de 'Equipo' se mitiga presentando 'Cancha' y ocultando campos de staff. Se declara explícitamente: **no hay amenazas high en este plan.**
</threat_model>

<verification>
- `npx tsc --noEmit` limpio.
- `npm run lint` sin errores nuevos.
- `npm test` (suite completa, 302+ tests) verde — cero regresión.
- Checkpoint humano: alta/edición/soft-delete de cancha con precio+duración propios y duraciones independientes; mapeo de espacios (1:1 default + compartir); regresión OK en salud/belleza/general (/servicios y /equipo intactos).
</verification>

<success_criteria>
- /servicios es el manager de canchas para el vertical canchas (D-03), consumiendo lib/canchas.ts.
- Crear cancha auto-provisiona la tupla con business_id (D-02); precio+duración propios por cancha (D-01); mapeo de espacios 1:1 + compartir (D-04); soft-delete (D-05).
- view='equipo' intacto para salud/belleza/general; agendas-cancha presentadas como 'Canchas', sin leak de staff ni service_id.
- tsc + lint + suite verdes; checkpoint humano aprobado.
</success_criteria>

<output>
Create `.planning/workstreams/canchas/phases/02-configuraci-n-de-canchas/02-02-SUMMARY.md` when done
</output>
