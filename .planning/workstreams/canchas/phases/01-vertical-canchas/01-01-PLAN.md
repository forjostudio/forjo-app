---
phase: 01-vertical-canchas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/verticals.ts
  - app/(dashboard)/equipo/page.tsx
autonomous: true
requirements: [VERT-01, VERT-02]

must_haves:
  truths:
    - "Un negocio con vertical 'canchas' resuelve a un VerticalConfig propio con terminologia 'Reserva'/'Cancha'/'Sede' (D-01, D-04)."
    - "El select de rubro (onboarding y settings) muestra el grupo 'Canchas' y permite guardar vertical='canchas' sin tocar esas pantallas (D-01)."
    - "El menu del dashboard de un negocio canchas NO incluye el item 'Equipo' ni 'Profesionales' (D-02)."
    - "Escribir /equipo a mano en un negocio canchas redirige a /dashboard via guard server-side (D-05)."
    - "Los verticales salud/belleza/general conservan menu y terminologia byte-identicos respecto a antes del cambio (D-07)."
  prohibitions:
    - "VERTICALS.canchas.menu NO contiene 'equipo' ni 'patients'."
    - "lib/verticals.ts NO contiene el simbolo TYPE_TERMINOLOGY_OVERRIDE tras el cambio."
    - "general.types NO contiene 'Cancha de futbol'."
    - "Los menus y terminology de salud/belleza/general NO cambian (cero regresion)."
    - "lib/verticals.ts NO importa React ni iconos (sigue framework-agnostico, D-03)."
  artifacts:
    - path: "lib/verticals.ts"
      provides: "VerticalKey extendido con 'canchas' + entrada VERTICALS.canchas + removal de TYPE_TERMINOLOGY_OVERRIDE"
      contains: "canchas:"
    - path: "app/(dashboard)/equipo/page.tsx"
      provides: "Guard server-side: redirect a /dashboard si vertical canchas"
      contains: "resolveVertical"
  key_links:
    - from: "app/(dashboard)/layout.tsx"
      to: "lib/verticals.ts"
      via: "resolveVertical(business) -> VerticalProvider inyecta la terminologia de canchas a todo el dashboard"
      pattern: "resolveVertical"
    - from: "components/dashboard/sidebar.tsx"
      to: "lib/verticals.ts"
      via: "buildNav mapea v.menu (sin 'equipo') -> el sidebar de canchas no renderiza Equipo"
      pattern: "v\\.menu\\.map"
    - from: "app/(dashboard)/equipo/page.tsx"
      to: "lib/verticals.ts"
      via: "resolveVertical(business).key === 'canchas' -> redirect('/dashboard')"
      pattern: "resolveVertical\\(business\\)\\.key"
---

<objective>
Agregar el vertical 'canchas' como `VerticalKey` de primera clase (Opcion A, D-01) al sistema
de verticales existente, con su terminologia ("Reserva + Cancha", D-04) y su menu propio sin
"Profesionales/Equipo" (D-02), mas un guard server-side anti-/equipo (D-05). El cambio es
declarativo: vive casi por completo en `lib/verticals.ts` (que ya es framework-agnostico) mas
una guarda de pagina. Las pantallas de onboarding/settings y el sidebar lo agarran
automaticamente porque derivan todo de `VERTICALS`.

Purpose: deja terminologia/menu del rubro canchas listos para que Phase 2 (config de cancha) y
Phase 3 (booking publico) cuelguen de ellos, garantizando CERO regresion en salud/belleza/general
(D-07).

Output: `VerticalKey` con 'canchas', entrada `VERTICALS.canchas`, types de canchas, removal de
`TYPE_TERMINOLOGY_OVERRIDE`, y guard en `app/(dashboard)/equipo/page.tsx`.
</objective>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**Simbolos CREADOS:**
- `'canchas'` agregado al union type `VerticalKey` en `lib/verticals.ts`.
- Entrada `canchas` en el objeto `VERTICALS` (un `VerticalConfig` completo: label, types, terminology, menu, features).
- Nuevos `types` del vertical canchas: `'Cancha de futbol'`, `'Cancha de padel'`, `'Cancha de tenis'`, `'Cancha de basquet'`, `'Otro'` (resuelto de Claude's Discretion — deportes mas comunes en AR + 'Otro' consistente con general).
- Guard server-side en `app/(dashboard)/equipo/page.tsx`: `if (resolveVertical(business).key === 'canchas') redirect('/dashboard')`.

**Simbolos ELIMINADOS:**
- `TYPE_TERMINOLOGY_OVERRIDE` (constante completa) — ya no se necesita: el vertical `canchas` posee la terminologia "Cancha" nativamente (D-06).
- `'Cancha de futbol'` removido de `general.types` (pasa a `canchas.types`, D-06).
- Las ramas de codigo en `getVertical` y `resolveVertical` que mergeaban el override por type (el spread de terminology vuelve a ser directo, sin `...(override ?? {})`).

**Simbolos DERIVADOS automaticamente (sin tocar su definicion):**
- `TYPE_GROUPS` gana el grupo "Canchas" (deriva de `VERTICALS`) → aparece en el select de onboarding/settings.
- `ALL_BUSINESS_TYPES` gana los nuevos types de canchas (deriva de `VERTICALS`) — Claude's Discretion: se incluyen (sin consumidor activo en codigo, no es regresion).
- `getVerticalKeyByType('Cancha de futbol')` ahora devuelve `'canchas'` (antes `'general'`).
</artifacts_this_phase_produces>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/canchas/ROADMAP.md
@.planning/workstreams/canchas/STATE.md
@.planning/workstreams/canchas/phases/01-vertical-canchas/01-CONTEXT.md

# Fuente de verdad del sistema de verticales (patron a replicar)
@lib/verticals.ts
@lib/use-terminology.tsx

# Consumidores del menu/terminologia (puntos de regresion — leer para NO romperlos)
@components/dashboard/sidebar.tsx
@app/(dashboard)/layout.tsx
@app/(dashboard)/settings/settings-client.tsx
@app/(onboarding)/onboarding/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar el vertical 'canchas' a VerticalKey + VERTICALS con terminologia y menu propios</name>
  <files>lib/verticals.ts</files>
  <read_first>
    - lib/verticals.ts (el archivo a modificar — ver el shape exacto de VerticalConfig, las
      entradas salud/belleza/general como plantilla, y como VERTICALS deriva TYPE_GROUPS /
      ALL_BUSINESS_TYPES).
    - components/dashboard/sidebar.tsx (§buildNav, objeto ITEMS lineas 37-49): la lista de keys
      de menu VALIDAS hoy es exactamente: dashboard, appointments, agenda, negocio, servicios,
      equipo, consultorios, clients, patients, finances, settings. El menu de canchas DEBE usar
      solo keys de esa lista; los keys desconocidos se filtran con .filter(Boolean) pero no deben
      introducirse keys nuevos sin entrada en ITEMS.
  </read_first>
  <action>
    Agregar el literal 'canchas' al union type `VerticalKey` (queda
    `'salud' | 'belleza' | 'general' | 'canchas'`).

    Agregar una entrada `canchas` al objeto `VERTICALS` con la misma forma que las existentes:
    - `label`: 'Canchas'.
    - `types`: ['Cancha de futbol', 'Cancha de padel', 'Cancha de tenis', 'Cancha de basquet', 'Otro'].
      (Usar la ortografia con tilde que ya usa el repo: 'Cancha de futbol' debe escribirse igual
      que el literal que hoy esta en general.types — copialo tal cual para que el cutover sea exacto.)
    - `terminology` (D-04, mapa "Reserva + Cancha"):
        client: 'Cliente', clients: 'Clientes',
        appointment: 'Reserva', appointments: 'Reservas',
        service: 'Cancha', services: 'Canchas',
        location: 'Sede', locations: 'Sedes',
        resource: 'Cancha', resources: 'Canchas'.
    - `menu` (D-02, sin 'equipo' ni 'patients'):
        ['dashboard', 'appointments', 'agenda', 'clients', 'finances', 'servicios', 'consultorios', 'negocio', 'settings'].
    - `features`: {} (objeto vacio, como general).

    En `general.types` (D-06): remover el string 'Cancha de futbol' del array, conservando el
    resto del array exactamente igual (orden y demas elementos intactos).

    NO importar React ni iconos (D-03: el archivo sigue framework-agnostico). NO tocar las
    entradas salud / belleza ni la terminology/menu de general (solo se quita ese type de
    general.types). NO redefinir TYPE_GROUPS ni ALL_BUSINESS_TYPES — derivan de VERTICALS y
    recogen 'canchas' solos.

    Actualizar el comentario de cabecera del campo `resource`/`resources` en la interface
    VerticalTerminology (lineas ~16-21) que hoy describe el mecanismo del override por type:
    reemplazar la referencia al override por una nota de que cada vertical define su eje de agenda
    directamente (canchas usa 'Cancha'/'Canchas'). El comentario NO debe nombrar el simbolo
    TYPE_TERMINOLOGY_OVERRIDE (ese simbolo se elimina en Task 2 y no debe quedar referenciado).
  </action>
  <acceptance_criteria>
    - SOURCE: `grep -n "'salud' | 'belleza' | 'general' | 'canchas'" lib/verticals.ts` devuelve la linea del type VerticalKey.
    - SOURCE: `grep -nc "canchas:" lib/verticals.ts` >= 1 (la clave de VERTICALS).
    - SOURCE: en el bloque VERTICALS.canchas, `appointment: 'Reserva'` y `resource: 'Cancha'` y `location: 'Sede'` estan presentes (grep de cada literal).
    - SOURCE (prohibicion): el array `menu` de canchas NO contiene los tokens 'equipo' ni 'patients' (revisar el array literal de VERTICALS.canchas.menu).
    - SOURCE (cutover): `general.types` NO contiene 'Cancha de futbol' (grep del literal dentro del array de general devuelve 0 ocurrencias en ese array; el literal solo debe quedar en canchas.types).
    - SOURCE (D-03): `grep -nE "from 'react'|lucide-react|import React" lib/verticals.ts` no devuelve coincidencias.
    - NO-REGRESION: las entradas `salud` y `belleza` quedan sin diff (su menu sigue conteniendo 'equipo' y su terminology sigue con resource: 'Profesional').
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit</automated>
  </verify>
  <done>VerticalKey incluye 'canchas'; VERTICALS.canchas existe con terminologia "Reserva+Cancha" y menu sin equipo/patients; 'Cancha de futbol' removido de general.types; tsc pasa sin errores nuevos.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar TYPE_TERMINOLOGY_OVERRIDE y limpiar sus usos en getVertical / resolveVertical</name>
  <files>lib/verticals.ts</files>
  <read_first>
    - lib/verticals.ts (lineas ~120-168): la constante `TYPE_TERMINOLOGY_OVERRIDE` y las funciones
      `getVertical` y `resolveVertical` que la consumen via `const override = ...` y el spread
      `terminology: { ...VERTICALS[key].terminology, ...(override ?? {}) }`. Confirmar que el
      override solo aporta 'Cancha'/'Canchas' al type 'Cancha de futbol', que ahora ya lo provee
      VERTICALS.canchas nativamente.
    - El grep de la fase confirmo que TYPE_TERMINOLOGY_OVERRIDE solo se usa dentro de
      lib/verticals.ts (no hay consumidores externos), y que ningun otro type lo usaba — solo
      'Cancha de futbol'. Es seguro borrarlo entero (D-06).
  </read_first>
  <action>
    Borrar por completo la declaracion de la constante `TYPE_TERMINOLOGY_OVERRIDE` (el Record
    entero, no dejar un Record vacio — borrado total, D-06 / Claude's Discretion).

    En `getVertical(businessType)`: remover la linea `const override = TYPE_TERMINOLOGY_OVERRIDE[businessType]`
    y simplificar el return para que `terminology` sea directamente `{ ...VERTICALS[key].terminology }`
    (o `VERTICALS[key].terminology` segun se prefiera; sin el spread del override).

    En `resolveVertical(business)`: remover la linea `const override = TYPE_TERMINOLOGY_OVERRIDE[business.type ?? '']`
    y, en la rama del stored vertical, simplificar el return para que `terminology` sea
    `{ ...VERTICALS[key].terminology }` sin el `...(override ?? {})`. La firma y el comportamiento
    de prioridad (stored vertical primero, fallback a getVertical por type) NO cambian.

    Actualizar/eliminar los comentarios que describen el override (lineas ~120-128 y los comentarios
    dentro de getVertical/resolveVertical que explican el merge del override por type) para que el
    codigo quede coherente: ya no hay override por type; cada vertical posee su terminologia.

    NO cambiar la logica de prioridad de resolveVertical ni la firma de ninguna funcion exportada.
    No tocar getVerticalKeyByType (sigue iterando VERTICALS — ahora 'Cancha de futbol' cae en
    canchas automaticamente).
  </action>
  <acceptance_criteria>
    - SOURCE (prohibicion, removal completo): `grep -c "TYPE_TERMINOLOGY_OVERRIDE" lib/verticals.ts` devuelve 0 (sin la constante ni referencias ni comentarios que la nombren).
    - SOURCE (repo-wide): `grep -rn "TYPE_TERMINOLOGY_OVERRIDE" lib app components` no devuelve referencias vivas.
    - BEHAVIOR: `getVertical('Cancha de futbol').key === 'canchas'` y su `terminology.resource === 'Cancha'` (ahora viene del vertical, no del override). Verificable con un snippet ts-node/tsx temporal o por inspeccion del flujo (canchas.types incluye 'Cancha de futbol' → getVerticalKeyByType devuelve 'canchas' → VERTICALS.canchas.terminology.resource es 'Cancha').
    - SOURCE: getVertical y resolveVertical ya NO contienen el token `override` ni el spread `...(override ?? {})`.
    - NO-REGRESION: resolveVertical({ vertical: 'salud' }).terminology.resource sigue siendo 'Profesional'; resolveVertical({ vertical: 'general' }).menu sigue conteniendo 'equipo'.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>TYPE_TERMINOLOGY_OVERRIDE eliminado; getVertical y resolveVertical sin referencias al override; tsc y lint pasan; la resolucion de salud/belleza/general no cambia.</done>
</task>

<task type="auto">
  <name>Task 3: Guard server-side anti-/equipo en negocios canchas (D-05)</name>
  <files>app/(dashboard)/equipo/page.tsx</files>
  <read_first>
    - app/(dashboard)/equipo/page.tsx (el archivo a modificar): es un Server Component async que ya
      hace getUser()→redirect('/login'), carga el business por owner_id→redirect('/onboarding') si
      no existe, y luego carga professionals/spaces/agenda_spaces y renderiza SettingsClient
      view="equipo". El guard nuevo se inserta DESPUES de obtener `business` y ANTES de las queries
      de professionals/spaces (para no filtrar las canchas-como-professionals).
    - lib/verticals.ts (resolveVertical): firma `resolveVertical(business: { vertical?, type? })`
      devuelve `{ key, ... }`. El guard usa `resolveVertical(business).key === 'canchas'`.
    - El patron getUser()→redirect ya esta presente en este mismo archivo; replicarlo (mismo import
      `redirect` de 'next/navigation', ya importado).
  </read_first>
  <action>
    Importar `resolveVertical` desde '@/lib/verticals' en app/(dashboard)/equipo/page.tsx.

    Insertar el guard justo despues de la guarda `if (!business) redirect('/onboarding')` y ANTES
    del `Promise.all` que carga professionals/spaces/agendaSpaces: si
    `resolveVertical(business).key === 'canchas'` entonces `redirect('/dashboard')`. Motivo (D-05):
    cada cancha es una fila de `professionals`, asi que cargar /equipo en un negocio canchas
    listaria las canchas como "Equipo" — leak de datos, no solo estetico. El redirect debe ocurrir
    antes de ejecutar las queries para no leer esos datos.

    Agregar un comentario breve en espanol explicando el porque (rubro canchas no tiene Equipo;
    /equipo expondria canchas-como-professionals). NO tocar el resto del flujo (queries, render de
    SettingsClient) para los demas verticales: para salud/belleza/general el comportamiento queda
    byte-identico.
  </action>
  <acceptance_criteria>
    - SOURCE: `grep -n "resolveVertical(business).key === 'canchas'" "app/(dashboard)/equipo/page.tsx"` devuelve la linea del guard.
    - SOURCE: `grep -n "redirect('/dashboard')" "app/(dashboard)/equipo/page.tsx"` devuelve >= 1.
    - SOURCE (orden): el guard aparece ANTES del `Promise.all`/queries de professionals en el archivo (la linea del redirect canchas tiene numero menor que la linea de `from('professionals')`).
    - SOURCE: `resolveVertical` esta importado desde '@/lib/verticals' en el archivo.
    - BEHAVIOR (human-check): un negocio con vertical='canchas' que navega a /equipo termina en /dashboard; un negocio salud/belleza/general sigue viendo la vista Equipo normal.
    - NO-REGRESION: para verticales != canchas no se introduce ningun redirect nuevo (el guard es condicional al key 'canchas').
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npm run lint</automated>
    <human-check>Con un negocio de prueba seteado a rubro Canchas (vertical='canchas'): (1) abrir el dashboard y confirmar que el sidebar NO muestra "Equipo" y que dice "Reservas"/"Canchas" en los labels; (2) escribir la URL /equipo a mano y confirmar que redirige a /dashboard. Con un negocio salud o general: (3) confirmar que /equipo sigue mostrando la vista Equipo y el sidebar mantiene su menu y terminologia de siempre (cero cambios).</human-check>
  </verify>
  <done>Guard inserta redirect('/dashboard') antes de las queries cuando vertical es canchas; tsc y lint pasan; verticales existentes sin regresion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (autenticado) → dashboard server components | El dueno autenticado pide rutas del dashboard; el server resuelve su vertical y decide menu/datos a renderizar. |

Nota: esta fase NO agrega tablas, columnas, route handlers publicos ni datos de tenant nuevos.
La columna `businesses.vertical` ya existe y ya esta bajo RLS por `business_id` (motor v0.12). No se
introduce ningun nuevo cruce de tenant. El cambio es de presentacion (terminologia/menu) + resolucion
de vertical, framework-agnostico.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | app/(dashboard)/equipo/page.tsx | mitigate | Guard server-side (D-05, Task 3): si `resolveVertical(business).key === 'canchas'` se hace `redirect('/dashboard')` ANTES de las queries de `professionals`/`spaces`, evitando exponer las canchas-como-professionals como "Equipo". El acceso sigue acotado por owner_id + RLS por business_id (sin cruce de tenant). |
| T-01-02 | Tampering | lib/verticals.ts (resolveVertical) | accept | El menu/terminologia derivan del `vertical` almacenado (bajo RLS por tenant). Manipular el vertical de otro negocio requiere escribir su fila — bloqueado por RLS preexistente, no por esta fase. Riesgo residual: cambiar el PROPIO vertical solo altera la presentacion del propio dashboard (sin impacto cross-tenant). |
| T-01-03 | Information Disclosure | general.types / TYPE_TERMINOLOGY_OVERRIDE removal | accept | El cutover (quitar 'Cancha de futbol' de general, borrar el override) es un cambio de configuracion de presentacion sin datos sensibles. Sin clientes en prod (D-06) → sin migracion ni exposicion de datos. |

## Disposition summary

Ninguna amenaza alcanza el umbral de bloqueo (ASVS L1, blocking threshold: high). El riesgo de esta
fase es de **regresion de presentacion**, NO de aislamiento: no hay tablas/columnas/endpoints nuevos
ni datos de tenant nuevos. El unico vector con sabor a seguridad (T-01-01) es el leak de UI de /equipo
en negocios canchas, mitigado por el guard server-side de D-05 antes de cualquier query. No se inventan
amenazas high inexistentes.
</threat_model>

<verification>
## Checks de la fase

1. `npx tsc --noEmit` pasa (proyecto strict:true) tras los 3 tasks.
2. `npm run lint` pasa.
3. `grep -rn "TYPE_TERMINOLOGY_OVERRIDE" lib app components` no devuelve referencias vivas.
4. SOURCE de no-regresion: las entradas `salud` y `belleza` en `VERTICALS` no tienen diff; `general` solo difiere en el removal de 'Cancha de futbol' de `types` (su `menu` y `terminology` intactos).
5. SOURCE: `VERTICALS.canchas.menu` no contiene 'equipo' ni 'patients'.
6. BEHAVIOR (human-check, fin de fase — human_verify_mode=end-of-phase): negocio canchas no muestra "Equipo" en el sidebar, muestra terminologia del rubro, y /equipo a mano redirige a /dashboard; negocio salud/general sin cambios.
</verification>

<success_criteria>
Mapea 1:1 a los 4 criterios del ROADMAP (VERT-01, VERT-02):

1. (Criterio 1, VERT-01) Un negocio puede setear rubro 'canchas' en onboarding/settings y resuelve a su VerticalConfig con terminologia "Reserva/Cancha/Sede". → `VERTICALS.canchas` + derivacion automatica de `TYPE_GROUPS`.
2. (Criterio 2, VERT-01) El dashboard canchas NO muestra "Profesionales/Equipo" en el menu y la terminologia visible es la del rubro. → `VERTICALS.canchas.menu` sin 'equipo' + `VerticalProvider` ya activo en layout.
3. (Criterio 3, VERT-02) salud/belleza/general y negocios existentes resuelven exactamente igual — cero regresion. → entradas existentes intactas (salvo removal inerte en general.types); override eliminado sin cambiar su comportamiento neto.
4. (Criterio 4, VERT-02) Resolucion deterministica: un negocio sin canchas nunca ve UI de canchas; uno de canchas nunca ve UI de profesionales. → vertical de primera clase determinista + guard server-side D-05 en /equipo.
</success_criteria>

<output>
Crear `.planning/workstreams/canchas/phases/01-vertical-canchas/01-01-SUMMARY.md` al terminar.
</output>
