---
phase: 03-rework-del-selector-de-rubro
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/(onboarding)/onboarding/page.tsx
  - app/(dashboard)/settings/settings-client.tsx
autonomous: false
requirements: [ONB-RUBRO-01, ONB-RUBRO-02]

must_haves:
  truths:
    - "El onboarding paso 'Tu negocio' muestra un Select con exactamente 4 rubros (Salud, Belleza/Estética/Spa, General, Canchas)"
    - "Debajo del Select hay un Input SIEMPRE visible con label '¿A qué se dedica tu negocio?' y leyenda 'Así aparecerá en tu página de reservas'"
    - "El placeholder del Input cambia según el rubro elegido (RUBRO_PLACEHOLDERS[vertical])"
    - "Elegir rubro es obligatorio para avanzar en el onboarding (canGoNext exige vertical)"
    - "El onboarding guarda el rubro elegido en la columna vertical y el texto libre en type"
    - "El auto-ocultar Profesionales en canchas keyea por la VerticalKey elegida (no getVerticalKeyByType(type))"
    - "Settings → Negocio muestra el MISMO control (4 rubros + Input libre + leyenda), sin el toggle 'Otro'"
    - "Settings guarda vertical = rubro elegido y type = texto libre, con .eq('id', business.id)"
    - "npm run lint queda limpio (sin OTRO_TYPE, predefinedTypes, typeIsOtro, typeSelectValue, onTypeChange, imports huérfanos)"
  artifacts:
    - path: "app/(onboarding)/onboarding/page.tsx"
      provides: "Selector de 4 rubros + Input libre siempre visible en el paso 'Tu negocio'"
      contains: "RUBRO_PLACEHOLDERS"
    - path: "app/(dashboard)/settings/settings-client.tsx"
      provides: "Mismo selector en Configuración → Negocio, sin andamiaje 'Otro'"
      contains: "RUBRO_PLACEHOLDERS"
  key_links:
    - from: "app/(onboarding)/onboarding/page.tsx"
      to: "lib/verticals.ts"
      via: "importa VERTICALS + RUBRO_PLACEHOLDERS + VerticalKey; el Select itera Object.keys(VERTICALS)"
      pattern: "RUBRO_PLACEHOLDERS\\[vertical\\]"
    - from: "app/(onboarding)/onboarding/page.tsx"
      to: "businesses (Supabase)"
      via: "insert con vertical = VerticalKey elegida, type = texto libre"
      pattern: "vertical,\\s*$|vertical: vertical"
    - from: "app/(dashboard)/settings/settings-client.tsx"
      to: "businesses (Supabase)"
      via: "update con vertical + type, scoped a .eq('id', business.id)"
      pattern: "\\.eq\\('id', business\\.id\\)"
  prohibitions:
    - "NO derivar el vertical desde type con getVerticalKeyByType en la UI nueva (type ahora es texto libre → siempre daría 'general')"
    - "NO hardcodear los placeholders en los componentes (usar RUBRO_PLACEHOLDERS de lib/verticals.ts)"
    - "NO reescribir estilos de Select/Input/Label (reusar los primitivos shadcn verbatim; tokens, no hex/px)"
    - "NO agregar botones/CTA propios (onboarding avanza con 'Siguiente', settings guarda con 'Guardar')"
    - "NO usar Write sobre estos archivos existentes; solo Edit (reemplazo parcial)"
    - "NO quitar el reload-on-verticalChanged ni el .eq('id', business.id) en settings"
---

<objective>
Reemplazar el selector de rubro en las DOS superficies (onboarding paso "Tu negocio" —D-01/D-04— y Configuración → Negocio —D-09—) por el control unificado del UI-SPEC: Select de 4 rubros planos (VerticalKey) + Input de texto libre SIEMPRE visible con placeholder por rubro + leyenda. Re-keyear la lógica que hoy deriva del `type` (auto-hide canchas, `canGoNext`, hints, init del grupo) para que lea la VerticalKey elegida directamente (D-07). Guardar rubro → `vertical`, texto libre → `type` (D-07).

Purpose: cumplir ONB-RUBRO-01 (onboarding) y ONB-RUBRO-02 (settings) con consistencia total entre superficies (UI-SPEC), arreglando de paso el bug del campo "Otro" que hoy no aparece en el onboarding (D-04), sin regresión en el auto-hide de canchas (Pitfall 2).

Output: `app/(onboarding)/onboarding/page.tsx` y `app/(dashboard)/settings/settings-client.tsx` con el nuevo control.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/onboarding/ROADMAP.md
@.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-CONTEXT.md
@.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-UI-SPEC.md
@.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-PATTERNS.md

# Depende de 03-01: consume RUBRO_PLACEHOLDERS y VerticalKey de lib/verticals.ts
@lib/verticals.ts
</context>

<interface_context>
De 03-01 (`lib/verticals.ts`), esta plan consume:
- `RUBRO_PLACEHOLDERS: Record<VerticalKey, string>` — placeholder del Input por rubro.
- `VERTICALS: Record<VerticalKey, VerticalConfig>` — iterar `Object.keys(VERTICALS)` para los 4 SelectItem (label = `VERTICALS[k].label`).
- `type VerticalKey = 'salud' | 'belleza' | 'general' | 'canchas'` — tipo del nuevo estado `vertical`.
- `getVerticalKeyByType(type)` — SOLO para inicializar el estado de filas viejas en settings (init), NO para derivar el vertical en el flujo nuevo.
Los `types` de `VERTICALS` ahora están vacíos (03-01) → NO iterar `group.types`; el Select es plano.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding — Select de 4 rubros + Input libre + re-keyear canchas/canGoNext/hints</name>
  <files>app/(onboarding)/onboarding/page.tsx</files>
  <read_first>
    - app/(onboarding)/onboarding/page.tsx:11,16,84-85,286-303,366-398,452-498 — imports, estado `type`, insert, steps/visibleSteps/canGoNext, y el bloque del Select + hints. Es su propio análogo.
    - 03-UI-SPEC.md §Interaction States (A/B/C), §Layout del bloque, §Copywriting Contract — la forma exacta del control, el copy bloqueado, y el layout mobile-first (Input libre a ancho completo `sm:col-span-2`, gap `space-y-2`).
    - 03-PATTERNS.md §app/(onboarding)/onboarding/page.tsx — los excerpts exactos del Select viejo, el insert, el auto-hide, el canGoNext y los hints con la condición a cambiar.
  </read_first>
  <action>
Editar `app/(onboarding)/onboarding/page.tsx` con Edit (parcial). Cambios:

1. **Imports (`:11,16`):** remover `SelectGroup, SelectLabel` del import de select si quedan sin uso (el Select nuevo es plano). Del import de `@/lib/verticals` (`:16`) sacar `TYPE_GROUPS`, sumar `VERTICALS`, `RUBRO_PLACEHOLDERS`, `type VerticalKey`. Mantener `getVerticalKeyByType` solo si algún otro uso lo requiere; si no queda ningún uso, quitarlo.

2. **Estado nuevo (`:85`):** agregar `const [vertical, setVertical] = useState<VerticalKey>('' as VerticalKey)` — o iniciar sin selección para forzar la elección (D-02). Mantener `const [type, setType] = useState('')` (ahora texto libre). Si iniciás `vertical` vacío, el placeholder del Input usa un fallback seguro cuando aún no hay rubro (ej. `RUBRO_PLACEHOLDERS[vertical] ?? ''`).

3. **Select (`:466-483`):** reemplazar el Select que itera `TYPE_GROUPS` (grupos+subtipos) por: `<Label>Rubro *</Label>` + `<Select value={vertical} onValueChange={v => setVertical(v as VerticalKey)}>` con placeholder "Elegí tu rubro" y 4 `SelectItem` planos iterando `(Object.keys(VERTICALS) as VerticalKey[])` con `value={k}` y `{VERTICALS[k].label}`.

4. **Campo libre SIEMPRE visible (D-04/D-05)** (nuevo, debajo, a ancho completo — `sm:col-span-2` o fila propia fuera de la grilla de 2 cols): `<Label>¿A qué se dedica tu negocio?</Label>` (copy literal D-05) + `<Input value={type} onChange={e => setType(e.target.value)} placeholder={RUBRO_PLACEHOLDERS[vertical] ?? ''} />` + `<p className="text-xs text-muted-foreground">Así aparecerá en tu página de reservas</p>` (leyenda literal D-05). Gap `space-y-2` (UI-SPEC onboarding).

5. **Insert (`:292-293`):** `type` sigue guardándose (texto libre); cambiar `vertical: getVerticalKeyByType(type)` → `vertical` (la VerticalKey elegida directa, D-07).

6. **Auto-hide canchas (`:380`):** `getVerticalKeyByType(type) === 'canchas'` → `vertical === 'canchas'`.

7. **canGoNext (`:396`):** `return name && slug && slugAvailable && type` → `return name && slug && slugAvailable && vertical` (el rubro es el required D-02, no el texto libre que es opcional D-03).

8. **Hints (`:487,493`):** las condiciones `type && getVerticalKeyByType(type) === 'salud'|'belleza'` → `vertical === 'salud'|'belleza'`. Los recuadros de hint (border-primary/30 bg-primary/5, icono lucide) SE CONSERVAN verbatim; solo cambia la condición.

Render con JSX interpolado (auto-escape). No reescribir estilos de los primitivos. Ejecutar `npm run lint` para confirmar cero imports/variables huérfanos.
  </action>
  <verify>
    <automated>npm run lint && grep -q '¿A qué se dedica tu negocio?' 'app/(onboarding)/onboarding/page.tsx' && grep -q 'Así aparecerá en tu página de reservas' 'app/(onboarding)/onboarding/page.tsx' && grep -q "vertical === 'canchas'" 'app/(onboarding)/onboarding/page.tsx' && grep -q 'RUBRO_PLACEHOLDERS' 'app/(onboarding)/onboarding/page.tsx' && ! grep -q 'TYPE_GROUPS' 'app/(onboarding)/onboarding/page.tsx' && npx tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El Select del paso "Tu negocio" itera `Object.keys(VERTICALS)` y NO usa `TYPE_GROUPS`/`SelectGroup`/`SelectLabel` (grep de `TYPE_GROUPS` en el archivo → 0).
    - Existe el Input con label literal "¿A qué se dedica tu negocio?" y la leyenda "Así aparecerá en tu página de reservas".
    - El placeholder del Input usa `RUBRO_PLACEHOLDERS[vertical]`.
    - `canGoNext` (step 1) depende de `vertical` (no de `type`).
    - El auto-hide de canchas usa `vertical === 'canchas'` (grep matchea).
    - El insert guarda `vertical` = VerticalKey elegida (no `getVerticalKeyByType(type)`).
    - `npm run lint` y `npx tsc --noEmit` limpios.
  </acceptance_criteria>
  <done>El onboarding muestra 4 rubros + Input libre siempre visible con placeholder por rubro y leyenda; guarda vertical=rubro y type=libre; el auto-hide de canchas y el gating leen la VerticalKey elegida; lint+tsc limpios.</done>
</task>

<task type="auto">
  <name>Task 2: Settings → Negocio (D-09) — mismo control, remover andamiaje 'Otro'</name>
  <files>app/(dashboard)/settings/settings-client.tsx</files>
  <read_first>
    - app/(dashboard)/settings/settings-client.tsx:21,25,27-30,240-288,1082-1119 — imports, andamiaje `OTRO_TYPE`/`predefinedTypes`/`typeGroup`/`typeIsOtro`/`typeSelectValue`/`onTypeChange`/`initTypeGroup`, `saveBusiness`, y el Select viejo. Es su propio análogo.
    - 03-UI-SPEC.md §Layout (settings usa `space-y-1`), §Interaction States, §Copywriting — mismo control que onboarding, gap `space-y-1`.
    - 03-PATTERNS.md §app/(dashboard)/settings/settings-client.tsx — lista exacta de código huérfano a remover (Pitfall 4) y el patrón de `saveBusiness` a preservar.
    - 03-01-PLAN.md (interface) — RUBRO_PLACEHOLDERS, VERTICALS, VerticalKey.
  </read_first>
  <action>
Editar `app/(dashboard)/settings/settings-client.tsx` con Edit (parcial). Cambios:

1. **Remover andamiaje "Otro" (Pitfall 4):** borrar `OTRO_TYPE` (:28), `predefinedTypes` (:30), `typeIsOtro`/`typeSelectValue` (:248-249), `onTypeChange` (:251-264), y el comentario de `initTypeGroup` (:242-243). Reemplazar `initTypeGroup`/`typeGroup` (:244-247) por un único estado `vertical`: `const [vertical, setVertical] = useState<VerticalKey>((business.vertical && business.vertical in VERTICALS ? business.vertical : getVerticalKeyByType(business.type)) as VerticalKey)`. El `type` sigue viviendo en `bizForm.type`.

2. **Imports (`:21,25`):** remover `SelectGroup, SelectLabel` del import de select si quedan sin uso. Del import de `@/lib/verticals` (`:25`) sacar `TYPE_GROUPS`, sumar `RUBRO_PLACEHOLDERS`. Mantener `VERTICALS`, `getVerticalKeyByType`, `resolveVertical`, `VerticalKey` (se siguen usando).

3. **Select viejo (`:1089-1118`):** reemplazar el bloque `<Label>Tipo</Label>` + `<div className="flex gap-2">` (Select con `typeSelectValue`/`onTypeChange` + Input condicional `typeIsOtro`) por el MISMO control del onboarding: `<Label>Rubro</Label>` + `<Select value={vertical} onValueChange={v => setVertical(v as VerticalKey)}>` con 4 SelectItem planos (placeholder "Elegí tu rubro") + `<Label>¿A qué se dedica tu negocio?</Label>` + `<Input value={bizForm.type} onChange={e => setBizForm(f => ({ ...f, type: e.target.value }))} placeholder={RUBRO_PLACEHOLDERS[vertical]} />` + `<p className="text-xs text-muted-foreground">Así aparecerá en tu página de reservas</p>`. Gap del contenedor `space-y-1` (patrón de settings). El hint "Rubro: <label> · define el menú…" (:1115-1118) SE CONSERVA leyendo `VERTICALS[vertical].label`.

4. **saveBusiness (`:277-287`):** `const vertical = typeGroup` → usar el nuevo estado `vertical`. `const type = typeIsOtro ? bizForm.type.trim() : bizForm.type` → `const type = bizForm.type.trim()`. Mantener `verticalChanged`, el `.update({ ...bizForm, type, whatsapp, vertical, maps_url }).eq('id', business.id)`, y el `if (verticalChanged) setTimeout(() => window.location.reload(), 600)`.

Reusar los primitivos shadcn verbatim (tokens, no hex/px). Ejecutar `npm run lint` — DEBE quedar limpio (sin OTRO_TYPE, predefinedTypes, typeIsOtro, typeSelectValue, onTypeChange, initTypeGroup, imports huérfanos).
  </action>
  <verify>
    <automated>npm run lint && grep -q '¿A qué se dedica tu negocio?' 'app/(dashboard)/settings/settings-client.tsx' && grep -q 'RUBRO_PLACEHOLDERS' 'app/(dashboard)/settings/settings-client.tsx' && grep -q "\.eq('id', business.id)" 'app/(dashboard)/settings/settings-client.tsx' && ! grep -q 'OTRO_TYPE\|predefinedTypes\|typeIsOtro\|typeSelectValue\|onTypeChange' 'app/(dashboard)/settings/settings-client.tsx' && npx tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'OTRO_TYPE\|predefinedTypes\|typeIsOtro\|typeSelectValue\|onTypeChange'` en el archivo → 0 matches (andamiaje removido).
    - Existe un Select de 4 rubros planos (iterando `Object.keys(VERTICALS)`) + Input libre siempre visible con label "¿A qué se dedica tu negocio?" y leyenda "Así aparecerá en tu página de reservas".
    - El placeholder del Input usa `RUBRO_PLACEHOLDERS[vertical]`.
    - `saveBusiness` guarda `vertical` = estado nuevo y `type` = `bizForm.type.trim()`, con `.eq('id', business.id)` intacto y el reload-on-verticalChanged conservado.
    - El hint "Rubro: … · define el menú" se conserva leyendo `VERTICALS[vertical].label`.
    - `npm run lint` y `npx tsc --noEmit` limpios.
  </acceptance_criteria>
  <done>Configuración → Negocio muestra el mismo control que el onboarding (4 rubros + Input libre + leyenda), sin toggle "Otro"; guarda vertical+type scoped al owner; lint+tsc limpios.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verificar el selector en onboarding y settings</name>
  <action>Pausar para verificación humana sobre la app corriendo: el dueño confirma que las dos superficies (onboarding paso "Tu negocio" y Configuración → Negocio) muestran el mismo control (4 rubros + Input libre siempre visible con placeholder por rubro + leyenda), que el auto-hide de canchas funciona, que el gating exige rubro, y que guardar en settings recarga con el vertical correcto. Seguir los pasos de <how-to-verify>.</action>
  <what-built>
    El selector nuevo (4 rubros + Input libre siempre visible + leyenda) en las dos superficies: onboarding paso "Tu negocio" (Task 1) y Configuración → Negocio (Task 2), visualmente idénticos por el UI-SPEC.
  </what-built>
  <how-to-verify>
    1. `npm run dev` y abrir `/onboarding`. En el paso "Tu negocio": ver el Select "Rubro *" con EXACTAMENTE 4 opciones (Salud, Belleza/Estética/Spa, General, Canchas). Debajo, el campo "¿A qué se dedica tu negocio?" SIEMPRE visible con la leyenda "Así aparecerá en tu página de reservas".
    2. Cambiar el rubro y confirmar que el placeholder del Input cambia (Salud → "Ej: Lic. en Psicología, Kinesiólogo", Canchas → "Ej: Canchas de fútbol", etc.).
    3. Elegir "Canchas" y confirmar que el stepper pasa a 3 pasos (Profesionales oculto). Elegir otro rubro → 4 pasos.
    4. Confirmar que "Siguiente" está deshabilitado hasta elegir un rubro (nombre + slug + rubro requeridos); el campo libre puede quedar vacío.
    5. Crear un negocio de prueba y verificar en `/dashboard` que la terminología/menú corresponden al rubro elegido.
    6. Ir a Configuración → Negocio: ver el MISMO control (4 rubros + Input libre + leyenda), sin dropdown de subtipos ni toggle "Otro". Cambiar de rubro y guardar → el panel recarga con la terminología/menú del nuevo rubro.
    7. Verificar estados: focus del Select/Input muestra el anillo de acento (ring-ring/50); en mobile 375px todo apila a 1 columna y el Input libre queda a ancho completo.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" si ambas superficies muestran 4 rubros + campo libre siempre visible con placeholder por rubro y leyenda, el auto-hide de canchas funciona, y el guardado en settings recarga con el vertical correcto. Si algo no coincide con el UI-SPEC, describilo.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (sesión autenticada) → businesses (RLS por business_id) | onboarding insert (`owner_id: user.id`) y settings update (`.eq('id', business.id)`) cruzan acá |
| texto libre `type` (input de usuario) → render público en /[slug] booking | el `type` se muestra en booking (consumido en 03-03); se escribe acá |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Elevation / Tampering | Cambio de `vertical`/`type` de otro tenant | mitigate | El insert usa `owner_id: user.id` (onboarding) y el update usa `.eq('id', business.id)` (settings) — scoped al negocio del owner autenticado, RLS por business_id vigente. Patrón sin cambio. |
| T-03-05 | Info disclosure / Tampering (XSS) | Texto libre `type` escrito acá se renderiza público en booking | mitigate | El valor se persiste tal cual y se renderiza con JSX interpolado (auto-escape de React) en 03-03. Defensivo: `bizForm.type.trim()` al guardar en settings (ya en el plan). Prohibido `dangerouslySetInnerHTML`. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes (reusa shadcn Select/Input/Label ya presentes). Sin superficie de supply-chain. |

**Nota de aislamiento:** no se introduce superficie nueva. La escritura sigue scoped al negocio del owner (patrón vigente). Sin amenazas HIGH.
</threat_model>

<verification>
- `npm run lint` limpio en ambos archivos (sin andamiaje "Otro" ni imports huérfanos).
- `npx tsc --noEmit` sin errores.
- Onboarding: 4 rubros, Input libre siempre visible, placeholder por rubro, leyenda, auto-hide canchas por VerticalKey, canGoNext por vertical.
- Settings: mismo control, sin toggle "Otro", saveBusiness scoped a `.eq('id', business.id)` + reload-on-verticalChanged.
- Copy literal verificado: "Rubro *"/"Rubro", "Elegí tu rubro", "¿A qué se dedica tu negocio?", "Así aparecerá en tu página de reservas".
</verification>

<success_criteria>
- ONB-RUBRO-01: onboarding con 4 rubros + campo personalizable siempre visible con sugerencia por rubro + leyenda; sin depender de "Otro".
- ONB-RUBRO-02: mismo selector en settings; rubro → vertical, texto libre → type; consistencia total onboarding ↔ settings.
- Cero regresión: auto-hide de canchas keyea por la VerticalKey elegida (Pitfall 2 evitado).
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-02-SUMMARY.md` when done
</output>
