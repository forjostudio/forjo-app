---
phase: 03-rework-del-selector-de-rubro
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - app/[slug]/booking-client.tsx
  - app/[slug]/canchas-booking-client.tsx
autonomous: false
requirements: [ONB-RUBRO-02]

must_haves:
  truths:
    - "El booking público muestra business.type como categoría cuando existe (comportamiento vigente)"
    - "Cuando type está vacío, el booking muestra el label del rubro (getVerticalLabel) como fallback — nunca queda sin subtítulo (D-03)"
    - "El fallback está aplicado en AMBOS clients: booking-client.tsx y canchas-booking-client.tsx (Pitfall 3)"
    - "El subtítulo se renderiza con JSX interpolado (auto-escape de React); sin dangerouslySetInnerHTML"
  artifacts:
    - path: "app/[slug]/booking-client.tsx"
      provides: "Subtítulo de categoría con fallback al label del rubro"
      contains: "getVerticalLabel"
    - path: "app/[slug]/canchas-booking-client.tsx"
      provides: "Subtítulo de categoría con fallback al label del rubro (gemelo canchas)"
      contains: "getVerticalLabel"
  key_links:
    - from: "app/[slug]/booking-client.tsx"
      to: "lib/verticals.ts (getVerticalLabel)"
      via: "import + business.type || getVerticalLabel(business)"
      pattern: "getVerticalLabel\\(business\\)"
    - from: "app/[slug]/canchas-booking-client.tsx"
      to: "lib/verticals.ts (getVerticalLabel)"
      via: "import + business.type || getVerticalLabel(business)"
      pattern: "getVerticalLabel\\(business\\)"
  prohibitions:
    - "NO usar dangerouslySetInnerHTML sobre el texto libre type (V5 input validation, RESEARCH Security)"
    - "NO arreglar solo un client (Pitfall 3: ambos tienen la línea gemela idéntica)"
    - "NO cambiar los estilos del <p> del subtítulo (text-sm text-primary-foreground/80 mt-1.5)"
    - "NO usar Write; solo Edit (reemplazo parcial) — el resto de estos archivos no se toca"
---

<objective>
Aplicar el fallback de display D-03 en las DOS páginas públicas de reservas: cuando `business.type` (texto libre, ahora opcional) está vacío, mostrar el label del rubro (`getVerticalLabel(business)`) como subtítulo de categoría, de modo que el negocio NUNCA quede sin subtítulo. Las dos líneas son gemelas idénticas (`booking-client.tsx:401` y `canchas-booking-client.tsx:331`); ambas deben cambiar (Pitfall 3).

Purpose: cerrar el criterio de ONB-RUBRO-02 de que "el texto libre se muestra como categoría del negocio en booking" con el fallback correcto cuando el campo opcional queda vacío (D-03), consistente con Phase 2 (todo completable después).

Output: `app/[slug]/booking-client.tsx` y `app/[slug]/canchas-booking-client.tsx` con el subtítulo de categoría usando `business.type || getVerticalLabel(business)`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-CONTEXT.md
@.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-PATTERNS.md
@.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-UI-SPEC.md

# Depende de 03-01: consume getVerticalLabel de lib/verticals.ts
@lib/verticals.ts
</context>

<interface_context>
De 03-01 (`lib/verticals.ts`), esta plan consume:
- `getVerticalLabel(business: { vertical?: string | null; type?: string | null }): string` — devuelve el label del rubro resuelto (reusa `resolveVertical`, prefiere `vertical`, cae a `type` legacy). SIEMPRE devuelve un string no vacío para los 4 VerticalKey, así que el guard `{business.type && ...}` puede simplificarse a mostrar siempre el subtítulo.
Ninguno de los dos booking clients importa hoy de `@/lib/verticals` → hay que AGREGAR el import en ambos.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Fallback de categoría en booking-client.tsx</name>
  <files>app/[slug]/booking-client.tsx</files>
  <read_first>
    - app/[slug]/booking-client.tsx:399-403 — el bloque del subtítulo `{business.type && <p className="text-sm text-primary-foreground/80 mt-1.5">{business.type}</p>}` (línea 401) y los imports del top del archivo (para agregar `getVerticalLabel`).
    - 03-PATTERNS.md §booking-client + canchas-booking-client — el patrón exacto del fallback D-03 a aplicar en ambos (líneas gemelas).
    - 03-UI-SPEC.md §Copywriting (Subtítulo de categoría en booking) — el patrón `business.type || getVerticalLabel(business)`, JSX interpolado, prohibido dangerouslySetInnerHTML.
  </read_first>
  <action>
Editar `app/[slug]/booking-client.tsx` con Edit (parcial):

1. Agregar el import: `import { getVerticalLabel } from '@/lib/verticals'` (ubicarlo junto a los otros imports `@/lib/...` del top del archivo; si ya hay un import de `@/lib/verticals`, sumar `getVerticalLabel` a la lista).

2. Reemplazar la línea 401 `{business.type && <p className="text-sm text-primary-foreground/80 mt-1.5">{business.type}</p>}` por el patrón con fallback:
   `{(business.type || getVerticalLabel(business)) && (<p className="text-sm text-primary-foreground/80 mt-1.5">{business.type || getVerticalLabel(business)}</p>)}`
   (Como `getVerticalLabel` siempre devuelve algo, en la práctica el subtítulo se muestra siempre; mantener el guard es defensivo y no cuesta nada.) Mantener las clases del `<p>` idénticas. Render con JSX interpolado; NO usar `dangerouslySetInnerHTML`.

Confirmar que `business` en scope tiene `type` y `vertical` (el server component pasa el negocio completo; `getVerticalLabel` acepta `{ vertical?, type? }`).
  </action>
  <verify>
    <automated>npm run lint && grep -q "getVerticalLabel" 'app/[slug]/booking-client.tsx' && grep -q "from '@/lib/verticals'" 'app/[slug]/booking-client.tsx' && ! grep -q 'dangerouslySetInnerHTML' 'app/[slug]/booking-client.tsx' && npx tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El archivo importa `getVerticalLabel` desde `@/lib/verticals`.
    - La línea del subtítulo usa `business.type || getVerticalLabel(business)` (grep matchea `getVerticalLabel(business)`).
    - Las clases del `<p>` (`text-sm text-primary-foreground/80 mt-1.5`) quedan intactas.
    - `grep 'dangerouslySetInnerHTML'` → 0 matches.
    - `npm run lint` y `npx tsc --noEmit` limpios.
  </acceptance_criteria>
  <done>El booking genérico muestra `business.type` o el label del rubro como fallback; import agregado; JSX auto-escapado; lint+tsc limpios.</done>
</task>

<task type="auto">
  <name>Task 2: Mismo fallback en canchas-booking-client.tsx (línea gemela)</name>
  <files>app/[slug]/canchas-booking-client.tsx</files>
  <read_first>
    - app/[slug]/canchas-booking-client.tsx:329-333 — la línea gemela idéntica `{business.type && <p className="text-sm text-primary-foreground/80 mt-1.5">{business.type}</p>}` (línea 331) y los imports del top.
    - app/[slug]/booking-client.tsx (tras Task 1) — el cambio ya aplicado en el gemelo, a replicar 1:1.
    - 03-PATTERNS.md §booking-client + canchas-booking-client (Pitfall 3) — ambos deben cambiar.
  </read_first>
  <action>
Editar `app/[slug]/canchas-booking-client.tsx` con Edit (parcial), replicando EXACTAMENTE el cambio de Task 1:

1. Agregar `import { getVerticalLabel } from '@/lib/verticals'` junto a los imports `@/lib/...` del top (o sumarlo a un import existente de `@/lib/verticals` si lo hubiera).

2. Reemplazar la línea 331 por `{(business.type || getVerticalLabel(business)) && (<p className="text-sm text-primary-foreground/80 mt-1.5">{business.type || getVerticalLabel(business)}</p>)}`. Clases del `<p>` intactas. JSX interpolado; NO `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>npm run lint && grep -q "getVerticalLabel" 'app/[slug]/canchas-booking-client.tsx' && grep -q "from '@/lib/verticals'" 'app/[slug]/canchas-booking-client.tsx' && ! grep -q 'dangerouslySetInnerHTML' 'app/[slug]/canchas-booking-client.tsx' && npx tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El archivo importa `getVerticalLabel` desde `@/lib/verticals`.
    - La línea del subtítulo usa `business.type || getVerticalLabel(business)`.
    - Las clases del `<p>` quedan intactas.
    - `grep 'dangerouslySetInnerHTML'` → 0 matches.
    - `npm run lint` y `npx tsc --noEmit` limpios.
  </acceptance_criteria>
  <done>El booking de canchas muestra `business.type` o el label del rubro como fallback, idéntico al gemelo genérico; lint+tsc limpios.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — verificar el fallback de categoría en ambos bookings</name>
  <action>Pausar para verificación humana sobre la app corriendo: el dueño confirma que ambas páginas públicas de reservas (genérica y canchas) muestran el texto libre `type` cuando existe y el label del rubro como fallback cuando está vacío, con estilo idéntico, y que el texto libre se auto-escapa (sin interpretar HTML). Seguir los pasos de <how-to-verify>.</action>
  <what-built>
    El fallback D-03 del subtítulo de categoría en las dos páginas públicas de reservas: `booking-client.tsx` (Task 1) y `canchas-booking-client.tsx` (Task 2). Con `type` vacío el subtítulo cae al label del rubro; con `type` seteado muestra el texto libre.
  </what-built>
  <how-to-verify>
    1. `npm run dev`. Abrir el `/[slug]` público de un negocio que tenga `type` seteado → el subtítulo bajo el nombre muestra ese texto libre (comportamiento vigente).
    2. Abrir (o crear vía el onboarding de 03-02) un negocio SIN `type` (campo libre vacío) de rubro Salud → el subtítulo muestra "Salud" (el label del rubro, fallback D-03), NO queda vacío.
    3. Repetir con un negocio de rubro Canchas sin `type` en su página pública de canchas → el subtítulo muestra "Canchas". Verifica el gemelo `canchas-booking-client`.
    4. Confirmar que el estilo del subtítulo es idéntico en ambos (mismo tamaño/opacidad/margen: `text-sm text-primary-foreground/80 mt-1.5`).
    5. (Seguridad) Setear un `type` con caracteres tipo `<b>test</b>` y confirmar que se muestra literal (auto-escapado), no interpretado como HTML.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" si ambos bookings (genérico y canchas) muestran el texto libre cuando existe y el label del rubro cuando está vacío, con el mismo estilo, y el texto libre se auto-escapa. Si un client no muestra el fallback, indicá cuál.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto libre `type` (input de usuario, escrito en 03-02) → render en /[slug] booking público (anon) | el subtítulo se renderiza en la superficie pública sin sesión |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Info disclosure / Tampering (XSS) | Texto libre `type` renderizado en el subtítulo público de booking | mitigate | Render con JSX interpolado (`{business.type || getVerticalLabel(business)}`) → auto-escape de React. Prohibido `dangerouslySetInnerHTML` (verificado por grep en el `verify` de cada task). Es el patrón vigente (la línea ya mostraba `{business.type}` interpolado); no se abre superficie nueva. |
| T-03-07 | Info disclosure | El fallback expone el label del rubro (dato ya público) cuando type está vacío | accept | El label del rubro (Salud/Belleza/General/Canchas) es data pública no sensible; mostrarla como categoría es el comportamiento deseado (D-03). Sin riesgo de aislamiento. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Esta fase NO instala paquetes. Sin superficie de supply-chain. |

**Nota de aislamiento:** el `business` que llega a estos clients ya viene resuelto por el server component de `/[slug]` (booking público, patrón vigente). No se agrega query ni superficie de datos nueva. Sin amenazas HIGH.
</threat_model>

<verification>
- `npm run lint` y `npx tsc --noEmit` limpios en ambos archivos.
- `getVerticalLabel` importado desde `@/lib/verticals` en AMBOS clients.
- El subtítulo usa `business.type || getVerticalLabel(business)` en ambos (Pitfall 3 evitado).
- `grep 'dangerouslySetInnerHTML'` → 0 en ambos.
- Clases del `<p>` (`text-sm text-primary-foreground/80 mt-1.5`) intactas.
</verification>

<success_criteria>
- ONB-RUBRO-02 (parte booking): el texto libre se muestra como categoría; con type vacío cae al label del rubro (D-03) — nunca sin subtítulo.
- El fallback está en AMBOS booking clients, idénticos (Pitfall 3 evitado).
- Render seguro (auto-escape, sin dangerouslySetInnerHTML).
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-03-SUMMARY.md` when done
</output>
