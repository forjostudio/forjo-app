---
phase: 03-rework-del-selector-de-rubro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/047_backfill_vertical.sql
  - lib/verticals.ts
  - test/verticals.test.ts
autonomous: false
requirements: [ONB-RUBRO-01, ONB-RUBRO-02]
user_setup:
  - service: supabase-prod
    why: "La migración 047 (backfill de businesses.vertical) se aplica a PROD A MANO, coordinada con el deploy del código que vacía los types (Vercel Hobby, disciplina de migraciones del proyecto). El db reset local solo VALIDA; NO empuja a prod."
    dashboard_config:
      - task: "Aplicar 047_backfill_vertical.sql a la base de PRODUCCIÓN a mano ANTES o DURANTE el deploy del código de esta fase. Verificar post-condición: SELECT count(*) FROM businesses WHERE vertical IS NULL → 0."
        location: "Supabase (SQL editor prod / psql). Paso manual, out-of-band. NUNCA correr supabase db push."

must_haves:
  truths:
    - "supabase db reset local aplica el baseline + 040..047 sin error (047 aplica limpio sobre PG local)"
    - "Tras 047, ninguna fila de businesses queda con vertical NULL (el CASE es total con ELSE 'general')"
    - "047 NO modifica la columna type de ninguna fila (solo escribe vertical donde IS NULL)"
    - "El label de belleza es 'Belleza/Estética/Spa' en VERTICALS.belleza.label"
    - "Los 4 arrays VERTICALS[*].types quedan vacíos ([])"
    - "RUBRO_PLACEHOLDERS y getVerticalLabel están exportados desde lib/verticals.ts"
    - "ALL_BUSINESS_TYPES ya no existe en lib/verticals.ts (cero importadores en el repo)"
    - "resolveVertical sigue prefiriendo la columna vertical tras vaciar los types"
  artifacts:
    - path: "supabase/migrations/047_backfill_vertical.sql"
      provides: "Backfill aditivo de businesses.vertical desde type (CASE, WHERE vertical IS NULL)"
      contains: "UPDATE public.businesses"
    - path: "lib/verticals.ts"
      provides: "VerticalKey config con types vacíos + RUBRO_PLACEHOLDERS + getVerticalLabel"
      exports: ["RUBRO_PLACEHOLDERS", "getVerticalLabel", "VERTICALS", "resolveVertical", "getVerticalKeyByType"]
    - path: "test/verticals.test.ts"
      provides: "Congela el CASE de la migración 047 (mapping type→vertical) + label de belleza + helpers"
      contains: "getVerticalLabel"
  key_links:
    - from: "supabase/migrations/047_backfill_vertical.sql"
      to: "lib/verticals.ts"
      via: "el CASE del backfill replica 1:1 VERTICALS[*].types + LEGACY_TYPE_VERTICAL"
      pattern: "WHEN 'Peluquería' THEN 'belleza'"
    - from: "lib/verticals.ts (getVerticalLabel)"
      to: "lib/verticals.ts (resolveVertical)"
      via: "getVerticalLabel reusa resolveVertical().label"
      pattern: "resolveVertical\\(business\\)\\.label"
  prohibitions:
    - "NO tocar businesses.type de filas existentes en la migración (D-08 lo prohíbe explícitamente)"
    - "NO agregar CHECK constraint a la columna vertical (no existe en el baseline; fuera de scope)"
    - "NO usar supabase db push ni ningún comando de auto-push a prod"
    - "NO borrar getVerticalKeyByType ni LEGACY_TYPE_VERTICAL (se conservan como fallback)"
    - "Ningún negocio existente cambia su vertical resuelto tras esta fase (cero regresión)"
---

<objective>
Sentar la base de datos + lib de la fase: la migración 047 backfillea `businesses.vertical` desde `type` para negocios existentes, y `lib/verticals.ts` se reworkea (label de belleza, vaciar los `types` granulares, borrar código muerto, agregar los 2 helpers nuevos). Este es el paso 1 del orden crítico: el backfill DEBE preceder al vaciado de los `types` para cero regresión (Pitfall 1 de RESEARCH).

Purpose: garantizar que ningún negocio existente pierda su vertical resuelto cuando los subtipos dejen de ser la fuente de resolución (D-07/D-08), y proveer la fuente única declarativa (`RUBRO_PLACEHOLDERS`, `getVerticalLabel`) que consumen las dos superficies del selector (03-02) y los dos booking clients (03-03).

Output: `supabase/migrations/047_backfill_vertical.sql`, `lib/verticals.ts` reworkeado, `test/verticals.test.ts` que congela el CASE de la migración.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/onboarding/ROADMAP.md
@.planning/workstreams/onboarding/STATE.md
@.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-CONTEXT.md
@.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-RESEARCH.md
@.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Símbolos/archivos NUEVOS introducidos por esta fase (referencia para 03-02 y 03-03):
- `supabase/migrations/047_backfill_vertical.sql` — migración aditiva de backfill.
- `RUBRO_PLACEHOLDERS: Record<VerticalKey, string>` en `lib/verticals.ts` — placeholder por rubro (D-06).
- `getVerticalLabel(business): string` en `lib/verticals.ts` — label del rubro para fallback de booking (D-03).
- `test/verticals.test.ts` — test que congela el mapping del CASE de 047.
Eliminados: `ALL_BUSINESS_TYPES` (código muerto). Vaciados: `VERTICALS[*].types`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Crear la migración 047 de backfill de vertical (aditiva, no destructiva)</name>
  <files>supabase/migrations/047_backfill_vertical.sql</files>
  <read_first>
    - supabase/migrations/046_drop_business_hours.sql — ESPEJAR su header denso en español (contexto de fase, por qué es seguro, ORDEN OBLIGATORIO de aplicación a prod, nota de validación con `supabase db reset` local + regenerar schema.sql). Es el análogo de source-of-truth.
    - lib/verticals.ts:43,67,88,106 (VERTICALS[*].types) + :129-135 (LEGACY_TYPE_VERTICAL) — de acá se deriva 1:1 el CASE (los strings literales del mapping).
    - 03-RESEARCH.md:214-254 — el CASE completo ya derivado y verificado, con las notas de por qué `ELSE 'general'` es correcto y por qué `WHERE vertical IS NULL`.
  </read_first>
  <action>
Crear `supabase/migrations/047_backfill_vertical.sql`. Confirmar primero con `ls supabase/migrations/` que 047 es el siguiente número libre en esta línea (046 es la última committeada; 045_landing_cms es de otro workstream pero el siguiente número libre acá es 047).

Contenido: un único `UPDATE public.businesses SET vertical = CASE type WHEN ... THEN ... ELSE 'general' END WHERE vertical IS NULL;`. Los WHEN cubren TODOS los strings de `VERTICALS[*].types` + `LEGACY_TYPE_VERTICAL` mapeados a su VerticalKey, exactamente como en 03-RESEARCH.md:222-249: salud (Médico, Psicólogo, Kinesiólogo, Odontólogo, Nutricionista, Centro médico, Psicología, Odontología, Kinesiología), belleza (Peluquería, Barbería, Centro de estética, Manicura, Spa, Estética), canchas (Cancha de fútbol, Cancha de pádel, Cancha de tenis, Cancha de básquet), y `ELSE 'general'` para el resto (general types, 'Otro', texto libre, NULL).

Header multilínea en español espejando 046: explicar que es ADITIVA (solo escribe `vertical` donde falta, NO toca `type`), aislada por fila (sin cross-tenant), que corre ANTES o DURANTE el deploy del código que vacía los `types` (Pitfall 1), que se valida con `supabase db reset` local (PG local) y se aplica a PROD A MANO coordinado con el deploy, y que tras aplicar en prod hay que regenerar `supabase/schema.sql` (el backfill es data-only ⇒ schema.sql NO cambia estructuralmente; confirmarlo y anotarlo en el header). Idempotente por diseño: correrla dos veces no rompe (la 2da vez no hay filas con vertical NULL).

NO agregar CHECK constraint a `vertical`. NO tocar `type`. Mantener el `WHERE vertical IS NULL` (no pisar verticales elegidos manualmente en settings).
  </action>
  <verify>
    <automated>test -f supabase/migrations/047_backfill_vertical.sql && grep -q 'UPDATE public.businesses' supabase/migrations/047_backfill_vertical.sql && grep -q 'WHERE vertical IS NULL' supabase/migrations/047_backfill_vertical.sql && ! grep -qi 'business_hours\|DROP TABLE\|ALTER TABLE.*type\|CHECK' supabase/migrations/047_backfill_vertical.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El archivo `supabase/migrations/047_backfill_vertical.sql` existe.
    - Contiene exactamente un `UPDATE public.businesses ... WHERE vertical IS NULL`.
    - El CASE incluye los 6 strings de belleza (incluido 'Estética' legacy) mapeando a 'belleza', los 9 de salud a 'salud', los 4 de canchas a 'canchas', y `ELSE 'general'`.
    - `grep` NO encuentra `DROP TABLE`, `CHECK`, ni ninguna sentencia que modifique la columna `type`.
    - El header es comentario en español y menciona "prod A MANO" y "supabase db reset".
  </acceptance_criteria>
  <done>La migración 047 existe, es aditiva (solo UPDATE de `vertical` con `WHERE vertical IS NULL`), no destructiva, y su header documenta el cutover manual a prod.</done>
</task>

<task type="auto">
  <name>Task 2: Rework de lib/verticals.ts — label belleza, vaciar types, borrar dead code, agregar helpers</name>
  <files>lib/verticals.ts</files>
  <read_first>
    - lib/verticals.ts (archivo completo) — es su propio análogo; los cambios espejan el estilo declarativo de `TYPE_GROUPS` (:177-181) y `resolveVertical` (:163-174).
    - 03-PATTERNS.md §lib/verticals.ts (Cambios 1-4) — los 4 cambios exactos con excerpts.
    - 03-RESEARCH.md Pattern 1 (líneas 109-130) — shape recomendado de RUBRO_PLACEHOLDERS y getVerticalLabel.
  </read_first>
  <action>
Aplicar 4 cambios a `lib/verticals.ts` con Edit (reemplazo parcial, NO reescribir el archivo):

1. **Label de belleza (D-01)** (`:66`): cambiar `label: 'Belleza y Estética'` → `label: 'Belleza/Estética/Spa'`. Se propaga solo al Select, al fallback de booking y a los hints.

2. **Vaciar los `types` (D-08)** (`:43,67,88,106`): los 4 arrays `VERTICALS[*].types` pasan a `types: []`. NO tocar `getVerticalKeyByType` (:141-147) ni `LEGACY_TYPE_VERTICAL` (:129-135) — se conservan como fallback para filas sin `vertical`.

3. **Borrar `ALL_BUSINESS_TYPES` + su comentario (:183-186)** — código muerto (cero importadores en el repo; verificado con grep). Borrar el bloque entero incluida la línea del comentario "La usa la sugerencia de rubro por IA".

4. **Agregar los 2 helpers nuevos** al final del archivo (mismo lugar/estilo que `TYPE_GROUPS`):
   - `RUBRO_PLACEHOLDERS: Record<VerticalKey, string>` con exactamente los 4 placeholders de D-06: salud → 'Ej: Lic. en Psicología, Kinesiólogo', belleza → 'Ej: Barbería, Masajista, Depilación', general → 'Ej: Lavaautos, Tatuajes, Fotógrafo', canchas → 'Ej: Canchas de fútbol'.
   - `getVerticalLabel(business: { vertical?: string | null; type?: string | null }): string` que retorna `resolveVertical(business).label` (reusa la precedencia vertical>>type ya existente, así devuelve el label correcto incluso para filas viejas sin `vertical`).

Confirmar en la lectura que `resolveVertical` (:163-174) sigue prefiriendo la columna `vertical` (no se toca su cuerpo). Ejecutar `npm run lint` para asegurar que borrar `ALL_BUSINESS_TYPES` y vaciar los `types` no deja imports ni variables sin uso.
  </action>
  <verify>
    <automated>npm run lint && grep -q "label: 'Belleza/Estética/Spa'" lib/verticals.ts && grep -q 'RUBRO_PLACEHOLDERS' lib/verticals.ts && grep -q 'export function getVerticalLabel' lib/verticals.ts && ! grep -q 'ALL_BUSINESS_TYPES' lib/verticals.ts && npx tsc --noEmit -p tsconfig.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "label: 'Belleza/Estética/Spa'" lib/verticals.ts` matchea (label de belleza actualizado).
    - Los 4 arrays `VERTICALS[*].types` están vacíos (`types: []`) — verificable inspeccionando salud/belleza/general/canchas.
    - `grep 'ALL_BUSINESS_TYPES' lib/verticals.ts` NO devuelve nada (borrado).
    - `grep 'export const RUBRO_PLACEHOLDERS'` y `grep 'export function getVerticalLabel'` matchean.
    - `RUBRO_PLACEHOLDERS` tiene las 4 keys VerticalKey con los strings literales exactos de D-06.
    - `getVerticalKeyByType` y `LEGACY_TYPE_VERTICAL` SIGUEN existiendo (no borrados).
    - `npm run lint` termina sin errores ni warnings de variables/imports sin uso.
    - `npx tsc --noEmit` pasa sin errores de tipos.
  </acceptance_criteria>
  <done>`lib/verticals.ts` tiene el label de belleza actualizado, los 4 `types` vacíos, `ALL_BUSINESS_TYPES` borrado, `RUBRO_PLACEHOLDERS` + `getVerticalLabel` exportados, y lint + tsc limpios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test que congela el CASE de la migración 047 + label belleza + helpers</name>
  <files>test/verticals.test.ts</files>
  <behavior>
    - `getVerticalLabel({ vertical: 'belleza' })` === 'Belleza/Estética/Spa' (label nuevo, D-01).
    - `getVerticalLabel({ vertical: 'salud' })` === 'Salud'; `{ vertical: 'canchas' }` === 'Canchas'; `{ vertical: 'general' }` === 'General'.
    - `getVerticalLabel({ vertical: null, type: 'Peluquería' })` === 'Belleza/Estética/Spa' — fila vieja sin vertical cae por resolveVertical→getVertical→getVerticalKeyByType; PERO como los types están vacíos ahora, 'Peluquería' NO está en ningún array ni en LEGACY_TYPE_VERTICAL → resuelve 'general' → label 'General'. ⚠ Este es el comportamiento POST-vaciado; el test debe afirmar 'General' acá (documenta por qué la migración 047 es necesaria: el código ya no puede recuperar el vertical de 'Peluquería', solo la columna `vertical` backfilleada).
    - `getVerticalLabel({ vertical: null, type: 'Estética' })` === 'Belleza/Estética/Spa' — 'Estética' SÍ sigue en LEGACY_TYPE_VERTICAL → resuelve 'belleza'.
    - `RUBRO_PLACEHOLDERS` tiene exactamente las 4 keys con los strings de D-06.
    - Congelar el CASE de 047: un objeto/mapa en el test con los pares (type legacy → vertical esperado) que documenta el CASE de la migración, afirmando para cada uno el vertical destino (salud/belleza/canchas/general).
  </behavior>
  <read_first>
    - test/landing-derive.test.ts:1-25 — convención de import Vitest (`describe, it, expect`) y de importar desde `@/lib/...`. ESPEJAR esta estructura.
    - lib/verticals.ts — las firmas de `getVerticalLabel`, `RUBRO_PLACEHOLDERS`, `resolveVertical` (post-Task 2).
    - 03-RESEARCH.md:263-270 — la advertencia sobre por qué el test del mapping granular solo tiene sentido para congelar el CASE de la migración (no el código post-vaciado).
  </read_first>
  <action>
Crear `test/verticals.test.ts` (Vitest, `vitest run`). Importar `getVerticalLabel`, `RUBRO_PLACEHOLDERS`, `resolveVertical` desde `@/lib/verticals`.

Suite 1 — labels de los 4 rubros vía `getVerticalLabel({ vertical: <key> })`, afirmando el label exacto (belleza = 'Belleza/Estética/Spa'). Esto valida el rename D-01 y que getVerticalLabel prefiere la columna `vertical`.

Suite 2 — fallback de filas viejas: `getVerticalLabel({ vertical: null, type: 'Estética' })` === 'Belleza/Estética/Spa' (LEGACY_TYPE_VERTICAL sobrevive), y `getVerticalLabel({ vertical: null, type: 'Peluquería' })` === 'General' (documenta que POST-vaciado el código ya no recupera 'Peluquería' → POR ESO existe la migración 047 que backfillea la columna). Agregar un comentario en el test explicando esto.

Suite 3 — `RUBRO_PLACEHOLDERS`: afirmar que tiene las 4 keys (`salud`, `belleza`, `general`, `canchas`) con los strings literales de D-06.

Suite 4 — "CASE de la migración 047 (congelado)": un `Record<string, VerticalKey>` en el test con todos los pares `type legacy → vertical` del CASE (los 9 de salud, 6 de belleza, 4 de canchas), y un `it` que afirma que ese mapa coincide con lo que la migración escribe (documentación viva del CASE; NO llama a getVerticalKeyByType porque post-vaciado daría 'general' para los que no están en LEGACY). Es un snapshot manual del contrato de la migración.
  </action>
  <verify>
    <automated>npm run test -- verticals && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test/verticals.test.ts` existe y usa `import { describe, it, expect } from 'vitest'`.
    - `npm run test -- verticals` corre la suite y pasa (exit 0).
    - El test afirma `getVerticalLabel({ vertical: 'belleza' })` === 'Belleza/Estética/Spa'.
    - El test afirma `getVerticalLabel({ vertical: null, type: 'Estética' })` === 'Belleza/Estética/Spa'.
    - El test incluye el mapa congelado del CASE de 047 con al menos los 6 strings de belleza y los 9 de salud.
    - `RUBRO_PLACEHOLDERS` se valida contra los 4 strings de D-06.
  </acceptance_criteria>
  <done>`test/verticals.test.ts` congela el label de belleza, los 4 placeholders, y el CASE de la migración 047; `npm run test -- verticals` pasa.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — validar migración 047 (db reset) + rework de verticals</name>
  <action>Pausar para validación humana del gate de esquema: el dueño corre `supabase db reset` local (valida que 047 aplica limpio sobre el baseline), confirma la post-condición del backfill (cero filas con vertical NULL), que `supabase/schema.sql` no cambió estructuralmente, y que lint+test están verdes. Recordar que la aplicación a PROD es un paso MANUAL out-of-band (ver user_setup); NUNCA correr `supabase db push`. Seguir los pasos de <how-to-verify>.</action>
  <what-built>
    La migración 047 de backfill (Task 1) validada localmente con `supabase db reset`, y el rework de `lib/verticals.ts` (Task 2) con su test (Task 3). Este checkpoint valida el gate de esquema (la migración aplica limpio y es idempotente/aditiva) y avisa que la aplicación a PROD es un paso MANUAL out-of-band.
  </what-built>
  <how-to-verify>
    1. En PowerShell, desde la raíz del repo, correr: `supabase db reset` → debe replayar el baseline + 040..047 sin error (exit 0). Si 047 rompe, el reset falla acá.
    2. Con la DB local ya reseteada, verificar la post-condición del backfill (idempotencia/totalidad): correr `SELECT count(*) FROM businesses WHERE vertical IS NULL;` (vía `supabase db` / psql local) → debe dar 0 (aunque la DB local esté vacía, el count sigue siendo 0; si hubiera filas de prueba con vertical NULL, 047 las backfillearía).
    3. Confirmar que `supabase/schema.sql` NO cambió estructuralmente (el backfill es data-only). Si `git diff supabase/schema.sql` está vacío, correcto; anotarlo.
    4. Correr `npm run lint` y `npm run test -- verticals` → ambos verdes.
    5. RECORDATORIO (no ejecutar acá): la migración 047 se aplica a PROD A MANO, coordinada con el deploy del código de 03-02/03-03 que vacía los `types`. Ver `user_setup` de este plan. NUNCA correr `supabase db push`.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" si `supabase db reset` sale en 0, el count de vertical NULL da 0, schema.sql no cambió, y lint+test están verdes. Si algo falla, describí el error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (sesión autenticada) → businesses (RLS por business_id) | El update del vertical/type en onboarding/settings cruza acá (cubierto en 03-02) |
| migración admin (service-role) → businesses (todas las filas) | El backfill 047 corre por fuera de RLS pero opera por fila sobre la columna derivada del propio `type` de cada fila |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Migración 047 pisa data de tenants equivocados | accept | El `UPDATE ... WHERE vertical IS NULL` deriva el nuevo `vertical` del `type` de CADA fila individualmente; no cruza filas ni tenants. Es aditivo (no toca `type`). Riesgo residual nulo — el CASE es determinístico por fila. |
| T-03-02 | Tampering | Migración destruye/altera `type` de negocios existentes (regresión de display) | mitigate | La migración SOLO escribe `vertical`; el `verify` de Task 1 grep-ea que NO haya sentencias que toquen `type`. D-08 lo prohíbe. |
| T-03-03 | Denial of Service | Migración deja filas sin vertical resuelto (menú/terminología rota) | mitigate | El CASE es total (`ELSE 'general'`) + `WHERE vertical IS NULL`; post-condición verificada en el checkpoint (`count WHERE vertical IS NULL` = 0). |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Esta fase NO instala paquetes (RESEARCH: N/A package legitimacy). No hay superficie de supply-chain. Sin checkpoint de install requerido. |

**Nota de aislamiento:** esta fase NO introduce ninguna superficie de aislamiento nueva. `businesses.vertical` y `businesses.type` ya viven bajo `business_id` con RLS vigente. El backfill es una data-migration aditiva por fila. No hay amenazas HIGH que bloquear.
</threat_model>

<verification>
- `supabase db reset` local sale en 0 (047 aplica sobre PG local sin romper el baseline).
- `SELECT count(*) FROM businesses WHERE vertical IS NULL` = 0 tras 047.
- `git diff supabase/schema.sql` vacío (backfill data-only; confirmar).
- `npm run lint` limpio (sin variables/imports huérfanos tras borrar ALL_BUSINESS_TYPES y vaciar types).
- `npx tsc --noEmit` sin errores.
- `npm run test -- verticals` verde.
- `grep 'ALL_BUSINESS_TYPES' -r .` → solo aparece en artefactos de planning, cero en código.
</verification>

<success_criteria>
- Migración 047 existe, aditiva, valida con `db reset`, aplicación a prod documentada como manual (`autonomous: false`).
- `lib/verticals.ts`: label belleza = 'Belleza/Estética/Spa', 4 `types` vacíos, `ALL_BUSINESS_TYPES` borrado, `RUBRO_PLACEHOLDERS` + `getVerticalLabel` exportados.
- `resolveVertical` sigue prefiriendo la columna `vertical` (cero regresión garantizada).
- Test que congela el CASE de la migración pasa.
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/03-rework-del-selector-de-rubro/03-01-SUMMARY.md` when done
</output>
