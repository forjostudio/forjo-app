---
phase: 04-recuperar-la-cuenta-auth-callback-reset
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-03"]
files_modified:
  - app/(auth)/(split)/forgot-password/page.tsx
  - app/(auth)/(split)/reset-password/page.tsx
autonomous: true
requirements: [AUTH-01, AUTH-02]

must_haves:
  truths:
    - "Un dueño entra a /forgot-password desde el login, pone su email y ve el estado 'revisá tu mail' — exista o no la cuenta, el resultado en pantalla es idéntico (AUTH-01, D-02, D-14)"
    - "Un dueño con sesión activa puede entrar a /forgot-password y usarlo: no lo rebotan al dashboard (D-06)"
    - "Con el link del mail, el dueño setea una contraseña nueva y queda adentro del panel sin volver a pasar por el login (AUTH-02, D-16)"
    - "Al guardar la contraseña nueva se cierran las otras sesiones y la actual sobrevive (D-17)"
    - "Un link vencido, ya usado, o una entrada directa a /reset-password sin sesión caen en /forgot-password con el aviso arriba y el campo Email listo — nunca en el login con un toast (D-18)"
  artifacts:
    - path: "app/(auth)/(split)/forgot-password/page.tsx"
      provides: "Pedido del link de recuperación + estado enviado + aviso de link vencido"
      contains: "resetPasswordForEmail"
    - path: "app/(auth)/(split)/reset-password/page.tsx"
      provides: "Seteo de la contraseña nueva con guard de sesión y cierre de otras sesiones"
      contains: "updateUser"
  key_links:
    - from: "app/(auth)/(split)/forgot-password/page.tsx"
      to: "components/auth/check-your-email.tsx"
      via: "render del estado enviado (D-02) con reenvío y cooldown (D-05)"
      pattern: "CheckYourEmail"
    - from: "app/(auth)/(split)/reset-password/page.tsx"
      to: "lib/auth/callback.ts"
      via: "import de INVALID_LINK_DEST para el guard de sesión (Pitfall 5 / D-18)"
      pattern: "INVALID_LINK_DEST"
    - from: "app/(auth)/(split)/reset-password/page.tsx"
      to: "app/(dashboard)/layout.tsx"
      via: "router.push('/dashboard') — el guard existente resuelve onboarding/suspendido (D-16, H-07)"
      pattern: "'/dashboard'"
---

<objective>
Las dos pantallas del recorrido de recuperación: `/forgot-password` (pedir el link — AUTH-01) y
`/reset-password` (setear la contraseña nueva y quedar adentro — AUTH-02). Las dos viven en el route group
`(split)` y heredan el panel Bauhaus del layout del plan `04-03`.

Purpose: es el agujero que deja gente afuera de su negocio hoy — `app/(auth)/` tiene solo login y register, y
`resetPasswordForEmail` tiene **cero ocurrencias en el repo**.
Output: `app/(auth)/(split)/forgot-password/page.tsx`, `app/(auth)/(split)/reset-password/page.tsx`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-CONTEXT.md
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Tipo | Dónde |
|----------|------|-------|
| `app/(auth)/(split)/forgot-password/page.tsx` | archivo nuevo | — |
| Ruta `/forgot-password` | URL nueva | — |
| `ForgotPasswordPage` | componente default export | `forgot-password/page.tsx` |
| `app/(auth)/(split)/reset-password/page.tsx` | archivo nuevo | — |
| Ruta `/reset-password` | URL nueva | — |
| `ResetPasswordPage` | componente default export | `reset-password/page.tsx` |

> **Decisión del planner — no se pasa `redirectTo` / `emailRedirectTo`.** RESEARCH §Pattern 2 muestra
> `resetPasswordForEmail(email, { redirectTo: ... })`. Se **diverge a propósito**: los templates de mail
> (plan `04-06`) arman el href con `{{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=recovery`, o sea
> que el destino lo fija la config del proyecto de Supabase y **el valor que mande el cliente se ignora**.
> Pasarlo sería código muerto que además reintroduce un valor controlable desde el navegador en la superficie
> donde un agujero entrega cuentas. Es la misma lógica que RESEARCH aplica a la tabla de destinos
> (*"la mejor defensa contra open redirect es no tener superficie"*), un nivel más arriba. **Consecuencia para el
> checkpoint (`04-06`): el `Site URL` del Dashboard pasa a ser load-bearing y hay que verificarlo**, además de la
> allowlist de Redirect URLs que D-20 fija igual (Phase 5 la necesita para OAuth).

<tasks>

<task type="auto">
  <name>Task 1: /forgot-password — pedir el link (AUTH-01, D-01/D-02/D-09/D-14/D-18)</name>

  <files>app/(auth)/(split)/forgot-password/page.tsx</files>

  <read_first>
    - `app/(auth)/(split)/login/page.tsx` (tras el plan `04-03`) — el set de imports de una página de auth con form, el `schema` de zod con `email: z.string().email('Email inválido')`, el setup de `useForm` + `zodResolver`, el markup exacto de Label + Input + error inline, y el Button con `disabled={loading}`.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md` §"`app/(auth)/(split)/forgot-password/page.tsx`" — tiene todos los excerpts a espejar **y la divergencia obligatoria del `onSubmit`**.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Pitfall 4" (enumeration) y §"Pattern 2".
    - `components/auth/check-your-email.tsx` (plan `04-03`) — las props de `CheckYourEmail`.
    - `app/(auth)/(split)/layout.tsx` (plan `04-03`) — ya aporta el `div` raíz, el panel y el `max-w-[340px]`: esta página renderiza **solo** su contenido, sin wrappers de layout.
  </read_first>

  <action>
    Creá `app/(auth)/(split)/forgot-password/page.tsx`, `'use client'`, default export `ForgotPasswordPage`.
    **No** repitas el markup del split: el layout del route group ya lo aporta. El `return` arranca en el `<h1>`.

    **Copy (D-09, literal — no lo cambies):** `<h1>` **`Recuperá tu cuenta`** y subtítulo
    **`Te mandamos un link para crear una contraseña nueva`**. Mismas clases que el h1/subtítulo del login.

    **Form:** schema de zod con un solo campo `email: z.string().email('Email inválido')` (mismo mensaje que
    login/register). `react-hook-form` + `zodResolver`, `Label` siempre visible, error inline en
    `<p className="text-destructive text-sm">`, `useState(loading)` con `disabled={loading}` en el Button.
    Texto del Button: **`Enviar link`**, y **`Enviando...`** mientras carga.

    **Estado enviado (D-02):** `useState<string | null>(sent)` con el email enviado. Cuando `sent !== null`, el
    form **se reemplaza** por `<CheckYourEmail>` **en el mismo card** — no navegás a otra pantalla, no tirás un
    toast. Props:
    - `email={sent}`
    - `title="Revisá tu mail"`
    - `description`: **`Si hay una cuenta con {sent}, te va a llegar un link para crear una contraseña nueva. El link vence en 1 hora.`**
      (el email en `<strong>`). El condicional *"Si hay una cuenta con"* es deliberado: es idéntico exista o no la
      cuenta (D-02) **y** además es honesto — no afirma que mandamos un mail que quizá no mandamos. Es la misma
      exigencia de honestidad que AUTH-06 le impone al alta. "1 hora" sale de D-19 (`otp_expiry = 3600`, que no se toca).
    - `onResend`: vuelve a llamar `resetPasswordForEmail` con el mismo email, **ignorando el resultado** igual que
      el submit.
    - `children`: un `<p className="text-center text-sm mt-5">` con `<Link href="/login">` y el texto
      **`Volver al login`** (D-02, literal).

    <!-- planner-discipline-allow: toast.error -->
    <!-- planner-discipline-allow: redirectTo -->
    **`onSubmit` — DIVERGENCIA OBLIGATORIA del analog, leé esto antes de escribir una línea:**
    `login/page.tsx:32-45` hace `if (error) { toast.error(...); return }`. **Esa rama está PROHIBIDA acá.**
    El `onSubmit` llama `await supabase.auth.resetPasswordForEmail(data.email)` (cliente de navegador,
    `@/lib/supabase/client`, sync sin `await` en el `createClient()`), **descarta el resultado** y hace
    `setSent(data.email)` **siempre**. Cualquier rama que dependa del resultado convierte el flujo en un oráculo
    de quién tiene cuenta en Forjo (D-02, D-14, Pitfall 4). Poné un comentario en español arriba del `onSubmit`
    explicando que el `error` se descarta **a propósito** y que no es un olvido — si alguien "arregla" esto
    después, reintroduce el agujero.
    **No pasás `redirectTo`**: el destino lo fija el template con `{{ .SiteURL }}` (ver la nota de arriba).
    Si `toast` deja de usarse en el archivo, **no lo importes** (eslint marca el unused).

    **Aviso de link vencido (D-18):** la página lee el search param `error`. Cuando vale `invalid_link` (es a
    donde el callback manda todo fallo, vía `INVALID_LINK_DEST`), renderiza **arriba del form** un aviso con el
    copy exacto **`Ese link ya venció o ya se usó. Pedí uno nuevo.`**, y el campo Email queda listo abajo — el
    error y la solución en la misma pantalla, en un click. Estilo: bloque con `border` + fondo sutil, texto
    `text-sm`, sin `text-destructive` a full (es información, no un error del usuario); usá los tokens del design
    system, nada hardcodeado. **Nunca** un rebote al login con toast: ese es el "error opaco" que el criterio 2 de
    la fase prohíbe.
    Para leer el param en un client component de Next 16 usá `useSearchParams()` de `next/navigation`. **Ojo:**
    `useSearchParams()` obliga a envolver el componente que lo usa en un `<Suspense>` para el prerender — resolvelo
    con el patrón que Next 16 exija (verificá contra `node_modules/next/dist/docs/`, no asumas la API de Next 14).
    Alternativa aceptable si simplifica: leer el param en el `useEffect` inicial desde
    `new URLSearchParams(window.location.search)`, como ya hace `register/page.tsx:38-43`.

    Accesibilidad: el aviso de D-18 va con `role="status"` (no `role="alert"`: no es una interrupción, es el
    estado de la pantalla al llegar).
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/(auth)/(split)/forgot-password/page.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - `app/(auth)/(split)/forgot-password/page.tsx` existe, es `'use client'` y exporta default `ForgotPasswordPage`.
    - `grep -c "resetPasswordForEmail" "app/(auth)/(split)/forgot-password/page.tsx"` ≥ 1.
    - `grep -c "Recuperá tu cuenta" "app/(auth)/(split)/forgot-password/page.tsx"` == 1 y `grep -c "Te mandamos un link para crear una contraseña nueva" "app/(auth)/(split)/forgot-password/page.tsx"` == 1 (D-09, literal).
    - `grep -c "Ese link ya venció o ya se usó. Pedí uno nuevo." "app/(auth)/(split)/forgot-password/page.tsx"` == 1 (D-18, literal).
    - `grep -c "Volver al login" "app/(auth)/(split)/forgot-password/page.tsx"` == 1 (D-02).
    - `grep -c "CheckYourEmail" "app/(auth)/(split)/forgot-password/page.tsx"` ≥ 1 (usa el componente compartido, no duplica el patrón).
    - **Anti-enumeration (T-04-12), la aserción central:** en el cuerpo del archivo sin comentarios, el resultado de `resetPasswordForEmail` no alimenta ninguna condición — `grep -v '^\s*//' "app/(auth)/(split)/forgot-password/page.tsx" | grep -c "toast.error"` == 0.
    - `grep -c "redirectTo" "app/(auth)/(split)/forgot-password/page.tsx"` == 0 (el destino lo fija el template).
    - `npm run build` exits 0 y `/forgot-password` aparece como ruta.
    - `npx eslint "app/(auth)/(split)/forgot-password/page.tsx"` exits 0 (sin imports huérfanos).
  </acceptance_criteria>

  <done>Un dueño pide su link desde `/forgot-password` y ve siempre la misma respuesta; el que llega con un link muerto ve qué pasó y cómo pedir otro sin cambiar de pantalla.</done>
</task>

<task type="auto">
  <name>Task 2: /reset-password — contraseña nueva, otras sesiones cerradas, adentro del panel (AUTH-02, D-03/D-16/D-17)</name>

  <files>app/(auth)/(split)/reset-password/page.tsx</files>

  <read_first>
    - `app/(auth)/register/page.tsx` (líneas 17-26 y 93-116) — **es el dueño del `.refine()` que D-03 espeja** y el markup exacto de los dos campos de contraseña. Copialos quitando el campo `email`.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md` §"`app/(auth)/(split)/reset-password/page.tsx`" — incluye la nota de que el guard client-side **no tiene precedente en el repo** y la trampa de orden del submit.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Pitfall 5" (guard de sesión), §"D-17 es implementable: `scope: 'others'`" (incluida la ⚠ trampa de orden y la nota A4), y §H-07 (`/suspendido`).
    - `app/(dashboard)/layout.tsx` (líneas 15-31) — el guard que D-16 reusa: `if (!business) redirect('/onboarding')` (línea 23) y `if (planStatus === 'suspended') redirect('/suspendido')` (línea 31). **No dupliques esta lógica acá.**
    - `lib/auth/callback.ts` (plan `04-01`) — `INVALID_LINK_DEST`.
    - `supabase/config.toml` §`[auth]` — `minimum_password_length = 6`.
  </read_first>

  <action>
    Creá `app/(auth)/(split)/reset-password/page.tsx`, `'use client'`, default export `ResetPasswordPage`. Igual
    que la Task 1: el layout aporta el split, la página arranca en el `<h1>`.

    **Copy (discreción del planner, resuelta acá):** `<h1>` **`Creá tu contraseña nueva`** y subtítulo
    **`Con esta contraseña vas a entrar de ahora en más.`** Sigue la estructura de D-09 (imperativo + subtítulo que
    anticipa qué va a pasar).

    **Form (D-03):** copiá el schema de `register/page.tsx:17-24` **sacando el campo `email`**: `password` con
    `.min(6, 'Mínimo 6 caracteres')` (el mínimo que exige GoTrue: `minimum_password_length = 6`) + `confirmPassword`
    + el `.refine()` con `message: 'Las contraseñas no coinciden'` y `path: ['confirmPassword']`. Mismos mensajes,
    mismo markup de los dos campos (`register/page.tsx:93-116`). Button: **`Guardar y entrar`** / **`Guardando...`**.

    **Aviso de D-17 (discreción del planner, resuelta acá):** debajo del Button, un `<p className="text-muted-foreground text-xs mt-3">` con
    **`Al guardar, se cierran tus sesiones abiertas en otros dispositivos. Vas a tener que volver a entrar ahí.`**
    Va **antes** de la acción, no después: D-17 acepta el precio de que el olvidadizo tenga que volver a entrar en
    el celu, y el trato es **cubrirlo con copy** — avisar después de hacerlo no es avisar.

    **Guard de sesión (Pitfall 5 — pieza SIN PRECEDENTE en el repo: todos los guards existentes son server).**
    La página es client, así que el guard va acá y **no** en el proxy (sumarla a `isDashboardRoute` haría que el
    proxy redirija a `/login`, el "error opaco" que D-18 prohíbe — por eso el plan `04-02` la dejó afuera a
    propósito):
    - Estado `checking: boolean` inicializado en `true`.
    - `useEffect` de montaje: `const { data: { user } } = await supabase.auth.getUser()`. Si **no hay user** →
      `router.replace(INVALID_LINK_DEST)` (importado de `@/lib/auth/callback`) y no renderiza el form. Si hay user
      → `setChecking(false)`.
    - Mientras `checking` es `true`, **no renderices el form**: mostrá un placeholder mínimo (un `<p>` con
      "Cargando..." o un skeleton acorde al design system). Sin esto se ve un flash del form antes del redirect.
    - Usá `router.replace`, **no** `router.push`: el que rebota no debe poder volver con el botón Atrás a una
      pantalla muerta.
    - Cubrí el cleanup del efecto async (`let cancelled = false` + chequeo antes de setear estado), como hace
      `components/dashboard/plan-banner.tsx:44-61`.

    **`onSubmit` — el orden importa y está verificado:**
    1. `await supabase.auth.updateUser({ password: data.password })` — **PRIMERO**.
    2. `await supabase.auth.signOut({ scope: 'others' })` — **DESPUÉS** (D-17). Al revés, la sesión actual podría
       quedar sin token válido para autorizar el `updateUser`. `scope: 'others'` cierra todas las demás sesiones y
       **no** dispara sign-out en la actual (verificado en `@supabase/auth-js` `types.d.ts:1555-1567`).
    3. `router.push('/dashboard')` + `router.refresh()` (D-16). **No** chequees acá si el dueño tiene negocio ni
       si su plan está suspendido: el guard de `app/(dashboard)/layout.tsx` ya manda a `/onboarding` (línea 23) o
       a `/suspendido` (línea 31, H-07). No dupliques esa lógica en una pantalla de auth.

    Manejo de error del paso 1: si `updateUser` devuelve error, `toast.error` con el mensaje y `setLoading(false)`,
    **quedándote en la página** para que el usuario reintente. Acá el `toast.error` **sí** está permitido (a
    diferencia de `/forgot-password`): el usuario ya está autenticado por el token, así que no hay oráculo de
    enumeration posible. Si el paso 2 falla, **no rompas el flujo**: la contraseña ya se cambió, que es lo que el
    usuario pidió — logueá `console.error('[reset-password] signOut others:', error.message)` y seguí al paso 3.

    Comentario de cabecera en español: que la sesión de esta página la creó `/auth/callback` con `verifyOtp` y es
    la **única** fuente de sesión de este flujo; el porqué del orden `updateUser` → `signOut`; y que el destino
    `/dashboard` delega en el guard que ya existe (D-16).
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/(auth)/(split)/reset-password/page.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - `app/(auth)/(split)/reset-password/page.tsx` existe, es `'use client'` y exporta default `ResetPasswordPage`.
    - `grep -c "Creá tu contraseña nueva" "app/(auth)/(split)/reset-password/page.tsx"` == 1.
    - `grep -c "confirmPassword" "app/(auth)/(split)/reset-password/page.tsx"` ≥ 3 y `grep -c "Las contraseñas no coinciden" "app/(auth)/(split)/reset-password/page.tsx"` == 1 (D-03 espeja el `.refine()` de register).
    - `grep -c "se cierran tus sesiones abiertas en otros dispositivos" "app/(auth)/(split)/reset-password/page.tsx"` == 1 (D-17 cubierto con copy, antes de la acción).
    - `grep -c "scope: 'others'" "app/(auth)/(split)/reset-password/page.tsx"` == 1 (D-17).
    - **Orden verificado (la trampa):** en `grep -n "updateUser\|scope: 'others'"`, la línea de `updateUser` tiene número **menor** que la de `scope: 'others'`.
    - `grep -c "INVALID_LINK_DEST" "app/(auth)/(split)/reset-password/page.tsx"` ≥ 1 y `grep -c "getUser()" "app/(auth)/(split)/reset-password/page.tsx"` ≥ 1 (guard de Pitfall 5, destino D-18).
    - `grep -c "router.replace" "app/(auth)/(split)/reset-password/page.tsx"` ≥ 1 (el rebote no queda en el historial).
    - `grep -c "'/onboarding'\|'/suspendido'\|business" "app/(auth)/(split)/reset-password/page.tsx"` == 0 (D-16: no se duplica el guard del panel).
    - `npm run build` exits 0 y `/reset-password` aparece como ruta.
    - `npx eslint "app/(auth)/(split)/reset-password/page.tsx"` exits 0.
  </acceptance_criteria>

  <done>Con el link del mail, el dueño setea su contraseña nueva, las otras sesiones quedan cerradas, y cae en el panel (o en onboarding/suspendido según su estado, vía el guard que ya existe). Sin sesión válida, cae en `/forgot-password` con el aviso — nunca en el login.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuario anónimo → `/forgot-password` | Cualquiera puede probar mails y observar la respuesta. |
| Sesión creada por `verifyOtp` → `/reset-password` | La única fuente de sesión de este flujo es el canje del token en el callback. |
| `/reset-password` → GoTrue | Escribe la contraseña y revoca sesiones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Information Disclosure | `/forgot-password` — user enumeration | mitigate | El `onSubmit` **descarta el resultado** de `resetPasswordForEmail` y hace `setSent(email)` siempre (D-02, D-14). Copy idéntico exista o no la cuenta ("Si hay una cuenta con..."). Verificado por `grep -c "toast.error" == 0` en el cuerpo del archivo. Se cierra explícitamente la rama `if (error) toast.error(...)` que el analog `login/page.tsx:32-45` usa — es el anti-analog de la fase. |
| T-04-15 | Elevation of Privilege | `/reset-password` alcanzable sin sesión válida del token | mitigate | Guard en la **página** (no en el proxy): `getUser()` en el montaje; sin user → `router.replace(INVALID_LINK_DEST)`. `updateUser` opera siempre sobre el JWT de la sesión, así que nadie puede setear la contraseña de otro; el guard cierra la pantalla muerta que violaría el criterio 2. |
| T-04-16 | Elevation of Privilege | La sesión del intruso sobrevive al reset | mitigate | `signOut({ scope: 'others' })` después del `updateUser` (D-17, verificado en `types.d.ts:1566`). **A4 del RESEARCH:** si GoTrue ya revocara las otras sesiones por sí solo (`secure_password_change = false`), el `signOut` sería redundante pero inofensivo. **Se verifica en el UAT con 2 navegadores** (plan `04-06`), no se asume. |
| T-04-01 | Tampering | Open redirect vía valor mandado por el cliente | mitigate | No se pasa `redirectTo` / `emailRedirectTo`: el href lo arma el template con `{{ .SiteURL }}`. No hay valor del navegador que llegue a un redirect. Verificado por `grep -c "redirectTo" == 0`. |
| T-04-13 | Information Disclosure | Rate limit como oráculo lateral en el reenvío | mitigate | El reenvío pasa por `CheckYourEmail` (plan `04-03`), cuyo cooldown de 60s se resetea en `finally` también ante error (D-05, Pitfall 4). |
</threat_model>

<verification>
- `npm run build` exits 0; `/forgot-password` y `/reset-password` aparecen como rutas.
- `npx tsc --noEmit` y `npx eslint "app/(auth)/(split)"` — exits 0.
- `npm test` exits 0.
- El flujo end-to-end (mail real → link → contraseña nueva → panel; otras sesiones muertas; link reusado) es
  **manual** y se cubre en el UAT del plan `04-06`: depende de GoTrue y del template, que no son testeables sin
  red ni credenciales (H-06).
</verification>

<success_criteria>
- AUTH-01: se pide el link desde el login con el email, y la respuesta en pantalla es idéntica exista o no la cuenta.
- AUTH-02: con el link, se setea la contraseña nueva y se queda adentro del panel, sin volver a pasar por el login.
- D-18: link vencido / usado / entrada directa → `/forgot-password` con el aviso y el campo listo. Cero rebotes al login.
- D-17: las otras sesiones quedan cerradas y el usuario está avisado **antes** de guardar.
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-04-SUMMARY.md` when done
</output>
