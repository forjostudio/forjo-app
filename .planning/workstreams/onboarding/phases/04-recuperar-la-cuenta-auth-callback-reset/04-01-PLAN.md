---
phase: 04-recuperar-la-cuenta-auth-callback-reset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auth/callback.ts
  - lib/auth/callback.test.ts
  - app/auth/callback/route.ts
autonomous: true
requirements: [AUTH-02, AUTH-06]

must_haves:
  truths:
    - "Un GET a /auth/callback con token_hash válido y type=recovery deja la sesión en cookies y redirige a /reset-password (D-03)"
    - "Un GET a /auth/callback con token_hash válido y type=signup deja la sesión en cookies y redirige a /onboarding (D-13)"
    - "Un GET a /auth/callback con token vencido, ya usado, ausente o type fuera de la allowlist redirige a /forgot-password?error=invalid_link — nunca al login (D-18)"
    - "El token_hash nunca aparece en la URL de destino ni en los logs"
  artifacts:
    - path: "lib/auth/callback.ts"
      provides: "parseCallbackParams + resolveDestination + INVALID_LINK_DEST — puros, sin red"
      exports: ["parseCallbackParams", "resolveDestination", "INVALID_LINK_DEST"]
    - path: "lib/auth/callback.test.ts"
      provides: "Cobertura de la allowlist de type, token_hash ausente y no-reflejo de input"
      contains: "resolveDestination"
    - path: "app/auth/callback/route.ts"
      provides: "Canje token_hash → sesión, con dispatch por parámetro extensible por Phase 5"
      exports: ["GET"]
  key_links:
    - from: "app/auth/callback/route.ts"
      to: "lib/auth/callback.ts"
      via: "import de parseCallbackParams / resolveDestination / INVALID_LINK_DEST"
      pattern: "from '@/lib/auth/callback'"
    - from: "app/auth/callback/route.ts"
      to: "lib/supabase/server.ts"
      via: "createClient() async — anon + cookies, NUNCA service role"
      pattern: "from '@/lib/supabase/server'"
---

<objective>
Construir `/auth/callback`: la ruta de intercambio de token por sesión que hoy no existe y que la Phase 5
va a reusar **sin reescribirla**. Estrena los dos flujos de mail de esta fase (`recovery` y `signup`) y deja
el paso 2 (canje) como único punto de extensión para el `oauth` de Phase 5 (D-13 + ROADMAP §Phase 5).

Purpose: sin esta ruta no hay recuperación de contraseña (AUTH-02) ni confirmación honesta del alta
(AUTH-06). Es la pieza de infraestructura común del milestone — se construye una sola vez.
Output: `lib/auth/callback.ts` (puro y testeable), `lib/auth/callback.test.ts`, `app/auth/callback/route.ts`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-CONTEXT.md
</context>

## Artifacts this phase produces

Símbolos y archivos **nuevos** creados por este plan (no existen hoy en el repo):

| Artifact | Tipo | Dónde |
|----------|------|-------|
| `lib/auth/callback.ts` | archivo nuevo | `lib/auth/` (carpeta nueva) |
| `parseCallbackParams` | función exportada | `lib/auth/callback.ts` |
| `resolveDestination` | función exportada | `lib/auth/callback.ts` |
| `INVALID_LINK_DEST` | constante exportada | `lib/auth/callback.ts` |
| `ParsedCallback` | tipo exportado | `lib/auth/callback.ts` |
| `ALLOWED_TYPES` | constante de módulo (privada) | `lib/auth/callback.ts` |
| `DESTINATIONS` | constante de módulo (privada) | `lib/auth/callback.ts` |
| `lib/auth/callback.test.ts` | archivo nuevo | `lib/auth/` |
| `app/auth/callback/route.ts` | archivo nuevo | `app/auth/callback/` (carpeta nueva) |
| `GET` | route handler exportado | `app/auth/callback/route.ts` |
| Ruta `/auth/callback` | URL nueva | — |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/auth/callback.ts — las piezas puras (parse + tabla de destinos)</name>

  <files>lib/auth/callback.ts</files>

  <read_first>
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Code Examples" (líneas ~905-939): el código de este módulo **ya está escrito ahí**. Copialo, no lo reinventes.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Decisión: la forma de `/auth/callback`" → tabla de destinos y por qué NO hay parámetro de retorno.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md` §"`lib/auth/callback.ts`": convenciones del módulo puro.
    - `lib/crm-reports.ts` — el analog de módulo `lib/` puro: named exports, constantes `UPPER_SNAKE_CASE`, secciones con barras Unicode, comentarios en español.
  </read_first>

  <behavior>
    - `parseCallbackParams(new URLSearchParams('token_hash=abc&type=recovery'))` → `{ ok: true, token_hash: 'abc', type: 'recovery' }`
    - `parseCallbackParams(new URLSearchParams('token_hash=abc&type=signup'))` → `{ ok: true, token_hash: 'abc', type: 'signup' }`
    - `type` fuera de la allowlist (`oauth`, `magiclink`, `email_change`, `invite`, `../../etc`) → `{ ok: false }`
    - `token_hash` ausente o string vacío → `{ ok: false }`
    - `resolveDestination('recovery')` → `'/reset-password'` · `resolveDestination('signup')` → `'/onboarding'`
    - `resolveDestination` con cualquier input desconocido (`https://evil.com`, `//evil.com`, `/\evil.com`, `javascript:alert(1)`) → `INVALID_LINK_DEST`, nunca el input
  </behavior>

  <action>
    Creá la carpeta `lib/auth/` y el módulo `lib/auth/callback.ts`, **puro**: sin importar `@supabase/supabase-js`
    en runtime, sin `next/headers`, sin red. El único import permitido es el **type-only** `EmailOtpType` desde
    `@supabase/supabase-js`.

    Exportá exactamente estos símbolos (nombres literales, named exports — nunca default):
    - `INVALID_LINK_DEST` — constante string con valor `/forgot-password?error=invalid_link` (D-18: el único
      destino de error de todo el flujo; nunca un rebote al login).
    - `ParsedCallback` — union type discriminado: `{ ok: true; token_hash: string; type: EmailOtpType }` | `{ ok: false }`.
      Es el patrón de retorno de los helpers de validación del repo (ver `lib/recaptcha.ts`, `lib/booking-core.ts`).
    - `parseCallbackParams(params: URLSearchParams): ParsedCallback`.
    - `resolveDestination(type: string): string`.

    Constantes de módulo privadas (no exportadas), en `UPPER_SNAKE_CASE`:
    - `ALLOWED_TYPES` — un `Set<string>` **cerrado** con `'recovery'` y `'signup'`. Comentá que Phase 5 le suma
      `'oauth'` y que el Set existe porque `EmailOtpType` incluye `(string & {})`, o sea que el tipo acepta
      cualquier string: la validación tiene que ser de runtime, no de tipos.
    - `DESTINATIONS` — `Record<string, string>` con `recovery` → `/reset-password` (D-03) y `signup` →
      `/onboarding` (D-13). Dejá comentada la fila `oauth` → `/dashboard` con la nota de que Phase 5 agrega una
      fila y no reescribe nada.

    `parseCallbackParams` rechaza (`{ ok: false }`) si falta `token_hash`, si falta `type`, o si `type` no está
    en `ALLOWED_TYPES`. `resolveDestination` devuelve `DESTINATIONS[type]` y cae a `INVALID_LINK_DEST` cuando no
    hay fila — **nunca** devuelve ni concatena el input recibido (esa es la mitigación de open redirect T-04-01:
    la defensa es no tener superficie, no sanitizar).

    Comentario de cabecera en español explicando el *por qué* (convención dura del repo): que el módulo es puro
    para poder testearse con Vitest sin credenciales (H-06 del RESEARCH: `vitest.setup.ts` carga `.env.local` =
    PROD), y que la fase **no acepta parámetro de retorno** (`next` / `redirect_to`) a propósito.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `lib/auth/callback.ts` existe y exporta `parseCallbackParams`, `resolveDestination`, `INVALID_LINK_DEST` y el tipo `ParsedCallback`.
    - `grep -c "INVALID_LINK_DEST = '/forgot-password?error=invalid_link'" lib/auth/callback.ts` ≥ 1.
    - `grep -v '^\s*//' lib/auth/callback.ts | grep -c "next/headers\|@/lib/supabase"` == 0 (el módulo es puro: sin Supabase ni Next).
    - El único import de `@supabase/supabase-js` es `import type` (type-only).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>El módulo puro existe, compila, y la allowlist de `type` + la tabla de destinos viven en un solo lugar importable por el route handler y por los tests.</done>
</task>

<task type="auto">
  <name>Task 2: lib/auth/callback.test.ts — el gate de Wave 0 (sin red, sin credenciales)</name>

  <files>lib/auth/callback.test.ts</files>

  <read_first>
    - `lib/crm-reports.test.ts` (líneas 1-12) — el analog **exacto**: test puro, sin `skipIf`, sin `test/env.ts`, fixtures locales, `describe`/`it` en español, alias `@/`.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Code Examples" (líneas ~943-975): el test **ya está escrito ahí**.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Validation Architecture" (H-06 + tabla Req→Test).
    - `vitest.setup.ts` — leé el comentario: la línea 13 carga `.env.local`, que apunta a **PRODUCCIÓN**.
    - `lib/auth/callback.ts` (creado en Task 1).
  </read_first>

  <action>
    Escribí `lib/auth/callback.test.ts` cubriendo el bloque `<behavior>` de la Task 1. Estructura: dos `describe`
    (`parseCallbackParams` y `resolveDestination`) con los casos de la tabla Req→Test del RESEARCH.

    **REGLA DURA (H-06) — ningún test de esta fase toca Supabase.** `vitest.setup.ts:13` hace
    `config({ path: '.env.local' })` y ese archivo apunta a prod: un test que llame `signUp` o
    `resetPasswordForEmail` **crea usuarios reales y quema la cuota de mails en producción**. Este test importa
    **solo** de `@/lib/auth/callback` y no hace red. Está prohibido usar `describe.skipIf(!hasSupabaseCreds)` o
    importar `test/env.ts`: eso es la marca de un test de integración (ver el contra-ejemplo
    `test/booking-public-regression.test.ts:20`). El analog correcto es `lib/crm-reports.test.ts`.

    Casos obligatorios:
    - `parseCallbackParams` acepta `recovery` y `signup` con `token_hash` presente, devolviendo el objeto completo.
    - Rechaza cada uno de estos `type`: `oauth`, `magiclink`, `email_change`, `invite`, `../../etc`. Poné un
      comentario aclarando que `oauth` todavía NO está permitido y que este test es el recordatorio de que
      Phase 5 lo suma a la allowlist a propósito.
    - Rechaza `token_hash` ausente (`type=recovery` solo) y `token_hash` vacío (`token_hash=&type=recovery`).
    - `resolveDestination` mapea `recovery` → `/reset-password` (D-03) y `signup` → `/onboarding` (D-13).
    - **Anti open redirect (T-04-01):** itera sobre `https://evil.com`, `//evil.com`, `/\evil.com` y
      `javascript:alert(1)` y asierta que el resultado es `INVALID_LINK_DEST` en los cuatro. Comentá que estos
      son los tres bypasses clásicos del `startsWith('/')` (Pitfall 2 del RESEARCH) y que el test queda de
      guardia para cuando Phase 5 evalúe sumar un parámetro de retorno.

    Usá un helper local `const sp = (s: string) => new URLSearchParams(s)` y comentarios que anclen cada bloque a
    su decisión (`D-xx`) o Pitfall, como hace `crm-reports.test.ts`.
  </action>

  <verify>
    <automated>npx vitest run lib/auth/callback.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run lib/auth/callback.test.ts` exits 0 con al menos 5 tests pasando.
    - `grep -c "skipIf\|test/env\|hasSupabaseCreds" lib/auth/callback.test.ts` == 0 (H-06: test puro).
    - `grep -c "evil.com" lib/auth/callback.test.ts` ≥ 1 (cobertura anti open redirect).
    - `npx vitest run lib/auth/callback.test.ts` corre en < 5s y **no** requiere `.env.local` (comprobalo temporalmente con `SUPABASE_URL= npx vitest run lib/auth/callback.test.ts` o razonando que no hay import de red).
  </acceptance_criteria>

  <done>La allowlist de `type`, la tabla de destinos y el no-reflejo de input están cubiertos por tests automáticos que corren sin red ni credenciales.</done>
</task>

<task type="auto">
  <name>Task 3: app/auth/callback/route.ts — el canje del token por sesión</name>

  <files>app/auth/callback/route.ts</files>

  <read_first>
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Architecture Patterns" → "Pattern 1: Route handler del callback": la mecánica **ya está escrita** (líneas ~534-579), con la justificación de `NextResponse.redirect` vs `redirect()` y de 303 vs 307.
    - `app/api/google/callback/route.ts` — **el modelo de ESTILO exacto**: imports, comentario de cabecera en español, closure `fail()`, guard clauses tempranas, `console.error('[modulo/accion] ...', error.message)`. **NO es modelo funcional** (es OAuth de integración).
    - `lib/supabase/server.ts` — el cliente correcto: `createClient()` **async**, anon + cookies. Su `setAll` (líneas 15-24) persiste las cookies que crea `verifyOtp`; en un route handler `cookies()` es escribible, así que el `catch` de la línea 20 no dispara. **No escribas un cliente nuevo.**
    - `lib/auth/callback.ts` (Task 1).
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md` §"`app/auth/callback/route.ts`" — incluye la tabla de **divergencias obligatorias** respecto del analog.
  </read_first>

  <action>
    Creá `app/auth/callback/route.ts` con un único export `GET(request: NextRequest)`.

    **Ubicación:** `app/auth/callback/`, fuera de todo route group. `(auth)` es un route group y no aporta URL —
    el callback necesita el path literal `/auth/callback` (H-04).

    **Cliente Supabase:** `import { createClient } from '@/lib/supabase/server'` y `await createClient()`.
    **PROHIBIDO `@/lib/supabase/admin`** (service role): este endpoint lo alcanza un usuario **anónimo desde un
    link de mail**; el service role bypassa RLS y no tiene nada que hacer acá (T-04-04, skill `supabase-multitenant-rls`).
    <!-- planner-discipline-allow: supabase/admin -->
    <!-- planner-discipline-allow: request.url) -->

    Estructura en 4 pasos — el paso 2 es el **único punto de extensión** para Phase 5 (dejá un comentario que lo
    diga explícito, citando D-13 y el ROADMAP §Phase 5 "suma `oauth`, no reescribe"):
    1. **PARSE + VALIDAR** — `parseCallbackParams(request.nextUrl.searchParams)`. Si `!parsed.ok` → `fail()`.
    2. **CANJEAR** — `supabase.auth.verifyOtp({ token_hash: parsed.token_hash, type: parsed.type })`. Phase 5
       agrega acá la rama `code` → `exchangeCodeForSession`, sin tocar 1, 3 ni 4.
    3. **RESOLVER DESTINO** — `resolveDestination(parsed.type)`, derivado server-side de la tabla por `type`.
    4. **REDIRECT + SCRUB**.

    Definí arriba del flujo una closure `fail()` que devuelva
    `NextResponse.redirect(new URL(INVALID_LINK_DEST, request.url), 303)` — **un solo destino de error** para el
    parse fallido y para el error de `verifyOtp` (link vencido / ya usado, D-18/D-19). Nunca redirijas al login
    con toast: ese es el "error opaco" que el criterio 2 de la fase prohíbe.

    **Divergencias obligatorias del analog `google/callback` — no las copies por inercia:**
    - Usá `new URL(dest, request.url)`, **no** una `base` armada desde `NEXT_PUBLIC_APP_URL` (el analog usa eso
      en su línea 8). `request.url` no depende de una env var.
    - Pasá **`303` explícito** como segundo argumento de `NextResponse.redirect`. El default es 307, que
      **preserva el método**; acá el navegador debe hacer GET del destino (doc de Next 16, `redirect.md:212-214`).
    - Seteá `res.headers.set('Referrer-Policy', 'no-referrer')` en la respuesta de éxito (capa 3 de la mitigación
      de fuga del token, T-04-02). El analog no lo hace.

    **Nunca uses `redirect()` de `next/navigation` acá:** lanza `NEXT_REDIRECT` y no permite adjuntar headers ni
    cookies a la respuesta — y el callback necesita `Set-Cookie` (sesión) + `Referrer-Policy` en la misma
    respuesta. Precedente en el repo: el comentario de `app/(dashboard)/layout.tsx:27-29`.

    **Logging (T-04-02):** `console.error('[auth/callback] verifyOtp:', error.message)` — **solo el `.message`**.
    Está prohibido loguear `request.url` o el `token_hash`: la query string es lo que termina en los logs de
    Vercel. Logueá distinto el fallo del paso 1 y el del paso 2 (Pitfall 1: si el template del mail está mal, el
    error aparece en el parse, no en el canje — esa distinción hace el diagnóstico obvio en 5 segundos).

    Comentario de cabecera en español explicando: por qué `token_hash` + `verifyOtp` y no `code` +
    `exchangeCodeForSession` (`@supabase/ssr` fuerza `flowType: "pkce"` y `exchangeCodeForSession` exige un
    `code_verifier` del navegador que inició el flujo — que en un webview in-app de Gmail no existe; H-03), y que
    esta fase **no acepta parámetro de retorno** a propósito.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx eslint app/auth/callback/route.ts</automated>
  </verify>

  <acceptance_criteria>
    - `app/auth/callback/route.ts` existe y exporta `async function GET`.
    - `grep -c "verifyOtp(" app/auth/callback/route.ts` ≥ 1.
    - `grep -c "from '@/lib/supabase/server'" app/auth/callback/route.ts` == 1.
    - `grep -c "supabase/admin" app/auth/callback/route.ts` == 0 (T-04-04: jamás service role acá).
    - `grep -c "Referrer-Policy" app/auth/callback/route.ts` ≥ 1 y `grep -c "303" app/auth/callback/route.ts` ≥ 1.
    - `grep -v '^\s*//' app/auth/callback/route.ts | grep -c "request.url)" ` ≥ 1 y `grep -v '^\s*\*\|^\s*//' app/auth/callback/route.ts | grep -c "console.error(.*request.url\|console.log"` == 0 (T-04-02: la URL nunca se loguea).
    - `grep -c "from '@/lib/auth/callback'" app/auth/callback/route.ts` == 1 (el ruteo y la validación vienen del módulo puro, no están inline).
    - `npx tsc --noEmit` exits 0 y `npx eslint app/auth/callback/route.ts` exits 0.
  </acceptance_criteria>

  <done>`GET /auth/callback?token_hash=X&type=recovery` canjea el token, escribe las cookies de sesión y devuelve 303 a `/reset-password` con `Referrer-Policy: no-referrer`; cualquier fallo devuelve 303 a `/forgot-password?error=invalid_link`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente de mail (webview in-app) → `/auth/callback` | Un usuario **anónimo** llega con un `token_hash` en la query string. Todo lo que viene en la URL es input no confiable. |
| `/auth/callback` → GoTrue | El canje del token es la **única** fuente de sesión de este flujo. |
| `/auth/callback` → navegador | La respuesta escribe cookies de sesión `httpOnly`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `resolveDestination` / destino del redirect | mitigate | **No existe superficie:** la fase no acepta `next`/`redirect_to`. El destino sale de `DESTINATIONS` por `type`, y `resolveDestination` cae a `INVALID_LINK_DEST` ante cualquier input desconocido — nunca lo refleja ni lo concatena. Verificado por `lib/auth/callback.test.ts` con los 3 bypasses (`//evil.com`, `https://evil.com`, `/\evil.com`) + `javascript:`. |
| T-04-02 | Information Disclosure | `token_hash` en URL / `Referer` / logs | mitigate | 4 capas: (1) el callback **nunca renderiza HTML** — solo 303, sin subrecursos que emitan `Referer`; (2) el destino es una **URL limpia** sin token; (3) `Referrer-Policy: no-referrer` en la respuesta; (4) se loguea `error.message`, **nunca** `request.url`. Verificado por grep en acceptance_criteria. |
| T-04-03 | Replay | Link reusado o vencido | mitigate | `verifyOtp` de GoTrue quema el token (single-use) y respeta `otp_expiry = 3600` (D-19, sin tocar). El error cae en `fail()` → `/forgot-password?error=invalid_link` (D-18), nunca en una pantalla muerta. |
| T-04-04 | Elevation of Privilege | Cliente Supabase del callback | mitigate | `@/lib/supabase/server` (anon + cookies). `@/lib/supabase/admin` (service role) **prohibido**: el endpoint lo alcanza un anónimo desde un link de mail. Verificado con `grep -c "supabase/admin" == 0`. |
| T-04-05 | Spoofing | `type` arbitrario en la query | mitigate | `ALLOWED_TYPES` es un `Set` **cerrado** validado en runtime (el tipo `EmailOtpType` incluye `(string & {})` → no alcanza con TypeScript). `oauth` queda **fuera** hasta Phase 5. |
| T-04-06 | Information Disclosure | Logs de acceso de Vercel guardan el path con el `token_hash` | **accept** | Residual asumido. Mitigado por diseño: el token es **single-use** (lo quema `verifyOtp`) y vence en 1h (D-19) → un token que aparece en los logs ya está consumido. Documentar en `/gsd:secure-phase` como riesgo aceptado, no ignorado. |
</threat_model>

<verification>
- `npx vitest run lib/auth/callback.test.ts` — verde.
- `npx tsc --noEmit` — exits 0.
- `npx eslint app/auth/callback/route.ts lib/auth/callback.ts` — exits 0.
- El flujo end-to-end contra GoTrue real **no se verifica en este plan**: requiere el template del mail con
  `{{ .TokenHash }}` (H-02) y el entorno local levantado. Se cubre en el UAT del plan `04-06`.
</verification>

<success_criteria>
- `/auth/callback` existe, rutea `recovery` y `signup`, y su paso de canje está aislado para que Phase 5 sume
  `oauth` sin tocar el resto (D-13).
- La validación de entrada y la tabla de destinos son puras, viven en un solo lugar y están cubiertas por tests
  que corren sin red ni credenciales (H-06).
- Ningún camino del handler refleja input del usuario en un redirect (T-04-01).
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-01-SUMMARY.md` when done
</output>
