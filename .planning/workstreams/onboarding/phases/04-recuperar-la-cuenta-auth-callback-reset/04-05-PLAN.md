---
phase: 04-recuperar-la-cuenta-auth-callback-reset
plan: 05
type: execute
wave: 2
depends_on: ["04-03"]
files_modified:
  - app/(auth)/register/page.tsx
autonomous: true
requirements: [AUTH-06]

must_haves:
  truths:
    - "Después de 'Crear cuenta gratis', el usuario ve un estado 'revisá tu mail' en el mismo card y NO lo mandan a una pantalla que lo va a rebotar al login (AUTH-06, D-12)"
    - "Registrarse con un email que ya tiene cuenta muestra exactamente lo mismo que un alta nueva (D-14)"
    - "El link del mail de confirmación cae en /onboarding con sesión ya creada (D-13)"
    - "/register sigue siendo el card centrado de siempre — no se rediseña (D-10)"
    - "El plan capturado desde los CTAs del landing (?plan=) sobrevive al cambio de flujo"
  artifacts:
    - path: "app/(auth)/register/page.tsx"
      provides: "Alta honesta: estado 'revisá tu mail' en vez del push a /onboarding sin sesión"
      contains: "CheckYourEmail"
  key_links:
    - from: "app/(auth)/register/page.tsx"
      to: "components/auth/check-your-email.tsx"
      via: "render del estado enviado con reenvío y cooldown (D-12, D-05)"
      pattern: "CheckYourEmail"
    - from: "app/(auth)/register/page.tsx"
      to: "app/auth/callback/route.ts"
      via: "el link del mail de confirmación entra por /auth/callback?type=signup (D-13)"
      pattern: "signUp"
---

<objective>
Arreglar **el bug de AUTH-06, que está vivo en producción hoy**: `register/page.tsx:56-57` festeja con un toast
y empuja a `/onboarding` — pero con la confirmación de mail ON (verificado en prod, H-01) `signUp` **no devuelve
sesión**, así que el proxy lo rebota al login. El alta miente.

Purpose: es literalmente el criterio 4 de la fase — *"el registro es honesto: lo que el usuario lee después de
crear la cuenta y a dónde lo mandan coincide con lo que Auth hace de verdad"*. Le pasa a todo el que se registra.
Output: `app/(auth)/register/page.tsx` con el estado "revisá tu mail" (D-12), sin rediseño visual (D-10).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-CONTEXT.md
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md
</context>

## Artifacts this phase produces

| Artifact | Tipo | Dónde |
|----------|------|-------|
| `sent` (estado local del card de alta) | `useState<string \| null>` | `app/(auth)/register/page.tsx` |

Este plan **no crea archivos ni símbolos exportados nuevos**: modifica el `onSubmit` de una página existente y
consume `CheckYourEmail` (creado en el plan `04-03`).

> **Contexto que cambia el encuadre (H-01, verificado read-only contra prod):** `mailer_autoconfirm: false` →
> la confirmación **ya está ON en producción**. Hay 3 usuarios y **0 sin confirmar**, y el más reciente confirmó
> con un click real (30s entre `created_at` y `email_confirmed_at`). **D-11 ya está cumplido y D-15 NO dispara:**
> no hay flag que prender ni usuarios colgados que arreglar a mano. Lo que sí hay es este bug, vivo.

> **Decisión del planner — no se pasa `emailRedirectTo`.** RESEARCH §"Code Examples" lo muestra, pero el template
> de confirmación (plan `04-06`) arma el href con `{{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=signup`:
> el destino lo fija la config del proyecto y el valor que mande el navegador se ignora. Misma decisión y misma
> justificación que en el plan `04-04` — consistencia entre los dos flujos de mail, y cero valor controlable
> desde el cliente en la superficie de auth (T-04-01).

<tasks>

<task type="auto">
  <name>Task 1: el card de alta deja de mentir — estado "revisá tu mail" (D-12, D-14)</name>

  <files>app/(auth)/register/page.tsx</files>

  <read_first>
    - `app/(auth)/register/page.tsx` — **el archivo entero**. Las líneas 45-59 son el `onSubmit` a cambiar; las 56-58 son lo que muere. El `useEffect` de las líneas 37-43 **no se toca**. El card (62-79), el lockup tinta/crema con su comentario (65-71) y el link "¿Ya tenés cuenta?" (121-126) **se conservan**.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Code Examples" → "`register/page.tsx` — el cambio de AUTH-06 (D-12)" (líneas ~979-1001): el ANTES y el DESPUÉS ya están escritos, con la explicación del bug.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §H-01 — por qué esto es un bug vivo y no una mejora preventiva.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md` §"`app/(auth)/register/page.tsx` (MODIFICAR — D-12)".
    - `components/auth/check-your-email.tsx` (plan `04-03`) — las props de `CheckYourEmail`.
    - `lib/supabase/middleware.ts` (líneas 32-48) — la mecánica del rebote: `/onboarding` matchea `isDashboardRoute`, `user === null` → redirect a `/login`. Ese es el bug.
  </read_first>

  <action>
    Agregá un estado `const [sent, setSent] = useState<string | null>(null)` y reescribí el `onSubmit`:

    - `supabase.auth.signUp({ email: data.email, password: data.password })` — **sin** `options`. El destino del
      link lo fija el template con `{{ .SiteURL }}` (ver la nota de arriba).
    - Si devuelve `error`: se conserva el comportamiento actual (`toast.error(error.message)` + `setLoading(false)`
      + `return`). **Acá el toast sí va**: son errores de forma (contraseña débil, mail inválido, rate limit), no un
      oráculo de existencia — con confirmación ON, un mail ya registrado **no** devuelve error, devuelve el mismo
      shape que un alta nueva (D-14).
    - En el camino feliz: **`setSent(data.email)`** y nada más. Muere el toast de éxito y muere la navegación a
      `/onboarding` — **eso es exactamente lo que AUTH-06 pide arreglar**: sin sesión, `/onboarding` matchea
      `isDashboardRoute` en `lib/supabase/middleware.ts:37`, `getUser()` devuelve `null` (líneas 44-48) y el proxy
      rebota al login. El usuario lee "Cuenta creada" y termina mirando la pantalla de la que venía.
      <!-- planner-discipline-allow: router.push -->
      <!-- planner-discipline-allow: toast.success -->
      <!-- planner-discipline-allow: Cuenta creada -->

    **Render (D-12):** cuando `sent !== null`, **el contenido del `<Card>` se reemplaza** por `<CheckYourEmail>` —
    mismo patrón que `/forgot-password` (D-02): son la misma pantalla conceptual, por eso comparten componente.
    **El card, el lockup de arriba y el layout centrado se conservan** — se reemplaza lo de adentro
    (`<CardHeader>` + `<CardContent>`), no la página. **D-10: `/register` NO se mueve al split ni se rediseña.**
    Props de `CheckYourEmail`:
    - `email={sent}`
    - `title="Revisá tu mail"`
    - `description`: **`Te mandamos un mail a {sent}. Confirmá tu cuenta para entrar.`** (el email en `<strong>`).
      Es el copy que fija D-12, textual.
    - `onResend`: `supabase.auth.resend({ type: 'signup', email: sent })`. Verificá la firma exacta contra
      `node_modules/@supabase/auth-js/dist/module/lib/types.d.ts` antes de escribirla — **no la asumas de memoria**
      (RESEARCH verificó `resetPasswordForEmail`, `verifyOtp`, `updateUser` y `signOut`, pero **no** `resend`).
      Descartá el resultado, igual que el submit: mostrar un error acá reintroduciría el oráculo que D-14 cierra.
    - `children`: un `<p className="text-center text-sm text-muted-foreground mt-4">` con `<Link href="/login">` y
      **`Volver al login`**.

    Si `<CardTitle>` / `<CardDescription>` quedan sin usar en la rama `sent`, está bien que el `<CardHeader>`
    directamente no se renderice en ese estado — pero **no borres los imports** si la rama del form los sigue usando.

    Comentario en español arriba del `onSubmit` explicando el porqué (convención del repo): que con
    `enable_confirmations` ON (verificado en prod) `signUp` no devuelve sesión, que por eso el alta termina en
    "revisá tu mail" y no en una navegación, y que un mail ya registrado devuelve el mismo shape a propósito
    (D-14) — el registro no puede ser un oráculo para averiguar el mail de otro.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/(auth)/register/page.tsx"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "setSent(data.email)" "app/(auth)/register/page.tsx"` == 1.
    - `grep -c "CheckYourEmail" "app/(auth)/register/page.tsx"` ≥ 2 (import + uso).
    - `grep -c "Confirmá tu cuenta para entrar" "app/(auth)/register/page.tsx"` == 1 (copy de D-12).
    - **El bug murió:** `grep -c "'/onboarding'" "app/(auth)/register/page.tsx"` == 0 y `grep -c "Cuenta creada" "app/(auth)/register/page.tsx"` == 0.
    - `grep -c "emailRedirectTo" "app/(auth)/register/page.tsx"` == 0 (el destino lo fija el template).
    - El card y el lockup siguen ahí: `grep -c "forjo-gestion-lockup-tinta.png" "app/(auth)/register/page.tsx"` == 1 y `grep -c "<Card>" "app/(auth)/register/page.tsx"` == 1 (D-10: no se rediseña).
    - `git diff "app/(auth)/register/page.tsx"` **no** muestra cambios en el bloque del card centrado (líneas 62-79 de hoy) ni en el link "¿Ya tenés cuenta?".
    - `npm run build` exits 0.
  </acceptance_criteria>

  <done>Crear una cuenta deja al usuario en un estado "revisá tu mail" que dice la verdad, en el mismo card, con reenvío y cooldown — y un mail ya registrado se ve exactamente igual que uno nuevo.</done>
</task>

<task type="auto">
  <name>Task 2: limpieza post-cambio y preservación del ?plan= del landing</name>

  <files>app/(auth)/register/page.tsx</files>

  <read_first>
    - `app/(auth)/register/page.tsx` (resultado de la Task 1).
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md` §"`app/(auth)/register/page.tsx`" → el bloque "`useEffect` del `?plan=` — **NO TOCAR, no perder**" y el Hallazgo 3 (imports huérfanos).
    - `eslint.config.mjs` — el preset `core-web-vitals` + `typescript` que marca los unused.
  </read_first>

  <action>
    **1) Huérfanos (PATTERNS Hallazgo 3).** Al morir la navegación del camino feliz, `router` y su hook quedan sin
    uso en el archivo y `eslint` (`core-web-vitals` + `typescript`) marca el unused var. Revisá el archivo entero:
    si ningún camino que queda usa el router, eliminá tanto la variable como su import de `next/navigation`.
    Si algún camino sí lo sigue usando, dejalo. **La fuente de verdad es `npx eslint`, no la vista rápida.**
    <!-- planner-discipline-allow: useRouter -->
    <!-- planner-discipline-allow: next/navigation -->
    Revisá lo mismo para `toast` (si la rama de error lo sigue usando, se queda) y para
    `CardTitle`/`CardDescription`.

    **2) El `?plan=` no se puede perder.** El `useEffect` de las líneas 37-43 captura `?plan=basic|studio|pro` de
    los CTAs del landing y lo escribe en `localStorage` bajo la clave `forjo_intended_plan`. **Queda tal cual, con
    su comentario.** No lo muevas adentro de ninguna rama de `sent` ni lo condiciones: tiene que correr en el
    montaje, antes de cualquier submit.
    Sobrevive al cambio de flujo por diseño: el usuario vuelve **al mismo navegador** desde el mail, así que el
    `localStorage` sigue intacto cuando el callback lo deja en `/onboarding`. Anotá en el SUMMARY que la
    verificación real de esto es del UAT (plan `04-06`): registrarse con `?plan=studio`, confirmar desde el mail, y
    comprobar que el plan se sigue aplicando post-confirmación.
  </action>

  <verify>
    <automated>npx eslint "app/(auth)/register/page.tsx" && npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `npx eslint "app/(auth)/register/page.tsx"` exits 0 **sin warnings** (cero variables o imports sin usar).
    - `grep -c "forjo_intended_plan" "app/(auth)/register/page.tsx"` == 1 (el `useEffect` del `?plan=` sigue vivo).
    - `grep -c "useEffect" "app/(auth)/register/page.tsx"` ≥ 1 y el `useEffect` del plan sigue con `[]` como dependencia (corre en el montaje).
    - `git diff "app/(auth)/register/page.tsx"` no muestra cambios en las líneas 37-43 de hoy (el bloque del `?plan=` con su comentario).
    - `npx tsc --noEmit` exits 0 y `npm test` exits 0.
  </acceptance_criteria>

  <done>El archivo compila y lintea limpio tras el cambio de flujo, y la captura del plan del landing quedó intacta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuario anónimo → `/register` | Cualquiera puede probar mails y observar la respuesta del alta. |
| `signUp` → GoTrue | Crea el usuario y dispara el mail de confirmación. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Information Disclosure | `/register` — user enumeration por mail ya existente | mitigate | Con `enable_confirmations` ON (H-01: ya ON en prod), GoTrue devuelve el **mismo shape** para un mail nuevo y para uno ya registrado → la UI muestra el mismo `CheckYourEmail` sin ninguna rama (D-14). El `toast.error` de la rama de `error` queda porque cubre errores de forma (contraseña débil, mail inválido), no existencia. El despistado que ya tenía cuenta está cubierto por el "¿Ya tenés cuenta? Iniciar sesión" que ya vive en el card. |
| T-04-18 | Spoofing | Cuentas creadas con mails random o con el mail de otro | mitigate | **Ya mitigado y verificado:** la confirmación de mail es un gate real en prod (H-01, `mailer_autoconfirm: false`) — es la razón textual de D-11. Sin ella, el mail de un usuario de contraseña nunca queda verificado, que es el vector de account takeover que la Phase 5 hereda. Este plan no lo rompe: no toca la config de Auth. |
| T-04-19 | Repudiation | El usuario cree que su cuenta está lista cuando no lo está | mitigate | AUTH-06: el card dice "Confirmá tu cuenta para entrar" y **no navega**. La navegación a `/onboarding` sin sesión (el bug actual) hacía que el usuario leyera "Cuenta creada" y terminara en el login sin explicación. |
| T-04-01 | Tampering | Open redirect vía `emailRedirectTo` del cliente | mitigate | No se pasa: el href lo arma el template con `{{ .SiteURL }}`. Verificado por `grep -c "emailRedirectTo" == 0`. |
| T-04-13 | Information Disclosure | Rate limit como oráculo lateral en el reenvío | mitigate | El reenvío pasa por `CheckYourEmail` (plan `04-03`): cooldown de 60s que arranca bloqueado y se resetea en `finally` incluso ante error (D-05, Pitfall 4). |
</threat_model>

<verification>
- `npm run build` exits 0.
- `npx eslint "app/(auth)/register/page.tsx"` exits 0 sin warnings.
- `npx tsc --noEmit` y `npm test` — exits 0.
- Flujo real (alta → mail → link → `/onboarding` con sesión; alta con mail ya existente → misma pantalla;
  `?plan=` aplicado post-confirmación): **manual**, en el UAT del plan `04-06`.
</verification>

<success_criteria>
- AUTH-06: lo que el usuario lee al crear la cuenta y a dónde lo mandan coincide con lo que Auth hace de verdad.
  Cero navegaciones a pantallas que van a rebotar.
- D-14: mail nuevo y mail ya registrado producen la **misma** pantalla.
- D-10: `/register` conserva su card centrado, su lockup y su URL — sin rediseño.
- El `?plan=` de los CTAs del landing sigue capturándose.
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-05-SUMMARY.md` when done
</output>
