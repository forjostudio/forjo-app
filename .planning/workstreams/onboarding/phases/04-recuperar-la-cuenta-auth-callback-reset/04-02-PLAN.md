---
phase: 04-recuperar-la-cuenta-auth-callback-reset
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auth/route-lists.ts
  - proxy.ts
  - lib/supabase/middleware.ts
  - test/proxy-auth-routes.test.ts
autonomous: true
requirements: [AUTH-01, AUTH-02]

must_haves:
  truths:
    - "Un link de recuperación abierto con la app en mantenimiento llega al callback y canjea su token, en vez de quemarse contra un 503 (D-21)"
    - "Las 3 rutas nuevas pasan por updateSession, así que la cookie de sesión se refresca y no queda stale (D-22)"
    - "Un dueño CON sesión abierta que entra a /forgot-password ve la pantalla de recuperación — no lo rebotan al dashboard (D-06)"
    - "Alguien que cae en /reset-password sin sesión válida NO es rebotado al login por el proxy (Pitfall 5 / D-18)"
  artifacts:
    - path: "lib/auth/route-lists.ts"
      provides: "Las listas de ruteo del Edge como predicados puros e importables"
      exports: ["isMaintExempt", "isKnownRoute", "isAuthRoute", "isDashboardRoute"]
    - path: "test/proxy-auth-routes.test.ts"
      provides: "Regresión permanente de la trampa de las 3 listas (D-06/D-21/D-22)"
      contains: "isAuthRoute"
  key_links:
    - from: "proxy.ts"
      to: "lib/auth/route-lists.ts"
      via: "import de isMaintExempt / isKnownRoute"
      pattern: "from '@/lib/auth/route-lists'"
    - from: "lib/supabase/middleware.ts"
      to: "lib/auth/route-lists.ts"
      via: "import de isAuthRoute / isDashboardRoute"
      pattern: "from '@/lib/auth/route-lists'"
---

<objective>
Meter las 3 rutas nuevas en las listas del Edge **exactamente donde van y en ninguna otra** (D-21, D-22, D-06),
y dejar esa configuración cubierta por un test — porque hoy es inasertable y la trampa reaparece como bug
intermitente.

Purpose: sin `/auth` en `MAINT_EXEMPT` un link de recuperación se quema contra la pantalla de mantenimiento
(los links vencen en 1h, D-19). Sin las 3 rutas en `KNOWN_PREFIXES` la cookie de sesión no se refresca. Y si
alguien las suma a `isAuthRoute` por inercia, rompe D-06 sin que nada falle en rojo.
Output: `lib/auth/route-lists.ts` (puro), `proxy.ts` y `lib/supabase/middleware.ts` recableados,
`test/proxy-auth-routes.test.ts`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-CONTEXT.md
</context>

## Artifacts this phase produces

| Artifact | Tipo | Dónde |
|----------|------|-------|
| `lib/auth/route-lists.ts` | archivo nuevo | `lib/auth/` |
| `MAINT_EXEMPT` | constante exportada (movida desde `proxy.ts:21`) | `lib/auth/route-lists.ts` |
| `KNOWN_PREFIXES` | constante exportada (movida desde `proxy.ts:40-55`) | `lib/auth/route-lists.ts` |
| `AUTH_ROUTE_PREFIXES` | constante exportada (nueva, derivada de `middleware.ts:31`) | `lib/auth/route-lists.ts` |
| `DASHBOARD_ROUTE_PREFIXES` | constante exportada (nueva, derivada de `middleware.ts:32-42`) | `lib/auth/route-lists.ts` |
| `isMaintExempt` | predicado exportado | `lib/auth/route-lists.ts` |
| `isKnownRoute` | predicado exportado | `lib/auth/route-lists.ts` |
| `isAuthRoute` | predicado exportado (era una const local inline) | `lib/auth/route-lists.ts` |
| `isDashboardRoute` | predicado exportado (era una const local inline) | `lib/auth/route-lists.ts` |
| `test/proxy-auth-routes.test.ts` | archivo nuevo | `test/` |

> **Decisión de estructura (PATTERNS Hallazgo 1 — el planner tenía que elegir y justificar).** PATTERNS dejó 3
> opciones para volver asertables las listas: (a) exportarlas desde `proxy.ts`, (b) extraerlas a un módulo puro
> de `lib/`, (c) leer el archivo con `readFileSync`. **Se elige (b)**, la opción recomendada:
> (a) obliga al test a importar `proxy.ts`, que arrastra `@/lib/supabase/public` y con él las env vars de
> Supabase — justo lo que H-06 prohíbe; (c) no tiene precedente en el repo y es frágil. (b) es coherente con el
> patrón `lib/` puro ya establecido (`lib/crm-reports.ts`, `lib/booking-core.ts`) y con `lib/auth/callback.ts`
> del plan `04-01`.
> **Alcance de la extracción:** se extraen los **cuatro** predicados, no solo los dos de `proxy.ts`. Motivo: el
> valor del test no está en asertar qué rutas SE agregan, sino qué rutas **NO se agregan** — `isAuthRoute` (D-06)
> e `isDashboardRoute` (Pitfall 5) son precisamente las dos listas donde el error es invisible. La extracción es
> **behavior-preserving**: se mueve la definición, no se cambia ninguna ruta matcheada.

<tasks>

<task type="auto">
  <name>Task 1: lib/auth/route-lists.ts — extraer las 4 listas del Edge a un módulo puro</name>

  <files>lib/auth/route-lists.ts</files>

  <read_first>
    - `proxy.ts` — leelo entero. `MAINT_EXEMPT` (línea 21) con su comentario; `KNOWN_PREFIXES` (líneas 40-55) con el comentario de `/admin` (49-51); el matcher de prefijos (línea 64: `pathname === p || pathname.startsWith(p + '/')`) y el de la línea 74-76.
    - `lib/supabase/middleware.ts` — `isAuthRoute` (línea 31) e `isDashboardRoute` (líneas 32-42), incluido el comentario de `/admin` (38-41).
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Pitfall 6": la tabla que mapea **las 3 listas + 1 matcher**, con línea y efecto exacto de cada una. Confirmado que no hay una cuarta lista.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-PATTERNS.md` §"`proxy.ts`" y §"`lib/supabase/middleware.ts` — NO MODIFICAR".
    - `lib/crm-pipeline.ts` — analog de módulo `lib/` puro con constantes exportadas.
  </read_first>

  <action>
    Creá `lib/auth/route-lists.ts`: módulo **puro**, sin imports de Next ni de Supabase (lo importan tanto
    `proxy.ts` como `lib/supabase/middleware.ts`, ambos en **Edge Runtime**, y también el test — no puede
    arrastrar env vars).

    Exportá 4 constantes de módulo `UPPER_SNAKE_CASE` con `as const`, y 4 predicados:

    - `MAINT_EXEMPT` — mové el array de `proxy.ts:21` **con su comentario**, y **agregá `'/auth'`** (D-21).
      Extendé el comentario existente (no lo borres) explicando el porqué del ítem nuevo: con la app en
      mantenimiento el kill switch devuelve 503 a todo lo demás, y un link de recuperación o de confirmación
      **quemaría su token contra una pantalla de mantenimiento** — los links vencen en 1h (D-19) y son
      single-use. Anotá explícitamente que `/forgot-password` y `/reset-password` **NO** se eximen: si el sistema
      está caído no hay a dónde entrar igual, y `/reset-password` escribe en la base — el mantenimiento existe
      justo para que nadie escriba.
    - `KNOWN_PREFIXES` — mové el array de `proxy.ts:40-55` **con sus comentarios** (incluido el de `/admin`), y
      **agregá `'/auth'`, `'/forgot-password'` y `'/reset-password'`** (D-22). Escribí para el bloque nuevo un
      comentario del mismo estilo que el de `/admin` (líneas 49-51), que es el **precedente exacto del mismo modo
      de falla**: sin el prefijo, la ruta cae en el `NextResponse.next()` de `proxy.ts:77-79` y NO pasa por
      `updateSession` → la cookie de sesión queda stale. Citá D-22.
    - `AUTH_ROUTE_PREFIXES` — derivado de `lib/supabase/middleware.ts:31`: exactamente `['/login', '/register']`.
      **NO le agregues ninguna ruta de esta fase.** Poné un comentario en mayúsculas que lo advierta: sumar
      `/forgot-password` acá **rompe D-06** — es el único camino para cambiar la contraseña (no existe pantalla de
      "cambiar contraseña" en el panel), así que rebotar al que tiene sesión abierta y no la recuerda lo deja sin
      salida. El síntoma del bug es silencioso: "con sesión abierta, la pantalla de recuperación me tira al
      dashboard". `test/proxy-auth-routes.test.ts` (Task 3) es el guardia permanente de esta lista.
    - `DASHBOARD_ROUTE_PREFIXES` — derivado de `lib/supabase/middleware.ts:32-42`: exactamente los 7 prefijos que
      hay hoy (`/dashboard`, `/appointments`, `/clients`, `/finances`, `/settings`, `/onboarding`, `/admin`), con
      el comentario de `/admin` conservado. **NO le agregues ninguna ruta de esta fase.** Comentario: sumar
      `/reset-password` acá haría que el proxy redirija a `/login`, que es exactamente el **"error opaco" que D-18
      prohíbe** — el guard de esa página va en la página, no en el proxy (Pitfall 5).

    Predicados (todos con la **misma semántica de matcheo que hoy**, no la cambies):
    - `isMaintExempt(pathname: string): boolean` y `isKnownRoute(pathname: string): boolean` → replicán el
      matcher de prefijo de `proxy.ts:64` / `74-76`: `pathname === p || pathname.startsWith(p + '/')`.
      Es lo que hace que `'/auth'` cubra `/auth/callback` sin cubrir `/authentication`.
    - `isAuthRoute(pathname: string): boolean` e `isDashboardRoute(pathname: string): boolean` → replicán la
      semántica de `middleware.ts`, que hoy usa `startsWith(p)` **pelado** (sin el `+ '/'`). **Conservá esa
      diferencia tal cual**: esta extracción es behavior-preserving, no es el momento de unificar semánticas.
      Dejá un comentario señalando que las dos familias de predicados matchean distinto **a propósito**, porque
      así está hoy en producción.

    Cabecera del módulo en español: qué es cada lista, qué efecto tiene, y que **son listas distintas con
    efectos distintos y hay que tocarlas distinto** (D-22). Secciones con barras Unicode.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `lib/auth/route-lists.ts` existe y exporta `MAINT_EXEMPT`, `KNOWN_PREFIXES`, `AUTH_ROUTE_PREFIXES`, `DASHBOARD_ROUTE_PREFIXES`, `isMaintExempt`, `isKnownRoute`, `isAuthRoute`, `isDashboardRoute`.
    - `grep -v '^\s*//' lib/auth/route-lists.ts | grep -c "next/server\|@supabase\|@/lib/supabase"` == 0 (módulo puro: importable desde Edge y desde el test sin arrastrar env vars).
    - `node -e "const m=require('ts-node')" ` NO es necesario; la verificación funcional es el test de la Task 3.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>

  <done>Las 4 listas del Edge viven en un módulo puro importable, con `/auth` sumado a `MAINT_EXEMPT` y las 3 rutas nuevas sumadas a `KNOWN_PREFIXES` — y las otras dos listas explícitamente intactas.</done>
</task>

<task type="auto">
  <name>Task 2: recablear proxy.ts y lib/supabase/middleware.ts para consumir el módulo</name>

  <files>proxy.ts, lib/supabase/middleware.ts</files>

  <read_first>
    - `proxy.ts` (entero) y `lib/supabase/middleware.ts` (entero) — el estado actual.
    - `lib/auth/route-lists.ts` (Task 1).
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Pitfall 6" — la tabla de efectos por lista.
  </read_first>

  <action>
    **`proxy.ts`:** borrá las definiciones locales de `MAINT_EXEMPT` (línea 21) y `KNOWN_PREFIXES` (líneas 40-55)
    — ya viven en `lib/auth/route-lists.ts` — e importá `isMaintExempt` e `isKnownRoute` desde
    `@/lib/auth/route-lists`. Reemplazá el `MAINT_EXEMPT.some(...)` de la línea 64 por `isMaintExempt(pathname)` y
    el `KNOWN_PREFIXES.some(...)` de las líneas 74-76 por `isKnownRoute(pathname)`. **Conservá los comentarios
    explicativos que quedan en el cuerpo de `proxy()`** (el del kill switch, líneas 60-63, y el del booking
    público, 72-73): describen el flujo, no las listas. Actualizá la mención a `MAINT_EXEMPT` del comentario de la
    línea 63 para que apunte al módulo nuevo. **No toques** `SUSPENDED_HTML`, `isMaintenance()` ni `config.matcher`
    (el matcher no es una cuarta lista: `/auth/callback` ya matchea).

    **`lib/supabase/middleware.ts`:** reemplazá las dos `const` locales `isAuthRoute` (línea 31) e
    `isDashboardRoute` (líneas 32-42) por llamadas a los predicados importados de `@/lib/auth/route-lists`,
    pasándoles `pathname`. **Este cambio es behavior-preserving y nada más:** el set de rutas matcheadas por cada
    predicado debe quedar **idéntico** al de hoy. NO le sumes ninguna ruta de esta fase a ninguno de los dos —
    `/forgot-password` y `/reset-password` **no van** ni a uno ni a otro (D-06/D-22 y Pitfall 5 respectivamente).
    El resto del archivo (`createServerClient`, el bloque de cookies `getAll`/`setAll`, `getUser()`, y los dos
    `if` de redirect de las líneas 44-54) queda **sin cambios**. Mové el comentario de `/admin` (líneas 38-41)
    junto a `DASHBOARD_ROUTE_PREFIXES` en el módulo si ya lo hiciste en la Task 1; no lo pierdas.

    Motivo de tocar este archivo pese a que PATTERNS lo marca "NO MODIFICAR": esa marca es sobre **el set de rutas**
    (no sumar `/forgot-password` a `isAuthRoute`), y eso se respeta al pie de la letra. Extraer la definición es lo
    que permite que la Task 3 ponga un guardia automático sobre exactamente esa regla — que hoy no tiene ninguno.
  </action>

  <verify>
    <automated>npx tsc --noEmit && npx eslint proxy.ts lib/supabase/middleware.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "from '@/lib/auth/route-lists'" proxy.ts` == 1 y `grep -c "from '@/lib/auth/route-lists'" lib/supabase/middleware.ts` == 1.
    - `grep -c "isMaintExempt(pathname)" proxy.ts` == 1 y `grep -c "isKnownRoute(pathname)" proxy.ts` == 1.
    - `git diff lib/supabase/middleware.ts` muestra cambios **solo** en el bloque de imports y en las líneas 31-42 (las dos const → llamadas a predicados). Las líneas 44-54 (los dos redirects) quedan idénticas.
    - `npx tsc --noEmit` exits 0 y `npx eslint proxy.ts lib/supabase/middleware.ts` exits 0 (sin unused vars: las const viejas ya no están).
    - `npm test` exits 0 (ninguna suite existente rompe por el recableo).
  </acceptance_criteria>

  <done>`proxy.ts` y `lib/supabase/middleware.ts` consumen los predicados del módulo puro; el comportamiento de ruteo es idéntico al de hoy salvo por los ítems de D-21/D-22.</done>
</task>

<task type="auto">
  <name>Task 3: test/proxy-auth-routes.test.ts — el guardia de la trampa de las 3 listas</name>

  <files>test/proxy-auth-routes.test.ts</files>

  <read_first>
    - `lib/crm-reports.test.ts` (líneas 1-12) — el analog de test puro.
    - `test/booking-public-regression.test.ts` (línea 20) — el **contra-ejemplo**: abre con `describe.skipIf(!hasSupabaseCreds)`, la marca de un test que toca red. Este test NO debe parecerse a ese.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Pitfall 6" (la tabla de las 3 listas) y §"Validation Architecture" (H-06 + la fila "Regresión de proxy").
    - `lib/auth/route-lists.ts` (Task 1).
  </read_first>

  <action>
    Escribí `test/proxy-auth-routes.test.ts` importando **solo** de `@/lib/auth/route-lists`. Sin `skipIf`, sin
    `test/env.ts`, sin red, sin credenciales (H-06). Es el primer test del repo que asierta sobre configuración
    de ruteo: dejá un comentario de cabecera explicando por qué existe — las 3 listas viven en 2 archivos, tienen
    efectos distintos, y el error clásico (sumar las rutas de recuperación a las listas de auth) **no falla en
    rojo**: aparece como un bug intermitente en producción (D-22).

    Aserciones obligatorias, agrupadas en 4 `describe` (uno por lista):

    **`isMaintExempt` (D-21):** `true` para `/auth` y `/auth/callback` (el link del mail sobrevive al
    mantenimiento). `false` para `/forgot-password` y `/reset-password` (D-21 los deja caer a propósito).
    `true` para los 3 que ya estaban: `/api`, `/admin`, `/login`. `false` para `/dashboard`.

    **`isKnownRoute` (D-22):** `true` para `/auth/callback`, `/forgot-password` y `/reset-password` (si no, caen
    en el `NextResponse.next()` y la cookie no se refresca). `true` para `/login` y `/dashboard`. `false` para un
    slug de booking público como `/mi-negocio` — comentá que ese `false` es **deliberado y load-bearing**: es lo
    que impide que las credenciales del dueño se filtren al flujo anónimo de reservas.

    **`isAuthRoute` (D-06 — LA aserción que justifica este archivo):** `false` para `/forgot-password` y
    `false` para `/reset-password`. Comentá arriba de estas dos líneas que un `true` acá significa que un dueño
    con sesión abierta que no recuerda su contraseña queda **sin salida**, porque no existe pantalla de "cambiar
    contraseña" en el panel. `true` para `/login` y `/register` (lo que hay hoy, sin cambios).

    **`isDashboardRoute` (Pitfall 5 / D-18):** `false` para `/reset-password` y `/forgot-password` — un `true` acá
    haría que el proxy redirija a `/login`, el "error opaco" que D-18 prohíbe; el guard de `/reset-password` va en
    la página. `true` para `/dashboard`, `/onboarding` y `/admin` (sin cambios).

    Agregá también un caso de borde del matcher: `isMaintExempt('/authentication')` → `false` (el matcher usa
    `pathname === p || startsWith(p + '/')`, así que `'/auth'` no sobre-matchea).
  </action>

  <verify>
    <automated>npx vitest run test/proxy-auth-routes.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run test/proxy-auth-routes.test.ts` exits 0 con al menos 4 tests pasando.
    - `grep -c "skipIf\|test/env\|hasSupabaseCreds" test/proxy-auth-routes.test.ts` == 0 (H-06).
    - El test importa únicamente de `@/lib/auth/route-lists`: `grep -c "^import" test/proxy-auth-routes.test.ts` == 2 (vitest + route-lists).
    - **Prueba de que el guardia sirve:** agregá temporalmente `'/forgot-password'` a `AUTH_ROUTE_PREFIXES`, corré `npx vitest run test/proxy-auth-routes.test.ts` y confirmá que **falla**; revertí el cambio y confirmá que vuelve a pasar. Dejá constancia del resultado en el SUMMARY.
    - `npm test` exits 0 (suite completa).
  </acceptance_criteria>

  <done>Existe un test automático que falla si alguien suma las rutas de recuperación a `isAuthRoute` o a `isDashboardRoute`, y que verifica que `/auth` sobrevive al mantenimiento.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → `proxy.ts` (Edge) | Todo request entra por acá; decide si se corta por mantenimiento, si refresca sesión, y si redirige por guard. |
| `proxy.ts` → `updateSession` | Único lugar donde se lee la sesión antes de las páginas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Denial of Service | Link de recuperación vs. kill switch de mantenimiento | mitigate | `'/auth'` en `MAINT_EXEMPT` (D-21): el callback siempre canjea el token y deja la sesión. Sin esto, un link single-use que vence en 1h (D-19) se quema contra un 503 y el dueño queda afuera. Verificado por `isMaintExempt('/auth/callback') === true` en `test/proxy-auth-routes.test.ts`. |
| T-04-08 | Elevation of Privilege | `/forgot-password` sumado a `isAuthRoute` por inercia | mitigate | `AUTH_ROUTE_PREFIXES` queda en `['/login', '/register']` y el test asierta `isAuthRoute('/forgot-password') === false`. No es EoP clásico: es **pérdida de disponibilidad de la cuenta** (D-06 — el dueño con sesión en el celu que no recuerda su contraseña queda sin salida). El guardia es automático y permanente. |
| T-04-09 | Information Disclosure | Credenciales del dueño filtradas al booking público | mitigate | El recableo **no cambia** la semántica de `isKnownRoute`: un slug público sigue devolviendo `false` → `NextResponse.next()` sin `updateSession`. Verificado por el caso `/mi-negocio` → `false` en el test. |
| T-04-10 | Spoofing | Sesión stale por no pasar por `updateSession` | mitigate | Las 3 rutas nuevas en `KNOWN_PREFIXES` (D-22). Mismo modo de falla que ya documentó `/admin` en `proxy.ts:49-51`. |
| T-04-11 | Tampering | La extracción a `lib/auth/route-lists.ts` cambia comportamiento sin querer | mitigate | La extracción es behavior-preserving por contrato: se conserva la diferencia de matcheo entre las dos familias de predicados (`=== p || startsWith(p+'/')` vs `startsWith(p)`), `git diff` de `lib/supabase/middleware.ts` acotado a imports + líneas 31-42, y `npm test` completo verde. |
</threat_model>

<verification>
- `npx vitest run test/proxy-auth-routes.test.ts` — verde.
- `npm test` — suite completa verde (el recableo no rompe nada existente).
- `npx tsc --noEmit` y `npx eslint proxy.ts lib/supabase/middleware.ts lib/auth/route-lists.ts` — exits 0.
- Prueba de mutación manual del guardia (documentada en el SUMMARY): agregar `/forgot-password` a
  `AUTH_ROUTE_PREFIXES` hace fallar el test.
</verification>

<success_criteria>
- `/auth` está exento de mantenimiento; `/forgot-password` y `/reset-password` **no** lo están (D-21).
- Las 3 rutas nuevas refrescan sesión vía `updateSession` (D-22).
- `isAuthRoute` e `isDashboardRoute` matchean **exactamente** las mismas rutas que antes de esta fase, y hay un
  test que lo garantiza para siempre (D-06 / Pitfall 5).
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-02-SUMMARY.md` when done
</output>
