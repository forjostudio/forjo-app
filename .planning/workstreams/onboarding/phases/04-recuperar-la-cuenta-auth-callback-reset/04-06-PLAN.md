---
phase: 04-recuperar-la-cuenta-auth-callback-reset
plan: 06
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05"]
files_modified:
  - supabase/config.toml
  - supabase/templates/recovery.html
  - supabase/templates/confirmation.html
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-06]

user_setup:
  - service: supabase
    why: "El link del mail no vuelve a la app sin la config del Dashboard, y el template default nunca entrega token_hash (H-02). Nada de esto está en git."
    dashboard_config:
      - task: "Verificar Site URL = https://gestion.forjo.studio"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Site URL"
      - task: "Agregar las 2 Redirect URLs de la allowlist (prod + local). SIN wildcard de previews (D-20)"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs"
      - task: "Cambiar el href del template 'Reset Password' a token_hash (H-02). SOLO el href — el idioma y la marca son Phase 6"
        location: "Supabase Dashboard -> Authentication -> Emails -> Templates -> Reset Password"
      - task: "Cambiar el href del template 'Confirm signup' a token_hash (H-02). SOLO el href"
        location: "Supabase Dashboard -> Authentication -> Emails -> Templates -> Confirm signup"

must_haves:
  truths:
    - "El entorno local reproduce el gate de confirmación de prod, así que el bug de AUTH-06 se puede probar sin tocar producción (D-23)"
    - "El link del mail de recuperación vuelve a /auth/callback con token_hash y deja al dueño seteando su contraseña (AUTH-01 + AUTH-02, H-02)"
    - "El link del mail de confirmación deja al usuario nuevo en /onboarding con sesión (AUTH-06, D-13)"
    - "La allowlist de Redirect URLs tiene solo prod + local, sin wildcard de previews (D-20)"
    - "La contraseña vieja ya no sirve y la nueva sí; las otras sesiones quedaron cerradas y la actual sobrevivió (D-17)"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Paridad local con prod: gate de confirmación + allowlist local + templates versionados"
      contains: "enable_confirmations = true"
    - path: "supabase/templates/recovery.html"
      provides: "Template local del mail de recuperación con el href de token_hash"
      contains: "TokenHash"
    - path: "supabase/templates/confirmation.html"
      provides: "Template local del mail de confirmación con el href de token_hash"
      contains: "TokenHash"
  key_links:
    - from: "supabase/templates/recovery.html"
      to: "app/auth/callback/route.ts"
      via: "href del mail -> /auth/callback?token_hash=...&type=recovery"
      pattern: "/auth/callback\\?token_hash="
    - from: "supabase/config.toml"
      to: "supabase/templates/recovery.html"
      via: "content_path del template versionado"
      pattern: "content_path"
---

<objective>
Cerrar la fase por donde no llega el código: la **mecánica del link del mail** y la **config de los servicios
vivos**. En local queda versionada en el repo; en prod es un **checkpoint humano** en el Dashboard de Supabase.
Después, el UAT completo — que por D-20 se hace **en local y se re-verifica en prod**, nunca en una preview.

Purpose: el template default de Supabase **nunca entrega `token_hash`** (H-02). Sin este plan, el código de los
planes 01-05 está bien y el flujo igual falla siempre — el link del mail cae en
`/forgot-password?error=invalid_link` y nadie entiende por qué (Pitfall 1).
Output: `supabase/config.toml`, `supabase/templates/{recovery,confirmation}.html`, y la config de prod aplicada
y verificada a mano.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md
@.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-CONTEXT.md
@.planning/workstreams/onboarding/ROADMAP.md
</context>

## Artifacts this phase produces

| Artifact | Tipo | Dónde |
|----------|------|-------|
| `supabase/templates/` | carpeta nueva | `supabase/` |
| `supabase/templates/recovery.html` | archivo nuevo | — |
| `supabase/templates/confirmation.html` | archivo nuevo | — |
| `[auth.email.template.recovery]` | sección nueva de config | `supabase/config.toml` |
| `[auth.email.template.confirmation]` | sección nueva de config | `supabase/config.toml` |
| Redirect URLs allowlist (prod) | config externa, no versionada | Supabase Dashboard |
| href de los templates `Reset Password` / `Confirm signup` (prod) | config externa, no versionada | Supabase Dashboard |

> **Dos correcciones al alcance que el research trajo — leelas antes de empezar:**
>
> **H-01 le SACA trabajo al checkpoint.** La confirmación de mail **ya está ON en prod** (probe read-only:
> `mailer_autoconfirm: false`; 3 usuarios, **0 sin confirmar**). **D-11 ya está cumplido y D-15 NO dispara:** no
> hay flag que prender ni usuarios colgados que arreglar a mano. **No planifiques ese trabajo** — solo verificá
> que siga ON y no lo toques.
>
> **H-02 le SUMA trabajo al checkpoint.** `token_hash` obliga a editar el `href` de los 2 templates **en esta
> fase**: `{{ .ConfirmationURL }}` pasa por `/auth/v1/verify` y vuelve con `?code=`, nunca con `token_hash`.
> CONTEXT los daba por intactos hasta Phase 6. **El corte acordado: Phase 4 cambia SOLO el `href`. Phase 6
> (MAIL-01) conserva idioma, marca y remitente.** El mail sigue siendo el feo de Supabase en inglés — eso está
> aceptado a propósito (CONTEXT §specifics, en palabras del usuario: *"no importa que sea el feo de supabase, lo
> corregimos en la siguiente fase"*). **Esto no es brandeo: es la mecánica sin la cual la fase no cierra.**
>
> Neteando, el checkpoint queda del mismo tamaño que decía el ROADMAP, pero con **otro contenido**.

<tasks>

<task type="auto">
  <name>Task 1: paridad del entorno local — config.toml + templates versionados (D-23, Pitfall 7, H-02)</name>

  <files>supabase/config.toml, supabase/templates/recovery.html, supabase/templates/confirmation.html</files>

  <read_first>
    - `supabase/config.toml` — §`[auth]` (`site_url` línea 159, `additional_redirect_urls` línea 163), §`[auth.email]` (`enable_confirmations` línea 226, `secure_password_change` 228, `otp_expiry` 234), §`[auth.rate_limit]` (`email_sent` 199), §`[inbucket]` (líneas 105-108) y el patrón **comentado** de `[auth.email.template.invite]` con `content_path` (líneas 247-249).
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §H-02 (el href correcto), §"Pitfall 7" (el typo de `additional_redirect_urls`), §H-05 (el capturador de mails) y §"Runtime State Inventory".
    - `lib/auth/callback.ts` (plan `04-01`) — los `type` que la allowlist del handler acepta: `recovery` y `signup`.
  </read_first>

  <action>
    <!-- planner-discipline-allow: https://127.0.0.1:3000 -->
    <!-- planner-discipline-allow: ConfirmationURL -->
    **1) `supabase/config.toml` — 3 ediciones acotadas. No toques nada más del archivo.**

    - **`enable_confirmations`** (línea 226): `false` → **`true`** (D-23). Comentario en español al lado:
      que es **paridad con producción**, donde ya está ON (verificado); que así el flag queda **versionado y
      documentado en el repo**, no solo en un Dashboard que nadie mira; y que es la única forma de probar el gate
      de confirmación y el link sin tocar prod.
    - **`additional_redirect_urls`** (línea 163): hoy dice `["https://127.0.0.1:3000"]` — **`https`**, con
      `site_url` (línea 159) en `http` y sin TLS local (`[api.tls] enabled = false`, línea 28). Es un **typo
      preexistente** y esa entrada no matchea nada útil. Cambiala a
      `["http://127.0.0.1:3000", "http://127.0.0.1:3000/auth/callback"]`. Comentario: el matcher de Supabase es
      **literal por host** → `localhost` **≠** `127.0.0.1`, y el UAT local se abre en `127.0.0.1:3000` (que es lo
      que ya dice `site_url`), **no** en `localhost:3000`, o el link vuelve rebotado.
    - **Templates versionados** (H-02): agregá dos secciones nuevas, siguiendo el patrón comentado de
      `[auth.email.template.invite]` (líneas 247-249). Los nombres de sección y las claves están **verificados
      contra el config reference de Supabase** (cerraba la asunción A2 del RESEARCH):
      `[auth.email.template.recovery]` con `subject` y `content_path = "./supabase/templates/recovery.html"`, y
      `[auth.email.template.confirmation]` con `subject` y `content_path = "./supabase/templates/confirmation.html"`.
      **Los `subject` quedan en inglés** y parecidos a los defaults (`"Reset Your Password"` /
      `"Confirm Your Signup"`): el idioma y la marca son Phase 6 (MAIL-01). Comentario citando H-02: **acá se
      cambia solo la mecánica del link**; el `content_path` deja el href en el repo, así que el UAT local no
      depende del Dashboard.

    **NO toques:** `otp_expiry` (queda en 3600 = 1 hora, D-19 — una perilla menos que se puede desincronizar
    entre local y prod); `minimum_password_length` (6); `email_sent` (2/h, es lo que justifica el cooldown de D-05);
    `secure_password_change`; `site_url`.

    **NO renombres `[inbucket]` a `[local_smtp]`** pese al warning de deprecación del CLI. Motivos: el shape exacto
    de la sección nueva **no está verificado** (A3 del RESEARCH), `[inbucket]` funciona hoy (solo warnea), y el
    beneficio es silenciar un warning. Esta es la superficie de auth: no es el lugar para un rename de config sin
    verificar. Si molesta, es un `/gsd:quick` aparte.

    **2) `supabase/templates/recovery.html` y `confirmation.html` (carpeta nueva).**
    HTML mínimo, **en inglés**, espejando el default feo de Supabase (una línea de texto + el link). **No los
    brandees, no los traduzcas, no les metas CSS**: eso es MAIL-01 en Phase 6 y adelantarlo le saca peso a esa
    fase, que es justo lo que el ROADMAP quiso preservar.

    Lo único que importa es el `href` (H-02, [CITED: supabase.com/docs/guides/auth/passwords]):
    - `recovery.html`: el `<a>` apunta a `{{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=recovery`
    - `confirmation.html`: el `<a>` apunta a `{{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=signup`

    **Usá `{{ .SiteURL }}`, NO `{{ .ConfirmationURL }}` ni `{{ .RedirectTo }}`.** `{{ .ConfirmationURL }}` es
    justamente el que rompe (pasa por `/auth/v1/verify` y vuelve con `?code=`, que nuestro handler no acepta).
    `{{ .RedirectTo }}` reflejaría un valor que viene del navegador — y los planes `04-04`/`04-05` decidieron **no
    mandar** ningún redirect precisamente para no tener esa superficie (T-04-01). Con `.SiteURL`, el destino lo fija
    la config del proyecto: local apunta a local, prod apunta a prod, y no hay nada que un atacante pueda inyectar.

    Los `type` (`recovery` / `signup`) tienen que coincidir **exactamente** con la allowlist `ALLOWED_TYPES` de
    `lib/auth/callback.ts`, o el parse falla y el link muere en `invalid_link`.

    **3) Levantá el entorno y comprobá que el CLI parsea la config**: `npx supabase stop` y `npx supabase start`
    (requiere Docker Desktop prendido — ver Task 3, paso 0). Si el CLI rechaza alguna clave o no encuentra un
    `content_path`, corregí el path (el patrón comentado del repo usa rutas relativas a la raíz del proyecto) y
    dejá constancia en el SUMMARY.
  </action>

  <verify>
    <automated>npx supabase start && npx supabase status</automated>
  </verify>

  <acceptance_criteria>
    - `grep -v '^#' supabase/config.toml | grep -c "enable_confirmations = true"` == 1 (D-23).
    - `grep -v '^#' supabase/config.toml | grep -c "https://127.0.0.1:3000"` == 0 (el typo de Pitfall 7 murió).
    - `grep -v '^#' supabase/config.toml | grep -c "auth.email.template.recovery"` == 1 y `grep -v '^#' supabase/config.toml | grep -c "auth.email.template.confirmation"` == 1.
    - `grep -v '^#' supabase/config.toml | grep -c "otp_expiry = 3600"` == 1 (D-19: sin tocar).
    - `supabase/templates/recovery.html` contiene `token_hash={{ .TokenHash }}&type=recovery` y `supabase/templates/confirmation.html` contiene `token_hash={{ .TokenHash }}&type=signup`.
    - `grep -c "ConfirmationURL" supabase/templates/*.html` == 0 (es el template que nunca entrega `token_hash`).
    - `npx supabase start` termina OK y `npx supabase status` lista el servicio de mails en `http://127.0.0.1:54324`.
    - `git diff supabase/config.toml` muestra **solo** las 3 ediciones descritas (nada de `[inbucket]`, `otp_expiry`, `site_url` ni `email_sent`).
  </acceptance_criteria>

  <done>El entorno local reproduce el gate de confirmación de prod y entrega links con `token_hash`, con el href versionado en el repo — el UAT local no depende de ningún Dashboard.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: CHECKPOINT HUMANO — Dashboard de Supabase (prod)</name>

  <files>— (config externa: Supabase Dashboard, proyecto tpvbjwqzskzkevepcwyb. Nada de esto vive en git)</files>

  <action>
    **PARÁ. Esta task NO la ejecuta el agente.** Es config del Dashboard de Supabase de producción: no hay CLI ni
    API en juego acá, y tocar prod a ciegas en la superficie de auth no es una opción. Presentá al humano los 5
    pasos de `<how-to-verify>` **textualmente** (los valores están para copiar y pegar), esperá el
    `<resume-signal>`, y registrá en el SUMMARY las 3 respuestas que el checkpoint pide: (a) si el Site URL ya
    estaba bien o se corrigió, (b) la lista de entradas preexistentes en Redirect URLs, (c) que "Confirm email"
    sigue ON.

    Si el humano reporta algo que no coincide con lo descrito — sobre todo una entrada con wildcard en Redirect
    URLs, o "Confirm email" apagado — **no improvises**: es una divergencia contra el research (H-01) y hay que
    revalidar antes de seguir.
  </action>

  <what-built>
    Los planes `04-01`..`04-05` dejaron el código completo: `/auth/callback`, `/forgot-password`,
    `/reset-password`, el link en el login y el alta honesta. La Task 1 dejó el entorno local a la par.
    **Falta lo único que un agente no puede hacer:** la config de producción en el Dashboard de Supabase. Sin
    esto, el link del mail no vuelve a la app y el flujo falla **siempre**, aunque el código esté perfecto
    (Pitfall 1).
  </what-built>

  <read_first>
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §H-01 y §H-02.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-CONTEXT.md` — D-20 y D-11.
    - `supabase/templates/recovery.html` y `confirmation.html` (Task 1) — el href de prod es **el mismo**.
  </read_first>

  <how-to-verify>
    Proyecto de Supabase: **`tpvbjwqzskzkevepcwyb`** (el de prod). Todo esto es en el Dashboard, a mano.

    **1) Authentication → URL Configuration → Site URL**
    Verificá que diga exactamente `https://gestion.forjo.studio`.
    **Es load-bearing:** los templates arman el link con `{{ .SiteURL }}`, así que si esto está mal, **todos** los
    links del mail apuntan al lugar equivocado. Si ya está bien, no lo toques.

    **2) Authentication → URL Configuration → Redirect URLs**
    Agregá estas dos entradas, **exactamente** (copiá y pegá):

        https://gestion.forjo.studio/auth/callback
        http://127.0.0.1:3000/auth/callback

    **NO agregues ningún wildcard de previews de Vercel** (nada de `https://*.vercel.app/**`). D-20 es explícito:
    la allowlist **es** la defensa contra el open redirect en la superficie donde un agujero entrega cuentas, y el
    matcher de Supabase es **glob** — los globs sobre-matchean. **Consecuencia asumida: auth no anda en preview.**
    Está aceptado a cambio de una allowlist chica.

    **3) Authentication → Emails → Templates → "Reset Password"**
    Buscá el `<a href="...">` del cuerpo. Hoy apunta a `{{ .ConfirmationURL }}`. Cambiá **solo el valor del href**
    por:

        {{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=recovery

    **No toques nada más:** ni el idioma (sigue en inglés), ni el texto, ni el subject, ni el remitente. Eso es
    Phase 6 (MAIL-01) y adelantarlo le saca peso a esa fase. Guardá.

    **4) Authentication → Emails → Templates → "Confirm signup"**
    Mismo procedimiento. El href queda:

        {{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=signup

    Guardá. Igual que antes: **solo el href**.

    **5) Authentication → Providers → Email → "Confirm email"**
    **Verificá que esté ON y NO lo toques.** El research ya lo comprobó read-only (`mailer_autoconfirm: false`, 3
    usuarios, 0 sin confirmar): **D-11 ya está cumplido y D-15 no dispara** — no hay nada que prender ni usuarios
    colgados que arreglar a mano. Este paso es solo confirmar que sigue así, porque el flujo de AUTH-06 depende de
    eso. Si lo encontrás apagado, **pará y avisá**: cambió algo desde el research y hay que revalidar.

    **Reportá al volver:** (a) si el Site URL ya estaba correcto o lo corregiste; (b) si había otras entradas en
    Redirect URLs además de las dos nuevas — **listalas**, porque una entrada vieja con wildcard sería un agujero
    que D-20 quiere cerrado; (c) confirmación de que "Confirm email" sigue ON.
  </how-to-verify>

  <acceptance_criteria>
    - Site URL de prod = `https://gestion.forjo.studio` (verificado, no asumido).
    - Redirect URLs contiene `https://gestion.forjo.studio/auth/callback` y `http://127.0.0.1:3000/auth/callback`, y **ninguna entrada con `*`**. Las entradas preexistentes quedaron listadas en el SUMMARY.
    - El href del template "Reset Password" contiene `token_hash={{ .TokenHash }}&type=recovery` y **no** contiene `{{ .ConfirmationURL }}`.
    - El href del template "Confirm signup" contiene `token_hash={{ .TokenHash }}&type=signup` y **no** contiene `{{ .ConfirmationURL }}`.
    - El idioma, el subject y el remitente de los 2 templates quedaron **sin cambios** (siguen siendo el default en inglés — MAIL-01 conserva su peso).
    - "Confirm email" sigue ON en prod.
  </acceptance_criteria>

  <resume-signal>Escribí "listo" cuando los 5 pasos estén hechos, incluyendo el reporte de (a), (b) y (c). Si algo no coincide con lo descrito, describí qué encontraste en vez de improvisar.</resume-signal>

  <done>Prod tiene el Site URL correcto, la allowlist mínima de D-20 (sin wildcards) y los 2 templates entregando `token_hash` — con idioma, marca y remitente intactos para Phase 6.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — local primero, prod después (D-20)</name>

  <files>— (verificación manual del flujo; no modifica archivos)</files>

  <action>
    **Esta task es verificación humana, no implementación.** El agente hace tres cosas y nada más:

    1. **Deja el entorno listo:** confirma que `npx supabase start` levantó (Task 1) y que la app corre en
       `http://127.0.0.1:3000` — **no** en `localhost:3000` (Pitfall 7: el matcher de Supabase es literal por host
       y `site_url` es `127.0.0.1`, así que con `localhost` el link vuelve rebotado). Si Docker Desktop está
       abajo, **pará y pedí que lo prendan**: sin él no hay capturador de mails y no hay UAT (H-05).
    2. **Presenta el guion de `<how-to-verify>` textualmente**, en orden (A → B → C → D → E). Los pasos marcados
       **BLOCKER** son de seguridad: si alguno falla, la fase no cierra.
    3. **Espera el `<resume-signal>`** y registra en el SUMMARY: el resultado de cada bloque, y en particular el
       hallazgo del paso C13 sobre la asunción **A4** (si GoTrue ya revocaba las otras sesiones por sí solo, el
       `signOut({ scope: 'others' })` es redundante pero inofensivo — es información para Phase 5, no un bug).

    El agente **no marca ningún criterio como cumplido por su cuenta**: los 4 criterios del ROADMAP viven en
    GoTrue, en el mail y en el Dashboard, que ningún test de esta fase puede tocar (H-06).
  </action>

  <what-built>
    El flujo completo de recuperación de contraseña y el alta honesta. Lo que se automatizó ya está verde
    (`lib/auth/callback.test.ts`, `test/proxy-auth-routes.test.ts`, `tsc`, `eslint`, `npm run build`), pero el
    valor de esta fase vive en **GoTrue + la config del Dashboard + el mail**, que no son testeables sin red ni
    secretos (H-06: `vitest.setup.ts` carga `.env.local` = PROD, así que **ningún test puede tocar Supabase**).
    Esto se prueba a mano o no se prueba.
  </what-built>

  <read_first>
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §"Validation Architecture" → "UAT: cómo se corre" y la tabla Req→Test (las filas marcadas **manual**).
    - `.planning/workstreams/onboarding/ROADMAP.md` §"Phase 4" → los 4 criterios de éxito. Son lo que este UAT verifica.
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-RESEARCH.md` §H-05 (Docker abajo), §H-07 (`/suspendido`), §"Pitfall 7" (`127.0.0.1` vs `localhost`).
  </read_first>

  <how-to-verify>
    **PASO 0 — pre-requisito, no es opcional (H-05).** Docker Desktop está **abajo** hoy (verificado). Sin él no
    hay Supabase local ni capturador de mails, y **no hay UAT**. Prendé Docker Desktop, esperá a que arranque, y
    corré `npx supabase start`. El capturador **no está apagado a propósito** (`enabled = true`, a diferencia de
    `studio`/`storage`/`analytics`): levanta solo.

    **Reglas del UAT local:**
    - Abrí la app en **`http://127.0.0.1:3000`**, **NUNCA** en `localhost:3000`. El matcher de Supabase es literal
      por host y `site_url` es `127.0.0.1` → con `localhost` el link vuelve rebotado (Pitfall 7).
    - Los mails se leen en **`http://127.0.0.1:54324`** (no salen a internet).
    - Si `supabase start` levantó una base limpia, creá primero una cuenta de prueba para tener con qué probar el reset.

    ### A) Alta honesta — AUTH-06 / criterio 4 (D-12, D-13, D-14)
    1. Entrá a `/register?plan=studio` y creá una cuenta con un mail nuevo.
       - **Esperado:** el card se reemplaza por "Revisá tu mail" con la dirección que pusiste. **NO** te navega a
         ningún lado, **NO** aparece un toast de "Cuenta creada". *(Antes de esta fase te mandaba a `/onboarding` y
         el proxy te rebotaba al login — ese es el bug que se arregló.)*
       - **Esperado:** el botón de reenvío arranca **deshabilitado**, contando "Reenviar en 59s…" (D-05).
    2. Abrí `http://127.0.0.1:54324`, buscá el mail de confirmación y clickeá el link.
       - **Esperado:** caés en **`/onboarding`**, ya autenticado (D-13). No te pide la contraseña que acabás de
         inventar. **El mail está en inglés y es feo: está bien, es a propósito** (Phase 6 lo brandea).
    3. Terminá el onboarding y confirmá que **el plan `studio` se sigue aplicando** post-confirmación (el `?plan=`
       viaja por `localStorage`, y el usuario vuelve al mismo navegador desde el mail).
    4. **Anti-enumeration (D-14):** volvé a `/register` y registrate con **el mail que acabás de usar**.
       - **Esperado:** **exactamente la misma pantalla** "Revisá tu mail". Ningún mensaje que revele que la cuenta
         ya existe. Si ves algo distinto, es un **BLOCKER de seguridad**.

    ### B) Recuperar la contraseña — AUTH-01 / criterio 1 (D-01, D-02, D-09)
    5. Deslogueate. En `/login`, verificá que el link **"¿Olvidaste tu contraseña?"** esté **debajo del botón
       Entrar y arriba de "¿No tenés cuenta?"** (D-04). Clickealo.
    6. **Esperado:** `/forgot-password` con el panel naranja **igual al del login** y el **mismo headline** (D-08),
       h1 "Recuperá tu cuenta" + "Te mandamos un link para crear una contraseña nueva" (D-09).
    7. Poné tu email y enviá. **Esperado:** el form se reemplaza por "Revisá tu mail" **en el mismo card**, con la
       dirección y el link "Volver al login". Cooldown de 60s activo.
    8. **Anti-enumeration (D-02) — BLOCKER si falla:** repetí con un mail **que no existe**
       (`nadie-1234@ejemplo.com`). **Esperado: exactamente la misma pantalla, el mismo texto y el mismo tempo.**
       Cualquier diferencia (mensaje, error, demora perceptible) convierte el flujo en un oráculo de quién tiene
       cuenta en Forjo.

    ### C) Contraseña nueva y adentro — AUTH-02 / criterios 2 y 3 (D-03, D-16, D-17)
    9. Abrí el mail de recuperación en `:54324` y clickeá el link.
       - **Esperado:** caés en **`/reset-password`** con los dos campos (contraseña + confirmar).
       - **Mirá la barra de direcciones: NO tiene que haber ningún `token_hash`.** El callback lo canjeó y redirigió
         a una URL limpia (T-04-02). Si el token está en la barra, es un **BLOCKER de seguridad**.
       - **Esperado:** debajo del botón, el aviso de que se van a cerrar las sesiones de otros dispositivos (D-17).
    10. Probá el `.refine()`: poné dos contraseñas distintas → **"Las contraseñas no coinciden"** inline. Poné una
        de 5 caracteres → **"Mínimo 6 caracteres"**.
    11. Poné una contraseña nueva válida y guardá.
        - **Esperado:** quedás **adentro del panel**, sin volver a pasar por el login (criterio 2, D-16).
        - **Ojo (H-07):** si la cuenta no tiene negocio, el guard te manda a `/onboarding`; si el plan está
          suspendido, a `/suspendido`. **Las dos cosas son correctas** — el criterio no es "siempre termina en el
          panel", es "no lo rebotan al login".
    12. **Criterio 3:** deslogueate. Entrá con la contraseña **vieja** → tiene que **fallar**. Entrá con la
        **nueva** → tiene que **funcionar**.
    13. **D-17 (2 navegadores) — esto también cierra la asunción A4 del research:** antes de resetear, dejá una
        sesión abierta en un **segundo navegador** (o ventana de incógnito). Repetí el reset desde el primero.
        - **Esperado:** la sesión del **segundo** navegador queda **muerta** (recargá: te manda al login). La del
          **primero** (donde reseteaste) **sobrevive**.
        - **Anotá el resultado:** si las otras sesiones ya morían sin el `signOut({ scope: 'others' })`, entonces
          GoTrue las revoca solo y nuestro `signOut` es **redundante pero inofensivo** (A4). Es información, no un
          bug — dejala en el SUMMARY.

    ### D) Links muertos — criterio 2 (D-18, D-19)
    14. Volvé a clickear el **mismo link de recuperación** que ya usaste.
        - **Esperado:** `/forgot-password` con el aviso **"Ese link ya venció o ya se usó. Pedí uno nuevo."**
          arriba y el campo Email listo. **NUNCA** un rebote al login con un toast (eso es el "error opaco" que el
          criterio 2 prohíbe).
    15. Entrá **a mano** a `/reset-password` sin link, sin sesión de recovery. **Esperado:** mismo destino que el
        14. Sin pantalla muerta, sin form que no lleva a nada.
    16. **D-06 (la trampa del proxy):** con **sesión activa** en el panel, entrá a `/forgot-password`.
        - **Esperado:** ves la pantalla de recuperación normalmente. **Si te rebota a `/dashboard`, es un bug:**
          alguien sumó la ruta a `isAuthRoute` y dejó sin salida al dueño que tiene la sesión abierta en el celu y
          no se acuerda la contraseña (es el único camino para cambiarla). `test/proxy-auth-routes.test.ts` debería
          haberlo agarrado antes.

    ### E) Re-verificación en PROD (D-20 — auth no anda en previews, así que no hay atajo)
    17. Después del deploy, repetí en `https://gestion.forjo.studio` los pasos **A1-A2** (alta + confirmación) y
        **B5-B7 + C9-C12** (reset completo) **con una cuenta de prueba nueva**. **No uses las 3 cuentas reales que
        hay en prod** (H-01) — creá una cuenta descartable.
    18. Si en prod tira **rate limit** de mails, no adivines: es A1 del research (los límites de prod no son
        consultables sin Management API). Se mira en el **Dashboard → Authentication → Rate Limits**.
  </how-to-verify>

  <acceptance_criteria>
    - **Criterio 1 (ROADMAP):** el mail de recuperación llega y el link funciona — verificado en local (`:54324`) y en prod.
    - **Criterio 2:** con el link se setea la contraseña nueva y se queda adentro del panel; el link reusado y la entrada directa a `/reset-password` caen en `/forgot-password` con el aviso de D-18. **Cero rebotes al login. Cero pantallas muertas.**
    - **Criterio 3:** la contraseña vieja falla y la nueva funciona.
    - **Criterio 4:** el alta muestra "revisá tu mail" y no navega a ninguna pantalla que rebote; el link de confirmación deja al usuario en `/onboarding` con sesión.
    - **Seguridad (BLOCKERS):** (a) mail existente y mail inexistente producen **pantalla idéntica** en `/forgot-password` (D-02) y en `/register` (D-14); (b) el `token_hash` **no** queda en la barra de direcciones (T-04-02); (c) `/forgot-password` es accesible con sesión activa (D-06).
    - **D-17:** la sesión del segundo navegador queda muerta y la actual sobrevive. El resultado de A4 (si GoTrue ya revocaba solo) quedó documentado en el SUMMARY.
    - **D-05:** el botón de reenvío arranca bloqueado y cuenta hasta 0 en las dos pantallas.
    - Re-verificación en prod hecha con **cuenta de prueba nueva**, no con las 3 reales.
  </acceptance_criteria>

  <resume-signal>Escribí "aprobado" si los 4 criterios del ROADMAP y los 3 blockers de seguridad pasan. Si algo falla, describí qué pantalla viste y en qué paso — con eso se arma el gap.</resume-signal>

  <done>Los 4 criterios de éxito del ROADMAP y los 3 blockers de seguridad (enumeration, token en la barra, D-06) verificados a mano en local y re-verificados en prod con una cuenta de prueba nueva. El hallazgo de A4 quedó documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GoTrue → cliente de mail | El `token_hash` viaja dentro del mail; el link es lo único que autoriza el reset. |
| Dashboard de Supabase → runtime | Site URL, allowlist y templates gobiernan a dónde vuelve el link. No están en git. |
| Cualquier host → allowlist de Redirect URLs | Es la defensa de fondo contra el open redirect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Open redirect vía la allowlist del Dashboard | mitigate | D-20: allowlist = **solo** `https://gestion.forjo.studio/auth/callback` + `http://127.0.0.1:3000/auth/callback`. **Sin wildcard de previews** — el matcher de Supabase es glob y los globs sobre-matchean [CITED: supabase.com/docs/guides/auth/redirect-urls]. El checkpoint exige **listar las entradas preexistentes**: una vieja con `*` sería el agujero que D-20 cierra. Refuerzo: los templates usan `{{ .SiteURL }}` (valor de config), no `{{ .RedirectTo }}` (valor del navegador) → no hay nada que reflejar. |
| T-04-20 | Tampering | Template con `redirect_to` arbitrario | mitigate | El href se escribe a mano con `{{ .SiteURL }}` y `type` fijo por template. Verificado por `grep -c "ConfirmationURL" == 0` en los templates locales y por inspección visual en el checkpoint de prod. |
| T-04-21 | Information Disclosure | Divergencia local↔prod que oculta bugs de auth | mitigate | D-23: `enable_confirmations = true` **versionado** en `config.toml`. Sin esto, el local no reproduce el gate de prod y el bug de AUTH-06 (que está **vivo en prod**) no se puede probar antes de deployar. |
| T-04-22 | Spoofing | UAT en prod contra cuentas reales | mitigate | La re-verificación en prod se hace con **cuenta de prueba nueva** (A5 del research). Las 3 cuentas reales no se tocan. |
| T-04-02 | Information Disclosure | `token_hash` visible en la barra / screenshot | mitigate | Verificado **a mano** en el paso C9 del UAT: tras el canje, la URL de `/reset-password` no lleva el token. Es la comprobación de que las 4 capas del plan `04-01` funcionan de punta a punta. |
| T-04-06 | Information Disclosure | Logs de acceso de Vercel con el `token_hash` en el path | **accept** | Residual asumido y documentado. El token es single-use (lo quema `verifyOtp`) y vence en 1h (D-19) → el que aparece en un log ya está consumido. Se declara en `/gsd:secure-phase` como riesgo aceptado, no ignorado. |
</threat_model>

<verification>
- `npx supabase start` levanta con la config nueva y `npx supabase status` expone el capturador en `:54324`.
- `git diff supabase/config.toml` acotado a las 3 ediciones (D-23, Pitfall 7, templates).
- Checkpoint humano completado con el reporte de (a) Site URL, (b) entradas preexistentes de Redirect URLs, (c) "Confirm email" ON.
- UAT: los 4 criterios del ROADMAP + los 3 blockers de seguridad, en local y re-verificados en prod.
- **Gate de fase:** `npm test` + `npx tsc --noEmit` verdes antes de `/gsd:verify-work`, y **`/gsd:secure-phase` es
  obligatorio** — es la superficie de autenticación (ROADMAP §Threat note).
</verification>

<success_criteria>
- El entorno local reproduce el gate de confirmación de prod y entrega links con `token_hash` (D-23, H-02).
- Prod tiene la allowlist mínima de D-20 y los 2 templates con el href correcto — y **nada más cambiado**: idioma,
  marca y remitente quedan intactos para que MAIL-01 conserve su peso en Phase 6.
- Los 4 criterios de éxito del ROADMAP verificados a mano, en local y en prod.
- Los 3 blockers de seguridad (enumeration, token en la barra, D-06) verificados a mano.
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-06-SUMMARY.md` when done
</output>
