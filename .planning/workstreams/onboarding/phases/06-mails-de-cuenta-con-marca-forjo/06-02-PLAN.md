---
phase: 06-mails-de-cuenta-con-marca-forjo
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: false
requirements: [MAIL-01, MAIL-02]

user_setup:
  - service: resend
    why: "El pass del SMTP de Supabase es un Resend API key. Sin él, GoTrue no puede mandar desde el remitente de Forjo (D-01). El dominio forjo.studio ya está verificado (lo usa lib/email.ts) — se confirma, no se crea de cero (D-02, A1)."
    dashboard_config:
      - task: "Confirmar que el dominio forjo.studio figura Verified (SPF/DKIM en verde)"
        location: "Resend Dashboard -> Domains"
      - task: "Crear una API key DEDICADA 'supabase-auth-smtp' (blast radius chico: rotarla no tumba los mails de turnos que usan RESEND_API_KEY)"
        location: "Resend Dashboard -> API Keys -> Create API Key"
  - service: supabase
    why: "El custom SMTP y los 2 templates de prod NO están en git. Sin esto el remitente sigue siendo noreply@mail.app.supabase.io con el pie 'powered by Supabase' (D-01)."
    env_vars:
      - name: RESEND_SMTP_PASSWORD
        source: "Resend Dashboard -> API Keys (el re_... va SOLO al campo Password del SMTP en el Dashboard de Supabase; NUNCA al repo ni a NEXT_PUBLIC_*)"
    dashboard_config:
      - task: "Custom SMTP: host smtp.resend.com, port 465, user resend, pass = Resend API key, sender no-reply@forjo.studio, sender name Forjo"
        location: "Supabase Dashboard -> Authentication -> Emails -> SMTP Settings"
      - task: "Pegar el HTML brandeado de supabase/templates/confirmation.html en 'Confirm signup' + subject 'Confirmá tu cuenta en Forjo', sin romper el href con token_hash"
        location: "Supabase Dashboard -> Authentication -> Emails -> Templates -> Confirm signup"
      - task: "Pegar el HTML brandeado de supabase/templates/recovery.html en 'Reset password' + subject 'Restablecé tu contraseña', sin romper el href con token_hash"
        location: "Supabase Dashboard -> Authentication -> Emails -> Templates -> Reset password"

must_haves:
  truths:
    - "En local (Mailpit) los 2 mails renderizan brandeados en español y el link completa el flujo end-to-end sin regresión de Phase 4 (criterio 3)"
    - "En prod, el remitente de los 2 mails es no-reply@forjo.studio con display 'Forjo', sin 'powered by Supabase' ni noreply@mail.app.supabase.io (D-01, D-03, MAIL-01/02)"
    - "Un envío real a una casilla de Gmail cae en la bandeja de entrada, no en spam (criterio 4)"
    - "El link de los 2 mails, mandado por el SMTP real, sigue llevando a /onboarding (confirmación) y a /reset-password (recuperación) — cero regresión (criterio 3)"
    - "Las credenciales SMTP de Resend viven solo en el Dashboard de Supabase, nunca en el repo ni en NEXT_PUBLIC_* (T-06-03)"
  artifacts:
    - path: "supabase/templates/confirmation.html"
      provides: "Fuente del HTML que se pega en el Dashboard de prod (Confirm signup)"
      contains: "TokenHash"
    - path: "supabase/templates/recovery.html"
      provides: "Fuente del HTML que se pega en el Dashboard de prod (Reset password)"
      contains: "TokenHash"
  key_links:
    - from: "Resend (smtp.resend.com)"
      to: "Supabase Auth (GoTrue) custom SMTP"
      via: "host/user/pass + sender no-reply@forjo.studio en el Dashboard de Supabase"
      pattern: "smtp.resend.com"
    - from: "template de prod (Dashboard)"
      to: "app/auth/callback/route.ts"
      via: "href del mail -> /auth/callback?token_hash=...&type=..."
      pattern: "/auth/callback\\?token_hash="
---

<objective>
Cerrar la fase por donde no llega el código: la **config externa de envío** (custom SMTP vía Resend) y los **2
templates pegados en el Dashboard de prod**, más el **UAT real**. En local se verifica primero (Mailpit, gratis y
sin riesgo); en prod es un **checkpoint humano** (Dashboard de Supabase + Resend) y un **envío real a Gmail** que
es la única forma de probar el remitente, la marca real y la deliverability (criterio 4).

Purpose: sin el custom SMTP, el remitente sigue siendo `noreply@mail.app.supabase.io` con el pie "powered by
Supabase" — MAIL-01/02 quedan a medias (D-01). El envío real a Gmail es lo único que prueba el criterio 4
(inbox, no spam), que no es reproducible en local (Mailpit captura, no manda).
Output: custom SMTP configurado, 2 templates brandeados en prod, y el UAT (local + prod) verificado a mano.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/onboarding/phases/06-mails-de-cuenta-con-marca-forjo/06-RESEARCH.md
@.planning/workstreams/onboarding/phases/06-mails-de-cuenta-con-marca-forjo/06-CONTEXT.md
@.planning/workstreams/onboarding/ROADMAP.md
@supabase/templates/confirmation.html
@supabase/templates/recovery.html
</context>

## Artifacts this phase produces

| Artifact | Tipo | Dónde |
|----------|------|-------|
| Custom SMTP (host/user/pass/sender) | config externa, no versionada | Supabase Dashboard → Auth → SMTP Settings |
| HTML + subject de "Confirm signup" (prod) | config externa, no versionada | Supabase Dashboard → Auth → Templates |
| HTML + subject de "Reset password" (prod) | config externa, no versionada | Supabase Dashboard → Auth → Templates |
| Resend API key dedicada "supabase-auth-smtp" | secreto, solo Dashboard | Resend → API Keys |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT LOCAL — Mailpit: render brandeado en español + link end-to-end (criterio 3, sin regresión de Phase 4)</name>

  <files>— (verificación manual local; no modifica archivos)</files>

  <action>
    **Esta task es verificación humana; el agente solo prepara el entorno y presenta el guion.** El agente:
    1. Confirma que Docker Desktop está prendido (sin él no hay Mailpit ni UAT) y corre `npx supabase stop` +
       `npx supabase start` para que el CLI reparse `config.toml` con los templates brandeados y los subjects en
       español (06-01). Si el CLI rechaza algo, PARÁ y corregí antes de seguir.
    2. Confirma que la app corre en **`http://127.0.0.1:3000`** (NO `localhost:3000`: el matcher y la cookie de
       Supabase son literales por host, y `site_url` local es `127.0.0.1` — con `localhost` el link vuelve
       rebotado; es el Pitfall que documenta `config.toml` líneas 163-169).
    3. Presenta el guion de `<how-to-verify>` textualmente y espera el `<resume-signal>`. El agente NO marca
       ningún criterio por su cuenta: el render y el link viven en GoTrue + Mailpit, que ningún test toca.
  </action>

  <what-built>
    El plan 06-01 dejó los 2 templates brandeados y los subjects en español, versionados en el repo y con el href
    verificado por el guard de vitest. Falta lo que un test no puede ver: que el mail **renderiza bien** (logo,
    marca, español) y que el link **sigue completando el flujo** después del rebranding. En local se prueba con
    Mailpit (`:54324`), que captura los mails sin mandarlos — gratis y sin tocar prod.
  </what-built>

  <read_first>
    - `06-RESEARCH.md` §"Local Reproducibility" (qué se prueba en local vs prod) y §"Common Pitfalls" 6 (el SMTP
      real NO se prueba en local — Mailpit captura).
    - `supabase/config.toml` líneas 163-169 (por qué el host local es `127.0.0.1`, no `localhost`) y §`[inbucket]`
      (Mailpit en `:54324`).
    - `.planning/workstreams/onboarding/phases/04-recuperar-la-cuenta-auth-callback-reset/04-06-PLAN.md` §Task 3 —
      el UAT local de Phase 4, que este UAT no debe regresionar.
  </read_first>

  <how-to-verify>
    **Reglas:** app en `http://127.0.0.1:3000`; mails en `http://127.0.0.1:54324`. Si la base está limpia, creá
    primero una cuenta de prueba.

    ### A) Mail de confirmación — MAIL-01 (render) + criterio 3 (link)
    1. Entrá a `/register` y creá una cuenta con un mail nuevo. Abrí Mailpit (`:54324`) y abrí el mail de
       confirmación.
       - **Esperado (render):** header dark con el **logo de Forjo** visible, título y texto **en español**,
         **un** botón naranja "Confirmar cuenta". Subject "Confirmá tu cuenta en Forjo". Nada de "powered by
         Supabase". Si el logo aparece roto, anotá la URL — se ajusta en prod (Pitfall 4).
    2. Clickeá el botón del mail.
       - **Esperado (link, criterio 3):** caés en **`/onboarding`** ya autenticado, con la barra de direcciones
         **sin** `token_hash` (el callback lo canjeó — T-04-02). Cero regresión de Phase 4.

    ### B) Mail de recuperación — MAIL-02 (render, misma familia) + criterio 3 (link)
    3. Deslogueate → `/login` → "¿Olvidaste tu contraseña?" → `/forgot-password`, poné tu email y enviá. Abrí el
       mail de recuperación en Mailpit.
       - **Esperado (render):** **misma familia visual** que el de confirmación (mismo header/logo/marca), copy en
         español, botón "Cambiar contraseña". Subject "Restablecé tu contraseña". (D-05: se ven del mismo
         producto, no de dos distintos.)
    4. Clickeá el botón.
       - **Esperado (link, criterio 3):** caés en **`/reset-password`**, sin `token_hash` en la barra. Seteá una
         contraseña nueva y confirmá que quedás adentro del panel. El link viejo, reusado, cae en
         `/forgot-password?error=invalid_link` (no un rebote opaco).

    **Reportá al volver:** (a) si el logo renderizó o quedó roto (y con qué URL); (b) que los 2 mails están en
    español, brandeados y de la misma familia; (c) que los 2 links completaron el flujo sin `token_hash` en la
    barra.
  </how-to-verify>

  <acceptance_criteria>
    - Los 2 mails renderizan en Mailpit **en español**, brandeados (logo + marca Forjo), sin "powered by Supabase",
      y de la **misma familia** (D-05).
    - Subjects en español correctos ("Confirmá tu cuenta en Forjo" / "Restablecé tu contraseña").
    - El link de confirmación deja en `/onboarding` con sesión; el de recuperación deja en `/reset-password` y
      luego adentro del panel — **sin `token_hash` en la barra** (T-04-02) y **sin regresión de Phase 4**
      (criterio 3).
    - El resultado del render del logo quedó registrado en el SUMMARY (para ajustar la URL en prod si hiciera falta).
  </acceptance_criteria>

  <resume-signal>Escribí "aprobado" si los 2 mails renderizan brandeados/español/misma-familia y los 2 links
  completan el flujo sin token en la barra. Si algo falla, describí qué mail y qué paso.</resume-signal>

  <done>En local, los 2 mails brandeados renderizan en español y de la misma familia, y sus links siguen andando
  end-to-end (criterio 3) — listo para pasar a la config de prod.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: CHECKPOINT HUMANO — Resend + Supabase Dashboard (custom SMTP + templates de prod, D-01/D-02/D-03)</name>

  <files>— (config externa: Resend + Supabase Dashboard, proyecto tpvbjwqzskzkevepcwyb. Nada de esto vive en git)</files>

  <action>
    <!-- planner-discipline-allow: ConfirmationURL -->
    **PARÁ. Esta task NO la ejecuta el agente.** Es config del Dashboard de Resend y del Dashboard de Supabase de
    producción: no hay CLI ni API en juego, y tocar la superficie de auth a ciegas no es una opción. Presentá al
    humano los pasos de `<how-to-verify>` textualmente (los valores están para copiar y pegar), esperá el
    `<resume-signal>`, y registrá en el SUMMARY las respuestas que pide el checkpoint (dominio verificado, puerto
    que funcionó, que el href de los 2 templates quedó con `token_hash`).

    Si el humano reporta algo que no coincide — sobre todo el dominio `forjo.studio` **no** verificado en Resend
    (contradice D-02 / la asunción A1), o el puerto 465 que da timeout — **no improvises**: para 465 hay fallback
    documentado (587); para el dominio no verificado hay que hacer DNS en Cloudflare (Resend da los registros)
    antes de seguir.
  </action>

  <what-built>
    Los planes 06-01 y la Task 1 dejaron los 2 templates brandeados y verificados en local. Falta lo único que un
    agente no puede hacer: el **custom SMTP** (para que el remitente sea de Forjo y desaparezca el "powered by
    Supabase") y **pegar los 2 templates + subjects en el Dashboard de prod**. Sin esto, MAIL-01/02 quedan a
    medias: el mail saldría del default `noreply@mail.app.supabase.io` (D-01).
  </what-built>

  <read_first>
    - `06-RESEARCH.md` §"Datos EXACTOS del SMTP de Resend" (la tabla host/port/user/pass/sender) y §"Deliverability"
      (dominio verificado = SPF/DKIM ya firmados).
    - `06-CONTEXT.md` — D-01 (SMTP custom vía Resend), D-02 (dominio ya verificado), D-03 (remitente
      `no-reply@forjo.studio`).
    - `supabase/templates/confirmation.html` y `recovery.html` (06-01) — el HTML **exacto** que se pega en prod; el
      href de prod es **el mismo** que el local.
    - `lib/email.ts:29` — la evidencia de que `forjo.studio` ya entrega en prod (los mails de turnos salen de ahí).
  </read_first>

  <how-to-verify>
    Proyecto de Supabase: **`tpvbjwqzskzkevepcwyb`** (prod). Todo esto es a mano, en los Dashboards.

    **1) Resend → Domains.** Confirmá que `forjo.studio` figura **Verified** (SPF/DKIM en verde). Debería estarlo:
    ya sirve los mails de turnos (`lib/email.ts`). Mandar desde una dirección NUEVA `no-reply@forjo.studio` **no
    requiere DNS nuevo** — Resend verifica el **dominio**, no la dirección (D-02, A1). Si NO está verificado, PARÁ:
    hay que agregar los registros DNS que da Resend en Cloudflare antes de seguir.

    **2) Resend → API Keys.** Creá una API key **dedicada**, nombre `supabase-auth-smtp`. (Dedicada y no la global
    `RESEND_API_KEY`: si hay que rotarla, no tumba los mails de turnos. Blast radius chico.) Copiá el `re_...` — va
    directo al paso 3, **nunca al repo ni a un `NEXT_PUBLIC_*`** (T-06-03).

    **3) Supabase Dashboard → Authentication → Emails → SMTP Settings.** Prendé "Enable Custom SMTP" y cargá,
    **exactamente**:

        Host:          smtp.resend.com
        Port:          465
        Username:      resend
        Password:      (la Resend API key re_... del paso 2)
        Sender email:  no-reply@forjo.studio
        Sender name:   Forjo

    Es **un solo remitente global**: aplica a los DOS mails (confirmación y recuperación), lo que garantiza el
    mismo `From` en los dos (cierra D-05 por diseño). Si el puerto 465 diera timeout al guardar/probar, usá **587**
    como fallback (ambos válidos en Resend) y anotá cuál quedó.

    **4) Supabase Dashboard → Authentication → Emails → Templates → "Confirm signup".**
    - Subject: `Confirmá tu cuenta en Forjo`
    - Message body: pegá el HTML **completo** de `supabase/templates/confirmation.html` (06-01).
    - Verificá que el href del botón quedó `{{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=signup`
      y que NO se coló la variable de confirmación por defecto de Supabase (que rompe el callback). Guardá.

    **5) Supabase Dashboard → Authentication → Emails → Templates → "Reset password".**
    - Subject: `Restablecé tu contraseña`
    - Message body: pegá el HTML completo de `supabase/templates/recovery.html`.
    - Verificá que el href quedó `{{ .SiteURL }}/auth/callback?token_hash={{ .TokenHash }}&type=recovery`. Guardá.

    **Reportá al volver:** (a) que `forjo.studio` está Verified en Resend; (b) qué puerto quedó (465 o 587);
    (c) que el href de los 2 templates de prod quedó con `token_hash` (no la variable por defecto).
  </how-to-verify>

  <acceptance_criteria>
    - Custom SMTP en prod: `smtp.resend.com`, user `resend`, sender `no-reply@forjo.studio` + name `Forjo`, pass =
      Resend API key dedicada. Puerto (465 o 587) registrado en el SUMMARY.
    - `forjo.studio` confirmado Verified en Resend (D-02) — o, si no, DNS agregado antes de continuar.
    - "Confirm signup" en prod: HTML brandeado pegado + subject "Confirmá tu cuenta en Forjo" + href con
      `token_hash={{ .TokenHash }}&type=signup`.
    - "Reset password" en prod: HTML brandeado pegado + subject "Restablecé tu contraseña" + href con
      `token_hash={{ .TokenHash }}&type=recovery`.
    - La Resend API key NO aparece en ningún diff versionado ni en un `NEXT_PUBLIC_*` (T-06-03).
  </acceptance_criteria>

  <resume-signal>Escribí "listo" cuando el SMTP esté cargado y los 2 templates + subjects estén pegados en prod,
  con el reporte de (a) dominio verificado, (b) puerto, (c) href con token_hash. Si algo no coincide, describilo.</resume-signal>

  <done>Prod manda los 2 mails de auth por el SMTP de Resend desde `no-reply@forjo.studio` "Forjo", con el HTML
  brandeado en español y el href de token_hash intacto — sin "powered by Supabase".</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT PROD — envío real a Gmail: remitente + marca + inbox-no-spam (criterio 4) + link end-to-end</name>

  <files>— (verificación manual en prod; no modifica archivos)</files>

  <action>
    **Esta task es verificación humana en producción.** El agente presenta el guion de `<how-to-verify>`
    textualmente, espera el `<resume-signal>`, y registra en el SUMMARY el resultado de cada bloque (remitente,
    marca, español, inbox-vs-spam, SPF/DKIM, link). El agente NO puede tocar prod ni una casilla de Gmail: esto es
    el único paso que prueba el criterio 4 (deliverability), que no existe en local.

    Usá una **cuenta de prueba descartable** — no las cuentas reales que hay en prod. Si prod tira rate limit de
    mails, no adivines: se mira en Dashboard → Authentication → Rate Limits.
  </action>

  <what-built>
    Con el custom SMTP y los templates pegados (Task 2), prod ya manda los mails brandeados desde Forjo. Falta la
    prueba que solo existe en prod: un **envío real a Gmail** que confirme el remitente, la marca renderizada en un
    cliente real, y — criterio 4 — que **cae en la bandeja de entrada, no en spam**. Mailpit no puede probar esto
    (captura local, no manda por SMTP real; Pitfall 6).
  </what-built>

  <read_first>
    - `06-RESEARCH.md` §"Deliverability / inbox-no-spam (criterio 4)" (qué mirar: SPF/DKIM pass, From-name claro) y
      §"Common Pitfalls" 4 (logo hospedado que renderiza en Gmail).
    - `.planning/workstreams/onboarding/ROADMAP.md` §"Phase 6" — los 4 criterios de éxito.
    - El resultado del render del logo que la Task 1 dejó en el SUMMARY (si en local quedó roto, verificar acá con
      qué URL renderiza en Gmail).
  </read_first>

  <how-to-verify>
    Todo en **`https://gestion.forjo.studio`**, con una casilla de **Gmail** real y una cuenta de prueba nueva.

    ### A) Confirmación — MAIL-01 + criterio 4
    1. Registrate en prod con un mail de Gmail de prueba. Abrí Gmail.
       - **Remitente (D-03):** el From dice **"Forjo" &lt;no-reply@forjo.studio&gt;** — NO `noreply@mail.app.supabase.io`.
       - **Marca/idioma (MAIL-01):** el mail renderiza con el **logo de Forjo** (no roto), en español, botón
         "Confirmar cuenta". **Sin "powered by Supabase"** en ningún lado.
       - **Inbox, no spam (criterio 4) — BLOCKER:** el mail cayó en **Recibidos**, no en Spam ni en Promociones.
       - **SPF/DKIM:** en Gmail "Mostrar original" → `SPF: PASS` y `DKIM: PASS` para `forjo.studio`.
    2. Clickeá el botón. **Esperado (criterio 3):** caés en `/onboarding` autenticado, sin `token_hash` en la barra.

    ### B) Recuperación — MAIL-02 + criterio 4
    3. Deslogueate → `/forgot-password` → pedí el link con el mismo Gmail. Abrí el mail.
       - **Esperado:** **mismo remitente** `no-reply@forjo.studio` "Forjo", **misma marca/familia** que el de
         confirmación (D-05), en español, subject "Restablecé tu contraseña". **Cae en Recibidos, no en spam**
         (criterio 4).
    4. Clickeá el botón. **Esperado (criterio 3):** `/reset-password`, seteás contraseña nueva, quedás adentro del
       panel. Sin `token_hash` en la barra.

    **Reportá al volver:** (a) remitente exacto de los 2 mails; (b) que los 2 cayeron en Recibidos (no spam);
    (c) SPF/DKIM PASS; (d) que el logo renderizó en Gmail; (e) que los 2 links completaron el flujo.
  </how-to-verify>

  <acceptance_criteria>
    - **Criterio 1 / MAIL-01:** el mail de confirmación llega en español, con marca Forjo, desde
      `no-reply@forjo.studio` "Forjo", sin "powered by Supabase".
    - **Criterio 2 / MAIL-02:** el mail de recuperación llega con el **mismo remitente y la misma marca/familia**
      (D-05).
    - **Criterio 3:** los 2 links funcionan end-to-end en prod (confirmación → `/onboarding`; recuperación →
      `/reset-password` → panel), sin `token_hash` en la barra.
    - **Criterio 4 (BLOCKER):** los 2 mails caen en la **bandeja de entrada** de Gmail, no en spam; SPF y DKIM
      dan PASS. El logo renderiza (no roto).
    - UAT hecho con **cuenta de prueba nueva**, no con cuentas reales de prod.
  </acceptance_criteria>

  <resume-signal>Escribí "aprobado" si los 4 criterios del ROADMAP pasan (remitente + marca + español + inbox-no-spam
  + links). Si algo falla —sobre todo spam o logo roto— describí qué mail y qué viste, para armar el gap.</resume-signal>

  <done>Los 4 criterios de éxito de la fase verificados con un envío real a Gmail: remitente de Forjo, marca en
  español, misma familia, inbox-no-spam y links andando end-to-end. MAIL-01 y MAIL-02 cerrados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resend/GoTrue → cliente de mail | El `token_hash` viaja dentro del mail entregado por SMTP real; el link es lo único que autoriza la confirmación/recuperación. |
| Dashboard de Supabase → runtime | El custom SMTP, el remitente y los 2 templates de prod gobiernan qué se manda y a dónde vuelve el link. No están en git. |
| Resend API key → almacenamiento | La credencial de envío. Su lugar correcto es el campo Password del Dashboard; cualquier otro lado es una fuga. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | Resend API key en el repo o en un `NEXT_PUBLIC_*` | mitigate | La key va SOLO al campo Password del SMTP en el Dashboard de Supabase, y es una key **dedicada** (blast radius chico). Ningún `re_...` en un diff versionado; el bloque `[auth.email.smtp]` de `config.toml` queda comentado (local no manda por SMTP real). |
| T-06-05 | Tampering | Pegar el HTML en el Dashboard introduce la variable por defecto y rompe/desvía el link | mitigate | El HTML que se pega es el de 06-01 (guard de vitest verde). El checkpoint verifica visualmente el href con `token_hash`; el UAT prod (Task 3) clickea los 2 links end-to-end. Continúa T-04-20. |
| T-06-01 | Information Disclosure / Tampering | El `token_hash` roto/expuesto por el template de prod | mitigate | El href de prod es idéntico al local verificado; el UAT confirma que la barra queda sin `token_hash` tras el canje (T-04-02). |
| T-06-04 | Information Disclosure | `token_hash` en los logs de Resend / en tránsito | **accept** | Residual asumido, continúa T-04-06: el token es single-use (lo quema `verifyOtp`) y vence en 1h — uno logueado ya está consumido. El proveedor decide qué loguea; sin mitigación de código razonable adicional. |
</threat_model>

<verification>
- UAT local (Task 1): los 2 mails renderizan brandeados/español/misma-familia en Mailpit y los links completan el
  flujo sin regresión (criterio 3).
- Checkpoint (Task 2): custom SMTP cargado + 2 templates/subjects pegados en prod, con reporte de dominio
  verificado, puerto y href con token_hash.
- UAT prod (Task 3): envío real a Gmail — remitente `no-reply@forjo.studio`, marca/español, **inbox-no-spam**
  (criterio 4), SPF/DKIM PASS, links end-to-end. Con cuenta de prueba nueva.
- **`/gsd:secure-phase` obligatorio** (alcance acotado): es el canal por el que viaja el token. Confirmar que
  T-04-02/T-04-06/T-04-20 siguen cerradas y registrar T-06-01..05.
</verification>

<success_criteria>
- Los 2 mails de auth llegan en prod en español, con marca Forjo, desde `no-reply@forjo.studio` "Forjo", sin
  "powered by Supabase" (MAIL-01, MAIL-02, D-01, D-03).
- Los 2 mails se ven de la misma familia (D-05) y caen en la bandeja de entrada de Gmail, no en spam (criterio 4).
- Los links siguen funcionando end-to-end tras el cambio de template y remitente (criterio 3, cero regresión de
  Phase 4).
- Las credenciales SMTP viven solo en el Dashboard (T-06-03).
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/06-mails-de-cuenta-con-marca-forjo/06-02-SUMMARY.md` when done
</output>
