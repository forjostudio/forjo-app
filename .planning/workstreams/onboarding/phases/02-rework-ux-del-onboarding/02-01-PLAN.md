---
phase: 02-rework-ux-del-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(onboarding)/onboarding/page.tsx
autonomous: false
requirements: [ONB-01, ONB-02]
tags: [onboarding, ux, wizard, forms]

must_haves:
  truths:
    - "El usuario ve un botón 'Omitir por ahora' en los pasos Servicios/Profesionales/Horarios (NO en Negocio) y al usarlo avanza al paso siguiente sin completar el paso (ONB-01, D-01/D-02/D-04)"
    - "Solo el paso 1 (Negocio) bloquea 'Siguiente'; Servicios/Profesionales/Horarios ya no bloquean el avance (D-02)"
    - "En el vertical 'canchas' el paso Profesionales no aparece: el flujo tiene 3 pasos (Negocio → Servicios → Horarios) con numeración 1-2-3 sin huecos (D-03)"
    - "El botón Finalizar solo aparece en el último paso del array filtrado (Horarios en ambos verticales); omitir el último paso = finalizar con él vacío (D-04)"
    - "Los labels de la grilla de Servicios (Nombre/Min./Precio) son visibles siempre, no solo en la primera fila (ONB-02, D-07)"
    - "WhatsApp y Precio validan inline onBlur con error inmediato (text-xs text-destructive + aria-invalid), no solo al finalizar (ONB-02, D-08)"
    - "Una fila de servicio con nombre y precio 0 es válida (servicio gratuito); solo precio negativo da error (D-09)"
  artifacts:
    - path: "app/(onboarding)/onboarding/page.tsx"
      provides: "Onboarding wizard con Omitir, stepper dinámico por vertical, header de columnas fijo en Servicios, validación inline onBlur y precio 0 permitido"
      contains: "Omitir por ahora"
  key_links:
    - from: "app/(onboarding)/onboarding/page.tsx (nav bar)"
      to: "app/(onboarding)/onboarding/page.tsx (visibleSteps array)"
      via: "last-step detection via visibleSteps.length (no hardcoded 4) + Omitir hidden on step 1 and on last step"
      pattern: "visibleSteps\\.length"
    - from: "app/(onboarding)/onboarding/page.tsx (steps filter)"
      to: "lib/verticals.ts"
      via: "getVerticalKeyByType(type) === 'canchas' filters out Profesionales"
      pattern: "getVerticalKeyByType\\(type\\)\\s*===\\s*'canchas'"
---

<objective>
Rework de UX del flujo de alta existente en `app/(onboarding)/onboarding/page.tsx` (ONB-01 + ONB-02), aplicando exactamente las decisiones D-01..D-09 de CONTEXT.md y los contratos de interacción del UI-SPEC. Cinco cambios acotados dentro de un ÚNICO archivo cliente ya existente:

1. Botón "Omitir por ahora" en pasos no obligatorios que avanza al siguiente paso sin validar (D-01/D-02/D-04, ONB-01).
2. Relajar el gating de `canGoNext`: solo el paso Negocio bloquea (D-02).
3. Stepper dinámico: ocultar Profesionales en el vertical `canchas` → 3 pasos (D-03).
4. Header de columnas fijo en Servicios (labels siempre visibles) (D-07, ONB-02).
5. Validación inline onBlur en WhatsApp y Precio + permitir precio 0 (D-08/D-09, ONB-02).

Purpose: reducir la fricción del alta (llegar rápido al dashboard, completar lo omitido después desde el panel — D-05) y pulir claridad/feedback, SIN pasos ni campos nuevos, SIN rediseño visual, SIN tocar la persistencia (`handleFinish` ya filtra vacíos).

Output: `app/(onboarding)/onboarding/page.tsx` modificado in-place (Edit, no rewrite — regla de proyecto #3). No crea archivos, migraciones ni símbolos exportados nuevos.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/onboarding/ROADMAP.md
@.planning/workstreams/onboarding/STATE.md
@.planning/workstreams/onboarding/phases/02-rework-ux-del-onboarding/02-CONTEXT.md
@.planning/workstreams/onboarding/phases/02-rework-ux-del-onboarding/02-PATTERNS.md
@.planning/workstreams/onboarding/phases/02-rework-ux-del-onboarding/02-UI-SPEC.md

# Archivo núcleo de la fase (el único que se modifica)
@app/(onboarding)/onboarding/page.tsx

# Resolución de vertical (auto-skip canchas, D-03) — ya importado en el onboarding
@lib/verticals.ts

# Skill de convenciones del proyecto (stack, verticals, naming, patrón Server/Client)
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<artifacts_this_phase_produces>
Símbolos NUEVOS que introduce esta fase (todos locales al componente `OnboardingPage`, ningún export nuevo):

- `visibleSteps` — array `steps` filtrado por vertical (deriva de `steps` filtrando `Profesionales` cuando `getVerticalKeyByType(type) === 'canchas'`). Reemplaza los usos directos de `steps` en el stepper, en `canGoNext` y en la detección de "último paso" de la nav bar. Los números de nodo derivan de la posición en `visibleSteps` (1..N sin huecos), NO del `s.n` hardcodeado.
- Estado `whatsappError` (`useState<string | undefined>`) + handler onBlur de WhatsApp — valida formato con `normalizeArWhatsApp` (D-08).
- Campo opcional `priceError?: string` en la interfaz `Service` (mismo patrón que `HourBlock.error`) + handler onBlur de Precio por fila (D-08/D-09).
- Handler "Omitir" (inline `onClick={() => setStep(s => s + 1)}` en un `Button variant="ghost"`, o un `handleSkip` local si se prefiere nombrarlo) — avanza sin correr gating (D-04).

NO produce: archivos nuevos, pasos nuevos, campos de formulario nuevos, migraciones, columnas/flags de estado de onboarding (D-05 = solo dejar el dato vacío), ni cambios en `handleFinish` (los inserts ya filtran vacíos y ya toleran precio 0).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Stepper dinámico por vertical + relajar gating (D-02, D-03)</name>
  <files>app/(onboarding)/onboarding/page.tsx</files>
  <read_first>
    - `app/(onboarding)/onboarding/page.tsx:317-329` — array `steps` (fijo, 4 items) y `canGoNext` (gating actual con branches step===2/step===3 y el residual `s.price > 0`).
    - `app/(onboarding)/onboarding/page.tsx:348-373` — render del stepper: itera `steps.map((s, idx) => ...)`, usa `s.n` para el número de nodo, `step > s.n` / `step === s.n` para estado, `idx < steps.length - 1` para el conector.
    - `app/(onboarding)/onboarding/page.tsx:344` — subtítulo del header `Configurá tu negocio en 4 pasos` (el "4" debe reflejar el conteo real o volverse count-agnostic).
    - `app/(onboarding)/onboarding/page.tsx:410,416` — patrón existente de render condicional por vertical (`getVerticalKeyByType(type) === 'salud'`) a imitar.
    - `lib/verticals.ts` — firma de `getVerticalKeyByType(businessType?: string | null): VerticalKey`; `VerticalKey` incluye `'canchas'`. Ya importado en `page.tsx:15`.
    - 02-UI-SPEC.md §"Dynamic stepper — canchas auto-hide" y §"Non-mandatory step gating relax".
    - 02-PATTERNS.md §3 (dynamic stepper) y §2 (canGoNext relax).
  </read_first>
  <action>
    Derivar un array `visibleSteps` a partir de `steps` filtrando el item `Profesionales` cuando `getVerticalKeyByType(type) === 'canchas'` (per D-03). Reglas:
    - Mantener el array `steps` base con las 4 entradas actuales (Negocio/Servicios/Profesionales/Horarios) — el orden NO cambia (D-06). Calcular `visibleSteps` como `steps.filter(...)` que descarta la entrada de Profesionales solo en canchas; en el resto de verticales `visibleSteps === steps` (4 pasos).
    - En el render del stepper (`page.tsx:348-373`), iterar sobre `visibleSteps` en vez de `steps`. El número visible del nodo debe derivar de la POSICIÓN en `visibleSteps` (`idx + 1`), NO del `s.n` hardcodeado, para que se lea 1-2-3 en canchas y 1-2-3-4 en el resto SIN huecos (per UI-SPEC §Dynamic stepper "Numbering consistency"). Comparar el estado activo/completado contra `idx + 1` (el paso actual) en vez de `s.n`. El conector usa `idx < visibleSteps.length - 1`. Mantener intacto TODO el contrato visual del nodo: `w-9 h-9` rounded, activo = `bg-primary ... ring-4 ring-primary/20`, completado = `bg-primary` + icono `Check`, pending = `bg-secondary text-muted-foreground`, conector `bg-primary`(done)/`bg-border`(pending), caption `text-xs ... hidden sm:block`.
    - Relajar `canGoNext` (per D-02): eliminar por completo los branches `step === 2` y `step === 3` (esto también elimina el residual `s.price > 0`, cumpliendo D-09 a nivel de gating). Solo el paso Negocio conserva su predicado. Como `step` ahora indexa contra `visibleSteps`, el chequeo del paso Negocio debe keyear contra el PRIMER paso (Negocio siempre es `visibleSteps[0]`, paso 1), no contra una posición que se corra. Concretamente: si `step === 1` (primer paso = Negocio) retornar `name && slug && slugAvailable && type`; para cualquier otro paso retornar `true`.
    - Subtítulo del header (`page.tsx:344`): que el conteo refleje `visibleSteps.length` (ej. interpolando el número) o volverlo count-agnostic (ej. "en pocos pasos"), per UI-SPEC §Dynamic stepper. No dejar el literal "4" hardcodeado.
    - IMPORTANTE: este task NO toca todavía la nav bar (`step < 4` en `page.tsx:668`); eso se resuelve en Task 2 usando `visibleSteps.length`. Pero dejá `visibleSteps` definido y disponible en el scope del componente para que Task 2 lo consuma.
    - Comentarios en español explicando el "por qué" del filtrado por vertical y de la numeración derivada (convención del repo). Usar Edit (reemplazo parcial), NO reescribir el archivo.
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `visibleSteps` existe y filtra `Profesionales` cuando `getVerticalKeyByType(type) === 'canchas'`; en el resto de verticales tiene 4 pasos.
    - El stepper renderiza sobre `visibleSteps` con numeración derivada de la posición (1..N sin huecos): canchas 1-2-3, resto 1-2-3-4.
    - `canGoNext` ya no tiene branches para Servicios/Profesionales; solo el primer paso (Negocio) bloquea; ya no existe ningún chequeo `price > 0`.
    - El subtítulo del header refleja el conteo real de pasos o es count-agnostic (sin literal "4").
    - `npx tsc --noEmit` pasa sin errores.
    - El contrato visual del stepper (tamaños, tokens de color, ring, Check, caption) queda idéntico al actual.
  </acceptance_criteria>
  <done>El stepper y el gating se manejan por `visibleSteps`; canchas muestra 3 pasos con numeración 1-2-3 sin huecos; Servicios/Profesionales/Horarios ya no bloquean Siguiente; `tsc` verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Botón "Omitir por ahora" + nav bar guiada por visibleSteps (D-01, D-04)</name>
  <files>app/(onboarding)/onboarding/page.tsx</files>
  <read_first>
    - `app/(onboarding)/onboarding/page.tsx:659-680` — nav bar actual: `Atrás` (ghost, disabled en step 1) y el ternario `step < 4 ? Siguiente : Finalizar`. El literal `4` debe volverse `visibleSteps.length`.
    - `app/(onboarding)/onboarding/page.tsx:534-541` y `640-647` — botones ghost existentes (Trash2 / "Agregar bloque") que confirman `variant="ghost"` como el estilo de-emphasis del repo y el focus ring default de shadcn (`focus-visible:ring-2 focus-visible:ring-ring`).
    - `visibleSteps` definido en Task 1 (mismo archivo, scope del componente).
    - 02-UI-SPEC.md §"Interaction Contracts — Omitir button (D-01, D-02, D-04)" (placement, style, behavior, states, mobile) y §"Copywriting Contract" (copy exacto `Omitir por ahora`).
    - 02-PATTERNS.md §1 (Omitir button + nav bar).
  </read_first>
  <action>
    Reescribir el cluster derecho de la nav bar (`page.tsx:659-680`) para que quede: `Atrás` (izquierda, sin cambios) — `[ Omitir por ahora ]  [ Siguiente ]` (derecha), y en el último paso solo la CTA `Finalizar`. Reglas exactas del UI-SPEC:
    - Detección de "último paso": reemplazar el literal `step < 4` por `step < visibleSteps.length`. En pasos intermedios se muestra `Siguiente` (CTA primaria, `Button` default) + `Omitir por ahora` a su izquierda; en el último paso (`step === visibleSteps.length`, que es Horarios en ambos verticales) se muestra la CTA `Finalizar y entrar al dashboard` (con estado loading `Guardando...`) y NO se muestra Omitir (omitir el último paso = finalizar con él vacío, D-04).
    - Botón Omitir: copy EXACTO `Omitir por ahora` (per UI-SPEC Copywriting). `variant="ghost"` (nunca accent, nunca destructive — el accent queda reservado para la única CTA forward). Se muestra SOLO en pasos no obligatorios y NO en el último paso: condición = `step > 1 && step < visibleSteps.length` (paso 1 = Negocio obligatorio → sin Omitir; último paso → sin Omitir, hay Finalizar). Ubicación: inmediatamente a la izquierda de `Siguiente` dentro del cluster derecho, como hermano de menor énfasis.
    - Comportamiento de Omitir: `onClick={() => setStep(s => s + 1)}` — avanza al paso siguiente SIN correr `canGoNext()` ni validación alguna (skip granular por paso, NO salto al final). No persiste nada: los vacíos ya se filtran en `handleFinish` (D-05). Omitir NO tiene estado disabled (siempre habilitado en pasos opcionales).
    - `Siguiente` mantiene `disabled={!canGoNext()}` (que tras Task 1 solo bloquea en Negocio). `Atrás` intacto (`disabled={step === 1}`).
    - Estados obligatorios del botón Omitir (todos por defaults de shadcn ghost — NO removerlos): hover `hover:bg-accent hover:text-accent-foreground`, focus-visible `focus-visible:ring-2 focus-visible:ring-ring`, active pressed default. Touch target: usar la altura default del `Button` (h-9+ con padding), no `size="sm"`.
    - Mobile 375px: en el contenedor derecho, permitir que Omitir + Siguiente sigan tappables si envuelven; mantener `Siguiente`/`Finalizar` como la acción más prominente. No introducir spacing arbitrario — usar la escala existente (`gap-2`/`gap-4`).
    - Comentarios en español explicando por qué Omitir avanza sin gating y por qué no aparece en paso 1 ni en el último. Usar Edit, NO reescribir el archivo.
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - El texto `Omitir por ahora` aparece en la nav bar con `variant="ghost"`, visible cuando `step > 1 && step < visibleSteps.length`, oculto en paso 1 y en el último paso.
    - Omitir avanza con `setStep(s => s + 1)` sin llamar a `canGoNext()`.
    - La detección de último paso usa `visibleSteps.length` (no queda ningún literal `4` en la nav bar).
    - En el último paso aparece `Finalizar y entrar al dashboard` (con `Guardando...` en loading) y NO Omitir.
    - `Atrás` y `Siguiente` conservan su comportamiento (Atrás disabled en step 1, Siguiente disabled según `canGoNext`).
    - `npx tsc --noEmit` y `npm run lint` pasan.
  </acceptance_criteria>
  <done>La nav bar ofrece Omitir por ahora (ghost) en pasos opcionales intermedios, avanza sin validar, y Finalizar aparece solo en el último paso derivado de `visibleSteps.length`; `tsc` + `lint` verdes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Header fijo de Servicios + validación inline onBlur (WhatsApp/Precio) + precio 0 (D-07, D-08, D-09)</name>
  <files>app/(onboarding)/onboarding/page.tsx</files>
  <read_first>
    - `app/(onboarding)/onboarding/page.tsx:59-63` — interfaz `Service` (donde se agrega `priceError?`).
    - `app/(onboarding)/onboarding/page.tsx:36-40` — `HourBlock { error? }` (patrón de error a nivel item a espejar para `Service.priceError`).
    - `app/(onboarding)/onboarding/page.tsx:490-551` — step Servicios: la grilla `grid grid-cols-12 gap-2 items-end`, los labels por primera fila `{i === 0 && <Label ...>}` (Nombre col-span-5, Min. col-span-3 con icono Clock, Precio col-span-3 con icono DollarSign), y el input de precio (`page.tsx:524-530`).
    - `app/(onboarding)/onboarding/page.tsx:81` (estado `whatsapp`) y `page.tsx:447-449` (Input de WhatsApp en step 1).
    - `app/(onboarding)/onboarding/page.tsx:236-243` — chequeo de formato de WhatsApp con `normalizeArWhatsApp` (hoy solo en `handleFinish`); `normalizeArWhatsApp` importado en `page.tsx:16`.
    - `app/(onboarding)/onboarding/page.tsx:607-637` — markup de error inline existente a espejar: `aria-invalid={!!b.error}` en el Input y `{b.error && <p className="text-xs text-destructive">{b.error}</p>}`.
    - `app/(dashboard)/agenda/agenda-client.tsx` `validateBlocks` (~línea 228-255) — analog del panel (error vive en el estado del item, render `text-xs text-destructive`, input `aria-invalid`).
    - 02-UI-SPEC.md §"Servicios column header (D-07)", §"Inline validation onBlur (D-08)", §"Servicios validation — allow price 0 (D-09)", §"Copywriting Contract" (mensajes exactos).
    - 02-PATTERNS.md §4 (Servicios header), §5 (inline validation), §6 (allow price 0).
  </read_first>
  <action>
    Tres cambios dentro del mismo step Servicios + step 1 (WhatsApp), todos con Edit:

    (A) Header de columnas fijo en Servicios (D-07): promover los tres labels (`Nombre`, `Min.` con icono `Clock`, `Precio` con icono `DollarSign`) a UNA fila de encabezado fija (no sticky) UBICADA arriba del `.map` de servicios, alineada a la misma grilla `grid grid-cols-12 gap-2`: `Nombre` = `col-span-5`, `Min.` = `col-span-3`, `Precio` = `col-span-3`, y un spacer `col-span-1` para la columna del trash. Labels con `text-xs text-muted-foreground` (idéntico a los actuales). Quitar los gates `{i === 0 && <Label ...>}` de las filas (los labels ahora son redundantes en la fila). La grilla/spacing de las filas se mantiene; `items-end` puede pasar a `items-center` ya que los labels ya no desplazan la fila 1 — mantené la alineación vertical consistente entre filas. Siempre visible sin importar qué fila tenga foco.

    (B) Validación inline onBlur (D-08):
    - WhatsApp (step 1, Input en `page.tsx:447-449`): agregar estado local nuevo `const [whatsappError, setWhatsappError] = useState<string | undefined>()`. En `onBlur` del Input: si `whatsapp.trim()` no está vacío y `normalizeArWhatsApp(whatsapp)` devuelve null → `setWhatsappError('WhatsApp inválido. Usá código de país y área, ej. +54 9 11 1234-5678')`; en cualquier otro caso (vacío o válido) limpiar el error. Limpiar también en `onChange` (o al escribir) para feedback en vivo. Poner `aria-invalid={!!whatsappError}` en el Input y renderizar `{whatsappError && <p className="text-xs text-destructive">{whatsappError}</p>}` debajo. WhatsApp sigue OPCIONAL: vacío = válido, sin error. Copy EXACTO reusando el mensaje del `handleFinish` (per UI-SPEC Copywriting).
    - Precio (Servicios): agregar campo opcional `priceError?: string` a la interfaz `Service` (espejando `HourBlock.error`). En `onBlur` del Input de precio de cada fila: si el valor es `< 0` → setear `priceError` de esa fila a `'El precio no puede ser negativo'`; si es `>= 0` (incluye 0) limpiar el error. Precio 0 y positivos son VÁLIDOS (D-09). `aria-invalid={!!service.priceError}` en el Input de precio y `{service.priceError && <p className="text-xs text-destructive">{service.priceError}</p>}` debajo de la fila/campo. Limpiar el error al escribir (`onChange` ya llama `updateService`; asegurar que el error se limpie o no persista tras corregir). El error vive en el estado del item (mismo criterio que `HourBlock.error` y `validateBlocks` del panel).
    - Mensajes: SOLO problema + fix, en español, per Copywriting Contract. No introducir tokens de color arbitrarios: error siempre `text-destructive`.

    (C) Permitir precio 0 (D-09): confirmar que NO queda ningún chequeo `price > 0` que bloquee un servicio gratuito válido (el de `canGoNext` ya se removió en Task 1). El insert de `services` (`page.tsx:265-267`, filtra por `s.name`) y el panel (`settings-client.tsx`, `min={0}`) YA toleran precio 0 — NO cambiar el modelo de datos ni el insert. `priceError` NO se agrega al payload del insert (es solo estado de UI); si TypeScript se queja porque el spread `{ ...s }` arrastra `priceError` a la fila de `services`, excluirlo explícitamente en el `.map` del insert (destructurar y descartar `priceError`) para no mandar una columna inexistente. Verificar/ajustar ese punto.

    Comentarios en español explicando el header fijo, la validación onBlur (por qué WhatsApp vacío es válido y por qué precio 0 se acepta). Usar Edit, NO reescribir el archivo.
  </action>
  <verify>
    <automated>cd "c:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Los labels Nombre/Min./Precio aparecen en una fila de encabezado fija arriba del `.map` (col-span 5/3/3 + spacer 1) y ya NO dependen de `i === 0`; son visibles en todas las filas.
    - WhatsApp valida onBlur: no-vacío + inválido muestra `WhatsApp inválido. Usá código de país y área, ej. +54 9 11 1234-5678` con `aria-invalid`; vacío o válido no muestra error; el error se limpia al corregir.
    - Precio valida onBlur: negativo muestra `El precio no puede ser negativo` con `aria-invalid`; precio 0 y positivos son válidos; el error se limpia al corregir.
    - `priceError` NO se envía al insert de `services` (no rompe el tipo de la fila insertada).
    - No queda ningún chequeo `price > 0` bloqueante en el flujo.
    - `npx tsc --noEmit` y `npm run lint` pasan.
  </acceptance_criteria>
  <done>Servicios tiene header de columnas fijo siempre visible; WhatsApp y Precio validan inline onBlur con el markup de error del repo; precio 0 es aceptado sin cambios en la persistencia; `tsc` + `lint` verdes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint — verificación humana del rework de UX del onboarding</name>
  <action>Pausar para verificación humana: el dueño confirma manualmente, sobre la app corriendo, que el rework cumple los contratos del UI-SPEC (Omitir por ahora en pasos opcionales, gating relajado, stepper dinámico de canchas, header fijo de Servicios, validación inline onBlur en WhatsApp/Precio, precio 0 válido) y que no hay regresión en el alta. Seguir los pasos de <how-to-verify>.</action>
  <what-built>
    El rework completo de UX del onboarding en `app/(onboarding)/onboarding/page.tsx`: botón "Omitir por ahora" en pasos opcionales, stepper dinámico (Profesionales oculto en canchas), header de columnas fijo en Servicios, validación inline onBlur en WhatsApp/Precio, y precio 0 permitido. Todo verificado con `npx tsc --noEmit` + `npm run lint`. Esta fase tiene UI hint = yes, así que la fidelidad visual e interacción requieren verificación humana contra el UI-SPEC.
  </what-built>
  <how-to-verify>
    1. Levantar el dev server: `npm run dev` y abrir el onboarding (crear/entrar con un usuario sin negocio → `/onboarding`, o la ruta que dispara el alta).
    2. **Omitir (ONB-01, D-01/D-04):** en el paso 1 (Negocio) NO debe haber botón "Omitir por ahora". Completar Negocio, Siguiente. En Servicios y Profesionales debe verse `Omitir por ahora` (estilo ghost, a la izquierda de "Siguiente", nunca con color de acento). Al clickear Omitir, avanza al paso siguiente sin exigir datos. En el ÚLTIMO paso (Horarios) NO debe haber Omitir, sino "Finalizar y entrar al dashboard".
    3. **Gating relajado (D-02/D-09):** en Servicios/Profesionales/Horarios, "Siguiente" debe estar habilitado aunque estén vacíos o con precio 0. Cargar un servicio con precio 0 y confirmar que se puede avanzar y finalizar.
    4. **Stepper dinámico (D-03):** elegir un tipo de negocio del rubro **canchas** en el paso 1 → el stepper debe mostrar 3 pasos (Negocio → Servicios → Horarios) numerados 1-2-3 sin huecos, sin "Profesionales". Con cualquier otro rubro, 4 pasos 1-2-3-4. El subtítulo del header debe reflejar el conteo (o ser count-agnostic).
    5. **Header Servicios (D-07):** en el paso Servicios, los labels Nombre/Min./Precio deben verse SIEMPRE arriba de la grilla, no solo en la primera fila. Agregar varias filas y confirmar que los labels siguen visibles.
    6. **Validación inline (D-08):** en WhatsApp (paso 1) escribir un número inválido y salir del campo (blur) → error inmediato en rojo abajo del campo; dejarlo vacío → sin error. En Precio poner un valor negativo y blur → `El precio no puede ser negativo`; poner 0 → sin error.
    7. **Accesibilidad/estados:** verificar focus ring visible al tabular por Omitir y los inputs; en 375px (responsive) la nav bar y los pasos siguen usables.
    8. **Cero regresión:** finalizar el alta (con o sin pasos omitidos) debe crear el negocio y redirigir al dashboard; los pasos omitidos aparecen vacíos en el panel (Servicios/Equipo/Agenda) sin errores.
  </how-to-verify>
  <resume-signal>Escribí "approved" si el flujo cumple los contratos del UI-SPEC (Omitir, stepper canchas, header Servicios, validación inline, precio 0), o describí los desvíos para corregir.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → estado local del wizard | La interacción del usuario (Omitir, avanzar, escribir) solo muta estado React local; no cruza a un endpoint nuevo. |
| cliente → `handleFinish` (insert Supabase) | Único punto que persiste; escribe SIEMPRE con `business.id` del negocio creado por la sesión del owner autenticado, bajo RLS + filtro `business_id` ya vigentes (patrón inalterado por esta fase). |

## STRIDE Threat Register

Relevancia de seguridad de la fase: **BAJA** (rework de presentación/UX). No agrega endpoints, ni escrituras nuevas, ni datos de tenant nuevos. La superficie modificada es navegación + validación client-side dentro de un componente ya existente. No se introduce ninguna amenaza de severidad `high`.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Flujo del wizard vía "Omitir" (D-01/D-04) | accept | "Omitir" solo saltea pasos OPCIONALES (Servicios/Profesionales/Horarios); solo Negocio es obligatorio (D-01) y ningún paso omitible es prerrequisito real de otro → el flujo no puede quedar en un estado persistido inconsistente. Los inserts de `handleFinish` ya filtran vacíos (D-05); omitir = no persistir, nunca persistir basura. |
| T-02-02 | Tampering | Validación client-side relajada (`canGoNext`, D-02/D-09) | accept | Relajar el gating de avance es puramente de UX (permite avanzar sin completar). NO debilita ninguna validación SERVER-SIDE: la creación del negocio y el aislamiento por tenant (RLS + `business_id`) viven en `handleFinish`/Supabase y quedan intactos. Precio 0 es un valor de dominio válido (servicio gratuito), no un bypass. |
| T-02-03 | Information Disclosure | Escritura del negocio en `handleFinish` | mitigate | Sin cambios en esta fase: `handleFinish` escribe con `business.id` del owner de la sesión y `linkLeadOnSignup` corre service-role re-derivando el email del owner (anti-tampering, T-04-09 previo). Esta fase no toca ese path → el aislamiento multi-tenant existente se preserva. |

**Sin amenazas `high`.** No hay tareas de instalación de paquetes (npm/pip/cargo) → sin gate de legitimidad de paquetes ni `T-02-SC`. No hay migraciones ni cambios de esquema → sin tarea `[BLOCKING]` de schema-push (confirmado por el orquestador).
</threat_model>

<verification>
## Verificación de fase

**Automatizada (corre en cada task):**
- `npx tsc --noEmit` — type safety del componente reworkeado (incluye que `priceError`/`whatsappError` y `visibleSteps` tipen correctamente y que `priceError` no rompa el tipo del insert).
- `npm run lint` — reglas de Next/Core Web Vitals (Tasks 2 y 3).

**Por qué no hay tests unitarios nuevos:** el runner del proyecto está configurado con `environment: 'node'` y explícitamente "esta suite no renderiza JSX" (D-01 del setup de Vitest); no hay infraestructura de testing de componentes (sin testing-library/jsdom). Toda la lógica de esta fase es estado de un componente React cliente (navegación del wizard, gating, onBlur), sin funciones puras extraíbles que justifiquen montar un harness nuevo — fuera de scope (REQUIREMENTS §Out of Scope: no rediseño, no capacidades nuevas). La verificación de comportamiento/fidelidad se cubre con el checkpoint human-verify (fase UI hint = yes).

**Humana (checkpoint blocking):** ver Task 4 — cubre los 8 criterios de interacción/visual contra el UI-SPEC (Omitir, gating, stepper canchas, header Servicios, validación inline, precio 0, accesibilidad, cero regresión).
</verification>

<success_criteria>
Fase completa cuando (mapeo a Success Criteria del ROADMAP Phase 2 + decisiones):
1. (SC1, ONB-01) El usuario ve "Omitir por ahora" en pasos no obligatorios, lo usa, y llega al dashboard con esos pasos sin completar. ✔ Tasks 1-2 + checkpoint.
2. (SC2, D-05) Un paso omitido se completa después desde el panel (dato vacío, empty states naturales del panel — sin cambios en esta fase). ✔ verificado en checkpoint (paso 8).
3. (SC3, D-01) Los pasos obligatorios siguen exigiéndose: solo Negocio bloquea; el resto es opcional y claramente distinguible (Omitir presente solo en opcionales). ✔ Task 1-2.
4. (SC4, ONB-02, D-07/D-08) El flujo se siente más claro: labels siempre visibles (header fijo Servicios), feedback inmediato onBlur (WhatsApp/Precio), orden lógico mantenido (D-06). ✔ Task 3.
5. (D-03) Canchas oculta Profesionales (3 pasos, numeración sin huecos). ✔ Task 1.
6. (D-09) Precio 0 aceptado como servicio gratuito. ✔ Tasks 1+3.
7. `npx tsc --noEmit` y `npm run lint` verdes; checkpoint human-verify aprobado.
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/02-rework-ux-del-onboarding/02-01-SUMMARY.md` when done
</output>
