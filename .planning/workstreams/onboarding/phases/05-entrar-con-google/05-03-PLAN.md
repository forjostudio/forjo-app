---
phase: 05-entrar-con-google
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified: []
autonomous: false
requirements: [AUTH-03, AUTH-04, AUTH-05]

user_setup:
  - service: google-cloud
    why: "Sin el redirect URI del callback de Supabase en las credenciales OAuth, Google rechaza el round-trip. Se reusan las credenciales que ya existen (client_secret_*.json, hoy de Calendar), no se crean nuevas."
    dashboard_config:
      - task: "Agregar el Authorized redirect URI del callback de Supabase (prod) a las credenciales OAuth existentes"
        location: "Google Cloud Console -> APIs & Services -> Credentials -> (OAuth 2.0 Client ID existente) -> Authorized redirect URIs"
  - service: supabase
    why: "El provider de Google no está habilitado. Sin esto el botón redirige a un flujo que no existe. El client_id/secret vive en el Dashboard, nunca en git."
    dashboard_config:
      - task: "Habilitar el provider Google y pegar client ID + secret de las credenciales existentes"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"
      - task: "Verificar que la allowlist de Redirect URLs contenga https://gestion.forjo.studio/auth/callback, SIN wildcard de previews (D-06)"
        location: "Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs"

must_haves:
  truths:
    - "El provider Google está habilitado en el Dashboard de Supabase y el redirect URI del callback de Supabase está en las credenciales OAuth existentes (checkpoint humano)"
    - "Una persona sin cuenta entra con 'Continuar con Google', elige su cuenta, y queda autenticada en Forjo sin inventar contraseña (AUTH-03)"
    - "Ese usuario nuevo cae en el onboarding y crea su negocio; al volver más tarde entra directo a su panel — la segunda vez es un login, no un alta duplicada (AUTH-04)"
    - "El mismo email registrado antes con contraseña entra después con Google (y al revés) y termina en UNA sola cuenta, con su negocio intacto — nunca duplicada ni con error opaco (AUTH-05, D-01, verificado en los dos órdenes)"
    - "Un fallo o una cancelación de Google cae en /login con un mensaje claro, no en una pantalla muerta (D-05)"
  artifacts:
    - path: "Supabase Dashboard -> Authentication -> Providers -> Google"
      provides: "Provider Google habilitado (config externa, no versionada en git)"
      contains: "enabled"
    - path: ".planning/workstreams/onboarding/phases/05-entrar-con-google/05-03-SUMMARY.md"
      provides: "Registro del checkpoint (A2 confirmado, entradas de la allowlist, resultado del cross-test de linking en los dos órdenes)"
      contains: "linking"
  key_links:
    - from: "Google Cloud (redirect URI)"
      to: "Supabase Auth (callback)"
      via: "https://<project-ref>.supabase.co/auth/v1/callback en las credenciales OAuth existentes"
      pattern: "auth/v1/callback"
    - from: "Supabase provider Google"
      to: "app/(auth) (botón + callback)"
      via: "el round-trip real que el botón (05-02) y el canje (05-01) esperan"
      pattern: "google"
---

<objective>
Cerrar la fase por donde no llega el código: la **config externa** que un agente no puede tocar (Google Cloud
+ Dashboard de Supabase) y el **UAT prod-first** que verifica los 3 requisitos end-to-end, incluido el
cross-test de account linking en los **dos órdenes** — la trampa real del milestone.

Purpose: sin el provider habilitado y el redirect URI cargado, el botón (05-02) redirige a un flujo que no
existe y el canje (05-01) no tiene qué canjear. Y el linking (D-01/AUTH-05) es comportamiento de GoTrue que
**no** se puede testear sin red ni credenciales (H-06): se prueba a mano o no se prueba. Si sale mal, un
cliente que ya paga puede quedar mirando el negocio de nadie.
Output: config de prod aplicada y verificada a mano; UAT de AUTH-03/04/05 con cuenta descartable; los
hallazgos registrados en el SUMMARY.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/onboarding/phases/05-entrar-con-google/05-RESEARCH.md
@.planning/workstreams/onboarding/phases/05-entrar-con-google/05-CONTEXT.md
@.planning/workstreams/onboarding/ROADMAP.md
</context>

## Artifacts this phase produces

| Artifact | Tipo | Dónde |
|----------|------|-------|
| Redirect URI del callback de Supabase | config externa, no versionada | Google Cloud (credenciales existentes) |
| Provider Google habilitado (client_id/secret) | config externa, no versionada | Supabase Dashboard |
| Verificación de la allowlist de Redirect URLs (sin wildcard) | config externa | Supabase Dashboard |
| Registro del UAT + cross-test de linking | sección del SUMMARY | `05-03-SUMMARY.md` |

> **Dos aclaraciones de alcance del research, leelas antes de arrancar:**
>
> **A2 (a confirmar en el checkpoint):** el plan asume que las credenciales OAuth existentes (`client_secret_*.json`,
> hoy usadas por Google Calendar) **sirven** como client_id/secret del provider de Supabase sin crear unas nuevas.
> Es MEDIUM confidence: si el "OAuth consent screen" o los scopes de esas credenciales no dejan que Supabase las
> use, habría que crear un client OAuth nuevo. **Confirmalo con el dueño en el checkpoint** — no lo des por hecho.
>
> **UAT prod-first (D-10):** el OAuth de Google contra el Supabase **local** exige credenciales reales + el URI
> `127.0.0.1:54321` en Google Cloud + `skip_nonce_check=true` — superficie extra que el dueño decidió **no** pagar.
> Se prueba en **prod** con una cuenta Google descartable, como el paso E de Phase 4 (T-04-22). **No** se versiona
> `[auth.external.google]` en `config.toml` en esta fase (respeta D-10; si el dueño después quiere UAT local, es un
> `/gsd:quick` aparte).
>
> **Este plan no modifica archivos del repo** (`files_modified: []`): es config de Dashboard + verificación manual.
> Su salida vive en el SUMMARY y en la config viva de prod.

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: CHECKPOINT HUMANO — Google Cloud + Dashboard de Supabase (prod)</name>

  <files>— (config externa: Google Cloud Console + Supabase Dashboard. Nada de esto vive en git. El client secret NUNCA va al repo ni a NEXT_PUBLIC_*.)</files>

  <action>
    **PARÁ. Esta task NO la ejecuta el agente.** Es config de Google Cloud y del Dashboard de Supabase de
    producción: no hay CLI ni API en juego, y tocar la superficie de auth de prod a ciegas no es una opción.
    Presentá al humano los pasos de `<how-to-verify>` **textualmente**, esperá el `<resume-signal>`, y registrá en
    el SUMMARY las respuestas que el checkpoint pide: (a) si las credenciales existentes sirvieron o hubo que crear
    un client OAuth nuevo (A2); (b) la lista de entradas preexistentes en Redirect URLs (una vieja con wildcard
    sería el agujero que D-06 cierra); (c) que "Confirm email" sigue ON en prod (gate del linking seguro, T-05-01).

    Si el humano reporta algo que no coincide — sobre todo un wildcard en Redirect URLs, o "Confirm email" apagado
    — **no improvises**: es una divergencia contra el research (H-01/D-02) que invalida la seguridad del linking
    automático. Pará y revalidá antes de seguir.
  </action>

  <what-built>
    Los planes `05-01` y `05-02` dejaron el código completo: el botón de Google en login/register, la rama
    `code`→`exchangeCodeForSession` en el callback, el destino `oauth`→`/dashboard`, y el error propio a
    `/login?error=oauth`. **Falta lo único que un agente no puede hacer:** habilitar el provider en Supabase y
    cargar el redirect URI en Google Cloud. Sin esto, el botón redirige a un flujo que no existe y el canje no
    tiene qué canjear.
  </what-built>

  <read_first>
    - `.planning/workstreams/onboarding/phases/05-entrar-con-google/05-RESEARCH.md` §"Security Domain" #4 (config / external setup), §"Assumptions Log" A2, §"Environment Availability".
    - `.planning/workstreams/onboarding/phases/05-entrar-con-google/05-CONTEXT.md` — D-06 (sin wildcard) y el bloque "Checkpoint humano".
    - `client_secret_*.apps.googleusercontent.com.json` (raíz del repo) — las credenciales OAuth que ya existen (Calendar). Son las que se reusan; el checkpoint NO crea unas nuevas salvo que A2 falle.
  </read_first>

  <how-to-verify>
    Proyecto de Supabase de prod: **`tpvbjwqzskzkevepcwyb`** (el `<project-ref>`). El callback de Supabase vive en
    `https://<project-ref>.supabase.co/auth/v1/callback`.

    **1) Google Cloud Console → APIs & Services → Credentials**
    Abrí el **OAuth 2.0 Client ID que ya existe** (el que usa Google Calendar — corresponde al
    `client_secret_*.json` de la raíz del repo). En **Authorized redirect URIs**, agregá **exactamente** (copiá y
    pegá, reemplazando `<project-ref>` por `tpvbjwqzskzkevepcwyb`):

        https://tpvbjwqzskzkevepcwyb.supabase.co/auth/v1/callback

    **No crees un client nuevo** salvo que el paso 2 falle porque el consent screen / scopes de este client no
    dejan usarlo desde Supabase (A2). Guardá.

    **2) Supabase Dashboard → Authentication → Providers → Google**
    Habilitá el provider. Pegá el **Client ID** y el **Client Secret** de las credenciales del paso 1 (salen del
    `client_secret_*.json`). Guardá.
    - Si el Dashboard rechaza las credenciales o el flujo de prueba falla por consent screen/scopes → **es A2**:
      hay que crear un OAuth Client ID nuevo dedicado a Supabase. Anotalo y avisá.

    **3) Supabase Dashboard → Authentication → URL Configuration → Redirect URLs**
    Verificá que esté `https://gestion.forjo.studio/auth/callback` (debería estar desde Phase 4). Si no está,
    agregala **exactamente**. **NO agregues ningún wildcard de previews** (nada de `https://*.vercel.app/**`): D-06
    es explícito, el matcher de Supabase es glob y los globs sobre-matchean en la superficie donde un agujero
    entrega cuentas. **Consecuencia asumida: OAuth no anda en previews.**

    **4) Supabase Dashboard → Authentication → Providers → Email → "Confirm email"**
    **Verificá que siga ON y NO lo toques.** Es el gate que hace seguro el linking automático (T-05-01): GoTrue
    solo auto-vincula una identidad Google a una cuenta existente si el email está **verificado**, y confirm-email
    ON garantiza que toda cuenta de contraseña ya lo está (H-01/D-02). Si lo encontrás apagado, **pará y avisá**:
    el linking automático dejaría de ser seguro.

    **Reportá al volver:** (a) si las credenciales existentes sirvieron o hubo que crear un client nuevo (A2); (b)
    las entradas preexistentes en Redirect URLs — **listalas**, y confirmá que ninguna tiene `*`; (c) que "Confirm
    email" sigue ON.
  </how-to-verify>

  <acceptance_criteria>
    - El redirect URI `https://tpvbjwqzskzkevepcwyb.supabase.co/auth/v1/callback` está en las Authorized redirect URIs del client OAuth (existente o, si A2 falló, el nuevo).
    - El provider Google está **habilitado** en el Dashboard con client ID + secret cargados.
    - Redirect URLs contiene `https://gestion.forjo.studio/auth/callback` y **ninguna entrada con `*`**. Las preexistentes quedaron listadas en el SUMMARY.
    - "Confirm email" sigue ON en prod (verificado, no asumido).
    - El resultado de A2 (credenciales reusadas vs client nuevo) quedó registrado en el SUMMARY.
  </acceptance_criteria>

  <resume-signal>Escribí "listo" cuando los 4 pasos estén hechos, incluyendo el reporte de (a), (b) y (c). Si algo no coincide con lo descrito (wildcard, Confirm email apagado, A2 falló), describí qué encontraste en vez de improvisar.</resume-signal>

  <done>Prod tiene el provider Google habilitado, el redirect URI cargado en las credenciales, la allowlist mínima de D-06 (sin wildcards) y "Confirm email" confirmado ON — el botón ya tiene un flujo real al que redirigir.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT prod-first — AUTH-03/04/05 + cross-test de linking en los dos órdenes (D-01/D-04/D-05/D-10)</name>

  <files>— (verificación manual del flujo; no modifica archivos)</files>

  <action>
    **Esta task es verificación humana, no implementación.** El agente hace tres cosas:

    1. **Confirma el pre-requisito:** el código de `05-01` y `05-02` está deployado a prod (`gestion.forjo.studio`)
       y el checkpoint (Task 1) está cerrado. Sin las dos cosas no hay UAT.
    2. **Presenta el guion de `<how-to-verify>` textualmente**, en orden (A → B → C → D). Los pasos marcados
       **BLOCKER** son de seguridad o de la trampa del milestone (linking): si alguno falla, la fase no cierra.
    3. **Espera el `<resume-signal>`** y registra en el SUMMARY: el resultado de cada bloque, y en particular el
       resultado del cross-test de linking (bloque C) en los **dos órdenes**, más el comportamiento del orden
       inverso raro (Pitfall 4) si el dueño lo prueba.

    El agente **no marca ningún criterio como cumplido por su cuenta**: AUTH-03/04/05 viven en GoTrue, en el
    provider de Google y en el Dashboard, que ningún test de esta fase puede tocar (H-06).
  </action>

  <what-built>
    El flujo completo de "entrar con Google": botón en login/register, canje `code`→sesión en el callback,
    destino `/dashboard` que rutea nuevo→onboarding / recurrente→dashboard, error propio a `/login?error=oauth`, y
    el auto-link de GoTrue que unifica el mismo email en una sola cuenta. Lo automatizado ya está verde
    (`lib/auth/callback.test.ts`, `tsc`, `eslint`, `npm run build`), pero el valor vive en **GoTrue + el provider
    + el navegador**, no testeables sin red ni credenciales (H-06). Esto se prueba a mano o no se prueba.
  </what-built>

  <read_first>
    - `.planning/workstreams/onboarding/ROADMAP.md` §"Phase 5" → los 4 criterios de éxito. Son lo que este UAT verifica.
    - `.planning/workstreams/onboarding/phases/05-entrar-con-google/05-RESEARCH.md` §"Hallazgo #1" (mecánica del linking: auto-link por email verificado, orden inverso ofuscado), §"Pitfall 4" (el orden inverso raro), §"Pitfall 6" (slug-collision preexistente, NO de esta fase), §"#5" (UAT prod-first).
    - `.planning/workstreams/onboarding/phases/05-entrar-con-google/05-CONTEXT.md` — D-01, D-04 (vínculo silencioso), D-10.
  </read_first>

  <how-to-verify>
    **Todo esto es en prod: `https://gestion.forjo.studio`.** Usá una **cuenta Google descartable** (no una de las
    3 cuentas reales de prod, H-01/D-10). Para el cross-test vas a necesitar **una dirección de email que puedas
    usar tanto con Google como con contraseña** (ej. una cuenta Gmail descartable: sirve para login con Google
    **y** para registrarte con contraseña con el mismo address).

    ### A) Alta con Google — AUTH-03 / criterio 1
    1. Deslogueado, entrá a `/register`. **Esperado:** debajo del form, el divisor "o" y el botón **"Continuar con
       Google"** con el glifo oficial. Verificá que en `/login` se vea el **mismo** botón, consistente.
    2. Clickeá "Continuar con Google" y elegí la cuenta descartable.
       - **Esperado:** volvés autenticado, **sin** haber inventado ninguna contraseña. **Mirá la barra de
         direcciones: no debe quedar ningún `code` colgado** — el callback lo canjeó y redirigió a una URL limpia
         (T-05-04). Si el `code` está en la barra, es un **BLOCKER de seguridad**.

    ### B) Nuevo → onboarding, recurrente → panel — AUTH-04 / criterios 2 y 3
    3. Como es tu primera vez con esa cuenta Google (sin negocio), **esperado:** caés en **`/onboarding`** (el
       destino `oauth` es `/dashboard`, y el layout te rebota a onboarding porque no tenés negocio — D-09). Creá el
       negocio y terminá el wizard → caés en el **panel**.
       - **Nota (Pitfall 6, NO es bug de esta fase):** el onboarding tiene un bug preexistente de slug-collision
         bajo RLS (memoria `onboarding-slug-collision-rls`). Si el insert del negocio falla por un slug repetido,
         **anotalo como pre-existente** — un usuario Google usa el mismo carril de onboarding, no lo introduce
         Phase 5.
    4. Deslogueate y volvé a entrar con **"Continuar con Google"** con la misma cuenta.
       - **Esperado (criterio 3):** entrás **directo al panel**, sin pasar de nuevo por el onboarding. La segunda
         vez es un **login**, no un alta duplicada.

    ### C) Cross-test de account linking — AUTH-05 / criterio 4 — LA TRAMPA DEL MILESTONE (BLOCKERS)
    Este es el bloque que puede morder a un cliente que ya paga. Se prueba en los **dos órdenes**.

    5. **Orden 1 — contraseña primero, después Google (el camino feliz de D-01):**
       - Con un **email nuevo descartable**, registrate en `/register` con **email + contraseña** y confirmá la
         cuenta (mail de confirmación). Creá el negocio en el onboarding. Anotá el nombre/slug del negocio.
       - Deslogueate. Ahora entrá con **"Continuar con Google"** usando **el mismo email**.
       - **Esperado (BLOCKER):** caés en **tu cuenta existente** — mismo negocio, mismos datos, **sin** duplicar la
         cuenta y **sin** error opaco. Es el auto-link de GoTrue (mismo `user.id`). El vínculo es **silencioso**
         (D-04): no ves ningún mensaje sobre "identidades vinculadas", simplemente caés en tu panel.
       - Verificá que después podés entrar **de las dos formas** (contraseña **y** Google) y siempre caés al mismo
         negocio.
    6. **Orden 2 — Google primero, después intentar contraseña (el borde de UX, Pitfall 4):**
       - Con **otro** email descartable, entrá **primero con Google** y creá el negocio.
       - Deslogueate y andá a `/register` a intentar registrarte con **el mismo email + una contraseña**.
       - **Esperado (comportamiento documentado, NO un bug de código):** por el anti-enumeration que cerró Phase 4
         (T-04-17), `/register` muestra la **misma** pantalla "Revisá tu mail" que para cualquier alta — GoTrue
         **no** setea una contraseña sobre una cuenta OAuth existente ni manda mail. **El camino correcto para ese
         usuario es entrar con el botón de Google** (que siempre funciona). Anotá en el SUMMARY qué viste: es una
         **limitación conocida** (riesgo aceptado T-05-09), no un fallo. **No** se toca el anti-enumeration para
         "arreglarlo".

    ### D) Error / cancelación de Google — D-05 / criterio 4 (BLOCKER)
    7. Clickeá "Continuar con Google" y en la pantalla de consent de Google **cancelá** (o cerrá sin elegir cuenta).
       - **Esperado (BLOCKER):** volvés a **`/login`** con el aviso **"No pudimos entrar con Google. Probá de
         nuevo, o entrá con tu email y contraseña."** **NUNCA** a `/forgot-password` con un mensaje de "link
         inválido" (ese sería el error opaco que D-05 prohíbe), ni a una pantalla muerta.
  </how-to-verify>

  <acceptance_criteria>
    - **Criterio 1 (AUTH-03):** una persona sin cuenta entra con "Continuar con Google" y queda autenticada sin inventar contraseña. El `code` **no** queda en la barra de direcciones (T-05-04, BLOCKER).
    - **Criterio 2 (AUTH-04):** el usuario nuevo cae en `/onboarding`, crea su negocio y llega al panel.
    - **Criterio 3 (AUTH-04):** al volver más tarde, el mismo usuario Google entra directo al panel — login, no alta duplicada.
    - **Criterio 4 (AUTH-05, BLOCKER):** el mismo email con contraseña que entra después por Google cae en su **cuenta existente** con su negocio intacto — nunca duplicada, nunca error opaco. El vínculo es silencioso (D-04). Verificado en el **orden 1**; el **orden 2** (Pitfall 4) quedó documentado como limitación conocida (T-05-09).
    - **D-05 (BLOCKER):** la cancelación del consent cae en `/login?error=oauth` con el mensaje claro, nunca en `/forgot-password` ni en una pantalla muerta.
    - **D-10:** el UAT se hizo en prod con cuentas descartables, no con las 3 reales.
    - El resultado del cross-test en los dos órdenes quedó registrado en el SUMMARY.
  </acceptance_criteria>

  <resume-signal>Escribí "aprobado" si los 4 criterios del ROADMAP y los blockers de seguridad/linking pasan. Si algo falla, describí qué pantalla viste y en qué paso — con eso se arma el gap.</resume-signal>

  <done>Los 4 criterios de éxito del ROADMAP verificados a mano en prod con cuentas descartables, incluido el cross-test de linking en los dos órdenes y la cancelación del consent. La limitación del orden inverso (Pitfall 4) quedó documentada como riesgo aceptado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Cloud → Supabase Auth | El redirect URI autoriza el round-trip; el client secret vive en el Dashboard, nunca en git. |
| Dashboard de Supabase → runtime | El provider habilitado, la allowlist de Redirect URLs y el gate de confirm-email gobiernan la seguridad del flujo. No están en git. |
| Cuenta de contraseña ↔ identidad Google (mismo email) | El auto-link de GoTrue las unifica; el gate es el email verificado. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing / EoP | Pre-account takeover por auto-link sobre email no verificado | mitigate | GoTrue solo auto-vincula sobre email **verificado** [CITED: supabase.com/docs/guides/auth/auth-identity-linking]; confirm-email ON en prod (H-01/D-02, cerrado en Phase 4 como T-04-18) garantiza que toda cuenta de contraseña ya está verificada. El checkpoint **re-verifica** que "Confirm email" siga ON. No se toca `enable_manual_linking`. |
| T-05-02 | Tampering | Open redirect vía la allowlist del Dashboard | mitigate | Redirect URLs = `https://gestion.forjo.studio/auth/callback`, **sin wildcard de previews** (D-06). El checkpoint exige **listar las entradas preexistentes**: una vieja con `*` sería el agujero. Refuerzo: el `redirectTo` del botón es el callback fijo (05-02) y el destino final sale de `DESTINATIONS` (05-01). |
| T-05-08 | Spoofing | UAT en prod contra cuentas reales | accept (proceso) | El UAT y el cross-test de linking se hacen con **cuentas Google descartables**, no con las 3 reales de prod (D-10, espejo de T-04-22). Control de proceso, sin superficie de código. |
| T-05-09 | Repudiation / UX | Orden inverso: register con contraseña sobre una cuenta solo-Google queda en un dead-end suave | accept | GoTrue ofusca la existencia (anti-enumeration, T-04-17): no setea contraseña ni manda mail. El camino correcto es entrar con Google. Agregar copy tipo "¿te registraste con Google?" reintroduciría el oráculo de enumeration que Phase 4 cerró → **no se toca**. Limitación conocida documentada en el UAT (Pitfall 4). La vía real de agregar contraseña a una cuenta OAuth (`updateUser({ password })` logueado) queda fuera de scope. |
</threat_model>

<verification>
- Checkpoint humano cerrado con el reporte de (a) A2, (b) entradas preexistentes de Redirect URLs, (c) "Confirm email" ON.
- UAT: los 4 criterios del ROADMAP + los blockers (code en la barra, linking orden 1, cancelación del consent), en prod con cuentas descartables.
- **Gate de fase:** `npm test` + `npx tsc --noEmit` verdes antes de `/gsd:verify-work`, y **`/gsd:secure-phase` es obligatorio** — es la superficie de autenticación + account linking (ROADMAP §Threat note). Verificar T-05-01..09 y que T-04-01/-02/-04 sigan en pie sobre la rama OAuth.
</verification>

<success_criteria>
- AUTH-03: alta y login con Google funcionan end-to-end en prod.
- AUTH-04: el nuevo cae en onboarding y crea su negocio; el recurrente entra directo al panel.
- AUTH-05: el mismo email termina en una sola cuenta con su negocio intacto (orden 1 verificado; orden 2 documentado).
- D-05: el fallo/cancelación de Google cae en `/login?error=oauth`, nunca en una pantalla muerta.
- D-06/D-10: allowlist sin wildcard; UAT prod-first con cuentas descartables.
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/05-entrar-con-google/05-03-SUMMARY.md` when done
</output>
