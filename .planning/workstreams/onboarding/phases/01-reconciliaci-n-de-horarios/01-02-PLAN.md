---
phase: 01-reconciliaci-n-de-horarios
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/agent-context.ts
  - app/api/agent/context/route.ts
  - lib/agent-context.test.ts
autonomous: true
requirements: [SCHED-02]
must_haves:
  truths:
    - "El endpoint del agente (/api/agent/context) deriva los horarios de time_blocks, no de business_hours"
    - "El agente de WhatsApp muestra los mismos horarios que el panel/booking (una sola fuente: time_blocks)"
    - "El shape del HANDOFF hacia el bot (hours[] = 7 días con ranges HH:MM) se preserva idéntico"
    - "Múltiples bloques del mismo día se listan como múltiples ranges (horario partido)"
  prohibitions:
    - "Ningún path del agente lee business_hours tras el cambio"
    - "No cambia el contrato de salida del endpoint (el bot no requiere cambios)"
    - "No se toca el motor de agenda (availability / book_slot_atomic)"
  artifacts:
    - path: "lib/agent-context.ts"
      provides: "Mapeo puro de time_blocks → hours[] del HANDOFF (7 días con ranges)"
      contains: "day_of_week"
    - path: "app/api/agent/context/route.ts"
      provides: "Endpoint que lee time_blocks (no business_hours) y devuelve hours[]"
      contains: "from('time_blocks')"
  key_links:
    - from: "app/api/agent/context/route.ts"
      to: "time_blocks (tabla)"
      via: "supabase.from('time_blocks').select(...).eq('business_id', ...)"
      pattern: "from\\('time_blocks'\\)"
    - from: "app/api/agent/context/route.ts"
      to: "lib/agent-context.ts"
      via: "mapa puro de filas time_blocks → hours[] del HANDOFF"
      pattern: "map(TimeBlocks|Hours|BusinessHours)"
---

<objective>
Migrar el ÚNICO lector de `business_hours` que queda vivo en el repo — el endpoint del agente de WhatsApp
(`app/api/agent/context/route.ts`) y su mapeo puro (`lib/agent-context.ts`) — para que derive los horarios
de `time_blocks` en vez de `business_hours` (D-05). Con eso, el agente muestra exactamente los mismos
horarios que el panel/booking/landing: una sola fuente de verdad (SCHED-02). El contrato de salida hacia
el bot (HANDOFF: `hours[]` = 7 días, cada uno con `ranges: [{open, close}]` en HH:MM) se preserva
idéntico — el bot NO requiere cambios.

Nota de estado real (verificado por grep repo-wide): la **landing** (`lib/landing/derive.ts` +
`components/landing/hours.tsx`) YA lee `time_blocks` (migrada en el milestone web-builder, Phase 7). Los
3 lectores que el CONTEXT asumía sobre `business_hours` son en realidad **1**: el agente. Este plan cubre
ese único lector vivo; la landing no se toca (ya está en la fuente correcta).

Purpose: eliminar la última divergencia de datos (agente leyendo la tabla vieja) antes del DROP de
`business_hours` (Plan 03). Implementa D-05 del CONTEXT para el lector del agente.

Output: `agent-context.ts` con un mapeo `time_blocks → hours[]`, `agent/context/route.ts` leyendo
`time_blocks`, y el test actualizado a la nueva entrada.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/onboarding/ROADMAP.md
@.planning/workstreams/onboarding/STATE.md
@.planning/workstreams/onboarding/phases/01-reconciliaci-n-de-horarios/01-CONTEXT.md

# Fuente de verdad de esta migración (leer antes de tocar):
@lib/agent-context.ts
@app/api/agent/context/route.ts
@lib/agent-context.test.ts
@lib/landing/derive.ts
@lib/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescribir el mapeo puro para agrupar time_blocks en el shape del HANDOFF</name>
  <files>lib/agent-context.ts, lib/agent-context.test.ts</files>
  <read_first>
    - lib/agent-context.ts — `mapBusinessHours` (~41-50), `BusinessHourRow` (~27-33), tipos `HoursRange`
      / `DayHours` (~17-25), y el array `DAYS` (~7-15). El shape de SALIDA (DayHours[] = 7 días con
      ranges [{open, close}] en HH:MM) es el HANDOFF hacia el bot y NO cambia.
    - lib/landing/derive.ts — `groupHoursByDay` (~51-59): patrón de referencia para agrupar filas de
      time_blocks por `day_of_week` acumulando rangos, y el slice(0,5) de "HH:MM:SS" → "HH:MM".
    - lib/types.ts — `interface TimeBlock` (~81): las columnas de la fila de entrada (day_of_week,
      start_time, end_time). En time_blocks NO existe `is_open` (un bloque presente = abierto; ausencia
      de bloques para un día = cerrado).
    - lib/agent-context.test.ts — los tests de `mapBusinessHours` que hay que reescribir a la nueva
      entrada (ver Task siguiente); el shape esperado de salida se mantiene.
  </read_first>
  <behavior>
    - Test: entrada vacía [] → devuelve 7 entradas (domingo..sábado) con `ranges: []`.
    - Test: dos bloques del mismo día (day_of_week=1, 09:00:00–13:00:00 y 17:00:00–21:00:00) → el lunes
      tiene `ranges: [{open:'09:00',close:'13:00'},{open:'17:00',close:'21:00'}]` (horario partido).
    - Test: los tiempos "HH:MM:SS" se recortan a "HH:MM".
    - Test: días sin bloques quedan con `ranges: []` (cerrado). No hay `is_open` en la entrada.
    - Test: input null/undefined → 7 días con `ranges: []` (tolerante).
  </behavior>
  <action>
    Reemplazar `mapBusinessHours(rows: BusinessHourRow[])` por una función que mapee filas de
    `time_blocks` al MISMO shape de salida `DayHours[]` (7 días, ranges [{open, close}] en HH:MM).
    Renombrar la función y el tipo de fila de entrada a algo alineado con la fuente (ej. la fila de
    entrada pasa a tener `day_of_week`, `start_time`, `end_time` en vez de `open_time`/`close_time`/`is_open`).
    Agrupar por `day_of_week` acumulando cada bloque como un range { open: start_time.slice(0,5),
    close: end_time.slice(0,5) } (patrón de `groupHoursByDay` de derive.ts). Sin `is_open`: cada fila
    presente es un tramo abierto; un día sin filas queda con `ranges: []`. Mantener el orden de salida
    domingo..sábado (índice 0..6) que ya usa el array `DAYS`, y la tolerancia a null/undefined. El módulo
    sigue siendo PURO (sin Supabase, sin React), named exports. `mapServices` NO se toca.
    Eliminar del módulo el tipo `BusinessHourRow` y toda referencia textual a `business_hours` (comentarios
    incluidos) — el string no debe quedar en el archivo.
  </action>
  <verify>
    <automated>npx vitest run lib/agent-context.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: grep de `business_hours` y `BusinessHourRow` en `lib/agent-context.ts` → 0 referencias.
    - source: la función de mapeo de horarios acepta filas con `day_of_week`/`start_time`/`end_time` y
      devuelve `DayHours[]` (7 días, ranges [{open,close}] HH:MM).
    - behavior: dos bloques del mismo día → dos ranges en ese día (horario partido).
    - test: `npx vitest run lib/agent-context.test.ts` pasa (RED→GREEN sobre la nueva entrada).
  </acceptance_criteria>
  <done>El mapeo del agente deriva horarios de time_blocks al shape del HANDOFF sin perder el contrato de salida; el módulo no menciona business_hours.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Endpoint del agente lee time_blocks + actualizar el test a la nueva entrada</name>
  <files>app/api/agent/context/route.ts, lib/agent-context.test.ts</files>
  <read_first>
    - app/api/agent/context/route.ts — el `Promise.all` (~28-38) que hoy hace
      `supabase.from('business_hours').select('day_of_week, open_time, close_time, is_open')` y luego
      `hours: mapBusinessHours(hours)` (~53). Ese select y esa llamada se migran a time_blocks + la nueva
      función de Task 1.
    - app/api/booking/availability/route.ts — referencia (NO tocar) de cómo el motor lee time_blocks
      (columnas day_of_week/start_time/end_time por business_id). Confirmar los nombres de columna.
    - lib/agent-context.test.ts — el describe `mapBusinessHours` (~8-48): reescribir las entradas de test
      a filas de time_blocks (day_of_week/start_time/end_time, sin is_open) y renombrar a la nueva función.
      Las aserciones de salida (7 días, ranges HH:MM, partido) se conservan.
  </read_first>
  <behavior>
    - Test: el describe de horarios usa filas de time_blocks (day_of_week/start_time/end_time) como
      entrada, sin `is_open`, y las aserciones de salida (7 días, ranges HH:MM, partido, null-tolerante)
      quedan verdes.
  </behavior>
  <action>
    En `app/api/agent/context/route.ts`: cambiar el select de `business_hours` por
    `supabase.from('time_blocks').select('day_of_week, start_time, end_time').eq('business_id', business.id)`
    (mismas columnas que lee el motor; confirmar contra availability). Reemplazar la llamada
    `mapBusinessHours(hours)` por la nueva función de mapeo de Task 1 sobre esas filas. El resto del
    endpoint queda igual: service-role por slug, force-dynamic, columnas no secretas explícitas, el shape
    de respuesta (`business`/`services`/`hours`/`notes`) y los headers no-store no cambian → el bot NO
    requiere cambios (T-06-08 accept ya vigente: solo devuelve datos públicos del negocio).
    En `lib/agent-context.test.ts`: reescribir las entradas del describe de horarios a filas de time_blocks
    (day_of_week/start_time/end_time, sin is_open), renombrar la función bajo test, y conservar las
    aserciones de salida (incluida la de horario partido y la de null-tolerancia). Quitar el caso
    "ignora filas con is_open=false" (ya no existe is_open) y reemplazarlo por un caso equivalente de la
    nueva semántica (día sin bloques → ranges:[]). Eliminar cualquier mención textual a `business_hours`
    en el test. `mapServices` y sus tests NO se tocan.
  </action>
  <verify>
    <automated>npx vitest run lib/agent-context.test.ts && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - source: grep de `business_hours` en `app/api/agent/context/route.ts` y `lib/agent-context.test.ts` → 0 referencias.
    - source: el endpoint hace `from('time_blocks').select('day_of_week, start_time, end_time').eq('business_id', ...)`.
    - behavior: el contrato de salida del endpoint (`hours[]` = 7 días con ranges HH:MM) es idéntico al de antes.
    - test: `npx vitest run lib/agent-context.test.ts` pasa; `npx tsc --noEmit` y `npm run lint` sin errores.
  </acceptance_criteria>
  <done>El endpoint del agente lee time_blocks y devuelve el mismo HANDOFF; el test cubre la nueva entrada; 0 referencias vivas a business_hours en el agente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot (anon, por slug) → endpoint /api/agent/context | El bot lee datos públicos del negocio por slug; el endpoint corre service-role y resuelve el tenant por slug. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | endpoint del agente (service-role) | mitigate | Se mantiene el patrón vigente: select de columnas NO secretas explícitas y tenant resuelto por slug; time_blocks solo aporta day_of_week/start_time/end_time (no secretos). Igual superficie que hoy (T-06-08 accept). |
| T-01-05 | Tampering | mapeo puro time_blocks → hours[] | mitigate | El módulo es puro (sin Supabase/React) y solo transforma filas ya filtradas por business_id en el route; no acepta ids del cliente. |
| T-01-06 | Information Disclosure | cambio de fuente business_hours→time_blocks | accept | time_blocks ya está bajo RLS por business_id y ya es leída por el booking público; no expone datos nuevos ni cruza tenants. |

Ninguna amenaza de este plan alcanza severidad **high**: es riesgo de **regresión** (que el agente deje
de mostrar horarios o cambie el contrato del HANDOFF), no de aislamiento. El endpoint mantiene su
superficie de datos públicos vigente; solo cambia de qué tabla derivan los mismos horarios.
</threat_model>

<verification>
- `npx vitest run lib/agent-context.test.ts` verde (nueva entrada time_blocks, incluido horario partido).
- `npx tsc --noEmit` y `npm run lint` sin errores.
- grep de `business_hours` en `lib/agent-context.ts`, `app/api/agent/context/route.ts`,
  `lib/agent-context.test.ts` → 0 referencias.
- No-regresión: el contrato de salida del endpoint (hours[] del HANDOFF) es idéntico; la landing (ya en
  time_blocks) y el motor (availability/book_slot_atomic) NO se tocan.
</verification>

<success_criteria>
- El agente deriva sus horarios de `time_blocks` (misma fuente que panel/booking/landing).
- El contrato del HANDOFF hacia el bot no cambia.
- 0 referencias vivas a `business_hours` en los archivos del agente.
- Tras este plan + Plan 01, un grep repo-wide de `business_hours` en `app/`, `lib/`, `components/` deja
  0 lectores/escritores vivos (queda solo el esquema/DB, que dropea el Plan 03).
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/01-reconciliaci-n-de-horarios/01-02-SUMMARY.md` when done
</output>
