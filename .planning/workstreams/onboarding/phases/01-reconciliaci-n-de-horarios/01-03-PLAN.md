---
phase: 01-reconciliaci-n-de-horarios
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - supabase/migrations/045_drop_business_hours.sql
  - supabase/schema.sql
  - lib/types.ts
autonomous: false
user_setup:
  - service: supabase
    why: "Aplicar la migración 045 (DROP business_hours) a la base de prod a mano, coordinado con el deploy del código que ya no lee la tabla (Plans 01+02)"
    dashboard_config:
      - task: "Correr 045_drop_business_hours.sql en el SQL editor de Supabase (prod) DESPUÉS de deployar el código de Plans 01+02"
        location: "Supabase Dashboard → SQL editor (prod)"
requirements: [SCHED-02]
must_haves:
  truths:
    - "La tabla business_hours y la vista public_business_hours dejan de existir tras la migración"
    - "supabase db reset local replaya la migración 045 sin error (con los readers ya migrados)"
    - "supabase/schema.sql no contiene la tabla business_hours, la vista public_business_hours, su policy ni sus grants"
    - "El tipo BusinessHour ya no existe en lib/types.ts"
    - "No queda divergencia: business_hours dropeada, 0 referencias vivas en el repo"
  prohibitions:
    - "El DROP no corre antes de que Plans 01+02 hayan migrado los readers/writers (grep repo-wide de business_hours en app/lib/components debe dar 0 referencias vivas)"
    - "No se toca time_blocks ni el motor (availability / book_slot_atomic)"
    - "No se hace backfill de datos business_hours → time_blocks (cutover limpio, D-02)"
  artifacts:
    - path: "supabase/migrations/045_drop_business_hours.sql"
      provides: "DROP idempotente de la vista public_business_hours, la policy, la tabla business_hours y sus grants"
      contains: "DROP TABLE"
    - path: "lib/types.ts"
      provides: "types sin BusinessHour"
  key_links:
    - from: "supabase/migrations/045_drop_business_hours.sql"
      to: "supabase/schema.sql"
      via: "el schema regenerado refleja la ausencia de business_hours / public_business_hours"
      pattern: "business_hours"
---

<objective>
Cerrar la reconciliación eliminando la tabla perdedora: `DROP` de `business_hours` (fold de
SCHED-DROP-01 a esta fase, D-03) — SIN backfill de datos (cutover limpio, D-02; no hay clientes).
Escribir la migración numerada `045_drop_business_hours.sql` (próximo número libre EN ESTA RAMA
`gsd/onboarding` — verificado: la secuencia llega hasta 044, 045 está libre; el 045 de
`gsd/motor-reservas` vive en otra rama y NO está acá), regenerar `supabase/schema.sql`, validar con
`supabase db reset` local, y quitar el tipo `BusinessHour` de `lib/types.ts`. Aplicación a prod = MANUAL,
coordinada con el deploy del código de Plans 01+02.

**Orden obligatorio (D-03):** este plan es Wave 2 — depende de Plans 01 y 02. El DROP solo es seguro
una vez que el onboarding (Plan 01) ya no ESCRIBE business_hours y el agente (Plan 02) ya no la LEE.
Antes de escribir la migración, un grep repo-wide de `business_hours` en `app/`, `lib/`, `components/`
debe dar 0 referencias vivas (código); solo pueden quedar referencias en `supabase/` (esquema/migraciones),
que son justamente lo que este plan limpia.

Purpose: eliminar la divergencia de raíz (una sola tabla de horarios en el esquema) y saldar SCHED-02
(cero divergencia entre business_hours y time_blocks — al no existir business_hours, no hay divergencia
posible). Implementa D-01 (business_hours se elimina) + D-03 (DROP + limpieza de esquema/tipos).

Output: migración 045, `schema.sql` regenerado sin business_hours/public_business_hours, `lib/types.ts`
sin `BusinessHour`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/onboarding/ROADMAP.md
@.planning/workstreams/onboarding/STATE.md
@.planning/workstreams/onboarding/phases/01-reconciliaci-n-de-horarios/01-CONTEXT.md

# Fuente de verdad del esquema a dropear (leer antes de tocar):
@supabase/migrations/README.md
@supabase/migrations/044_public_canchas.sql
@lib/types.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Escribir la migración 045_drop_business_hours.sql + validar con db reset</name>
  <files>supabase/migrations/045_drop_business_hours.sql</files>
  <read_first>
    - supabase/migrations/README.md — convención: migraciones numeradas idempotentes, "Nada destructivo"
      salvo excepción marcada con ⚠. Esta migración ES la excepción destructiva (un DROP TABLE), así que
      documentarla con un header ⚠ explicando por qué es segura (cutover limpio, sin data que preservar,
      readers ya migrados en Plans 01+02).
    - supabase/schema.sql — DDL a eliminar del esquema (referencia de lo que existe hoy): la tabla
      `business_hours` (~307-317), la vista `public_business_hours` (~685-695), la policy
      "business member access" ON business_hours (~1525) y ENABLE ROW LEVEL SECURITY (~1549), la FK
      `business_hours_business_id_fkey` (~1198-1199), el PK (~883-884) y los GRANT de la tabla (~3288-3290)
      y de la vista (~3414-3416). La vista DEPENDE de la tabla → dropear la vista ANTES que la tabla (o
      usar DROP TABLE ... CASCADE).
    - supabase/migrations/044_public_canchas.sql — molde de estilo/comentarios de una migración del repo
      (header en español explicando contexto + qué hace + reglas).
    - IMPORTANTE (gate de orden): antes de escribir el archivo, verificar con grep repo-wide que
      `business_hours` NO aparezca en `app/`, `lib/`, `components/` (0 referencias vivas). Si aparece,
      DETENERSE: Plans 01/02 no están completos y el DROP rompería ese path.
  </read_first>
  <action>
    Crear `supabase/migrations/045_drop_business_hours.sql`. Header en español (molde de 044) que explique:
    contexto (reconciliación de horarios v0.14, time_blocks es la fuente única — D-01), por qué es seguro
    el DROP (cutover limpio sin backfill — D-02, no hay clientes; los 2 readers/writers vivos ya migraron a
    time_blocks en Plans 01/02), y que es la ÚNICA migración destructiva del repo (marcarla con ⚠).
    La migración, idempotente, debe: (1) `DROP VIEW IF EXISTS public.public_business_hours;` (la vista
    depende de la tabla → primero la vista); (2) `DROP POLICY IF EXISTS "business member access" ON public.business_hours;`
    (defensivo, el DROP TABLE se lleva las policies igual); (3) `DROP TABLE IF EXISTS public.business_hours;`
    (esto elimina tabla, PK, FK, RLS y grants asociados). Confirmar contra schema.sql los nombres EXACTOS
    de la vista y la policy antes de escribir. NO tocar time_blocks. NO hacer backfill.
    Validar localmente con `supabase db reset` (replaya baseline + 040..045): debe correr sin error. Si el
    entorno Supabase local no está levantado en la sesión de ejecución, marcar el `db reset` como
    verificación diferida a correr por el operador (es parte del user_setup de aplicación manual), pero
    dejar la migración escrita y revisada.
  </action>
  <verify>
    <automated>test -f "supabase/migrations/045_drop_business_hours.sql" && grep -qi "DROP TABLE IF EXISTS \"\?public\"\?\.\"\?business_hours" supabase/migrations/045_drop_business_hours.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - source: existe `supabase/migrations/045_drop_business_hours.sql` con `DROP VIEW IF EXISTS ... public_business_hours`,
      `DROP POLICY IF EXISTS ... ON ... business_hours` y `DROP TABLE IF EXISTS ... business_hours`, en ese orden.
    - CLI (operador / entorno local): `supabase db reset` replaya baseline + 040..045 sin error.
    - behavior: la migración es idempotente (correrla dos veces no rompe: todos los DROP usan IF EXISTS).
    - gate: antes de escribir, grep de `business_hours` en `app/`, `lib/`, `components/` → 0 referencias
      vivas (Plans 01+02 completos); si no, detenerse.
  </acceptance_criteria>
  <done>Migración 045 escrita (DROP idempotente de vista + policy + tabla), validable con db reset local; sin backfill; time_blocks intacta.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Regenerar schema.sql sin business_hours + quitar el tipo BusinessHour de types.ts</name>
  <files>supabase/schema.sql, lib/types.ts</files>
  <read_first>
    - supabase/schema.sql — todas las apariciones de business_hours / public_business_hours a eliminar
      del snapshot (tabla ~307-317, vista ~685-695, PK ~883-884, FK ~1198-1199, policy ~1525, RLS ~1549,
      grants tabla ~3288-3290, grants vista ~3414-3416). schema.sql es un snapshot regenerado del estado
      post-migraciones; tras el DROP no debe contener ninguno de esos bloques.
    - lib/types.ts — `interface BusinessHour` (~168-175): el tipo a eliminar. Verificar (grep) que NO
      quede importado/usado en ningún módulo tras Plans 01/02 (el único uso era el mapeo del agente y el
      onboarding, ya migrados). El tipo `TimeBlock` (~81) NO se toca.
  </read_first>
  <action>
    Regenerar `supabase/schema.sql` para reflejar el DROP: eliminar del snapshot el `CREATE TABLE business_hours`,
    su `ALTER TABLE ... OWNER`, la `CREATE OR REPLACE VIEW public_business_hours` (+ su OWNER), el PK
    `business_hours_pkey`, la FK `business_hours_business_id_fkey`, la policy "business member access" ON
    business_hours, el `ENABLE ROW LEVEL SECURITY` de la tabla, y todos los `GRANT ... ON TABLE business_hours`
    y `... public_business_hours`. Preferir regenerar el schema con la herramienta del repo (`supabase db dump`
    / el flujo con el que se generó el snapshot) tras el `db reset`; si eso no está disponible en la sesión,
    editar el snapshot quitando quirúrgicamente esos bloques (Edit parcial, sin reescribir el archivo entero).
    Verificar al final: grep de `business_hours` en `supabase/schema.sql` → 0 referencias.
    En `lib/types.ts`: eliminar la `interface BusinessHour` (Edit parcial del bloque ~168-175). NO tocar
    `TimeBlock` ni otros tipos. Confirmar con grep repo-wide que `BusinessHour` (como tipo) no queda
    importado en ningún archivo.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - source: grep de `business_hours` en `supabase/schema.sql` → 0 referencias (tabla, vista, policy,
      grants, PK y FK eliminados del snapshot).
    - source: grep de `interface BusinessHour` / `BusinessHour` (como tipo) en `lib/` y `app/` → 0 referencias.
    - source: `TimeBlock` sigue existiendo en `lib/types.ts` (no se tocó).
    - test: `npx tsc --noEmit` y `npm run lint` pasan sin errores.
  </acceptance_criteria>
  <done>schema.sql no contiene business_hours ni public_business_hours; el tipo BusinessHour fue removido; TimeBlock intacto; el proyecto compila.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint — aplicar el DROP a prod coordinado con el deploy</name>
  <action>Verificación humana: aplicar la migración 045 a la base de prod DESPUÉS de deployar el código de Plans 01+02, y correr los smoke checks. El executor NO corre el DROP en prod.</action>
  <what-built>
    Migración 045 (DROP business_hours), schema.sql regenerado sin la tabla/vista, y tipo BusinessHour
    removido. El código (Plans 01+02) ya no lee ni escribe business_hours. La aplicación del DROP a la
    base de PROD es manual y debe coordinarse con el deploy — no la hace el executor.
  </what-built>
  <how-to-verify>
    1. Verificar localmente (si tenés Supabase local levantado): `supabase db reset` corre sin error y
       replaya hasta 045.
    2. Confirmar orden de deploy: primero deployar el código de esta fase (onboarding + agente ya en
       time_blocks), DESPUÉS correr `045_drop_business_hours.sql` en el SQL editor de Supabase (prod).
       Si se dropea antes de deployar el código, el endpoint del agente que aún leyera business_hours en
       prod fallaría — por eso el orden importa.
    3. Correr `045_drop_business_hours.sql` en Supabase prod (SQL editor). Confirmar que `business_hours`
       y `public_business_hours` ya no existen (ej. querés un SELECT contra la tabla y da "does not exist").
    4. Smoke check post-deploy: la landing pública de un negocio muestra horarios (ya leía time_blocks),
       el endpoint `/api/agent/context?slug=...` devuelve `hours[]` sin error, y un alta nueva en el
       onboarding con horario partido aparece en el panel de agenda.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" cuando el DROP esté aplicado en prod y los smoke checks pasen, o describí el problema.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migración → DB (prod) | Un DROP TABLE destructivo sobre la base; debe aplicarse solo tras el deploy del código que ya no usa la tabla. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Denial of Service | DROP TABLE business_hours corriendo antes del deploy del código | mitigate | Orden obligatorio (D-03): Wave 2 depende de Plans 01+02; gate de grep repo-wide (0 referencias vivas) antes de escribir la migración; checkpoint humano exige deployar el código ANTES de correr el DROP en prod. |
| T-01-08 | Tampering | migración destructiva no idempotente | mitigate | Todos los DROP usan IF EXISTS (idempotente, molde del README); validada con `supabase db reset` local antes de prod. |
| T-01-09 | Information Disclosure | limpieza de policy/RLS/grants de business_hours | mitigate | `DROP TABLE` elimina policies, RLS y grants asociados; se dropea además explícitamente la vista `public_business_hours` (que tenía GRANT a anon) → no queda ninguna superficie colgada de la tabla eliminada. |
| T-01-10 | Repudiation | pérdida de datos por el DROP | accept | Cutover limpio sin backfill (D-02): no hay clientes en prod y el usuario confirmó "todo en 0 OK"; no hay datos de horarios que preservar. |

Ninguna amenaza de este plan alcanza severidad **high**: el riesgo es de **regresión** (dropear antes de
tiempo o dejar una referencia colgada), mitigado por el orden en waves, el gate de grep y el checkpoint de
deploy. No hay riesgo de aislamiento: se ELIMINA una tabla, no se expone ninguna; la vista pública
`public_business_hours` (única superficie anon) se dropea explícitamente.
</threat_model>

<verification>
- Antes de escribir la migración: grep repo-wide de `business_hours` en `app/`, `lib/`, `components/` → 0
  referencias vivas (Plans 01+02 completos).
- `supabase db reset` local replaya baseline + 040..045 sin error (o diferido al operador si no hay local).
- grep de `business_hours` en `supabase/schema.sql` → 0 referencias tras regenerar.
- grep de `BusinessHour` (tipo) en `lib/` y `app/` → 0 referencias.
- `npx tsc --noEmit` y `npm run lint` sin errores.
- No-regresión: time_blocks y el motor (availability/book_slot_atomic) NO se tocan.
</verification>

<success_criteria>
- La tabla `business_hours` y la vista `public_business_hours` dejan de existir (migración 045 + schema regenerado).
- 0 referencias vivas a `business_hours` en el repo (código); solo la migración 045 la menciona, para dropearla.
- El tipo `BusinessHour` fue removido; `TimeBlock` intacto.
- La aplicación a prod queda documentada como manual, coordinada con el deploy (user_setup + checkpoint).
</success_criteria>

<output>
Create `.planning/workstreams/onboarding/phases/01-reconciliaci-n-de-horarios/01-03-SUMMARY.md` when done
</output>
