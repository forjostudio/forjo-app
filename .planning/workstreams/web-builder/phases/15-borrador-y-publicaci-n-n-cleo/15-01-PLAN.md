---
phase: 15-borrador-y-publicaci-n-n-cleo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/050_landing_draft.sql
  - test/isolation.test.ts
autonomous: true
requirements: [PUB-03, PUB-07, PUB-08]
user_setup: []

must_haves:
  truths:
    - "Un negocio que ya tenía su landing al aire la sigue viendo idéntica después de la migración (SC5)"
    - "Ese mismo negocio abre el editor y su borrador es una copia fiel de lo publicado (SC5)"
    - "Un negocio que nunca publicó sigue viendo su página de reservas de siempre (SC5 / PUB-07)"
    - "Un visitante anónimo NO puede leer el borrador de ningún negocio (Core Value)"
    - "Un dueño NO puede leer ni escribir el borrador de otro negocio (Core Value)"
  artifacts:
    - path: "supabase/migrations/050_landing_draft.sql"
      provides: "Columna businesses.landing_draft jsonb (nullable, sin valor por defecto) + backfill idempotente desde landing_config"
      contains: "ADD COLUMN IF NOT EXISTS \"landing_draft\" jsonb"
    - path: "test/isolation.test.ts"
      provides: "4 casos nuevos de aislamiento del borrador (vista pública, cross-read, cross-write, same-tenant)"
      contains: "landing_draft"
  key_links:
    - from: "supabase/migrations/050_landing_draft.sql"
      to: "businesses (RLS policy 'owner access')"
      via: "la columna nueva hereda el aislamiento fila-a-fila; NO se agrega policy ni permiso nuevo"
      pattern: "ALTER TABLE \"public\"\\.\"businesses\""
    - from: "test/isolation.test.ts"
      to: "public_businesses (vista security-DEFINER)"
      via: "asierta que select('landing_draft') sobre la vista ERRA (la columna no está ahí)"
      pattern: "public_businesses"
---

<objective>
Crear la columna `businesses.landing_draft` (migración **050**, aditiva y no destructiva) y **probar**
—no asumir— que el borrador queda aislado: invisible para `anon` y para cualquier otro tenant.

Purpose: es la fundación de toda la fase y su único agujero posible. `landing_draft` es contenido del
tenant y vive en `businesses`, la tabla que sostiene el Core Value. Si la columna se cuela en la vista
pública `public_businesses` (que corre **security-DEFINER** y bypassa RLS a propósito), el borrador de
**todos** los negocios queda legible por cualquier visitante. La migración lo evita **por construcción**
—no tocando la vista— y este plan lo convierte en un test que se pone rojo si alguien lo rompe.

Output: `supabase/migrations/050_landing_draft.sql` validada con `supabase db reset` local (PG17), y
`test/isolation.test.ts` extendido con 4 casos nuevos **en VERDE, no skipeados**.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-builder/ROADMAP.md
@.planning/workstreams/web-builder/phases/15-borrador-y-publicaci-n-n-cleo/15-RESEARCH.md
@.planning/workstreams/web-builder/phases/15-borrador-y-publicaci-n-n-cleo/15-PATTERNS.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
</context>

## Artifacts this phase produces (parte de este plan)

| Artefacto | Tipo | Dónde |
|-----------|------|-------|
| `businesses.landing_draft` | columna `jsonb` (nullable, sin valor por defecto) | `supabase/migrations/050_landing_draft.sql` |
| `050_landing_draft.sql` | migración numerada (DDL + backfill idempotente) | `supabase/migrations/` |
| 4 casos de aislamiento del borrador | tests (vitest, anon-key) | `test/isolation.test.ts` |

> Los símbolos de código (`saveLandingDraft`, `publishLanding`, `discardLandingDraft`,
> `configsEqual`, `deriveEditorState`, token `--warning`, barra publish) los producen **15-02** y
> **15-03**. Este plan solo produce el esquema y su prueba de aislamiento.

<tasks>

<task type="auto">
  <name>Task 1: Escribir la migración 050 — businesses.landing_draft (aditiva, sin valor por defecto)</name>
  <files>supabase/migrations/050_landing_draft.sql</files>
  <read_first>
    - `supabase/migrations/049_clients_origin.sql` — **el molde exacto** del archivo: título de una línea, bloque `-- Contexto:`, `-- Qué hace:`, `-- Racional D-0x (lockeado):`, `-- Qué NO hace (invariantes del proyecto):` y recién después el SQL. Comentarios densos en español explicando el *porqué*. Su bloque "Qué NO hace" (incluida la línea que prohíbe el push remoto y exige `supabase db reset` local + aplicación a mano en prod + `NOTIFY pgrst`) se replica con el contenido propio de la 050.
    - `.planning/workstreams/web-builder/phases/15-borrador-y-publicaci-n-n-cleo/15-RESEARCH.md` §"Patrón 1" — el SQL de la 050 ya está redactado ahí, con su bloque de comentarios completo. **Copiarlo verbatim.**
    - `supabase/migrations/00000000000000_baseline.sql` — solo las secciones necesarias: la definición de la vista `public_businesses` (~línea 520), `ALTER TABLE businesses ENABLE ROW LEVEL SECURITY` (~1239), la policy `owner access` (~1310) y el trigger `businesses_protect_admin_columns` (~897). Confirmar antes de escribir que la vista lista columnas explícitas y que hay **una sola** policy sobre `businesses`.
    - `.planning/workstreams/web-builder/phases/15-borrador-y-publicaci-n-n-cleo/15-PATTERNS.md` §"050_landing_draft.sql" — qué del molde 049 se copia y qué NO (`045_landing_cms.sql` es un anti-molde: crea policies y grants; la 050 no crea ninguno).
  </read_first>
  <action>
    Crear `supabase/migrations/050_landing_draft.sql` (050 = primer número libre; 049 es la última aplicada). Dos sentencias, nada más:

    1. `ALTER TABLE "public"."businesses" ADD COLUMN IF NOT EXISTS "landing_draft" jsonb;` — nullable, **sin valor por defecto**. Pitfall 6 del RESEARCH: un `'{}'::jsonb` inicial rompería el fail-safe `parseLandingConfig(null) → null`, que es la señal semántica de "negocio legacy" y de lo que depende PUB-07 (la reserva simple). `NULL` acá significa algo; no es "falta un dato".
    2. Backfill idempotente: `UPDATE "public"."businesses" SET "landing_draft" = "landing_config" WHERE "landing_draft" IS NULL AND "landing_config" IS NOT NULL;` — esto ES PUB-08: el negocio con landing al aire abre el editor y ve una copia fiel de lo publicado; su web no se mueve (la columna publicada no se toca). El `WHERE landing_draft IS NULL` hace que correr la migración dos veces NO pise un borrador que el dueño ya empezó a editar.

    Bloque de comentarios de cabecera al estilo de la 049 (español, denso, explicando el porqué). El bloque "Qué NO hace" debe declarar explícitamente los cuatro invariantes: (a) no toca la vista `public_businesses` — es la única puerta de `anon` a esta tabla y corre security-DEFINER, meter el borrador ahí lo expondría a todos; (b) no crea ninguna regla de acceso ni permiso nuevo — `businesses` ya tiene RLS con la policy `owner access` (`owner_id = auth.uid()`) como única policy y la columna hereda ese aislamiento fila-a-fila; (c) no agrega `landing_draft` al trigger `businesses_protect_admin_columns` (ese trigger protege columnas administrativas; el dueño SÍ debe poder escribir su propio borrador); (d) validación autónoma = `supabase db reset` local, prod a mano coordinado con el deploy + `NOTIFY pgrst, 'reload schema';` + regenerar `supabase/schema.sql`.

    El archivo **no** contiene ninguna sentencia de vista, de policy, de permiso ni de tipo enum. Tampoco se edita ni renumera ninguna migración ajena.
  </action>
  <verify>
    <automated>grep -c 'ADD COLUMN IF NOT EXISTS "landing_draft" jsonb' supabase/migrations/050_landing_draft.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/050_landing_draft.sql` existe y `grep -c 'ADD COLUMN IF NOT EXISTS "landing_draft" jsonb' supabase/migrations/050_landing_draft.sql` sale **1**.
    - `grep -c 'SET "landing_draft" = "landing_config"' supabase/migrations/050_landing_draft.sql` sale **1**.
    - **SQL efectivo limpio** (excluyendo comentarios, que sí nombran los invariantes): `grep -v '^--' supabase/migrations/050_landing_draft.sql | grep -icE 'create (or replace )?view|create policy|drop policy|^ *grant|default|enum'` sale **0**.
    - `grep -v '^--' supabase/migrations/050_landing_draft.sql | grep -c 'public_businesses'` sale **0** (la vista no se toca; solo se la nombra en los comentarios).
    - `ls supabase/migrations/` NO muestra ningún archivo `05[1-9]_*` ni una 050 duplicada, y ningún archivo `04[0-9]_*` quedó modificado (`git diff --name-only supabase/migrations/` lista **solo** `050_landing_draft.sql` como archivo nuevo).
  </acceptance_criteria>
  <done>La migración 050 existe, es aditiva, idempotente, sin valor por defecto, y no toca vista/policy/permisos/trigger.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Validar la 050 con `supabase db reset` local + probar que la vista pública NO quedó contaminada</name>
  <files>supabase/migrations/050_landing_draft.sql</files>
  <read_first>
    - `supabase/config.toml` — confirmar la config del Supabase local (puertos; analytics/studio/storage apagados en Windows).
    - `.planning/workstreams/web-builder/phases/15-borrador-y-publicaci-n-n-cleo/15-RESEARCH.md` §"Migración 050" (bloque "Verificación local (obligatoria…)") y §"Orden de deploy".
    - `supabase/migrations/README.md` — reglas de migraciones numeradas (idempotencia, nada destructivo, correr en orden).
  </read_first>
  <action>
    Validación obligatoria y **bloqueante** de la migración contra Postgres 17 local. Docker tiene que estar corriendo.

    1. `npx supabase start` (si no está levantado) y luego `npx supabase db reset` — replaya el baseline numerado + 040..050 en orden. Si el reset falla, la 050 está mal: arreglarla y repetir. **No se avanza con el reset en rojo.**
    2. Probar la ausencia de fuga en la vista, contra la base recién reseteada: consultar `information_schema.columns` filtrando por `table_name = 'public_businesses'` y `column_name = 'landing_draft'` (el SQL exacto está en el RESEARCH §"Verificación de que la vista NO se contaminó"). **Debe devolver 0 filas.** Si devuelve 1, hay fuga: la migración tocó la vista → volver a la Task 1.
    3. Probar que la columna sí existe en la tabla: la misma consulta con `table_name = 'businesses'` debe devolver **1** fila con `data_type = 'jsonb'` y `is_nullable = 'YES'` y `column_default` vacío.

    **Está PROHIBIDO empujar la migración al Supabase remoto desde esta tarea.** El comando de push remoto de la CLI no se usa en este repo (restricción dura del proyecto): las migraciones se aplican a prod **a mano**, coordinadas con el deploy, según el runbook al pie de este plan. La única validación autónoma es el reset local.
  </action>
  <verify>
    <automated>npx supabase db reset</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db reset` termina con exit code **0** (replay completo del baseline + 040..050).
    - Consulta a `information_schema.columns` con `table_name='public_businesses' AND column_name='landing_draft'` devuelve **0 filas** (aserción central de seguridad de la migración).
    - Consulta a `information_schema.columns` con `table_name='businesses' AND column_name='landing_draft'` devuelve **1 fila**: `data_type = 'jsonb'`, `is_nullable = 'YES'`, `column_default` NULL/vacío.
    - Ningún comando ejecutado en esta tarea empuja el esquema al proyecto Supabase remoto.
  </acceptance_criteria>
  <done>El reset local pasa en verde, la columna existe en `businesses` con la forma correcta y NO existe en `public_businesses`.</done>
</task>

<task type="auto">
  <name>Task 3: Extender test/isolation.test.ts con los 4 casos de aislamiento del borrador (en VERDE, no skipeados)</name>
  <files>test/isolation.test.ts</files>
  <read_first>
    - `test/isolation.test.ts` (íntegro, 231 líneas) — la disciplina del archivo es la parte importante: las **aserciones** de aislamiento usan SOLO clientes anon-key autenticados (`anonA` / `anonB`); `seeded.admin` (service-role) se usa **exclusivamente** para sembrar y para un check independiente del efecto de un UPDATE, nunca como cliente de aserción (Pitfall 12). Y nunca `.eq('business_id', …)` dentro de la aserción: eso testearía el WHERE, no la RLS. Molde directo: los casos `cross-WRITE landing_config` (~línea 117), `same-tenant WRITE` (~138) y `D-10b` (~209).
    - `test/env.ts` — `hasSupabaseCreds` exige las TRES vars (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`). Sin ellas, `describe.skipIf` **skipea todo el archivo en silencio**.
    - `vitest.setup.ts` — carga `.env.local` y luego pisa con `.env.test.local` (`override: true`), que es el que apunta la suite al Supabase **LOCAL**.
    - `test/helpers/supabase-fixtures.ts` — `seedTwoTenants()` / `teardown()` y la forma de `seeded` (`bizA`, `bizB`, `admin`, `emailA/B`, `password`).
    - `.planning/workstreams/web-builder/phases/15-borrador-y-publicaci-n-n-cleo/15-RESEARCH.md` §"Los 4 casos nuevos de `test/isolation.test.ts`" — los 4 tests ya están escritos ahí, incluida la nota de **orden**.
  </read_first>
  <action>
    Agregar al `describe` existente un bloque `── landing_draft: aislamiento del BORRADOR (Phase 15 / PUB-03) ──` con **4** `it`, copiando los cuerpos del RESEARCH:

    1. **`landing_draft` no está en la vista pública**: `anonA.from('public_businesses').select('landing_draft').eq('id', seeded.bizA).single()` → `expect(error).not.toBeNull()` (PostgREST responde "column does not exist"). Es la aserción central de seguridad de la fase: la vista corre security-DEFINER, cualquier columna que entre ahí queda legible por anon y por cualquier usuario autenticado de cualquier tenant.
    2. **cross-READ**: `anonB.from('businesses').select('id, landing_draft')` **sin** `.eq(...)` → la fila `seeded.bizA` NO aparece en el resultado. Sin filtro a propósito: que la fila la esconda la RLS, no el WHERE.
    3. **cross-WRITE**: `anonB` intenta `update({ landing_draft: … }).eq('id', seeded.bizA).select('id')` → `error !== null || filas === 0`. Después, check **independiente** del efecto con `seeded.admin`: el `landing_draft` de A sigue en `null`.
    4. **same-tenant (happy path)**: `anonA` escribe su propio `landing_draft` → `error` es null y el check con `seeded.admin` muestra el config escrito.

    **Orden obligatorio**: el cross-WRITE (que asierta `null`) va **antes** del same-tenant (que escribe). Invertirlos rompe la aserción del cross-write. Misma disciplina que ya siguen los casos de `landing_config` del archivo.

    Correr la suite y **exigir los 4 casos en VERDE, no en skip**. Si salen skipeados, `hasSupabaseCreds` es false: crear/completar `.env.test.local` con las 3 vars tomadas de `npx supabase status` (API URL + anon key + service_role key del stack local, ya reseteado en la Task 2) y volver a correr. **Un "0 failed" con los tests skipeados NO cuenta como verificación**: sería firmar el control de seguridad central de una fase security-sensitive sin haberlo corrido.
  </action>
  <verify>
    <automated>npx vitest run test/isolation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/isolation.test.ts` sale con exit code **0** y el reporte muestra los **4 casos nuevos como passed** — `0 skipped` en ese archivo. Un reporte con `skipped` en el describe de aislamiento **falla** esta tarea.
    - `grep -c 'landing_draft' test/isolation.test.ts` sale **≥ 6** (los 4 tests + los checks de efecto).
    - `grep -c "from('public_businesses')" test/isolation.test.ts` aumenta en 1 respecto de HEAD (el caso nuevo de la vista).
    - En los 4 casos nuevos, ningún `expect(...)` corre sobre un resultado obtenido con `seeded.admin` salvo el check independiente de efecto de los casos 3 y 4 (revisión de código: las aserciones de RLS usan `anonA`/`anonB`).
    - `npx vitest run` (suite completa) sigue en verde: 0 failed.
  </acceptance_criteria>
  <done>Los 4 casos de aislamiento del borrador corren en verde con anon-key contra el Supabase local reseteado, y la suite completa sigue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitante anónimo (`anon`) → Postgres | Entra sin sesión. Su ÚNICA puerta a `businesses` es la vista `public_businesses` (security-DEFINER, columnas explícitas) |
| dueño autenticado (tenant B) → Postgres | Entra con anon-key + JWT propio. RLS `owner access` (`owner_id = auth.uid()`) es lo único que lo separa de la fila de otro negocio |
| operador (service-role, fuera del runtime web) | Bypassa RLS por diseño. Vive en scripts locales; nunca en la superficie web |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | vista `public_businesses` (security-DEFINER) | mitigate | La 050 **no toca la vista**. Verificado dos veces: (a) Task 2, consulta a `information_schema.columns` = 0 filas; (b) Task 3, caso 1 — `anonA.select('landing_draft')` sobre la vista **debe errar**. Es el único agujero posible de la fase |
| T-15-02 | Information Disclosure | `businesses.landing_draft` (cross-tenant read) | mitigate | La columna hereda la RLS de la fila (policy `owner access`, única policy). La 050 no agrega policy ni permiso. Test: cross-READ **sin** `.eq()` — la fila de A no aparece para B (Task 3, caso 2) |
| T-15-03 | Tampering | `businesses.landing_draft` (cross-tenant write) | mitigate | Ídem RLS. Test: `anonB` update sobre `id = bizA` → error o 0 filas + check independiente de que el borrador de A sigue intacto (Task 3, caso 3) |
| T-15-06 | Elevation of Privilege | trigger `businesses_protect_admin_columns` | mitigate | La 050 **no agrega `landing_draft` a la lista de columnas protegidas** (el dueño debe poder escribir su borrador) y **no modifica el trigger**, que sigue revirtiendo en silencio los UPDATE no-service_role sobre `has_web_custom`/`has_whatsapp`/`plan`/`plan_status`. El caso ya cubierto en `isolation.test.ts` (~línea 165) sigue en verde |
| T-15-11 | Denial of Service | orden de deploy (código nuevo sin migración) | mitigate | La migración es aditiva ⇒ aplicarla ANTES del deploy es 100% seguro para el código viejo (la columna nueva le es invisible). Al revés, cada guardado del editor falla contra una columna inexistente. Runbook explícito al pie de este plan + `NOTIFY pgrst, 'reload schema';` obligatorio |
| T-15-SC | Tampering | npm installs | accept | Esta fase **no instala ningún paquete** (RESEARCH §Package Legitimacy Audit: no aplica; cero dependencias nuevas es mandato del UI-SPEC). Sin superficie de supply chain nueva |
</threat_model>

<deploy_runbook>
## Orden de deploy a producción (MANUAL — no se ejecuta durante `/gsd:execute-phase`)

Este repo **no** empuja migraciones al remoto desde la CLI. Se aplican a mano, en este orden, y el
orden **no es negociable** (RESEARCH §"Orden de deploy"):

1. Aplicar `050_landing_draft.sql` a **prod** desde el SQL editor de Supabase (a mano).
2. `NOTIFY pgrst, 'reload schema';` en el mismo bloque SQL. **Sin esto, PostgREST sigue con su schema
   cache viejo y las acciones del editor fallan con "column businesses.landing_draft does not exist"
   aunque la columna exista.**
3. Deploy del código (Vercel) — recién a partir de acá el editor escribe el borrador.
4. Regenerar `supabase/schema.sql` y commitearlo (patrón del repo, igual que tras 037/039/042/043).

Entre los pasos 1-2 y el 3 hay una **ventana segura**: la columna nueva es invisible para el código
viejo, que sigue escribiendo `landing_config` y publicando al instante (comportamiento de hoy). Al
revés se rompe el editor.

**Nota para el operador (Open Question 2 del RESEARCH):** entre el deploy de la Phase 15 y el de la
Phase 16, `scripts/setup-landing.ts` **sigue escribiendo lo publicado** (sale al aire al instante). No
correr la skill sobre un negocio nuevo en esa ventana, o pegar la Phase 16 a la 15.
</deploy_runbook>

<verification>
1. `npx supabase db reset` → exit 0 (baseline + 040..050 replayado en PG17 local).
2. `information_schema.columns` sobre `public_businesses` + `landing_draft` → **0 filas**.
3. `information_schema.columns` sobre `businesses` + `landing_draft` → 1 fila, `jsonb`, nullable, sin default.
4. `npx vitest run test/isolation.test.ts` → exit 0, **4 casos nuevos passed, 0 skipped**.
5. `npx vitest run` (suite completa) → 0 failed.
</verification>

<success_criteria>
- [ ] `supabase/migrations/050_landing_draft.sql` creada: aditiva, idempotente, nullable, sin valor por defecto, con backfill `landing_draft := landing_config`.
- [ ] La migración NO crea vistas, policies, permisos ni toca el trigger de columnas admin.
- [ ] `supabase db reset` local en verde.
- [ ] `landing_draft` **no existe** en `public_businesses` (probado, no asumido).
- [ ] 4 casos nuevos de aislamiento en `test/isolation.test.ts`, corriendo en **VERDE** (no skipped).
- [ ] Ningún comando de push remoto de migraciones fue ejecutado.
</success_criteria>

<output>
Crear `.planning/workstreams/web-builder/phases/15-borrador-y-publicaci-n-n-cleo/15-01-SUMMARY.md` al terminar.
Incluir en el SUMMARY: el `<deploy_runbook>` completo (es el paso manual que el usuario tiene que
ejecutar en prod) y la nota al operador sobre la ventana 15↔16 de la skill.
</output>

<!-- planner-discipline-allow: DEFAULT -->
<!-- planner-discipline-allow: GRANT -->
<!-- planner-discipline-allow: CREATE POLICY -->
