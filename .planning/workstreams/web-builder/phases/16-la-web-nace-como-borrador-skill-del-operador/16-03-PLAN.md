---
phase: 16-la-web-nace-como-borrador-skill-del-operador
plan: 03
type: execute
wave: 3
depends_on: ["16-02"]
files_modified:
  - .claude/skills/forjo-web-builder/SKILL.md
autonomous: true
requirements: [SKILL-07, SKILL-08]

must_haves:
  truths:
    - "El operador que corre la skill entiende, sin leer el código, que la web que arma NACE COMO BORRADOR y que el dueño es quien la publica."
    - "El MODO EDICIÓN reconstruye el payload SIEMPRE desde el borrador, con fallback a lo publicado solo si el borrador es NULL."
    - "El checkpoint humano pre-escritura muestra el aviso de choque cuando el dueño tiene cambios sin publicar."
    - "El operador sabe que --publish existe, qué hace, y que solo se usa con el OK explícito del dueño."
  artifacts:
    - path: ".claude/skills/forjo-web-builder/SKILL.md"
      provides: "el flujo del operador alineado con el contrato borrador/publicado (D-01..D-05)"
      contains: "--publish"
  key_links:
    - from: ".claude/skills/forjo-web-builder/SKILL.md (MODO EDICIÓN)"
      to: "scripts/setup-landing.ts (--inspect)"
      via: "el modo edición reconstruye desde la clave `pendiente_de_aprobacion` del --inspect, cayendo a `al_aire` solo si es null (D-04)"
      pattern: "pendiente_de_aprobacion"
    - from: ".claude/skills/forjo-web-builder/SKILL.md (paso 6, checkpoint)"
      to: "scripts/setup-landing.ts (aviso de choque)"
      via: "el checkpoint bloqueante incluye el aviso ⚠ del dueño con cambios sin publicar (D-01/D-02)"
      pattern: "cambios sin publicar"
---

<objective>
Alinear `.claude/skills/forjo-web-builder/SKILL.md` con el contrato nuevo. Hoy la skill documenta —y
por lo tanto le ENSEÑA al agente operador— tres cosas que después del plan 16-02 son **falsas**:

1. que la escritura "sobre-escribe el `landing_config`" y deja la preview viva en `/[slug]`;
2. que el MODO EDICIÓN reconstruye el estado desde el `landing_config` volcado por `--inspect`
   (**el bug de D-04**: partir de lo publicado DESCARTA en silencio los retoques que el dueño hizo y
   no publicó — la misma pérdida que D-01 viene a avisar, pero por la puerta de atrás, donde ningún
   aviso es posible);
3. que el gate de escritura es el validador fail-safe de lectura.

Purpose: la SKILL.md **es** el flujo. El checkpoint humano pre-escritura vive acá, no en el proceso
Node — el script solo imprime; quien bloquea es esta skill. Si la doc queda desfasada, el agente
operador va a hacer exactamente lo que la fase vino a prevenir.

Output: SKILL.md reescrita en sus 4 puntos de flujo + el frontmatter, la intro, el OUTPUT y los
guardrails. Cero código.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-builder/STATE.md
@.planning/workstreams/web-builder/ROADMAP.md
@.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-CONTEXT.md
@.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-PATTERNS.md
@.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-02-SUMMARY.md
@.claude/skills/forjo-web-builder/SKILL.md
@scripts/setup-landing.ts
</context>

## Artifacts this phase produces (plan 16-03)

| Artefacto | Dónde | Nuevo/Modificado |
|---|---|---|
| Paso 2 (RESOLVER el negocio) reescrito al contrato borrador/publicado | `SKILL.md` | MODIFICADO |
| MODO EDICIÓN paso 1 → reconstruye desde `pendiente_de_aprobacion` (D-04) | `SKILL.md` | MODIFICADO |
| Paso 6 (CHECKPOINT BLOQUEANTE) → incluye el aviso de choque D-01/D-02 | `SKILL.md` | MODIFICADO |
| Paso 7 (ESCRIBIR) → gate estricto + escribe el borrador + `--publish` opt-in | `SKILL.md` | MODIFICADO |
| Paso 8 (OUTPUT) → sin preview pública por defecto | `SKILL.md` | MODIFICADO |
| Guardrail nuevo: "La web NACE COMO BORRADOR" | `SKILL.md` | NUEVO |
| `description` del frontmatter alineada (ya no promete preview viva en `/[slug]`) | `SKILL.md` | MODIFICADO |

NO se toca ningún archivo de código. NO se agrega ninguna dependencia.

<tasks>

<task type="auto">
  <name>Task 1: Los 4 puntos del flujo — paso 2, MODO EDICIÓN, checkpoint y escritura</name>

  <read_first>
    - `.claude/skills/forjo-web-builder/SKILL.md` — LEER ENTERO. Los 4 puntos a reescribir: paso 2 (línea 60: "avisá al operador que la escritura lo **sobre-escribe**"), MODO EDICIÓN paso 1 (líneas 74-85: "El `--inspect` extendido **vuelca el `landing_config` completo**"), paso 6 CHECKPOINT BLOQUEANTE (líneas 213-226) y paso 7 ESCRIBIR (líneas 228-247, con el `GATE parseLandingConfig` → `UPDATE`).
    - `scripts/setup-landing.ts` — el script YA REESCRITO por el plan 16-02. Es la fuente de verdad del contrato que esta doc describe: las claves reales del `--inspect` (`al_aire`, `pendiente_de_aprobacion`, `tiene_cambios_sin_publicar`, `partes_sin_publicar`, `nunca_publico`), el flag `--publish`, el gate estricto y el copy exacto de los avisos. **Copiá los strings del script, no los inventes.**
    - `.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-CONTEXT.md` — D-01, D-02, D-03, D-04, D-04b (el flag `--from-published` está **DESCARTADO**: no lo documentes ni lo propongas), D-05, D-06 (avisarle al dueño: **NADA en código** — el operador le escribe por WhatsApp; no inventes un mail ni un badge).
    - `.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-PATTERNS.md` — §SKILL.md: la tabla de los 4 puntos exactos y la estructura del archivo (frontmatter → intro → Prerrequisitos → Flujo 1..8 con MODO EDICIÓN intercalado → Guardrails → Reusos declarados; comandos en bloques ```powershell).
  </read_first>

  <files>.claude/skills/forjo-web-builder/SKILL.md</files>

  <action>
    Reescribí los 4 puntos. Mantené el tono, la estructura, la numeración y el formato de bloques
    ```powershell del archivo. No reordenes secciones.

    **(1) Paso 2 — RESOLVER el negocio (read-only).** El `--inspect` ahora vuelca DOS columnas por
    separado. Documentá las claves reales del JSON: `al_aire` (LO PUBLICADO — lo que ve cualquier
    visitante en `/[slug]` ahora mismo), `pendiente_de_aprobacion` (EL BORRADOR — lo que ve el dueño
    en su editor y todavía no salió al aire), `nunca_publico`, `tiene_cambios_sin_publicar` y
    `partes_sin_publicar`.
    Reemplazá el renglón que hoy dice que la escritura sobre-escribe el config poblado por el contrato
    nuevo, explícito:
    - la escritura por defecto **NO toca lo publicado**: escribe el borrador y el dueño lo publica
      desde su panel;
    - si `tiene_cambios_sin_publicar` es `true`, el dueño tiene trabajo sin publicar → **eso se lleva
      al checkpoint del paso 6** (no es una decisión que se toma acá).

    **(2) MODO EDICIÓN paso 1 — de qué columna se parte (D-04).** Éste es el punto de seguridad del
    plan. Cambiá la instrucción: cuando NO existe `landing-payloads/<slug>.json`, la reconstrucción del
    `BuilderInput`/`brand` se hace **SIEMPRE desde `pendiente_de_aprobacion`** (el borrador), y **solo
    si esa clave es `null`** se cae a `al_aire` (negocio legacy sin borrador).
    Escribí el POR QUÉ, textual y sin ablandarlo: el borrador es "lo último que se tocó" e **incluye los
    retoques que el dueño hizo y no publicó**. Partir de lo publicado los **descartaría en silencio** al
    re-escribir. Es la misma pérdida que el aviso de choque viene a evitar, pero por la puerta de atrás,
    donde ningún aviso es posible.
    **NO documentes ningún flag `--from-published`** (D-04b: descartado). Si el dueño rompió su borrador
    y quiere volver a lo que está al aire, eso se resuelve con el botón "Descartar" de SU editor, que es
    donde corresponde — decilo así.

    **(3) Paso 6 — CHECKPOINT DE APROBACIÓN (BLOQUEANTE).** Sumá un ítem MÁS a la lista de lo que se
    muestra (no es un gate nuevo: cuelga del que ya existe). El checkpoint tiene que mostrar, además de
    secciones / tema / imágenes / copy:
    - **Destino de la escritura**: `landing_draft` (borrador — el dueño lo revisa y publica) o, si el
      operador pidió `--publish`, borrador **+ AL AIRE**.
    - **Aviso de choque, si aplica**: cuando el `--inspect` del paso 2 marcó
      `tiene_cambios_sin_publicar`, mostralo con el copy del script:
      `⚠ El dueño tiene cambios sin publicar. Secciones que difieren de lo publicado: hero, gallery. Si seguís, los pisás.`
      El operador decide en el momento, con el dato a la vista. **El flujo NO aborta ni obliga a
      re-correr nada**: el caso limpio (el 95%) no paga fricción. Si el operador no quiere pisar el
      trabajo del dueño, para y le escribe (WhatsApp) — eso es una decisión suya, no del script.
    - Dejá escrito que **el checkpoint bloqueante vive acá y no en el script**: el script solo imprime;
      cuando el proceso Node arranca, ya está aprobado.

    **(4) Paso 7 — ESCRIBIR (solo tras aprobar).** El comando por defecto **no cambia**:
    `npm run setup:landing -- --slug <slug> --config landing-payloads/<slug>.json`
    Lo que cambia es qué hace, y hay que decirlo:
    - resuelve `business_id` → re-hostea imágenes → arma el config → **GATE estricto**
      (`parseLandingConfigForWrite`: rechaza ruidosamente con `invalid_config` / `config_too_large`, NO
      degrada una sección inválida a `{}`) → `UPDATE ... WHERE id = businessId` escribiendo **SOLO
      `landing_draft`**.
    - **La web NO sale al aire.** El dueño la revisa en su editor y la publica él.
    - **`--publish` (opt-in, sección aparte):** documentá el comando
      `npm run setup:landing -- --slug <slug> --config landing-payloads/<slug>.json --publish`
      y qué hace: escribe las DOS columnas (borrador + publicado) con el mismo config validado, y ANTES
      imprime qué web está por reemplazar (o avisa que es el GO-LIVE del negocio). **Regla dura: solo se
      usa cuando el DUEÑO dio el OK explícito** (el caso real: pidió "hacémela y subila" y nunca entra al
      panel). **Nunca por defecto, nunca "por las dudas", nunca sin decírselo.**
    - Mantené el renglón de "si el script aborta, reportá el error tal cual y NO reintentes a ciegas",
      ahora con los códigos reales del gate estricto.
    - **Avisarle al dueño que su web está lista**: el operador le escribe por WhatsApp. **No hay nada
      en código** que lo notifique (D-06) — no inventes un mail ni un badge.
  </action>

  <verify>
    <automated>rg -c "pendiente_de_aprobacion" .claude/skills/forjo-web-builder/SKILL.md</automated>
    <automated>rg -c -- "--publish" .claude/skills/forjo-web-builder/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `rg -c "al_aire" .claude/skills/forjo-web-builder/SKILL.md` devuelve 1 o más.
    - `rg -c "pendiente_de_aprobacion" .claude/skills/forjo-web-builder/SKILL.md` devuelve 2 o más (paso 2 + MODO EDICIÓN).
    - `rg -c "tiene_cambios_sin_publicar" .claude/skills/forjo-web-builder/SKILL.md` devuelve 1 o más.
    - `rg -c -- "--publish" .claude/skills/forjo-web-builder/SKILL.md` devuelve 2 o más (el comando + la regla de uso).
    - `rg -c "parseLandingConfigForWrite" .claude/skills/forjo-web-builder/SKILL.md` devuelve 1 o más.
    - `rg -c "from-published" .claude/skills/forjo-web-builder/SKILL.md` devuelve 0 (D-04b: descartado, no se documenta).
    - `rg -c "El dueño tiene cambios sin publicar" .claude/skills/forjo-web-builder/SKILL.md` devuelve 1 (el copy del aviso, idéntico al del script).
    - El MODO EDICIÓN dice explícitamente que reconstruye desde el borrador y que cae a lo publicado SOLO si el borrador es `null`.
    - El paso 6 lista el destino de la escritura (borrador vs borrador+al aire) como ítem del checkpoint.
    - El paso 7 dice que por defecto la web NO sale al aire.
    - `git diff --name-only` lista ÚNICAMENTE `.claude/skills/forjo-web-builder/SKILL.md` (cero cambios de código en este plan).
  </acceptance_criteria>

  <done>Los 4 puntos del flujo describen el contrato real del script post-16-02: inspect con dos columnas, edición desde el borrador, checkpoint con el aviso de choque, escritura draft-only con `--publish` opt-in.</done>
</task>

<task type="auto">
  <name>Task 2: Frontmatter, intro, OUTPUT y guardrails — la skill deja de prometer una preview que no existe</name>

  <read_first>
    - `.claude/skills/forjo-web-builder/SKILL.md` — los 4 lugares que todavía prometen publicación instantánea: el `description` del frontmatter (líneas 3-12: "deja la preview viva en su `/[slug]`" + "Produce el config POR SLUG y devuelve la URL de preview"), la intro (líneas 17-21: "aplicar una web es escribir la fila, instantáneo, sin deploy"), el paso 8 OUTPUT (líneas 249-252: "Devolvé la **URL de preview** (`/[slug]` es `force-dynamic` → inmediata, sin deploy)") y el guardrail "Escritura SOLO por `setup:landing`" (línea 270).
    - `scripts/setup-landing.ts` — el mensaje de cierre bifurcado que dejó el plan 16-02 (default vs `--publish`): la doc tiene que coincidir con lo que el script realmente imprime.
    - `.planning/workstreams/web-builder/REQUIREMENTS.md` — §Out of Scope: "Preview compartible por link" está **fuera de alcance**. No inventes un mecanismo de preview para el operador.
  </read_first>

  <files>.claude/skills/forjo-web-builder/SKILL.md</files>

  <action>
    Cuatro ajustes, todos de coherencia con el contrato nuevo.

    **(a) `description` del frontmatter.** Reescribí la parte que promete "deja la preview viva en su
    `/[slug]`" y "devuelve la URL de preview". El resultado real de la skill es: deja la web armada
    **como borrador esperando la aprobación del dueño**, y devuelve el estado (borrador escrito, qué
    partes cambian, si hay choque). Conservá los triggers (las frases de activación) TAL CUAL: son lo
    que hace que la skill se dispare, y tocarlas la rompe.

    **(b) Intro (el párrafo bajo `# Forjo Web Builder`).** El renglón "el config es dato, no código →
    aplicar una web es escribir la fila, instantáneo, sin deploy" sigue siendo cierto técnicamente,
    pero ahora la fila que se escribe por defecto es el **borrador**. Reformulalo: no hay deploy, sí
    hay una **aprobación del dueño** entre la escritura y el público. Ese es el circuito
    **operador → dueño → público**.

    **(c) Paso 8 — OUTPUT.** Bifurcalo, igual que el mensaje de cierre del script:
    - **por defecto**: NO hay URL de preview pública. Devolvé el resumen de lo armado (secciones, tema,
      imágenes re-hosteadas) + el hecho de que quedó **como borrador** + el próximo paso real: avisarle
      al dueño (WhatsApp) para que entre a su panel, lo revise y lo publique. **No inventes un link de
      preview**: el preview compartible está explícitamente fuera de alcance (REQUIREMENTS.md §Out of
      Scope) y `/[slug]` sigue mostrando la web vieja (o la reserva simple).
    - **con `--publish`**: ahí sí, la URL de `/[slug]` (`force-dynamic` → inmediata, sin deploy). El
      script ya la imprime.

    **(d) Guardrails.** Actualizá el guardrail "Escritura SOLO por `setup:landing`" (el gate Zod que
    menciona ya no es el fail-safe de lectura sino el estricto de escritura) y **agregá un guardrail
    nuevo**, en el mismo tono no-negociable que los demás:
    **"La web NACE COMO BORRADOR"** — la escritura por defecto va a `landing_draft` y NO sale al aire.
    Publicar es del dueño. `--publish` existe, es opt-in explícito y **solo se usa con el OK del dueño**;
    nunca por defecto, nunca silencioso. Si el dueño tiene cambios sin publicar, el aviso del checkpoint
    (paso 6) es de lectura obligatoria antes de seguir.
  </action>

  <verify>
    <automated>rg -c "borrador" .claude/skills/forjo-web-builder/SKILL.md</automated>
    <human-check>Leer la SKILL.md de punta a punta como si fueras el agente operador que la va a ejecutar: ¿queda alguna instrucción que lleve a publicar sin querer, o a reconstruir el payload desde lo publicado? (revisión de coherencia, va al UAT de fin de fase).</human-check>
  </verify>

  <acceptance_criteria>
    - `rg -c "NACE COMO BORRADOR" .claude/skills/forjo-web-builder/SKILL.md` devuelve 1 o más (el guardrail nuevo).
    - `rg -c "borrador" .claude/skills/forjo-web-builder/SKILL.md` devuelve 6 o más.
    - El `description` del frontmatter conserva TODOS los triggers de activación originales ("armale la web a [cliente]", "personalizar la web de un negocio", "generar landing para [slug]", "hacele una web estilo jjotalab a [cliente]", "convertir el Instagram de [cliente] en su página de reservas") — verificable con `rg -c "armale la web a" .claude/skills/forjo-web-builder/SKILL.md` = 1 y `rg -c "generar landing para" .claude/skills/forjo-web-builder/SKILL.md` = 1.
    - El frontmatter sigue siendo YAML válido con las claves `name` y `description` (`rg -c "^name: forjo-web-builder" .claude/skills/forjo-web-builder/SKILL.md` = 1).
    - El paso 8 (OUTPUT) distingue el camino por defecto (sin URL pública) del camino `--publish` (con URL).
    - El guardrail "Escritura SOLO por `setup:landing`" sigue existiendo y su gate nombra el validador estricto.
    - Los guardrails preexistentes siguen todos en pie: secciones fijas, solo componentes Forjo-native, no tocar booking/pagos, `services` auto desde la tabla, nunca campos sensibles, re-host obligatorio de imágenes (`rg -c "Re-host OBLIGATORIO" .claude/skills/forjo-web-builder/SKILL.md` = 1).
    - `git diff --name-only` lista ÚNICAMENTE `.claude/skills/forjo-web-builder/SKILL.md`.
  </acceptance_criteria>

  <done>La skill ya no promete una preview pública que no existe, declara el circuito operador → dueño → público, y tiene el guardrail no-negociable de que la web nace como borrador. Los triggers de activación y los guardrails viejos quedan intactos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → agente operador (Claude Code) | La doc **es** el flujo ejecutable. Una instrucción desactualizada acá es un bug de producto ejecutado por un agente con service-role. |
| checkpoint humano del paso 6 → escritura service-role | Es el ÚNICO gate humano entre el agente y una fila real de producción. No hay confirmación interactiva en el proceso Node: el gate vive en esta doc. |
| MODO EDICIÓN → `landing_draft` del dueño | Reconstruir el payload desde la columna equivocada descarta el trabajo sin publicar del dueño **sin ningún aviso posible**. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02b | Tampering (pérdida silenciosa — la 2ª regresión del Threat note) | SKILL.md § MODO EDICIÓN paso 1 | mitigate | La doc instruye reconstruir **SIEMPRE desde `pendiente_de_aprobacion`** (el borrador), cayendo a `al_aire` solo si es `null` (D-04). Partir de lo publicado descartaría en silencio los retoques que el dueño no publicó, y re-publicaría contenido viejo. Acceptance criterion explícito + grep de `pendiente_de_aprobacion`. |
| T-16-08 | Tampering (checkpoint degradado) | SKILL.md § paso 6 | mitigate | El aviso de choque (D-01/D-02) se cuelga del checkpoint BLOQUEANTE que ya existe, con el copy exacto del script. El script **solo imprime**: si esta doc no lo lleva al checkpoint, el aviso se pierde en el scroll y el operador pisa el trabajo del dueño sin registrarlo. |
| T-16-04b | Elevation of Privilege (operador → producción) | SKILL.md § paso 7 (`--publish`) | mitigate | `--publish` se documenta como opt-in explícito con una **regla dura**: solo con el OK del dueño. Nunca por defecto, nunca "por las dudas". El guardrail nuevo ("La web NACE COMO BORRADOR") lo repite en la sección no-negociable, que es la que el agente lee como restricción dura. |
| T-16-09 | Spoofing / Repudiation (falsa expectativa de publicación) | SKILL.md § frontmatter + intro + paso 8 | mitigate | La skill deja de prometer "preview viva en `/[slug]`" / "URL de preview". Un operador que cree que publicó y no publicó (o al revés) toma decisiones sobre un estado falso. El OUTPUT bifurcado dice la verdad de cada camino. |
| T-16-10 | Denial of Service (skill rota) | SKILL.md § frontmatter | mitigate | Los **triggers de activación** del `description` se conservan verbatim: tocarlos hace que la skill deje de dispararse y el operador vuelva a escribir configs a mano (sin gate, sin checkpoint). Acceptance criterion con grep de los triggers. |
| T-16-11 | Information Disclosure | SKILL.md § guardrails | mitigate | Se conserva intacto el guardrail "NUNCA campos sensibles en el config ni en la vista `public_businesses`" y el de re-host obligatorio de imágenes (nunca hot-link al CDN de IG). Este plan no los toca; acceptance criterion verifica que siguen en pie. |
| T-16-SC | Tampering (supply chain) | npm installs | mitigate | **Este plan no toca código y no instala nada.** Es una edición de markdown. `git diff --name-only` debe listar un solo archivo. |
</threat_model>

<verification>
```bash
rg -n "al_aire|pendiente_de_aprobacion|tiene_cambios_sin_publicar" .claude/skills/forjo-web-builder/SKILL.md
rg -n -- "--publish" .claude/skills/forjo-web-builder/SKILL.md
rg -c "from-published" .claude/skills/forjo-web-builder/SKILL.md     # 0
rg -c "NACE COMO BORRADOR" .claude/skills/forjo-web-builder/SKILL.md # >= 1
git diff --name-only                                                 # solo SKILL.md
```

Coherencia doc ↔ código (el checker la valida leyendo los dos archivos):
- Cada clave del `--inspect` que nombra la SKILL.md existe en `scripts/setup-landing.ts`.
- El copy del aviso de choque de la SKILL.md es el MISMO string que imprime el script.
- El comando `--publish` documentado es el que el script realmente acepta (`hasFlag('publish')`).

Verificación de operador (UAT de fin de fase, `/gsd:verify-work 16`):
- Correr la skill de punta a punta sobre un negocio real y confirmar que el agente (a) muestra el
  aviso de choque cuando corresponde, (b) NO publica por defecto, (c) en MODO EDICIÓN parte del
  borrador.
</verification>

<success_criteria>
- [ ] Paso 2 documenta `al_aire` / `pendiente_de_aprobacion` / `tiene_cambios_sin_publicar` / `partes_sin_publicar` / `nunca_publico`.
- [ ] MODO EDICIÓN reconstruye SIEMPRE desde el borrador; fallback a lo publicado solo si es `null` (D-04). Cero mención de `--from-published` (D-04b).
- [ ] El checkpoint del paso 6 incluye el destino de la escritura y el aviso de choque, con el copy exacto del script.
- [ ] El paso 7 describe el gate estricto, la escritura draft-only y `--publish` como opt-in "solo con el OK del dueño".
- [ ] El paso 8 no promete una URL de preview pública en el camino por defecto.
- [ ] Guardrail nuevo "La web NACE COMO BORRADOR" presente; los guardrails viejos y los triggers del frontmatter intactos.
- [ ] Cero cambios de código: `git diff --name-only` lista solo `.claude/skills/forjo-web-builder/SKILL.md`.
</success_criteria>

<output>
Create `.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-03-SUMMARY.md` when done.
</output>
