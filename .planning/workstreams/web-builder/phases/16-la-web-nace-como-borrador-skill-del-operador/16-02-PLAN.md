---
phase: 16-la-web-nace-como-borrador-skill-del-operador
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - scripts/setup-landing.ts
autonomous: true
requirements: [SKILL-07, SKILL-08]

must_haves:
  truths:
    - "El operador corre el script sin flags y la web armada NO aparece en /[slug]: solo se escribió landing_draft."
    - "El operador corre el script con --publish y la web SÍ sale al aire: se escriben las dos columnas con el mismo objeto validado."
    - "Antes de escribir, si el dueño tiene cambios sin publicar, el script imprime QUÉ partes difieren de lo publicado — y sigue adelante (avisa, no aborta)."
    - "Un config armado inválido ABORTA la escritura ruidosamente (invalid_config / config_too_large) en vez de degradar una sección a {} en silencio."
    - "--inspect vuelca las dos columnas por separado y dice cuál está AL AIRE y cuál está PENDIENTE DE APROBACIÓN."
  artifacts:
    - path: "scripts/setup-landing.ts"
      provides: "write path draft-only por defecto + flag --publish + aviso de choque + --inspect con las dos columnas"
      contains: "landingWriteColumns"
  key_links:
    - from: "scripts/setup-landing.ts"
      to: "lib/landing/write.ts"
      via: "el GATE de escritura usa parseLandingConfigForWrite (estricto) y el UPDATE usa landingWriteColumns(config, publish)"
      pattern: "parseLandingConfigForWrite|landingWriteColumns"
    - from: "scripts/setup-landing.ts"
      to: "lib/landing/editor-draft.ts"
      via: "el aviso de choque y el flag de --inspect usan diffConfigParts sobre las dos columnas"
      pattern: "diffConfigParts"
    - from: "scripts/setup-landing.ts (resolveBusiness)"
      to: "businesses.landing_draft"
      via: "el SELECT trae landing_draft — hoy el script lo escribe a ciegas y nunca lo lee"
      pattern: "landing_draft"
---

<objective>
`scripts/setup-landing.ts` deja de publicar. Después de este plan:

- **Por defecto escribe SOLO el borrador** (`landing_draft`). La web armada por el operador NO aparece
  en `/[slug]`: espera al dueño (D-03, SKILL-07).
- **`--publish`** (flag booleano explícito, opt-in) escribe además lo publicado, con el MISMO objeto
  validado (D-03b), e imprime ANTES qué web está por reemplazar.
- **Avisa el choque** operador↔dueño: si el dueño tiene cambios sin publicar, imprime qué partes
  difieren de lo publicado (D-01/D-02). Avisa — **no aborta, no obliga a re-correr nada**.
- **El gate de validación pasa al estricto**: `parseLandingConfigForWrite` (reject-on-invalid) en vez
  del fail-safe de LECTURA que degradaba una sección inválida a `{}` en silencio.
- **`--inspect` vuelca las dos columnas por separado** y dice cuál es cuál (D-05, SKILL-08).

Purpose: es el corazón de la fase. Hoy el script es el único escritor del landing y publica al
instante; a partir de acá hay DOS escritores sobre `landing_draft` (operador y dueño) y ninguno puede
pisar al otro en silencio.

Output: `scripts/setup-landing.ts` reescrito en sus 4 puntos (resolución, inspect, gate, update).
Cero deps npm nuevas. Cero endpoints. Cero migraciones. El script sigue siendo local, service-role,
fuera del runtime web (invariante D10-01 / SKILL-04).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-builder/STATE.md
@.planning/workstreams/web-builder/ROADMAP.md
@.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-CONTEXT.md
@.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-PATTERNS.md
@.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-01-SUMMARY.md
@scripts/setup-landing.ts
@lib/landing/write.ts
@lib/landing/editor-draft.ts
</context>

## Artifacts this phase produces (plan 16-02)

| Símbolo | Dónde | Tipo | Nuevo/Modificado |
|---|---|---|---|
| `hasFlag(name: string): boolean` | `scripts/setup-landing.ts` | función de módulo (privada) | NUEVO |
| `readLandingState(biz)` | `scripts/setup-landing.ts` | función de módulo (privada) | NUEVO |
| `formatPart(p: ConfigPart): string` | `scripts/setup-landing.ts` | función de módulo (privada) | NUEVO |
| `--publish` | CLI de `npm run setup:landing` | flag booleano opt-in | NUEVO |
| `al_aire`, `pendiente_de_aprobacion`, `tiene_cambios_sin_publicar`, `partes_sin_publicar`, `nunca_publico` | claves del JSON de `--inspect` | contrato de salida | NUEVO |
| `landing_config` (clave del JSON de `--inspect`) | salida de `--inspect` | contrato de salida | REEMPLAZADA por `al_aire` + `pendiente_de_aprobacion` |
| `landing_draft` en el `select` de `resolveBusiness` | `scripts/setup-landing.ts` | columna leída | NUEVO |

NO se crea ningún archivo. NO se agrega ninguna dependencia (`hasFlag` son 3 líneas; **no** se instala
ninguna lib de prompts — el checkpoint humano vive en el SKILL.md, no en el proceso Node).

<tasks>

<task type="auto">
  <name>Task 1: Leer el borrador — hasFlag, landing_draft en resolveBusiness, y el estado draft/publicado</name>

  <read_first>
    - `scripts/setup-landing.ts` — LEER ENTERO. En particular: `getFlag` (líneas 61-71, solo soporta `--x <valor>` / `--x=valor`, NO booleanos), `resolveBusiness` (líneas 76-104, su `select` NO trae `landing_draft` y su cast tampoco), y el dispatch de `main()` (líneas 358-377).
    - `lib/landing/editor-draft.ts` — el `diffConfigParts` / `ConfigPart` que dejó el plan 16-01, y el bloque de comentario de `canonical` (por qué un `JSON.stringify` crudo NO sirve).
    - `lib/landing/schema.ts` — `parseLandingConfig`: el validador **fail-safe de LECTURA** (`null → null`; presente-pero-inválido → `DEFAULT_LANDING_CONFIG`). Acá SÍ es el correcto: estamos LEYENDO las dos columnas para compararlas, no escribiendo.
    - `.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-PATTERNS.md` — §"No Analog Found": no existe ningún flag booleano ni ningún prompt interactivo en el repo. No los inventes.
    - `scripts/setup-admin.ts` — convenciones de scripts CLI service-role (dotenv, `console.error('[modulo] ...')` + `process.exitCode = 1` + `return`, NUNCA `process.exit()`).
  </read_first>

  <files>scripts/setup-landing.ts</files>

  <action>
    Tres cambios de plomería, todos dentro de `scripts/setup-landing.ts`. Nada de esto escribe: es
    todo lectura, y lo consumen tanto `runInspect` (Task 2) como `runWrite` (Task 3).

    **(a) `hasFlag(name: string): boolean`** — al lado de `getFlag`. Devuelve `true` si `--{name}`
    aparece como token exacto en `process.argv.slice(2)`. `getFlag('publish')` NO sirve para un flag
    booleano: devolvería el argv siguiente (o `undefined`), así que `--publish --slug x` daría
    `'--slug'` y `--publish` suelto daría `undefined`. Comentario en español explicando eso.

    **(b) `resolveBusiness` lee `landing_draft`.** Agregá `landing_draft` a la lista de columnas del
    `.select(...)` y `landing_draft: unknown` al cast del tipo de retorno. Es el ÚNICO punto que hay
    que tocar: `runInspect` y `runWrite` comparten este helper. Hoy el script escribe esa columna a
    ciegas y nunca la lee — de ahí sale la mitad de la fase.

    **(c) `readLandingState(biz)`** — helper nuevo que deriva, de las dos columnas crudas, el estado
    que consumen el aviso de choque (D-01/D-02) y `--inspect` (D-05). Firma sugerida:
    recibe el objeto que devuelve `resolveBusiness` y devuelve
    `{ published: LandingConfig | null; draft: LandingConfig | null; nuncaPublico: boolean; borradorSinComparar: boolean; partes: ConfigPart[]; tieneCambiosSinPublicar: boolean }`.

    Reglas exactas:
    - `published = parseLandingConfig(biz.landing_config)` y `draft = parseLandingConfig(biz.landing_draft)`.
      Usá el validador **fail-safe de LECTURA** de `@/lib/landing/schema` a propósito: acá se está
      leyendo para comparar, y un config roto en la DB no debe tumbar el script. El validador
      **estricto** es del write path y se usa en la Task 3. Dejá esa distinción escrita en el
      comentario — es exactamente el error que esta fase viene a corregir en el otro sentido.
    - `nuncaPublico = published === null`.
    - Si `draft === null` → no hay nada pendiente: `partes = []`, `tieneCambiosSinPublicar = false`,
      `borradorSinComparar = false`.
    - Si `draft !== null` y `published === null` → el dueño tiene un borrador y el negocio nunca
      publicó: no hay contra qué diffear. `partes = []`, `tieneCambiosSinPublicar = true`,
      `borradorSinComparar = true`.
    - Si los dos existen → `partes = diffConfigParts(draft, published)` y
      `tieneCambiosSinPublicar = partes.length > 0`; `borradorSinComparar = false`.

    **DESVIACIÓN DECLARADA de D-01 (dejala escrita en el comentario, mismo ethos que la desviación
    que declaró 15-02):** D-01 dice "detecta si `landing_draft` ≠ `landing_config` con `configsEqual`".
    La señal real es `diffConfigParts(draft, published).length > 0`, que es `configsEqual` aplicado
    **por parte** más una normalización previa de ambos lados. Misma intención (avisar cuando el dueño
    tiene trabajo sin publicar), mejor mecanismo: `configsEqual` crudo daría "distinto" también cuando
    el editor del dueño simplemente materializó las 8 secciones fijas al guardar sin cambiar nada
    visible — un aviso que grita en el caso limpio es un aviso que el operador aprende a ignorar. NO
    "simplificar" esto de vuelta a `configsEqual`.

    **(d) `formatPart(p: ConfigPart): string`** — mapea la parte a la etiqueta que ve el operador:
    `'theme' → 'tema'`, `'motion' → 'movimiento'`, y cualquier tipo de sección se imprime tal cual
    (`hero`, `about`, `services`, `gallery`, `location`, `hours`, `cta`, `booking`). Es presentación,
    va en el script y no en el módulo puro.

    Ajustá los imports de la cabecera: sumá `diffConfigParts` y el tipo `ConfigPart` de
    `@/lib/landing/editor-draft`, y el tipo `LandingConfig` de `@/lib/landing/schema`.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` sale con exit code 0.
    - `npm test` sale con exit code 0 (nada de la suite se rompe).
    - `rg -c "function hasFlag" scripts/setup-landing.ts` devuelve 1.
    - `rg -c "landing_draft" scripts/setup-landing.ts` devuelve 2 o más (el `select` y el cast).
    - El string del `.select(...)` de `resolveBusiness` incluye `landing_draft`.
    - `rg -c "function readLandingState" scripts/setup-landing.ts` devuelve 1.
    - `rg -c "diffConfigParts" scripts/setup-landing.ts` devuelve 1 o más.
    - `rg -c "function formatPart" scripts/setup-landing.ts` devuelve 1.
    - El comentario de `readLandingState` declara la desviación de D-01 (usa `diffConfigParts` por parte, no `configsEqual` crudo) y explica por qué.
    - No hay ninguna dependencia npm nueva: `git diff package.json package-lock.json` está vacío.
  </acceptance_criteria>

  <done>El script sabe leer el borrador: `resolveBusiness` trae `landing_draft`, `hasFlag` existe, y `readLandingState` deriva el estado (nunca publicó / tiene cambios sin publicar / qué partes difieren) sin escribir nada.</done>
</task>

<task type="auto">
  <name>Task 2: --inspect vuelca borrador y publicado por separado (SKILL-08 / D-05)</name>

  <read_first>
    - `scripts/setup-landing.ts` — `runInspect` (líneas 106-158) y el comentario de las líneas 150-153, que hoy dice que re-correr la escritura SOBRE-ESCRIBE el config. Después de la Task 3 eso deja de ser cierto por defecto: hay que reescribirlo, no borrarlo.
    - `.claude/skills/forjo-web-builder/SKILL.md` — el MODO EDICIÓN (líneas 66-108): quién consume este JSON y para qué. El plan 16-03 lo reescribe para leer `pendiente_de_aprobacion`; el contrato de salida que definís acá es el que ese plan documenta.
    - El `readLandingState` que dejó la Task 1.
  </read_first>

  <files>scripts/setup-landing.ts</files>

  <action>
    `runInspect` sigue siendo **read-only** (no sube nada, no hace `UPDATE`, re-correrlo da el mismo
    output). Lo que cambia es el `resumen` que imprime.

    Sacá la clave `landing_config` del objeto `resumen` y poné, en su lugar, estas cinco:

    - `al_aire`: el `landing_config` crudo de la fila, o `null`. **Es LO PUBLICADO** — lo que ve
      cualquier visitante en `/[slug]` ahora mismo.
    - `pendiente_de_aprobacion`: el `landing_draft` crudo de la fila, o `null`. **Es EL BORRADOR** —
      lo que ve el dueño al abrir su editor, y lo que todavía NO salió al aire.
    - `nunca_publico`: `boolean` (de `readLandingState().nuncaPublico`). Si es `true`, ese negocio
      todavía muestra su página de reservas simple en `/[slug]`.
    - `tiene_cambios_sin_publicar`: `boolean` (de `readLandingState().tieneCambiosSinPublicar`).
    - `partes_sin_publicar`: `string[]` — las partes que difieren, ya pasadas por `formatPart`
      (ej. `["hero","gallery"]`, o `["tema"]`). Va `[]` cuando no hay nada pendiente **o** cuando el
      negocio nunca publicó (`borradorSinComparar`: no hay contra qué comparar).

    Volcá los valores **crudos** de la fila (no los parseados) en `al_aire` / `pendiente_de_aprobacion`:
    el operador tiene que ver lo que hay en la DB, no una versión degradada por el validador fail-safe.

    Además del JSON, imprimí DEBAJO un renglón humano que deje obvio el caso "el dueño tiene cambios
    sin publicar" (es el mismo que dispara el aviso de D-01/D-02):
    - si `tiene_cambios_sin_publicar` y hay partes → `⚠ El dueño tiene cambios sin publicar. Secciones que difieren de lo publicado: hero, gallery. Si seguís, los pisás.` (las partes joineadas con `, `).
    - si `tiene_cambios_sin_publicar` y `borradorSinComparar` → `⚠ El dueño tiene un borrador sin publicar (este negocio NUNCA publicó su web: /[slug] sigue mostrando la reserva simple).`
    - si no hay cambios pendientes y `nunca_publico` es `false` → `✓ Borrador y publicado coinciden: no hay nada pendiente de aprobación.`
    - si no hay borrador y `nunca_publico` es `true` → `· Este negocio no tiene web: ni publicada ni en borrador.`

    Reescribí el comentario de las líneas 150-153 (el que dice que re-correr la escritura sobre-escribe
    el config): el nuevo contrato es que la escritura por defecto **NO toca lo publicado**, escribe el
    borrador, y solo `--publish` reemplaza la web al aire.

    Documentá `--publish` en el bloque de uso/ayuda de `main()` (el `console.error` de las líneas
    369-373), con las tres formas del comando:
    `--inspect <slug>` (read-only) · `--slug <slug> --config <ruta.json>` (escribe el BORRADOR) ·
    `--slug <slug> --config <ruta.json> --publish` (borrador + AL AIRE).
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm test</automated>
    <human-check>Correr `npm run setup:landing -- --inspect &lt;slug&gt;` contra un negocio real y confirmar que el JSON trae `al_aire` y `pendiente_de_aprobacion` por separado, y que el renglón de abajo dice claramente si hay o no cambios sin publicar. (Requiere `.env.local` con el service-role — verificación de operador, va al UAT de fin de fase.)</human-check>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` sale con exit code 0.
    - `npm test` sale con exit code 0.
    - `rg -c "al_aire" scripts/setup-landing.ts` devuelve 1 o más.
    - `rg -c "pendiente_de_aprobacion" scripts/setup-landing.ts` devuelve 1 o más.
    - `rg -c "tiene_cambios_sin_publicar" scripts/setup-landing.ts` devuelve 1 o más.
    - `rg -c "partes_sin_publicar" scripts/setup-landing.ts` devuelve 1 o más.
    - `rg -c "nunca_publico" scripts/setup-landing.ts` devuelve 1 o más.
    - El objeto `resumen` de `runInspect` ya no tiene una clave llamada como la columna publicada: sus dos volcados de config son `al_aire` y `pendiente_de_aprobacion`.
    - `runInspect` no contiene ninguna llamada a `.update(` ni a `.upload(` (sigue read-only).
    - El bloque de uso de `main()` menciona `--publish` y describe las tres formas del comando.
    - El comentario de `runInspect` sobre el efecto de re-correr la escritura fue REESCRITO al contrato nuevo (borrador por defecto), no borrado.
  </acceptance_criteria>

  <done>`--inspect` muestra las dos columnas por separado, dice cuál está al aire y cuál pendiente de aprobación, y deja obvio el caso "el dueño tiene cambios sin publicar". Sigue siendo read-only.</done>
</task>

<task type="auto">
  <name>Task 3: El write path escribe el BORRADOR — gate estricto, aviso de choque y --publish opt-in (SKILL-07 / D-01 / D-02 / D-03 / D-03b)</name>

  <read_first>
    - `scripts/setup-landing.ts` — `runWrite` COMPLETA (líneas 225-337). Leé con atención: el gate de las líneas 304-311 (usa el validador fail-safe de lectura: **ése es el bug**), el `.update()` de las líneas 313-331 y **el comentario de las líneas 315-322** (commit `f98ed6b`, fix de CR-01: describe un incidente REAL). Y la cabecera del archivo (líneas 1-29), que documenta los dos modos y el gate.
    - `lib/landing/write.ts` — `parseLandingConfigForWrite` (devuelve un **discriminated union**: `{ ok: true, data }` | `{ ok: false, error }`, con `error ∈ 'invalid_config' | 'config_too_large'`) y `landingWriteColumns` (del plan 16-01).
    - `app/(dashboard)/web/_landing-actions.ts` — líneas ~100-120: el uso canónico del validador estricto (`const parsed = parseLandingConfigForWrite(input); if (!parsed.ok) return { ok: false, error: parsed.error }`). Copiá esa forma.
    - `lib/landing/editor-draft.ts` — `deriveEditorState`: qué va a ver el dueño en su editor según el par (`landing_config`, `landing_draft`) que deje este script. Es el otro lado del contrato.
    - El `readLandingState` / `hasFlag` / `formatPart` de la Task 1.
  </read_first>

  <files>scripts/setup-landing.ts</files>

  <action>
    Reescribí los pasos 4, 5 y 6 de `runWrite`, y sumá dos pasos nuevos. El orden final de `runWrite`
    queda: 1) resolver `business_id` → 2) leer el estado draft/publicado → **3) AVISO de choque** →
    4) re-hostear imágenes → 5) armar el config → **6) GATE estricto** → **7) pre-print de `--publish`**
    → 8) UPDATE → 9) mensaje de cierre.

    **Firma.** `runWrite` recibe un parámetro nuevo `publish: boolean`. En `main()`, calculalo con
    `hasFlag('publish')` y pasalo. El flag es **opt-in explícito**: nunca por defecto, nunca inferido
    de otra cosa.

    **AVISO DE CHOQUE (D-01/D-02).** Después de resolver el negocio y ANTES de subir una sola imagen,
    llamá a `readLandingState(biz)` e imprimí, si `tieneCambiosSinPublicar`:
    - con partes → exactamente:
      `⚠ El dueño tiene cambios sin publicar. Secciones que difieren de lo publicado: hero, gallery. Si seguís, los pisás.`
      (las partes reales, pasadas por `formatPart`, joineadas con `, `).
    - con `borradorSinComparar` → `⚠ El dueño tiene un borrador sin publicar (este negocio NUNCA publicó su web). Si seguís, lo pisás.`

    **AVISA, NO ABORTA.** El script sigue adelante y escribe. **NO agregues ninguna confirmación
    interactiva** — ni con el módulo nativo de Node ni con una lib de terceros. El repo no tiene una
    sola confirmación interactiva en ningún script y no es el momento de inventar el patrón.
    **El checkpoint humano vive en el SKILL.md (paso 6)**: cuando el proceso Node arranca, ya está
    aprobado. El script solo IMPRIME; quien bloquea es el flujo de la skill (el plan 16-03 cuelga el
    aviso de ese checkpoint). Dejá eso escrito en un comentario arriba del aviso, para que nadie lo
    "arregle" agregando una confirmación en el proceso.

    **GATE ESTRICTO.** Reemplazá el gate actual por el validador de ESCRITURA:
    `const parsed = parseLandingConfigForWrite(landingConfig)`; si `!parsed.ok` → `console.error` con
    el `parsed.error` tal cual (`invalid_config` / `config_too_large`), `process.exitCode = 1`, `return`
    — **sin ningún UPDATE**. De acá en más el config es `parsed.data`, no `parsed`.
    Importá `parseLandingConfigForWrite` y `landingWriteColumns` de `@/lib/landing/write`. Ajustá los
    imports de `@/lib/landing/schema`: el write path deja de necesitar el centinela del config por
    defecto, y `parseLandingConfig` queda importado SOLO por el camino de lectura (`readLandingState`).
    Escribí en el comentario del gate POR QUÉ cambió: el validador de lectura es **fail-safe** (ante un
    `data` de sección inválido degrada esa sección a `{}` en silencio y el operador no se entera); el de
    escritura **rechaza ruidosamente**. En un write path, degradar en silencio es perder la sección
    entera sin que nadie lo vea — es la clase de bug que cerró T-15-16.

    **PRE-PRINT DE `--publish` (D-03).** Si `publish` es `true`, ANTES del UPDATE imprimí qué web está
    por reemplazar, usando el `published` que trajo `readLandingState`:
    - si `nuncaPublico` → `[setup-landing] --publish: este negocio NUNCA publicó — esto es su GO-LIVE (su /<slug> deja de mostrar la reserva simple y pasa a mostrar esta web).`
    - si ya hay web al aire → `[setup-landing] --publish: vas a REEMPLAZAR la web que está al aire.` + un renglón con las secciones activas de `published` y su `theme.preset` / `theme.overrides.palette`.

    **UPDATE.** Una sola llamada, filtrada por el `id` resuelto del slug (NUNCA por slug — Pitfall 6 /
    T-10-07):
    `.from('businesses').update(landingWriteColumns(parsed.data, publish)).eq('id', businessId)`.
    Con `publish=false` eso escribe SOLO el borrador; con `publish=true`, las dos columnas con el MISMO
    objeto (D-03b). La decisión de columnas ya está unit-testeada en `test/landing-write.test.ts`: no
    la re-implementes inline. Ajustá el `console.error` del `updErr` para que nombre lo que realmente
    se intentó escribir.

    **EL COMENTARIO DE `f98ed6b` SE REEMPLAZA, NO SE BORRA.** El bloque de las líneas 315-322 documenta
    un incidente real (escribir solo `landing_config` dejaba el borrador del dueño desincronizado con la
    web VIEJA, y su botón "Publicar" REVERTÍA la web recién armada — pérdida de datos silenciosa, sin
    historial ni undo). Ese incidente **sigue vigente en el camino `--publish`**, y es exactamente la
    razón de D-03b. El comentario nuevo, en español, tiene que:
    1. conservar la explicación del incidente y por qué `--publish` escribe las DOS columnas con el
       mismo objeto parseado;
    2. explicar la bifurcación nueva: por defecto SOLO el borrador (SKILL-07 — la web nace esperando la
       aprobación del dueño), las dos columnas únicamente con `--publish`;
    3. decir por qué el default cambió respecto de Phase 15: el script dejó de ser el único escritor de
       la columna, y publicar sin que nadie mire la web es el bug que este milestone viene a matar.

    **MENSAJE DE CIERRE (bifurcado).** El copy de hoy miente en el camino por defecto: no hay preview
    pública.
    - default (`publish=false`):
      `✓ Borrador (landing_draft) actualizado para "<slug>" (business <id>). NO se tocó lo publicado.`
      + un renglón diciendo que el dueño la revisa y publica desde su panel (`/web`), y otro con el
      comando exacto para publicarla desde acá **solo si el dueño dio el OK** (el mismo comando + `--publish`).
      **NO imprimas una URL de preview pública**: por defecto no hay nada que ver en `/<slug>` (el
      preview compartible por link está explícitamente Out of Scope en REQUIREMENTS.md).
    - `--publish`:
      `✓ Publicado: landing_config + landing_draft actualizados para "<slug>" (business <id>).`
      + la URL de `/<slug>` como HOY (es `force-dynamic` → inmediata, sin deploy).

    **CABECERA DEL ARCHIVO.** Actualizá el bloque de comentario de las líneas 1-29 (`QUÉ hace — DOS
    modos` + `USO`): el modo escritura ya no "sobre-escribe el `landing_config`", escribe el borrador;
    el gate es el estricto; existe `--publish`. El párrafo de POR QUÉ es un script local y NO un
    endpoint web (D10-01 / SKILL-04) **se conserva intacto**: es un invariante, no una preferencia.
  </action>

  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm test</automated>
    <automated>npm run lint</automated>
    <human-check>Contra un negocio real con `.env.local`: (1) correr el script SIN `--publish` sobre un negocio ya publicado → su `/[slug]` sigue mostrando la web VIEJA, y `--inspect` muestra la nueva en `pendiente_de_aprobacion`; (2) correrlo sobre un negocio que nunca publicó → `/[slug]` sigue mostrando la reserva simple; (3) correrlo con `--publish` → la web sale al aire y el editor del dueño abre en `✓ Publicado`. Verificación de operador (service-role + DB real): va al UAT de fin de fase.</human-check>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` sale con exit code 0.
    - `npm test` sale con exit code 0.
    - `npm run lint` sale con exit code 0.
    - `rg -c "parseLandingConfigForWrite" scripts/setup-landing.ts` devuelve 1 o más.
    - `rg -c "landingWriteColumns" scripts/setup-landing.ts` devuelve 1 o más.
    - `rg -c "hasFlag\('publish'\)" scripts/setup-landing.ts` devuelve 1.
    - `rg -c "\.update\(" scripts/setup-landing.ts` devuelve exactamente **1** (una sola llamada de escritura en todo el script) y su argumento es el resultado de `landingWriteColumns(parsed.data, publish)`.
    - `rg -c "\.eq\('id', businessId\)" scripts/setup-landing.ts` devuelve 1 (el UPDATE sigue filtrado por id, nunca por slug).
    - `rg -c "El dueño tiene cambios sin publicar" scripts/setup-landing.ts` devuelve 1.
    - `rg -c "^import" scripts/setup-landing.ts` — la lista de imports no incorpora ningún módulo de confirmación interactiva (`node:readline` / `inquirer` / `prompts`): `rg -c "node:readline|inquirer" scripts/setup-landing.ts` devuelve 0.
    - `git diff package.json package-lock.json` está vacío (cero deps npm nuevas).
    - El comentario del `.update()` menciona `f98ed6b` (o describe su incidente) Y explica la bifurcación default/`--publish`. El comentario viejo fue REEMPLAZADO, no borrado: `git diff scripts/setup-landing.ts` muestra que el bloque explicativo sigue existiendo, reescrito.
    - La cabecera del archivo sigue conteniendo el párrafo de por qué es un script local y no un endpoint web (D10-01 / SKILL-04).
    - El mensaje de cierre del camino por defecto NO imprime una URL de `/<slug>` como preview.
  </acceptance_criteria>

  <done>Corriendo el script sin flags se escribe SOLO `landing_draft`; con `--publish` se escriben las dos columnas con el mismo objeto validado. El gate es estricto y aborta ruidosamente. El aviso de choque se imprime antes de tocar nada. El comentario de `f98ed6b` está reescrito, no perdido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| máquina del operador (Node, service-role) → fila `businesses` | El script bypassa RLS por diseño. La única frontera es el checkpoint humano del SKILL.md. Invariante v0.10 (D10-01 / SKILL-04): NO se mueve a un endpoint web, NO se le agrega auth. |
| operador (`--publish`) → público (`/[slug]`) | Escribir `landing_config` pone contenido delante de TODO visitante, sin deploy (`force-dynamic`). Es un camino directo del operador a producción. |
| editor del dueño (`landing_draft`) ↔ script del operador (`landing_draft`) | **Dos escritores sobre la misma columna.** Riesgo de pisar el trabajo sin publicar del dueño en silencio (CR-01 con los roles invertidos). |
| payload JSON del operador → `businesses.landing_config` (jsonb) | El `data` de cada sección viaja al render (y a cualquier sink futuro). Un `javascript:` en `map_url` no puede llegar a la DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering (regresión de producto — la del Threat note) | `runWrite` → `.update()` | mitigate | El UPDATE por defecto usa `landingWriteColumns(parsed.data, false)`, que NO incluye la clave `landing_config`. Un negocio ya publicado NO puede ser pisado por el camino default. Unit-testeado en `test/landing-write.test.ts` (16-01) + assertion de fuente: hay **una sola** llamada `.update(` en el script y su argumento es `landingWriteColumns(...)`. |
| T-16-02 | Tampering (pérdida silenciosa del trabajo del dueño) | `runWrite` → aviso de choque | mitigate | Antes de subir una sola imagen, `readLandingState` + `diffConfigParts` detectan que el dueño tiene cambios sin publicar y el script imprime QUÉ partes difieren. El operador decide con el dato a la vista, en el checkpoint humano del SKILL.md (paso 6). D-01: avisa, no aborta — el caso limpio no paga fricción. |
| T-16-03 | Tampering (config hostil / inválido persistido) | GATE de `runWrite` | mitigate | El gate pasa del validador **fail-safe de lectura** (degrada una sección inválida a `{}` EN SILENCIO) al **estricto de escritura** `parseLandingConfigForWrite` (reject-on-invalid: `invalid_config` / `config_too_large`, tope 256 KB, `data` validado por tipo de sección con las variantes `Strict`, allowlist de protocolo en TODAS las URLs). Config inválido → `process.exitCode = 1` y CERO UPDATE. Cierra la regresión de clase de T-15-16 en el único write path que quedaba afuera. |
| T-16-04 | Elevation of Privilege (operador → producción) | flag `--publish` | mitigate | `--publish` es opt-in explícito vía `hasFlag('publish')` (token exacto en `argv`): nunca por defecto, nunca inferido. Antes de publicar, el script imprime qué web está por reemplazar (o avisa que es un GO-LIVE). El SKILL.md (16-03) lo documenta como "solo con el OK del dueño". |
| T-16-05 | Elevation of Privilege (service-role) | `createClient(url, serviceKey)` | mitigate | **Sin cambios y verificado no debilitado:** el script sigue corriendo en Node, FUERA del runtime web, con `.env.local` local; no se agrega ningún endpoint, Server Action ni ruta. El UPDATE sigue filtrado por el `id` resuelto del slug (`.eq('id', businessId)`), nunca por slug (Pitfall 6 / T-10-07). El párrafo de la cabecera que declara este invariante se conserva intacto. |
| T-16-06 | Information Disclosure | salida de `--inspect` en la terminal del operador | accept | `--inspect` vuelca `landing_config` y `landing_draft`: copy, URLs públicas de Storage y tema. El config **nunca** contiene campos sensibles (guardrail del SKILL.md: "NUNCA campos sensibles en el config ni en la vista `public_businesses`") y el `select` no trae ninguna columna de secretos. Riesgo bajo, contenido en la máquina del operador. |
| T-16-07 | Denial of Service (auto-infligido) | tamaño del config | mitigate | `parseLandingConfigForWrite` aplica `MAX_CONFIG_BYTES = 256 KB` sobre el config ya normalizado. `/[slug]` es `force-dynamic` (lee el config en CADA request): un jsonb inflado se paga en cada visita. Un payload gigante del operador ahora rebota con `config_too_large` en vez de persistirse. |
| T-16-SC | Tampering (supply chain) | npm installs | mitigate | **Este plan instala CERO paquetes.** `hasFlag` son 3 líneas de `argv`; el prompt interactivo se **descarta explícitamente** (no existe ningún `readline`/`prompts`/`inquirer` en el repo — el checkpoint humano vive en el SKILL.md). Acceptance criterion: `git diff package.json package-lock.json` vacío. El Package Legitimacy Gate no aplica porque no hay tareas de instalación. |
</threat_model>

<verification>
```bash
npx tsc --noEmit          # 0
npm test                  # 0 — suite completa verde
npm run lint              # 0
```

Assertions de fuente sobre `scripts/setup-landing.ts`:
- `rg -c "\.update\(" scripts/setup-landing.ts` → **1** (una sola escritura, y usa `landingWriteColumns`).
- `rg -n "parseLandingConfigForWrite|landingWriteColumns|diffConfigParts|hasFlag" scripts/setup-landing.ts` → los 4 símbolos presentes.
- `rg -n "al_aire|pendiente_de_aprobacion|tiene_cambios_sin_publicar|partes_sin_publicar|nunca_publico" scripts/setup-landing.ts` → las 5 claves de `--inspect`.
- `rg -c "node:readline|inquirer" scripts/setup-landing.ts` → 0 (no se agregó confirmación interactiva).
- `git diff package.json package-lock.json` → vacío.
- `git diff --name-only` → solo `scripts/setup-landing.ts`.

Verificación de operador (va al UAT de fin de fase, `/gsd:verify-work 16` — requiere `.env.local` con service-role y un negocio real):
1. Negocio YA publicado, script sin `--publish` → `/[slug]` sigue mostrando la web VIEJA; `--inspect` muestra la nueva en `pendiente_de_aprobacion` (SC1 del ROADMAP).
2. El dueño abre su editor, ve el borrador del operador, toca Publicar → recién ahí sale al aire (SC2).
3. `--inspect` distingue al aire vs pendiente de aprobación (SC3).
4. Segunda corrida sobre un negocio cuyo dueño guardó cambios sin publicar → el aviso `⚠ El dueño tiene cambios sin publicar…` lista las partes correctas.
5. `--publish` sobre un negocio nuevo → GO-LIVE: `/[slug]` deja de mostrar la reserva simple; el editor del dueño abre en `✓ Publicado` (no en un falso "sin publicar").
</verification>

<success_criteria>
- [ ] `resolveBusiness` trae `landing_draft`; el script LEE la columna que hasta hoy escribía a ciegas.
- [ ] `hasFlag('publish')` existe y `--publish` es opt-in explícito.
- [ ] Por defecto el UPDATE escribe SOLO `landing_draft` (vía `landingWriteColumns(parsed.data, false)`).
- [ ] Con `--publish` escribe las dos columnas con el MISMO objeto (`parsed.data`).
- [ ] El gate de escritura usa `parseLandingConfigForWrite` y aborta con `invalid_config` / `config_too_large` sin tocar la fila.
- [ ] El aviso de choque (D-01/D-02) se imprime antes de subir/escribir nada, listando las partes que difieren. Avisa, NO aborta.
- [ ] `--inspect` vuelca `al_aire` y `pendiente_de_aprobacion` por separado + el flag de cambios sin publicar.
- [ ] El comentario de `f98ed6b` fue REEMPLAZADO (conserva el incidente, agrega la bifurcación), no borrado.
- [ ] El mensaje de cierre por defecto no promete una preview pública que no existe.
- [ ] Cero deps npm nuevas, cero prompts interactivos, cero endpoints, cero migraciones.
</success_criteria>

<output>
Create `.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-02-SUMMARY.md` when done.
</output>
