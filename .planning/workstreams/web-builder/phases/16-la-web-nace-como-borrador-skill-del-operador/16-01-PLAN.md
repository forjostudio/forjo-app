---
phase: 16-la-web-nace-como-borrador-skill-del-operador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/landing/editor-draft.ts
  - lib/landing/write.ts
  - test/landing-editor-draft.test.ts
  - test/landing-write.test.ts
autonomous: true
requirements: [SKILL-07, SKILL-08]

must_haves:
  truths:
    - "Dados dos configs (borrador y publicado), una función pura dice EXACTAMENTE qué partes difieren (secciones + tema + movimiento), inmune al reordenamiento de claves del jsonb."
    - "Dado un config y el flag publish=false, una función pura devuelve un payload de UPDATE que contiene SOLO la columna landing_draft — nunca landing_config."
    - "Dado un config y el flag publish=true, esa misma función devuelve las DOS columnas con el MISMO objeto (draft === published por referencia)."
  artifacts:
    - path: "lib/landing/editor-draft.ts"
      provides: "diffConfigParts() + tipo ConfigPart — el compare canónico por parte que D-01/D-02 y --inspect (D-05) necesitan"
      exports: ["diffConfigParts", "ConfigPart"]
    - path: "lib/landing/write.ts"
      provides: "landingWriteColumns() — la decisión draft-only vs draft+published, unit-testeable (SKILL-07)"
      exports: ["landingWriteColumns"]
    - path: "test/landing-editor-draft.test.ts"
      provides: "cobertura de diffConfigParts: secciones, tema, movimiento, reordenamiento de claves, normalización"
      contains: "diffConfigParts"
    - path: "test/landing-write.test.ts"
      provides: "cobertura de landingWriteColumns: el default NO trae landing_config (la regresión que mata la fase)"
      contains: "landingWriteColumns"
  key_links:
    - from: "lib/landing/editor-draft.ts"
      to: "lib/landing/editor-draft.ts (canonical, privada)"
      via: "diffConfigParts reusa el serializador canónico privado del módulo — NO se exporta canonical, NO se usa un JSON.stringify crudo"
      pattern: "canonical\\("
    - from: "test/landing-write.test.ts"
      to: "lib/landing/write.ts"
      via: "import { landingWriteColumns } from '@/lib/landing/write'"
      pattern: "landingWriteColumns"
---

<objective>
Poner en la capa PURA (y por lo tanto testeada) las dos decisiones que sostienen toda la fase, antes
de que el script las consuma:

1. **`diffConfigParts(a, b)`** — qué partes de un config difieren de otro, con el compare **canónico**
   de Phase 15 (inmune al reordenamiento de claves que Postgres hace en el `jsonb`). Es el motor del
   aviso de choque operador↔dueño (D-01/D-02) y del flag de `--inspect` (D-05).
2. **`landingWriteColumns(config, publish)`** — qué columnas escribe el script. Por defecto **SOLO
   `landing_draft`**; con `--publish`, las dos con el MISMO objeto parseado (D-03 / D-03b).

Purpose: `scripts/setup-landing.ts` NO es unit-testeable (side-effects, `process.argv`, service-role).
Si la lógica que decide "no pisar lo publicado" vive inline en el script, la regresión #1 de esta fase
(T-16-01: pisar la web al aire de un negocio publicado) **no tiene ni un solo test que la agarre**.
Extraerla a `lib/landing/write.ts` la vuelve una assertion de Vitest.

Output: 2 funciones exportadas nuevas + sus tests. Cero deps npm nuevas, cero migraciones, cero
superficie web nueva.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-builder/STATE.md
@.planning/workstreams/web-builder/ROADMAP.md
@.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-CONTEXT.md
@.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-PATTERNS.md
@lib/landing/editor-draft.ts
@lib/landing/write.ts
@lib/landing/schema.ts
</context>

## Artifacts this phase produces (plan 16-01)

| Símbolo | Dónde | Tipo | Nuevo/Modificado |
|---|---|---|---|
| `diffConfigParts(a: LandingConfig, b: LandingConfig): ConfigPart[]` | `lib/landing/editor-draft.ts` | export nuevo | NUEVO |
| `type ConfigPart = SectionType \| 'theme' \| 'motion'` | `lib/landing/editor-draft.ts` | export nuevo | NUEVO |
| `landingWriteColumns(config: LandingConfig, publish: boolean)` | `lib/landing/write.ts` | export nuevo | NUEVO |
| tests de `diffConfigParts` | `test/landing-editor-draft.test.ts` | describe nuevo | MODIFICADO |
| tests de `landingWriteColumns` | `test/landing-write.test.ts` | describe nuevo | MODIFICADO |

NO se crea ningún archivo nuevo. NO se agrega ninguna dependencia. `canonical` sigue siendo **privada**.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: diffConfigParts — el compare canónico POR PARTE (D-01 / D-02 / D-05)</name>

  <read_first>
    - `lib/landing/editor-draft.ts` — LEER ENTERO. Especialmente el bloque de comentario de las líneas 191-213 (por qué `canonical` existe y por qué un `JSON.stringify` crudo NO sirve) y `normalizeSections` (líneas 33-49). `canonical` es **privada** (línea 214) y así se queda.
    - `lib/landing/schema.ts` — `SECTION_TYPES` (línea 8: `['hero','about','services','gallery','location','hours','cta','booking']`) y el tipo `LandingConfig`.
    - `test/landing-editor-draft.test.ts` — líneas 1-35: el estilo de import, el helper `baseConfig()` y la convención `describe('<fn> (<qué prueba en español>)')`.
    - `.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-PATTERNS.md` — §"Compare canónico (D-01/D-02)".
  </read_first>

  <files>lib/landing/editor-draft.ts, test/landing-editor-draft.test.ts</files>

  <behavior>
    - Dos configs idénticos → `[]`.
    - El mismo config con las claves de un objeto en distinto orden (lo que devuelve Postgres tras el round-trip por `jsonb`) → `[]`. Este es el test que prueba que se usa `canonical` y no un stringify crudo.
    - Cambia `hero.data.headline` → `['hero']`.
    - Cambian `hero.data.headline` y `gallery.data.images` → `['hero', 'gallery']`, en ese orden (orden de `SECTION_TYPES`).
    - Se togglea `enabled` de `about` → `['about']`.
    - Se reordena hero↔about (cambian sus `order`) → `['hero', 'about']` (los dos, en orden de `SECTION_TYPES`).
    - Cambia `theme.overrides.palette` → `['theme']`.
    - Cambia `theme.preset` → `['theme']`.
    - Cambia `motion` de `'premium'` a `'subtle'` → `['motion']`.
    - Config A con 5 secciones (el que emite `buildLandingConfig`, que OMITE las vacías) vs config B con las 8 secciones materializadas por `normalizeSections` (el que emite el editor del dueño) y sin ninguna otra diferencia semántica → `[]`. **Éste es el test anti-falso-positivo**: la mera materialización de secciones NO es un cambio del dueño.
    - El orden del array de retorno es determinista: primero las secciones en orden de `SECTION_TYPES`, después `'theme'`, después `'motion'`.
    - `diffConfigParts` es PURA: no muta ninguno de los dos argumentos (comprobar con un snapshot `structuredClone` antes/después).
  </behavior>

  <action>
    Agregá al final de `lib/landing/editor-draft.ts` (módulo PURO — sin React, sin Supabase, sin `'use client'`/`'use server'`, named exports) dos símbolos nuevos:

    `export type ConfigPart = SectionType | 'theme' | 'motion'` (donde `SectionType` es el alias local ya declarado en la línea 19 sobre `SECTION_TYPES`).

    `export function diffConfigParts(a: LandingConfig, b: LandingConfig): ConfigPart[]`.

    Implementación exacta:
    - Normalizá AMBOS configs con `normalizeSections` antes de comparar. Motivo (documentalo en el comentario de cabecera de la función): `buildLandingConfig` omite las secciones vacías (emite 5) y el editor del dueño las materializa a 8 con `enabled:false`. Sin normalizar, un negocio cuyo dueño solo abrió el editor y guardó —sin cambiar nada visible— dispararía el aviso de choque con TODAS las secciones materializadas listadas. Sería ruido: el aviso perdería su señal justo en el caso que D-01 quiere cubrir.
    - Para cada `type` de `SECTION_TYPES`, tomá la sección de cada config normalizado y compará el objeto sección COMPLETO (`type`, `enabled`, `order`, `data`) serializando con el `canonical` PRIVADO del módulo: `JSON.stringify(canonical(sa)) !== JSON.stringify(canonical(sb))` → la parte difiere. `order` cuenta: si el dueño reordenó, eso es un cambio sin publicar.
    - Después: `theme` (compará `canonical(a.theme)` vs `canonical(b.theme)`) y `motion` (compará con `!==`; puede ser `undefined` en configs viejos y eso es un valor válido de comparación).
    - NO exportes `canonical`. NO uses `JSON.stringify` crudo sobre objetos que vengan de la DB: Postgres reordena las claves del `jsonb` y el compare daría "distinto" siempre. Ese es el bug más probable de la fase y el type-check NO lo agarra.

    Escribí el comentario de cabecera de la función en **español**, explicando el POR QUÉ (convención del repo): por qué normaliza, por qué usa `canonical`, y para qué la consume el script (aviso de choque D-01/D-02 + flag de `--inspect` D-05).

    En `test/landing-editor-draft.test.ts` agregá un `describe('diffConfigParts (qué partes del config difieren entre borrador y publicado)')` con un `it` por cada línea de `<behavior>`. Reusá el helper `baseConfig()` que ya está en el archivo. Para el caso del reordenamiento de claves del `jsonb`, construí el config "de la DB" reordenando las claves a mano (ej. `{ order, type, enabled }` en vez de `{ type, enabled, order }`), no con un mock de Supabase.
  </action>

  <verify>
    <automated>npm test -- test/landing-editor-draft.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` sale con exit code 0.
    - `npm test` sale con exit code 0 (la suite completa sigue verde: `test/landing-editor-draft.test.ts` ya tenía ~517 líneas de tests que NO deben romperse).
    - `rg -c "export function diffConfigParts" lib/landing/editor-draft.ts` devuelve 1.
    - `rg -c "export type ConfigPart" lib/landing/editor-draft.ts` devuelve 1.
    - `rg -c "^export function canonical|^export const canonical" lib/landing/editor-draft.ts` devuelve 0 (sigue privada).
    - `rg -c "diffConfigParts" test/landing-editor-draft.test.ts` devuelve 1 o más.
    - Existe un test que pasa dos configs semánticamente idénticos con las claves de un objeto en distinto orden y espera `[]`.
    - Existe un test que pasa un config de 5 secciones y otro de 8 (mismas secciones activas, mismo data) y espera `[]`.
    - Existe un test que cambia solo `theme.overrides.palette` y espera `['theme']`.
    - Existe un test que cambia solo `motion` y espera `['motion']`.
  </acceptance_criteria>

  <done>`diffConfigParts` está exportada, es pura, usa el compare canónico, normaliza ambos lados, y sus tests (incluidos el del reorden de claves y el anti-falso-positivo de normalización) pasan con la suite entera verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: landingWriteColumns — draft-only por defecto, las dos columnas solo con --publish (SKILL-07, D-03/D-03b)</name>

  <read_first>
    - `lib/landing/write.ts` — LEER ENTERO. Es el módulo del **contrato de ESCRITURA** (`parseLandingConfigForWrite`, `SECTION_DATA_SCHEMAS`, `MAX_CONFIG_BYTES`). Su cabecera explica por qué es PURO a propósito (sin `'use server'`): para ser importable en Vitest.
    - `app/(dashboard)/web/_landing-actions.ts` — el OTRO lado del contrato: `saveLandingDraft` hace `.update({ landing_draft: parsed.data })` y `publishLanding()` hace `.update({ landing_config: parsed.data })` dejando el borrador intacto. El script NO llama a estas acciones (es service-role, fuera del runtime web), pero su semántica de columnas es la que hay que replicar.
    - `test/landing-write.test.ts` — líneas 1-30: import style, el helper `wrap()` y la convención de `describe`.
    - `scripts/setup-landing.ts` — líneas 313-336: el `.update({ landing_config: parsed, landing_draft: parsed })` de HOY y el comentario del commit `f98ed6b` que lo justifica. Esta función es la que lo va a reemplazar (el reemplazo del comentario se hace en el plan 16-02, NO acá).
  </read_first>

  <files>lib/landing/write.ts, test/landing-write.test.ts</files>

  <behavior>
    - `landingWriteColumns(cfg, false)` → `Object.keys(...)` es exactamente `['landing_draft']`. `'landing_config' in resultado` es `false`. **Ésta es la assertion que mata T-16-01** (el script pisando la web al aire de un negocio ya publicado).
    - `landingWriteColumns(cfg, false).landing_draft` es el MISMO objeto que `cfg` (`toBe`, identidad por referencia).
    - `landingWriteColumns(cfg, true)` → tiene AMBAS claves: `landing_draft` y `landing_config`.
    - `landingWriteColumns(cfg, true).landing_draft` y `.landing_config` son el MISMO objeto (`toBe`, identidad por referencia) — es el invariante D-03b: post-`--publish` las dos columnas quedan byte-idénticas y el dueño abre su editor en `✓ Publicado`, no en un falso "sin publicar".
    - La función es pura: no muta `cfg`.
  </behavior>

  <action>
    Agregá al final de `lib/landing/write.ts` un export nuevo:

    `export function landingWriteColumns(config: LandingConfig, publish: boolean): { landing_draft: LandingConfig; landing_config?: LandingConfig }`

    Cuerpo: si `publish` es `true` devolvé `{ landing_draft: config, landing_config: config }` (el MISMO objeto en las dos claves — identidad por referencia, no un clon); si es `false` devolvé `{ landing_draft: config }` y **nada más** (la clave `landing_config` NO debe existir en el objeto, ni siquiera como `undefined`: PostgREST manda las claves presentes y un `undefined` explícito es una fuente de bugs).

    Escribí un comentario de cabecera en **español** explicando el POR QUÉ (convención del repo). Tiene que decir, sin ambigüedad:
    - Que ésta es la decisión de SKILL-07: la web que arma el operador **nace como borrador** y NO sale al aire hasta que el dueño la mira y publica desde su panel.
    - Que el default es draft-only y que publicar es **opt-in explícito** (`--publish`), **nunca por defecto, nunca silencioso**.
    - Que en el camino `--publish` se escriben las DOS columnas con el MISMO objeto parseado a propósito (D-03b): si se escribiera solo `landing_config`, el borrador del dueño quedaría desincronizado con la web VIEJA y su botón "Publicar" —que es exactamente lo que la barra le pide tocar— REVERTIRÍA la web que el operador acaba de publicar. No hay historial ni undo: sería pérdida de datos silenciosa. Ése es el incidente que documentó el commit `f98ed6b` y **sigue vigente en el camino `--publish`**.
    - Que vive acá (módulo puro) y no inline en `scripts/setup-landing.ts` porque el script no es unit-testeable: sin esta extracción, la regresión "el script pisa lo publicado" no tendría ni un test que la agarre.

    En `test/landing-write.test.ts` agregá un `describe('landingWriteColumns (qué columnas escribe el operador según --publish)')` con un `it` por cada línea de `<behavior>`. Importá `landingWriteColumns` del mismo `@/lib/landing/write` que ya importa el archivo. Para el config de prueba usá un `LandingConfig` válido (podés parsear uno con `parseLandingConfigForWrite` y tomar su `.data`, o construirlo a mano con `theme` + `sections`).
  </action>

  <verify>
    <automated>npm test -- test/landing-write.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` sale con exit code 0.
    - `npm test` sale con exit code 0 (la suite completa verde; `test/landing-write.test.ts` ya tenía ~300 líneas que NO deben romperse).
    - `rg -c "export function landingWriteColumns" lib/landing/write.ts` devuelve 1.
    - `rg -c "landingWriteColumns" test/landing-write.test.ts` devuelve 1 o más.
    - Existe un test que asserta `Object.keys(landingWriteColumns(cfg, false))` igual a `['landing_draft']`.
    - Existe un test que asserta `'landing_config' in landingWriteColumns(cfg, false)` es `false`.
    - Existe un test que asserta, para `publish: true`, que `.landing_draft` y `.landing_config` son el mismo objeto (`toBe`).
    - El comentario de cabecera de `landingWriteColumns` menciona `f98ed6b` y explica por qué `--publish` escribe las dos columnas.
  </acceptance_criteria>

  <done>`landingWriteColumns` está exportada de `lib/landing/write.ts`, su default no incluye la clave `landing_config`, el camino `--publish` devuelve las dos columnas con el mismo objeto, y los tests lo prueban con la suite entera verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| máquina del operador (Node, service-role) → fila `businesses` | El script bypassa RLS por diseño. La única frontera es el checkpoint humano del SKILL.md. Invariante de v0.10 (D10-01 / SKILL-04): NO se mueve a un endpoint web. |
| operador (`--publish`) → público (`/[slug]`) | Escribir `landing_config` pone contenido delante de TODO visitante, sin deploy (`force-dynamic`). |
| editor del dueño (`landing_draft`) ↔ script del operador (`landing_draft`) | **Dos escritores sobre la misma columna** — riesgo de pisar el trabajo del dueño en silencio. Es la clase de bug de CR-01 (Phase 15) con los roles invertidos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering (regresión de producto) | `lib/landing/write.ts` → `landingWriteColumns` | mitigate | El default (`publish=false`) devuelve un payload de UPDATE **sin la clave `landing_config`**. Verificado por unit test (`Object.keys(...) === ['landing_draft']` + `'landing_config' in r === false`) en `test/landing-write.test.ts`. Sin esta extracción, la regresión "el script pisa la web al aire de un negocio ya publicado" no tendría ningún test que la agarre. |
| T-16-02 | Tampering (pérdida silenciosa) | `lib/landing/editor-draft.ts` → `diffConfigParts` | mitigate | Es el motor del aviso de choque (D-01/D-02): sin él, el operador pisaría el borrador del dueño sin enterarse. Se apoya en el compare **canónico** (`canonical`, privada) — un `JSON.stringify` crudo daría "distinto" siempre (Postgres reordena las claves del `jsonb`) y el aviso se volvería ruido que el operador aprende a ignorar. Test explícito del reorden de claves. |
| T-16-03 | Tampering (falso positivo → aviso ignorado) | `diffConfigParts` | mitigate | Ambos configs se normalizan (`normalizeSections`) antes de comparar: la mera materialización de secciones que hace el editor (5→8) NO cuenta como cambio del dueño. Test anti-falso-positivo explícito. |
| T-16-04 | Tampering (invariante del editor) | `landingWriteColumns(cfg, true)` | mitigate | Devuelve el MISMO objeto en las dos claves (identidad por referencia): post-`--publish` `landing_draft === landing_config` byte a byte, y `deriveEditorState` le muestra al dueño `✓ Publicado` en vez de un falso "Guardado — sin publicar" cuyo botón Publicar revertiría la web recién publicada (incidente `f98ed6b`). Test `toBe` explícito. |
| T-16-05 | Elevation of Privilege | service-role del script | mitigate | Este plan NO toca el script ni abre ninguna superficie: solo agrega funciones PURAS a `lib/landing/*` (sin Supabase, sin React, sin `'use server'`). El invariante "service-role fuera del runtime web" queda intacto. |
| T-16-SC | Tampering (supply chain) | npm installs | mitigate | **Este plan instala CERO paquetes.** No hay tareas de `npm install`, por lo que el Package Legitimacy Gate no aplica (`tsx`/`vitest`/`zod` ya están en el repo). Si el ejecutor cree necesitar una dep nueva, tiene que parar: es señal de que se desvió del plan. |
</threat_model>

<verification>
```bash
npx tsc --noEmit          # 0
npm test                  # 0 — suite completa verde, sin regresiones
npm run lint              # 0
```

Assertions de fuente:
- `rg -n "export function diffConfigParts|export type ConfigPart" lib/landing/editor-draft.ts` → 2 matches.
- `rg -n "export function landingWriteColumns" lib/landing/write.ts` → 1 match.
- `rg -n "^export .*canonical" lib/landing/editor-draft.ts` → 0 matches (`canonical` sigue privada).
- `git diff --name-only` NO lista `scripts/setup-landing.ts` ni `.claude/skills/forjo-web-builder/SKILL.md` (esos son 16-02 y 16-03).
- `git diff package.json` está vacío (cero deps nuevas).
</verification>

<success_criteria>
- [ ] `diffConfigParts(a, b): ConfigPart[]` exportada de `lib/landing/editor-draft.ts`, pura, normaliza ambos lados y usa el `canonical` privado.
- [ ] `canonical` sigue sin exportarse.
- [ ] `landingWriteColumns(config, publish)` exportada de `lib/landing/write.ts`; `publish=false` NO incluye la clave `landing_config`.
- [ ] Tests nuevos en `test/landing-editor-draft.test.ts` y `test/landing-write.test.ts` cubriendo todos los `<behavior>`.
- [ ] `npm test` y `npx tsc --noEmit` en 0, sin romper ningún test preexistente.
- [ ] Cero archivos nuevos, cero deps nuevas, cero migraciones, cero superficie web.
</success_criteria>

<output>
Create `.planning/workstreams/web-builder/phases/16-la-web-nace-como-borrador-skill-del-operador/16-01-SUMMARY.md` when done.
</output>
