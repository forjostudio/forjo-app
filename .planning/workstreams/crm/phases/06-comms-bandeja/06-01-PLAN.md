---
phase: 06-comms-bandeja
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/038_conversations_messages.sql
  - lib/conversations.ts
  - lib/conversations.test.ts
  - lib/agent-context.ts
  - lib/agent-context.test.ts
  - app/api/agent/inbox/route.ts
  - app/api/agent/context/route.ts
  - app/api/agent/inbox/state/route.ts
  - app/(crm)/admin/bandeja/actions.ts
  - app/(crm)/admin/bandeja/_bandeja-actions.schemas.ts
  - lib/crm-timeline.ts
autonomous: false
requirements: [COMMS-01, COMMS-02]
user_setup:
  - service: whatsapp-ai-agent-kit (otro repo, VPS)
    why: "El bot sincroniza conversaciones a Forjo vía el endpoint de ingest. La sync real es Deferred (otro repo); Forjo solo expone los endpoints listos."
    env_vars:
      - name: FORJO_AGENT_TOKEN
        source: "Generar un secreto fuerte y setearlo en Vercel (server-only, NO NEXT_PUBLIC_). El mismo valor va en el .env.local del bot. Sin él, el ingest responde 401 (fail-closed correcto)."

must_haves:
  truths:
    - "El bot puede POSTear un mensaje al endpoint de ingest con FORJO_AGENT_TOKEN y queda persistido en conversations/messages"
    - "Un POST de ingest sin token válido recibe 401 (fail-closed)"
    - "El mismo external_id POSTeado dos veces NO duplica el mensaje (idempotencia)"
    - "El tenant del ingest se resuelve del slug validado, nunca del body"
    - "El bot puede GET /api/agent/context?slug= y recibe name/slug/address/mapsUrl/bookingUrl + services + hours + notes"
    - "El bot puede GET /api/agent/inbox/state?slug=&phone= y recibe el handled_by de esa conversación para saber si debe pausar"
    - "El operador puede tomar una conversación (takeConversation) → handled_by pasa a 'human', queda auditado, y el bot lo lee vía el state endpoint"
    - "El dueño (sesión, RLS) ve SOLO conversaciones de su business_id; el operador is_admin ve todas"
  artifacts:
    - path: "supabase/migrations/038_conversations_messages.sql"
      provides: "Tablas conversations/messages + RLS mixta owner OR is_admin + índices de idempotencia"
      contains: "create table"
    - path: "lib/conversations.ts"
      provides: "matchEntity (lead por phone/email), schema zod del ingest, validez de transición handled_by"
    - path: "lib/agent-context.ts"
      provides: "mapeo puro business_hours→hours[] y services→[] del context endpoint"
    - path: "app/api/agent/inbox/route.ts"
      provides: "Ingest POST del bot (token fail-closed + slug→tenant + upsert idempotente)"
      exports: ["POST"]
    - path: "app/api/agent/context/route.ts"
      provides: "Context read-only por slug para el prompt del bot"
      exports: ["GET"]
    - path: "app/api/agent/inbox/state/route.ts"
      provides: "Estado handled_by por slug+phone para el polling de pausa del bot"
      exports: ["GET"]
    - path: "app/(crm)/admin/bandeja/actions.ts"
      provides: "takeConversation + releaseConversation (server actions auditadas)"
      exports: ["takeConversation", "releaseConversation"]
  key_links:
    - from: "app/api/agent/inbox/route.ts"
      to: "supabase/migrations/038_conversations_messages.sql"
      via: "upsert sobre conversations (onConflict business_id,channel,contact_phone) y messages (onConflict external_id) vía createAdminClient"
      pattern: "from\\('conversations'\\)|from\\('messages'\\)"
    - from: "app/(crm)/admin/bandeja/actions.ts"
      to: "lib/audit.ts"
      via: "logAudit('conversation.takeover') tras mutar handled_by"
      pattern: "logAudit"
    - from: "app/api/agent/inbox/route.ts"
      to: "lib/conversations.ts"
      via: "inboundSchema.safeParse(raw) — shape del bot validado con zod"
      pattern: "inboundSchema|safeParse"
  prohibitions:
    - "NINGUNA policy con using(true) ni write policy para usuarios — solo service-role escribe conversations/messages"
    - "NUNCA createAdminClient en page.tsx/*-client.tsx (esa es la lectura del operador — va en 06-02 con session client)"
    - "El business_id del ingest NUNCA sale del body del POST — siempre del slug validado"
    - "Auth del ingest fail-CLOSED: secreto ausente → 401 (no fail-open if (secret))"
    - "Sin envío manual saliente (composer 'send') — DIFERIDO (D-03). NO planear el endpoint send."
    - "Sin tab/canal Email ni mail — COMMS-03 DIFERIDO a v2 (D-01)."
---

<objective>
Construye TODO el lado-Forjo de la bandeja de WhatsApp listo para que el bot externo sincronice: la capa de datos (migración 038 con `conversations`/`messages` + RLS mixta owner-OR-admin), las libs puras testeables (`lib/conversations.ts`, `lib/agent-context.ts`), los tres endpoints del agente (ingest POST, context GET, state GET), y la server action de takeover auditada. La UI de la bandeja (la superficie del operador) llega en 06-02, que depende de que estas tablas existan en Supabase.

Cubre COMMS-01 (modelo de datos + ingest + asociación a lead/negocio) y COMMS-02 (estado por conversación + takeover que el bot lee para pausar), per D-02/D-03/D-04/D-05/D-06.

Purpose: Es el cimiento de seguridad de la fase. La RLS **mixta** (`business_id`-del-dueño OR `is_admin`-del-operador) es la pieza load-bearing — primera tabla del CRM que el dueño también lee (base del add-on "Mensajes" de gestion-rebrand). El ingest es input no confiable (espejo exacto del webhook de pago): token fail-closed + tenant del slug, nunca del body.
Output: 1 migración SQL, 2 libs puras + sus tests vitest, 3 route handlers, 1 archivo de server actions + sus schemas, 1 línea nueva en el ACTION_LABEL central.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/06-comms-bandeja/06-CONTEXT.md
@.planning/workstreams/crm/phases/06-comms-bandeja/06-RESEARCH.md
@.planning/workstreams/crm/phases/06-comms-bandeja/06-PATTERNS.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
@.claude/skills/convenciones-forjo/SKILL.md

# Análogos exactos (leer ANTES de codear — la regla es NO desviarse del patrón probado)
@app/api/payment/webhook/[slug]/route.ts
@app/api/booking/availability/route.ts
@supabase/migrations/034_crm_pipeline_tags_timeline.sql
@app/(crm)/admin/_content-actions.ts
@lib/audit.ts
@lib/admin-guard.ts
@lib/whatsapp.ts
@lib/crm-timeline.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migración 038 (conversations/messages + RLS mixta) + libs puras testeables</name>
  <files>supabase/migrations/038_conversations_messages.sql, lib/conversations.ts, lib/conversations.test.ts, lib/agent-context.ts, lib/agent-context.test.ts</files>
  <read_first>
    - supabase/migrations/034_crm_pipeline_tags_timeline.sql (RLS admin-only del CRM: `(select auth.jwt() -> 'app_metadata' ->> 'is_admin') = 'true'`; sin policy de write para usuarios)
    - supabase/schema.sql líneas 130-137 (business_hours: day_of_week/open_time/close_time/is_open), 160-195 (businesses: name/slug/address/maps_url), 381-389 (leads: name/email/whatsapp/business_id), 523-534 (services: name/duration_minutes/price/description/active), y la región de "owner access" (subselect `business_id in (select id from public.businesses where owner_id = (select auth.uid()))`)
    - 06-RESEARCH.md §"Migración 038 — esqueleto de tablas" (líneas 446-480) y §"Pattern 2: RLS mixta" (líneas 229-258)
    - lib/whatsapp.ts (normalizeArWhatsApp — idempotente, usar en el match por teléfono)
    - .claude/skills/supabase-multitenant-rls/SKILL.md (RLS habilitada + subselect de auth.uid + service-role solo server)
  </read_first>
  <behavior>
    - Test (lib/conversations.test.ts): matchEntity({ phone, email, leads }) devuelve el lead_id cuando leads.whatsapp normalizado coincide con el phone normalizado; matchea por email (case-insensitive) si no hay match de phone; devuelve null si no hay match.
    - Test: el schema zod del ingest acepta el payload válido { slug, external_id, contact:{phone,name?,email?}, direction:'inbound'|'outbound', body, sender:'contact'|'ai'|'human', sent_at? } y rechaza (safeParse.success=false) payloads sin slug / sin external_id / direction inválida.
    - Test: isValidHandledByTransition(from,to) acepta 'ai'→'human', 'human'→'ai', 'unassigned'→'human'; el universo de estados es exactamente {'unassigned','ai','human'}.
    - Test (lib/agent-context.test.ts): mapBusinessHours(rows) agrupa por day_of_week, ignora filas con is_open=false / open_time null, devuelve 7 entradas [{day, ranges:[{open,close}]}] con open/close en HH:MM (slice(0,5)); mapServices(rows) mapea name/durationMinutes/price(number)/description y tolera price null.
  </behavior>
  <action>
    Crear `supabase/migrations/038_conversations_messages.sql` (aplicar a mano EN ORDEN tras 037 — convención manual del proyecto). Dos tablas canónicas (D-04):
    - `conversations`: id uuid PK; business_id uuid NOT NULL FK businesses(id) on delete cascade; channel text NOT NULL default 'whatsapp' check in ('whatsapp') (mail diferido, D-01); contact_phone text NOT NULL (normalizado con normalizeArWhatsApp); contact_name text; lead_id uuid FK leads(id) on delete set null (match por phone/email, D-04); handled_by text NOT NULL default 'ai' check in ('unassigned','ai','human'); unread_count integer NOT NULL default 0; last_message_at timestamptz NOT NULL default now(); created_at timestamptz NOT NULL default now(). Índice UNIQUE (business_id, channel, contact_phone) — onConflict del upsert del ingest. Índices (business_id) y (last_message_at desc).
    - `messages`: id uuid PK; conversation_id uuid NOT NULL FK conversations(id) on delete cascade; business_id uuid NOT NULL FK businesses(id) on delete cascade (DENORMALIZADO para que la RLS sea byte-idéntica a conversations, RESEARCH Pitfall 2); external_id text NOT NULL (id del mensaje en el bot → idempotencia, D-05); direction text NOT NULL check in ('inbound','outbound'); sender text NOT NULL default 'contact' check in ('contact','ai','human'); body text NOT NULL; sent_at timestamptz NOT NULL default now(); created_at timestamptz NOT NULL default now(). Índice UNIQUE (external_id) — idempotencia del upsert. Índice (conversation_id, sent_at).
    - RLS MIXTA en AMBAS tablas (la pieza load-bearing): `alter table ... enable row level security` + DOS policies permissive de SELECT por tabla que Postgres OR-ea — (A) "owner read": `using (business_id in (select id from public.businesses where owner_id = (select auth.uid())))`; (B) "admin read": `using ((select auth.jwt() -> 'app_metadata' ->> 'is_admin') = 'true')`. SIN policy de insert/update/delete para usuarios — solo el service-role escribe (espejo 034, lección 029). NO usar una condición gigante; NO using(true).
    Crear `lib/conversations.ts` (puro, SIN cliente Supabase): `matchEntity(...)` (usa normalizeArWhatsApp de lib/whatsapp.ts, no regex ad-hoc); el schema zod `inboundSchema` (con `.default('contact')` en sender) exportado para reusar en el ingest; `isValidHandledByTransition(from,to)` + el tipo `HandledBy = 'unassigned'|'ai'|'human'`. Comentarios ddensos en español explicando el porqué del match por phone-then-email y la denormalización.
    Crear `lib/agent-context.ts` (puro): `mapBusinessHours(rows)` y `mapServices(rows)` extraídos del context endpoint (route delgada). DAYS = ['domingo'..'sábado'] (day_of_week 0..6).
    Crear `lib/conversations.test.ts` y `lib/agent-context.test.ts` (vitest) cubriendo los Test de <behavior>.
    NO crear policies de write; NO tocar otras migraciones; NO regenerar schema.sql todavía (es checkpoint operativo aparte, Task 4).
  </action>
  <verify>
    <automated>npx vitest run lib/conversations.test.ts lib/agent-context.test.ts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "enable row level security" supabase/migrations/038_conversations_messages.sql</automated>
    <automated>grep -v '^--' supabase/migrations/038_conversations_messages.sql | grep -c "using (true)"</automated>
  </verify>
  <acceptance_criteria>
    - vitest verde en ambos archivos de test (matchEntity, inboundSchema, transición handled_by, mapeo hours/services).
    - tsc sin errores.
    - El archivo de migración tiene `enable row level security` en ambas tablas (grep cuenta ≥ 2).
    - El grep de `using (true)` (excluyendo comentarios) devuelve 0 — sin agujero RLS.
    - Existen DOS policies de SELECT por tabla (owner + admin) y CERO policies de insert/update/delete para usuarios.
    - `messages` tiene `business_id` NOT NULL (denormalizado) + índice UNIQUE sobre `external_id`.
  </acceptance_criteria>
  <done>Migración 038 escrita con tablas + RLS mixta sin agujeros; libs puras + tests verdes; tsc limpio. Las tablas todavía NO están en Supabase (las aplica el operador en Task 4).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Endpoints del agente (ingest POST + context GET + state GET) + takeover server action auditada</name>
  <files>app/api/agent/inbox/route.ts, app/api/agent/context/route.ts, app/api/agent/inbox/state/route.ts, app/(crm)/admin/bandeja/actions.ts, app/(crm)/admin/bandeja/_bandeja-actions.schemas.ts, lib/crm-timeline.ts</files>
  <read_first>
    - app/api/payment/webhook/[slug]/route.ts (auth-first fail-closed → slug→tenant → service-role write; el body es input no confiable; tenant del slug, no del body)
    - app/api/booking/availability/route.ts (GET read-only por slug: `export const dynamic = 'force-dynamic'`, `request.nextUrl.searchParams.get(...)` síncrono — Next 16)
    - app/(crm)/admin/_content-actions.ts (patrón obligatorio de 6 pasos de la server action: requireAdmin → zod → createAdminClient → mutar/throw → logAudit → revalidatePath)
    - lib/admin-guard.ts (requireAdmin lanza, devuelve User cuyo id es el actorId)
    - lib/audit.ts (logAudit best-effort, firma { actorId, action, targetType, targetId, businessId, risk, reason, metadata })
    - lib/crm-timeline.ts líneas 67-93 (ACTION_LABEL central — registrar el código nuevo)
    - lib/conversations.ts + lib/agent-context.ts (de Task 1: inboundSchema, matchEntity, mapBusinessHours, mapServices)
    - 06-RESEARCH.md §"Pattern 1: Ingest" (156-226), §"GET /api/agent/context" (392-442), §"Pattern 4: Takeover" (283-311); 06-PATTERNS.md §inbox/state y §actions.ts
  </read_first>
  <behavior>
    - Ingest: un POST sin header Authorization (o con token != FORJO_AGENT_TOKEN, o con FORJO_AGENT_TOKEN sin setear) → 401, sin escribir nada (fail-closed).
    - Ingest: POST válido con token correcto + slug existente → upsert conversation (onConflict business_id,channel,contact_phone) + upsert message (onConflict external_id, ignoreDuplicates) → 200 { ok: true }. El business_id sale del slug validado, NUNCA del body.
    - Ingest: el mismo external_id POSTeado dos veces NO crea una segunda fila en messages (idempotencia).
    - Ingest: slug inexistente → 404; body no parseable → 400 bad_request; payload que falla inboundSchema → 400 missing_fields.
    - Context: GET sin slug → 400; slug inexistente → 404; slug válido → 200 con { business:{name,slug,address,mapsUrl,bookingUrl}, services:[], hours:[7], notes:null }, header Cache-Control no-store.
    - State: GET con token válido + slug + phone → 200 { handled_by }; sin token → 401; sin match de conversación → 200 { handled_by: null } (o 404, elegir y documentar). El phone se normaliza con normalizeArWhatsApp antes de buscar.
    - takeConversation(id): requireAdmin → handled_by='human' vía service-role → logAudit('conversation.takeover', risk 'bajo') → revalidatePath('/admin/bandeja'). releaseConversation(id) simétrico → handled_by='ai', logAudit('conversation.release').
  </behavior>
  <action>
    Crear `app/api/agent/inbox/route.ts` (POST): `export const dynamic = 'force-dynamic'`. authOk(request) FAIL-CLOSED — lee FORJO_AGENT_TOKEN; ausente → false → 401 (espejo de verifyMPSignature, SIN rama `if (secret)`). Parseo defensivo del body (try/catch → 400 bad_request). `inboundSchema.safeParse` (de lib/conversations.ts) → 400 missing_fields. createAdminClient(). Resolver business por `eq('slug', msg.slug).single()` (anti-tampering: el tenant del slug, no del body) → 404 si no existe. matchEntity para lead_id (best-effort, nullable). Upsert conversation (incluye business_id, channel 'whatsapp', contact_phone normalizado, contact_name, lead_id, last_message_at) onConflict 'business_id,channel,contact_phone'. Upsert message (conversation_id, business_id denormalizado, external_id, direction, sender, body, sent_at) onConflict 'external_id' ignoreDuplicates. Errores → 500 insert_failed. Escritura SÍNCRONA (sin after(): el bot quiere que el 200 signifique "persistido" para su loop de reintento). Respuestas con `Response.json({ ok, ... }, { status })` y códigos snake_case (convención).
    Crear `app/api/agent/context/route.ts` (GET): `force-dynamic`; slug de `request.nextUrl.searchParams.get('slug')` (síncrono, Next 16); createAdminClient; leer businesses (id,name,slug,address,maps_url) + Promise.all(services activos, business_hours); usar mapServices/mapBusinessHours de lib/agent-context.ts; bookingUrl = `${NEXT_PUBLIC_APP_URL || 'https://gestion.forjo.studio'}/${slug}/turno` (OBLIGATORIO por el HANDOFF); notes: null. Header Cache-Control no-store.
    Crear `app/api/agent/inbox/state/route.ts` (GET): `force-dynamic`; mismo authOk fail-closed que el ingest; slug+phone de searchParams; normalizeArWhatsApp(phone); createAdminClient; resolver business por slug; leer conversations.handled_by por (business_id, contact_phone); devolver { handled_by } (null si no hay conversación).
    Crear `app/(crm)/admin/bandeja/_bandeja-actions.schemas.ts` (zod: takeConversationSchema/releaseConversationSchema con conversationId uuid).
    Crear `app/(crm)/admin/bandeja/actions.ts` ('use server'): `takeConversation` y `releaseConversation` siguiendo los 6 pasos VERBATIM de _content-actions.ts (requireAdmin → schema.parse → createAdminClient → update handled_by → logAudit → revalidatePath('/admin/bandeja')). El bot lee el resultado vía el state endpoint y pasa a Modo Humano (pausa) — D-03.
    Registrar en `lib/crm-timeline.ts` ACTION_LABEL: `'conversation.takeover': 'Tomó conversación'` y `'conversation.release': 'Liberó conversación'` (para que rinda en auditoría/timeline). Es una edición aditiva de las dos líneas, sin tocar el resto del mapa.
    NO construir el composer / endpoint send (DIFERIDO D-03). NO endpoint ni columna de mail (DIFERIDO D-01).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run lib/conversations.test.ts lib/agent-context.test.ts</automated>
    <automated>grep -c "createAdminClient" app/api/agent/inbox/route.ts app/api/agent/context/route.ts app/api/agent/inbox/state/route.ts</automated>
    <automated>grep -rn "FORJO_AGENT_TOKEN" app/api/agent/inbox/route.ts app/api/agent/inbox/state/route.ts</automated>
    <automated>grep -n "conversation.takeover" lib/crm-timeline.ts app/(crm)/admin/bandeja/actions.ts</automated>
  </verify>
  <acceptance_criteria>
    - tsc sin errores; vitest verde.
    - Los tres route handlers usan createAdminClient (service-role correcto para callers sin sesión).
    - El ingest y el state endpoint referencian FORJO_AGENT_TOKEN (auth fail-closed presente).
    - `export const dynamic = 'force-dynamic'` en los tres route handlers.
    - El ingest resuelve business por `.eq('slug', ...)` y NUNCA lee business_id del body.
    - actions.ts: takeConversation/releaseConversation llaman requireAdmin como PRIMERA línea, mutan vía service-role, y logAudit con action 'conversation.takeover'/'conversation.release'.
    - ACTION_LABEL en lib/crm-timeline.ts incluye 'conversation.takeover' y 'conversation.release'.
    - NO existe ningún archivo/endpoint de send manual ni de mail.
  </acceptance_criteria>
  <done>Los tres endpoints del agente funcionan (ingest fail-closed + idempotente, context con shape del HANDOFF, state para el polling del bot) y la server action de takeover muta+audita; el código compila y los tests pasan. Falta aplicar la migración a Supabase (Task 4) para probar contra tablas vivas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Checkpoint — aplicar migración 038 a Supabase (manual, en orden tras 037)</name>
  <action>BLOQUEA hasta que el operador aplique a mano `supabase/migrations/038_conversations_messages.sql` en el SQL Editor de Supabase (en orden, tras 037) y verifique las tablas + la RLS mixta. NO continuar 06-02 (que lee las tablas vivas) hasta la señal "aplicada". Convención manual del proyecto — NUNCA `supabase db push`. Ver <how-to-verify>.</action>
  <what-built>
    La migración `supabase/migrations/038_conversations_messages.sql` (tablas `conversations`/`messages` + RLS mixta owner-OR-admin + índices de idempotencia). El código (endpoints + libs + takeover) ya compila y pasa tests, PERO las tablas todavía NO existen en el Postgres del proyecto. Esta es la convención manual del proyecto: las migraciones se aplican A MANO, EN ORDEN (última aplicada: 037), nunca con `supabase db push`. Espejo de cómo se aplicaron 034/036.
  </what-built>
  <how-to-verify>
    1. Abrir el SQL Editor de Supabase (proyecto de producción del CRM).
    2. Pegar y ejecutar el contenido completo de `supabase/migrations/038_conversations_messages.sql` (EN ORDEN, tras 037).
    3. Verificar que existen las tablas `public.conversations` y `public.messages`.
    4. Verificar RLS habilitada en ambas (`select relrowsecurity from pg_class where relname in ('conversations','messages')` → ambas true).
    5. Verificar que cada tabla tiene EXACTAMENTE dos policies de SELECT ("owner read..." y "admin read...") y CERO policies de insert/update/delete (`select policyname, cmd from pg_policies where tablename in ('conversations','messages')`).
    6. Verificar los índices UNIQUE: `conversations (business_id, channel, contact_phone)` y `messages (external_id)`.
    7. (Recomendado, prueba load-bearing de aislamiento) Con un usuario DUEÑO (no admin) y un usuario `is_admin`, confirmar que el dueño NO vería conversaciones de otro negocio y el admin las vería todas — se valida de verdad en 06-02 con la UI, acá basta confirmar las policies.
    8. Tras aplicar: regenerar `supabase/schema.sql` con `supabase db dump --linked` (TODO operativo NO bloqueante, igual que 034/036) — puede hacerse después.
  </how-to-verify>
  <resume-signal>Escribir "aplicada" cuando la migración 038 esté aplicada en Supabase y las policies verificadas. Si algo falla, describir el error SQL.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot (otro repo, VPS) → API ingest/state | Input NO confiable: solo posee el token compartido; sin sesión Supabase. El payload (incluido cualquier business_id que mandara) es no confiable. |
| operador (sesión is_admin) → server action takeover | Invocable directo (curl/devtools) sin pasar por el layout → re-guard obligatorio. |
| service-role → DB | El admin client bypassa RLS; el aislamiento se garantiza por el slug validado + filtro business_id en la query. |
| dueño (sesión, RLS) → conversations/messages | El dueño solo debe ver su business_id (RLS owner). Lectura del operador es 06-02. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | RLS mixta de conversations/messages | mitigate | Dos policies permissive de SELECT (owner business_id OR is_admin); subselect de auth.uid; SIN write policy para usuarios. Probar con 2 usuarios (checklist supabase-multitenant-rls). |
| T-06-02 | Spoofing | Ingest POST (caller externo) | mitigate | authOk fail-CLOSED sobre FORJO_AGENT_TOKEN Bearer; secreto ausente → 401 (espejo verifyMPSignature, sin fail-open). |
| T-06-03 | Tampering | Resolución de tenant en el ingest | mitigate | business_id SIEMPRE del slug validado (`.eq('slug', ...)`), NUNCA del body (anti-tampering, CLAUDE.md). |
| T-06-04 | Tampering / Integridad | Reintento del bot duplica mensajes | mitigate | Índice UNIQUE messages.external_id + upsert onConflict ignoreDuplicates (atómico, sin race). |
| T-06-05 | Elevation of Privilege | service-role en la lectura del operador | mitigate | Prohibido createAdminClient en page/client; la lectura del operador (06-02) usa session client; el override admin vive en la RLS. |
| T-06-06 | Repudiation | Takeover sin traza | mitigate | logAudit('conversation.takeover'/'conversation.release') en la server action (best-effort, no falsificable). |
| T-06-07 | Information Disclosure | FORJO_AGENT_TOKEN filtrado al cliente | mitigate | Server-only (NO NEXT_PUBLIC_), igual que la service-role key. Setear en Vercel. |
| T-06-08 | Information Disclosure | Context endpoint expone datos privados | accept | Devuelve solo lo que la página pública ya muestra (name/slug/address/services/hours/bookingUrl); sin secretos. Riesgo bajo. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Cero dependencias nuevas en esta fase (zero-install verificado en RESEARCH §Package Legitimacy Audit). Sin superficie de supply-chain. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpio en todo el repo.
- `npx vitest run` (suite completa) verde — los nuevos tests de lib/conversations + lib/agent-context suman sin romper los 204 existentes.
- La migración 038 aplicada a Supabase (checkpoint resuelto) con RLS mixta verificada (2 SELECT policies por tabla, 0 write policies).
- `grep` confirma: sin `using (true)`, FORJO_AGENT_TOKEN en ingest+state, force-dynamic en los 3 routes, ACTION_LABEL con los dos códigos nuevos.
</verification>

<success_criteria>
- COMMS-01 (datos): tablas conversations/messages canónicas con RLS mixta + ingest que persiste mensajes del bot idempotentemente, asociando lead_id por phone/email.
- COMMS-02 (estado/takeover): handled_by ('unassigned'/'ai'/'human') + takeConversation auditada + state endpoint que el bot lee para pausar.
- D-05/D-06 cubiertos: ingest con token fail-closed + tenant del slug; context endpoint con el shape del HANDOFF.
- Cero deps nuevas; cero envío manual saliente; cero mail (todo DIFERIDO).
</success_criteria>

<output>
Crear `.planning/workstreams/crm/phases/06-comms-bandeja/06-01-SUMMARY.md` al terminar.
</output>
