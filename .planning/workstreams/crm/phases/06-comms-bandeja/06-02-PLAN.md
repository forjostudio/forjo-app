---
phase: 06-comms-bandeja
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/(crm)/admin/bandeja/page.tsx
  - app/(crm)/admin/bandeja/bandeja-client.tsx
  - components/crm/crm-sidebar.tsx
autonomous: false
requirements: [COMMS-01, COMMS-02]

must_haves:
  truths:
    - "El operador entra a /admin/bandeja y ve la lista de conversaciones de WhatsApp ordenadas por última actividad"
    - "Cada conversación muestra su estado (IA atendiendo / Vos atendés / Sin asignar) con el badge de color del CRM"
    - "Cada fila muestra nombre del contacto, negocio asociado, preview del último mensaje, hora (AR) y contador de no-leídos"
    - "El operador selecciona una conversación y ve el thread (mensajes inbound/outbound) con el banner 'El agente IA está respondiendo'"
    - "El operador hace clic en 'Tomar conversación' → la server action takeConversation corre → toast de éxito → el estado pasa a 'Vos atendés'"
    - "El composer está DESHABILITADO con copy 'próximamente' / 'Tomá la conversación para escribir manualmente' (envío manual diferido)"
    - "El operador filtra por Todas / WhatsApp"
    - "El item 'Bandeja' del sidebar enruta a /admin/bandeja (ya no PRONTO)"
  artifacts:
    - path: "app/(crm)/admin/bandeja/page.tsx"
      provides: "RSC que lee conversations con session client (RLS-gated, override admin)"
      min_lines: 20
    - path: "app/(crm)/admin/bandeja/bandeja-client.tsx"
      provides: "Lista + thread + filtros + estados + Tomar conversación + composer disabled, fiel a 07-bandeja.png"
      min_lines: 80
    - path: "components/crm/crm-sidebar.tsx"
      provides: "Item Bandeja habilitado (href /admin/bandeja, sin soon)"
  key_links:
    - from: "app/(crm)/admin/bandeja/page.tsx"
      to: "app/(crm)/admin/bandeja/bandeja-client.tsx"
      via: "pasa rows (conversations) + loadError como props"
      pattern: "BandejaClient"
    - from: "app/(crm)/admin/bandeja/bandeja-client.tsx"
      to: "app/(crm)/admin/bandeja/actions.ts"
      via: "el botón 'Tomar conversación' invoca takeConversation"
      pattern: "takeConversation"
    - from: "app/(crm)/admin/bandeja/page.tsx"
      to: "supabase/migrations/038_conversations_messages.sql"
      via: "createClient().from('conversations').select(...) — session client, RLS admin-override"
      pattern: "from\\('conversations'\\)"
  prohibitions:
    - "NUNCA createAdminClient en page.tsx ni en bandeja-client.tsx — la lectura del operador va con session client (createClient); el override admin vive en la RLS (Pitfall 1, threat de fuga de tenants)"
    - "Sin tab/canal Email en los filtros — solo Todas/WhatsApp (COMMS-03 diferido, D-01)"
    - "Composer NO funcional: input + botón Enviar DESHABILITADOS con copy 'próximamente' (envío manual diferido, D-03). NO cablear ningún send."
    - "NO inventar CSS de badges nuevo — reusar el patrón RiskBadge/StatusBadge del CRM y los tokens dark del tema Forjo"
---

<objective>
La superficie del operador: la página `/admin/bandeja` (RSC que lee `conversations` con el **session client**, RLS-gated) + el componente cliente `bandeja-client.tsx` (lista de 2 paneles + thread + estados + filtros + "Tomar conversación", con el **composer deshabilitado**), reproduciendo fielmente el mock `crm-design/07-bandeja.png` (contrato A12, `--skip-ui`). Más habilitar el item "Bandeja" del sidebar.

Cubre la cara visible de COMMS-01 (bandeja unificada asociada a lead/negocio) y COMMS-02 (estados + takeover) sobre la base de datos/acciones de 06-01.

Purpose: Es la única superficie de UI de la fase. La regla de seguridad load-bearing acá es leer con session client (NUNCA service-role): el override admin ya vive en la RLS mixta de 06-01; meter createAdminClient en un page/client filtraría TODOS los tenants (Pitfall 1, documentado como threat en auditoria/page.tsx).
Output: 1 page RSC, 1 componente cliente, 1 línea modificada del sidebar.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/06-comms-bandeja/06-CONTEXT.md
@.planning/workstreams/crm/phases/06-comms-bandeja/06-RESEARCH.md
@.planning/workstreams/crm/phases/06-comms-bandeja/06-PATTERNS.md
@.planning/workstreams/crm/phases/06-comms-bandeja/06-01-SUMMARY.md
@.claude/skills/supabase-multitenant-rls/SKILL.md

# Mock = contrato (reproducir tal cual, --skip-ui)
# c:/Users/franc/Desktop/Forjo Studio/crm-design/07-bandeja.png

# Análogos exactos de UI (leer ANTES de codear)
@app/(crm)/admin/auditoria/page.tsx
@app/(crm)/admin/auditoria/auditoria-client.tsx
@app/(crm)/admin/pipeline/pipeline-client.tsx
@app/(crm)/admin/bandeja/actions.ts
@components/crm/crm-sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Página RSC /admin/bandeja (session read, RLS-gated) + habilitar item del sidebar</name>
  <files>app/(crm)/admin/bandeja/page.tsx, components/crm/crm-sidebar.tsx</files>
  <read_first>
    - app/(crm)/admin/auditoria/page.tsx (líneas 1-39: el patrón EXACTO — createClient() session, SELECT explícito sin comodín, order+limit, loadError, pasa rows al client. El comentario de cabecera 7-14 documenta la regla load-bearing: leer con session client, nunca service-role)
    - components/crm/crm-sidebar.tsx (líneas 53-82: NAV_GROUPS; el item Bandeja está en línea 58 como `{ href: '#', label: 'Bandeja', icon: Inbox, soon: true }`; los items Pipeline/Negocios/Reportes ya NO tienen `soon` → mismo cambio)
    - 06-RESEARCH.md §"Pattern 3: Lectura RLS-gated" (260-281) y §Pitfall 1 (349-353)
    - 06-PATTERNS.md §page.tsx y §crm-sidebar
  </read_first>
  <action>
    Crear `app/(crm)/admin/bandeja/page.tsx` (RSC `export default async function BandejaPage`): `const supabase = await createClient()` (session — anon key + cookies, NO createAdminClient). SELECT explícito sin comodín sobre `conversations`: `id, business_id, contact_name, contact_phone, handled_by, unread_count, last_message_at`. Para mostrar el negocio asociado y el preview del último mensaje, hacer también la lectura del nombre del negocio (join embebido `businesses(name)` o segunda query) y, por cada conversación seleccionable, el último mensaje (puede leerse on-demand en el client vía un map ya cargado, o pasar los mensajes de las primeras N conversaciones — mantener simple: pasar conversations + un mapa de últimos mensajes por conversación leído con el MISMO session client). `.order('last_message_at', { ascending: false }).limit(100)`. `if (error) console.error('[crm/bandeja] read error:', error.message)`. Pasar `rows` + `loadError` a `<BandejaClient />`. Comentario de cabecera en español replicando el de auditoria/page.tsx (la garantía de aislamiento es la RLS admin-read mixta de 038, NO el cliente; service-role bypassaría el gate → Pitfall 1).
    Modificar `components/crm/crm-sidebar.tsx` línea 58: cambiar `{ href: '#', label: 'Bandeja', icon: Inbox, soon: true }` por `{ href: '/admin/bandeja', label: 'Bandeja', icon: Inbox }` (Inbox ya está importado, línea 11). Edición de una sola línea; NO tocar otros items. El badge de no-leídos del sidebar (el "3" del mock) es opcional/cosmético — si se agrega, derivar del count real, no hardcodear.
    NO usar createAdminClient en este archivo. NO agregar canal Email.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "createAdminClient" app/(crm)/admin/bandeja/page.tsx</automated>
    <automated>grep -n "createClient" app/(crm)/admin/bandeja/page.tsx</automated>
    <automated>grep -n "label: 'Bandeja'" components/crm/crm-sidebar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tsc sin errores.
    - page.tsx usa `createClient` (session) y el grep de `createAdminClient` devuelve 0 (sin fuga de tenants).
    - SELECT explícito sin `select('*')`; order por last_message_at desc; limit 100.
    - El item Bandeja del sidebar tiene `href: '/admin/bandeja'` y ya NO tiene `soon: true`.
    - Pasa `rows` + `loadError` a BandejaClient.
  </acceptance_criteria>
  <done>La página /admin/bandeja lee conversaciones con session client (RLS admin-override) y el sidebar enruta a ella; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: bandeja-client.tsx — lista + thread + estados + filtros + Tomar + composer disabled (fiel a 07-bandeja.png)</name>
  <files>app/(crm)/admin/bandeja/bandeja-client.tsx</files>
  <read_first>
    - app/(crm)/admin/auditoria/auditoria-client.tsx (líneas 1-59: imports 'use client' + Tabs/Table + RiskBadge; el helper AR_TZ + formatWhen — COPIAR VERBATIM, no hand-rollear toLocaleString; el row type en snake_case como viene de Supabase)
    - app/(crm)/admin/pipeline/pipeline-client.tsx (filtros/estados + tokens dark del CRM var(--card)/--border/--foreground; cómo invoca server actions con toast de sonner)
    - app/(crm)/admin/bandeja/actions.ts (takeConversation/releaseConversation de 06-01 — el botón las llama)
    - components/crm/ (RiskBadge/StatusBadge — el patrón de chip de estado a reusar; NO inventar CSS)
    - crm-design/07-bandeja.png (el contrato visual — reproducir tal cual)
    - 06-PATTERNS.md §bandeja-client (lista de detalles de UI del mock)
  </read_first>
  <action>
    Crear `app/(crm)/admin/bandeja/bandeja-client.tsx` ('use client'). Layout de 2 paneles fiel al mock:
    - PANEL IZQUIERDO (lista): search box "Buscar conversación…" (filtra client-side por nombre/negocio/preview); filtros como Tabs "Todas / WhatsApp" (NO "Email" — D-01); por fila: avatar con iniciales, nombre del contacto, nombre del negocio (línea secundaria), preview del último mensaje truncado, hora con formatWhen (AR), badge de no-leídos amarillo cuando unread_count>0, icono de canal (WhatsApp), y el badge de ESTADO mapeado desde handled_by: 'ai'→"IA atendiendo", 'human'→"Vos atendés", 'unassigned'→"Sin asignar" (reusar RiskBadge/StatusBadge chip con los acentos del tema; amarillo principal / azul info — un solo rojo SOLO peligro, acá no aplica). La fila seleccionada se resalta (fondo elevado + barra de acento, como el nav activo).
    - PANEL DERECHO (thread): header con nombre del contacto + negocio + "WhatsApp Business" + botón "Tomar conversación" (acento amarillo). Banner "El agente IA está respondiendo automáticamente. Tomá la conversación para intervenir." cuando handled_by='ai'. Burbujas de mensajes: inbound a la izquierda (contact), outbound a la derecha etiquetadas "Agente IA" / "Vos" según sender, con timestamps y divisor "HOY"/fecha. Empty state honesto si no hay conversación seleccionada.
    - COMPOSER DESHABILITADO (D-03): barra de aviso "Tomá la conversación para escribir manualmente." + input "Escribir por WhatsApp…" + botón "Enviar" AMBOS `disabled`. NO cablear ningún submit/send. Tooltip/copy "próximamente". El composer queda visualmente igual al mock pero inerte.
    - "Tomar conversación": onClick → invoca `takeConversation(id)` (server action), con estado loading (deshabilitar el botón para evitar doble submit), toast.success('Tomaste la conversación') / toast.error en falla, y refresca el estado de la fila a 'human' ("Vos atendés"). Mostrar "Liberar" (releaseConversation) cuando handled_by='human' (simétrico, opcional pero coherente con la action existente).
    - Tipos: row en snake_case (ConversationRow { id, business_id, contact_name, contact_phone, handled_by, unread_count, last_message_at, business_name?, last_message_preview? }). Si loadError, mostrar estado de error en vez de empty engañoso (como auditoria-client).
    Reusar exclusivamente componentes @/components/ui/* y @/components/crm/* + tokens CSS del tema; cero hex hardcodeado, cero deps nuevas. Touch targets ≥ 44px, estados focus visibles, contraste WCAG AA (perfil UX del proyecto).
    NO construir send. NO canal Email.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx next build</automated>
    <automated>grep -c "createAdminClient" app/(crm)/admin/bandeja/bandeja-client.tsx</automated>
    <automated>grep -En "disabled" app/(crm)/admin/bandeja/bandeja-client.tsx</automated>
    <automated>grep -n "takeConversation" app/(crm)/admin/bandeja/bandeja-client.tsx</automated>
    <automated>grep -ci "email" app/(crm)/admin/bandeja/bandeja-client.tsx</automated>
  </verify>
  <acceptance_criteria>
    - tsc sin errores y `next build` verde.
    - El grep de `createAdminClient` en el client devuelve 0 (jamás service-role en cliente).
    - El composer (input + botón Enviar) tiene `disabled` — el grep de disabled encuentra esos elementos.
    - El botón Tomar conversación invoca takeConversation.
    - El grep case-insensitive de "email" en el client devuelve 0 (sin tab/canal Email — D-01).
    - Los tres estados (IA atendiendo / Vos atendés / Sin asignar) están mapeados desde handled_by.
    - Filtros Todas/WhatsApp presentes; search box presente.
  </acceptance_criteria>
  <done>La bandeja reproduce 07-bandeja.png: lista 2-paneles + thread + estados + filtros + Tomar conversación funcional + composer deshabilitado, sin Email, sin service-role en el cliente; build verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual — la bandeja contra 07-bandeja.png + takeover + composer disabled</name>
  <action>BLOQUEA hasta que el operador verifique la pantalla /admin/bandeja contra el mock `crm-design/07-bandeja.png` (contrato A12, --skip-ui): fidelidad visual, takeover que muta+audita, composer deshabilitado, sin tab Email, y aislamiento por tenant. Ver <how-to-verify>.</action>
  <what-built>
    La superficie completa de la bandeja en /admin/bandeja: lista de conversaciones con estados/filtros/no-leídos, panel de thread con banner del agente IA, botón "Tomar conversación" que muta el estado + audita, y el composer deshabilitado ("próximamente"). El item Bandeja del sidebar ya enruta. Es la primera vez que se ve la pantalla contra el mock 07-bandeja.png (contrato A12).
  </what-built>
  <how-to-verify>
    1. Entrar al CRM como operador (is_admin) y abrir /admin/bandeja desde el sidebar (el item ya no dice PRONTO).
    2. Si la migración 038 está aplicada pero NO hay datos del bot todavía: confirmar el empty state honesto (no un crash). Para probar con datos, insertar 1-2 conversaciones + mensajes a mano vía SQL Editor (o esperar a la sync real del bot, Deferred).
    3. Comparar la pantalla contra `crm-design/07-bandeja.png`: lista 2-paneles, badges de estado (IA atendiendo / Vos atendés / Sin asignar), no-leídos amarillo, thread con banner del agente IA, burbujas inbound/outbound, divisor HOY.
    4. Clic en "Tomar conversación" → confirmar toast de éxito + el badge pasa a "Vos atendés" + el banner del agente desaparece. Confirmar en /admin/auditoria que quedó la entrada "Tomó conversación".
    5. Confirmar que el composer (input + Enviar) está DESHABILITADO con el copy de "próximamente" — NO debe poder enviarse nada.
    6. (Aislamiento — load-bearing) Si hay un usuario DUEÑO de prueba, confirmar que NO ve conversaciones de otro negocio (RLS owner); el operador is_admin las ve todas.
    7. Verificar dark/tokens del tema Forjo, contraste AA, y que no haya tab "Email".
  </how-to-verify>
  <resume-signal>Escribir "aprobado" si la bandeja coincide con el mock y el takeover + composer-disabled funcionan; o describir los diffs/problemas a corregir.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador (sesión is_admin) → page.tsx (lectura) | La lectura debe pasar por la RLS (session client). El override admin vive en la policy, no en el cliente. |
| operador → bandeja-client → takeConversation | Mutación a través de la server action re-guardada (requireAdmin en 06-01). |
| dueño (sesión, RLS) → /admin | No accede al CRM (layout (crm) gatea is_admin); pero la RLS owner de 038 lo habilita para el add-on Mensajes futuro. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Elevation of Privilege | service-role en la lectura del operador | mitigate | page.tsx y bandeja-client usan SOLO session client (createClient); grep verifica 0 createAdminClient; el override admin vive en la RLS mixta de 038 (Pitfall 1, threat documentado en auditoria/page.tsx). |
| T-06-09 | Information Disclosure | Filas crudas / datos sensibles al cliente | mitigate | SELECT explícito de columnas no sensibles; el client recibe solo lo necesario para la lista/thread; sin secretos. Defensa en profundidad: layout (crm) ya gatea is_admin. |
| T-06-06 | Repudiation | Takeover desde la UI sin traza | mitigate | El botón invoca takeConversation, que ya hace logAudit (06-01). La UI no es la garantía; la action re-guarda y audita. |
| T-06-10 | Tampering | Composer / envío manual no autorizado | mitigate | Composer DESHABILITADO sin ningún submit cableado (D-03 diferido). No hay superficie de envío en v1. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Cero dependencias nuevas (zero-install; recharts/sonner/shadcn ya en el repo). Sin superficie de supply-chain. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpio y `npx next build` verde.
- `grep` confirma 0 `createAdminClient` en page.tsx y bandeja-client.tsx; 0 ocurrencias de "email" en el client; composer con `disabled`; sidebar item habilitado.
- Checkpoint humano: la pantalla coincide con 07-bandeja.png; takeover funciona + audita; composer inerte; sin fuga de tenant para el dueño.
</verification>

<success_criteria>
- COMMS-01 (UI): bandeja unificada de WhatsApp asociada al negocio, lista + thread, leída con RLS-gated session client.
- COMMS-02 (UI): estados IA/Vos/Sin asignar visibles + "Tomar conversación" que muta y audita; el bot lo lee (06-01) para pausar.
- Fidelidad al mock 07-bandeja.png (A12, --skip-ui); composer deshabilitado; sin Email; cero deps; cero service-role en el cliente.
</success_criteria>

<output>
Crear `.planning/workstreams/crm/phases/06-comms-bandeja/06-02-SUMMARY.md` al terminar.
</output>
