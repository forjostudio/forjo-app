---
phase: 03-impersonaci-n-read-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(crm)/admin/_actions.schemas.ts
  - app/(crm)/admin/_actions.schemas.test.ts
  - app/(crm)/admin/_actions.ts
  - lib/business-secrets.ts
  - lib/impersonation.ts
  - app/(crm)/admin/negocios/[id]/ver/page.tsx
  - app/(crm)/admin/auditoria/auditoria-client.tsx
autonomous: true
requirements: [IMP-01, IMP-02, IMP-03]
user_setup: []

must_haves:
  truths:
    - "Iniciar una impersonación escribe exactamente UNA fila en audit_log con action='user.impersonate', risk='alto', business_id y reason=motivo (D-08)."
    - "El motivo se valida server-side con z.string().trim().min(10): un POST directo con motivo vacío o 'a' es rechazado antes de auditar (D-07, Pitfall 5)."
    - "La capa de lectura resuelve el negocio impersonado por business_id (id de URL), nunca por owner_id ni por la sesión del operador (D-03)."
    - "Toda colección leída bajo impersonación está acotada con .eq('business_id', businessId); el negocio con .eq('id', businessId) (D-03, cero fuga cross-tenant)."
    - "El loader /ver/page.tsx re-llama requireAdmin() y no confía solo en el guard del layout (Phase-1 Pitfall 2)."
    - "La impersonación vive en una ruta DEDICADA bajo /admin (app/(crm)/admin/negocios/[id]/ver): el operador conserva su propia sesión admin y solo LEE datos del otro negocio; NO se reusa el dashboard real del cliente con su sesión ni una cookie de impersonación (D-01)."
    - "Re-entrar a la vista re-pide motivo y genera una fila nueva en audit_log (D-09)."
    - "TRAIL COMPLETO (producto multi-operador): CADA carga del loader /ver — incluido el acceso directo por URL sin pasar por el botón — escribe una fila de auditoría action='user.impersonate.view' (risk='medio', sin motivo). No existe forma de ver datos de un tenant sin dejar traza; la fila de entrada con motivo (user.impersonate) y la(s) fila(s) de vista (user.impersonate.view) son complementarias."
    - "loadImpersonationData expone la PRESENCIA de integraciones (MercadoPago/email/reCAPTCHA/Google como booleanos) vía getBusinessIntegrationStatus, NUNCA el valor crudo de un secreto/token (RESEARCH §1 / Open Q1)."
  artifacts:
    - path: "app/(crm)/admin/_actions.schemas.ts"
      provides: "startImpersonationSchema (businessId uuid + reason min 10)"
      contains: "startImpersonationSchema"
    - path: "app/(crm)/admin/_actions.ts"
      provides: "startImpersonation server action (audit + redirect, sin mutar negocio)"
      contains: "startImpersonation"
    - path: "lib/impersonation.ts"
      provides: "loadImpersonationData(businessId) service-role scoped read layer"
      contains: "loadImpersonationData"
    - path: "app/(crm)/admin/negocios/[id]/ver/page.tsx"
      provides: "RSC loader read-only: requireAdmin + createAdminClient + PaletteScript del negocio"
      contains: "loadImpersonationData"
  key_links:
    - from: "app/(crm)/admin/_actions.ts"
      to: "lib/audit.ts"
      via: "logAudit({ action: 'user.impersonate', risk: 'alto', businessId, reason })"
      pattern: "logAudit\\("
    - from: "app/(crm)/admin/negocios/[id]/ver/page.tsx"
      to: "lib/impersonation.ts"
      via: "loadImpersonationData(id) — lectura service-role acotada por business_id"
      pattern: "loadImpersonationData"
    - from: "lib/impersonation.ts"
      to: "lib/supabase/admin.ts"
      via: "createAdminClient() + .eq('business_id', businessId) por colección"
      pattern: "\\.eq\\('business_id'"
  prohibitions:
    - "NO existe ninguna server action de mutación de datos de negocio declarada en el árbol app/(crm)/admin/negocios/[id]/ver/ (D-02): la única action involucrada (startImpersonation) solo audita + redirige, no hace admin.from(...).update/insert/delete ni revalidatePath."
    - "lib/impersonation.ts NO importa @/lib/supabase/client (cliente browser) ni @/lib/supabase/server (sesión del operador): la lectura cross-tenant SOLO usa createAdminClient() (D-03)."
    - "lib/impersonation.ts NO contiene ninguna llamada .update(/.insert(/.delete(/.upsert( — es solo-lectura por construcción (IMP-01/D-02)."
    - "NINGUNA query de lib/impersonation.ts usa .eq('owner_id', ...) — resolver por owner_id leería el negocio del operador, no el impersonado."
    - "Ni lib/impersonation.ts ni el loader /ver/page.tsx llaman getBusinessSecrets (que devuelve valores crudos) ni seleccionan columnas de tokens/secretos: la PRESENCIA se obtiene SOLO vía getBusinessIntegrationStatus (booleanos), nunca el valor crudo del secreto (RESEARCH §1 / Open Q1)."
---

<objective>
Construir el BACKBONE de seguridad de la impersonación read-only: (1) el schema y la server action de entrada que audita cada acceso con motivo validado server-side y navega a la sub-página (sin mutar nada), y (2) la capa de lectura cross-tenant service-role acotada por `business_id` más el loader RSC de la sub-página `/ver` que la consume y aplica la paleta del negocio impersonado.

Esta es la superficie MÁS sensible del milestone (impersonación cross-tenant). La garantía IMP-01 (read-only) se logra por AUSENCIA de write paths, no bloqueando writes: este plan NO introduce ninguna mutación en el árbol `/ver` y centraliza todo el scope `business_id` en un único helper para que ninguna query pueda olvidar el filtro.

Purpose: garantizar server-side (D-02/D-03) que ningún write ocurre bajo impersonación, que cada acceso queda auditado con motivo (D-07/D-08/D-09) y que la lectura nunca filtra datos de otro tenant.
Output: `startImpersonationSchema`, `startImpersonation` action, `loadImpersonationData()` read helper, `/ver/page.tsx` loader (sin renderers todavía — los provee Plan 03-02).
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-CONTEXT.md
@.planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-RESEARCH.md
@.planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-PATTERNS.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-01)

| Símbolo | Tipo | Archivo |
|---------|------|---------|
| `startImpersonationSchema` | zod schema | `app/(crm)/admin/_actions.schemas.ts` |
| `StartImpersonationInput` | tipo inferido | `app/(crm)/admin/_actions.schemas.ts` |
| `startImpersonation(input)` | server action (audit + redirect) | `app/(crm)/admin/_actions.ts` |
| `loadImpersonationData(businessId)` | read helper service-role scoped | `lib/impersonation.ts` |
| `ImpersonationData` | tipo de retorno del helper | `lib/impersonation.ts` |
| `VerPage` (default export RSC) | loader read-only de la sub-página | `app/(crm)/admin/negocios/[id]/ver/page.tsx` |
| ruta `/admin/negocios/[id]/ver` | nueva ruta de impersonación — D-01: vista DEDICADA bajo `/admin`, no se reusa el dashboard real del cliente con su sesión (el operador sigue siendo él mismo y solo LEE) | `app/(crm)/admin/negocios/[id]/ver/page.tsx` |
| `action='user.impersonate'`, `risk='alto'` | código de auditoría de ENTRADA con motivo (ya reconocido por el visor) | escrito por `startImpersonation` |
| `action='user.impersonate.view'`, `risk='medio'` | código de auditoría POR CARGA (#2 trail completo, sin motivo) + su label en el visor | escrito por el loader `/ver/page.tsx`; label en `auditoria-client.tsx` |
| `getBusinessIntegrationStatus(businessId)` | presencia de integraciones (booleanos, sin valores) (#4) | `lib/business-secrets.ts` |

**Sin migración:** esta fase NO introduce ninguna migración SQL. `audit_log` ya existe (migración 031) y soporta `action='user.impersonate'` + `risk='alto'` + `reason` sin cambios de schema (RESEARCH §6). No se requiere tarea de schema-apply.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: startImpersonationSchema + startImpersonation action (audit + redirect, sin mutar)</name>
  <files>app/(crm)/admin/_actions.schemas.ts, app/(crm)/admin/_actions.schemas.test.ts, app/(crm)/admin/_actions.ts</files>
  <read_first>
    - app/(crm)/admin/_actions.schemas.ts (archivo a modificar — calcar el estilo de changePlanSchema/extendTrialSchema, módulo PURO sin 'use server')
    - app/(crm)/admin/_actions.schemas.test.ts (archivo a modificar — calcar el estilo de los describe/it de Vitest, incl. la constante UUID v4 válida)
    - app/(crm)/admin/_actions.ts (archivo a modificar — calcar el patrón obligatorio de action; la cabecera :16-30 documenta el orden requireAdmin→parse→...→logAudit)
    - .planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-PATTERNS.md (§ "startImpersonation en _actions.ts" y § "startImpersonationSchema en _actions.schemas.ts" — analog exacto: changePlan :46-69 quitando el update)
    - lib/audit.ts (signature de logAudit y AuditInput; service-role, best-effort)
    - lib/admin-guard.ts (requireAdmin devuelve el User; su .id es el actorId)
  </read_first>
  <behavior>
    - startImpersonationSchema acepta { businessId: <uuid v4 válido>, reason: 'soporte: revisar config' } → success true.
    - Rechaza businessId no-uuid ('abc') → success false.
    - Rechaza reason de menos de 10 chars tras trim (ej. 'a', '   corto  ' que quede <10) → success false.
    - Acepta reason de exactamente 10 chars no-blancos → success true.
    - reason se normaliza con .trim() (espacios de borde no cuentan para el mínimo).
  </behavior>
  <action>
    En `_actions.schemas.ts`: declarar y exportar `startImpersonationSchema = z.object({ businessId, reason: z.string().trim().min(10) })` reusando la const `businessId = z.uuid()` ya existente en el archivo; exportar el tipo `StartImpersonationInput = z.infer<typeof startImpersonationSchema>`. El mínimo de 10 chars es la decisión D-07 ("mínimo de caracteres") validada SERVER-SIDE (Pitfall 5: el dialog es refuerzo, no la garantía).

    En `_actions.schemas.test.ts`: añadir un bloque `describe('startImpersonationSchema', ...)` con casos para uuid válido + reason >=10, businessId no-uuid rechazado, reason corto rechazado, y reason de borde (exactamente 10 chars). Reusar la const `UUID` ya definida en el archivo.

    En `_actions.ts`: importar `startImpersonationSchema` del módulo de schemas y `redirect` de 'next/navigation'. Declarar y exportar `async function startImpersonation(input: unknown): Promise<void>` siguiendo el patrón obligatorio del archivo pero SIN mutar `businesses`: (1) `const actor = await requireAdmin()` como PRIMERA línea (Pitfall 2: la action es un endpoint POST invocable directo); (2) `const { businessId, reason } = startImpersonationSchema.parse(input)`; (3) `await logAudit({ actorId: actor.id, action: 'user.impersonate', targetType: 'business', targetId: businessId, businessId, risk: 'alto', reason })` — usar el string EXACTO `'user.impersonate'` (lo mapea el visor en auditoria-client.tsx:67; con `'impersonate'` a secas mostraría el código crudo) y `risk: 'alto'` (D-08); (4) `redirect(\`/admin/negocios/${businessId}/ver\`)` (D-04: navegar, sin estado global). NO declarar `createAdminClient`, NO `admin.from(...).update/insert/delete`, NO `revalidatePath`: esta action es la ÚNICA del árbol de impersonación y no muta datos de negocio (D-02). El `redirect()` va fuera de cualquier try/catch (lanza NEXT_REDIRECT).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx vitest run "app/(crm)/admin/_actions.schemas.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "startImpersonationSchema" "app/(crm)/admin/_actions.schemas.ts"` muestra la declaración exportada con `reason: z.string().trim().min(10)`.
    - SOURCE: `grep -n "user.impersonate" "app/(crm)/admin/_actions.ts"` muestra el action code exacto dentro de `logAudit`.
    - SOURCE (prohibición D-02): el grep `grep -nE "\.update\(|\.insert\(|\.delete\(|\.upsert\(|revalidatePath" "app/(crm)/admin/_actions.ts"` SÍ devuelve coincidencias y eso es esperado — pertenecen a las otras actions de Phase 2 (`changePlan`, `suspendBusiness`, `toggleAddon`, `updatePlanPrice`, etc.). El check NO es "0 coincidencias en el archivo": es que NINGUNA caiga DENTRO del cuerpo de `startImpersonation`. Verificación acotada (no falsos positivos): `sed -n '/export async function startImpersonation/,/^export /p' "app/(crm)/admin/_actions.ts" | grep -nE "\.update\(|\.insert\(|\.delete\(|\.upsert\(|revalidatePath"` NO devuelve nada.
    - SOURCE: `grep -n "redirect(" "app/(crm)/admin/_actions.ts"` muestra `redirect(\`/admin/negocios/${businessId}/ver\`)`.
    - TEST: el comando de `<verify>` pasa con todos los casos del nuevo describe en verde.
  </acceptance_criteria>
  <done>startImpersonationSchema valida businessId uuid + reason min 10 server-side; startImpersonation audita con action='user.impersonate'/risk='alto'/reason y redirige a /ver sin mutar ningún dato de negocio; tests del schema en verde.</done>
</task>

<task type="auto">
  <name>Task 2: getBusinessIntegrationStatus (presencia) + lib/impersonation.ts — capa de lectura service-role acotada por business_id</name>
  <files>lib/business-secrets.ts, lib/impersonation.ts</files>
  <read_first>
    - lib/business-secrets.ts (archivo a modificar — añadir getBusinessIntegrationStatus; calcar getBusinessSecrets: server-only, createAdminClient, scoped por business_id, business_secrets migr. 027)
    - lib/impersonation.ts (archivo NUEVO a crear)
    - lib/supabase/admin.ts (createAdminClient — única vía de lectura cross-tenant, D-03)
    - app/(crm)/admin/negocios/[id]/page.tsx (analog del read scoped: createAdminClient + .eq('id', id) + maybeSingle; comentario de aislamiento :9-19)
    - app/(dashboard)/appointments/page.tsx:18-35 (shape de queries de turnos: appointments con joins professionals(name)/services(name,price,duration_minutes), professionals active, services active, time_blocks)
    - app/(dashboard)/clients/page.tsx:18-32 (shape de queries de clientes: clients, appointments con services(name,price), professionals id/name active — D-15 PII completa)
    - app/(dashboard)/servicios/page.tsx:14-17 (services + locations por created_at)
    - app/(dashboard)/equipo/page.tsx:14 (professionals por created_at)
    - app/(dashboard)/consultorios/page.tsx:14 (locations por created_at)
    - .planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-PATTERNS.md (§ "lib/impersonation.ts" — tabla de queries exactas por sección, todas verificadas)
    - .planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-RESEARCH.md (§2 capa de lectura + Pitfall 2: olvidar .eq('business_id') = fuga cross-tenant)
    - .claude/skills/supabase-multitenant-rls/SKILL.md (checklist: con service-role el filtro de la app es la ÚNICA barrera)
  </read_first>
  <action>
    Primero, en `lib/business-secrets.ts`: añadir y exportar `async function getBusinessIntegrationStatus(businessId: string): Promise<{ mercadopago: boolean; email: boolean; recaptcha: boolean; google: boolean }>` que lee `business_secrets` con `createAdminClient()` acotado por `.eq('business_id', businessId)` y devuelve SOLO booleanos de presencia: `mercadopago = mp_access_token != null`, `email = resend_api_key != null`, `recaptcha = recaptcha_secret_key != null`, `google = google_refresh_token != null`. NUNCA devolver el valor crudo de ningún token (esto resuelve #4: la sección config necesita presencia y antes no tenía fuente de datos; getBusinessSecrets sigue siendo la única que devuelve valores y NO se llama bajo impersonación). Reusar el patrón de `getBusinessSecrets` del mismo archivo (server-only, fila ausente → todo false).

    Después, crear `lib/impersonation.ts` (server-only) que exporta `async function loadImpersonationData(businessId: string)` y el tipo `ImpersonationData` de su retorno. La función crea `createAdminClient()` UNA sola vez, importa `getBusinessIntegrationStatus` de `@/lib/business-secrets`, y ejecuta TODAS las lecturas en `Promise.all` (incluyendo `getBusinessIntegrationStatus(businessId)`), devolviendo `{ business, appointments, services, professionals, locations, clients, timeBlocks, scheduleExceptions, integrationStatus }`. El campo `integrationStatus` alimenta la sección config de Plan 03-02 (presencia conectado/desconectado).

    Reglas de scope NO negociables (D-03, la barrera única bajo service-role): el `business` se lee con `.from('businesses').select('id, name, slug, plan, plan_status, palette, theme, font').eq('id', businessId).maybeSingle()` — incluir explícitamente `palette, theme, font` (los necesita PaletteScript, D-12) y NUNCA columnas de tokens/secretos. TODAS las demás colecciones se acotan con `.eq('business_id', businessId)`: `appointments` con `.select('*, professionals(name), services(name, price, duration_minutes)')` ordenado por date/time; `services` `.select('*')` por created_at; `professionals` `.select('*')` por created_at; `locations` `.select('*')` por created_at; `clients` `.select('*')` por created_at (D-15: PII completa, sin masking); `time_blocks` `.select('*')`; `schedule_exceptions` `.select('*')` por date. Calcar los SELECT exactos de los loaders del dashboard listados en read_first, sustituyendo el cliente de sesión por `admin` y resolviendo por `businessId` en vez de `owner_id`.

    PROHIBIDO en este archivo (garantía D-02/D-03, verificado en acceptance): no importar `@/lib/supabase/client` ni `@/lib/supabase/server`; no usar `.eq('owner_id', ...)`; no usar `.update(`/`.insert(`/`.delete(`/`.upsert(`; no llamar `getBusinessSecrets` ni seleccionar columnas de secretos. Centralizar el scope acá evita que una query suelta olvide el filtro (Pitfall 2). Si `business` es null, devolver `business: null` y dejar que el loader haga `notFound()`.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c ".eq('business_id'," lib/impersonation.ts` devuelve >= 7 (una por cada colección acotada; el conteo exacto depende del número de tablas, pero nunca 0).
    - SOURCE (prohibición D-03): `grep -c "owner_id" lib/impersonation.ts` devuelve 0.
    - SOURCE (prohibición D-02): `grep -cE "\.update\(|\.insert\(|\.delete\(|\.upsert\(" lib/impersonation.ts` devuelve 0.
    - SOURCE (prohibición fuga de clientes Supabase): `grep -cE "@/lib/supabase/client|@/lib/supabase/server" lib/impersonation.ts` devuelve 0 (solo se importa createAdminClient de @/lib/supabase/admin).
    - SOURCE (prohibición secretos crudos): `grep -c "getBusinessSecrets\b" lib/impersonation.ts` devuelve 0 (no se llama la función que devuelve valores crudos; solo getBusinessIntegrationStatus).
    - SOURCE (#4 presencia): `grep -n "getBusinessIntegrationStatus" lib/business-secrets.ts` muestra la función exportada devolviendo `{ mercadopago, email, recaptcha, google }` booleanos; `grep -n "getBusinessIntegrationStatus\|integrationStatus" lib/impersonation.ts` muestra que se llama y que `integrationStatus` está en el retorno/tipo.
    - SOURCE (#4 sin valores crudos): en `lib/business-secrets.ts`, getBusinessIntegrationStatus NO retorna `mp_access_token`/`resend_api_key`/`google_refresh_token` ni ningún valor string — solo `!= null` mapeado a boolean.
    - SOURCE: `grep -n "palette, theme, font\|palette,theme,font" lib/impersonation.ts` muestra que el SELECT de businesses incluye palette/theme/font (D-12).
    - BUILD: `npx tsc --noEmit` compila sin errores.
  </acceptance_criteria>
  <done>getBusinessIntegrationStatus devuelve presencia (booleanos) sin exponer valores; loadImpersonationData(businessId) lee todas las secciones operativas con service-role acotado por business_id (negocio por id) + integrationStatus, incluye palette/theme/font, sin owner_id, sin clientes de sesión/browser, sin mutaciones, sin secretos crudos; compila.</done>
</task>

<task type="auto">
  <name>Task 3: /ver/page.tsx — loader RSC read-only con guard + paleta del negocio + auditoría por carga; label del visor</name>
  <files>app/(crm)/admin/negocios/[id]/ver/page.tsx, app/(crm)/admin/auditoria/auditoria-client.tsx</files>
  <read_first>
    - app/(crm)/admin/negocios/[id]/ver/page.tsx (archivo NUEVO a crear)
    - app/(crm)/admin/negocios/[id]/page.tsx (analog del loader: params Promise + await params, createAdminClient, notFound; comentario de aislamiento)
    - app/(crm)/layout.tsx (guard del layout (crm) + el wrapper `dark crm-shell` que envuelve TODO el subtree — Pitfall 4: hay que escapar de él en /ver)
    - lib/admin-guard.ts (requireAdmin — re-validar en el loader, Pitfall 2; devuelve el User → .id es el actorId)
    - lib/audit.ts (logAudit — escribir la fila user.impersonate.view por carga; service-role, best-effort)
    - app/(crm)/admin/auditoria/auditoria-client.tsx (archivo a modificar — el mapa ACTION_LABEL :62 necesita el label del nuevo código user.impersonate.view, si no el visor muestra el código crudo)
    - lib/impersonation.ts (loadImpersonationData — creado en Task 2)
    - components/palette-script.tsx (PaletteScript: recibe palette/theme/font y los setea en <html> antes de pintar)
    - app/(dashboard)/layout.tsx:40 (uso de PaletteScript con palette/theme/font del negocio)
    - .planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-PATTERNS.md (§ "/ver/page.tsx" — loader + aplicación de paleta + escape del dark shell)
    - .planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-RESEARCH.md (§4 PaletteScript + Pitfall 4 dark shell)
  </read_first>
  <action>
    Crear `app/(crm)/admin/negocios/[id]/ver/page.tsx` como RSC default export `VerPage({ params }: { params: Promise<{ id: string }> })`. Orden: (1) `const actor = await requireAdmin()` PRIMERA línea — re-valida en el loader, NO confía solo en el guard del layout (Pitfall 2); (2) `const { id } = await params` (Next 16: params es Promise); (3) `const data = await loadImpersonationData(id)`; (4) si `data.business` es null → `notFound()`.

    #2 TRAIL COMPLETO — auditoría por carga: DESPUÉS del `notFound()` (o sea, solo cuando el negocio existe y se va a renderizar), escribir `await logAudit({ actorId: actor.id, action: 'user.impersonate.view', targetType: 'business', targetId: id, businessId: id, risk: 'medio' })` — SIN motivo (el motivo vive en la fila de entrada user.impersonate). Esto garantiza que NINGUNA vista de datos del tenant ocurra sin dejar traza, incluido el acceso directo por URL (bookmark/historial) que no pasa por el botón. Comentario español: por qué se audita en el loader y no solo en la action (trail hermético para producto multi-operador); aceptamos sobre-conteo menor por refresh/prefetch — `/ver` es ruta autenticada dinámica (requireAdmin + service-role la hacen no-prefetchable de forma agresiva), si en el futuro genera ruido se deduplica por (actor, business, minuto). logAudit es best-effort y NO debe romper el render si falla.

    Render: aplicar `<PaletteScript palette={data.business.palette} theme={data.business.theme} font={data.business.font} />` con los valores del negocio IMPERSONADO (D-12). Resolver el ESCAPE del shell dark del CRM (Pitfall 4): el layout `(crm)` envuelve todo en `<div className="dark crm-shell">`, lo opuesto a lo que pide D-12 (la paleta del cliente, no el shell dark). Montar el contenido de `/ver` dentro de un contenedor full-bleed propio que NEUTRALICE el shell dark — un wrapper raíz que NO herede `dark crm-shell` aplicando su propio contexto de tema del negocio (ej. contenedor con clases que reseteen background/foreground al tema del negocio y, si hace falta, `:not(.dark)` semantics vía el data-palette que PaletteScript ya setea en <html>). Documentar en un comentario español POR QUÉ /ver escapa del shell dark (objetivo D-12: ver lo que ve el cliente, no el chrome del CRM).

    En este plan el cuerpo de la página puede dejar un placeholder mínimo donde irán los renderers + el banner (los provee Plan 03-02): renderizar `data.business.name` y un marcador comentado `{/* Plan 03-02: ImpersonationBanner + renderers read-only por sección */}`. NO montar ningún `*-client.tsx` del dashboard, NO importar `@/lib/supabase/client`, NO declarar server actions de mutación (D-02).

    #2 Label del visor: en `app/(crm)/admin/auditoria/auditoria-client.tsx`, agregar al mapa `ACTION_LABEL` (:62) la entrada `'user.impersonate.view': 'Vio ficha (impersonación)'` para que las filas de carga se muestren legibles (sin esto el visor cae al código crudo vía `?? action`). NO tocar nada más del visor.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit -p tsconfig.json && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "requireAdmin" "app/(crm)/admin/negocios/[id]/ver/page.tsx"` muestra la llamada como primera operación del loader (Pitfall 2).
    - SOURCE: `grep -n "await params" "app/(crm)/admin/negocios/[id]/ver/page.tsx"` muestra el await del Promise de params (Next 16).
    - SOURCE: `grep -n "loadImpersonationData" "app/(crm)/admin/negocios/[id]/ver/page.tsx"` muestra la lectura vía el helper (no queries inline).
    - SOURCE: `grep -n "PaletteScript" "app/(crm)/admin/negocios/[id]/ver/page.tsx"` muestra PaletteScript alimentado con palette/theme/font del negocio.
    - SOURCE (prohibición D-02): `grep -cE "@/lib/supabase/client|agenda-client|appointments-client|clients-client|settings-client" "app/(crm)/admin/negocios/[id]/ver/page.tsx"` devuelve 0.
    - SOURCE (#2 auditoría por carga): `grep -n "user.impersonate.view" "app/(crm)/admin/negocios/[id]/ver/page.tsx"` muestra el logAudit con action='user.impersonate.view' y risk='medio', ubicado DESPUÉS del notFound() (solo se audita si el negocio existe).
    - SOURCE (#2 label visor): `grep -n "user.impersonate.view" "app/(crm)/admin/auditoria/auditoria-client.tsx"` muestra la entrada agregada al mapa ACTION_LABEL.
    - BUILD: `npx tsc --noEmit` compila y `npm run lint` no reporta errores en el archivo nuevo.
  </acceptance_criteria>
  <done>El loader /ver/page.tsx re-valida requireAdmin, lee por business_id vía loadImpersonationData, hace notFound() si no existe, audita CADA carga con action='user.impersonate.view'/risk='medio' (trail completo #2), aplica PaletteScript del negocio impersonado, escapa del shell dark del CRM (D-12) y deja el punto de montaje para banner+renderers de Plan 03-02; el visor muestra el label nuevo; sin clientes que muten; compila y lint limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador (browser) → server action `startImpersonation` | input no confiable (businessId, reason) cruza; un POST directo salta el ConfirmDialog |
| server action / loader → Postgres (service-role) | service-role bypassa RLS; el filtro `business_id` de la app es la ÚNICA barrera de aislamiento |
| URL param `[id]` → loader `/ver` | el id viene del cliente; NO es autorización (la garantía es requireAdmin) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `loadImpersonationData` (service-role sin filtro) | high | mitigate | Toda query acotada con `.eq('business_id', businessId)` (negocio con `.eq('id', businessId)`); centralizado en un único helper para no olvidar el filtro; acceptance griega `owner_id`==0 y cuenta los `.eq('business_id',`. (Task 2) |
| T-03-02 | Tampering | árbol `app/(crm)/admin/negocios/[id]/ver/` | high | mitigate | Read-only POR CONSTRUCCIÓN (D-02): la única action del árbol (`startImpersonation`) no muta `businesses`; `lib/impersonation.ts` no contiene update/insert/delete; acceptance griega esos verbos ==0. (Tasks 1,2) |
| T-03-03 | Elevation of Privilege | loader `/ver/page.tsx` y action invocados directo | high | mitigate | `requireAdmin()` re-llamado en el loader Y como primera línea de la action (Pitfall 2: no confiar solo en el layout). (Tasks 1,3) |
| T-03-04 | Repudiation | acceso sin motivo / no auditado | high | mitigate | Entrada por botón: `reason` validado server-side `z.string().trim().min(10)` + `logAudit` action='user.impersonate' (D-08), re-entrada = fila nueva (D-09) (Task 1). **#2 TRAIL COMPLETO:** además el loader audita CADA carga con action='user.impersonate.view'/risk='medio' (Task 3), de modo que el acceso directo por URL —sin botón— TAMBIÉN deja traza: no hay vista de datos del tenant sin auditoría (requisito de producto multi-operador). |
| T-03-05 | Information Disclosure | secretos del negocio bajo impersonación | medium | mitigate | El helper NO llama `getBusinessSecrets` ni selecciona columnas de tokens; config se mostrará como presencia (Plan 03-02). (Task 2) |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | Esta fase NO instala paquetes (RESEARCH: cero paquetes nuevos); sin superficie de supply-chain nueva. |
</threat_model>

<verification>
- `npx vitest run "app/(crm)/admin/_actions.schemas.test.ts"` en verde (schema del motivo server-side).
- `npx tsc --noEmit` compila todo el proyecto.
- `npm run lint` sin errores en los archivos nuevos/modificados.
- Greps de prohibición (read-only-by-construction + scope) pasan según acceptance_criteria de cada tarea.
</verification>

<success_criteria>
- IMP-02: iniciar impersonación exige motivo (min 10 server-side) y registra UNA fila en audit_log (action='user.impersonate', risk='alto', business_id, reason); re-entrada = fila nueva. #2 Además, CADA carga de /ver audita action='user.impersonate.view' (risk='medio', sin motivo): trail completo, ninguna vista del tenant sin traza (incluido acceso directo por URL).
- IMP-03 (parcial): la lectura está acotada por business_id sin fuga cross-tenant; el negocio se resuelve por id de URL con service-role, nunca por owner_id.
- IMP-01 (backbone): el árbol /ver no tiene ningún write path (action sin mutación, helper sin update/insert/delete, sin cliente browser) — la garantía read-only por ausencia de writes queda establecida.
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-01-SUMMARY.md` when done
</output>
