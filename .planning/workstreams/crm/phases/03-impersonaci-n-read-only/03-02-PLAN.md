---
phase: 03-impersonaci-n-read-only
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/crm/impersonation-banner.tsx
  - app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx
  - app/(crm)/admin/negocios/[id]/ver/page.tsx
  - components/crm/confirm-dialog.tsx
  - components/crm/confirm-dialog.test.tsx
  - app/(crm)/admin/negocios/[id]/ficha-client.tsx
autonomous: false
requirements: [IMP-01, IMP-03]
user_setup: []

must_haves:
  truths:
    - "La sub-página /ver muestra un banner fijo arriba 'Estás viendo como {nombre} · solo lectura' con acción 'Salir de la vista' (D-12)."
    - "'Salir de la vista' es un <Link> que navega a otra parte del CRM (no apaga ningún estado global) — salir = navegar (D-04)."
    - "Las secciones operativas (agenda/turnos, servicios, equipo, consultorios, clientes, negocio/config) se renderizan en read-only desde la data ya cargada server-side, sin botones de acción ni mutación."
    - "El botón 'Ver como cliente' en la ficha de Phase 2 abre el ConfirmDialog nivel 'VER' con motivo obligatorio + disclaimer legal, y al confirmar llama startImpersonation (D-07/D-10/D-14)."
    - "#3 El ConfirmDialog del 'VER' exige el motivo con mínimo 10 caracteres ANTES de habilitar confirmar (vía la prop aditiva minReasonLength=10), alineado con el min(10) server-side: un motivo corto se bloquea inline con helper, no produce el toast genérico de error. La prop es opcional (default = comportamiento actual 'no vacío'); las llamadas de Phase 2 NO se modifican."
    - "#4 La sección config bajo impersonación muestra la PRESENCIA de integraciones (MercadoPago/email/reCAPTCHA/Google: conectado/desconectado) leída de data.integrationStatus (booleanos), nunca el valor crudo de un token."
    - "Se puede iniciar la vista para un negocio en cualquier estado, incluido suspended (D-11); el estado se refleja en el banner."
    - "Las secciones excluidas (historia clínica, finanzas) NO se renderizan bajo impersonación (D-06)."
  artifacts:
    - path: "components/crm/impersonation-banner.tsx"
      provides: "Banner fijo amarillo + 'Salir de la vista' (presentacional puro)"
      contains: "Salir de la vista"
    - path: "app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx"
      provides: "Renderers read-only por sección, alimentados por props (sin 'use client' que mute)"
      contains: "ImpersonationView"
    - path: "app/(crm)/admin/negocios/[id]/ficha-client.tsx"
      provides: "Botón 'Ver como cliente' + ConfirmDialog confirmWord='VER' requireReason → startImpersonation"
      contains: "confirmWord=\"VER\""
  key_links:
    - from: "app/(crm)/admin/negocios/[id]/ficha-client.tsx"
      to: "app/(crm)/admin/_actions.ts"
      via: "onConfirm={async (reason) => await startImpersonation({ businessId, reason })}"
      pattern: "startImpersonation\\("
    - from: "app/(crm)/admin/negocios/[id]/ver/page.tsx"
      to: "app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx"
      via: "render de las secciones operativas con la data de loadImpersonationData"
      pattern: "ImpersonationView"
    - from: "app/(crm)/admin/negocios/[id]/ver/page.tsx"
      to: "components/crm/impersonation-banner.tsx"
      via: "banner fijo con businessName + planStatus"
      pattern: "ImpersonationBanner"
  prohibitions:
    - "NINGÚN renderer read-only ni impersonation-view.tsx importa @/lib/supabase/client (cliente browser) (D-02)."
    - "NINGÚN componente del árbol /ver importa o monta agenda-client/appointments-client/clients-client/settings-client (los *-client.tsx del dashboard mutan; D-02/Pitfall 1)."
    - "NINGÚN renderer read-only contiene fetch('/api/...') de mutación ni botones onClick que disparen update/delete/insert (D-02)."
    - "NO se renderizan secciones clinical-history ni finances bajo impersonación (D-06)."
    - "Config bajo impersonación muestra solo PRESENCIA de integraciones (conectado/desconectado), nunca el valor crudo de un secreto/token (RESEARCH §1 / Open Q1)."
---

<objective>
Construir la SUPERFICIE VISUAL read-only de la impersonación sobre el backbone de Plan 03-01: el banner fijo de impersonación, los renderers presentacionales por sección (alimentados por la data ya cargada server-side por `loadImpersonationData`), y el punto de entrada en la ficha de Phase 2 (botón "Ver como cliente" + ConfirmDialog nivel "VER" con motivo + disclaimer que dispara `startImpersonation`).

Regla central (D-02/D-13, Pitfall 1): D-13 dice "reusar componentes presentacionales alimentados con data read-only; los que mutan necesitan variante read-only por props". El mapeo de RESEARCH §1 resolvió que los 4 `*-client.tsx` operativos NO son reusables (todos mutan), así que la "variante read-only por props" se materializa como renderers NUEVOS y delgados sobre primitivos puros (`Card`/`Badge`/`Table`/`StatusBadge`/`UpcomingAppointments`), calcando el ESTILO de render de `dashboard/page.tsx`. NO se reusa ningún `*-client.tsx` operativo del dashboard. La ausencia de write paths en esta superficie ES la garantía read-only.

Purpose: cerrar IMP-03 (banner + salir + fidelidad visual) e IMP-01 desde el lado UI (refuerzo, no garantía) habilitando la entrada auditada desde la ficha.
Output: `ImpersonationBanner`, `ImpersonationView` (+ renderers por sección), botón + dialog "VER" en `ficha-client.tsx`, y el render final de `/ver/page.tsx`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-CONTEXT.md
@.planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-RESEARCH.md
@.planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-PATTERNS.md
@.planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-01-SUMMARY.md
@.claude/skills/convenciones-forjo/SKILL.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03-02)

| Símbolo | Tipo | Archivo |
|---------|------|---------|
| `ImpersonationBanner` | componente (presentacional) | `components/crm/impersonation-banner.tsx` |
| `ImpersonationView` | componente (presentación read-only, secciones operativas) | `app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx` |
| renderers read-only por sección (turnos/servicios/equipo/consultorios/clientes/negocio-config) | sub-componentes presentacionales | `app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx` |
| `minReasonLength?` (prop opcional aditiva) | #3 alinea el min del motivo dialog↔server (default = comportamiento actual) | `components/crm/confirm-dialog.tsx` |
| botón "Ver como cliente" + `<ConfirmDialog confirmWord="VER" requireReason minReasonLength={10}>` | wiring de entrada (D-10) | `app/(crm)/admin/negocios/[id]/ficha-client.tsx` |
| render final de `VerPage` (banner + ImpersonationView) | montaje | `app/(crm)/admin/negocios/[id]/ver/page.tsx` |
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: ImpersonationBanner + renderers read-only (impersonation-view.tsx) + montaje en /ver/page.tsx</name>
  <files>components/crm/impersonation-banner.tsx, app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx, app/(crm)/admin/negocios/[id]/ver/page.tsx</files>
  <read_first>
    - components/crm/impersonation-banner.tsx (archivo NUEVO)
    - app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx (archivo NUEVO)
    - app/(crm)/admin/negocios/[id]/ver/page.tsx (archivo a modificar — reemplazar el placeholder de Plan 03-01 por banner + ImpersonationView)
    - app/(dashboard)/dashboard/page.tsx:148-187 (ESTILO a calcar: render inline que SOLO mapea data a Card/CardHeader/CardContent/Badge sin cliente browser ni mutación)
    - components/crm/status-badge.tsx (StatusBadge: presentacional puro, reusar tal cual para el estado del negocio)
    - components/dashboard/upcoming-appointments.tsx (presentacional puro verificado: recibe appointments por props, sin Supabase ni mutación — reusable para la sección agenda/próximos turnos)
    - app/(crm)/layout.tsx:47 (referencia del accent amarillo del shell para el color del banner)
    - .planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-PATTERNS.md (§ "impersonation-banner.tsx" y § "Renderers de presentación read-only" — analog de estilo + ANTI-PATRÓN crítico Pitfall 1)
    - .planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-RESEARCH.md (§1 mapeo de componentes: los 4 *-client.tsx son clase (b) inservibles; nota de scope sobre secretos de config)
    - .planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-01-SUMMARY.md (forma exacta del tipo ImpersonationData devuelto por loadImpersonationData)
  </read_first>
  <action>
    `components/crm/impersonation-banner.tsx`: componente presentacional con props `{ businessName: string; planStatus: string | null }`. Render: banner FIJO arriba (sticky/fixed top, full-width), fondo amarillo del CRM, copy "Estás viendo como {businessName} · solo lectura" + un `<Link href="/admin/negocios">` con texto "Salir de la vista" (D-04: salir = navegar a otra parte del CRM, NO apaga estado). Reflejar el estado del negocio reusando `StatusBadge` (D-11: incluido suspended). Comentario español: el banner es FEEDBACK, no la garantía read-only (la garantía es server-side por ausencia de writes, RESEARCH §3). Calcar el estilo de componente de status-badge.tsx (presentacional puro).

    `app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx`: exporta `ImpersonationView` que recibe la `ImpersonationData` de `loadImpersonationData` (importar el tipo del SUMMARY de 03-01) y renderiza las secciones operativas (D-05) en read-only: agenda/turnos, servicios, equipo, consultorios, clientes (PII completa, D-15), y negocio/config. Construir sub-componentes/secciones NUEVOS y delgados calcando el estilo de `dashboard/page.tsx:148-187` (Card + map a filas display, Badge de estado), reusando primitivos `@/components/ui/*`, `StatusBadge` y `UpcomingAppointments` donde aplique. Para la sección config/negocio: mostrar solo PRESENCIA de integraciones (ej. "MercadoPago: conectado/desconectado", "Google Calendar: conectado/desconectado") leída de `data.integrationStatus` (`{ mercadopago, email, recaptcha, google }` booleanos provistos por loadImpersonationData de Plan 03-01, #4), NUNCA el valor de un token/secreto (RESEARCH §1 / Open Q1). NO renderizar secciones clinical-history ni finances (D-06).

    PROHIBIDO (D-02, verificado en acceptance): no `'use client'` con mutación, no importar `@/lib/supabase/client`, no importar/montar ningún `*-client.tsx` del dashboard, no `fetch('/api/...')` de mutación, no botones onClick que disparen update/delete/insert. Si una sección necesita interactividad de solo-display (ej. navegación de semana de UpcomingAppointments) está permitida porque no muta.

    `app/(crm)/admin/negocios/[id]/ver/page.tsx`: reemplazar el placeholder de Plan 03-01 por el render real: `<ImpersonationBanner businessName={data.business.name} planStatus={data.business.plan_status} />` seguido de `<ImpersonationView data={data} />`, manteniendo el `PaletteScript` y el escape del shell dark ya resueltos en 03-01.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit -p tsconfig.json && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "Estás viendo como\|Salir de la vista" components/crm/impersonation-banner.tsx` muestra el copy del banner y la acción de salir.
    - SOURCE: `grep -n "href=\"/admin/negocios\"\|href={'/admin/negocios'}\|/admin/negocios" components/crm/impersonation-banner.tsx` muestra que "Salir de la vista" es un Link de navegación (D-04).
    - SOURCE (prohibición D-02): `grep -cE "@/lib/supabase/client|agenda-client|appointments-client|clients-client|settings-client" "app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx"` devuelve 0.
    - SOURCE (prohibición mutación): `grep -cE "\.update\(|\.insert\(|\.delete\(|fetch\('/api" "app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx"` devuelve 0.
    - SOURCE (prohibición D-06): `grep -cE "clinical-history|finances|historia clínica|finanzas" "app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx"` devuelve 0 (las secciones excluidas no aparecen ni como import ni como render).
    - SOURCE: `grep -n "ImpersonationBanner\|ImpersonationView" "app/(crm)/admin/negocios/[id]/ver/page.tsx"` muestra ambos montados con la data del loader.
    - SOURCE (#4 config presencia): `grep -n "integrationStatus" "app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx"` muestra que la sección config consume `data.integrationStatus` (booleanos), y `grep -cE "mp_access_token|resend_api_key|google_refresh_token" "app/(crm)/admin/negocios/[id]/ver/impersonation-view.tsx"` devuelve 0 (ningún valor crudo de secreto).
    - BUILD: `npx tsc --noEmit` compila y `npm run lint` sin errores en los archivos nuevos.
  </acceptance_criteria>
  <done>Banner fijo con copy + StatusBadge + 'Salir de la vista' (Link); ImpersonationView renderiza las secciones operativas read-only desde props (config solo presencia, sin secretos), sin clientes que muten, sin clinical-history/finances; /ver/page.tsx monta banner + view; compila y lint limpio.</done>
</task>

<task type="auto">
  <name>Task 2: ConfirmDialog minReasonLength (aditivo) + entrada en la ficha — botón "Ver como cliente" + "VER" + motivo + disclaimer</name>
  <files>components/crm/confirm-dialog.tsx, components/crm/confirm-dialog.test.tsx, app/(crm)/admin/negocios/[id]/ficha-client.tsx</files>
  <read_first>
    - components/crm/confirm-dialog.tsx (archivo a modificar de forma ADITIVA — #3: agregar prop opcional minReasonLength; ver ConfirmDialogProps :41-55, ConfirmStateInput :59-65, computeConfirmState :81-99 donde `reasonOk = reason.trim().length > 0`, y el helper reasonHelper :96)
    - components/crm/confirm-dialog.test.tsx (archivo a modificar — sumar casos para minReasonLength en los tests de computeConfirmState; entorno node)
    - app/(crm)/admin/negocios/[id]/ficha-client.tsx (archivo a modificar — sección "Acciones" :217-277, los useState :73-78, los imports de actions :27, el bloque de ConfirmDialog :298-350)
    - app/(crm)/admin/_actions.ts (startImpersonation — creada en Plan 03-01: recibe { businessId, reason })
    - .planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-PATTERNS.md (§ "Modificación de ficha-client.tsx" — patrón de cableado calcado del dialog "SUSPENDER" :311-323)
    - .planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-RESEARCH.md (§5 ConfirmDialog "VER" + motivo: onConfirm ya recibe reason; el min va server-side)
    - .planning/workstreams/crm/phases/01-cimientos-auditor-a/01-UI-SPEC.md (copy/contrato visual del nivel "VER" y disclaimer si está especificado)
  </read_first>
  <action>
    #3 — Extensión ADITIVA del ConfirmDialog (resuelve el mismatch dialog/server que hoy produce el toast genérico confuso). En `components/crm/confirm-dialog.tsx`, de forma backward-compatible (las llamadas de Phase 2 NO pasan la prop → comportamiento idéntico): (a) sumar `minReasonLength?: number` a `ConfirmDialogProps` y a `ConfirmStateInput`; (b) en `computeConfirmState`, cambiar `reasonOk = !requireReason || reason.trim().length > 0` por `reasonOk = !requireReason || reason.trim().length >= (minReasonLength ?? 1)` (default 1 = el "no vacío" actual, sin cambio para quien no pasa la prop); (c) cuando `requireReason` y el motivo es no vacío pero `< minReasonLength`, mostrar `reasonHelper = 'El motivo debe tener al menos ${minReasonLength} caracteres'` (cuando está vacío, mantener 'El motivo es obligatorio'); (d) pasar `minReasonLength` al `computeConfirmState` desde el componente. NO cambiar nada más del dialog (copy de error, niveles, estados, type-word). En `components/crm/confirm-dialog.test.tsx`: sumar casos a los tests de `computeConfirmState` — sin la prop el comportamiento no cambia (motivo 'a' habilita), con `minReasonLength: 10` un motivo de 3 chars NO habilita y de 10 sí.

    En `ficha-client.tsx`: (1) importar `startImpersonation` desde `@/app/(crm)/admin/_actions` (sumar al import existente de actions :27). (2) Añadir `const [verOpen, setVerOpen] = React.useState(false)` junto a los otros useState (:73-78). (3) En la sección "Acciones" (:217-277), añadir un botón "Ver como cliente" — habilitado para CUALQUIER estado incl. suspended (D-11: la vista es read-only y el soporte se necesita justo cuando hay problema), por lo tanto NO gatearlo con `canChangePlan`/`isSuspended`. (4) En el bloque de diálogos (:298-350), añadir un `<ConfirmDialog>` con `open={verOpen}`, `onOpenChange={setVerOpen}`, `title="Ver como cliente"`, `description=` el disclaimer legal (D-14: "Acceso de soporte en modo solo lectura. Queda auditado con tu identidad y el motivo. Uso responsable de datos de terceros."), `confirmWord="VER"`, `requireReason`, `minReasonLength={10}` (#3: alinea la UI con el min(10) server-side → feedback inline, no toast genérico), `risk="alto"`, `confirmLabel="Ver como cliente"`, y `onConfirm={async (reason) => { await startImpersonation({ businessId: data.id, reason: reason! }) }}`.

    El min(10) sigue garantizado server-side por el schema de Plan 03-01 (Pitfall 5: el dialog es refuerzo, no la garantía); `minReasonLength` solo mejora la UX para que el operador no choque contra el error server. `startImpersonation` redirige a `/ver` desde el server (D-04), así que el onConfirm no necesita navegación manual.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx vitest run "components/crm/confirm-dialog.test.tsx" && npx tsc --noEmit -p tsconfig.json && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -n "confirmWord=\"VER\"" "app/(crm)/admin/negocios/[id]/ficha-client.tsx"` muestra el dialog nivel "VER".
    - SOURCE: `grep -n "requireReason" "app/(crm)/admin/negocios/[id]/ficha-client.tsx"` muestra que el motivo es obligatorio (D-07).
    - SOURCE (#3): `grep -n "minReasonLength={10}\|minReasonLength: 10\|minReasonLength" "app/(crm)/admin/negocios/[id]/ficha-client.tsx"` muestra que el dialog "VER" recibe minReasonLength=10.
    - SOURCE: `grep -n "startImpersonation" "app/(crm)/admin/negocios/[id]/ficha-client.tsx"` muestra la invocación en onConfirm con { businessId: data.id, reason }.
    - SOURCE: `grep -n "Ver como cliente" "app/(crm)/admin/negocios/[id]/ficha-client.tsx"` muestra el botón de entrada y el title del dialog.
    - SOURCE (D-11): el botón "Ver como cliente" NO está condicionado por `isSuspended`/`canChangePlan` (verificar que el botón se renderiza sin gating de estado).
    - SOURCE (#3 cambio aditivo y acotado): `grep -n "minReasonLength" "components/crm/confirm-dialog.tsx"` muestra la prop opcional agregada en ConfirmDialogProps + ConfirmStateInput + computeConfirmState; el resto del componente (copy de error, niveles, estados) sin cambios. Las llamadas de Phase 2 (suspend/precio/plan/addon/trial) NO pasan minReasonLength → su comportamiento es idéntico.
    - TEST (#3): el comando de `<verify>` corre `confirm-dialog.test.tsx` en verde — incl. los casos nuevos (sin prop: motivo 'a' habilita; con minReasonLength=10: motivo de 3 chars NO habilita, de 10 sí).
    - BUILD: `npx tsc --noEmit` compila y `npm run lint` sin errores.
  </acceptance_criteria>
  <done>El ConfirmDialog gana la prop opcional minReasonLength (aditiva, Phase 2 intacta) con su test en verde; la ficha de Phase 2 tiene el botón "Ver como cliente" (sin gating de estado, D-11) que abre ConfirmDialog "VER" con motivo obligatorio min 10 (feedback inline) + disclaimer (D-14) y dispara startImpersonation; compila y lint limpio.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint humano — flujo end-to-end de impersonación read-only</name>
  <action>Verificación humana (no automatizable end-to-end sin sesión real de operador admin + datos de negocio): el operador valida el flujo completo de impersonación en el navegador siguiendo los pasos de <how-to-verify> y aprueba o reporta problemas. Cubre lo que los greps no pueden: fidelidad visual de la paleta del negocio (D-12), banner fijo, ausencia de botones de mutación a la vista, y la fila de auditoría generada por acceso.</action>
  <what-built>
    El flujo completo de impersonación read-only: botón "Ver como cliente" en la ficha → ConfirmDialog nivel "VER" con motivo obligatorio + disclaimer → audita el acceso (action='user.impersonate', risk='alto') → navega a /admin/negocios/[id]/ver renderizado con la paleta del negocio impersonado, banner fijo "Estás viendo como X · solo lectura" + "Salir de la vista", y las secciones operativas en read-only.
  </what-built>
  <how-to-verify>
    1. Arrancar el dev server: `npm run dev`. Loguearse como operador con is_admin=true.
    2. Ir a `/admin/negocios`, abrir la ficha de un negocio (probar con uno ACTIVO y con uno SUSPENDED — D-11).
    3. En "Acciones", clickear "Ver como cliente". Verificar: el dialog pide escribir "VER" (exacto) y un motivo; el botón confirmar queda deshabilitado hasta tener ambos; el disclaimer legal está visible (D-14).
    4. #3 Escribir "VER" + un motivo de < 10 chars (ej. "ok"): el botón confirmar debe quedar DESHABILITADO con helper inline "El motivo debe tener al menos 10 caracteres" (NO debe aparecer el toast genérico de error). Completar el motivo a >=10 chars → el botón se habilita. Confirmar.
    5. Verificar que navega a `/admin/negocios/[id]/ver`: la página se ve con la PALETA/TEMA del negocio (no el shell dark del CRM, D-12); el banner amarillo fijo arriba dice "Estás viendo como {nombre} · solo lectura" y muestra el estado (incl. "Suspendido" si aplica); hay un link "Salir de la vista".
    6. Verificar que se ven las secciones operativas (agenda/turnos, servicios, equipo, consultorios, clientes con datos completos) y que NO hay botones de editar/eliminar/guardar en ninguna (read-only). NO debe haber sección de historia clínica ni finanzas (D-06). La config muestra "conectado/desconectado", nunca un token.
    7. Clickear "Salir de la vista": navega fuera de /ver (a /admin/negocios), sin pedir nada (D-04).
    8. Ir a `/admin/auditoria` (visor de auditoría): verificar que hay una fila "Impersonó negocio" (riesgo Alto) con el motivo escrito, MÁS una fila "Vio ficha (impersonación)" (riesgo Medio, sin motivo) por la carga de /ver (#2). Re-entrar a /ver desde la ficha → otra fila de entrada + otra de vista (D-09).
    9. #2 trail completo: copiar la URL `/admin/negocios/[id]/ver` y entrar DIRECTO (sin pasar por el botón). Verificar en /admin/auditoria que esa carga directa TAMBIÉN dejó una fila "Vio ficha (impersonación)" (riesgo Medio) — ninguna vista del tenant sin traza.
  </how-to-verify>
  <resume-signal>Escribí "aprobado" o describí los problemas encontrados (ej. el banner no es fijo, se ve el shell dark, falta una sección, aparece un botón de mutación).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador (browser) → ConfirmDialog → server action | el dialog es refuerzo UX; la confirmación NO autoriza (la garantía es requireAdmin server-side de Plan 03-01) |
| data server-side → renderers read-only (props) | la data ya viene acotada por business_id; los renderers solo presentan |
| superficie /ver → datos del negocio impersonado | el operador ve PII de clientes finales (D-15) y datos operativos; cualquier botón de mutación sería una violación de D-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | renderers read-only / impersonation-view | high | mitigate | Renderers NUEVOS presentacionales: sin `@/lib/supabase/client`, sin `*-client.tsx` del dashboard, sin fetch de mutación, sin botones de update/delete; acceptance griega esos patrones ==0. (Task 1) |
| T-03-07 | Information Disclosure | sección config bajo impersonación | medium | mitigate | Config muestra solo presencia (conectado/desconectado), nunca el valor del secreto; no se llama getBusinessSecrets (heredado del helper de 03-01). (Task 1) |
| T-03-08 | Information Disclosure | historia clínica / finanzas | high | mitigate | clinical-history y finances NO se renderizan (D-06); acceptance griega su ausencia. (Task 1) |
| T-03-09 | Repudiation | entrada sin auditar desde la UI | medium | mitigate | El botón "Ver como cliente" SIEMPRE pasa por startImpersonation (que audita); no hay link directo a /ver sin pasar por la action. (Task 2) |
| T-03-10 | Repudiation/EoP | acceso directo a /ver por URL sin auditar | medium | mitigate | **#2 (revisado para producto multi-operador):** el loader /ver re-valida requireAdmin Y audita CADA carga con action='user.impersonate.view' (Plan 03-01 Task 3). Un acceso directo por URL —sin pasar por el botón— YA NO queda sin traza: genera su fila user.impersonate.view. El trail es hermético (ninguna vista del tenant sin auditoría); la entrada por botón aporta además el motivo. |
| T-03-SC | Tampering | npm installs | n/a | accept | Esta fase NO instala paquetes (cero paquetes nuevos). |
</threat_model>

<verification>
- `npx tsc --noEmit` compila todo el proyecto.
- `npm run lint` sin errores en archivos nuevos/modificados.
- Greps de prohibición (sin cliente browser, sin *-client.tsx, sin mutación, sin clinical-history/finances) pasan según acceptance_criteria.
- Checkpoint humano: flujo end-to-end (ficha → dialog "VER" + motivo → /ver con paleta del negocio + banner + read-only + auditoría) aprobado.
</verification>

<success_criteria>
- IMP-03: banner "Estás viendo como X · solo lectura" + "Salir de la vista" siempre presente; la vista usa la paleta/tema del negocio impersonado (D-12); funciona para cualquier estado incl. suspended (D-11).
- IMP-01 (UI/refuerzo): la superficie /ver no tiene ningún botón ni path de mutación; la entrada solo ocurre vía startImpersonation (auditada).
- D-06: historia clínica y finanzas no se muestran.
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/03-impersonaci-n-read-only/03-02-SUMMARY.md` when done
</output>
