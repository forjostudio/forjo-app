---
phase: 02-admin-de-plataforma
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - components/crm/addon-toggle.tsx
  - components/crm/extend-trial-dialog.tsx
  - components/crm/plan-price-card.tsx
  - app/(crm)/admin/negocios/[id]/page.tsx
  - app/(crm)/admin/negocios/[id]/ficha-client.tsx
  - app/(crm)/admin/planes/page.tsx
  - app/(crm)/admin/planes/planes-client.tsx
autonomous: true
requirements: [ADM-02, ADM-03, ADM-04, ADM-05, ADM-06]
must_haves:
  truths:
    - "La ficha /admin/negocios/[id] muestra plan, plan_status (StatusBadge), estado de suscripción MP, contacto (email del dueño + WhatsApp) y los 2 add-ons con su estado"
    - "El operador cambia el plan desde la ficha (ConfirmDialog simple → changePlan) y queda en auditoría"
    - "El operador suspende (ConfirmDialog 'SUSPENDER' → suspendBusiness) o reactiva, y extiende trial (ExtendTrialDialog presets 7/14/30 + fecha exacta → extendTrial)"
    - "El operador togglea los 2 add-ons (Web a medida / Recordatorios WhatsApp) con AddonToggle → toggleAddon, optimista + revert en error"
    - "El editor /admin/planes muestra 3 PlanPriceCard (basic/studio/pro = Básico/Estudio/Pro, ARS) y edita el precio (ConfirmDialog 'CONFIRMAR' → updatePlanPrice)"
    - "El banner de /admin/planes aclara que editar un precio NO altera suscripciones activas sin aviso (D-04)"
  prohibitions:
    - "NUNCA renderizar 'SMS', 'Reportes avanzados', 'Multi-sucursal' ni 'Acceso API' como add-ons toggleables: exactamente 2 flags (has_web_custom='Web a medida', has_whatsapp='Recordatorios WhatsApp')"
    - "Los precios mostrados NO salen del price_usd de lib/plans.ts: vienen de plan_prices (ARS) vía getPlanPrices"
    - "Las cards de planes NO usan los labels/montos del mock ('Equipo $48.000'): usar las 3 keys reales basic/studio/pro con su precio real"
    - "NO se agrega un bloque de precios de add-ons en /admin/planes (deferido a v2 ADDON-PAY-01)"
  artifacts:
    - path: "components/crm/addon-toggle.tsx"
      provides: "Switch de add-on (optimista + toast + revert) sobre @base-ui/react/switch"
      exports: ["AddonToggle"]
    - path: "components/crm/extend-trial-dialog.tsx"
      provides: "Dialog de extender trial (presets + react-day-picker)"
      exports: ["ExtendTrialDialog"]
    - path: "components/crm/plan-price-card.tsx"
      provides: "Card de plan con editor de precio (ConfirmDialog 'CONFIRMAR')"
      exports: ["PlanPriceCard"]
    - path: "app/(crm)/admin/negocios/[id]/page.tsx"
      provides: "Ficha de negocio RSC (dynamic param)"
    - path: "app/(crm)/admin/planes/page.tsx"
      provides: "Editor de precios RSC"
  key_links:
    - from: "app/(crm)/admin/negocios/[id]/ficha-client.tsx"
      to: "app/(crm)/admin/_actions.ts"
      via: "ConfirmDialog/AddonToggle/ExtendTrialDialog invocan changePlan/suspendBusiness/extendTrial/toggleAddon"
      pattern: "changePlan|suspendBusiness|extendTrial|toggleAddon"
    - from: "app/(crm)/admin/planes/planes-client.tsx"
      to: "app/(crm)/admin/_actions.ts"
      via: "ConfirmDialog 'CONFIRMAR' invoca updatePlanPrice"
      pattern: "updatePlanPrice"
    - from: "app/(crm)/admin/negocios/[id]/page.tsx"
      to: "lib/plan-prices.ts"
      via: "precio del plan actual desde plan_prices"
      pattern: "getPlanPrices"
---

<objective>
Construir las pantallas de mutación del CRM: la Ficha de negocio `/admin/negocios/[id]` (contacto + suscripción + acciones plan/suspender/extender-trial + toggles de add-ons) y el Editor de precios `/admin/planes` (3 cards de plan editables). Cada acción peligrosa pasa por el ConfirmDialog escalonado de Phase 1 y dispara la server action correspondiente de Plan 02.

Purpose: ADM-02 (ficha completa), ADM-03 (cambiar plan), ADM-04 (suspender/reactivar + extender trial), ADM-05 (editar precios), ADM-06 (add-ons). Reproduce los mocks 04-ficha-resumen.png y 08-planes.png con los datos y add-ons reales (2 flags, ARS, keys basic/studio/pro), aplicando los fixes del UI-SPEC (SMS→WhatsApp; copy de D-04).
Output: 3 componentes nuevos, ficha RSC+client, planes RSC+client.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/02-admin-de-plataforma/02-UI-SPEC.md
@.planning/workstreams/crm/phases/02-admin-de-plataforma/02-PATTERNS.md
@components/crm/confirm-dialog.tsx
@components/ui/dialog.tsx
@components/ui/calendar.tsx
@lib/plans.ts
</context>

<interface_context>
De Plan 02-02 (wave 2): `app/(crm)/admin/_actions.ts` exporta `changePlan`, `suspendBusiness`, `reactivateBusiness`, `extendTrial`, `toggleAddon`, `updatePlanPrice` (`'use server'`). Cada una recibe `input: unknown`, valida con zod y lanza `Error('update_failed')` en fallo de DB (el ConfirmDialog ya captura el throw → toast + deja el dialog abierto, `confirm-dialog.tsx:192-205`).

De Plan 02-03 (wave 2): `components/crm/status-badge.tsx` (`StatusBadge`); el directorio `/admin/negocios` cuyas filas ya enlazan a `/admin/negocios/{id}` (esta ficha es el destino).

De Plan 02-01 (wave 1): `getPlanPrices()` (lib/plan-prices.ts); `resolveTrialEndsAt` (lib/crm-metrics.ts) si el dialog necesita previsualizar la fecha; columnas `has_web_custom`/`has_whatsapp`.

ConfirmDialog (Phase 1, contrato locked): props `open/onOpenChange/title/description/confirmWord/requireReason/risk/confirmLabel/destructive/onConfirm`. Niveles: Cambiar plan = simple (sin confirmWord); Suspender = confirmWord="SUSPENDER" + destructive; Editar precio = confirmWord="CONFIRMAR". El `onConfirm:(reason?)=>Promise<void>` llama la server action; el dialog maneja loading/anti-doble-submit/toast-error/no-cerrar-en-fallo.

Switch: NO existe `components/ui/switch.tsx`. Usar el primitivo `@base-ui/react/switch` (ya instalado — mismo origen que `@base-ui/react/dialog` que usa el repo). Cero paquetes nuevos.

Calendario: `@/components/ui/calendar.tsx` (react-day-picker, ya en repo) para la fecha exacta.

Copy LOCKED (02-UI-SPEC §"Copywriting Contract"): usar verbatim los títulos/descripciones de los ConfirmDialog (Cambiar plan / Suspender / Editar precio), los labels de contacto ("WHATSAPP · OBLIGATORIO" / "EMAIL · OBLIGATORIO"), "ZONA SENSIBLE", el banner de /admin/planes, los toasts.

**Next 16:** consultar `node_modules/next/dist/docs/` para params dinámicos (`[id]`) en RSC + invocación de server actions desde client en 16.2.7.
</interface_context>

<artifacts_this_phase_produces>
- `components/crm/addon-toggle.tsx` (`AddonToggle`)
- `components/crm/extend-trial-dialog.tsx` (`ExtendTrialDialog`)
- `components/crm/plan-price-card.tsx` (`PlanPriceCard`)
- `app/(crm)/admin/negocios/[id]/page.tsx` + `ficha-client.tsx`
- `app/(crm)/admin/planes/page.tsx` + `planes-client.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: AddonToggle + ExtendTrialDialog + PlanPriceCard (componentes de acción)</name>
  <files>components/crm/addon-toggle.tsx, components/crm/extend-trial-dialog.tsx, components/crm/plan-price-card.tsx</files>
  <read_first>
    - components/crm/confirm-dialog.tsx (chrome del Dialog + patrón loading/anti-doble-submit; ExtendTrialDialog y PlanPriceCard reusan el Dialog base, no hand-roll)
    - components/ui/calendar.tsx (react-day-picker — fecha exacta del trial)
    - node_modules/@base-ui/react/switch (primitivo Switch — root + thumb; AddonToggle se compone sobre él)
    - lib/plans.ts (features read-only para la card de precio; topes/features viven acá, NO el precio)
    - 02-UI-SPEC.md §"AddonToggle" / §"ExtendTrialDialog" / §"PlanPriceCard" + §"Copywriting Contract" (banner planes, presets, "MÁS ELEGIDO" en pro)
  </read_first>
  <action>
    `components/crm/addon-toggle.tsx` (client): Switch sobre `@base-ui/react/switch` (root + thumb estilados con los tokens del CRM: track on = verde `--crm-success`, thumb cream, off = `--secondary`; focus-visible ring amarillo; hit area ≥44px touch). Props `{ businessId, addon:'has_web_custom'|'has_whatsapp', label, checked }`. onChange dispara `toggleAddon({businessId, addon, value})` con UI OPTIMISTA (set local + revert si la action lanza) + `toast.success`/`toast.error` (sonner). Estados off/on/focus/loading(disabled+spinner)/disabled. SIN ConfirmDialog (riesgo bajo). `aria-checked` + labelled por el nombre del add-on.

    `components/crm/extend-trial-dialog.tsx` (client, D-07): sobre el Dialog base (mismo chrome que ConfirmDialog, NO el type-to-confirm — riesgo bajo, confirmación simple). Título "Extender trial" + desc del UI-SPEC. Preset chips `+7 días`/`+14 días`/`+30 días` (toggle-group, active amarillo) Y un calendario `@/components/ui/calendar.tsx` para fecha exacta (elegir uno deselecciona el otro) — presets 7/14/30 + fecha exacta es exactamente lo que pide D-07. Mostrar "Nuevo fin de trial: {fecha}" (zona AR, fin del día — usar resolveTrialEndsAt de lib/crm-metrics para previsualizar). Botón "Extender trial" (amarillo) con loading (spinner + "Confirmando…", no cerrable) → dispara `extendTrial({businessId, preset|exactDate})`. Props `{ open, onOpenChange, businessId, currentTrialEndsAt }`.

    `components/crm/plan-price-card.tsx` (client): Card por plan. Nombre display (Básico/Estudio/Pro) + mono "{N} negocios activos" (count recibido por prop) + precio `text-4xl` heading ARS (`$15.000`) + mono "/mes" + feature list (✓ rows desde lib/plans.ts features, read-only) + botón full-width "Editar precio" (amarillo) que abre un ConfirmDialog confirmWord="CONFIRMAR" cuyo cuerpo incluye el input de monto (mono, ARS, numérico) + el copy locked "Editar precio del plan". onConfirm → `updatePlanPrice({planKey, priceArs})`. La card del plan `pro` lleva el pill "MÁS ELEGIDO" amarillo + borde de acento (atado a la key real `pro`, no al "Equipo" del mock). Props `{ planKey:'basic'|'studio'|'pro', name, priceArs, activeCount, features }`.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && test -f components/crm/addon-toggle.tsx && test -f components/crm/extend-trial-dialog.tsx && test -f components/crm/plan-price-card.tsx && grep -q "toggleAddon" components/crm/addon-toggle.tsx && grep -q "@base-ui/react/switch" components/crm/addon-toggle.tsx && grep -q "updatePlanPrice" components/crm/plan-price-card.tsx && grep -q "CONFIRMAR" components/crm/plan-price-card.tsx && echo "OK 3 componentes de accion" && npx tsc --noEmit 2>&1 | grep -iE "addon-toggle|extend-trial|plan-price" | head -5; echo "tsc done"</automated>
  </verify>
  <done>Los 3 componentes existen; AddonToggle usa @base-ui/react/switch + toggleAddon optimista; ExtendTrialDialog tiene presets + calendario; PlanPriceCard edita vía ConfirmDialog "CONFIRMAR" → updatePlanPrice; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Ficha /admin/negocios/[id] (RSC + client)</name>
  <files>app/(crm)/admin/negocios/[id]/page.tsx, app/(crm)/admin/negocios/[id]/ficha-client.tsx</files>
  <read_first>
    - app/(crm)/admin/auditoria/page.tsx (analog RSC + SELECT explícito); 02-PATTERNS §"negocios/[id]/page.tsx" (dynamic param + email del dueño vía admin.auth.admin.getUserById con fallback a notification_email)
    - components/crm/status-badge.tsx (Plan 03) + components/crm/confirm-dialog.tsx + addon-toggle.tsx + extend-trial-dialog.tsx (Task 1)
    - app/(crm)/admin/_actions.ts (Plan 02 — changePlan/suspendBusiness/reactivateBusiness/extendTrial/toggleAddon)
    - lib/plan-prices.ts (getPlanPrices — precio del plan actual) + .claude/skills/mercadopago-suscripciones/SKILL.md (semántica plan_status/subscription para el "Estado de cobro", solo lectura)
    - 02-UI-SPEC.md §"Ficha de negocio" (hero, tabs Resumen/Timeline-disabled, dos columnas, contacto, suscripción, acciones, add-ons) + §"Copywriting Contract"
  </read_first>
  <action>
    `app/(crm)/admin/negocios/[id]/page.tsx` (RSC, dynamic param): `createAdminClient()`; leer el business por id (SELECT explícito de columnas no sensibles incl. has_web_custom/has_whatsapp/whatsapp/notification_email/owner_id/plan/plan_status/trial_ends_at/subscription_ends_at/mp_subscription_id/created_at); resolver el email del dueño con `admin.auth.admin.getUserById(owner_id)` fallback a notification_email (Pitfall 6 — solo extraer el email); `getPlanPrices()` para el precio del plan actual. Si no existe el negocio → notFound(). Pasar al client solo los datos no sensibles + email + precio.

    `app/(crm)/admin/negocios/[id]/ficha-client.tsx` (client): hero (back link "Volver a negocios" + avatar + nombre `text-2xl` h1 único + StatusBadge + meta mono Dueño·Ciudad·cliente desde·plan). Tabs Resumen (activo) + Timeline (disabled/"PRONTO", convención Phase 1). Dos columnas:
    - Izquierda: Contacto (bloques WhatsApp `businesses.whatsapp`→wa.me y Email del dueño, micro-labels mono "WHATSAPP · OBLIGATORIO"/"EMAIL · OBLIGATORIO"; si falta un valor → "—" + "Sin dato", nunca inventado). Suscripción·MercadoPago (Estado de cobro: pill "Al día" verde / "Suspendido" rojo / "Vencido" rojo derivado de plan_status+subscription_ends_at; Plan actual "{Plan} · ${price}/mes" desde plan_prices; ID suscripción mono mp_subscription_id o "—").
    - Derecha: Acciones (botones amarillos "Cambiar plan" → ConfirmDialog simple → changePlan; "Extender trial" → ExtendTrialDialog → extendTrial; label mono rojo "ZONA SENSIBLE" sobre botón full-width "Suspender negocio" destructive → ConfirmDialog "SUSPENDER" → suspendBusiness; si ya está suspended, ese botón es "Reactivar negocio" amarillo no-destructive → reactivateBusiness). Add-ons: EXACTAMENTE 2 filas (AddonToggle): "Web a medida" (has_web_custom) y "Recordatorios WhatsApp" (has_whatsapp). NUNCA SMS/Reportes/Multi-sucursal/API. Copy de los ConfirmDialog verbatim del UI-SPEC.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && test -f "app/(crm)/admin/negocios/[id]/page.tsx" && grep -q "changePlan" "app/(crm)/admin/negocios/[id]/ficha-client.tsx" && grep -q "suspendBusiness" "app/(crm)/admin/negocios/[id]/ficha-client.tsx" && grep -q "Recordatorios WhatsApp" "app/(crm)/admin/negocios/[id]/ficha-client.tsx" && grep -q "Web a medida" "app/(crm)/admin/negocios/[id]/ficha-client.tsx" && ! grep -q "Recordatorios SMS\|>SMS<" "app/(crm)/admin/negocios/[id]/ficha-client.tsx" && echo "OK ficha (2 add-ons, sin SMS)" && npx tsc --noEmit 2>&1 | grep -iE "negocios/\[id\]" | head -5; echo "tsc done"</automated>
  </verify>
  <done>La ficha muestra contacto/suscripción/plan/estado/add-ons; las acciones disparan las server actions vía ConfirmDialog; exactamente 2 add-ons (Web a medida / Recordatorios WhatsApp), nunca SMS; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 3: Editor de precios /admin/planes (RSC + client)</name>
  <files>app/(crm)/admin/planes/page.tsx, app/(crm)/admin/planes/planes-client.tsx</files>
  <read_first>
    - app/(crm)/admin/auditoria/page.tsx (analog RSC); 02-PATTERNS §"planes-client.tsx"
    - lib/plan-prices.ts (getPlanPrices) + lib/plans.ts (features read-only) + lib/subscription-plans.ts (names Básico/Estudio/Pro)
    - components/crm/plan-price-card.tsx (Task 1)
    - 02-UI-SPEC.md §"PlanPriceCard" + §"Copywriting Contract" (banner warning de /admin/planes — copy truthful D-04, NO el del mock) + §"Mock-vs-data reconciliation"
  </read_first>
  <action>
    `app/(crm)/admin/planes/page.tsx` (RSC): `createAdminClient()`; `getPlanPrices()`; contar negocios activos por plan (`select plan, plan_status` y contar los `active` por plan, o reusar el mismo select del dashboard). Pasar al client el record de precios + los counts + las features por plan (de lib/plans.ts).

    `app/(crm)/admin/planes/planes-client.tsx` (client): banner warning full-width arriba del grid (amarillo-tinted + icono): copy del UI-SPEC "Editar un precio impacta la facturación de planes futuros, no altera las suscripciones activas sin aviso. Cada cambio pide confirmación y queda en auditoría." (truthful D-04, NO el del mock). Grid de 3 `PlanPriceCard` con las keys reales basic/studio/pro (Básico/Estudio/Pro), su price_ars real, su activeCount y sus features. La card pro lleva "MÁS ELEGIDO". NO agregar bloque de precios de add-ons (deferido v2). Cada card edita su precio vía ConfirmDialog "CONFIRMAR" → updatePlanPrice (cableado en el componente del Task 1).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && test -f "app/(crm)/admin/planes/page.tsx" && grep -q "getPlanPrices" "app/(crm)/admin/planes/page.tsx" && grep -q "PlanPriceCard" "app/(crm)/admin/planes/planes-client.tsx" && grep -qi "no altera las suscripciones activas" "app/(crm)/admin/planes/planes-client.tsx" && ! grep -q "Equipo" "app/(crm)/admin/planes/planes-client.tsx" && echo "OK planes (3 keys reales, copy D-04, sin Equipo)" && npx tsc --noEmit 2>&1 | grep -iE "planes" | head -5; echo "tsc done"</automated>
  </verify>
  <done>/admin/planes muestra 3 PlanPriceCard reales (basic/studio/pro, ARS de plan_prices) con el banner truthful de D-04; editar dispara updatePlanPrice vía ConfirmDialog "CONFIRMAR"; sin labels del mock ni bloque de add-ons; tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (ficha/planes) → server action | Las acciones de plan/suspender/trial/add-on/precio se disparan desde el client; la garantía es requireAdmin server-side (Plan 02), no el ConfirmDialog |
| RSC de la ficha [id] → render | El id viene del cliente (URL); el RSC re-valida admin (vía layout) y lee con service-role, no confía en el id como autorización |
| operator → admin.auth.admin.getUserById | Resolver el email del dueño expone datos del user de auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Elevation of Privilege | IDOR en la ruta [id] (manipular el business_id de la URL) | mitigate | La autorización es requireAdmin (layout guard + cada action), NO el id; el operador es super-admin y puede ver cualquier negocio legítimamente; las mutaciones re-validan admin en la action, nunca confían en el id como permiso |
| T-02-13 | Elevation of Privilege | toggle de add-on / cambio de plan sin guard si se invoca directo | mitigate | AddonToggle/ConfirmDialog solo invocan las actions; cada action corre requireAdmin (Plan 02 T-02-04) — la UI es refuerzo |
| T-02-14 | Information Disclosure | email del dueño via getUserById propaga datos de más al client | mitigate | Extraer solo el string de email en el RSC; no pasar el objeto user de auth al client |
| T-02-15 | (integridad de cobro) | mostrar/editar el precio en la moneda equivocada (usd) | mitigate | Precios desde getPlanPrices (ARS, plan_prices); features read-only de plans.ts; verify negativo de "Equipo" y del mock |
| T-02-SC | Tampering | npm installs | accept | Phase 2 no instala paquetes nuevos; Switch usa el primitivo @base-ui/react ya instalado (RESEARCH §Package Legitimacy Audit: no aplica) |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores nuevos.
- Ficha: changePlan/suspendBusiness/extendTrial/toggleAddon cableados; exactamente 2 add-ons; sin SMS.
- Planes: 3 cards reales + banner D-04; updatePlanPrice cableado; sin "Equipo" ni bloque de add-ons.
- Manual/UAT: cambiar plan / suspender / reactivar / extender trial / togglear add-on / editar precio → cada acción muestra su ConfirmDialog correcto, se refleja tras revalidatePath y aparece en /admin/auditoria.
</verification>

<success_criteria>
- El operador gestiona el ciclo de vida completo de un negocio desde la ficha (ADM-02..06) con la confirmación según riesgo.
- El operador edita los precios de los 3 planes con doble confirmación + auditoría, sin alterar suscripciones activas sin aviso (ADM-05/D-04).
- Add-ons = exactamente 2 flags, cobro manual fuera de la app.
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/02-admin-de-plataforma/02-04-SUMMARY.md` when done.
</output>
