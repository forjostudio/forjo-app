---
phase: 02-admin-de-plataforma
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/crm/kpi-card.tsx
  - components/crm/alert-list.tsx
  - components/crm/status-badge.tsx
  - app/(crm)/admin/page.tsx
  - app/(crm)/admin/negocios/page.tsx
  - app/(crm)/admin/negocios/negocios-client.tsx
  - components/crm/crm-sidebar.tsx
autonomous: true
requirements: [ADM-01, ADM-07, ALERT-01]
must_haves:
  truths:
    - "El dashboard /admin muestra 4 KPI cards con datos reales: MRR, Negocios activos, Trials por vencer, Pagos fallidos (calculados en vivo con computeKpis)"
    - "El dashboard muestra una lista de alertas clickeables (deriveAlerts); cada alerta navega a /admin/negocios/{id} (D-12)"
    - "El directorio /admin/negocios lista TODOS los negocios; los suspendidos siempre están visibles y marcados con StatusBadge rojo (ADM-01, Pitfall 4)"
    - "El directorio filtra por tabs (Todos/Activos/Trial/Suspendidos/Churn) y busca por nombre/dueño/slug; 'Todos' es el default e incluye suspendidos"
    - "Cada fila del directorio navega a la ficha del negocio y es focusable por teclado (≥44px, focus ring amarillo)"
    - "Los items de sidebar 'Negocios' y 'Planes y precios' enrutan a páginas reales (sin 'PRONTO')"
  prohibitions:
    - "El RSC NO pasa el cliente createAdminClient ni columnas secretas (tokens/secrets) al componente client: solo filas con columnas no sensibles (anti-pattern T-01-09)"
    - "El directorio NO aplica un default que oculte los suspendidos (Pitfall 4)"
    - "Las KPI cards NO muestran un delta 'vs mes anterior' ni sparkline de tendencia fabricado (no hay histórico en v1, D-03): slot vacío/plano"
    - "NO se renderizan los bloques 'Pipeline por etapa' / 'Próximas actividades' / 'Actividad reciente' del mock (Phase 4/6)"
  artifacts:
    - path: "components/crm/kpi-card.tsx"
      provides: "KPI card (label mono + valor display + slot sparkline vacío)"
      exports: ["KpiCard"]
    - path: "components/crm/alert-list.tsx"
      provides: "Lista de alertas clickeables → ficha"
      exports: ["AlertList"]
    - path: "components/crm/status-badge.tsx"
      provides: "Badge de estado del negocio (active/trial/suspended/churn)"
      exports: ["StatusBadge"]
    - path: "app/(crm)/admin/negocios/page.tsx"
      provides: "Directorio RSC (lectura global service-role)"
    - path: "app/(crm)/admin/negocios/negocios-client.tsx"
      provides: "Tabla + filtros + búsqueda + navegación a ficha"
  key_links:
    - from: "app/(crm)/admin/page.tsx"
      to: "lib/crm-metrics.ts"
      via: "computeKpis + deriveAlerts sobre el select de businesses + getPlanPrices"
      pattern: "computeKpis|deriveAlerts"
    - from: "app/(crm)/admin/negocios/negocios-client.tsx"
      to: "lib/crm-directory.ts"
      via: "filterBusinesses para tabs/búsqueda client-side"
      pattern: "filterBusinesses"
    - from: "components/crm/crm-sidebar.tsx"
      to: "app/(crm)/admin/negocios/page.tsx"
      via: "item 'Negocios' enruta a /admin/negocios"
      pattern: "/admin/negocios"
---

<objective>
Construir las pantallas de lectura del CRM: el Dashboard `/admin` (4 KPI cards con datos reales + lista de alertas clickeables) y el Directorio `/admin/negocios` (tabla buscable/filtrable que SIEMPRE muestra los suspendidos marcados), más los componentes `KpiCard`/`AlertList`/`StatusBadge`, y cablear los items de sidebar `Negocios` y `Planes y precios` a rutas reales.

Purpose: ADM-07/ALERT-01 (el operador ve KPIs y alertas arriba de todo) y ADM-01 (directorio buscable con suspendidos visibles). Todo en vivo, sin cron ni tabla de eventos (D-10/D-11). Reproduce los mocks 01-dashboard.png y 03-negocios.png con los datos reales (no los labels/montos del mock).
Output: 3 componentes nuevos, 2 páginas RSC + 1 client, sidebar cableado.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/02-admin-de-plataforma/02-UI-SPEC.md
@.planning/workstreams/crm/phases/02-admin-de-plataforma/02-PATTERNS.md
@.planning/workstreams/crm/phases/01-cimientos-auditor-a/01-UI-SPEC.md
@app/(crm)/admin/auditoria/page.tsx
@app/(crm)/admin/auditoria/auditoria-client.tsx
@components/crm/risk-badge.tsx
@components/crm/crm-sidebar.tsx
</context>

<interface_context>
De Plan 02-01 (wave 1, completo): `getPlanPrices()` en `lib/plan-prices.ts`; `computeKpis(rows, prices, now)`, `deriveAlerts(rows, now)` en `lib/crm-metrics.ts`; `filterBusinesses(rows, {query, tab})` en `lib/crm-directory.ts`. Columnas `businesses.has_web_custom`/`has_whatsapp` y tabla `plan_prices` ya existen.

Cliente Supabase: el directorio lee con `createAdminClient()` (service-role) DENTRO del RSC porque NO existe policy "is_admin lee todo" en businesses (las policies son tenant-scoped por owner_id). El RSC ya corre tras el guard de `app/(crm)/layout.tsx`. NUNCA pasar el cliente ni columnas secretas al client — solo filas leídas (anti-pattern T-01-09). SELECT explícito de columnas no sensibles (ver el SELECT en 02-PATTERNS §"negocios/page.tsx").

Analog de RSC+client: `auditoria/page.tsx` (RSC con SELECT explícito + error handling) + `auditoria-client.tsx` (FILTERS const + useState query/filter + useMemo + tabla `@/components/ui/table` + EmptyState + export CSV client-side).
</interface_context>

<artifacts_this_phase_produces>
- `components/crm/kpi-card.tsx` (`KpiCard`)
- `components/crm/alert-list.tsx` (`AlertList`)
- `components/crm/status-badge.tsx` (`StatusBadge` — calque de RiskBadge cva)
- `app/(crm)/admin/page.tsx` (Dashboard RSC — reemplaza el placeholder de Phase 1)
- `app/(crm)/admin/negocios/page.tsx` (Directorio RSC) + `negocios-client.tsx`
- sidebar: items Negocios y Planes y precios cableados
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: StatusBadge + KpiCard + AlertList (componentes presentacionales)</name>
  <files>components/crm/status-badge.tsx, components/crm/kpi-card.tsx, components/crm/alert-list.tsx</files>
  <read_first>
    - components/crm/risk-badge.tsx (calque del cva: variantes, dot inline con var(--crm-danger), NO editar el badge compartido)
    - components/crm/crm-topbar.tsx (uso de var(--crm-danger) inline + fuentes mono var(--font-geist-mono))
    - 02-UI-SPEC.md §"StatusBadge" / §"KpiCard" / §"AlertList" (contratos visuales + tabla de estados + copy de empty states)
    - app/(crm)/admin/auditoria/auditoria-client.tsx:293-317 (patrón EmptyState a reusar para AlertList vacío)
  </read_first>
  <action>
    `components/crm/status-badge.tsx`: calcar el cva de RiskBadge (NO editar el badge compartido). Variantes por estado: `active` → verde `--crm-success`; `trial` → azul `--crm-info`; `suspended` → rojo `--crm-danger` (distintivo, nunca oculto); `cancelled|expired` → churn (rojo muted). Mismo shape pill + leading dot (dot con `style={{backgroundColor:'var(--crm-...)'}}`). Texto del pill ≥3:1 sobre su fondo (dot lleva el hue, texto queda foreground/muted donde el contraste es justo). Prop `planStatus: string`; mapear cancelled/expired a "Churn". NUNCA `--destructive` (un solo rojo, §12).

    `components/crm/kpi-card.tsx`: Card (`bg-card rounded-xl ring-1 ring-foreground/10 p-4`/`p-5`). Label mono uppercase 12px (`text-muted-foreground`, var(--font-geist-mono)). Valor display `text-4xl` heading 700 (el valor real computado; MRR formateado ARS con agrupación es-AR, string limpio, sin overlap `$3.24M`+`ARS`). Slot sparkline top-right VACÍO/plano (sin tendencia fabricada, D-03). Línea secundaria opcional `sub?` solo si es derivable en vivo. Card "Pagos fallidos" → regla fina de acento superior en `--crm-danger`. Props: `label`, `value`, `tone:'accent'|'info'|'success'|'danger'|'neutral'`, `sub?`, `href?`.

    `components/crm/alert-list.tsx`: Card con heading "Alertas" + subtítulo mono. Cada alerta = fila CLICKEABLE → `/admin/negocios/{id}` (toda la fila es el affordance: `Link` wrap o role+tabindex+Enter/Space, `cursor-pointer`, hover `bg-secondary`, focus-visible ring amarillo, ≥44px). Layout fila: dot de estado (rojo `pago_fallido` / amarillo `trial_por_vencer`) + icono lucide + nombre del negocio (14px) + caption mono ("Pago fallido" / "Trial vence en {n} días" / "Trial vence hoy") + chevron. Empty state (reusar patrón EmptyState): heading "Todo en orden" + body del copy del UI-SPEC. Solo 2 tipos de alerta (D-10). Props: `alerts: {businessId, name, tipo}[]`.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && test -f components/crm/status-badge.tsx && test -f components/crm/kpi-card.tsx && test -f components/crm/alert-list.tsx && echo "OK 3 componentes" && npx tsc --noEmit 2>&1 | grep -iE "status-badge|kpi-card|alert-list" | head -5; echo "tsc done"</automated>
  </verify>
  <done>Los 3 componentes existen y compilan; StatusBadge calca el cva de RiskBadge con un solo rojo --crm-danger; KpiCard sin delta histórico fabricado; AlertList con filas clickeables + empty state.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard /admin (KPIs + alertas) — RSC</name>
  <files>app/(crm)/admin/page.tsx</files>
  <read_first>
    - app/(crm)/admin/page.tsx (placeholder actual de Phase 1 a reemplazar)
    - app/(crm)/admin/auditoria/page.tsx (analog RSC: SELECT explícito + error handling + pasar filas al client)
    - lib/crm-metrics.ts (computeKpis, deriveAlerts — Plan 01) + lib/plan-prices.ts (getPlanPrices — Plan 01)
    - lib/supabase/admin.ts (createAdminClient)
    - 02-UI-SPEC.md §"Phase 2 UI Scope" + §"Component Contracts" (KpiCard/AlertList) + §"Copywriting Contract" (page title "Dashboard", breadcrumb "Operación · Overview", labels KPI)
  </read_first>
  <action>
    Reemplazar `app/(crm)/admin/page.tsx` por un RSC que: (1) `createAdminClient()` server-only; (2) `select('id, name, plan, plan_status, trial_ends_at')` de businesses (columnas no sensibles); (3) `getPlanPrices()`; (4) `computeKpis(rows, prices)` + `deriveAlerts(rows)`; (5) renderizar el grid de 4 `KpiCard` (MRR tone accent, Negocios activos tone neutral/info, Trials por vencer tone info, Pagos fallidos tone danger con regla superior) + `AlertList`. Labels: "MRR" / "NEGOCIOS ACTIVOS" / "TRIALS POR VENCER" / "PAGOS FALLIDOS". NO renderizar pipeline/actividad del mock (Phase 4/6). NO pasar el cliente admin al client; KpiCard/AlertList reciben solo valores/filas. Error handling como auditoria (loguear + render con datos vacíos). El título de página lo provee el CrmTopbar (heredado) — esta página solo monta el contenido; si necesita un título propio usar h1 único "Dashboard".
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && grep -q "computeKpis" "app/(crm)/admin/page.tsx" && grep -q "deriveAlerts" "app/(crm)/admin/page.tsx" && grep -q "createAdminClient" "app/(crm)/admin/page.tsx" && echo "OK dashboard usa métricas" && npx tsc --noEmit 2>&1 | grep -i "admin/page" | head -5; echo "tsc done"</automated>
  </verify>
  <done>/admin renderiza 4 KpiCard con datos reales (computeKpis) + AlertList (deriveAlerts) leyendo businesses+plan_prices vía service-role en el RSC; sin bloques de pipeline/actividad; tsc limpio.</done>
</task>

<task type="auto">
  <name>Task 3: Directorio /admin/negocios (RSC + client) + cablear sidebar</name>
  <files>app/(crm)/admin/negocios/page.tsx, app/(crm)/admin/negocios/negocios-client.tsx, components/crm/crm-sidebar.tsx</files>
  <read_first>
    - app/(crm)/admin/auditoria/auditoria-client.tsx (calcar verbatim: FILTERS const, useState query/filter, useMemo, tabla @/components/ui/table, EmptyState, export CSV client-side, formato fecha AR)
    - lib/crm-directory.ts (filterBusinesses — Plan 01) + lib/plan-prices.ts (getPlanPrices)
    - components/crm/status-badge.tsx (Task 1) + components/crm/crm-sidebar.tsx:63-78 (items Negocios/Planes con soon:true a cablear)
    - 02-UI-SPEC.md §"Directory table" (columnas NEGOCIO/PLAN/ESTADO/CONTACTO/ADD-ONS/MRR, tabs, search placeholder, filas clickeables) + §"Mock-vs-data reconciliation" (keys reales basic/studio/pro)
    - 02-PATTERNS.md §"negocios/page.tsx" (SELECT explícito + email del dueño vía admin.auth.admin.getUserById con fallback a notification_email) + §"negocios-client.tsx"
  </read_first>
  <action>
    `app/(crm)/admin/negocios/page.tsx` (RSC): `createAdminClient()`; `select('id, name, slug, owner_id, plan, plan_status, trial_ends_at, subscription_ends_at, mp_subscription_id, whatsapp, notification_email, has_web_custom, has_whatsapp, created_at')` ordenado por created_at desc; `getPlanPrices()`. Resolver el email del dueño vía `admin.auth.admin.getUserById(owner_id)` con fallback a `notification_email` (Pitfall 6) — hacerlo de forma acotada (solo el dato de email, no exponer el user completo). Pasar al client solo filas con columnas no sensibles + el email resuelto + los precios. NUNCA pasar el cliente admin.

    `app/(crm)/admin/negocios/negocios-client.tsx` (client): calcar la mecánica de auditoria-client. Tabs `Todos {n} · Activos {n} · Trial {n} · Suspendidos {n} · Churn {n}` (active = amarillo; `Todos` default e INCLUYE suspendidos — Pitfall 4). Búsqueda "Buscar negocio, dueño…" sobre nombre/dueño/slug. Usar `filterBusinesses` para el filtrado en memoria (no reimplementar). Tabla con columnas NEGOCIO (avatar iniciales + nombre como link + sub-caption mono Dueño·Ciudad) / PLAN (Básico/Estudio/Pro) / ESTADO (StatusBadge) / CONTACTO (botones email mailto + WhatsApp wa.me) / ADD-ONS (count de flags activas: "2 activos"/"1 activo"/"–") / MRR (precio del plan si active, si no "–") + chevron. Fila navega a `/admin/negocios/{id}` y es focusable (tabindex/link + Enter/Space, ≥44px, focus ring amarillo). EmptyStates con el copy del UI-SPEC ("Todavía no hay negocios" / "Ningún negocio coincide" + "Limpiar filtros"). Botón "Exportar CSV" (reusar rowsToCsv/Blob de auditoria-client). Omitir la fila "TAGS" y el botón "Nuevo negocio" del mock (Phase 4 / fuera de scope v1).

    `components/crm/crm-sidebar.tsx`: en NAV_GROUPS, item `Negocios` → `href:'/admin/negocios'` (quitar `soon:true`); item `Planes y precios` → `href:'/admin/planes'` (quitar `soon:true`). No tocar el resto de los items (siguen "PRONTO").
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && grep -q "filterBusinesses" "app/(crm)/admin/negocios/negocios-client.tsx" && grep -q "createAdminClient" "app/(crm)/admin/negocios/page.tsx" && grep -q "/admin/negocios" components/crm/crm-sidebar.tsx && grep -q "/admin/planes" components/crm/crm-sidebar.tsx && echo "OK directorio + sidebar cableado" && npx tsc --noEmit 2>&1 | grep -iE "negocios|crm-sidebar" | head -5; echo "tsc done"</automated>
  </verify>
  <done>/admin/negocios lista todos los negocios con suspendidos siempre visibles + StatusBadge; tabs y búsqueda usan filterBusinesses; filas navegan a la ficha y son focusables; sidebar enruta Negocios y Planes y precios a páginas reales; tsc limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC del CRM (service-role) → componente client | El RSC lee con service-role (bypassa RLS); solo deben cruzar columnas no sensibles, nunca el cliente ni tokens |
| operator → directorio (lectura global cross-tenant) | Lectura legítima del super-admin de TODOS los negocios; va server-side tras el guard del layout |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | service-role o columnas secretas filtradas al client (negocios-client / dashboard) | mitigate | Leer en el RSC, SELECT explícito de columnas no sensibles, pasar solo filas/valores al client; el cliente nunca importa createAdminClient (anti-pattern T-01-09) |
| T-02-10 | Information Disclosure | email del dueño resuelto vía admin API expone más datos del user de lo necesario | mitigate | getUserById acotado: extraer solo el email, no propagar el objeto user completo al client |
| T-02-11 | (integridad de visibilidad) | filtro que oculta suspendidos del directorio | mitigate | 'Todos' incluye suspendidos por contrato (filterBusinesses + tab Suspendidos como pestaña extra), StatusBadge rojo distintivo (Pitfall 4) |
| T-02-SC | Tampering | npm installs | accept | Phase 2 no instala paquetes nuevos (RESEARCH §Package Legitimacy Audit: no aplica) |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores nuevos.
- Dashboard usa computeKpis/deriveAlerts; directorio usa filterBusinesses.
- Sidebar enruta Negocios → /admin/negocios y Planes y precios → /admin/planes.
- Manual/UAT: el directorio muestra los suspendidos en "Todos"; cada fila navega a la ficha; las KPI cards muestran números reales; las alertas navegan a la ficha del negocio.
</verification>

<success_criteria>
- El operador ve KPIs reales + alertas clickeables arriba de todo (ADM-07/ALERT-01).
- El operador busca/filtra el directorio con los suspendidos siempre visibles y marcados (ADM-01).
- El service-role nunca cruza al cliente.
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/02-admin-de-plataforma/02-03-SUMMARY.md` when done.
</output>