---
phase: 02-admin-de-plataforma
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/032_crm_admin.sql
  - app/api/admin/set-plan/route.ts
  - lib/plan-prices.ts
  - lib/crm-metrics.ts
  - lib/crm-metrics.test.ts
  - lib/crm-directory.ts
  - lib/crm-directory.test.ts
autonomous: false
requirements: [ADM-04, ADM-05, ADM-07, ALERT-01]
user_setup:
  - service: supabase
    why: "Migración 032 se aplica A MANO al Postgres del proyecto (convención del repo: migraciones numeradas aplicadas en orden, coordinadas con el deploy). supabase db push NO corre desatendido en CI."
    dashboard_config:
      - task: "Aplicar supabase/migrations/032_crm_admin.sql al proyecto Supabase (SQL Editor o supabase db push), DESPUÉS de escribir el archivo y ANTES de la verificación. Verificar que plan_prices tiene 3 filas (basic/studio/pro) y que businesses tiene las columnas has_web_custom/has_whatsapp."
        location: "Supabase Dashboard → SQL Editor (o CLI supabase db push contra el proyecto)"

must_haves:
  truths:
    - "Existen las columnas businesses.has_web_custom y businesses.has_whatsapp (boolean not null default false)"
    - "D-02: existe la tabla plan_prices con 3 filas seedeadas en ARS (no USD): basic=15000, studio=30000, pro=50000 — los precios se editan y muestran en ARS"
    - "El dueño de un negocio NO puede auto-asignarse add-ons: un UPDATE de has_web_custom/has_whatsapp por el owner via su sesión es rechazado (solo service-role escribe esas flags)"
    - "computeKpis devuelve MRR = Σ(precio del plan × negocios activos por plan), negociosActivos, trialsPorVencer (≤7d) y pagosFallidos (cancelled+expired)"
    - "deriveAlerts emite pago_fallido (cancelled/expired) y trial_por_vencer (trial + ≤7d) con businessId para navegar a la ficha"
    - "'suspended' es un valor aceptado de plan_status en set-plan (VALID_STATUSES)"
  prohibitions:
    - "plan_prices NO usa using(true) en su policy SELECT (lección migraciones 029/031): SELECT solo para is_admin via JWT app_metadata"
    - "El precio editable NO usa el price_usd de lib/plans.ts: la fuente ARS es el price_ars de lib/subscription-plans.ts (15000/30000/50000)"
    - "La migración 032 NO agrega ningún CHECK constraint a plan_status (es text libre; 'suspended' entra sin migración de constraint)"
  artifacts:
    - path: "supabase/migrations/032_crm_admin.sql"
      provides: "plan_prices + has_web_custom/has_whatsapp + RLS admin-read + cierre del agujero de auto-asignación de add-ons del owner"
      contains: "create table if not exists public.plan_prices"
    - path: "lib/plan-prices.ts"
      provides: "getPlanPrices() lee plan_prices con fallback a subscription-plans.ts price_ars"
      exports: ["getPlanPrices"]
    - path: "lib/crm-metrics.ts"
      provides: "computeKpis() + deriveAlerts() + resolveTrialEndsAt() — funciones puras"
      exports: ["computeKpis", "deriveAlerts", "resolveTrialEndsAt"]
    - path: "lib/crm-directory.ts"
      provides: "filterBusinesses() — helper puro de filtro/búsqueda que incluye suspendidos en 'todos'"
      exports: ["filterBusinesses"]
  key_links:
    - from: "supabase/migrations/032_crm_admin.sql"
      to: "lib/subscription-plans.ts"
      via: "seed de plan_prices con los price_ars literales (15000/30000/50000)"
      pattern: "15000"
    - from: "lib/plan-prices.ts"
      to: "supabase/migrations/032_crm_admin.sql"
      via: "lee la tabla plan_prices via createAdminClient"
      pattern: "plan_prices"
---

<objective>
Sentar el substrato de datos y lógica pura de Phase 2: la migración 032 (precios editables en DB + add-ons como flags booleanas + cierre del agujero RLS de auto-asignación de add-ons del dueño), el getter de precios con fallback, las funciones puras de KPIs/alertas/trial (testeadas), el helper puro de filtro del directorio (testeado), y sumar `'suspended'` al universo de estados en `set-plan`.

Purpose: Todo lo demás de la fase (server actions, dashboard, directorio, ficha, planes) lee de esta tabla y de estos helpers. Cerrar acá la fuente-de-verdad de precios (ARS, no USD — Pitfall 1) y el escape de privilegio del owner sobre add-ons (A5) antes de que ninguna pantalla los consuma.
Output: migración 032 aplicada a mano, `lib/plan-prices.ts`, `lib/crm-metrics.ts` (+test), `lib/crm-directory.ts` (+test), `set-plan` con `'suspended'`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/02-admin-de-plataforma/02-CONTEXT.md
@.planning/workstreams/crm/phases/02-admin-de-plataforma/02-RESEARCH.md
@.planning/workstreams/crm/phases/02-admin-de-plataforma/02-PATTERNS.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
@supabase/migrations/031_crm_audit_log.sql
@lib/subscription-plans.ts
@lib/audit.ts
</context>

<artifacts_this_phase_produces>
Nuevos símbolos que ESTE plan introduce (referencia para los planes posteriores):
- Migración `032_crm_admin.sql`
- Tabla `plan_prices(plan_key pk, price_ars int, updated_at, updated_by)`
- Columnas `businesses.has_web_custom`, `businesses.has_whatsapp` (boolean not null default false)
- Policy RLS `"admin read plan_prices"` (SELECT solo is_admin); cierre de la policy UPDATE del owner sobre businesses para que NO pueda escribir las flags de add-on
- `getPlanPrices()` en `lib/plan-prices.ts`
- `computeKpis()`, `deriveAlerts()`, `resolveTrialEndsAt()` en `lib/crm-metrics.ts`
- `filterBusinesses()` en `lib/crm-directory.ts`
- `'suspended'` sumado a `VALID_STATUSES` en `app/api/admin/set-plan/route.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migración 032 — plan_prices + add-on flags + cierre del agujero RLS del owner</name>
  <files>supabase/migrations/032_crm_admin.sql</files>
  <read_first>
    - supabase/migrations/031_crm_audit_log.sql (analog exacto: cabecera en español, numeración "a mano y en orden", patrón RLS admin-read con `auth.jwt() -> 'app_metadata' ->> 'is_admin') = 'true'`, TODO post-deploy regenerar schema.sql)
    - supabase/migrations/004_fixed_expenses.sql (analog de policy FOR UPDATE con USING + WITH CHECK por tenant)
    - supabase/schema.sql:766 (la ÚNICA policy actual de businesses: `"owner access" ON businesses USING (owner_id = auth.uid())` — FOR ALL sin WITH CHECK ni restricción de columnas → el owner HOY puede UPDATE businesses set has_whatsapp=true)
    - lib/subscription-plans.ts (price_ars reales: basic 15000 / studio 30000 / pro 50000 — fuente del seed)
    - .claude/skills/supabase-multitenant-rls/SKILL.md (checklist RLS)
    - 02-RESEARCH.md §"Schema SQL propuesto — migración 032_crm_admin.sql" (schema verbatim a copiar) + §"Open question de RLS"
  </read_first>
  <action>
    Crear `supabase/migrations/032_crm_admin.sql` calcando la cabecera en español de 031 (QUÉ agrega, "se aplica A MANO y EN ORDEN — última aplicada: 031", razón D-01/D-08/D-09, advertencia de coordinar con el deploy del código que corta por 'suspended', TODO post-deploy regenerar schema.sql con supabase db dump).

    Contenido del SQL (copiar del schema propuesto en 02-RESEARCH §"Schema SQL propuesto — 032"):
    1. `alter table public.businesses add column if not exists has_web_custom boolean not null default false, add column if not exists has_whatsapp boolean not null default false;` Comentario: has_whatsapp es la MISMA flag que gatea la Bandeja del milestone Gestión rebrand (D-09); display "Recordatorios WhatsApp" nunca SMS.
    2. `create table if not exists public.plan_prices (plan_key text primary key check (plan_key in ('basic','studio','pro')), price_ars integer not null check (price_ars >= 0), updated_at timestamptz not null default now(), updated_by uuid references auth.users(id) on delete set null);`
    3. Seed idempotente: `insert into public.plan_prices (plan_key, price_ars) values ('basic',15000),('studio',30000),('pro',50000) on conflict (plan_key) do nothing;` — montos = los price_ars literales de lib/subscription-plans.ts (lo que cobra MP), NO el price_usd de lib/plans.ts.
    4. RLS de plan_prices: `alter table public.plan_prices enable row level security;` + `drop policy if exists "admin read plan_prices" on public.plan_prices;` + `create policy "admin read plan_prices" on public.plan_prices for select using ((select auth.jwt() -> 'app_metadata' ->> 'is_admin') = 'true');` SIN policy de insert/update/delete (escritura exclusiva del service-role). La policy SELECT NUNCA debe ser un predicado siempre-verdadero (lección de las migraciones 029/031) — el predicado SIEMPRE chequea is_admin.
    5. CIERRE DEL AGUJERO RLS (A5 — crítico): la policy actual `"owner access" ON businesses USING (owner_id = auth.uid())` es FOR ALL sin WITH CHECK, así que el owner puede hacer `update businesses set has_whatsapp=true` sobre su propia fila (bypass del cobro manual de add-ons, D-08). Reemplazar esa policy por uno de estos patrones (elegir el que NO rompa los updates legítimos del dashboard del dueño — onboarding, settings, palette, etc.): drop de la policy FOR ALL existente y recrearla como policies separadas SELECT/INSERT/DELETE con `owner_id = auth.uid()` + una policy FOR UPDATE con `USING (owner_id = auth.uid()) WITH CHECK (owner_id = auth.uid())` cuyo WITH CHECK use una función/trigger o un guard de columnas que impida cambiar `has_web_custom`/`has_whatsapp`/`plan`/`plan_status`. Patrón concreto recomendado (Postgres no restringe columnas en RLS directamente): crear un `BEFORE UPDATE` trigger `businesses_protect_admin_columns()` que, si la sesión NO es service-role (`current_setting('request.jwt.claims', true)` sin is_admin), revierta cambios a `has_web_custom`/`has_whatsapp`/`plan`/`plan_status` (set NEW.col = OLD.col) — así el service-role (toggleAddon/changePlan) sí escribe y el owner queda bloqueado. Comentar en español el porqué (anti-escalada de add-ons + plan, D-08). Idempotente (drop trigger/function if exists antes de create).

    NO agregar CHECK a plan_status (es text libre; 'suspended' entra sin constraint). Idempotente en todo: `if not exists` / `drop policy if exists` / `on conflict do nothing`.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && grep -v '^--' supabase/migrations/032_crm_admin.sql | grep -c "plan_prices" | awk '{ if ($1>=3) print "OK plan_prices referenciada"; else { print "FAIL"; exit 1 } }' && grep -q "has_web_custom" supabase/migrations/032_crm_admin.sql && grep -q "has_whatsapp" supabase/migrations/032_crm_admin.sql && grep -q "15000" supabase/migrations/032_crm_admin.sql && echo "OK seed ARS" && grep -q "is_admin" supabase/migrations/032_crm_admin.sql && echo "OK RLS admin-read en plan_prices"</automated>
  </verify>
  <done>El archivo 032_crm_admin.sql existe con: las 2 columnas add-on, la tabla plan_prices seedeada con 15000/30000/50000, la policy admin-read sin using(true), y el mecanismo que impide al owner escribir has_web_custom/has_whatsapp/plan/plan_status por su sesión. Header en español calcado de 031.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [BLOCKING] Aplicar migración 032 a mano en Supabase</name>
  <files>supabase/migrations/032_crm_admin.sql</files>
  <action>Acción humana (sin CLI desatendida): el operador aplica `supabase/migrations/032_crm_admin.sql` al Postgres del proyecto (convención del repo: migraciones a mano, en orden, coordinadas con el deploy). Sin esto, el resto de la fase verifica en falso (types/build pasan sin la tabla/columnas). Ver pasos en <how-to-verify>.</action>
  <what-built>El archivo SQL `supabase/migrations/032_crm_admin.sql` (Task 1). No tiene efecto hasta aplicarse al Postgres del proyecto.</what-built>
  <how-to-verify>
    1. Abrir el contenido de `supabase/migrations/032_crm_admin.sql`.
    2. Aplicarlo al proyecto Supabase: SQL Editor del dashboard (pegar y ejecutar) o `supabase db push` desde la CLI contra el proyecto correcto. La convención del repo (CLAUDE.md) es aplicar las migraciones A MANO, en orden, coordinadas con el deploy — NO asumir que CI la corre.
    3. Verificar en el SQL Editor: `select plan_key, price_ars from public.plan_prices order by plan_key;` debe devolver 3 filas (basic 15000, pro 50000, studio 30000). `select column_name from information_schema.columns where table_name='businesses' and column_name in ('has_web_custom','has_whatsapp');` debe devolver 2 filas.
    4. (Opcional, recomendado) Como un usuario dueño NO-admin, intentar `update businesses set has_whatsapp=true where id='<su negocio>';` y confirmar que la flag NO cambia (el trigger/policy lo revierte o lo bloquea).
    5. TODO operativo posterior (no bloqueante de esta fase): regenerar `supabase/schema.sql` con `supabase db dump`.
  </how-to-verify>
  <resume-signal>Escribí "aplicada" cuando la migración esté en la base y las 3 filas de plan_prices + las 2 columnas existan. Si algo falla, pegá el error.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lib/plan-prices.ts + lib/crm-metrics.ts + lib/crm-directory.ts (funciones puras + tests)</name>
  <files>lib/plan-prices.ts, lib/crm-metrics.ts, lib/crm-metrics.test.ts, lib/crm-directory.ts, lib/crm-directory.test.ts</files>
  <read_first>
    - lib/subscription-plans.ts (patrón getter con fallback: `getSubscriptionPlan` retorna `?? SUBSCRIPTION_PLANS.basic`; price_ars 15000/30000/50000)
    - lib/supabase/admin.ts (createAdminClient — getPlanPrices lee plan_prices con service-role server-only)
    - components/crm/confirm-dialog.tsx:81-124 (patrón de funciones PURAS testeables: computeConfirmState/buildSubmitGuard, sin React ni DB, `now` inyectable)
    - app/(dashboard)/layout.tsx:25-28 (criterio de daysLeft de trial — replicar el mismo cálculo de ≤7d para trialsPorVencer)
    - app/(crm)/admin/auditoria/auditoria-client.tsx:35-58 (AR_TZ + formato es-AR, zona AR fija UTC-3 — Pitfall 7)
    - 02-RESEARCH.md §"Pattern 5" (implementación completa de computeKpis/deriveAlerts) + §"Pattern 4" (resolveTrialEndsAt) + §"Validation Architecture"
  </read_first>
  <behavior>
    computeKpis(rows, prices, now):
    - Test: 2 activos plan basic (15000) + 1 activo studio (30000) → mrr = 60000, negociosActivos = 3.
    - Test: un negocio trial con trial_ends_at a 3 días → cuenta en trialsPorVencer; uno a 10 días → NO cuenta; uno ya vencido (fecha pasada) → NO cuenta.
    - Test: negocios cancelled + expired → pagosFallidos los suma; active/trial NO.
    - Test: un plan sin fila de precio (prices[plan] undefined) → suma 0, no NaN.
    deriveAlerts(rows, now):
    - Test: negocio cancelled → alerta tipo 'pago_fallido' con su businessId+name; expired → igual.
    - Test: negocio trial a 3 días → 'trial_por_vencer'; trial a 10 días → ninguna alerta.
    - Test: cada alerta lleva businessId (para navegar a /admin/negocios/{id}, D-12).
    resolveTrialEndsAt({ preset, exactDate }, now):
    - Test: preset '7' → now + 7 días; preset '14' → +14; '30' → +30.
    - Test: exactDate ISO → fin del día AR de esa fecha (no recortar un día por el offset UTC-3, Pitfall 7).
    - Test: ni preset ni exactDate → lanza/retorna error (refine 'preset_or_date_required').
    filterBusinesses(rows, { query, tab }):
    - Test: tab 'todos' INCLUYE los suspendidos (Pitfall 4 — nunca un default que los oculte).
    - Test: tab 'suspendidos' devuelve solo plan_status==='suspended'; 'activos' solo 'active'; 'trial' solo 'trial'; 'churn' solo cancelled/expired.
    - Test: query 'aco' matchea por nombre/email/slug (case-insensitive); query vacío devuelve todo el tab.
  </behavior>
  <action>
    Crear `lib/plan-prices.ts`: `getPlanPrices(): Promise<Record<'basic'|'studio'|'pro', number>>` que lee `plan_prices` con `createAdminClient()` (server-only) y arma el record; si falta una fila o hay error, fallback al `price_ars` literal de lib/subscription-plans.ts (15000/30000/50000) — calcar el patrón de fallback de `getSubscriptionPlan`. CRÍTICO (Pitfall 1): la fuente es price_ars (lo que cobra MP), NUNCA el price_usd de lib/plans.ts.

    Crear `lib/crm-metrics.ts` con `computeKpis`, `deriveAlerts`, `resolveTrialEndsAt` como funciones PURAS (sin React, sin DB, `now = new Date()` inyectable como último parámetro para determinismo en test). Tipos `BizRow`/`Prices`/`Alert` exportados. MRR = Σ(prices[r.plan] ?? 0) sobre los `plan_status==='active'` (D-03). trialsPorVencer = trial + trial_ends_at entre now y now+7d. pagosFallidos = cancelled+expired (D-10). deriveAlerts: pago_fallido (cancelled/expired) y trial_por_vencer (trial ≤7d) con `{businessId, name, tipo}`. resolveTrialEndsAt con date-fns (addDays para presets; fin del día AR para exactDate). Reusar el AR_TZ del proyecto.

    Crear `lib/crm-directory.ts` con `filterBusinesses(rows, { query, tab })` PURA: tabs `'todos'|'activos'|'trial'|'suspendidos'|'churn'` (todos incluye suspendidos), búsqueda sobre nombre/email/slug case-insensitive. Tipos exportados.

    Crear los tests `lib/crm-metrics.test.ts` y `lib/crm-directory.test.ts` con Vitest (environment node, como lib/audit.test.ts de Phase 1): cubrir los casos de <behavior>, con `now` fijo.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx vitest run lib/crm-metrics.test.ts lib/crm-directory.test.ts</automated>
  </verify>
  <done>Las tres libs existen; `npx vitest run lib/crm-metrics.test.ts lib/crm-directory.test.ts` pasa en verde cubriendo todos los casos de <behavior>. getPlanPrices usa price_ars con fallback. computeKpis/deriveAlerts/filterBusinesses puras y deterministas.</done>
</task>

<task type="auto">
  <name>Task 4: Sumar 'suspended' a VALID_STATUSES en set-plan</name>
  <files>app/api/admin/set-plan/route.ts</files>
  <read_first>
    - app/api/admin/set-plan/route.ts:7 (VALID_STATUSES actual = ['trial','active','expired','cancelled'])
  </read_first>
  <action>
    En `app/api/admin/set-plan/route.ts:7`, sumar `'suspended'` al array `VALID_STATUSES` (queda `['trial','active','expired','cancelled','suspended']`) — D-05. NO tocar nada más del archivo: este route lo sigue usando el actor externo con `x-admin-secret`; solo se amplía el universo de estados válidos para que la suspensión del CRM (Plan 02) y este endpoint coincidan.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && grep "VALID_STATUSES" app/api/admin/set-plan/route.ts | grep -q "suspended" && echo "OK suspended en VALID_STATUSES"</automated>
  </verify>
  <done>`VALID_STATUSES` en set-plan incluye 'suspended'. Ningún otro cambio en el archivo (el actor externo x-admin-secret sigue intacto).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner (sesión tenant) → businesses UPDATE | El dueño autenticado puede actualizar su propia fila de businesses vía RLS; las columnas de add-on/plan son entrada no confiable que NO debe poder tocar |
| operator → plan_prices / businesses (service-role) | El operador escribe precios y flags solo vía service-role tras requireAdmin (en Plan 02); plan_prices nunca lo lee/escribe un no-admin |
| public/anon → plan_prices | Lectura de precios solo para is_admin (no para anon ni para dueños) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | owner UPDATE businesses set has_whatsapp/has_web_custom/plan/plan_status | mitigate | Trigger BEFORE UPDATE / policy que revierte cambios a esas columnas si la sesión no es service-role (Task 1, cierre del agujero A5) — el cobro manual de add-ons no se puede saltear |
| T-02-02 | Information Disclosure / Tampering | plan_prices leído/escrito por no-admin | mitigate | RLS habilitada en la misma migración; SELECT solo is_admin via JWT app_metadata; SIN policy de write para users (solo service-role escribe) — nunca using(true) |
| T-02-03 | Tampering | seed de precios con la moneda equivocada (usd vs ars) | mitigate | Seed con price_ars de subscription-plans.ts (lo que cobra MP); getPlanPrices lee price_ars; verify negativo de price_usd |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 no instala ningún paquete nuevo (RESEARCH §Package Legitimacy Audit: no aplica); cero superficie de supply-chain |
</threat_model>

<verification>
- `npx vitest run lib/crm-metrics.test.ts lib/crm-directory.test.ts` verde.
- Migración 032 aplicada (checkpoint): plan_prices con 3 filas + 2 columnas add-on en businesses.
- `grep -v '^--' supabase/migrations/032_crm_admin.sql | grep -c "using(true)"` = 0.
- set-plan VALID_STATUSES incluye 'suspended'.
- `npx tsc --noEmit` sin errores nuevos.
</verification>

<success_criteria>
- La fuente de precios editables (plan_prices, ARS) existe y está seedeada; getPlanPrices la lee con fallback.
- Las funciones puras de KPIs/alertas/trial/filtro están testeadas en verde.
- El dueño no puede auto-asignarse add-ons (agujero A5 cerrado).
- 'suspended' es estado válido en set-plan.
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/02-admin-de-plataforma/02-01-SUMMARY.md` when done.
</output>