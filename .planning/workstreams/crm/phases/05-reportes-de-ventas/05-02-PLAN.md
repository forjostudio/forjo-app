---
phase: 05-reportes-de-ventas
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/(crm)/admin/reportes/page.tsx
  - app/(crm)/admin/reportes/reportes-client.tsx
autonomous: false
requirements: [RPT-01, RPT-02]

must_haves:
  truths:
    - "El operador (is_admin) abre /admin/reportes y ve la pantalla de Reportes de Ventas reproduciendo el mock 06-reportes.png"
    - "Se muestran las 5 tarjetas KPI: MRR (con VAR vs mes previo), Ingresos del mes (proxy recurrente, NO 'cobrado'), ARPA, Conversión Lead→Activo, Churn mensual (cuenta de bajas siempre; % o '— sin historia suficiente')"
    - "Se renderizan los 4 gráficos recharts: Evolución de MRR (barras), MRR por plan (donut con total al centro), Embudo de conversión (barras por etapa), Ranking (tabla top cuentas por MRR)"
    - "El toggle 3/6/12 meses re-filtra la serie de mrr_snapshots en memoria, mostrando solo los meses disponibles (sin inventar datos)"
    - "El service-role y las filas crudas de businesses NUNCA cruzan al cliente: reportes-client recibe SOLO agregados/no sensibles"
  artifacts:
    - path: "app/(crm)/admin/reportes/page.tsx"
      provides: "RSC que lee fuentes (session client + service-role tras guard), llama lib/crm-reports.ts, pasa solo agregados"
      contains: "crm-reports"
      min_lines: 40
    - path: "app/(crm)/admin/reportes/reportes-client.tsx"
      provides: "'use client' con 5 KPIs + 4 charts recharts + toggle 3/6/12m + empty-states honestos"
      contains: "'use client'"
      min_lines: 120
  key_links:
    - from: "app/(crm)/admin/reportes/page.tsx"
      to: "lib/crm-reports.ts"
      via: "llama funnel/churn/mrrByPlan/ranking sobre los datos leídos y pasa solo los agregados"
      pattern: "from '@/lib/crm-reports'"
    - from: "app/(crm)/admin/reportes/page.tsx"
      to: "mrr_snapshots (session client RLS-gated)"
      via: "supabase.from('mrr_snapshots').select(...) con createClient (NO service-role; hereda RLS admin-read)"
      pattern: "from\\('mrr_snapshots'\\)"
    - from: "app/(crm)/admin/reportes/reportes-client.tsx"
      to: "recharts"
      via: "BarChart/PieChart/ResponsiveContainer dentro de 'use client' con altura fija en el padre"
      pattern: "from 'recharts'"
---

<objective>
Construir la superficie visual de Reportes de Ventas: la ruta `/admin/reportes` (RSC que lee las fuentes con el split session/service-role y calcula con `lib/crm-reports.ts`) y `reportes-client.tsx` (`'use client'` con los 5 KPIs y los 4 gráficos recharts), reproduciendo con fidelidad el mock aprobado `crm-design/06-reportes.png`.

Purpose: Cierra RPT-01 (revenue/MRR visualizado) y RPT-02 (conversión por etapa + ranking con gráficos interactivos). El item "Reportes" ya existe en el sidebar del CRM; esta es la pantalla a la que apunta.
Output: `app/(crm)/admin/reportes/page.tsx` + `app/(crm)/admin/reportes/reportes-client.tsx`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/05-reportes-de-ventas/05-CONTEXT.md
@.planning/workstreams/crm/phases/05-reportes-de-ventas/05-RESEARCH.md
@.planning/workstreams/crm/phases/05-reportes-de-ventas/05-PATTERNS.md
@.planning/workstreams/crm/phases/05-reportes-de-ventas/05-01-SUMMARY.md

# Contrato de diseño LOCKED (A12) — leer la imagen, es el contrato exacto
@c:/Users/franc/Desktop/Forjo Studio/crm-design/06-reportes.png

# Analogs a espejar (LEER las líneas citadas)
@app/(crm)/admin/auditoria/page.tsx
@app/(crm)/admin/negocios/page.tsx
@app/(crm)/admin/pipeline/pipeline-client.tsx
@lib/crm-reports.ts
@lib/crm-pipeline.ts
@lib/plan-prices.ts
@lib/subscription-plans.ts
@app/(crm)/layout.tsx

# Skills del proyecto
@.claude/skills/convenciones-forjo/SKILL.md
@.claude/skills/supabase-multitenant-rls/SKILL.md
</context>

<artifacts_this_phase_produces>
Símbolos NUEVOS que este plan introduce (excluir de la verificación de drift):
- Ruta `app/(crm)/admin/reportes/page.tsx` (RSC) y `app/(crm)/admin/reportes/reportes-client.tsx` (client).
- Sub-componentes recharts dentro de reportes-client (MrrBarChart, PlanDonut, FunnelBars, RankingTable, tooltips custom dark) — PRIMEROS charts de la app, sin analog interno.
- (La tabla mrr_snapshots, lib/crm-reports.ts y el bloque del cron los produce el plan 05-01.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: RSC /admin/reportes/page.tsx — lectura mixta (session + service-role tras guard) + cálculo + props solo-agregados</name>
  <files>app/(crm)/admin/reportes/page.tsx</files>
  <read_first>
    - app/(crm)/admin/auditoria/page.tsx:1-39 — patrón session client (`createClient()`) para tablas con policy admin-read; SELECT explícito sin comodín; docblock que explica POR QUÉ session y no service-role (T-01-09); el layout ya redirige al no-admin (defensa en profundidad, no re-guardar).
    - app/(crm)/admin/negocios/page.tsx:1-55 — patrón service-role (`createAdminClient()`) SOLO para `businesses` (sin policy is_admin-lee-todo) + `getPlanPrices()`; docblock T-01-09/T-02-09: el admin client y las filas crudas NUNCA cruzan al cliente, solo columnas no sensibles.
    - 05-RESEARCH.md líneas 321-343 — lectura combinada en un `Promise.all([...])` mezclando ambos clientes (deals/audit_log/mrr_snapshots con session; businesses con admin); luego llamar lib pura y pasar SOLO agregados.
    - lib/crm-reports.ts (del plan 05-01) — firmas de funnel/churn/mrrByPlan/ranking/arpa que consume este RSC.
    - lib/plan-prices.ts:23-42 — `getPlanPrices()` (precios ARS editables con fallback).
    - lib/subscription-plans.ts:4-22 — display names por plan (basic→Básico, studio→Estudio, pro→Pro) para etiquetar donut/ranking desde la fuente de verdad, NO desde el label del mock.
    - 05-PATTERNS.md "reportes/page.tsx" (líneas 121-149) + "Shared Patterns → Session-vs-service-role read split" (líneas 193-202).
  </read_first>
  <action>
    Crear `app/(crm)/admin/reportes/page.tsx` como RSC con docblock en español explicando el split de lectura y el racional de aislamiento (gate `is_admin`, NO `business_id`; cross-tenant por diseño, D-10). Usar `createClient()` (session, RLS-gated) para `deals` (select stage,status,created_at, filtrado a ventana 90d por created_at), `audit_log` (select action,created_at, `.in('action', ['business.suspend','business.reactivate'])` dentro del mes actual) y `mrr_snapshots` (select month,plan,mrr,active_count, ordenado por month). Usar `createAdminClient()` SOLO para `businesses` (select id,name,plan,plan_status — columnas no sensibles) y `getPlanPrices()`. Combinar las lecturas en un `Promise.all([...])`. NO filtrar por `business_id` en ninguna query. Llamar a `lib/crm-reports.ts` (funnel, churn con el denom = active_count del mes previo de los snapshots, mrrByPlan, ranking, arpa) y a la fórmula MRR para los KPIs. Etiquetar planes con los display names de `SUBSCRIPTION_PLANS[plan].name` (fuente de verdad), no con el texto del mock. Pasar a `<ReportesClient .../>` SOLO los agregados serializables: los 5 valores KPI con sus VAR, la serie de snapshots agrupada por mes (para Evolución de MRR y el toggle 3/6/12m), el desglose MRR por plan (plan, label, mrr, count, pct), los pasos del embudo (label, count, pct), y el ranking (name, plan/label, mrr, var). NUNCA pasar el admin client, filas crudas de businesses, tokens ni secretos (T-01-09/T-02-09). Loguear errores de lectura best-effort con `console.error('[crm/reportes] ...:', ...)` y renderizar empty-states en vez de romper.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && grep -q "from '@/lib/crm-reports'" "app/(crm)/admin/reportes/page.tsx" && grep -q "from('mrr_snapshots')" "app/(crm)/admin/reportes/page.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/(crm)/admin/reportes/page.tsx` existe, importa de `@/lib/crm-reports` y lee `mrr_snapshots`, `deals`, `audit_log` con `createClient()` (session) — grep confirma `createClient` y NO `createAdminClient` para esas tres tablas.
    - `businesses` y `getPlanPrices()` se leen con `createAdminClient()` (service-role) y solo columnas no sensibles (id,name,plan,plan_status).
    - Ninguna query usa `.eq('business_id', ...)` (cross-tenant por diseño): grep `business_id` en el archivo → 0 en queries.
    - Las props pasadas a ReportesClient son solo agregados/no sensibles: no se pasa el admin client, ni filas crudas de businesses con columnas sensibles, ni tokens/secretos.
    - `npx tsc --noEmit` pasa.
  </acceptance_criteria>
  <done>RSC lee las fuentes con el split session/service-role correcto, calcula con la lib pura, y pasa solo agregados al cliente; tsc limpio; sin filtro business_id.</done>
</task>

<task type="auto">
  <name>Task 2: reportes-client.tsx — 5 KPIs + 4 charts recharts (Evolución MRR / donut / embudo / ranking) + toggle 3/6/12m, fiel a 06-reportes.png</name>
  <files>app/(crm)/admin/reportes/reportes-client.tsx</files>
  <read_first>
    - c:/Users/franc/Desktop/Forjo Studio/crm-design/06-reportes.png — CONTRATO de diseño (A12). Layout exacto: header "Reportes de ventas" + subtítulo "Rendimiento comercial del SaaS · {mes}" + toggle 3/6/12 meses (arriba der.); fila de 5 KPI cards (MRR amarillo / Ingresos del mes / ARPA / Conversión Lead→Activo / Churn mensual rojo, cada una con valor + delta de color); fila de 2 (Evolución de MRR = barras verticales Ene..Jun amarillas / MRR por plan = donut con total al centro + leyenda plan·neg·%); fila de 2 (Embudo de conversión = barras horizontales por etapa con count + % entre etapas / Ranking = tabla #·Negocio·Plan·MRR·VAR).
    - app/(crm)/admin/pipeline/pipeline-client.tsx:1-54 — `'use client'` al tope; prop types exportados de los agregados serializables; `arsFormatter = new Intl.NumberFormat('es-AR', { style:'currency', currency:'ARS', maximumFractionDigits:0 })` (líneas 50-54, COPIAR VERBATIM).
    - lib/crm-pipeline.ts:16-22 — `STAGES[].color` (tokens CSS por etapa) para colorear las barras del embudo; NO hardcodear hex.
    - 05-RESEARCH.md Patterns 1-3 (líneas 111-163: BarChart con ResponsiveContainer dentro de `<div className="h-72 w-full">`; donut Pie innerRadius>0 + Cell + Label central; embudo BarChart layout="vertical" + YAxis type="category") + Pitfalls 1/2/6 (líneas 213-228, 248-252) + Open Question 2 (tooltip dark custom tipado TooltipContentProps).
    - 05-PATTERNS.md "reportes-client.tsx" (líneas 152-177) + "No Analog Found" (líneas 222-226): los charts son los PRIMEROS de la app, seguir RESEARCH v3 verbatim, NO copiar tutoriales v2.
    - app/(crm)/layout.tsx:39-54 — el shell corre en `.dark .crm-shell`; los tooltips recharts default son claros → usar tooltips custom con tokens.
  </read_first>
  <action>
    Crear `app/(crm)/admin/reportes/reportes-client.tsx` con `'use client'` al tope y los prop types exportados de los agregados que envía el RSC. Importar recharts SOLO acá (nunca en el RSC — rompe el build/hydration). Reproducir con fidelidad el mock 06-reportes.png respetando la jerarquía visual (un elemento dominante por widget) y la paleta CRM LOCKED (§12): acento amarillo `var(--primary)` para series principales, `var(--crm-info)` azul para info, `var(--crm-success)` verde, `var(--crm-danger)` rojo SOLO para churn/peligro. Copiar el `arsFormatter` verbatim de pipeline-client.tsx para todo monto ARS; un formateador `%` para deltas/porcentajes. NUNCA hardcodear hex en los charts — usar tokens CSS; para el embudo reusar `STAGES[].color`.
    Header: título "Reportes de ventas", subtítulo "Rendimiento comercial del SaaS · {mes actual}", y el toggle 3/6/12 meses (estado local; re-filtra la serie de snapshots EN MEMORIA mostrando solo los meses disponibles — D-08, sin inventar meses faltantes). El botón "Exportar" se DIFIERE a v2 (D-08): NO renderizar una acción muerta (omitirlo).
    5 KPI cards: MRR (D-02: Σ precio×activos, con VAR vs mes previo, "—" si no hay snapshot previo), "Ingresos del mes" relabelada como proxy recurrente (subtítulo tipo "recurrente del mes", NO afirmar "cobrado" — D-03), ARPA, Conversión Lead→Activo, y Churn mensual — la card de churn muestra SIEMPRE la cuenta de bajas; el % se muestra solo si `pct !== null`, si es null renderiza "— / sin historia suficiente" (D-05/Pitfall 6, jamás NaN%).
    4 charts (recharts v3, seguir snippets de RESEARCH): (1) Evolución de MRR = `BarChart` vertical de la serie por mes, barras `var(--primary)`, dentro de `<div className="h-72 w-full"><ResponsiveContainer .../></div>` (Pitfall 1: padre con altura explícita). (2) MRR por plan (D-06: Σ del MRR del mes desglosado por plan, con monto/%/conteo) = `PieChart` donut (`Pie innerRadius>0` + un `Cell` por plan + `<Label position="center">` con el total MRR), leyenda con label de plan + count de negocios + %. (3) Embudo de conversión = `BarChart layout="vertical"` con `YAxis type="category" dataKey="label"`, count por barra y el % entre etapas (LabelList), colores de STAGES. (4) Ranking (D-07: top cuentas por MRR; MRR por negocio = price_ars del plan si activo; VAR vs snapshot del mes previo, "—" sin historia) = tabla (#, Negocio, Plan, MRR formateado ARS, VAR con "—" sin historia). Tooltips custom dark tipados `TooltipContentProps` usando `var(--card)`/`var(--border)`/`var(--foreground)` (Pitfall 2, v3). Empty-states honestos donde falten datos (serie con un solo mes, ranking vacío) — nunca un chart roto ni un dato inventado.
    Accesibilidad: headings jerárquicos correctos (h1 de la pantalla, h2 por widget), `accessibilityLayer` en los charts (default true en v3), contraste sobre el shell dark.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && npx tsc --noEmit && npm run build && grep -q "'use client'" "app/(crm)/admin/reportes/reportes-client.tsx" && grep -q "from 'recharts'" "app/(crm)/admin/reportes/reportes-client.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `app/(crm)/admin/reportes/reportes-client.tsx` empieza con `'use client'` e importa de `'recharts'`.
    - Renderiza las 5 KPI cards (MRR, Ingresos del mes como proxy recurrente, ARPA, Conversión Lead→Activo, Churn mensual) y los 4 widgets (Evolución MRR, MRR por plan donut, Embudo, Ranking).
    - La card de churn muestra "— / sin historia suficiente" cuando el pct es null (no "NaN%" ni "Infinity%").
    - La card "Ingresos del mes" NO afirma "cobrado" (es proxy recurrente, D-03).
    - El toggle 3/6/12m está presente y re-filtra la serie en memoria; el botón "Exportar" NO se renderiza (diferido, D-08).
    - Ningún hex hardcodeado en los charts (grep de `#[0-9a-fA-F]{6}` en el archivo no aparece en props de color de recharts; se usan `var(--...)` y STAGES[].color).
    - `npx tsc --noEmit` y `npm run build` pasan (recharts solo en este client component).
  </acceptance_criteria>
  <done>Pantalla /admin/reportes reproduce 06-reportes.png con los 5 KPIs y 4 charts recharts, toggle funcional, empty-states honestos; build verde; cero deps nuevas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Verificación visual de /admin/reportes contra el mock 06-reportes.png</name>
  <action>BLOQUEA hasta que el operador revise visualmente `/admin/reportes` contra el mock aprobado `crm-design/06-reportes.png` y confirme con "approved" (o describa diferencias para iterar). Es un paso humano de verificación visual, sin cambios de código.</action>
  <what-built>La pantalla `/admin/reportes` completa: RSC con lectura segura (session/service-role split, solo agregados al cliente) + reportes-client con los 5 KPIs y los 4 gráficos recharts (Evolución MRR, MRR por plan donut, Embudo, Ranking), toggle 3/6/12 meses, reproduciendo el mock aprobado 06-reportes.png sobre el shell dark del CRM.</what-built>
  <how-to-verify>
    1. Levantar la app (`npm run dev`) y entrar como operador `is_admin` a `/admin/reportes` (o vía el item "Reportes" del sidebar del CRM).
    2. Comparar contra `crm-design/06-reportes.png`: header + subtítulo, fila de 5 KPI cards (MRR amarillo con VAR, Ingresos del mes como recurrente, ARPA, Conversión Lead→Activo, Churn rojo), fila Evolución de MRR (barras) + MRR por plan (donut con total al centro + leyenda), fila Embudo (barras por etapa con count y %) + Ranking (tabla).
    3. Probar el toggle 3/6/12 meses: la serie de Evolución de MRR debe re-filtrarse a los meses disponibles, sin meses inventados.
    4. Verificar el empty-state honesto de churn: con un solo snapshot (mes actual recién sembrado, sin mes previo) la card de churn debe mostrar la cuenta de bajas + "— / sin historia suficiente", NO un % falso.
    5. Confirmar que "Ingresos del mes" NO dice "cobrado" (es proxy recurrente) y que no hay botón "Exportar" muerto.
    6. Verificar dark/contraste: los charts y tooltips respetan el shell dark del CRM (tooltips legibles, acento amarillo en series principales).
  </how-to-verify>
  <resume-signal>Escribir "approved" o describir las diferencias contra el mock para iterar.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operador (`is_admin`) → RSC `/admin/reportes` | El layout `(crm)` ya redirige al no-admin server-side (FND-01); la página no re-guarda |
| RSC → Postgres (session client) | `deals`/`audit_log`/`mrr_snapshots` se leen con la sesión del operador; el gate real es la RLS admin-read |
| RSC → Postgres (service-role) | `businesses` (sin policy is_admin) se lee con service-role SOLO tras el guard del layout |
| RSC (server) → reportes-client (browser) | Solo cruzan agregados/no sensibles; el admin client y las filas crudas nunca salen del server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | service-role → cliente | mitigate | Pasar SOLO agregados (KPIs, serie, MRR por plan, embudo, ranking name/plan/mrr/var) a reportes-client; NUNCA el admin client, filas crudas de businesses, tokens ni secretos (negocios/page.tsx docblock, T-01-09/T-02-09). Verificado en acceptance de Task 1. |
| T-05-06 | Elevation of Privilege | lectura de tablas admin-only | mitigate | `deals`/`audit_log`/`mrr_snapshots` se leen con `createClient()` (session, hereda RLS admin-read), NO service-role (lección T-04-10): leer con service-role bypassaría la policy. service-role solo para `businesses` (sin policy is_admin) tras el guard. |
| T-05-07 | Elevation of Privilege | acceso a `/admin/reportes` | mitigate | Guard server-side del layout `(crm)` (FND-01): redirige a no-admin antes de cualquier render; aun si llegara, la RLS no devolvería filas de las tablas admin-only. Sin re-guard redundante en la página (defensa en profundidad ya provista). |
| T-05-08 | Information Disclosure | scope cross-tenant | accept | Los reportes son agregados cross-tenant POR DISEÑO (datos del operador); el aislamiento es `is_admin`, NO `business_id` (D-10). No se filtra por tenant a propósito; residual aceptado y documentado. Solo cruzan agregados, no datos sensibles de un negocio puntual. |
| T-05-SC | Tampering (supply-chain) | npm installs | mitigate | Cero dependencias nuevas (D-09: recharts ^3.8.1 ya instalado). Este plan no toca `package.json`/`package-lock.json` → sin superficie de cadena de suministro. |
</threat_model>

<verification>
- `npx tsc --noEmit` sin errores.
- `npm run build` verde (recharts solo importado en el client component; no hay import de recharts en el RSC).
- `npm test` (suite completa) sigue verde.
- Checkpoint humano: la pantalla reproduce 06-reportes.png; toggle funcional; empty-state honesto de churn; "Ingresos del mes" relabelada; sin botón Exportar muerto.
</verification>

<success_criteria>
- RPT-01: el operador ve revenue/MRR por mes (Evolución de MRR) y los KPIs derivados de precios editables + snapshot persistido.
- RPT-02: el operador ve la conversión por etapa (Embudo) y el ranking, con gráficos interactivos recharts.
- El aislamiento se mantiene: solo agregados cruzan al cliente; tablas admin-only leídas con el cliente correcto; gate `is_admin`.
- Fidelidad al mock LOCKED 06-reportes.png; cero dependencias nuevas.
</success_criteria>

<output>
Crear `.planning/workstreams/crm/phases/05-reportes-de-ventas/05-02-SUMMARY.md` al terminar.
</output>