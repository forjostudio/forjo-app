---
phase: 04-pipeline-tags-timeline
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - components/crm/tag-manager-dialog.tsx
autonomous: true
requirements: [PIPE-04]
must_haves:
  truths:
    - "Desde el TagManagerDialog (abierto desde una tarjeta del pipeline, entityType='lead') el operador puede QUITAR una tag ya asignada a esa tarjeta, no solo asignar/crear"
    - "Las tags ya asignadas a la entidad son VISIBLES dentro del diálogo (sección 'Asignadas'), aunque el catálogo de 'Asignar existente' quede vacío porque todas están asignadas"
  artifacts:
    - path: "components/crm/tag-manager-dialog.tsx"
      provides: "Sección 'Asignadas' que renderiza assignedTags como TagChip removable + handler handleRemove que invoca removeTag({ tagId, entityType, entityId }) + onChanged()"
      contains: "removeTag"
      min_lines: 80
  key_links:
    - from: "components/crm/tag-manager-dialog.tsx"
      to: "app/(crm)/admin/_tag-actions.ts"
      via: "handleRemove invoca removeTag (genérico lead|business) + onChanged() para que el padre refresque"
      pattern: "removeTag\\("
    - from: "components/crm/tag-manager-dialog.tsx"
      to: "components/crm/tag-chip.tsx"
      via: "assignedTags renderizadas con <TagChip removable onRemove={() => handleRemove(t)} />"
      pattern: "removable"
---

<objective>
Cierre del ÚNICO gap abierto del re-test 2 del UAT de la Phase 4 (test 7, `failed`):

- **Gap test 7 (MAJOR) — no hay forma de QUITAR/modificar una tag ya asignada a una tarjeta del pipeline (lead/deal).** El `TagManagerDialog` compartido (creado en 04-08, `components/crm/tag-manager-dialog.tsx`) asigna tags existentes y crea+auto-asigna nuevas, pero se diseñó deliberadamente como "solo asignar + crear" (comentario líneas 14-16). Recibe la prop `assignedTags` (34,42) pero SOLO la usa para calcular `availableTags` (51-55) y nunca la renderiza con un afordance de quitar; tampoco importa `removeTag` (import línea 25 solo `createTag`/`assignTag`). En la ficha el quitar vive aparte (fila de chips `removable` + `ConfirmDialog`, `ficha-client.tsx:336-343,691-705`), pero ESE flujo es para `entity_type='business'`; el pipeline monta el diálogo con `entity_type='lead'` (`pipeline-client.tsx:396-408`) y su ÚNICA UI de tags es este diálogo → un lead/deal NO tiene ningún surface para quitar una tag y una tag mal puesta queda permanente.
- **Secundario (minor) — "No hay tags disponibles" confuso.** Cuando todas las tags del catálogo ya están asignadas, `availableTags` queda vacío y la sección "Asignar existente" muestra "No hay tags disponibles. Creá una abajo.", que parece que se perdieron las tags. Al hacer visibles las asignadas (sección nueva), el operador ve que sus tags siguen ahí → resuelve la confusión.

Estrategia (calcada del patrón existente, cero dependencias nuevas): **agregar una sección "Asignadas" al `TagManagerDialog`** que renderice la prop `assignedTags` (ya pasada por ambos surfaces) como `<TagChip removable onRemove={() => handleRemove(t)} />` (el `TagChip` ya soporta `removable`/`onRemove` con la X de lucide, `tag-chip.tsx:76-86`), cableada a un `handleRemove` que invoca `removeTag({ tagId, entityType, entityId })` + `onChanged()` con la MISMA forma `pending`/try-catch/`toast` que el `handleAssignExisting` ya presente (57-69). Esto le da al pipeline (`entityType='lead'`) el surface de quitar que hoy no existe, sin tocar nada más.

Purpose: el feature de tags del pipeline queda completo end-to-end (asignar → quitar → filtrar); una tag mal puesta en una tarjeta deja de ser permanente.
Output: una sección nueva en el diálogo compartido + import de `removeTag` + handler `handleRemove`. UN solo archivo modificado.

Constraints (del planning_context, respetar):
- Next.js 16 (App Router, `proxy.ts`). `removeTag` ya cumple el contrato de 6 pasos server-side (`requireAdmin` → `removeTagSchema.parse` → delete → `logAudit` 'tag.remove' risk bajo → `revalidatePath`); la UI es SOLO el afordance (clase de threat T-04-16, ya mitigada server-side).
- Aislamiento multi-tenant + RLS: `removeTag` borra una fila de `entity_tags` vía service-role tras `requireAdmin`; al cliente NO cruza dato nuevo (la prop `assignedTags` ya llegaba: solo id/label/color). No cambia la exposición de datos (T-04-13 intacto).
- Zero-install: NO agregar dependencias. Reusar `TagChip` (`removable`/`onRemove`), `removeTag`, `toast` de `sonner` y los tokens CSS del shell CRM dark YA presentes en el archivo.
- **NO tocar el flujo verificado de "Quitar tag" de la ficha** (test 13, `entity_type='business'`): la fila de chips `removable` + `ConfirmDialog` de `ficha-client.tsx` queda intacta. El diálogo compartido NO se monta en la ficha para gestionar el remove de esa fila; cuando la ficha abre el diálogo (`entityType='business'`), la nueva sección "Asignadas" actúa como afordance directo adicional de bajo riesgo, sin reemplazar ni duplicar la fila del Resumen.
- Mantener intacto TODO lo ya verificado: asignar existente, auto-crear+asignar (gap 13), filtro OR, DnD, markWon/markLost, createDeal, timeline, "Limpiar filtros" del directorio.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-UAT.md
@components/crm/tag-manager-dialog.tsx
@components/crm/tag-chip.tsx
@app/(crm)/admin/_tag-actions.ts
@app/(crm)/admin/pipeline/pipeline-client.tsx
@app/(crm)/admin/negocios/[id]/ficha-client.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sección "Asignadas" en TagManagerDialog — quitar tags ya asignadas (gap test 7)</name>
  <files>components/crm/tag-manager-dialog.tsx</files>
  <action>
    Cierra el gap test 7 agregando al diálogo compartido el afordance de QUITAR una tag ya asignada, espejando el patrón de la fila de tags de la ficha (`TagChip removable onRemove`) pero sin `ConfirmDialog` (acción de riesgo bajo, afordance directo). Es el ÚNICO archivo que se toca.

    **A) Importar `removeTag`.**
    En el import de `_tag-actions` (línea 25, hoy `import { createTag, assignTag } from '@/app/(crm)/admin/_tag-actions'`) agregar `removeTag`: queda `import { createTag, assignTag, removeTag } from '@/app/(crm)/admin/_tag-actions'`. `removeTag` ya existe y es genérico (`entityType: 'lead' | 'business'`, firma `(input: unknown) => Promise<void>`, `_tag-actions.ts:96-120`) — NO se toca el server.

    **B) Handler `handleRemove`.**
    Agregar una función `async function handleRemove(tag: Tag)` modelada VERBATIM sobre el `handleAssignExisting` ya presente (57-69): misma guarda `if (pending) return`, `setPending(true)`, try/catch, `finally { setPending(false) }`. Dentro del try:
    - `await removeTag({ tagId: tag.id, entityType, entityId })`
    - `onChanged?.()`
    En el catch: `console.error('[crm/tags] removeTag error:', e instanceof Error ? e.message : e)` y `toast.error('No se pudo quitar la tag. Probá de nuevo.')` (mismo estilo que `handleAssignExisting`).

    **C) Sección "Asignadas" en el markup.**
    Insertar un bloque NUEVO ANTES de la sección "Asignar existente" (antes del `{/* Asignar existente */}` de la línea ~110), con la misma estructura visual que esa sección (mismo wrapper `<div className="space-y-2">`, mismo `<p>` de label `font-[family-name:var(--font-geist-mono)] text-[11px] uppercase tracking-wide text-muted-foreground`, mismo contenedor de chips `<div className="flex flex-wrap gap-1.5">`):
    - Label: "Asignadas".
    - Si `assignedTags.length === 0`: `<p className="text-sm text-muted-foreground">Sin tags asignadas.</p>`.
    - Si hay asignadas: mapear `assignedTags.map((t) => <TagChip key={t.id} label={t.label} color={t.color} removable onRemove={() => handleRemove(t)} />)`. NO pasar `onToggle` (la variante estática `removable` de `TagChip` es la que muestra la X; pasar `onToggle` la convertiría en botón toggle sin X — usar SOLO `removable`/`onRemove`, como hace la ficha en `ficha-client.tsx:336-343`).
    - Separador visual coherente con la sección "Crear nueva" (que ya usa `border-t border-border pt-4`): la sección "Asignar existente" queda DEBAJO de "Asignadas"; mantené la jerarquía Asignadas → Asignar existente → Crear nueva. Si conviene para la separación visual, la sección "Asignar existente" puede llevar `border-t border-border pt-4` en su wrapper (espejo de "Crear nueva") — decisión cosmética menor, mantener consistencia con los tokens YA usados en el archivo, sin hex nuevos.

    **Decisión de scope (lead vs both) — DOCUMENTAR en el comentario de cabecera del archivo:**
    El remove del diálogo aplica para AMBOS `entityType` ('lead' y 'business'), NO se gatea solo a 'lead'. Razón: (1) `removeTag` es de riesgo `bajo` (solo borra una fila de `entity_tags`, auditado server-side, no toca datos del negocio); (2) la ficha conserva INTACTA su fila de chips `removable` + `ConfirmDialog` "Quitar tag" (test 13 verificado) — el diálogo es una superficie distinta, no la reemplaza; (3) gatear a `entityType==='lead'` agregaría una rama condicional sin beneficio y dejaría una asimetría confusa (en business verías las asignadas pero no podrías quitarlas desde el diálogo). Es un afordance directo de bajo riesgo, espejo del patrón `TagChip removable` ya presente en la ficha.

    **Actualizar el comentario de cabecera (líneas 14-16):** hoy dice "NO incluye removeTag: quitar una tag queda en cada surface con su propio refuerzo (la ficha usa un ConfirmDialog ya verificado; no se mueve acá)". Reescribirlo para reflejar que el diálogo AHORA incluye una sección "Asignadas" con quitar directo (`removeTag`, riesgo bajo, afordance directo sin ConfirmDialog) para AMBOS entityType, y que la fila de chips + ConfirmDialog de la ficha (`entity_type='business'`) sigue intacta como surface adicional/refuerzo y NO se movió acá.

    NO tocar: `handleAssignExisting`, `handleCreateTag` (auto-asignación gap 13), `availableTags`/`assignedIds`, la sección "Crear nueva", el overlay/`stopPropagation`, ni la a11y (`role="dialog"`/`aria-modal`/`aria-label`). NO modificar `_tag-actions.ts`, `pipeline-client.tsx`, `ficha-client.tsx` ni ningún otro archivo (la prop `assignedTags` ya se pasa correctamente desde ambos: pipeline `pipeline-client.tsx:401-405`, ficha `assignedTags={tags}`). Cuidar variables/imports sin uso y exhaustive-deps.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -c "removeTag" components/crm/tag-manager-dialog.tsx && grep -c "removable" components/crm/tag-manager-dialog.tsx</automated>
  </verify>
  <done>El TagManagerDialog importa removeTag, tiene un handler handleRemove (removeTag + onChanged, mismo pending/try-catch/toast que handleAssignExisting) y una sección "Asignadas" que renderiza assignedTags como TagChip removable con X. Desde una tarjeta del pipeline (entityType='lead') se puede quitar una tag asignada y, tras onChanged→router.refresh(), el chip desaparece de la tarjeta y deja de matchear el filtro OR. La fila de "Quitar tag" de la ficha (ConfirmDialog, entity_type='business') queda intacta. tsc exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server action | removeTag invocada desde el diálogo compartido (pipeline y, como afordance adicional, ficha); la garantía es requireAdmin+zod server-side (Plan 01), la UI es solo el afordance |
| RSC service-role→client | sin cambio: la prop assignedTags (id/label/color) ya cruzaba al cliente en 04-08; este plan no agrega exposición de datos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 (removeTag lead UI) | Tampering | sección "Asignadas" del diálogo (X por tag) | mitigate | removeTag revalida requireAdmin()+removeTagSchema.parse() server-side (verificado en 04-SECURITY); entityType es un valor del enum zod (no se confía en el cliente), entityId=leadId es uuid validado; logAudit 'tag.remove' risk bajo; la UI solo invoca |
| T-04-13 (assignedTags read) | Information Disclosure | prop assignedTags ya en el cliente | accept | sin cambio vs 04-08: solo id/label/color (no sensible) ya cruzaban; este plan no lee ni pasa datos nuevos |
| T-04-SC | Tampering (supply-chain) | npm installs | mitigate | cero dependencias nuevas; se edita 1 archivo existente reusando TagChip + removeTag + sonner ya presentes |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` → exit 0.
- `npm run build` → compila; `/admin/pipeline` y `/admin/negocios/[id]` presentes.
- `grep -n "removeTag" components/crm/tag-manager-dialog.tsx` → import + invocación en handleRemove.
- `grep -n "removable" components/crm/tag-manager-dialog.tsx` → assignedTags renderizadas como TagChip removable.
- `grep -n "Asignadas" components/crm/tag-manager-dialog.tsx` → existe la sección nueva.
- No debe quedar el comentario de cabecera afirmando que el diálogo "NO incluye removeTag" (se reescribe); tsc/eslint sin unused.
- Manual (downstream UAT): en el pipeline, +Tag en una tarjeta con tags → la sección "Asignadas" muestra los chips con X → click en la X quita la tag → tras refresh el chip desaparece de la tarjeta y el filtro OR deja de matchearla. En la ficha, la fila de "Quitar tag" (ConfirmDialog) sigue funcionando igual.
</verification>

<success_criteria>
- Gap test 7 cerrado: desde una tarjeta del pipeline (entityType='lead') se puede QUITAR una tag ya asignada; una tag mal puesta deja de ser permanente.
- Minor cerrado: las tags asignadas son visibles dentro del diálogo (sección "Asignadas"), aunque "Asignar existente" quede vacío.
- Cero dependencias nuevas; un solo archivo modificado; el flujo de "Quitar tag" de la ficha (ConfirmDialog, entity_type='business') y todo lo demás ya verificado (asignar, auto-crear+asignar, filtro OR, DnD, markWon/markLost, createDeal, timeline, "Limpiar filtros") quedan intactos.
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-09-SUMMARY.md` when done
</output>
