---
phase: 04-pipeline-tags-timeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/034_crm_pipeline_tags_timeline.sql
  - lib/crm-pipeline.ts
  - lib/crm-pipeline.test.ts
  - lib/crm-tags.ts
  - lib/crm-tags.test.ts
  - lib/crm-timeline.ts
  - app/(crm)/admin/_crm-actions.schemas.ts
  - app/(crm)/admin/_crm-actions.schemas.test.ts
  - app/(crm)/admin/_tag-actions.ts
  - components/crm/tag-chip.tsx
autonomous: false
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04, TL-01]

must_haves:
  truths:
    - "Las 6 tablas nuevas (leads, deals, tags, entity_tags, notes, tasks) existen con RLS habilitada y SOLO una policy de SELECT admin-only (is_admin del JWT), sin policy de insert/update/delete"
    - "D-11: La VIEW crm_timeline existe con WITH (security_invoker = true) y hace UNION ALL de audit_log + notes + tasks con shape comun (kind, actor_type, title, body, occurred_at, metadata, business_id) — vista agregada on-the-fly, SIN tabla materializada timeline_events"
    - "D-03: La columna deals.stage es text con CHECK in ('lead','calificado','trial','propuesta','pago') (etapas fijas en codigo, NO enum nativo) y D-04: deals.status es text con CHECK in ('open','won','lost') separado de la etapa"
    - "La constante STAGES en lib/crm-pipeline.ts es la unica fuente de verdad de las 5 etapas (key/label/color/order) y sus keys coinciden exactamente con el CHECK de la migracion"
    - "El filtro de tags por OR (lib/crm-tags.ts) y la constante STAGES tienen tests unitarios que pasan con npm test"
    - "Las server actions del catalogo de tags (createTag, assignTag, removeTag) viven en app/(crm)/admin/_tag-actions.ts como foundation compartido (D-08: un solo catalogo), con requireAdmin primera linea, importables por el tablero (Plan 02) y por la ficha/directorio (Plan 03) sin acoplarse entre si"
  artifacts:
    - path: "supabase/migrations/034_crm_pipeline_tags_timeline.sql"
      provides: "6 tablas admin-only + VIEW crm_timeline security_invoker"
      contains: "security_invoker = true"
    - path: "lib/crm-pipeline.ts"
      provides: "constante STAGES (fuente de verdad de etapas) + helpers de totales por columna"
      exports: ["STAGES"]
    - path: "lib/crm-tags.ts"
      provides: "filtro de tags por semantica OR (puro, testeable)"
    - path: "lib/crm-timeline.ts"
      provides: "tipo de fila del timeline + helpers de presentacion"
    - path: "app/(crm)/admin/_crm-actions.schemas.ts"
      provides: "schemas zod de las acciones del pipeline/tags/notas/tareas (modulo puro)"
    - path: "app/(crm)/admin/_tag-actions.ts"
      provides: "server actions del catalogo compartido de tags: createTag, assignTag, removeTag (foundation D-08)"
      exports: ["createTag", "assignTag", "removeTag"]
    - path: "components/crm/tag-chip.tsx"
      provides: "chip de tag (dot de color + texto), toggle para filtro"
  key_links:
    - from: "lib/crm-pipeline.ts"
      to: "supabase/migrations/034_crm_pipeline_tags_timeline.sql"
      via: "las keys de STAGES son identicas al CHECK de deals.stage"
      pattern: "lead.*calificado.*trial.*propuesta.*pago"
    - from: "supabase/migrations/034_crm_pipeline_tags_timeline.sql"
      to: "supabase/migrations/031_crm_audit_log.sql"
      via: "policy admin-read espejada verbatim (is_admin via app_metadata JWT)"
      pattern: "app_metadata.*is_admin"
    - from: "app/(crm)/admin/_tag-actions.ts"
      to: "lib/admin-guard.ts"
      via: "requireAdmin() primera linea de createTag/assignTag/removeTag (catalogo compartido, D-08)"
      pattern: "requireAdmin"

prohibitions:
  - "NUNCA usar using(true) en ninguna policy RLS (agujero de la migracion 029); la policy SIEMPRE chequea is_admin"
  - "NUNCA crear la VIEW crm_timeline sin WITH (security_invoker = true): un CREATE VIEW plano corre security-definer y bypassa el gate admin"
  - "NUNCA usar un enum nativo de Postgres para stage/status: es text + CHECK (D-03/D-04)"
  - "NUNCA crear una tabla materializada timeline_events: el timeline es una VIEW on-the-fly (D-11)"
  - "NUNCA poner 'use server' en _crm-actions.schemas.ts ni en los lib/*.ts: deben ser modulos puros importables desde Vitest"
---

<objective>
Construir los cimientos de datos de Phase 4: la migracion 034 con las 6 tablas nuevas del CRM (leads, deals, tags, entity_tags, notes, tasks) todas admin-only por RLS, la VIEW crm_timeline con security_invoker, las librerias puras (STAGES como fuente de verdad, filtro de tags OR, tipos de timeline), los schemas zod de las acciones, las server actions del catalogo compartido de tags (_tag-actions.ts) y el componente tag-chip reusable. Sin esto, ningun surface de Wave 2 tiene tablas live, la constante de etapas, ni las acciones de tags compartidas.

Purpose: Toda la fase depende de que las tablas existan y de que STAGES sea la unica fuente de verdad de las etapas (la consume el tablero, los totales $ por columna y Phase 5 RPT-02). Como las tags son UN SOLO catalogo compartido (D-08) usado tanto por el tablero (Plan 02) como por la ficha/directorio (Plan 03), sus server actions viven aca como foundation: asi ambos surfaces de Wave 2 importan de _tag-actions.ts y quedan paralelos (ambos depends_on solo de 04-01, cero overlap). El gate de seguridad mas alto del milestone vive aca: las tablas admin-only y la VIEW security_invoker.
Output: Migracion 034 aplicada, libs puras con tests, schemas zod, server actions de tags compartidas, tag-chip.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-CONTEXT.md
@.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-RESEARCH.md
@.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-PATTERNS.md
@.claude/skills/supabase-multitenant-rls/SKILL.md

# Analogs LOCKED a espejar (leer antes de escribir codigo)
@supabase/migrations/031_crm_audit_log.sql
@app/(crm)/admin/_actions.schemas.ts
@lib/crm-directory.ts
@lib/crm-directory.test.ts
@components/crm/status-badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migracion 034 — 6 tablas admin-only + VIEW crm_timeline security_invoker</name>
  <files>supabase/migrations/034_crm_pipeline_tags_timeline.sql</files>
  <read_first>
    - supabase/migrations/031_crm_audit_log.sql (patron RLS admin-only verbatim: columnas, indices, comentarios densos en espanol, separadores de seccion, policy de SELECT que lee is_admin del JWT app_metadata, SIN policy de write)
    - supabase/migrations/027_business_secrets_and_public_views.sql (el caso INVERSO: las public views usan security-definer para bypassar RLS para anon — crm_timeline necesita LO OPUESTO)
    - 04-RESEARCH.md Pattern 1/2/3 y Pitfall 1 (security_invoker)
  </read_first>
  <action>
    Crear supabase/migrations/034_crm_pipeline_tags_timeline.sql. Header en espanol explicando: que agrega, que se aplica A MANO y EN ORDEN (ultima aplicada: 033), que toda tabla nueva es admin-only espejando 031, y el TODO operativo post-deploy (regenerar supabase/schema.sql con supabase db dump). Numero correcto: 034 (D-15).

    Crear las 6 tablas con convenciones de 031: id uuid primary key default gen_random_uuid(), created_at timestamptz not null default now(), metadata jsonb not null default '{}'::jsonb donde aplique, comentarios densos en espanol explicando el por que.

    - leads: id, name text not null, email text (normalizado lowercase a nivel app), whatsapp text, business_id uuid references public.businesses(id) on delete set null (NULL hasta convertir), source text, created_at. Indice leads_email_idx (lower(email)) y leads_business_id_idx.
    - deals: (D-01: leads 1 → deals N — el FK lead_id habilita varios deals por lead) id, lead_id uuid not null references public.leads(id) on delete cascade, title text, value_ars integer not null default 0, probability integer, expected_close_date date, stage text not null default 'lead' check (stage in ('lead','calificado','trial','propuesta','pago')), status text not null default 'open' check (status in ('open','won','lost')), lost_reason text, business_id uuid references public.businesses(id) on delete set null, created_at, updated_at timestamptz not null default now(). Indices deals_lead_id_idx, deals_stage_idx, deals_status_idx, deals_business_id_idx. Comentario: stage = progreso; status separado (D-04); 'won' al llegar a pago/convertir, 'lost' manual con lost_reason.
    - tags: id, label text not null, color text not null, created_at. Catalogo global compartido (D-07/D-08). Indice unico opcional sobre lower(label).
    - entity_tags: id, tag_id uuid not null references public.tags(id) on delete cascade, entity_type text not null check (entity_type in ('lead','business')), entity_id uuid not null, created_at. Join unico con discriminador de tipo (D-08). Indice unico entity_tags_unique_idx on (tag_id, entity_type, entity_id) para evitar duplicados; indice entity_tags_entity_idx on (entity_type, entity_id).
    - notes: id, business_id uuid references public.businesses(id) on delete cascade, lead_id uuid references public.leads(id) on delete cascade, body text not null, created_at, updated_at. Fuente del timeline (D-12). Indice notes_business_id_idx.
    - tasks: id, business_id uuid references public.businesses(id) on delete cascade, lead_id uuid references public.leads(id) on delete cascade, title text not null, due_date date, done boolean not null default false, completed_at timestamptz, created_at. Tareas livianas (D-12): titulo + due opcional + flag done; SIN asignacion/recordatorios/recurrencia. Indice tasks_business_id_idx.

    Para CADA una de las 6 tablas (D-14: todas las tablas nuevas del CRM son admin-only por RLS): alter table ... enable row level security; drop policy if exists; create policy "admin read <tabla>" ... for select using ((select auth.jwt() -> 'app_metadata' ->> 'is_admin') = 'true'); y un comentario explicando que NO se crea policy de insert/update/delete (solo service-role escribe) y que NUNCA se usa using(true). Espejar exactamente 031 lineas 47-62.

    Crear la VIEW: create or replace view public.crm_timeline with (security_invoker = true) as (UNION ALL de tres ramas con shape identico: kind text, actor_type text, title text, body text, occurred_at timestamptz, metadata jsonb, business_id uuid). Rama 1 'cambio' desde audit_log (actor_type = case when actor_id is null then 'sistema' else 'operador' end, title = action, body = reason, occurred_at = created_at, metadata = metadata, business_id = business_id). Rama 2 'nota' desde notes (title = 'Nota', body = body, occurred_at = created_at). Rama 3 'tarea' desde tasks (title = case when done then 'Tarea completada' else 'Tarea creada' end, body = title, occurred_at = coalesce(completed_at, created_at)). Comentario en espanol explicando por que security_invoker = true (la view corre con privilegios del CALLER → la RLS admin-read de las tablas base se HEREDA; sin el flag corre security-definer por default y bypassa el gate; es lo OPUESTO de las public views de 027). NO ordenar dentro de la view; el consumidor ordena por occurred_at desc.
  </action>
  <verify>
    <automated>grep -v '^--' supabase/migrations/034_crm_pipeline_tags_timeline.sql | grep -c 'security_invoker = true'</automated>
  </verify>
  <acceptance_criteria>
    - Las 6 tablas tienen enable row level security + exactamente una policy de SELECT que lee is_admin del JWT app_metadata; ninguna tabla tiene policy de insert/update/delete.
    - No aparece using(true) en ninguna policy (grep -v '^--' ... | grep -c 'using(true)' devuelve 0).
    - deals.stage tiene CHECK in ('lead','calificado','trial','propuesta','pago') y deals.status tiene CHECK in ('open','won','lost').
    - La VIEW crm_timeline usa WITH (security_invoker = true) y hace UNION ALL de audit_log + notes + tasks con shape comun.
    - No hay enum nativo de Postgres (create type ... as enum) ni tabla timeline_events.
  </acceptance_criteria>
  <done>Migracion 034 escrita con las 6 tablas admin-only y la VIEW crm_timeline security_invoker, lista para aplicar a mano.</done>
</task>

<task type="auto">
  <name>Task 2: Librerias puras — STAGES, filtro de tags OR, tipos de timeline (+ tests)</name>
  <files>lib/crm-pipeline.ts, lib/crm-pipeline.test.ts, lib/crm-tags.ts, lib/crm-tags.test.ts, lib/crm-timeline.ts</files>
  <read_first>
    - lib/crm-directory.ts (patron de helper PURO de filtro: tipos de dominio, funciones puras, sin DB ni React)
    - lib/crm-directory.test.ts (patron de test Vitest: describe/it/expect, factory de filas, casos de borde)
    - 04-RESEARCH.md Pattern 2 (constante STAGES con colores) y 04-PATTERNS.md seccion lib/crm-pipeline.ts
    - app/globals.css lineas 173-178 (tokens --crm-danger, --crm-info azul, --crm-success verde ya confirmados presentes)
  </read_first>
  <action>
    Crear lib/crm-pipeline.ts: exportar la constante STAGES as const = arreglo de 5 objetos {key,label,color,order} con keys EXACTAS lead/calificado/trial/propuesta/pago (deben coincidir con el CHECK de deals.stage en 034). Colores del mock 02-pipeline.png mapeados a tokens CSS confirmados: lead → var(--muted-foreground) (gris), calificado → var(--crm-info) (azul), trial → var(--primary) (amarillo), propuesta → var(--primary) (ambar), pago → var(--crm-success) (verde). Exportar tipo StageKey derivado de STAGES (typeof STAGES[number]['key']). Exportar helper puro stageTotals(deals): dado un arreglo de deals con {stage,value_ars,status}, devuelve por stage la suma de value_ars de los deals open. Exportar helper puro pipelineSummary(deals): devuelve {openTotal, wonTotal} (sumas de value_ars por status open vs won) para el header "$X abiertos · $Y ganados". Sin 'use server', sin import de Supabase ni React.

    Crear lib/crm-tags.ts: exportar el filtro puro de semantica OR (D-09). Firma filterByTags(rows, selectedTagIds): dado filas con un campo tagIds: string[] y un arreglo de tag ids seleccionados, devuelve las filas donde selectedTagIds.length === 0 || selectedTagIds.some((t) => row.tagIds.includes(t)). Espejar la pureza de filterBusinesses. Sin React ni DB.

    Crear lib/crm-timeline.ts: exportar el tipo TimelineRow (kind: 'cambio'|'nota'|'tarea'|'mensaje'|'llamada', actor_type: string, title: string, body: string|null, occurred_at: string, metadata, business_id: string|null) que matchea el shape de la VIEW crm_timeline. Exportar el set de filtros del timeline (Todo/Mensajes/Llamadas/Notas/Tareas/Cambios) y un helper puro que mapea cada kind al filtro al que pertenece, mas un helper que indica que filtros muestran empty state en esta fase (Mensajes/Llamadas → llegan con la Bandeja, D-13). Reusar la idea de ACTION_LABEL de auditoria-client (no duplicar copy de etiquetas de accion: exportar el mapa para que el render lo consuma). Sin 'use server'.

    Crear lib/crm-pipeline.test.ts y lib/crm-tags.test.ts con Vitest (espejar lib/crm-directory.test.ts): para STAGES verificar que tiene 5 entradas, keys correctas y order 0..4; para stageTotals/pipelineSummary verificar sumas con un fixture; para filterByTags verificar OR (vacio devuelve todo, una tag matchea any, varias tags matchean union, ninguna coincide devuelve vacio).
  </action>
  <verify>
    <automated>npm test -- lib/crm-pipeline.test.ts lib/crm-tags.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - STAGES tiene exactamente 5 entradas con keys lead/calificado/trial/propuesta/pago y order 0,1,2,3,4.
    - Las keys de STAGES son identicas al CHECK de deals.stage en la migracion 034 (verificacion cruzada manual + tests).
    - filterByTags implementa OR: vacio devuelve todas las filas, seleccion devuelve la union de las filas que tengan cualquiera de las tags.
    - npm test pasa para ambos archivos de test nuevos sin tocar los tests existentes.
    - Ningun archivo lib/*.ts importa Supabase, React ni tiene 'use server'.
  </acceptance_criteria>
  <done>STAGES es la fuente de verdad de etapas con tests; filtro de tags OR y tipos de timeline puros y testeados.</done>
</task>

<task type="auto">
  <name>Task 3: Schemas zod de las acciones + tag-chip reusable</name>
  <files>app/(crm)/admin/_crm-actions.schemas.ts, app/(crm)/admin/_crm-actions.schemas.test.ts, components/crm/tag-chip.tsx</files>
  <read_first>
    - app/(crm)/admin/_actions.schemas.ts (modulo PURO sin 'use server', const businessId = z.uuid(), z.enum([...] as const), tipos inferidos con z.infer)
    - app/(crm)/admin/_actions.schemas.test.ts (patron de test de schemas)
    - components/crm/status-badge.tsx (pill tematica con tokens CSS; tag-chip se modela igual)
    - lib/crm-pipeline.ts (Task 2) — las keys de stage del schema se calcan de STAGES
  </read_first>
  <action>
    Crear app/(crm)/admin/_crm-actions.schemas.ts (modulo PURO, SIN 'use server', importable desde Vitest — mismo motivo que _actions.schemas.ts). Reusar const id = z.uuid(). Definir los schemas que consumiran las acciones de tags de este mismo plan (Task 4, _tag-actions.ts) y las acciones de Wave 2 (Plans 02 y 03), calcando los stage keys de STAGES (no redeclarar el enum a mano: importar STAGES de lib/crm-pipeline y construir z.enum a partir de sus keys):
    - createDealSchema: { leadName: z.string().trim().min(1), leadEmail: z.string().trim().toLowerCase().email().optional(), valueArs: z.int().min(0).default(0), stage: enum de STAGES keys (default 'lead') }
    - moveStageSchema: { dealId: id, stage: enum de STAGES keys }
    - markLostSchema: { dealId: id, reason: z.string().trim().min(1) }
    - convertLeadSchema: { leadId: id, businessId: id }  (conversion manual desde el tablero)
    - linkLeadOnSignupSchema: { businessId: id }  (conversion automatica; el email del owner se re-deriva server-side, NUNCA llega en el input — anti-tampering)
    - createTagSchema: { label: z.string().trim().min(1), color: z.string().trim().min(1) }
    - assignTagSchema: { tagId: id, entityType: z.enum(['lead','business']), entityId: id }
    - removeTagSchema: { tagId: id, entityType: z.enum(['lead','business']), entityId: id }
    - createNoteSchema: { businessId: id.nullable().optional(), leadId: id.nullable().optional(), body: z.string().trim().min(1) }
    - editNoteSchema: { noteId: id, body: z.string().trim().min(1) }
    - deleteNoteSchema: { noteId: id }
    - createTaskSchema: { businessId: id.nullable().optional(), leadId: id.nullable().optional(), title: z.string().trim().min(1), dueDate: z.iso.date().optional() }
    - completeTaskSchema: { taskId: id, done: z.boolean() }
    Exportar los tipos inferidos con z.infer para cada schema.

    Crear app/(crm)/admin/_crm-actions.schemas.test.ts (espejar _actions.schemas.test.ts): casos validos e invalidos para los schemas clave — moveStageSchema rechaza un stage fuera de STAGES, convertLeadSchema exige uuids, createNoteSchema rechaza body vacio, assignTagSchema rechaza entityType fuera de ('lead','business').

    Crear components/crm/tag-chip.tsx (presentacional, modelado sobre status-badge.tsx): props { label: string, color: string, selected?: boolean, onToggle?: () => void, removable?: boolean, onRemove?: () => void }. Render: pill con un dot de color (size-1.5 rounded-full, backgroundColor = color) + texto. Si onToggle, es un boton toggle (aria-pressed, ≥44px touch target en su area clickable, focus ring). Si removable, muestra una X (lucide) con onRemove. Usar tokens CSS del shell CRM, nunca hex hardcodeado. Sin estado propio (controlado por el padre).
  </action>
  <verify>
    <automated>npm test -- app/(crm)/admin/_crm-actions.schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - _crm-actions.schemas.ts no tiene 'use server' y es importable desde Vitest.
    - El enum de stage de moveStageSchema/createDealSchema se construye a partir de las keys de STAGES (no un literal duplicado).
    - linkLeadOnSignupSchema NO incluye email ni leadId en el input (el email se re-deriva server-side — anti-tampering documentado en comentario).
    - Los tests de schemas pasan; npm test global sigue verde.
    - tag-chip.tsx es controlado (sin useState propio), con dot de color via token CSS, toggle accesible (aria-pressed + focus visible) y boton de remover opcional.
  </acceptance_criteria>
  <done>Schemas zod puros y testeados para todas las acciones de Wave 2; tag-chip reusable listo para pipeline, ficha y directorio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Server actions del catalogo compartido de tags (_tag-actions.ts)</name>
  <files>app/(crm)/admin/_tag-actions.ts</files>
  <read_first>
    - app/(crm)/admin/_actions.ts (contrato de 6 pasos LOCKED: requireAdmin() primera linea → schema.parse → createAdminClient → mutar/throw 'update_failed' → logAudit con action code exacto → revalidatePath; helper fichaPath; comentario de header explicando el patron)
    - lib/audit.ts (firma logAudit: {actorId, action, targetType, targetId?, businessId?, risk, reason?, metadata?}; best-effort, nunca throw)
    - lib/admin-guard.ts (requireAdmin lanza 'unauthorized'/'forbidden', devuelve User; su id es el actorId)
    - lib/supabase/admin.ts (createAdminClient service-role)
    - app/(crm)/admin/_crm-actions.schemas.ts (Task 3: createTagSchema, assignTagSchema, removeTagSchema)
    - 04-CONTEXT.md D-07/D-08 (catalogo global administrable, UN SOLO catalogo compartido entre leads y negocios via join por tipo de entidad)
  </read_first>
  <behavior>
    - createTag({label, color}): inserta en tags, logAudit 'tag.create' risk bajo, revalidatePath. Si el insert falla → throw 'update_failed'.
    - assignTag({tagId, entityType, entityId}): inserta en entity_tags (idempotente via el indice unico tag_id+entity_type+entity_id; on conflict do nothing o ignorar el 23505), logAudit 'tag.assign' risk bajo, revalidatePath del surface relevante.
    - removeTag({tagId, entityType, entityId}): borra la fila de entity_tags, logAudit 'tag.remove' risk bajo, revalidatePath.
  </behavior>
  <action>
    Crear app/(crm)/admin/_tag-actions.ts con 'use server' al tope. Importar requireAdmin, logAudit, createAdminClient, revalidatePath, y los schemas createTagSchema/assignTagSchema/removeTagSchema de _crm-actions.schemas.ts.

    Implementar createTag, assignTag, removeTag siguiendo el contrato de 6 pasos de _actions.ts VERBATIM: requireAdmin() PRIMERA linea (devuelve el User → actorId), schema.parse SEGUNDA, createAdminClient, mutar con throw 'update_failed' si error, logAudit con action code exacto, revalidatePath. Action codes EXACTOS para registrar en el ACTION_LABEL del visor (Plan 03 los agrega al mapa): 'tag.create', 'tag.assign', 'tag.remove'. assignTag debe ser idempotente respecto al indice unico de entity_tags (tag_id, entity_type, entity_id): tratar el conflicto 23505 como exito (la tag ya estaba asignada) en vez de propagar el error.

    Comentario denso en espanol en el header explicando: estas acciones viven en su PROPIO archivo (no en _pipeline-actions.ts ni _content-actions.ts) PORQUE las tags son UN SOLO catalogo compartido (D-08) que consumen tanto el tablero (Plan 02, filtro/asignacion en pipeline) como la ficha y el directorio (Plan 03, fila "+ Tag" + filtro del directorio); centralizarlas aca permite que ambos surfaces de Wave 2 importen de un foundation comun y queden paralelos sin acoplarse entre si.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - _tag-actions.ts tiene 'use server' al tope y exporta createTag, assignTag, removeTag.
    - Cada action tiene requireAdmin() como PRIMERA linea y schema.parse como segunda; escribe SOLO via createAdminClient (service-role).
    - assignTag es idempotente respecto al indice unico de entity_tags (el conflicto 23505 se trata como exito).
    - Las tres acciones llaman logAudit con los action codes exactos: tag.create, tag.assign, tag.remove.
    - npx tsc --noEmit -p tsconfig.json sale con exit 0 (sin errores de tipos).
  </acceptance_criteria>
  <done>Server actions del catalogo compartido de tags (createTag/assignTag/removeTag) en su propio foundation _tag-actions.ts, importables por Plan 02 y Plan 03 sin acoplarse entre si.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5 [BLOCKING]: Aplicar migracion 034 a mano en Supabase</name>
  <action>El operador aplica supabase/migrations/034_crm_pipeline_tags_timeline.sql a mano, en orden, coordinado con el deploy (regla CLAUDE.md — el repo NO corre supabase db push automatizado). Ningun task que dependa de las tablas live (Wave 2: Plans 02 y 03) puede verificarse hasta que esta migracion este aplicada. Ver los pasos de verificacion abajo.</action>
  <what-built>La migracion 034_crm_pipeline_tags_timeline.sql con las 6 tablas admin-only (leads, deals, tags, entity_tags, notes, tasks) y la VIEW crm_timeline (security_invoker). El repo aplica migraciones A MANO y EN ORDEN, coordinadas con el deploy (regla CLAUDE.md) — NO se corre supabase db push automatizado.</what-built>
  <how-to-verify>
    1. Abrir el SQL Editor de Supabase del proyecto Forjo (o ejecutar la migracion en orden tras la 033 — ultima aplicada).
    2. Ejecutar el contenido de supabase/migrations/034_crm_pipeline_tags_timeline.sql.
    3. Verificar que las 6 tablas existen: select tablename from pg_tables where schemaname='public' and tablename in ('leads','deals','tags','entity_tags','notes','tasks'); → debe devolver 6 filas.
    4. Verificar la VIEW: select definition from pg_views where viewname='crm_timeline'; → debe contener el UNION de audit_log/notes/tasks; y select reloptions from pg_class where relname='crm_timeline'; → debe incluir security_invoker=true.
    5. Verificar RLS: select tablename, policyname, cmd from pg_policies where tablename in ('leads','deals','tags','entity_tags','notes','tasks'); → SOLO policies de SELECT, ninguna de insert/update/delete.
    6. (Operativo, no bloqueante) regenerar supabase/schema.sql con supabase db dump cuando se pueda.
  </how-to-verify>
  <resume-signal>Escribir "aplicada" cuando las 6 tablas + la VIEW crm_timeline existan en la base, o describir el error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente autenticado (dueno) → tablas CRM | El dueno de un negocio tiene sesion anon; NUNCA debe leer/escribir leads/deals/tags/notes/tasks |
| anon (publico) → tablas CRM / VIEW | Visitantes anonimos nunca deben acceder a datos del CRM ni al timeline |
| operador (is_admin) → datos cross-tenant | El operador lee cross-entidad pero cada query debe filtrar por business_id/lead_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | crm_timeline VIEW | mitigate | WITH (security_invoker = true) → la view hereda la RLS admin-read de audit_log/notes/tasks; leida con session client en Plan 03. Un CREATE VIEW plano correria security-definer y bypassaria el gate (high) |
| T-04-02 | Elevation of Privilege | 6 tablas nuevas | mitigate | RLS habilitada en la misma migracion; policy de SELECT que SIEMPRE chequea is_admin del JWT app_metadata; SIN policy de write (solo service-role escribe) → no falsificable. NUNCA using(true) (agujero 029) |
| T-04-03 | Tampering | deals.stage / deals.status | mitigate | CHECK constraints en DB (stage in 5 keys, status in open/won/lost); STAGES como unica fuente de verdad calcada en los schemas zod (validacion en Plan 02/03) |
| T-04-04 | Information Disclosure | entity_tags (discriminador) | mitigate | entity_type CHECK in ('lead','business') + indice unico (tag_id, entity_type, entity_id); RLS admin-only impide que un dueno lea asignaciones de tags de otra entidad |
| T-04-15 | Tampering | _tag-actions.ts (createTag/assignTag/removeTag invocadas directo) | mitigate | requireAdmin() PRIMERA linea + zod.parse SEGUNDA en cada action; escritura SOLO via service-role; assignTag idempotente (23505 → exito) evita estados inconsistentes. Foundation compartido (D-08) consumido por Plan 02 y Plan 03 |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Esta fase NO instala paquetes nuevos (RESEARCH: no DnD lib bundleada; native HTML5 DnD en Plan 02). Si se propusiera @dnd-kit, requiere checkpoint humano antes de instalar |
</threat_model>

<verification>
- npm test pasa para todos los archivos de test nuevos (crm-pipeline, crm-tags, _crm-actions.schemas) y los existentes siguen verdes.
- npx tsc --noEmit -p tsconfig.json sale con exit 0 (incluye _tag-actions.ts).
- grep confirma security_invoker = true en la VIEW y cero using(true) en las policies.
- Las keys de STAGES coinciden 1:1 con el CHECK de deals.stage.
- _tag-actions.ts exporta createTag/assignTag/removeTag con requireAdmin primera linea (foundation compartido D-08).
- Checkpoint humano: las 6 tablas + la VIEW existen en la base tras aplicar 034 a mano.
</verification>

<success_criteria>
- Migracion 034 escrita y aplicada (checkpoint) con 6 tablas admin-only + VIEW crm_timeline security_invoker.
- lib/crm-pipeline.ts (STAGES + totales), lib/crm-tags.ts (filtro OR), lib/crm-timeline.ts (tipos) puros y testeados.
- _crm-actions.schemas.ts con todos los schemas zod de Wave 2 + tests.
- _tag-actions.ts (createTag/assignTag/removeTag) como foundation compartido de tags (D-08), importable por Plan 02 y Plan 03.
- components/crm/tag-chip.tsx reusable.
</success_criteria>

<artifacts_this_phase>
## Artifacts this phase produces (todos los simbolos de Phase 4)

**Migracion / DB (Plan 01):**
- `supabase/migrations/034_crm_pipeline_tags_timeline.sql`
- Tablas: `leads`, `deals`, `tags`, `entity_tags`, `notes`, `tasks` (todas admin-only por RLS)
- VIEW: `crm_timeline` (security_invoker = true)
- Columnas clave: `deals.stage` (text+CHECK 5 etapas), `deals.status` (text+CHECK open/won/lost), `entity_tags.entity_type` (CHECK lead/business)

**Librerias puras (Plan 01):**
- `lib/crm-pipeline.ts` → `STAGES` (fuente de verdad de etapas), `StageKey`, `stageTotals()`, `pipelineSummary()`
- `lib/crm-tags.ts` → `filterByTags()` (OR)
- `lib/crm-timeline.ts` → `TimelineRow`, set de filtros del timeline, mapa central de etiquetas de accion
- `app/(crm)/admin/_crm-actions.schemas.ts` → createDealSchema, moveStageSchema, markLostSchema, convertLeadSchema, linkLeadOnSignupSchema, createTagSchema, assignTagSchema, removeTagSchema, createNoteSchema, editNoteSchema, deleteNoteSchema, createTaskSchema, completeTaskSchema (+ tipos inferidos)

**Componentes (Plan 01 + 03):**
- `components/crm/tag-chip.tsx` (Plan 01)
- `components/crm/timeline-entry.tsx` (Plan 03)

**Server actions (Plan 01 + 02 + 03):**
- `app/(crm)/admin/_tag-actions.ts` (Plan 01, foundation compartido D-08) → createTag, assignTag, removeTag
- `app/(crm)/admin/_pipeline-actions.ts` (Plan 02) → createDeal, moveStage, markLost, convertLead, linkLeadOnSignup
- `app/(crm)/admin/_content-actions.ts` (Plan 03) → createNote, editNote, deleteNote, createTask, completeTask

**Rutas / superficies (Plan 02 + 03):**
- `app/(crm)/admin/pipeline/page.tsx` + `pipeline-client.tsx` (tablero, Plan 02)
- `app/(crm)/admin/negocios/[id]/page.tsx` + `ficha-client.tsx` (tab Timeline + tags, Plan 03)
- `app/(crm)/admin/negocios/page.tsx` + `negocios-client.tsx` (filtro de tags, Plan 03)

**Action codes nuevos (registrar en ACTION_LABEL, Plan 03):**
- deal.create, deal.stage_change, deal.mark_lost, lead.convert, tag.create, tag.assign, tag.remove, note.create, note.edit, note.delete, task.create, task.complete
</artifacts_this_phase>

<source_coverage_audit>
## Multi-Source Coverage Audit

**GOAL (ROADMAP Phase 4):** operar pipeline completo + conversion lead→business con historial + tags filtrables + timeline unificado → COVERED por Plans 01-03.

**REQ (phase_req_ids):**
- PIPE-01 (lead entra al pipeline, leads/deals) → COVERED (Plan 01 tablas + Plan 02 board/createDeal + Plan 02 conversion)
- PIPE-02 (mover entre etapas) → COVERED (Plan 02 DnD + moveStage)
- PIPE-03 (conversion lead→business con historial) → COVERED (Plan 02 linkLeadOnSignup auto + convertLead manual)
- PIPE-04 (tags color+texto, filtro pipeline + directorio) → COVERED (Plan 01 tablas+tag-chip+filterByTags+_tag-actions.ts createTag/assignTag/removeTag, Plan 02 asignacion+filtro pipeline, Plan 03 filtro directorio + fila de tags)
- TL-01 (tab timeline cronologico unificado) → COVERED (Plan 01 VIEW crm_timeline, Plan 03 tab Timeline + notas/tareas)

**RESEARCH (load-bearing findings):**
- Conversion en ONBOARDING handleFinish (no register) → COVERED (Plan 02 Task 3)
- crm_timeline security_invoker + session-client read → COVERED (Plan 01 Task 1 + Plan 03 Task 2)
- DnD nativo HTML5 (no dep nueva) → COVERED (Plan 02 Task 2 + prohibition)
- stage = text+CHECK, status separado, STAGES unica fuente → COVERED (Plan 01 Task 1+2)
- reuse 031 RLS / 6-step action contract / ConfirmDialog / formatWhen+ACTION_LABEL → COVERED (todos los plans, read_first + acceptance)

**CONTEXT (D-01..D-15):** los 15 citados (D-01 Plan 01, D-02/D-05 Plan 02, D-03/D-04/D-14/D-15 Plan 01, D-07/D-08 Plan 01 _tag-actions.ts foundation compartido, D-06 Plan 01/02, D-09 Plan 01/02/03, D-10 Plan 03, D-11 Plan 01, D-12/D-13 Plan 03). Sin Deferred Ideas planificados.

**Gaps:** ninguno. Todos los items COVERED.
</source_coverage_audit>

<output>
Create `.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-01-SUMMARY.md` when done
</output>
