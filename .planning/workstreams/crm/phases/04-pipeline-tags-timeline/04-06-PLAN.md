---
phase: 04-pipeline-tags-timeline
plan: 06
type: execute
wave: 2
depends_on: ["04-04"]
gap_closure: true
files_modified:
  - app/(crm)/admin/pipeline/page.tsx
  - app/(crm)/admin/pipeline/pipeline-client.tsx
autonomous: true
requirements: [PIPE-01, PIPE-02]
must_haves:
  truths:
    - "Al crear un deal, la tarjeta aparece en el tablero al instante (sin navegar y volver)"
    - "Existe un botón 'Marcar ganado' en la tarjeta que saca la tarjeta del board e invoca markWon"
    - "El header muestra '$ ganados' real, calculado server-side desde los deals won"
    - "La tarjeta muestra el nombre con que se creó el deal (deal.title prioritario sobre lead.name)"
  artifacts:
    - path: "app/(crm)/admin/pipeline/page.tsx"
      provides: "wonTotal calculado server-side (suma status='won') pasado como prop + contactName prioriza d.title"
      contains: "wonTotal"
    - path: "app/(crm)/admin/pipeline/pipeline-client.tsx"
      provides: "createDeal con sync optimista/router.refresh + botón 'Marcar ganado' (markWon) + header usa wonTotal prop"
      contains: "markWon"
  key_links:
    - from: "app/(crm)/admin/pipeline/pipeline-client.tsx"
      to: "app/(crm)/admin/_pipeline-actions.ts"
      via: "import markWon e invocación en el handler del botón 'Marcar ganado'"
      pattern: "markWon\\("
    - from: "app/(crm)/admin/pipeline/page.tsx"
      to: "app/(crm)/admin/pipeline/pipeline-client.tsx"
      via: "prop wonTotal calculado server-side"
      pattern: "wonTotal="
---

<objective>
Cierre de gaps del UAT (test 4a, test 5 UI, test 4b UI) en la superficie del tablero de pipeline. Depende de 04-04 (markWon + reuse acotado ya existen en _pipeline-actions.ts).

Resuelve:
- **Gap test 4a (createDeal no refresca):** el tablero renderiza desde estado local `useState(initialDeals)`; `handleCreateDeal` (pipeline-client.tsx:134-153) solo cierra el diálogo y hace toast, nunca sincroniza el nuevo deal → recién aparece al remontar.
- **Gap test 5 UI (ganados):** falta el botón "Marcar ganado" en la tarjeta (espejo de "Marcar perdido"), y el header lee `summary.wonTotal` calculado en el cliente sobre deals que NO incluyen los won (page.tsx:49 filtra `status='open'`). DECIDIDO POR EL USUARIO: el header debe recibir `wonTotal` calculado server-side.
- **Gap test 4b UI (nombre):** la tarjeta muestra `lead?.name ?? d.title` (page.tsx:89) → nombre viejo del lead. DECIDIDO POR EL USUARIO: priorizar `d.title` para mostrar el nombre con que se creó el deal.

Purpose: el operador ve sus deals aparecer al instante, puede marcarlos ganados, ve el total ganado real y la tarjeta muestra el nombre correcto.
Output: `pipeline/page.tsx` con wonTotal server-side + prioridad de d.title; `pipeline-client.tsx` con sync optimista del createDeal, botón "Marcar ganado" y header que usa el prop wonTotal.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-UAT.md
@app/(crm)/admin/pipeline/page.tsx
@app/(crm)/admin/pipeline/pipeline-client.tsx
@app/(crm)/admin/_pipeline-actions.ts
@lib/crm-pipeline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx — wonTotal server-side + contactName prioriza d.title</name>
  <files>app/(crm)/admin/pipeline/page.tsx</files>
  <action>
    En `pipeline/page.tsx`:

    1) **wonTotal server-side (gap test 5).** El RSC hoy lee solo deals `status='open'` (línea 49). Agregar una query AGREGADA aparte que sume `value_ars` de los deals `status='won'`, SIN traer esos deals al cliente (mantener las columnas/tarjetas mostrando solo open — DECIDIDO POR EL USUARIO). Implementarla en el `Promise.all` existente: `admin.from('deals').select('value_ars').eq('status', 'won')`, luego sumar en JS `wonTotal = (wonRows ?? []).reduce((acc, r) => acc + (r.value_ars ?? 0), 0)`. Manejar el error con `console.error('[crm/pipeline] won read error:', ...)` (calca el patrón de los otros reads). Pasar `wonTotal={wonTotal}` como prop nueva a `<PipelineClient />`.

    2) **contactName prioriza d.title (gap test 4b UI).** En el `.map` que arma `deals: PipelineDeal[]` (línea 89), cambiar `contactName: lead?.name ?? d.title ?? 'Sin nombre'` por `contactName: d.title ?? lead?.name ?? 'Sin nombre'`. Comentario: "DECIDIDO POR EL USUARIO (gap 4b): el deal.title (nombre con que se creó el deal) tiene prioridad sobre el lead.name viejo."

    NO cambiar el filtro `.eq('status','open')` de las columnas (las tarjetas siguen mostrando solo open; los won solo alimentan el total del header).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -c "wonTotal" app/\(crm\)/admin/pipeline/page.tsx</automated>
  </verify>
  <done>page.tsx calcula wonTotal sumando deals status='won' (query aparte, no cruza esos deals al cliente), lo pasa como prop; contactName usa d.title ?? lead?.name. tsc exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: pipeline-client.tsx — sync optimista de createDeal + header usa wonTotal prop</name>
  <files>app/(crm)/admin/pipeline/pipeline-client.tsx</files>
  <action>
    En `pipeline-client.tsx`:

    1) **Aceptar el prop wonTotal.** Agregar `wonTotal: number` a la firma de props de `PipelineClient` y al tipo de la desestructuración. El header (línea 174) hoy usa `summary.wonTotal` (cliente, siempre 0) — reemplazarlo por el prop `wonTotal` que viene server-side. Mantener `summary.openTotal` (el cliente sí tiene los deals open) para `$ abiertos`. NOTA: `pipelineSummary` se sigue usando para openTotal; si tras el cambio `wonTotal` de `summary` queda sin uso, dejar el cálculo pero leer solo `.openTotal`, o ajustar el useMemo para devolver solo openTotal — lo que mantenga tsc limpio sin warnings de variable sin uso.

    2) **Sync optimista del createDeal (gap test 4a).** `handleCreateDeal` (línea 134) hoy solo cierra y toast. DECIDIDO POR EL USUARIO (Opción B mínima del UAT, robusta con server-side state): tras el `await createDeal(...)` exitoso, llamar `router.refresh()` para revalidar el RSC, Y re-sincronizar el estado local cuando lleguen los nuevos props. Implementación:
       - Importar `useRouter` de `next/navigation` y `useEffect` de `react`; instanciar `const router = useRouter()`.
       - Agregar `useEffect(() => { setDeals(initialDeals) }, [initialDeals])` para re-sincronizar el estado local cuando el RSC re-renderiza con datos frescos (tras router.refresh el prop `initialDeals` cambia → el effect actualiza `deals`).
       - En `handleCreateDeal`, después de `setForm(...)`, agregar `router.refresh()`.
       Cuidar el lint `react-hooks/exhaustive-deps`: el effect depende de `initialDeals`. Esto es seguro porque el DnD optimista revierte/aplica sobre el mismo estado; tras un refresh el server es la fuente de verdad. Comentario explicando el porqué.

    3) **Botón "Marcar ganado" (gap test 5 UI).** Espejo de "Marcar perdido":
       - Importar `markWon` desde `@/app/(crm)/admin/_pipeline-actions` (sumarlo al import existente de createDeal/moveStage/markLost).
       - En `DealCard`, agregar un botón "Marcar ganado" junto a "Marcar perdido" (línea 429-438), con una prop `onMarkWon: () => void`. Estilo consistente con el botón existente (mismo tamaño/typo; podés usar el color `var(--crm-success)` en hover para diferenciarlo del perdido, respetando los tokens del shell CRM).
       - En el render de `DealCard` dentro del map (línea 242), pasar `onMarkWon={() => handleMarkWon(deal)}`.
       - Agregar `handleMarkWon(deal)`: optimista (sacar la tarjeta del board con `setDeals(prev => prev.filter(d => d.id !== deal.id))`), luego `await markWon({ dealId: deal.id })`; si falla, revertir (re-insertar el deal) + `toast.error('No se pudo marcar como ganado. Probá de nuevo.')` y `router.refresh()`. Calcar el patrón optimista-revertible del DnD (Pitfall 6: no mentir).
       NO usar ConfirmDialog para "ganado" (ganar no es destructivo; el UAT lo pide como acción directa espejo de markLost pero sin la fricción del motivo). NO acoplar stage↔status: markWon solo cambia status, la tarjeta sale del board porque ya no es open.

    Verificar que el build pasa y que no quedan variables sin uso.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npm run build 2>&1 | grep -E "(admin/pipeline|Compiled|error)" | head -20</automated>
  </verify>
  <done>El header usa el prop wonTotal (no el cálculo cliente); createDeal hace router.refresh + el useEffect re-sincroniza deals con initialDeals; existe botón "Marcar ganado" que invoca markWon optimista-revertible. tsc exit 0; build compila con /admin/pipeline presente.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server action | el botón "Marcar ganado" invoca markWon con un dealId; la garantía es requireAdmin+zod server-side (04-04), la UI es refuerzo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 (markWon UI) | Tampering | botón Marcar ganado | mitigate | la UI no autoriza; markWon revalida requireAdmin()+markWonSchema.parse() server-side (Plan 04-04). El optimismo se revierte si la action falla (no mentir). |
| T-04-10 (wonTotal) | Information Disclosure | query won server-side | mitigate | wonTotal es un agregado (suma) calculado en el RSC tras el layout guard; SOLO el número cruza al cliente, no las filas won ni columnas sensibles |
| T-04-SC | Tampering (supply-chain) | npm installs | mitigate | cero dependencias nuevas; solo se editan 2 archivos existentes |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` → exit 0.
- `npm run build` → compila; `/admin/pipeline` presente.
- `grep -n "markWon" app/\(crm\)/admin/pipeline/pipeline-client.tsx` → import + invocación presentes.
- `grep -n "router.refresh" app/\(crm\)/admin/pipeline/pipeline-client.tsx` → presente en handleCreateDeal y handleMarkWon.
- El header lee el prop wonTotal, no summary.wonTotal.
</verification>

<success_criteria>
- Crear un deal lo muestra al instante (router.refresh + re-sync del estado local).
- Botón "Marcar ganado" saca la tarjeta del board e invoca markWon (optimista-revertible).
- El header muestra $ ganados real (server-side).
- La tarjeta muestra el nombre del deal (d.title prioritario).
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-06-SUMMARY.md` when done
</output>
