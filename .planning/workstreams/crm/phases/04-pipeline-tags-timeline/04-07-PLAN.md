---
phase: 04-pipeline-tags-timeline
plan: 07
type: execute
wave: 2
depends_on: ["04-05"]
gap_closure: true
files_modified:
  - app/(crm)/admin/negocios/[id]/page.tsx
  - app/(crm)/admin/negocios/[id]/ficha-client.tsx
  - components/crm/confirm-dialog.tsx
autonomous: true
requirements: [PIPE-04, TL-01]
must_haves:
  truths:
    - "El botón '+Tag' permite crear la primera tag del catálogo (ya no nace deshabilitado por catálogo vacío)"
    - "Una tarea creada en la ficha puede marcarse como completada desde la UI (checkbox/botón)"
    - "En el ConfirmDialog, el badge de riesgo va al lado del título, sin superponerse a la X de cerrar"
  artifacts:
    - path: "app/(crm)/admin/negocios/[id]/ficha-client.tsx"
      provides: "import createTag + input 'Crear tag' en el diálogo + handler/checkbox para completeTask + lista de tareas con id"
      contains: "createTag"
    - path: "app/(crm)/admin/negocios/[id]/page.tsx"
      provides: "lectura de tasks con id (espejo de fichaNotes) + prop tasks al client"
      contains: "FichaTask"
    - path: "components/crm/confirm-dialog.tsx"
      provides: "header con gap-2 pr-8 (badge pegado al título)"
      contains: "pr-8"
  key_links:
    - from: "app/(crm)/admin/negocios/[id]/ficha-client.tsx"
      to: "app/(crm)/admin/_tag-actions.ts"
      via: "import createTag e invocación en el diálogo 'Crear tag'"
      pattern: "createTag\\("
    - from: "app/(crm)/admin/negocios/[id]/ficha-client.tsx"
      to: "app/(crm)/admin/_content-actions.ts"
      via: "completeTask invocada desde el checkbox de cada tarea"
      pattern: "completeTask\\("
    - from: "app/(crm)/admin/negocios/[id]/page.tsx"
      to: "app/(crm)/admin/negocios/[id]/ficha-client.tsx"
      via: "prop tasks (FichaTask[] con id) al client"
      pattern: "tasks="
---

<objective>
Cierre de gaps del UAT (test 13 deadlock de tags, test 11 completar tareas, test 12 cosmético) en la ficha del negocio y el ConfirmDialog compartido. Depende de 04-05 (la VIEW de-duplicada ya aplicada, para que la tarea aparezca una sola vez en el timeline).

Resuelve:
- **Gap test 13 (DEADLOCK de tags):** el botón +Tag nace `disabled={availableTags.length === 0 || pending}` (ficha-client.tsx:342); en instalación nueva el catálogo está vacío → siempre deshabilitado. La ficha SOLO asigna tags existentes; `createTag` existe en `_tag-actions.ts:34` pero ninguna UI la invoca → el catálogo nunca se llena. DECIDIDO: importar `createTag`, quitar `availableTags.length===0` del disabled, agregar un input "Crear tag" en el diálogo.
- **Gap test 11 (completar tareas):** `completeTask` está importada (ficha-client.tsx:32) pero NUNCA se invoca; falta affordance. Las tareas no se leen con id en page.tsx (solo las notas, 114-125). DECIDIDO: leer tasks con id (espejo de fichaNotes), tipo FichaTask, prop al client; handler `handleCompleteTask` + checkbox por tarea en TimelineTab (espejo del bloque showNotesManager).
- **Gap test 12 (cosmético):** confirm-dialog.tsx:231 usa `flex items-center justify-between gap-2` → empuja el RiskBadge a la misma coordenada que la X absoluta. DECIDIDO: cambiar por `flex items-center gap-2 pr-8`.

Purpose: el operador puede crear/asignar tags (desbloquea filtros de pipeline test 7 y directorio test 14), completar tareas, y el badge de riesgo se ve prolijo.
Output: ficha-client.tsx + page.tsx (ficha) + confirm-dialog.tsx.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-UAT.md
@app/(crm)/admin/negocios/[id]/page.tsx
@app/(crm)/admin/negocios/[id]/ficha-client.tsx
@app/(crm)/admin/_tag-actions.ts
@app/(crm)/admin/_content-actions.ts
@components/crm/confirm-dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx (ficha) — leer tasks con id (espejo de fichaNotes) + prop al client</name>
  <files>app/(crm)/admin/negocios/[id]/page.tsx</files>
  <action>
    En `negocios/[id]/page.tsx`, espejar EXACTAMENTE el bloque de "Notas con id" (líneas 110-125) para tareas:

    1) Importar el tipo `FichaTask` desde `./ficha-client` (sumarlo al import existente de FichaData/FichaTag/FichaNote en la línea 6).
    2) Agregar una lectura service-role de `tasks` con id (tras el bloque de notas, ~línea 125): `admin.from('tasks').select('id, title, done, completed_at, created_at').eq('business_id', id).order('created_at', { ascending: false }).limit(100)`, envuelta en try/catch con `console.error('[crm/ficha] tasks read error:', ...)`. `tasks` es admin-only por RLS; el gate is_admin ya lo dio el layout (mismo razonamiento que notes). Solo id/title/done/completed_at/created_at cruzan al cliente (no sensible).
    3) Pasar `tasks={fichaTasks}` como prop nueva a `<FichaClient />` (línea 147-155).

    NO leer las tareas desde la VIEW crm_timeline (no expone id). El timeline sigue mostrando las tareas vía su rama propia; esta lectura aparte es para el affordance de completar (igual que las notas borrables).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -c "FichaTask" app/\(crm\)/admin/negocios/\[id\]/page.tsx</automated>
  </verify>
  <done>page.tsx lee tasks con id (service-role tras el guard), tipo FichaTask, y las pasa como prop tasks al FichaClient. tsc exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: ficha-client.tsx — desbloqueo de tags (createTag) + completar tareas (completeTask)</name>
  <files>app/(crm)/admin/negocios/[id]/ficha-client.tsx</files>
  <action>
    En `ficha-client.tsx`:

    **A) Tipo FichaTask + prop (gap test 11).**
    - Exportar `export type FichaTask = { id: string; title: string; done: boolean; completed_at: string | null; created_at: string }` (junto a FichaNote, ~línea 52).
    - Agregar `tasks: FichaTask[]` a la firma de props de `FichaClient` (junto a `notes`).

    **B) Desbloqueo del deadlock de tags (gap test 13).**
    - Importar `createTag` desde `@/app/(crm)/admin/_tag-actions` (sumarlo al import de assignTag/removeTag, línea 33).
    - Quitar `availableTags.length === 0` del disabled del botón +Tag (línea 342): dejar `disabled={pending}`.
    - En el diálogo "Asignar tag" (líneas 557-588), agregar un sub-bloque "Crear tag": un Input para el label + un Input para el color (puede ser un `<input type="color">` nativo o un Input de texto con un color por defecto, ej. un token/hex; respetar createTagSchema que pide label+color no vacíos) + un Button "Crear tag" que invoque `handleCreateTag`. Renombrar el título del diálogo a algo que cubra ambos casos (ej. "Tags") o mantener dos secciones ("Asignar existente" / "Crear nueva"). Mantener el diseño dark del shell CRM (tokens CSS, no hex hardcodeado salvo el value del color picker).
    - Agregar `handleCreateTag`: setPending, `await createTag({ label, color })`, limpiar los inputs, `router.refresh()` (el catálogo se recarga via el RSC y la nueva tag aparece en availableTags). Idealmente, tras crear, dejar el diálogo abierto para que el operador la asigne con el chip recién aparecido (router.refresh trae el nuevo catálogo). Manejar el caso de label duplicado (el índice único `tags_label_unique_idx` puede tirar 23505 → createTag lanza 'update_failed'): envolver en try/catch con `toast.error('No se pudo crear la tag (¿ya existe?).')`.

    **C) Completar tareas (gap test 11).**
    - Agregar `handleCompleteTask(task: FichaTask, done: boolean)`: setPending, `await completeTask({ taskId: task.id, done })`, `router.refresh()`, finally setPending(false). `completeTask` ya está importada (línea 32) — ahora se usa.
    - Pasar `tasks={tasks}` y `onCompleteTask={handleCompleteTask}` como props a `<TimelineTab />` (líneas 274-288).
    - En `TimelineTab` (firma ~644-658): agregar `tasks: FichaTask[]` y `onCompleteTask: (task: FichaTask, done: boolean) => void` a las props.
    - En `TimelineTab`, agregar una sección "Tareas" análoga a `showNotesManager` (líneas 751-776): mostrarla cuando `(timelineFilter === 'tareas' || timelineFilter === 'todo') && tasks.length > 0`. Listar cada tarea con un checkbox (`<input type="checkbox" checked={task.done} onChange={(e) => onCompleteTask(task, e.target.checked)} disabled={pending} aria-label={...}>`) + el título (tachado si `done`). Touch target accesible (min 44x44 o checkbox con label clickeable). Tokens CSS del shell CRM.

    Cuidar exhaustive-deps y variables sin uso. Verificar build.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -c "createTag\|onCompleteTask\|completeTask(" app/\(crm\)/admin/negocios/\[id\]/ficha-client.tsx</automated>
  </verify>
  <done>+Tag ya no nace disabled por catálogo vacío; el diálogo crea tags (createTag) además de asignar; existe una sección Tareas con checkbox por tarea que invoca completeTask via handleCompleteTask. tsc exit 0; build compila.</done>
</task>

<task type="auto">
  <name>Task 3: confirm-dialog.tsx — fix cosmético del badge de riesgo</name>
  <files>components/crm/confirm-dialog.tsx</files>
  <action>
    En `components/crm/confirm-dialog.tsx`, línea 231: cambiar la clase del header del DialogHeader de
    `flex items-center justify-between gap-2`
    por
    `flex items-center gap-2 pr-8`.

    Razón (diagnóstico UAT test 12): `justify-between` mandaba el RiskBadge al extremo derecho, a la misma coordenada que la X de cerrar (que es `absolute top-2 right-2` en dialog.tsx:68, fuera del flujo → no reserva espacio). Con `gap-2 pr-8` el badge queda pegado al título a la izquierda y el padding-right reserva el espacio de la X. NO tocar `components/ui/dialog.tsx` (la X es global a todos los dialogs).
  </action>
  <verify>
    <automated>grep -c "items-center gap-2 pr-8" components/crm/confirm-dialog.tsx</automated>
  </verify>
  <done>El header del ConfirmDialog usa `flex items-center gap-2 pr-8`; el RiskBadge queda al lado del título sin superponerse a la X. dialog.tsx intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server action | createTag/completeTask invocadas desde la ficha; la garantía es requireAdmin+zod server-side (Plan 01/03), la UI es refuerzo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 (createTag UI) | Tampering | input Crear tag | mitigate | createTag revalida requireAdmin()+createTagSchema.parse() server-side (04-SECURITY T-04-15 verificado); la UI solo invoca, no autoriza |
| T-04-11 (completeTask UI) | Tampering | checkbox completar tarea | mitigate | completeTask revalida requireAdmin()+completeTaskSchema.parse() server-side (04-SECURITY T-04-11 verificado); taskId es uuid validado |
| T-04-13 (tasks/tags read) | Information Disclosure | lectura de tasks con id | mitigate | service-role tras el layout guard (is_admin); solo id/title/done/completed_at/created_at cruzan al cliente (no sensible), espejo de la lectura de notas ya verificada |
| T-04-SC | Tampering (supply-chain) | npm installs | mitigate | cero dependencias nuevas; solo se editan 3 archivos existentes |
</threat_model>

<verification>
- `npx tsc --noEmit -p tsconfig.json` → exit 0.
- `npm run build` → compila; `/admin/negocios/[id]` presente.
- `grep -n "createTag" app/\(crm\)/admin/negocios/\[id\]/ficha-client.tsx` → import + invocación.
- `grep -n "completeTask(" app/\(crm\)/admin/negocios/\[id\]/ficha-client.tsx` → invocación en handleCompleteTask.
- El botón +Tag ya NO tiene `availableTags.length === 0` en su disabled.
- `grep -c "items-center gap-2 pr-8" components/crm/confirm-dialog.tsx` → 1 (y `justify-between` removido de esa línea).
</verification>

<success_criteria>
- +Tag desbloqueado: crea la primera tag y se puede asignar (desbloquea filtros test 7 y test 14).
- Cada tarea tiene checkbox de completar que invoca completeTask.
- El badge de riesgo del ConfirmDialog queda al lado del título.
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-07-SUMMARY.md` when done
</output>
