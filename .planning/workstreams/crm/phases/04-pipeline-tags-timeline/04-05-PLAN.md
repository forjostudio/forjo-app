---
phase: 04-pipeline-tags-timeline
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - supabase/migrations/035_crm_timeline_dedup.sql
autonomous: false
requirements: [TL-01]
user_setup:
  - service: supabase
    why: "Migraciones SQL numeradas se aplican A MANO y en orden (regla del repo); no se usa supabase db push"
    dashboard_config:
      - task: "Aplicar 035_crm_timeline_dedup.sql en el SQL Editor de Supabase (última aplicada: 034)"
        location: "Supabase Dashboard -> SQL Editor"
must_haves:
  truths:
    - "Cada nota y cada tarea aparece UNA sola vez en el timeline (no duplicada con su evento de auditoría)"
    - "La VIEW crm_timeline sigue siendo security_invoker=true (hereda la RLS admin-read)"
    - "audit_log queda intacto: el visor de auditoría sigue mostrando note.*/task.*"
  artifacts:
    - path: "supabase/migrations/035_crm_timeline_dedup.sql"
      provides: "create or replace view crm_timeline con WHERE que excluye los audit codes note.*/task.* de la rama audit_log"
      contains: "security_invoker = true"
  key_links:
    - from: "supabase/migrations/035_crm_timeline_dedup.sql"
      to: "supabase/migrations/034_crm_pipeline_tags_timeline.sql"
      via: "redefine la VIEW crm_timeline (rama audit_log) preservando las ramas notes/tasks de 034"
      pattern: "create or replace view public.crm_timeline"
---

<objective>
Cierre del gap de duplicación del timeline (UAT test 11, observación extra "cada nota/tarea aparece duplicada").

Diagnóstico del 04-UAT.md: cada nota/tarea genera 2 filas en `crm_timeline` — la fila de su tabla (rama `notes`/`tasks` de la VIEW, migr.034:229-251) Y la fila de `audit_log` que `logAudit` inserta (`createNote` _content-actions.ts:56-63, `createTask` 128-135), que entra por la rama `audit_log` de la VIEW (migr.034:217-225).

DECIDIDO (Opción A recomendada en el UAT): una migración nueva 035 que agregue `WHERE action NOT IN (...)` a la rama `audit_log` de la VIEW, excluyendo los codes `note.create/note.edit/note.delete/task.create/task.complete`. Mantiene `audit_log` intacto (el visor de auditoría sigue mostrando esos eventos), solo deja de mostrarlos por partida doble en el timeline (que ya los muestra vía sus propias ramas notes/tasks).

Purpose: el timeline deja de duplicar notas/tareas. Es prerequisito DB del Plan 04-07 (UI de notas/tareas en la ficha), pero NO comparte archivos con él → puede aplicarse en Wave 1 en paralelo con 04-04.
Output: migración 035 + checkpoint blocking-human para aplicarla a mano en Supabase + nota de regenerar schema.sql.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-UAT.md
@supabase/migrations/034_crm_pipeline_tags_timeline.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migración 035 — de-duplicar crm_timeline (excluir note.*/task.* de la rama audit_log)</name>
  <files>supabase/migrations/035_crm_timeline_dedup.sql</files>
  <action>
    Crear `supabase/migrations/035_crm_timeline_dedup.sql` con un `create or replace view public.crm_timeline with (security_invoker = true) as (...)` que reproduzca EXACTAMENTE la VIEW de 034 (las tres ramas UNION ALL: audit_log + notes + tasks, con el mismo shape de columnas kind/actor_type/title/body/occurred_at/metadata/business_id), con un ÚNICO cambio en la rama 1 (audit_log): agregar al final de ese SELECT un filtro que excluya los action codes que ya aparecen por sus propias ramas:

      where action not in ('note.create','note.edit','note.delete','task.create','task.complete')

    Copiar la rama audit_log, la rama notes y la rama tasks VERBATIM de 034 (migr.034:216-251) salvo ese WHERE. Conservar `with (security_invoker = true)` (CRÍTICO, T-04-10: sin el flag la VIEW correría security-definer y bypassaría la RLS admin-only).

    Cabecera de comentario (español, estilo del repo, explicando el POR QUÉ): la VIEW ya tenía ramas propias para notes y tasks; el logAudit de _content-actions.ts también escribe en audit_log con codes note.*/task.*, así que esos eventos entraban DOS veces al timeline (una por su tabla, otra por audit_log). Este WHERE deja audit_log intacto (el visor de auditoría sigue mostrándolos) y solo evita la doble aparición en el timeline. NO tocar la migración 034 (las migraciones son inmutables una vez aplicadas; 035 redefine la VIEW con create or replace).

    Repetir el `comment on view public.crm_timeline is '...'` con el texto de 034 (recordatorio de NUNCA quitar security_invoker) más una línea sobre la de-duplicación.

    No incluir ningún literal de la forma `action not in` en un comentario que un gate pueda negar-grepear; el WHERE va en el cuerpo SQL real, no en prosa. (Esto es SQL, no TS con `<action>` — sin restricción de comment-text de plan.)
  </action>
  <verify>
    <automated>test -f supabase/migrations/035_crm_timeline_dedup.sql && grep -c "security_invoker = true" supabase/migrations/035_crm_timeline_dedup.sql && grep -c "note.create" supabase/migrations/035_crm_timeline_dedup.sql</automated>
  </verify>
  <done>Existe 035_crm_timeline_dedup.sql con create or replace view crm_timeline, security_invoker=true presente, y el WHERE de la rama audit_log excluye los 5 codes note.*/task.*. Las ramas notes/tasks idénticas a 034.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2 [BLOCKING]: Aplicar migración 035 a mano en Supabase</name>
  <action>El operador aplica supabase/migrations/035_crm_timeline_dedup.sql a mano, en orden (última aplicada: 034), en el SQL Editor de Supabase (regla CLAUDE.md — el repo NO corre supabase db push automatizado). El Plan 04-07 (UI de notas/tareas) asume la VIEW de-duplicada para que la tarea/nota aparezca una sola vez. Ver los pasos de verificación abajo.</action>
  <what-built>La migración 035_crm_timeline_dedup.sql que redefine la VIEW crm_timeline para de-duplicar notas/tareas en el timeline. No puedo aplicarla yo: las migraciones de este repo se aplican A MANO en orden en el SQL Editor de Supabase (regla CLAUDE.md, NO supabase db push).</what-built>
  <how-to-verify>
    1. Abrir el SQL Editor de Supabase del proyecto.
    2. Verificar que la última migración aplicada fue 034 (la VIEW crm_timeline ya existe).
    3. Pegar y ejecutar el contenido completo de `supabase/migrations/035_crm_timeline_dedup.sql`.
    4. Confirmar que el `create or replace view` corrió sin error.
    5. (Opcional) Verificar de-dup: crear una nota en la ficha de un negocio y comprobar que aparece UNA sola vez en el tab Timeline (tras desplegar la UI del Plan 04-07).
    6. (No bloqueante) Regenerar `supabase/schema.sql` con `supabase db dump` cuando se pueda, para mantenerlo en sync con 035.
  </how-to-verify>
  <resume-signal>Escribí "aplicada" cuando 035 esté corrida en Supabase, o describí el error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller→VIEW | la VIEW crm_timeline expone audit_log/notes/tasks; debe correr con privilegios del caller para heredar la RLS admin-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure | crm_timeline VIEW | mitigate | 035 conserva `with (security_invoker = true)`: la VIEW redefinida sigue heredando la RLS admin-read de las tablas base (no se introduce security-definer). El WHERE solo filtra filas duplicadas, no amplía visibilidad. |
| T-04-SC | Tampering (supply-chain) | npm installs | mitigate | solo se crea un archivo .sql; cero dependencias |
</threat_model>

<verification>
- `grep -c "security_invoker = true" supabase/migrations/035_crm_timeline_dedup.sql` → 1 (flag conservado).
- La rama audit_log de 035 contiene el WHERE con los 5 codes note.*/task.*; las ramas notes y tasks son idénticas a 034.
- 034 NO se modificó (migración inmutable).
- Checkpoint blocking-human resuelto con "aplicada".
</verification>

<success_criteria>
- 035_crm_timeline_dedup.sql redefine la VIEW de-duplicando notas/tareas, conservando security_invoker.
- audit_log intacto (visor de auditoría sin cambios).
- Operador confirma la aplicación a mano en Supabase.
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-05-SUMMARY.md` when done
</output>
