---
phase: 04-pipeline-tags-timeline
plan: 04
type: tdd
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/(crm)/admin/_pipeline-actions.ts
  - app/(crm)/admin/_pipeline-actions.test.ts
  - app/(crm)/admin/_crm-actions.schemas.ts
  - lib/crm-timeline.ts
autonomous: true
requirements: [PIPE-02]
must_haves:
  truths:
    - "Existe una server action markWon que setea deals.status='won' y audita 'deal.won'"
    - "Al crear un deal con un email de un lead ya convertido (business_id no null), createDeal NO reusa ese lead: crea uno nuevo con el nombre del formulario"
    - "markWon respeta el contrato de 6 pasos (requireAdmin → zod.parse → service-role → leer-prev → mutar/throw → logAudit → revalidatePath)"
  artifacts:
    - path: "app/(crm)/admin/_pipeline-actions.ts"
      provides: "markWon (espejo de markLost) + createDeal con reuse-by-email acotado a leads activos"
      contains: "export async function markWon"
    - path: "app/(crm)/admin/_crm-actions.schemas.ts"
      provides: "markWonSchema (dealId uuid)"
      contains: "markWonSchema"
    - path: "lib/crm-timeline.ts"
      provides: "label 'deal.won' en ACTION_LABEL central"
      contains: "'deal.won'"
    - path: "app/(crm)/admin/_pipeline-actions.test.ts"
      provides: "tests RED→GREEN de markWon + reuse-by-email acotado"
      contains: "describe('markWon'"
  key_links:
    - from: "app/(crm)/admin/_pipeline-actions.ts"
      to: "app/(crm)/admin/_crm-actions.schemas.ts"
      via: "import markWonSchema y .parse() segunda línea"
      pattern: "markWonSchema\\.parse"
    - from: "app/(crm)/admin/_pipeline-actions.ts"
      to: "lib/crm-timeline.ts"
      via: "logAudit action 'deal.won' que ACTION_LABEL mapea"
      pattern: "'deal\\.won'"
---

<objective>
Cierre de gaps del UAT (test 5 backend + test 4b backend) en la capa de server actions del pipeline.

Resuelve dos diagnósticos del 04-UAT.md:
- **Gap test 5 (ganados):** no existe ninguna acción que marque un deal como ganado (`status='won'`); por eso `$ ganados` queda siempre en $0. DECIDIDO POR EL USUARIO: implementar una acción explícita `markWon` (espejo de `markLost`), NO acoplar `stage↔status`.
- **Gap test 4b backend (reuse de lead):** `createDeal` reusa cualquier lead con ese email — incluso ya convertido/perdido — y nunca actualiza el nombre. DECIDIDO POR EL USUARIO: NO reusar leads convertidos; acotar el query de reuse-by-email a leads activos (`business_id IS NULL`) y, si no hay lead activo, crear uno nuevo con `data.leadName`.

Purpose: dar el backbone de mutación + auditoría para que la UI (Plan 04-06) tenga `markWon` que invocar y un `createDeal` que no resucite leads viejos. La UI del header (`wonTotal` server-side) y el botón "Marcar ganado" llegan en 04-06 (Wave 2, depends_on 04-04).
Output: `markWon` + `markWonSchema` + label `deal.won` + `createDeal` con reuse acotado, todo cubierto por tests RED→GREEN en `_pipeline-actions.test.ts`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-UAT.md
@app/(crm)/admin/_pipeline-actions.ts
@app/(crm)/admin/_crm-actions.schemas.ts
@app/(crm)/admin/_pipeline-actions.test.ts
@lib/crm-timeline.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Schema + label + tests RED de markWon</name>
  <files>app/(crm)/admin/_crm-actions.schemas.ts, lib/crm-timeline.ts, app/(crm)/admin/_pipeline-actions.test.ts</files>
  <behavior>
    - markWon({ dealId }) con dealId uuid válido → setea status='won' en deals (updatedRows tiene una fila deals con status 'won') y audita action 'deal.won'.
    - markWon audita con risk 'bajo' y actorId del requireAdmin (igual patrón que markLost pero risk bajo: ganar no es destructivo).
    - requireAdmin lanza → markWon no muta ni audita (espejo del test existente de moveStage).
    - input inválido (dealId no-uuid) → rechaza antes de mutar (zod).
    - createDeal con email de un lead YA CONVERTIDO (lookup devuelve un lead con business_id no-null): NO reusa ese lead → inserta un lead nuevo (insertedRows tiene una fila leads) con name = data.leadName.
    - createDeal con email de un lead ACTIVO (business_id null): reusa el lead existente (NO inserta lead nuevo), inserta solo el deal.
  </behavior>
  <action>
    En `_crm-actions.schemas.ts`: agregar `markWonSchema = z.object({ dealId: id })` en la sección "Pipeline" (junto a moveStageSchema/markLostSchema), y exportar `export type MarkWonInput = z.infer<typeof markWonSchema>` junto a los demás tipos. Reusar el `id = z.uuid()` ya definido. NO redeclarar el listado de etapas.

    En `lib/crm-timeline.ts`: agregar la entrada `'deal.won': 'Marcó ganado',` al objeto `ACTION_LABEL` central, en el bloque "Phase 4 (pipeline / tags / contenido)" junto a `'deal.mark_lost'`. ACTION_LABEL es la single source of truth del copy (lo consumen auditoria-client y timeline-entry); NO crear un mapa local en ningún otro archivo.

    En `_pipeline-actions.test.ts`: agregar `describe('markWon', ...)` con los casos del bloque behavior, calcando los helpers de mock ya presentes (tableHandlers/updatedRows/auditCalls/requireAdminImpl). Agregar `markWon` al import de `./_pipeline-actions` (línea 72). Para los casos de createDeal-reuse, extender el `describe('createDeal')` existente con: (a) lead convertido NO reusado (el mock de `leads` devuelve `{ id, business_id: 'biz-1' }`, y se asserta que insertedRows incluye una fila `leads` con `name === 'Franco Vellani'`); (b) lead activo reusado (`leads` devuelve `{ id, business_id: null }`, se asserta que NO se insertó fila `leads`, solo `deals`). El mock actual no distingue lecturas por filtro; documentar en comentario que el handler devuelve la fila previa y la aserción es sobre la presencia/ausencia del insert de `leads`.

    Correr la suite: DEBE fallar en RED (markWon no existe en _pipeline-actions.ts; createDeal todavía reusa convertidos). Commit `test(04-04): failing tests for markWon + reuse-by-email acotado`.

    IMPORTANTE: no escribir aún la implementación de markWon ni tocar createDeal en _pipeline-actions.ts en esta tarea (eso es Task 2). Esta tarea es solo RED.
  </action>
  <verify>
    <automated>npx vitest run app/\(crm\)/admin/_pipeline-actions.test.ts 2>&1 | grep -E "(fail|markWon)" || true</automated>
  </verify>
  <done>La suite de _pipeline-actions.test.ts incluye describe('markWon') + casos de reuse, y FALLA (RED) porque markWon no existe y createDeal aún reusa leads convertidos. markWonSchema y el label 'deal.won' existen. Commit test() hecho.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Implementar markWon + acotar createDeal reuse-by-email (GREEN)</name>
  <files>app/(crm)/admin/_pipeline-actions.ts</files>
  <behavior>
    - Tras la implementación, todos los tests de Task 1 pasan (GREEN), y los 10 tests preexistentes de _pipeline-actions.test.ts siguen verdes.
  </behavior>
  <action>
    En `_pipeline-actions.ts`:

    1) Importar `markWonSchema` desde `./_crm-actions.schemas` (sumarlo al import existente que ya trae createDealSchema/moveStageSchema/markLostSchema/convertLeadSchema/linkLeadOnSignupSchema).

    2) Agregar la action `markWon` siguiendo el contrato de 6 pasos VERBATIM (espejo de markLost, ver _pipeline-actions.ts:117-138), ubicada justo después de markLost:
       - `const actor = await requireAdmin()` (primera línea)
       - `const data = markWonSchema.parse(input)` (segunda línea)
       - `const admin = createAdminClient()`
       - update `deals` set `status: 'won', updated_at: new Date().toISOString()` where `id = data.dealId`; si error → `throw new Error('update_failed')`
       - `logAudit` con `action: 'deal.won'`, `targetType: 'deal'`, `targetId: data.dealId`, `risk: 'bajo'` (ganar NO es destructivo, a diferencia de mark_lost que es medio)
       - `revalidatePath(PIPELINE_PATH)`
       NO setear stage (D-04: stage y status son ortogonales; markWon solo toca status). NO acoplar stage↔status.

    3) Acotar el reuse-by-email de `createDeal` (actualmente _pipeline-actions.ts:78-86). Cambiar el lookup para reusar SOLO leads activos (no convertidos): agregar `.is('business_id', null)` al query de reuse, de modo que un lead ya convertido (business_id no-null) NO se encuentre y se caiga a la rama de crear un lead nuevo con `data.leadName` (la rama `if (!leadId)` ya existente en 88-96 hace exactamente eso). Comentario explicando: "DECIDIDO POR EL USUARIO (gap 4b): no reusar leads convertidos — solo se reusa un lead activo (business_id null); si no hay, se crea uno nuevo con el nombre del formulario."

    Correr la suite: DEBE pasar (GREEN). Commit `feat(04-04): markWon action + reuse-by-email acotado a leads activos (PIPE-02)`.
  </action>
  <verify>
    <automated>npx vitest run app/\(crm\)/admin/_pipeline-actions.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>markWon implementada (status='won', audita 'deal.won', risk bajo, contrato de 6 pasos); createDeal reusa solo leads con business_id null. Toda la suite de _pipeline-actions.test.ts en verde; tsc --noEmit exit 0. Commit feat() hecho.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server action | input no confiable (dealId/email) cruza a una action que escribe tablas admin-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 (markWon) | Tampering | markWon server action | mitigate | requireAdmin() primera línea + markWonSchema.parse() segunda línea (calca el contrato verificado de moveStage/markLost en 04-SECURITY) |
| T-04-08 (markWon) | Repudiation | markWon audit | mitigate | logAudit action 'deal.won' con actorId del admin + targetId del deal; alimenta el visor/timeline |
| T-04-16 | Tampering | createDeal reuse-by-email | mitigate | el reuse se acota server-side a `business_id IS NULL`; un email de un lead convertido no permite re-vincular ni renombrar un lead ajeno (el lookup no lo encuentra → se crea uno nuevo). El cliente no controla qué lead se reusa. |
| T-04-SC | Tampering (supply-chain) | npm installs | mitigate | cero dependencias nuevas; solo se editan archivos TS existentes |
</threat_model>

<verification>
- `npx vitest run` → toda la suite verde (los 172 previos + los nuevos de markWon/reuse).
- `npx tsc --noEmit -p tsconfig.json` → exit 0.
- `grep -n "deal.won" lib/crm-timeline.ts app/\(crm\)/admin/_pipeline-actions.ts` → presente en ambos (label + audit).
- markWon NO contiene ningún `update` de `stage` (grep manual): D-04 stage/status ortogonales.
</verification>

<success_criteria>
- Existe `markWon` con contrato de 6 pasos, audita 'deal.won', sin tocar stage.
- `createDeal` reusa solo leads activos (business_id null); un lead convertido genera un lead nuevo con el nombre del formulario.
- `markWonSchema` + label `deal.won` en su single source.
- RED→GREEN documentado en dos commits (test() luego feat()).
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/04-pipeline-tags-timeline/04-04-SUMMARY.md` when done
</output>
