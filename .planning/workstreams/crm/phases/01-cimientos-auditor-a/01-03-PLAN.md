---
phase: 01-cimientos-auditor-a
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - components/crm/crm-sidebar.tsx
  - components/crm/crm-topbar.tsx
  - components/crm/risk-badge.tsx
  - components/crm/crm-toaster.tsx
  - app/(crm)/layout.tsx
  - app/(crm)/admin/auditoria/page.tsx
autonomous: false
requirements: [FND-04, FND-02]
must_haves:
  truths:
    - "El CRM se muestra con su shell propio: CrmSidebar agrupado (OPERACIÓN/VENTAS/INSIGHTS/CUENTA), top bar y área de contenido en dark"
    - "El item de nav activo se marca con barra de acento amarillo + fondo elevado (no un peso de fuente más pesado)"
    - "Existe el RiskBadge con variantes Alto (dot rojo) / Medio (pill amarillo) / Bajo (dot neutro)"
    - "La página /admin/auditoria lista filas reales de audit_log con columnas QUIÉN/ACCIÓN/NEGOCIO/DETALLE/MOTIVO/CUÁNDO/RIESGO y empty state"
    - "Los toasts del CRM salen en tema dark (Toaster scopeado), no en el light global"
  artifacts:
    - path: "components/crm/crm-sidebar.tsx"
      provides: "Sidebar agrupado propio del CRM (NUEVO, no reusa el de dashboard)"
      min_lines: 40
    - path: "components/crm/risk-badge.tsx"
      provides: "RiskBadge con variantes alto/medio/bajo (cva CRM, no edita el badge compartido)"
      exports: ["RiskBadge"]
    - path: "app/(crm)/admin/auditoria/page.tsx"
      provides: "Visor read-only de audit_log que ejercita tabla + RiskBadge + tabs de filtro"
      contains: "audit_log"
  key_links:
    - from: "app/(crm)/admin/auditoria/page.tsx"
      to: "supabase audit_log"
      via: "select admin-only sobre audit_log (RLS, sesión del operador) ordenado por created_at desc"
      pattern: "from\\('audit_log'\\)"
    - from: "app/(crm)/layout.tsx"
      to: "components/crm/crm-sidebar.tsx"
      via: "el layout monta CrmSidebar + topbar + Toaster scopeado dentro del wrapper crm-shell"
      pattern: "CrmSidebar"
  prohibitions:
    - "NO reusar components/dashboard/sidebar.tsx (es flat y business-scoped via buildNav) — crear CrmSidebar propio"
    - "NO editar el components/ui/badge.tsx compartido — las variantes de riesgo van en components/crm/"
    - "El visor de auditoría usa la sesión del operador (RLS admin-read), NO createAdminClient (no exponer service-role en lectura)"
    - "Los badges de dominio peligro usan --crm-danger, nunca el token --destructive"
---

<objective>
Entregar el shell de UI del CRM (FND-04): el `CrmSidebar` agrupado nuevo, la top bar, un `Toaster` scopeado dark, el `RiskBadge` con sus tres niveles, y montarlos en el layout del CRM. Además, una página visor read-only de auditoría en `/admin/auditoria` que ejercita el contrato de tabla + RiskBadge + tabs de filtro con filas reales de `audit_log` (cierra FND-02 del lado de lectura y evita shippear componentes muertos).

Purpose: FND-04 pide un shell propio anclado al tema Forjo default en dark (D6 — dark forzado y scopeado al CRM, acentos amarillo/azul/un solo rojo) con sidebar agrupado, cards, tablas, badges, tabs, dialog y toasts. El sidebar del dashboard no sirve (es plano y business-scoped). El visor de auditoría hace visible el log que escribe `logAudit` con la RLS admin-only-read de D5, y le da a Phases 2+ una tabla probada.
Output: `components/crm/crm-sidebar.tsx`, `crm-topbar.tsx`, `risk-badge.tsx`, `crm-toaster.tsx`, montaje en `app/(crm)/layout.tsx`, `app/(crm)/admin/auditoria/page.tsx`.
</objective>

<execution_context>
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/franc/Desktop/Forjo Studio/forjo-app/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/crm/phases/01-cimientos-auditor-a/01-UI-SPEC.md
@.planning/workstreams/crm/phases/01-cimientos-auditor-a/01-RESEARCH.md
@.planning/workstreams/crm/phases/01-cimientos-auditor-a/01-CONTEXT.md

# Layout del CRM ya creado (plan 01-02) — se monta el shell adentro
@app/(crm)/layout.tsx

# Patrones del codebase a calcar / reusar
@components/dashboard/sidebar.tsx
@components/ui/badge.tsx
@components/ui/table.tsx
@components/ui/tabs.tsx
@components/ui/sonner.tsx
@lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CrmSidebar agrupado + top bar + Toaster dark scopeado, montados en el layout</name>
  <files>components/crm/crm-sidebar.tsx, components/crm/crm-topbar.tsx, components/crm/crm-toaster.tsx, app/(crm)/layout.tsx</files>
  <read_first>
    - components/dashboard/sidebar.tsx (patrón de sidebar + drawer mobile + footer de usuario — pero NO reusar; el CRM es agrupado y no business-scoped)
    - components/ui/sonner.tsx (Toaster existente; props theme)
    - 01-UI-SPEC.md §"CrmSidebar", §"Top bar", §"Toasts", §"Theming anchor", §"Typography", §"Spacing Scale", §"Accessibility & Interaction"
    - 01-RESEARCH.md §"Sidebar agrupado" (no existe uno; crear propio)
  </read_first>
  <action>
    Crear `components/crm/crm-sidebar.tsx` (client component): sidebar agrupado NUEVO (NO importar ni reusar `components/dashboard/sidebar.tsx`, que es flat + business-scoped vía `buildNav(business)`). Surface `bg-sidebar`, borde derecho `--sidebar-border`, `w-60` desktop / drawer en mobile (< lg) calcando el patrón mobile del sidebar de dashboard. Brand header (`p-5`, borde inferior): wordmark "forjo studio" (heading, `forjo` bold) + caption mono "CONSOLA · OPERACIÓN". Nav agrupado con labels de sección mono uppercase `tracking-wider text-muted-foreground` sobre los grupos: OPERACIÓN (Dashboard, Bandeja), VENTAS (Pipeline, Negocios), INSIGHTS (Reportes, Auditoría), CUENTA (Planes y precios, Ajustes). En Phase 1 SOLO Dashboard (→ /admin) y Auditoría (→ /admin/auditoria) enrutan a páginas reales; el resto son placeholders navegables o items deshabilitados con tag "PRONTO" (discreción — calcar el styling del mock). Item de nav: `flex items-center gap-3 px-2 py-2 rounded-lg text-sm`, default `text-muted-foreground hover:bg-secondary hover:text-foreground`; ACTIVO = fondo elevado `bg-secondary` + `text-foreground` + barra de acento amarillo izquierda de 2px (`border-l-2` en `--primary`) + icono tintado amarillo — el estado activo se diferencia por fondo + barra + color, NUNCA por un peso de fuente más pesado (solo 400/700 en todo el sistema). Icono lucide por item (16px). Badge numérico opcional alineado a la derecha (mono, muted). Footer (`p-2`, borde superior): bloque de usuario (iniciales en cuadrado redondeado, nombre + caption mono "Operador · dueño") + botón icono de logout. Estados requeridos en cada item interactivo: default / hover / focus-visible (ring amarillo 3px) / active. Sin afordances solo-hover (mobile no tiene hover). Detectar la ruta activa con `usePathname()`.

    Crear `components/crm/crm-topbar.tsx`: título de página (heading) + breadcrumb mono; input de búsqueda global ("Buscar en todo…") con chip de hint "⌘K" e icono de búsqueda a la izquierda; icono de notificaciones (bell) con badge de count que usa el ÚNICO rojo (`--crm-danger`). En Phase 1 la búsqueda puede ser un input no funcional (las pantallas con datos llegan en Phases 2+); documentar como placeholder.

    Crear `components/crm/crm-toaster.tsx`: un Toaster scopeado al CRM en tema dark (`theme="dark"`), envolviendo o reusando el `Toaster` de `components/ui/sonner.tsx`. Resuelve A4 del research: como el CRM fuerza dark localmente pero next-themes global sigue en light, montar este Toaster dentro del layout del CRM hace que los toasts del CRM salgan dark. Posición top-right desktop.

    Editar `app/(crm)/layout.tsx` (creado en 01-02) para montar el shell DENTRO del wrapper `<div className="dark crm-shell">`: `<CrmSidebar />` + un `<main>` con `<CrmTopbar />` arriba y `{children}` en el área de contenido + `<CrmToaster />`. Respetar el offset de contenido por el sidebar (`lg:pl-60`) calcando el dashboard. Mantener el guard server-side intacto (no moverlo ni envolverlo en try/catch).
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && grep -c "buildNav" components/crm/crm-sidebar.tsx | grep -q "^0$" && grep -c "CrmSidebar" "app/(crm)/layout.tsx" | grep -qv "^0$" && grep -c 'theme="dark"' components/crm/crm-toaster.tsx | grep -qv "^0$" && echo "CrmSidebar is independent (no buildNav), mounted in layout, dark toaster scoped"</automated>
    <human-check>En /admin (como operador admin): el sidebar agrupado se ve con las 4 secciones, el item activo tiene la barra amarilla; la top bar muestra título + búsqueda + bell; en mobile el sidebar colapsa a drawer; un toast de prueba sale en dark.</human-check>
  </verify>
  <acceptance_criteria>
    - `components/crm/crm-sidebar.tsx` es nuevo y NO usa `buildNav`/`resolveVertical` (no business-scoped).
    - Nav agrupado en 4 secciones; item activo con barra amarilla + fondo elevado + color (no peso de fuente).
    - `crm-topbar.tsx` con título, breadcrumb mono, búsqueda con hint ⌘K, bell con badge rojo.
    - `crm-toaster.tsx` monta un Toaster `theme="dark"`.
    - El layout monta CrmSidebar + topbar + children + Toaster dentro de `dark crm-shell`, sin tocar el guard.
    - Estados focus-visible (ring amarillo) presentes; sidebar colapsa a drawer en mobile.
  </acceptance_criteria>
  <done>El CRM tiene su shell propio (sidebar agrupado, top bar, área de contenido, toasts dark) en dark con acento amarillo, sin reusar el sidebar business-scoped del dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: RiskBadge (variantes alto/medio/bajo) + visor read-only de auditoría</name>
  <files>components/crm/risk-badge.tsx, app/(crm)/admin/auditoria/page.tsx</files>
  <read_first>
    - components/ui/badge.tsx (variantes existentes; NO editarlo)
    - components/ui/table.tsx, components/ui/tabs.tsx (reusar verbatim)
    - lib/supabase/server.ts (createClient para leer con la sesión del operador, RLS admin-read)
    - 01-UI-SPEC.md §"Badge — Riesgo", §"Table — audit-log look", §"Tabs", §"Copywriting Contract" (empty states), §"Audit viewer decision"
    - 01-RESEARCH.md §"Schema SQL" (columnas de audit_log)
  </read_first>
  <action>
    Crear `components/crm/risk-badge.tsx` con un componente `RiskBadge` que recibe `risk: 'alto' | 'medio' | 'bajo'` y renderiza la variante correcta SIN editar el `components/ui/badge.tsx` compartido (usar `cn`/composición o un `riskBadgeVariants` cva local en `components/crm/`). Variantes (UI-SPEC §"Badge — Riesgo"): Alto = pill oscuro (`bg-secondary`/outline), `text-foreground`, dot rojo a la izquierda usando `--crm-danger` (NO `--destructive`); Medio = pill amarillo relleno (`bg-primary text-primary-foreground` ink); Bajo = pill oscuro, `text-muted-foreground`, dot neutro/muted. Estos badges de dominio peligro referencian `--crm-danger`, nunca `--destructive` (§12).

    Crear `app/(crm)/admin/auditoria/page.tsx` (Server Component): leer `audit_log` con `createClient()` (sesión del operador → la RLS admin-read del plan 01-01 garantiza que solo un admin ve filas; NO usar `createAdminClient` para LEER — no exponer service-role en lectura). Query: `from('audit_log').select(...).order('created_at', { ascending: false })`, con un límite razonable (ej. 100). Renderizar la tabla con el look del UI-SPEC §"Table": headers mono uppercase 12px (`QUIÉN / ACCIÓN / NEGOCIO / DETALLE / MOTIVO / CUÁNDO / RIESGO`); celda QUIÉN con actor (Operador / Sistema); ACCIÓN en label; DETALLE/MOTIVO en muted/mono donde sea transición de valor; CUÁNDO en timestamp mono muted (formato `Hoy · 13:22` / `14 jun · 09:12` con date-fns + zona America/Argentina/Buenos_Aires); RIESGO con `<RiskBadge>` alineado a la derecha. Arriba de la tabla: input de búsqueda ("Buscar acción, negocio…") + filter tabs (Todos / Altos / Medios / Bajos, variante default, activo amarillo) + botón outline "Exportar log" (placeholder en Phase 1). Empty states (copy exacto del UI-SPEC §"Copywriting Contract"): sin filas → heading "Todavía no hay acciones registradas" + body explicativo; filtrado sin match → "Ninguna acción coincide con este filtro." + acción "Limpiar filtros". El filtrado por tab/búsqueda puede ser client-side sobre las filas cargadas (separar un client component si hace falta interactividad). NO usar "SMS" en ningún dato de ejemplo — el add-on es "Recordatorios WhatsApp" (fix brief §11).

    NOTA de scope: el visor es read-only y muestra lo que `logAudit` haya escrito; en Phase 1 puede estar vacío hasta que corran acciones (Phases 2+). Eso es esperado — el valor es el contrato de tabla/badge/tabs probado.
  </action>
  <verify>
    <automated>cd "C:/Users/franc/Desktop/Forjo Studio/forjo-app" && grep -c "from('audit_log')" "app/(crm)/admin/auditoria/page.tsx" | grep -qv "^0$" && grep -c "createAdminClient" "app/(crm)/admin/auditoria/page.tsx" | grep -q "^0$" && grep -c "crm-danger" components/crm/risk-badge.tsx | grep -qv "^0$" && grep -v '^\s*//' "app/(crm)/admin/auditoria/page.tsx" | grep -ci "SMS" | grep -q "^0$" && echo "audit viewer reads audit_log with session (no service-role), RiskBadge uses crm-danger, no SMS literal in code"</automated>
    <human-check>/admin/auditoria muestra la tabla con headers QUIÉN/ACCIÓN/…/RIESGO; con log vacío aparece el empty state "Todavía no hay acciones registradas"; las tabs Todos/Altos/Medios/Bajos filtran; el activo está en amarillo.</human-check>
  </verify>
  <acceptance_criteria>
    - `RiskBadge` renderiza alto (dot rojo --crm-danger) / medio (pill amarillo) / bajo (dot neutro) sin editar el badge compartido.
    - `/admin/auditoria` lee `audit_log` con la sesión del operador (RLS), NO con createAdminClient.
    - Tabla con las 7 columnas del UI-SPEC, timestamps mono, RiskBadge en RIESGO, tabs de filtro y empty states con el copy exacto.
    - Cero ocurrencias de "SMS" (usar WhatsApp).
    - El visor typechecks y compila.
  </acceptance_criteria>
  <done>Existe un visor read-only de auditoría que ejercita tabla + RiskBadge + tabs con filas reales de audit_log leídas con la sesión admin (RLS), y RiskBadge queda disponible como primitiva para Phases 2+.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (operador) → audit_log (lectura) | El visor lee con la anon key + sesión; la RLS admin-read es la que filtra, no el cliente |
| visor de auditoría → service-role | Leer con createAdminClient expondría todo el log sin pasar por RLS — debe evitarse |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure | visor /admin/auditoria | mitigate | Lee `audit_log` con `createClient()` (sesión del operador) → la policy RLS admin-read (plan 01-01) es la garantía; NO usa createAdminClient para leer (no bypassa RLS en lectura). Un no-admin igual no llega: el layout lo redirige. |
| T-01-10 | Elevation of Privilege | acceso al shell sin admin | mitigate | El shell vive bajo `app/(crm)/layout.tsx` cuyo guard server-side (plan 01-02) ya bloquea no-admins antes del render del sidebar/visor. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 1 no instala paquetes nuevos (RESEARCH §"Package Legitimacy Audit": no aplica). |
</threat_model>

<verification>
- CrmSidebar agrupado nuevo (sin buildNav), montado en el layout; item activo con barra amarilla.
- Toaster del CRM en dark; top bar con búsqueda + bell rojo.
- RiskBadge con las tres variantes usando --crm-danger (no --destructive).
- /admin/auditoria lee audit_log con sesión (no service-role), tabla con 7 columnas + tabs + empty states + sin "SMS".
- `npm run lint` y `npx tsc --noEmit` sin errores nuevos.
</verification>

<success_criteria>
- El CRM se muestra con su shell propio (sidebar agrupado, cards, tablas, badges, tabs, toasts) anclado al tema Forjo default en dark con acentos amarillo/azul/rojo (FND-04).
- El visor de auditoría hace visible el audit_log con el contrato de tabla/RiskBadge/tabs probado para Phases 2+ (FND-02 lectura).
- Ninguna lectura del log usa service-role; ningún dato menciona "SMS".
</success_criteria>

<output>
Create `.planning/workstreams/crm/phases/01-cimientos-auditor-a/01-03-SUMMARY.md` when done.
</output>
